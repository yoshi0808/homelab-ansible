---
# Deploy recovery-exec (Codex runner) to ansy or quory,
# and set up SSH landing accounts on target nodes (authy, monnie).
#
# Development (ansy):
#   ansible-playbook playbooks/recovery_exec_setup.yml -l ansy
#
# Production (quory):
#   ansible-playbook playbooks/recovery_exec_setup.yml -l quory
#
# Skip target node setup (e.g. when authy/monnie is unreachable):
#   ansible-playbook playbooks/recovery_exec_setup.yml -l ansy \
#     -e recovery_exec_setup_targets=false
#
# Notes:
# - vault_codex_openai_api_key must be added to inventories/vars/slack.yml
# - Run this before recovery_io_setup.yml (creates codex-exec-wrapper)
# - After first run, verify key permissions:
#     ansible ansy -m command -a "sudo -u nobody stat /home/recovery-exec/.ssh/id_recovery_investigate"
#   Expected: Permission denied
#
# tester-gate: risk-accepted — tester_mode/tester_gate を廃止。--check の有無に
# かかわらず常に本実行する。理由: recovery-exec ユーザー作成・SSH鍵生成(creates:
# で既存キーがあれば元々no-op)・wrapperスクリプト配置・sudoers・ACLのみで、
# サービス再起動を一切伴わない(recovery-exec自体はデーモンではない)ため、
# Yoshinobuが安全側と判断(2026-07-06)。
#
# 2026-07-11追記: 上記判断は「本番影響ゼロ/軽微」を単一ホストへの影響のみで
# 評価しており、target(authy/monnie/pve1/pve2)のauthorized_keysを実行元
# ホストの鍵で排他上書きする横方向の影響を見落としていた。2026-07-08、
# `-l ansy`の通常実行(recovery_exec_setup_targets既定true)がtargetsを
# ansy鍵で上書きし、quoryの本番investigate/action SSH経路を3日間切断する
# インシデントが発生(詳細:
# docs/ai/reviews/autonomous_recovery/2026-07-11_002_ssh_key_investigation.md)。
# 再発防止として、target側への鍵配布は正規配布元quoryからの実行時のみ許可する
# assertをrecovery_execロールに追加した(2026-07-11、2ラウンドのレビューを経て
# 確定: 許可主体をcontrol_nodesグループ所属にすると将来のグループ変更で再発し
# 得るため却下、変数化(recovery_exec_setup_authoritative_host)も-eで上書き
# 可能なため却下し、最終的にtasks/main.yml内でquoryをリテラル固定した。詳細:
# docs/ai/reviews/autonomous_recovery/2026-07-11_009_implement.md)。
#
# 保証範囲の補足: このガードが防ぐのは target authorized_keys の排他的
# 横方向上書きのみである。recovery-execユーザー作成・SSH鍵生成・wrapper配置・
# sudoers・ACLといった単一ホスト内のローカル変更は、guardの前段で従来どおり
# 実行される(ガードはtargetsへの配布直前にのみ効く)。この限定的なローカル
# 変更こそが元々のrisk-accepted判断(2026-07-06)の対象であり、そこにサービス
# 再起動を伴わないという前提は変わらない。両者を合わせてrisk-acceptedの分類は
# 継続する。詳細は docs/ai/policies/ansible_test_safety_policy.md。

- name: Deploy recovery-exec (Codex runner + SSH key generation)
  hosts: dev_nodes:control_nodes
  gather_facts: true
  become: true
  vars:
    # skip_notifications: "{{ ansible_check_mode }}" removed 2026-07-31 (R2,
    # docs/ai/reviews/check_mode_semantics/2026-07-31_001_requirement.md) —
    # R1 below now refuses --check outright, so this line's only effect
    # (skip_notifications=true under --check) never had a chance to run.
    recovery_exec_group_members:
      - yoshi
  pre_tasks:
    - name: "[migration] --check has no dry-run here — refuse and stop before any changes"
      ansible.builtin.assert:
        that: not (ansible_check_mode | bool)
        fail_msg: >-
          This playbook (tester-gate: risk-accepted) has no --check dry-run:
          every task always applies for real, so --check would look like a
          simulation while actually making the change (see
          docs/ai/memory/incidents/2026-07-31_subagent-unintended-deploy-risk-accepted-check.md).
          Re-run without --check to apply for real.
          See docs/ai/reviews/check_mode_semantics/2026-07-31_001_requirement.md.
        quiet: true

    - name: "[migration] tester_mode is deprecated for this playbook — it always runs for real"
      ansible.builtin.assert:
        that: not (tester_mode | default(false) | bool)
        fail_msg: >-
          This playbook no longer supports -e tester_mode=true and has no
          dry-run mode (--check included): recovery-exec setup always runs
          for real. See docs/ai/policies/ansible_test_safety_policy.md.
        quiet: true

  tasks:
    - name: Run recovery_exec role
      ansible.builtin.import_role:
        name: recovery_exec
      check_mode: false
