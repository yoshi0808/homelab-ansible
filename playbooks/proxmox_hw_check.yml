---
# tester-gate: safe-readonly — read-only診断（冪等な収集script配置とlocal report保存を含む）。通知経路はない
- name: Proxmox hardware check
  hosts: proxmox
  gather_facts: true
  become: true

  roles:
    - proxmox_hw_check
