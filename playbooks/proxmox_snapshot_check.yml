---
# tester-gate: safe-readonly — read-only 収集のみ(冪等な収集スクリプト配置とローカルレポート出力を含む)。通知は common_slack の tester_mode ガードで抑止される
- name: Proxmox snapshot check
  hosts: proxmox
  gather_facts: true
  become: true

  roles:
    - proxmox_snapshot_check
