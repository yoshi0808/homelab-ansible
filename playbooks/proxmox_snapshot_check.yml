---
# tester-gate: safe-readonly — read-only診断（冪等な収集script配置とlocal report保存を含む）。異常時通知はcaller側に抑止guardがなく、common_slack内のtester_mode/skip_notificationsで抑止する
- name: Proxmox snapshot check
  hosts: proxmox
  gather_facts: true
  become: true

  roles:
    - proxmox_snapshot_check
