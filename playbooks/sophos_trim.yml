---
# tester-gate: gated-in-role — ゲートは roles/sophos_trim/tasks/main.yml 先頭の tester_gate include
- name: Sophos Firewall SSD Trim
  hosts: sophos
  gather_facts: false
  become: false
  vars:
    skip_notifications: "{{ tester_mode | default(false) | bool }}"

  roles:
    - sophos_trim
