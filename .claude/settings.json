{
  "permissions": {
    "allow": [
      "Edit(docs/ai/reviews/**)",
      "Edit(prompts/**)",
      "Edit(playbooks/**)",
      "Edit(roles/**)",
      "Edit(scripts/**)",
      "Edit(./**)",
      "Write(docs/ai/reviews/**)",
      "Write(prompts/**)",
      "Write(playbooks/**)",
      "Write(roles/**)",
      "Write(scripts/**)",
      "Write(./**)",
      "Bash(bash scripts/check-tester-gate.sh*)"
    ],
    "deny": [
      "Bash(git commit*)",
      "Bash(git push*)"
    ],
    "defaultMode": "auto"
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PROJECT_DIR:-.}/scripts/session-context.sh\"",
            "timeout": 10,
            "statusMessage": "現在地(docs/ai/status.md)を読み込み中"
          }
        ]
      }
    ]
  },
  "autoMode": {
    "classifyAllShell": true,
    "soft_deny": [
      "$defaults",
      "Non-idempotent operations against the Proxmox nodes (pve1, pve2), the Sophos firewall (sophos-fw), or UniFi equipment (cloudkey, access points, switches). This covers package/patch apply, reboot, shutdown, VM or CT migration, HA failover, firewall rule changes, inventory changes, service stop/restart/disable, and configuration writes. Read-only checks are NOT covered: ansible-playbook --syntax-check, --check runs via scripts/safe-ansible-check.sh, ansible-lint, healthcheck and dry-run playbooks that only collect state, and decoy-inventory runs that never contact these hosts should all be allowed.",
      "Irrecoverable deletion of production data with no backup on a real target host, such as wiping a datastore, log store, or state directory. Deletion of the agent's own scratch files under the repository working tree or /tmp on ansy is NOT covered and should be allowed."
    ],
    "hard_deny": [
      "$defaults",
      "git commit and git push in any repository, including invocations by absolute path such as /usr/bin/git. Commits and pushes are always performed by Yoshinobu, never by an agent. Preparing a commit (git add, git diff, drafting a message) is allowed."
    ],
    "environment": [
      "$defaults",
      "This is homelab-ansible: Ansible playbooks and roles managing a home lab. Real target hosts are the Proxmox cluster (pve1, pve2), the QDevice/execution host quory, the monitoring VM monnie, the RADIUS host authy, the Sophos firewall sophos-fw, and UniFi network gear.",
      "Agents run on the ansy VM. Operations confined to the repository working tree or /tmp on ansy are local and reversible, including deleting scratch files the agent created itself.",
      "The Coordinator (the main session) judges whether a planned real-host operation falls within the scope Yoshinobu approved when the work was requested, per docs/ai/roles/coordinator.md. Routine verification is expected and should not be blocked."
    ]
  }
}
