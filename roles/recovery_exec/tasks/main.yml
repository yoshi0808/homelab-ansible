---
# recovery_exec: Codex runner for autonomous recovery pipeline (§4.2)
#
# Creates recovery-exec user, generates three shared SSH keys
# (investigate / action / PVE investigate), deploys wrapper scripts,
# Codex config and AGENTS.md.
# Calls target_setup.yml to register keys and deploy scripts on the target
# nodes: authy / monnie (investigate + action, recovery_exec_targets) and
# pve1 / pve2 (investigate only, separate key, recovery_exec_pve_targets).
#
# Authentication: Codex uses browser OAuth. The token is stored in
# /home/recovery-exec/.codex/ (managed by Codex itself). No API key required.

# ---------------------------------------------------------------------------
# Service user (home at /home/recovery-exec for SSH config + Codex config)
# ---------------------------------------------------------------------------

- name: Create recovery-exec user (restricted shell, home in /home)
  ansible.builtin.user:
    name: "{{ recovery_exec_user }}"
    shell: /bin/sh
    create_home: true
    home: "{{ recovery_exec_home }}"
    password: "!"
    comment: "Recovery Codex runner service account"
  become: true
  when: not ansible_check_mode
  tags: [destructive]

# ---------------------------------------------------------------------------
# SSH key directories
# ---------------------------------------------------------------------------

- name: Create .ssh directory for recovery-exec
  ansible.builtin.file:
    path: "{{ recovery_exec_ssh_dir }}"
    state: directory
    owner: "{{ recovery_exec_user }}"
    group: "{{ recovery_exec_user }}"
    mode: "0700"
  become: true
  when: not ansible_check_mode
  tags: [destructive]

# check-mode-native (TS-015): the three ssh-keygen tasks and the chmod that
# follows are one interdependent chain, not four independent destructive
# tasks — they are also contiguous in this file (only comments separate
# them), unlike the slurp -> authorized_keys dependency in target_setup.yml
# (six independent tasks sit between them there; see the note on that file).
# "Lock down SSH key permissions" only means something if the three
# ssh-keygen tasks actually ran for real: on a fresh host under --check,
# with each key-gen task gated individually, the .pub/private key files
# this task chmods would not exist, and ansible.builtin.file's implicit
# state=file fails outright when the path is absent (verified in the
# batchB2 implement record's decoy tests). Gated once at the block level
# instead of repeating `when: not ansible_check_mode` on each task — same
# shape as roles/recovery_push/tasks/sender_setup.yml's
# "Generate and read push SSH key" block, see
# docs/ai/reviews/check_mode_semantics/2026-07-31_010_round2_batchB1_implement.md
# §2.2 and docs/ai/reviews/check_mode_semantics/2026-07-31_013_round2_batchB2_implement.md.
- name: Generate and lock down recovery-exec SSH keys (destructive; TS-015 chain)
  when: not ansible_check_mode
  tags: [destructive]
  block:
    - name: Generate investigation SSH key (shared, parameterized forced command)
      ansible.builtin.command:
        cmd: >-
          ssh-keygen -t ed25519 -N ''
          -C "recovery-investigate"
          -f {{ recovery_exec_investigate_key }}
        creates: "{{ recovery_exec_investigate_key }}"
      become: true

    - name: Generate action SSH key (shared, fixed forced commands)
      ansible.builtin.command:
        cmd: >-
          ssh-keygen -t ed25519 -N ''
          -C "recovery-action"
          -f {{ recovery_exec_action_key }}
        creates: "{{ recovery_exec_action_key }}"
      become: true

    - name: Generate PVE investigation SSH key (shared across pve1/pve2, read-only Proxmox investigation only)
      ansible.builtin.command:
        cmd: >-
          ssh-keygen -t ed25519 -N ''
          -C "recovery-investigate-pve"
          -f {{ recovery_exec_investigate_pve_key }}
        creates: "{{ recovery_exec_investigate_pve_key }}"
      become: true

    - name: Lock down SSH key permissions
      ansible.builtin.file:
        path: "{{ item }}"
        owner: "{{ recovery_exec_user }}"
        group: "{{ recovery_exec_user }}"
        mode: "0600"
      loop:
        - "{{ recovery_exec_investigate_key }}"
        - "{{ recovery_exec_action_key }}"
        - "{{ recovery_exec_investigate_pve_key }}"
      become: true

# ---------------------------------------------------------------------------
# Codex configuration directory (OAuth token stored here by Codex itself)
# ---------------------------------------------------------------------------

- name: Create .codex directory (OAuth token stored by Codex)
  ansible.builtin.file:
    path: "{{ recovery_exec_codex_dir }}"
    state: directory
    owner: "{{ recovery_exec_user }}"
    group: "{{ recovery_exec_user }}"
    mode: "0700"
  become: true
  when: not ansible_check_mode
  tags: [destructive]

- name: Deploy Codex config.toml (model + sandbox)
  ansible.builtin.template:
    src: codex-config.toml.j2
    dest: "{{ recovery_exec_codex_dir }}/config.toml"
    owner: "{{ recovery_exec_user }}"
    group: "{{ recovery_exec_user }}"
    mode: "0600"
  become: true
  when: not ansible_check_mode
  tags: [destructive]

# ---------------------------------------------------------------------------
# Codex execution wrapper (restricts args to "exec" subcommand only)
# ---------------------------------------------------------------------------

- name: Deploy codex-exec-wrapper
  ansible.builtin.template:
    src: codex-exec-wrapper.j2
    dest: "{{ recovery_exec_codex_wrapper }}"
    owner: "{{ recovery_exec_user }}"
    group: "{{ recovery_exec_user }}"
    mode: "0500"
  become: true
  when: not ansible_check_mode
  tags: [destructive]

# ---------------------------------------------------------------------------
# Workspace + AGENTS.md (Codex reads this for tool documentation)
# ---------------------------------------------------------------------------

- name: Create Codex workspace directory
  ansible.builtin.file:
    path: "{{ recovery_exec_workspace }}"
    state: directory
    owner: "{{ recovery_exec_user }}"
    group: "{{ recovery_exec_user }}"
    mode: "0770"
  become: true
  when: not ansible_check_mode
  tags: [destructive]

- name: Add group members to recovery-exec group (for workspace read/write)
  ansible.builtin.user:
    name: "{{ item }}"
    groups: "{{ recovery_exec_user }}"
    append: true
  loop: "{{ recovery_exec_group_members }}"
  become: true
  when: not ansible_check_mode
  tags: [destructive]

- name: Deploy AGENTS.md (tool documentation for Codex)
  ansible.builtin.template:
    src: AGENTS.md.j2
    dest: "{{ recovery_exec_workspace }}/AGENTS.md"
    owner: "{{ recovery_exec_user }}"
    group: "{{ recovery_exec_user }}"
    mode: "0640"
  become: true
  when: not ansible_check_mode
  tags: [destructive]

# ---------------------------------------------------------------------------
# Local wrapper scripts: homelab-investigate-* and homelab-recover-*
# ---------------------------------------------------------------------------

- name: Deploy homelab-investigate-* wrapper scripts
  ansible.builtin.template:
    src: homelab-investigate.sh.j2
    dest: "/usr/local/bin/homelab-investigate-{{ target.name }}"
    owner: root
    group: root
    mode: "0755"
  loop: "{{ recovery_exec_targets }}"
  loop_control:
    loop_var: target
  become: true
  when: not ansible_check_mode
  tags: [destructive]

- name: Deploy homelab-investigate-pve{1,2} wrapper scripts (read-only Proxmox investigation)
  ansible.builtin.template:
    src: homelab-investigate-pve.sh.j2
    dest: "/usr/local/bin/homelab-investigate-{{ target.name }}"
    owner: root
    group: root
    mode: "0755"
  loop: "{{ recovery_exec_pve_targets }}"
  loop_control:
    loop_var: target
  become: true
  when: not ansible_check_mode
  tags: [destructive]

- name: Deploy homelab-recover-* wrapper scripts
  ansible.builtin.copy:
    src: "homelab-recover-{{ item.name }}"
    dest: "/usr/local/bin/homelab-recover-{{ item.name }}"
    owner: root
    group: root
    mode: "0755"
  loop: "{{ recovery_exec_targets }}"
  become: true
  when: not ansible_check_mode
  tags: [destructive]

- name: Deploy homelab-monitoring-{pause,resume,status} scripts
  ansible.builtin.copy:
    src: "{{ item }}"
    dest: "/usr/local/bin/{{ item }}"
    owner: root
    group: root
    mode: "0755"
  loop:
    - homelab-monitoring-pause
    - homelab-monitoring-resume
    - homelab-monitoring-status
  become: true
  when: not ansible_check_mode
  tags: [destructive]

# Per-target mute wrappers (Codex-facing). Separate command names from the
# root-only human CLI (roles/recovery_mute/files/homelab-mute) so the Codex path
# (sudo-less, ACL-backed) stays distinct from the human sudo path. Write access
# is granted below via POSIX ACL, not sudo (§4.5 / §6).
- name: Deploy homelab-mute-{set,status,clear} wrapper scripts (Codex-facing, sudo-less)
  ansible.builtin.copy:
    src: "{{ item }}"
    dest: "/usr/local/bin/{{ item }}"
    owner: root
    group: root
    mode: "0755"
  loop:
    - homelab-mute-set
    - homelab-mute-status
    - homelab-mute-clear
  become: true
  when: not ansible_check_mode
  tags: [destructive]

# ---------------------------------------------------------------------------
# Investigation helpers: reports/ JSON lookup + Semaphore failure lookup
# (quory-local; no SSH hop to a target node).
#
# These do NOT use sudo: Codex invokes them inside a sandbox with
# no_new_privileges set, which blocks sudo unconditionally regardless of
# sudoers config (confirmed via a live failure: "sudo: The 'no new
# privileges' flag is set, which prevents sudo from running as root").
# Access to otherwise-unreadable paths is instead granted via POSIX ACL,
# which is a plain permission bit recovery-exec's own UID already holds
# (no escalation happens), so no_new_privileges does not affect it.
# ---------------------------------------------------------------------------

# ansible.posix.acl invokes `setfacl` (and getfacl for its check-mode
# assessment), which resolves entity: "{{ recovery_exec_user }}" to a real
# UID via getpwnam — verified empirically (decoy test, see the implement
# record) that this resolution FAILS even under --check ("setfacl: Option
# -m: Invalid argument") when the user does not exist yet. That is fine here
# because the ACL grants below are themselves destructive and thus gated
# with the identical `when: not ansible_check_mode` as "Create recovery-exec
# user" above — both skip together under --check, so the failing lookup is
# never attempted. Do not remove this task's gate independently of the user
# task's gate.
- name: Install acl package (setfacl/getfacl, needed for ansible.posix.acl)
  ansible.builtin.apt:
    name: acl
    state: present
  become: true
  when: not ansible_check_mode
  tags: [destructive]

- name: Grant recovery-exec ACL traverse on /home/yoshi (for reports/ read access, no sudo)
  ansible.posix.acl:
    path: /home/yoshi
    entity: "{{ recovery_exec_user }}"
    etype: user
    permissions: x
    state: present
  become: true
  when: not ansible_check_mode
  tags: [destructive]

- name: Grant recovery-exec ACL traverse on /var/lib/semaphore (no sudo)
  ansible.posix.acl:
    path: /var/lib/semaphore
    entity: "{{ recovery_exec_user }}"
    etype: user
    permissions: x
    state: present
  become: true
  when: not ansible_check_mode
  tags: [destructive]

- name: Grant recovery-exec ACL read on semaphore.db (no sudo; journal_mode=delete, no -wal/-shm to also grant)
  ansible.posix.acl:
    path: /var/lib/semaphore/semaphore.db
    entity: "{{ recovery_exec_user }}"
    etype: user
    permissions: r
    state: present
  become: true
  when: not ansible_check_mode
  tags: [destructive]

# Per-target mute write access for the homelab-mute-{set,clear} wrappers.
# Granted via POSIX ACL (recovery-exec's own uid gains rwx on the mute dir),
# not sudo — Codex's no_new_privileges sandbox blocks sudo/setuid regardless of
# sudoers (§4.5 / §6). Scope is the mute directory ONLY; no other write is
# added. Files stay 0644/world-readable so readers (recovery-probe as yoshi,
# recovery-push-dispatch) keep reading them. The dir has no sticky bit, so
# recovery-exec can atomically rename/rm over root-owned auto-mute files.
#
# Permission model (reworked per review 2026-07-14_008 F1): the dir is created
# ONCE at 0755 (create-only via `mkdir -m 0755 -p` + `creates:`, never re-chmod'd),
# and the named-user ACL below raises the ACL mask to rwx while group::/other::
# stay r-x. A blanket 0775 was wrong: on a host/time WITHOUT the ACL (e.g. ansy,
# which runs deploy_cli.yml via recovery_probe_setup but has no recovery_exec ACL,
# or quory before this ACL task) the group bits map to ACL_GROUP_OBJ, so 0775 would
# grant the owning group (root) real write. Creating at 0755 and never re-chmod'ing
# keeps group::r-x everywhere, and setfacl adds mask::rwx only where recovery-exec
# is set up. Final states:
#   ansy (no ACL): root:root 0755 — group r-x, other r-x, no group write.
#   quory (ACL):   group::r-x, mask::rwx, user:recovery-exec:rwx (effective rwx),
#                  other::r-x.
# All other writers (deploy_cli.yml, set.yml, human CLI) are likewise create-only
# and never re-chmod, so none of them resets the mask or widens the group.
- name: Ensure mute directory exists at 0755 (create-only; ACL below adds the mask)
  ansible.builtin.command:
    cmd: mkdir -m 0755 -p /var/lib/homelab-recovery/mute
    creates: /var/lib/homelab-recovery/mute
  become: true
  when: not ansible_check_mode
  tags: [destructive]

- name: Grant recovery-exec ACL rwx on mute dir (no sudo; mask becomes rwx, group/other stay r-x)
  ansible.posix.acl:
    path: /var/lib/homelab-recovery/mute
    entity: "{{ recovery_exec_user }}"
    etype: user
    permissions: rwx
    state: present
  become: true
  when: not ansible_check_mode
  tags: [destructive]

# Shared per-target flock targets (review 2026-07-14_005 F2). All mute writers
# (Codex wrappers, human CLI, Ansible auto-mute) take an exclusive flock on these
# before their read → max() → write, so a concurrent long auto-mute is never
# shortened by a short manual mute's stale read. Each writer opens the lock file
# read-only on fd 9 and flock's that fd; on a local fs an exclusive flock is
# granted regardless of the fd's open mode, so no write permission on the lock file
# is needed. This task pre-creates them 0644 root:root; if a wrapper's fallback
# create runs first the lock is recovery-exec-owned instead — both work, since
# locking does not depend on ownership. access/modification_time preserved for idempotence.
- name: Ensure per-target mute lock files exist (shared flock targets)
  ansible.builtin.file:
    path: "/var/lib/homelab-recovery/mute/.{{ item }}.lock"
    state: touch
    owner: root
    group: root
    mode: "0644"
    access_time: preserve
    modification_time: preserve
  loop:
    - authy
    - monnie
    - sophos-fw
  become: true
  when: not ansible_check_mode
  tags: [destructive]

- name: Deploy homelab-reports wrapper (Codex-facing, validates then reads directly)
  ansible.builtin.copy:
    src: homelab-reports
    dest: /usr/local/bin/homelab-reports
    owner: root
    group: root
    mode: "0755"
  become: true
  when: not ansible_check_mode
  tags: [destructive]

- name: Deploy recovery-reports-helper (privileged, re-validates independently)
  ansible.builtin.copy:
    src: recovery-reports-helper
    dest: /usr/local/sbin/recovery-reports-helper
    owner: root
    group: root
    mode: "0755"
  become: true
  when: not ansible_check_mode
  tags: [destructive]

- name: Deploy homelab-semaphore-query wrapper (fixed queries, reads semaphore.db directly)
  ansible.builtin.copy:
    src: homelab-semaphore-query
    dest: /usr/local/bin/homelab-semaphore-query
    owner: root
    group: root
    mode: "0755"
  become: true
  when: not ansible_check_mode
  tags: [destructive]

- name: Deploy homelab-incident-bundle wrapper (Codex-facing, validates then reads directly)
  ansible.builtin.copy:
    src: homelab-incident-bundle
    dest: /usr/local/bin/homelab-incident-bundle
    owner: root
    group: root
    mode: "0755"
  become: true
  when: not ansible_check_mode
  tags: [destructive]

- name: Deploy incident-bundle-helper (privileged, re-validates independently)
  ansible.builtin.copy:
    src: incident-bundle-helper
    dest: /usr/local/sbin/incident-bundle-helper
    owner: root
    group: root
    mode: "0755"
  become: true
  when: not ansible_check_mode
  tags: [destructive]

- name: Remove legacy homelab-mute-exec wrapper
  ansible.builtin.file:
    path: /usr/local/bin/homelab-mute-exec
    state: absent
  become: true
  when: not ansible_check_mode
  tags: [destructive]

- name: Remove legacy sudoers for mute-exec
  ansible.builtin.file:
    path: /etc/sudoers.d/recovery-exec-mute
    state: absent
  become: true
  when: not ansible_check_mode
  tags: [destructive]

- name: Remove legacy sudoers for recovery-exec investigation helpers (replaced by ACL; sudo is blocked by Codex sandbox's no_new_privileges)
  ansible.builtin.file:
    path: /etc/sudoers.d/recovery-exec-investigate
    state: absent
  become: true
  when: not ansible_check_mode
  tags: [destructive]

# ---------------------------------------------------------------------------
# Target node setup (authy/monnie)
# ---------------------------------------------------------------------------

# Structural guard against lateral clobber (2026-07-08 incident: a restricted
# `-l ansy` run, with recovery_exec_setup_targets left at its true default,
# pushed ansy's own investigate/action keys to authy/monnie/pve1/pve2's
# authorized_keys and locked out quory's production recovery-exec SSH access
# for 3 days. authorized_keys.j2 / authorized_keys-pve.j2 are drift-safe
# templates that slurp *whichever host is currently running this role* and
# replace the target's authorized_keys with exactly that host's keys — so any
# non-authoritative controller running this role with targets enabled would
# overwrite the production controller's keys.
#
# Two-stage guard (2026-07-11, revised after 2 rounds of review): the sole
# authoritative distributor is `quory` — hardcoded as a literal below, not a
# variable, and not control_nodes group membership. Two things were tried and
# rejected first:
#   - group membership (`inventory_hostname in groups['control_nodes']`): a
#     future second control_nodes member would inherit distribution rights
#     just by being added to the group, reopening this incident class.
#   - a role-default variable (`recovery_exec_setup_authoritative_host`,
#     defaulting to quory): role defaults are overridable by extra vars,
#     which have higher CLI precedence. `-e
#     recovery_exec_setup_authoritative_host=ansy` would pass both checks
#     below and let ansy distribute — reproducing the original incident
#     through the safety mechanism itself (caught in review).
# The literal `quory` below is the only form that can't be redirected by any
# combination of -l/-e at invocation time.
#
# 1. Assert quory is part of *this run's* host set whenever targets=true, so
#    `-l ansy` alone (quory excluded) fails here, before any target is
#    touched.
# 2. Gate the actual target_setup.yml include to quory only, so an
#    unrestricted run (both ansy and quory in the play) still succeeds
#    end-to-end — ansy completes its local setup and simply does not
#    distribute to targets, only quory does.
#
# See docs/ai/reviews/autonomous_recovery/2026-07-11_002_ssh_key_investigation.md.
- name: Assert quory is part of this run when target distribution is enabled
  ansible.builtin.assert:
    that:
      - (not (recovery_exec_setup_targets | default(true) | bool)) or ('quory' in ansible_play_hosts_all)
    fail_msg: >-
      recovery_exec_setup_targets is true, but quory (the sole authoritative
      distributor of target authorized_keys — hardcoded, not a variable; see
      the comment above this task) is not part of this run's host set
      ({{ ansible_play_hosts_all | join(', ') }}). Running with a restricted
      -l scope that excludes quory would let this host's own keys reach
      target nodes instead, overwriting the production controller's key on
      authy/monnie/pve1/pve2 and locking out its recovery-exec SSH access
      (2026-07-08 incident, see
      docs/ai/reviews/autonomous_recovery/2026-07-11_002_ssh_key_investigation.md).
      Re-run with -e recovery_exec_setup_targets=false to set up this host's
      local recovery-exec user/keys without touching target nodes.
    quiet: true

- name: Setup recovery-exec on target nodes (quory only)
  ansible.builtin.include_tasks: target_setup.yml
  when:
    - recovery_exec_setup_targets | default(true) | bool
    - inventory_hostname == 'quory'
