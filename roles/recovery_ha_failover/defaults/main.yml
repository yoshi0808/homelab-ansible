---
# HA 移行完了(VM が target-node で running)までの最大待機秒数
recovery_ha_failover_timeout_s: 300
recovery_ha_failover_poll_s: 10

# レポート保存先
recovery_ha_failover_report_dir: "{{ reports_base_dir }}/recovery_investigations/{{ target | default('unknown') }}"
