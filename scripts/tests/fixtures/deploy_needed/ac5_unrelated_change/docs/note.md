# Fixture note

This file stands in for a docs-only change staged in isolation from any
`roles/*/files/` or `roles/*/templates/` path. AC5 expects zero
deploy-related output when only this kind of file is staged.
