# audit: 起動契機をcallbackからバンドル走査へ切り替え

Auditor: Claude Code subagent(auditor role)。

対象: `docs/ai/reviews/incident_investigate_trigger/`(001_requirement〜004_test_result)。読み始めは`2026-07-31_001_requirement.md`。そこから`docs/ai/memory/incidents/2026-07-31_incident-investigate-callback-did-not-enqueue.md`、`docs/ai/adr/009-per-incident-investigation-runtime.md`、`docs/ai/policies/incident_capture_policy.md`§3.5、`docs/ai/status.md`該当行、および現物diff(`git diff`、未commit10ファイル)を辿った。Coordinatorからの経緯説明は入力にしていない。

## 再構成できたか

**できた。** requirement→implement→review→test_resultの4文書だけで、問題(callbackがSemaphore実行で発火しなかった)、決定(方針C=バンドル走査へ切替、暫定・可逆)、実装差分、差し戻し(R1/R2/R3)とその対応、独立レビューのApprove、Testerの実測PASSまで、他の説明なしに辿れた。現物diff(10ファイル)は記録の記述と一致し、参照した`docs/ai/reviews/incident_auto_investigation/`配下・ADR-009・Policy IC-034〜042の実在も確認した。

## 指摘

### 1. 親記録(Incident)のステータスが実態より古いまま(クローズを妨げない)

`docs/ai/memory/incidents/2026-07-31_incident-investigate-callback-did-not-enqueue.md`は、requirement冒頭で「Incidentが正本」と明記されている文書だが、

- ヘッダ: `状態: 原因判明・対応方針決定(暫定)・実装未着手`
- 「修正内容」節: `未実施。方針(C)は決まっており、実装はこれから。`
- 「確認方法」節: `未確定。`

のいずれも、本審査対象の4文書が示す実態(実装完了・差し戻し対応済み・独立レビューApprove・Testerが scratch でAC1〜AC7/R1/R2をすべてPASS実測済み)と食い違っている。他の解決済みIncident(例: `2026-07-31_codex-execpolicy-allowlist-not-enforcing.md`)は状態欄を`クローズ`や`解決済み`に更新する慣行があり、本件だけがこの更新を欠いている。

Incidentだけを読む読者(本件のrequirementが要求する「親記録から辿る」経路そのもの)は、「実装はまだこれから」と誤解する。**クローズを妨げる指摘ではない**(反映後の再監査を前提にしない、というAuditorの原則どおり)が、Coordinatorが案件をクローズする際にIncident側のヘッダ・「修正内容」・「確認方法」を現状(実装・レビュー・テスト完了、本番配備とAC1/AC6の実発火観測のみ未了)に合わせて更新することを推奨する。

### 2. その他

矛盾・根拠の欠落は見つからなかった。具体的に確認した点:

- T1〜T9・AC1〜AC7・§8 Q1〜Q4の全項目が、requirement→implement→review→test_resultの4文書を通じて対応・検証まで追跡できる。
- implement記録が引用する差分(R1のtry/except削除、R2のscan_errors隔離、T3のansible.cfg2行削除、T8のタスク削除、T9のdocstring追記)は現物diffと一致することを`git diff`で確認した。
- review記録のSuggestion #1〜#3はimplement記録の§0で対応済みと記録され、現物コード(`list_candidate_bundles`・`_summary_mtime`)にもその対応が反映されている。Suggestion #4(ADR-009未追随)はCoordinatorの並行対応と記録されており、実際に`docs/ai/adr/009-per-incident-investigation-runtime.md`が該当箇所((a)(f)節)を暫定supersedeとして更新済みであることを確認した。
- implement記録内の「R2の自己検証」手順(`chmod 000`をsummary.json自体にかける)は技術的に誤っていたが、この誤りはimplement記録自身に【2026-07-31 Coordinatorによる訂正】として明示され、かつtest_result側が独立に発見・訂正して正しい手順(親ディレクトリを塞ぐ)でPASSを再実測している。誤りの記録・発見経路・訂正のいずれも辿れ、「機構は健全で誤りは検証手段の記述側」という結論も両文書で一致している。
- `docs/ai/policies/incident_capture_policy.md`§3.5(IC-034〜042)はいずれも挙動レベルの規律であり、起動契機がキュー読みからバンドル走査へ変わったこと自体による改訂は不要という判断は妥当で、Policy側に矛盾する記述もない。
- `docs/ai/status.md`のNow #1行は「計画は合意済み」と現状(実装・レビュー・テスト完了、配備待ち)に対してまだ楽観側に古いが、これは案件クローズ時にCoordinatorが更新する対象であり、Auditor起動時点で残っていること自体は指摘に当たらない(Role本文の規律どおり)。

## 受入の可否

**条件付き受入。** 列挙した指摘1件(Incident側のステータス欄・「修正内容」・「確認方法」の更新)を反映すればクローズしてよい。反映の確認のための再監査は前提にしない。requirement・implement・review・test_resultの4文書自体には差し戻しを要する欠陥は無い。

---

## Coordinatorの判断(2026-07-31)

**指摘1件を反映し、この案件を閉じる。** 親Incident `docs/ai/memory/incidents/2026-07-31_incident-investigate-callback-did-not-enqueue.md` のヘッダ・「修正内容」・「確認方法」の3箇所を、実装/レビュー/検証が完了し**本番配備と実発火の観測だけが残る**現状へ更新した。あわせて `原因分類: #テスト不足` を記入した(環境変数の注入をソースの確認だけで前提化し、実行時に一度も実測しなかったことが本件の構造であるため)。

**状態は `解決済み` にせず `調査中` のままとした。** この機構は「発火するはず」で本番へ入って発火しなかったのが本件であり、**配備後の実発火を観測するまで解決とみなさない**。観測項目は同Incident「確認方法」が正本で、`docs/ai/status.md` のWatchが期日を持つ。
