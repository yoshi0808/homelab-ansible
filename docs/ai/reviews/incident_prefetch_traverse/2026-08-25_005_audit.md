# Audit: incident_prefetch_traverse

作成: 2026-08-25 / Auditor

対象: `docs/ai/reviews/incident_prefetch_traverse/`(`001_requirement.md` / `002_implement.md` / `003_review.md` / `004_test_result.md`)。参照先として `docs/ai/reviews/investigate_acl_observation/2026-08-25_005_finding.md`、`docs/ai/status.md`、commit `7bcc1f0` / `bbf2afa` / `08698d1` / `0d45183` / `a8aabf1`、`docs/ai/policies/execution_boundary_policy.md`(EXEC-043)、`docs/ai/policies/autonomous_recovery_policy.md`(AR-104)を確認した。

## 再構成できたか

**できた。** 経緯は次のとおり一本の筋で追える。

1. `investigate_acl_observation` 案件が先読み失敗の原因(`/var/lib/incident-inspect` に `yoshi` の traverse が無い)を確定させた(`2026-08-25_005_finding.md`)。
2. 本requirementがその修正(親への `x` のみACL付与)をP0-1として起票し、finding の「修正の方向」節と一致する。
3. Implementerが `roles/incident_investigate/tasks/main.yml` / `defaults/main.yml` に鏡像grantを追加(commit `bbf2afa`)。差分の内容・コメント・自己検証(syntax-check / ansible-lint差分比較 / decoyでのAC5確認)は実装記録の記述と一致することを diff で確認した。
4. Reviewer(Codex)がAC2の誤り(`yoshi` と `dev-investigate` の identity混同)をCritical指摘し、Request Changesを返した。
5. Coordinatorがrequirementの当該箇所だけを修正(commit `08698d1`)。requirement本文に「訂正」注記があり、commit messageの説明とも一致する。**実装は変えていない**という記述も、`08698d1` の diff が `requirement.md` と `003_review.md` の新規追加のみであることと一致する。
6. Testerが配備後のAC1/AC2/AC4/AC6を実測（commit `0d45183`）。値・identity・コマンドはすべて実際のdispatchスクリプト(`roles/dev_investigate/files/recovery-investigate-dispatch-quory.sh`)の対応表と整合していることを確認した。AC3/AC5は明示的に範囲外・観測待ちとして扱われており、黙って落ちていない。

詰まった箇所は無かった。

## 指摘

| 分類 | 内容 | クローズを妨げるか |
|---|---|---|
| 記録どうしの整合(軽微) | `002_implement.md` の「対象commit: `7bcc1f0`」は実装commit本体ではなく親commitを指しており、Reviewerが Suggestion として既に指摘済みだが未修正のまま残っている。実害は無い(Reviewer自身がblockingとせず、Testerは正しく`bbf2afa`を実装本体として参照している)。 | 妨げない |
| 反証の反映(軽微) | `003_review.md` は Verdict が **`Request Changes`** のまま更新されていない。指摘対象だったAC2はrequirement側の訂正(`08698d1`)とTesterのnegative check PASSで事実上解消しているが、`003_review.md` 自身にはその解消を示す追記が無い。**このファイルだけを読むと「Request Changesが未対応のまま案件が進んだ」ように見える。** | 妨げない(後続のrequirement訂正・Test PASSが同一フォルダ内に残っており、読み進めれば解消を再構成できるため) |
| 参照の弱さ(軽微) | requirement §4非ゴールの「先読みが失敗したときの通知の見え方…問題は別にある(`docs/ai/status.md`)」は、`docs/ai/status.md` 内に対応する独立した項目を特定できなかった(該当しそうなのは本件と同じ「壊れている」ブロック内の記述のみ)。ファイル全体への参照であり、別建ての記述の実在は確認できていない。 | 妨げない(非ゴールの背景説明であり、本案件のACや実装判断はこれに依存していない) |

## 受入条件の充足状況(記録上の確認)

- AC1・AC2・AC4・AC6: Testerが配備後に実測しPASSと記録。値は参照した現物(dispatchスクリプト・commit `a8aabf1`)と整合。
- AC3: 「意図的に起こせない」ため観測待ちとして明示。requirement §8とTest Result双方が同じ扱いで一致。
- AC5: Implementerが decoy(`/tmp` の使い捨てplaybook)で確認済みとTest Resultが引き取り、Testerは「配備の再実行が要るため範囲外」と明記。**独立した第三者(Tester)による本番相当の再確認は無い**が、範囲外である理由は記録されており、黙って落ちてはいない。
- P0-2 / AC4(`workspace` が閉じたまま): 開発側から本番現物を確認する手段が無いことをrequirement §7が事前に想定しており、Implementer・Reviewer・Testerの3記録が同じ限界を一貫して残存リスクとして記録している。矛盾は無い。

## `docs/ai/status.md` との突合

- 該当行(48行目「壊れている: 一次調査の先読みファイルが書けていない — 原因確定、修正待ち」)は、requirement P1-1 が指定するとおり**本案件クローズ時にCoordinatorが更新する**ものであり、現時点で残っていること自体は指摘ではない。
- 除去・更新すべき実施事項(指摘ではない):
  - 48行目ブロックを「修正済み・AC3観測待ち」へ更新する(P0-1の内容、`bbf2afa`、配備ジョブ#841を反映)。
  - 「修正は未実施。既にある付与の鏡像(親へ `yoshi` の `x`)になる。採る前に…確かめる」という行(56行目付近)は既に実施済みのため古い。
  - AC3(先読み書込みが実際に機能すること)を新規の「観測待ち」項目として起票する(次にSemaphoreジョブが失敗し一次調査が動いたときが確認機会)。

## 判定

**受入。** 反映すべき事項は上表のとおり非blockingであり、`docs/ai/status.md` の更新はrequirementが最初から「クローズ時にCoordinatorが行う」と設計している実施事項であって指摘ではない。そのままクローズしてよい。

---

## クローズの判断(Coordinator、2026-08-25)

**クローズする。** 指摘3件はいずれも記録の精度に関するもので、すべて反映した。

1. **実装記録の「対象commit」ラベル** — 着手基点と実装が入ったcommitを両方明示する形へ直した。**同じ指摘が `investigate_acl_observation` でも出ており、2案件連続である。** ラベルの書き方そのものが曖昧だった。
2. **レビュー記録の Verdict が「Request Changes」のままだった** — 解消したことを追記した。**単独で読むと未解決に見える**という指摘は正しい。
3. **非ゴールの参照先が実在しなかった** — 「別にある」と書いたが `docs/ai/status.md` に独立した行は無かった。**案件を起こしていない**ことを明示し、申し送りの置き場も書いた。

### 残して閉じるもの

**AC3(実際に書けたか)は未充足のまま閉じる。** Semaphore ジョブの失敗が要り、意図的に起こせない。`docs/ai/status.md` の観測待ちへ置いた。

**`workspace` の本番現物は観測できない。** `acl-status` の表に arm が無く、repo 側の定義までしか言えない。**親へ traverse を与えたことで `yoshi` がそこへ到達できるようになっていないことは実測していない** — 残存リスクとして記録した。

**通知の見え方(先読みが空でも「調査したがわからなかった」と同じ形で出る)は本案件の非ゴールであり、案件を起こしていない。** `docs/ai/status.md` の当該行末尾に申し送りとして置いた。
