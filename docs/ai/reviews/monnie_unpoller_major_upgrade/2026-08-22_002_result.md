# result: monnie 月次 full-upgrade(unpoller 4.0.0)— 適用完了

実行: Yoshinobu(quory の Semaphore、2026-08-22)/ 記録: Coordinator

## 結論

**適用は完了した。unpoller 4.0.0 は正常に稼働している。ただし1回目のジョブは conffile プロンプトで停止し、手動介入を要した。**

## 経過

| 時刻 (JST) | 出来事 |
|---|---|
| 14:47:21 | ジョブ **#802** 開始。recovery mute 120分を設定し `apt-get -y full-upgrade` へ入る |
| 14:48:22 頃 | grafana 13.2.0 まで設定完了。**`unpoller` の設定中に `/etc/unpoller/up.conf` の conffile プロンプトで停止** |
| 〜14:59 | Yoshinobu が異常に気づく。**monnie の load は 0.02 / 0.09 / 0.10 で、15分間ブロックしていた**ことが read 専用チェックで判明 |
| 15:02 頃 | `/var/log/apt/term.log` でプロンプト本体を確認。**答えるべきは `N`(現状維持)**だが、stdin は Ansible の SSH セッションにあり誰も答えられない |
| — | ジョブ #802 を停止。**quory 側の ansible は止まったが、monnie の `apt-get`(pid 90674)は孤児として残り待ち続けた** |
| 〜15:07 | `apt-get` を終了させ `dpkg --configure -a` で完了。4本とも `ii` |
| 15:08:07 | **unpoller を手動再起動。4.0.0 が起動し `Metric: 2034` / `Err: 0`** — 適用前のベースラインと一致 |
| 15:11:52〜15:13:28 | ジョブ **#803**(同テンプレート再実行)が **success**。再起動と復帰確認が設計どおり通った |

## 適用後の確認

| 確認点 | 結果 |
|---|---|
| unpoller | `4.0.0+git`、`active (running)`、`Metric: 2034` / `Err: 0`(ベースライン一致) |
| prometheus targets | `prometheus` up / `unifi-poller` up、`lastError` は両方とも空 |
| grafana | `13.2.0`、`database: ok` |
| loki | `3.7.6`、応答あり |
| failed unit | 0件 |

## ブリーフの誤り2件

`2026-08-22_001_apply_brief.md` は訂正済み。**誤っていたのは次の2点である。**

1. **「conffile は上書きされない」の根拠が誤り。** `DEBIAN_FRONTEND=noninteractive` が抑止するのは **debconf** であって、**dpkg の conffile プロンプトではない**。結論(上書きされない)は正しかったが、**プロンプトが出ないという前提が誤り**で、これが当日の停止の直接の原因である
2. **「60秒のバックグラウンド更新」を裏が取れないとして判断材料から外したのは誤り。** 起動ログに `Prometheus scrape cache enabled, refresh interval: 1m0s` が実在する。**GitHub Releases の本文に無いことを「無い」の根拠にした** — release 本文は挙動の網羅ではない

## 判明した事実

- **CloudKey(UniFi Network Application)は 10.5.67。** 未確認だった項目が閉じた
- **`up.conf` はローカル変更がある。** 手動管理(Notion手順書)なので当然だが、**それが毎月の apply を止めうる**ことは認識されていなかった

## 残っているもの

- **`apply.yml` の conffile 指定**。本件の真因であり、`docs/ai/reviews/ubuntu_vm_apply_conffile_prompt/` で別案件として起票した
- **scrape cache が 3.3.4 から在ったのかは未確認。** 4.0.0 で入ったのならメトリクスの時間分解能が 15秒から 60秒へ落ちる。確認は `journalctl -u unpoller --since "2026-08-07 03:30" --until "2026-08-07 03:33"`(monnie 上、journal に残っている)
