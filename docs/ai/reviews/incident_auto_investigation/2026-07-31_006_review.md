# Review: 障害の一次調査の自動化(U1 `roles/incident_inspect` + U2 `roles/incident_investigate`)

Reviewer: Claude Code subagent(reviewer role)。実装(U1/U2いずれのImplementer)には関与していない別セッション。

対象: 作業ツリー未commit差分すべて(`git status --porcelain=v1 -uall` で確認した17パスの新規/変更ファイル一式)。実装記録 `2026-07-31_004_u1_implement.md`・`2026-07-31_005_u2_implement.md` は参照のみとし、記載内容は現物(コード・自ホストでの再実行)で裏取りした。

## 確認方法(自己検証)

- `docs/ai/core.md`・`docs/ai/roles/reviewer.md`・requirement(`2026-07-31_001_001`)・ADR-009・`incident_capture_policy.md` §3.5/IC-010/011/017/021/030 を通読。
- 全成果物ファイルを現物で読了(`roles/incident_inspect/**`、`roles/incident_investigate/**`、両setup playbook、`ansible.cfg`のdiff)。
- `ansible-playbook --syntax-check`(両playbook)、`ansible-lint`(両role)、`python3 -m py_compile`、`bash scripts/check-tester-gate.sh` を自分で再実行し独立に確認(実装記録の記述を鵜呑みにしない)。
- `/tmp`のscratch環境でのみ、①この`ansible.cfg`と同一のcallback plugin登録を再現し実際に失敗playを走らせて `PLAY RECAP`/終了コード/キューファイル生成を実測、②実在する`codex`バイナリ(`codex-cli 0.145.0`、既存の認証済みセッション)へ`codex-investigate-wrapper.j2`と同一のフラグ組み合わせ(`--sandbox read-only -c 'approval_policy="never"' -c 'sandbox_workspace_write.network_access=false'`)で`exec`を実行し、外部コマンド実行が機能することを確認。**いずれも実ホスト(quory/ansy等)・実identityには一切触れていない。**
- `homelab-reports`/`homelab-semaphore-query`/`recovery-reports-helper`の引数文法を、`AGENTS.md.j2`とincident-investigate.pyの`build_prompt()`の呼び出し文字列に対して行単位で突き合わせ。
- ACL付与(traverse/read)がIC-030(reports/直下への書込禁止)に抵触しないことを、実際のACL付与タスクの`permissions:`値(すべて`x`/`r`、`w`は皆無)で確認。

### 各AC(受入条件)についての到達点

| AC | 判断 | 根拠 |
|---|---|---|
| AC1 | 満たすと判断 | callback pluginはstatsを読むのみで書き換えない。成功playでは`_maybe_enqueue`が`failed_hosts`/`dark_hosts`空で即return(コード読了) |
| AC2 | **満たすと判断(実測)** | scratchで同一ansible.cfg設定を再現し、失敗playの終了コードが callback有効/無効のいずれでも`2`で一致することを確認済み(下記「What Looks Good」参照) |
| AC3 | 満たすと判断 | queueディレクトリ不在時は`os.path.isdir`一発で即return。ansyには`incident_investigate_queue_dir`を作るタスクがどこにも無い(`roles/incident_investigate/tasks/main.yml`は`hosts: quory`のplaybookからしか呼ばれない) |
| AC4 | **部分的にしか判断できない** | フィールド構成・命名・書き込み経路はコードで確認済み。**実際のCodex呼び出し(実バンドル・実`incident-inspect`)は未実行**(Implementer・本レビューとも同様。Tester領域) |
| AC5 | 満たすと判断(構造は確認、実行呼び出しは未検証) | `.ssh`作成タスク皆無・鍵複製皆無をtasks全文で確認。読み取り2コマンドの到達性はACL設計から筋が通る。**実際に`incident-inspect`のUIDで`homelab-recover-*`を実行させて失敗を見る/`homelab-semaphore-query`を実行させて成功を見る、という両側の実測はTesterの領域(AC5本文が明記)** |
| AC6 | 満たすと判断 | `invoke_llm`のtimeout/非ゼロ終了双方が`llm_error`経由で`assemble_artifact`の`status: failed`・`main()`の`EXIT_INVESTIGATION_FAILED(2)`に落ちる経路をコードで追跡 |
| AC7 | 満たすと判断(複数の競合順序を追跡) | `artifact_already_exists`によるLLM再呼び出し防止と、`unresolved-*`→後日f-2解決/`semaphore-<id>`→先着処理、のどちらの処理順でも二重LLM呼び出しが起きないことをロジックで追跡(下記詳細) |
| AC8 | 満たすと判断 | `incident_sync_excludes`は`_spool/`と`*.tmp`のみで、`_investigations/`配下の`.json`/`.md`(atomic rename後の最終ファイル)は除外対象外であることを確認 |
| AC9 | **判断できない(下記Critical Issues #2)** | 内部IP/認証情報の混入防止が prompt指示 + 文字数切り詰めのみに依存しており、機械的なフィルタが無い。バンドルは非信頼データ(IC-016)である一方、その内容がLLMの応答経由でそのまま成果物へ転記される経路を塞ぐ技術的な層が無い |

## Critical Issues

| # | File | Line | Issue | Severity |
|---|---|---|---|---|
| 1 | `roles/incident_investigate/callback_plugins/incident_investigate_trigger.py` | 74–77(コメント)、`ansible.cfg` の追加2行 | **`callbacks_enabled = incident_investigate_trigger` は実際には何も有効化していない。** このクラスは `CALLBACK_NEEDS_ENABLED`(または新API `REQUIRES_ENABLED`)を一切設定しておらず、ansible-core(`ansible/executor/task_queue_manager.py:275,294`)は未設定時に`callback_needs_enabled = False`として扱う。この場合の読み込み条件は`not self._run_additional_callbacks`だけで決まり、`ansible-playbook`実行では`_run_additional_callbacks`は常に`True`(引数省略時のデフォルト、adhocの`ansible`コマンドとは異なる)。**つまりこのcallbackは`callback_plugins`の検索パスに乗った時点で、`callbacks_enabled`の中身に関わらず常に読み込まれ発火する。** 実際に`/tmp`のscratchで`callback_plugins`だけを設定し`callbacks_enabled`を一切書かずに失敗playを実行したところ、queueファイルが生成されることを実測で確認した(再現手順は下記)。コード内コメント「既定(`CALLBACK_NEEDS_ENABLED`を明示しない = **True相当**)のままにする」は**事実と逆**であり、U2実装記録§5「壊れたときにどう戻すか」の「`callbacks_enabled`から名前が消えればロードされなくなる」という説明も**成立しない**(`callback_plugins`側の1行が生きている限り、`callbacks_enabled`を空にしても発火し続ける)。**現状の2行構成では実害は無い**(両方削除すれば意図通り無効化される)が、「有効化をansible.cfgの1箇所(`callbacks_enabled`)だけで制御する」という設計意図そのものが成立しておらず、将来「`callbacks_enabled`だけ外して一時的に切る」という運用判断をした場合に無言で失敗する。 | High(実害なし・現状の2行構成では動作は意図通り。だが文書化された制御・復旧手順の前提が事実と異なる) |
| 2 | `roles/incident_investigate/files/incident-investigate.py` | `build_prompt()` L308–363、`assemble_artifact()` L401–491、`truncate_free_text()` L393–398 | **AC9/IC-040(内部IP・認証情報の実値を書かない)の担保が、prompt指示 + 文字数切り詰めのみで、機械的なフィルタを一切持たない。** IC-016はバンドルを「非信頼データ」と明言し、`homelab-semaphore-query task-output`はバンドルの生ログをそのままモデルへ読ませる経路を提供している。モデルが指示に従わずログの一部(内部ホスト名・IPアドレスを含みうる行)を`observations`/`notes`へ転記した場合、`assemble_artifact`はその文字列を(4000/1000文字での切り詰め以外)無検査でそのまま`_investigations/*.json`・`*.md`へ書き、`incident_sync`が毎時これをansyへ複製する。この経路は本Policyが他の箇所で繰り返し要求してきた原則(「禁止はpromptの文言で表現しない」IC-018、`docs/ai/memory/lessons/permission-boundaries-must-be-designed-not-prompted.md`)と同じクラスの弱さを持つが、IC-034(行動の禁止)と異なりIC-040(内容の禁止)には機構での強制が課されておらず、requirement §7も技術的な担保を要求していない。**要件・Policyの文言上は違反ではない**が、この案件が生まれた文脈(「開発と運用の分離」「非信頼データの明記」)からすると一貫性を欠く実装判断であり、Coordinator/Yoshinobuの意思決定が要ると判断した。 | Medium(要件・Policy違反ではないが、Policyの精神との整合性の欠如。Tester/Coordinatorでの扱いを要確認) |

## Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| 1 | `docs/ai/adr/009-per-incident-investigation-runtime.md` | Consequences節「ACL 2件」 | U1の実装は`/home/yoshi`traverse・`/var/lib/semaphore`traverse・`semaphore.db`readの3タスク(`roles/incident_inspect/tasks/main.yml` L113–138)。ADR本文の「2件」という記載と数が合わない(recovery_execの既存実装も同じ3タスク構成であり、実装自体は正しい)。ADR側の記載更新を推奨。U1の実装記録§6も同じ食い違いを自己申告済み。 | Documentation |
| 2 | `roles/incident_investigate/files/incident-investigate.py` | L100–113(`GO_TIME_RE`/`parse_go_time`) | 定義されているが呼び出し箇所が無い(grep で確認、使用箇所0件)。`finalized`判定は`row["end_raw"]`の真偽のみで、Go time文字列の妥当性検証は行われていない。デッドコードとして削除するか、意図した用途(例: end確定後の所要時間ログ化)を実装するか、どちらかに倒すことを推奨。 | Cleanup / duplication-reuse |
| 3 | `roles/incident_investigate/templates/incident-investigate.service.j2` | L12(`flock -n -E 75`) | 同一jobの多重起動を`flock`が拒否した場合、`ExecStart`はexit 75で終了し、`Type=oneshot`のためsystemdはこのunitを`failed`として報告する(=`systemctl is-failed`が`failed`を返す)。これは正常なskipであり調査の失敗ではないが、外形的にはAC4/AC6が想定する「失敗」と見分けが付かない。既存`roles/incident_capture`が同じパターン(`-E 75`)を採用済みであり本diff固有の新規問題ではないが、Tester/監視側が`failed`をどう解釈するかの申し送りを推奨。 | Observability(既存パターン踏襲、非ブロッキング) |
| 4 | (横断) | 実際のCodex呼び出し | U1・U2とも自己検証はfake/stubスクリプトに限定しており(両実装記録に明記)、実`codex`バイナリ・実`incident-inspect`ユーザーでの通しの動作確認は行われていない。本レビューで`--sandbox read-only`+wrapperと同一フラグ組み合わせの一般的な動作は独自に確認済み(下記)だが、`incident-inspect`固有のOAuth・`config.toml`込みの通し確認はまだ無い。`codex login --device-auth`実施後、Testerでの実地確認を推奨(AC4/AC5/AC6がこれに依存する)。 | Test coverage |

## What Looks Good

- U1/U2間の結合値(`incident_inspect_user`/`incident_inspect_codex_wrapper`/`incident_inspect_workspace`/呼び出し引数`exec --cd <workspace> <message>`)が両role の`defaults/main.yml`で文字どおり一致していることを確認した(`roles/incident_inspect/defaults/main.yml` L17–24、`roles/incident_investigate/defaults/main.yml` L35–37)。
- ACL付与はすべて`permissions: x`または`r`のみで`w`は皆無(`roles/incident_inspect/tasks/main.yml` L113–138)。IC-030(reports/直下への書込禁止)・C1不変条件(reports/incidents/配下のchmod禁止)のいずれにも抵触しない。両roleの`tasks/main.yml`全文を読み、`mode:`付きタスクが`reports/incidents/`配下に存在しないことを確認した。
- `incident-inspect`ユーザーに`.ssh`ディレクトリ・鍵生成タスクが一切無いことを`roles/incident_inspect/tasks/main.yml`全文で確認(`ssh-keygen`/`generate_ssh_key`相当の記述なし)。
- **AC2を実測で確認した**: `/tmp`のscratchでこのcallback pluginをansible.cfg経由で有効化し、意図的に失敗するplayを実行したところ、callback有効時・無効時のいずれも`ansible-playbook`の終了コードは`2`で一致し、`PLAY RECAP`のfailed件数も一致した。
- `--sandbox read-only`が実在するcodexのオプションであること、かつ`exec`サブコマンドで外部コマンドの実行(`echo`)が実際に成功することを、既存の認証済みセッションを使い`/tmp`のscratchディレクトリ内でのみ確認した(wrapperと同一のフラグ組み合わせ、実ホスト・実identityへの変更なし)。
- `homelab-reports show-report incidents semaphore-<id> summary.json`(`build_prompt()`が案内する呼び出し)が`homelab-reports`→`recovery-reports-helper`の4引数ケース(`playbook`/`target`/`filename`)を正しく通り、実際のバンドルパス`reports/incidents/semaphore-<id>/summary.json`に解決されることを両ファイルの引数解析ロジックを突き合わせて確認した。
- AC7の重複排除を、`unresolved-*`(f-1失敗)が先に処理され後で`semaphore-<id>.json`が来る場合、逆の順で来る場合の両方で追跡し、いずれの順序でもLLM呼び出しが2回目には走らないことを確認した(`artifact_already_exists`によるガード)。
- `ansible-playbook --syntax-check`(両playbook)、`ansible-lint`(両role)、`python3 -m py_compile`(両Pythonファイル)、`scripts/check-tester-gate.sh`を独立に再実行し、実装記録の主張どおりすべて通過することを確認した。ansible-lintの`risky-file-permissions`指摘1件は`roles/incident_capture`が既に持つ同クラスの指摘(2件)と同型のベースラインであり、新規の退行ではないことも実際に`roles/incident_capture`単体でansible-lintを走らせて確認した。
- IPv4リテラルは新規ファイル一式・`ansible.cfg`のdiff・両実装記録のいずれにも見つからなかった(独自にgrepで確認)。
- 両setup playbookとも対象ホストが`quory`単体または`dev_nodes:control_nodes`に限定されており、保護対象ホスト(authy/monnie/pve1/pve2/sophos-fw/cloudkey)への到達経路が存在しないことをplaybook全文で確認した。

## Verdict

**Request Changes**(Critical Issues 2件の解消、または少なくともCoordinatorの明示判断を条件に、Testerフェーズへの引き継ぎ自体は妨げない)。

- Critical #1(`callbacks_enabled`の実効性)は、`CALLBACK_NEEDS_ENABLED = True`をクラスへ追加する1行修正で、コメント・実装記録の記述どおりの制御に一致させられる(現状の動作自体は変わらない — 両方の設定行が揃っている限り発火するため)。
- Critical #2(AC9の機械的担保の不在)は実装の巧拙ではなく設計判断の要否であり、Coordinatorが「このまま許容する」か「機械的フィルタ(例: IPv4正規表現の除去)を`assemble_artifact`に足す」かを決める必要がある。
