# review: pre-commit検査の拡張(R1: 新規playbook Semaphoreボタン助言 / R2: staged一覧の一本化)

日付: 2026-08-05 (JST)
対象: requirement `2026-08-05_001_requirement.md`(R1/R2、AC1〜AC7、非ゴール、制約、OQ1/OQ2)
参照(検査対象、裏取り前提で読んだ): implement `2026-08-05_002_implement.md`

対象差分: `scripts/check-deploy-needed.py`、`scripts/git-pre-commit-check.sh`、
`scripts/tests/fixtures/deploy_needed/{README.md,run-fixture-checks.sh}`、
新規fixture3件(`newplaybook_ac1_no_catalog_entry/` `newplaybook_ac2_has_catalog_entry/`
`newplaybook_ac3_existing_modified/`)。

`docs/ai/status.md` / `roles/proxmox_exec_node/tasks/main.yml` /
`docs/ai/reviews/proxmox_exec_node_limit/` はCoordinatorの変更として対象外とし、
一切変更していない(下記「確認した手段」参照)。

## Summary

R1・R2とも要件どおりに実装されている。fixture一式(既存8+新規3+AC4向け2)を
実行し全PASS、さらに実リポジトリの実際のstaged状態を影のindexで再現して
`git-pre-commit-check.sh`本体を実行し `exit 0` を確認した。カタログ読み取り
失敗時にブロックしないこと、python3不在時の既存挙動が変わらないこと、
staged一覧の受け渡し(`--staged-paths -`)がquoted pathで安全側に倒れることも
実行して確認した。**1件、Minor**指摘がある — OQ1の「rename は対象外」は
git のrename検出(類似度閾値)に依存しており、rename+大幅書き換えが同一
commitに混在すると`--diff-filter=A`側に倒れ、旧名でカタログ済みのplaybook
に対して誤って警告が出る(false positive)。設計上許容範囲内(advisory・
non-blocking)だが、実装記録の「OQ1は実際に確認済み」という言明は
100%類似度のrenameしか確かめておらず、この分岐は未検証だった。

## Critical Issues

なし。

## Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| 1 | `scripts/check-deploy-needed.py` | `git_added_playbooks()` (`--diff-filter=A`) | rename+同一commit内での大幅内容変更（gitのrename類似度閾値50%を下回る場合）は `git diff --cached --diff-filter=A` 側で `D`+`A` として現れ、旧名で `semaphore_templates_catalog` に載っている playbook のrenameを「新規・カタログ無し」と誤検知する（実行して再現済み、下記参照）。advisory・non-blockingなので実害は「無意味な警告が出る」に留まり、要件のOQ1が許容する誤検知コストの範囲内と判断できるが、①implement記録がOQ1について「実際に`git mv`で検証済み」と述べているのはこの分岐を含まない(100%類似度のrenameのみ検証)こと、②この既知の限界をコード側のコメントか実装記録に一言残していないことは、次にこのスクリプトを触る人への申し送りとして足りない。ブロック不要、コメント追記の提案に留める。 | Documentation / edge-case coverage |
| 2 | `scripts/check-deploy-needed.py` | `git_added_playbooks()` | このR1専用の関数は`staged_paths`(unified list, R2の対象)を使わず独自に`git diff --cached --name-status --diff-filter=A -- playbooks/*.yml`を叩いている。R2が一本化した対象は「shellとPythonの間の一般的なstaged一覧」であり、Added限定・path限定のこのクエリは元々unified listでは表現できない情報(status letter)を要求するため設計上妥当だが、「staged一覧の取得を1箇所にする」というR2の見出しだけを読むと、この3つ目の独立git呼び出しが残っていることに気づきにくい。`git_staged_paths()`のdocstringか、この関数自身のdocstringに「これはR2が一本化した対象の外側」である旨を一言足すと誤解を防げる(実際には既にdocstringにやや説明があり、読めば分かる — 優先度は低い)。 | Documentation |

## What Looks Good

- **AC1**: 実カタログ(`roles/semaphore_templates/defaults/main.yml`、実ファイル)に対し、影のindexへ新規`playbooks/`ファイルを注入し `check-deploy-needed.py` を実行 → 助言メッセージが出力され `exit 0`。確認手段: `GIT_INDEX_FILE`複製 + `git update-index --add --cacheinfo` で作業ツリー非改変のまま検証(下記コマンド参照)。
- **AC2/AC3**: fixture `newplaybook_ac2_has_catalog_entry` / `newplaybook_ac3_existing_modified` を実行、どちらも警告なしで `exit 0`。fixtureのcatalog内容も目視確認し、「catalogが空だから警告が出ないだけ」ではなく意図した対象外ケースになっていることを確認(catalogに無関係playbookのエントリを1件入れて空カタログ問題を排除する設計、AC1側も同様)。
- **AC3の前提(既存playbookのM状態)**: before/after二段構えのfixtureを読み、`before/`のcatalogが`playbooks/existing_thing.yml`用のエントリを意図的に持たない設計であることを確認 — 「ACMR全体を見ていたら誤検知したはずの状態」を正しく作れている。
- **AC4**: `ac4_quoted_path_parity` fixtureを実行し、シェル側(`git-pre-commit-check.sh`実行)とPython側(自前導出)がバックスラッシュ入りファイル名に対して両方fail-closedになることを確認(shell exit 1 / python exit 2、両方 `cannot be safely checked` 相当のメッセージ)。さらに独自に改行入りファイル名でも同じ判断(両方 `"..."` でクオートされたまま返る)になることを別途確認。`ac4_given_staged_paths_is_authoritative` fixtureで、`--staged-paths`経由の値が実際に権威的である(Pythonが自前でgitへ再問い合わせしない)ことも確認。
- **AC5/AC6**: `scripts/tests/fixtures/deploy_needed/run-fixture-checks.sh` 全11ケース(既存5 + 新規3 + AC4向け2の一部)PASS、`scripts/tests/fixtures/run-fixture-checks.sh`(doc-consistency側)5fixture全PASS。
- **実リポジトリの実際のstaged状態での終端テスト**: `.git/index`を`GIT_INDEX_FILE`で複製した影のindexへ、この案件で変更されたファイルのみ(Coordinator変更分は除く)を`git add -A`し、実際の`scripts/git-pre-commit-check.sh`を実行 → gitleaks・tester-gate-lint(52 playbooks)・check1〜3(doc-consistency)すべてOKで`[pre-commit] OK`、`exit 0`。新規playbookの追加が無い差分なのでR1の助言は出ず、それも期待どおり。
- **カタログ読み取り失敗時にブロックしないこと**: 影のindexで`roles/semaphore_templates/defaults/main.yml`をindexから除去した状態で実行 → 助言は出力されず、クラッシュもせず`exit 0`。`load_semaphore_templates_catalog()`が`None`を返し呼び出し元が「言わない」を選ぶ設計どおり。
- **AC7**: python3を含まないPATH(usr-merge環境のため、python3以外の全実行ファイルへのシンボリックリンクで代替PATHを構築、gitleaksも別途リンク)で`git-pre-commit-check.sh`を実行 → 既存のYAML構文チェックのガード行で`ERROR: python3 not found; cannot run staged YAML syntax check`、`exit 1`(変更前と同じ停止点・同じ挙動)。今回変更した`check-deploy-needed.py`呼び出し直前のガード行自体は今回の差分で無変更であることもdiffで確認済み。
- **既存検査への影響なし(AC5)**: `git-pre-commit-check.sh`のdiffは末尾ブロック(コメント追加+呼び出し1行の変更)のみで、gitleaks・危険ファイル名・IPv4リテラル・vault-header・staged YAML・tester-gate・doc-consistencyの各ブロックは無変更であることをdiff全文で確認。呼び出し順序も変わっていない。
- **shell側の変数展開**: 新規追加行 `printf '%s\n' "$staged_files" | python3 ... --staged-paths -` は`"$staged_files"`が引用符付きで、CLI引数でなくstdin経由のためファイル名が`-`始まりでもコマンドインジェクション/引数注入のリスクが無い(旧来の実装より安全な形)。
- **OQ1(rename、100%類似度のケース)**: `git mv`による同一内容renameを実際に作った使い捨てリポジトリで再現し、警告が出ないことを確認 — 実装記録の主張どおり。
- **重複/再利用チェック**: `duplication-reuse-check` skill観点で確認。R2は要件が実装方法を指定しない(「シェル取得→渡す」か「Python側へ寄せる」かは実装判断)ため、既存資産の再利用漏れという意味での指摘対象はなし。`git_added_playbooks()`が独自にgit呼び出しを持つ点は上記Suggestions #2で言及済み。
- **Ansibleセキュリティレビュー観点**: 本差分はAnsible task/role content(shell/commandモジュール等)を一切含まず、pre-commitのシェル/Pythonスクリプトのみが対象のため、`ansible-security-review` skillの直接の適用対象外と判断した。

## 確認した手段(コマンドと結果の要約)

- `bash scripts/tests/fixtures/deploy_needed/run-fixture-checks.sh` → 全PASS、`exit 0`
- `bash scripts/tests/fixtures/run-fixture-checks.sh` → 全PASS、`exit 0`
- `cp .git/index <shadow>; GIT_INDEX_FILE=<shadow> git add -A; git diff --cached --name-only` で対象差分のみをstageし、`GIT_INDEX_FILE=<shadow> bash scripts/git-pre-commit-check.sh` → `[pre-commit] OK`、`exit 0`。直後に`unset GIT_INDEX_FILE; git diff --cached --stat`が空であることを確認し、実indexが無傷であることを検証
- 上記影のindexに`git hash-object -w /dev/null`＋`git update-index --add --cacheinfo`で新規playbookパスのみを注入(作業ツリーには書き込まず)、`check-deploy-needed.py --staged-paths <(...)`実行 → AC1の助言メッセージが実カタログに対して出力されること、`exit 0`を確認
- 同様の手法で`roles/semaphore_templates/defaults/main.yml`をindexから除去 → 助言なし、`exit 0`(カタログ不読でもブロックしない)
- `/tmp`配下の使い捨てgitリポジトリで、①改行入りファイル名でのquoted-path parity、②100%類似度renameでの非警告、③rename検出閾値を下回る(内容を大幅に書き換えた)renameでの誤警告再現、を実行・確認
- python3を除いた代替PATH(usr-merge全実行ファイルへのシンボリックリンク+gitleaks)で`bash scripts/git-pre-commit-check.sh`を実行 → 既存のYAML構文チェックガードで`exit 1`(AC7)
- `git diff -- scripts/check-deploy-needed.py scripts/git-pre-commit-check.sh scripts/tests/fixtures/deploy_needed/README.md scripts/tests/fixtures/deploy_needed/run-fixture-checks.sh`全文を読み、変更範囲がrequirementの対象と一致すること、既存ブロックが無変更であることを確認
- 新規fixture3件全ファイルの中身を`cat`で確認し、catalog/playbook内容がAC1〜AC3の意図どおりであることを確認
- 未使用の一時ファイル・使い捨てリポジトリはすべて`/tmp`または本セッションのscratchpad配下に作成し、レビュー終了後に削除。`git add`・`git checkout`・`git restore`・`git stash`・`git commit`・`git push`は一切実行していない
- 最終確認として `git status --porcelain` を実行し、レビュー開始時と同じ差分集合であること(対象外ファイルに触れていないこと)を確認

## Verdict

**Approve**(Suggestions 2件は次回以降の申し送りで良く、ブロック不要)
