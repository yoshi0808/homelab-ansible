# 自律復旧トリガ基盤 E-2 再テスト結果 — 2026-07-03

## 結論

quory 側で `git pull` 済みの commit `32d9a78` を前提に、`2026-07-03_003_test_plan.md`
の phase 3 から再実施した。

- phase 3 本番デプロイ: OK
- quory / authy / monnie の E-2 配布物: 配置確認 OK
- authy / monnie の OnFailure: E-2 drop-in に更新済み
- phase 4 手動 push: 未実施(前提未充足のため)
- phase 5 後片付け: 残骸なし

E-2 はまだ完了扱いにできない。

理由:

1. quory に `/usr/local/bin/homelab-mute` がないため、計画どおりの mute 操作ができない
2. quory の `recovery-exec` shell がまだ `/usr/sbin/nologin` のため、
   authy / monnie からの SSH forced command が dispatch script へ到達しない

## 前提

phase 1 / 2 は `2026-07-03_004_test_result.md` で OK 済み。

- 静的検証: OK
- `recovery_push_setup.yml -l quory -e tester_mode=true`: OK、changed=0

本ファイルは、commit `32d9a78` 反映後の phase 3 以降の再実施結果を記録する。

## phase 3: 本番デプロイ再実施

```bash
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local \
ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote \
ANSIBLE_SSH_CONTROL_PATH_DIR=/tmp/ansible-cp \
ANSIBLE_SSH_COMMON_ARGS='-F /dev/null' \
ansible-playbook playbooks/recovery_push_setup.yml -l quory
```

結果: OK。

```text
PLAY RECAP *********************************************************************
quory : ok=31 changed=2 unreachable=0 failed=0 skipped=1 rescued=0 ignored=0
```

ただし task name の Jinja 評価警告は残っている。

```text
Encountered 1 template error.
error 1 - 'svc' is undefined
```

対象 task:

- `Create drop-in directory for {{ svc }} on {{ push_target.name }}`
- `Deploy recovery-trigger drop-in for {{ svc }} on {{ push_target.name }}`

loop 自体は実行され、対象ファイルは配置済み。警告は task name の評価タイミングによるもの。
運用上のノイズとして修正推奨。

### quory 配布物確認

```text
recovery-exec:x:1004:1006:Recovery Codex runner service account:/home/recovery-exec:/usr/sbin/nologin
missing:/usr/local/bin/homelab-mute
-rwxr-xr-x root:root /usr/local/sbin/recovery-push-dispatch.sh
-rw------- recovery-exec:recovery-exec /home/recovery-exec/.ssh/authorized_keys
authorized_key_lines=2
```

`authorized_keys` は 2 行あり、authy / monnie の forced command entry は配備済み。

### authy 配布物確認

```text
OnFailure=recovery-trigger@freeradius.service.service
-rwxr-xr-x root:root /usr/local/sbin/recovery-push.sh
-rw------- root:root /etc/homelab-recovery/push-key
-rw-r--r-- root:root /etc/homelab-recovery/push-known_hosts
-rw-r--r-- root:root /etc/systemd/system/recovery-trigger@.service
-rw-r--r-- root:root /etc/systemd/system/freeradius.service.d/recovery-trigger.conf
```

drop-in 内容は E-2 の `OnFailure=recovery-trigger@%n.service` に更新済み。

### monnie 配布物確認

```text
OnFailure=recovery-trigger@prometheus.service.service
-rwxr-xr-x root:root /usr/local/sbin/recovery-push.sh
-rw------- root:root /etc/homelab-recovery/push-key
-rw-r--r-- root:root /etc/homelab-recovery/push-known_hosts
-rw-r--r-- root:root /etc/systemd/system/recovery-trigger@.service
-rw-r--r-- root:root /etc/systemd/system/prometheus.service.d/recovery-trigger.conf
```

drop-in 内容は E-2 の `OnFailure=recovery-trigger@%n.service` に更新済み。

## phase 4: 手動 push テスト

未実施。

理由:

- 計画では push 前に quory で `homelab-mute set ...` を実行する必要があるが、
  quory に `/usr/local/bin/homelab-mute` が存在しない
- quory の `recovery-exec` shell が `/usr/sbin/nologin` のままであり、
  sender から SSH しても forced command が dispatch script に到達できない

今回の確認:

```text
recovery-exec:x:1004:1006:Recovery Codex runner service account:/home/recovery-exec:/usr/sbin/nologin
missing:/usr/local/bin/homelab-mute
dispatch_log: none
```

前回実行時の push 失敗:

```text
authy | FAILED | rc=1 >>
This account is currently not available.

monnie | FAILED | rc=1 >>
This account is currently not available.
```

同じ前提状態のため、今回も mute あり push / lock push は実施していない。
mute なし push は計画どおり実施していない。

## phase 5: 後片付け確認

```bash
ansible quory -b -m ansible.builtin.shell -a '
  echo dispatch_log
  ls -l /var/lib/homelab-recovery/push/dispatch.log 2>/dev/null || true
  tail -n 20 /var/lib/homelab-recovery/push/dispatch.log 2>/dev/null || true
  echo mute_files
  find /var/lib/homelab-recovery/mute -maxdepth 1 -type f -print 2>/dev/null | sort
  echo locks
  find /var/lib/homelab-recovery/push -maxdepth 1 -type d -name "*.lock" -print 2>/dev/null | sort
'
```

結果:

```text
dispatch_log
mute_files
locks
```

dispatch log なし、mute file なし、push lock なし。

## 未達事項

- authy push + mute の dispatch log `muted ... skip`
- monnie push + mute の dispatch log `muted ... skip`
- authy lock 時の dispatch log `push session already running — skip`
- recovery-exec プロセスなし確認は、dispatch まで到達していないため有意な確認にならず

## 総合判定

E-2 の配布 playbook自体は完走し、push 用ファイルと OnFailure drop-in は配置された。
しかし push 実行経路はまだ成立していない。

修正が必要な点:

1. quory に `homelab-mute` を配布する、または E-2 の前提として E-1 quory 配布を明示して実施する
2. quory 実機の `recovery-exec` shell を `/bin/sh` に更新する手順を E-2 deploy に含めるか、
   E-2 前提手順として `recovery_exec_setup` 等を実施する
3. `sender_setup.yml` の loop task name で `svc` 未定義警告が出ないよう、task name を固定化するか
   loop 表示を `loop_control.label` に寄せる

上記対応後、phase 4 の mute あり push / lock skip を再実施する必要がある。
