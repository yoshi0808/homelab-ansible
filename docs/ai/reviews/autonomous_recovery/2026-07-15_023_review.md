# pve1/pve2 調査コマンド Phase 2 — 再レビュー結果

- 日付: 2026-07-15
- 対象 requirement: `docs/ai/reviews/autonomous_recovery/2026-07-15_019_requirement.md`
- 前回 review: `docs/ai/reviews/autonomous_recovery/2026-07-15_021_review.md`
- 対象 implement: `docs/ai/reviews/autonomous_recovery/2026-07-15_022_implement.md`
- 担当: `reviewer`

## 結論

**approve。前回の must-fix 2件は解消した。tester 工程へ進めてよい。**

must-fix / suggestion は0件。非ブロッキングの nit が1件ある。

## 前回 findings の確認

### 解消: 正本 policy と Phase 2 実装の矛盾

`docs/ai/prompts/autonomous_recovery_policy.md` §4.6 は、次の実態へ同期された。

- 引数なし固定12 checkと、パラメータ付き9 checkを過不足なく列挙。
- wrapperを一次フィルタ、pve forced-command dispatchを本ゲートとする二段検証を明記。
- exact arity、完全一致regex / allowlist、limit 1..2000、過剰トークン拒否を明記。
- sudo argvを絶対path・固定read-only動詞・検証済みoperandで位置固定し、`eval`や
  command文字列の再解釈を禁止。
- sudoers wildcardを検証済みoperand位置だけに限定し、`qm *`、`pvenode task *`、
  `journalctl -u *`を禁止。
- `zfs-list-vm`の固定`zfs list` rule再利用と、pve1/pve2 investigate-only制約を維持。
- 初期3 checkの実績と、追加checkについてtesterで確認すべきpath / unit / target側
  sudoers grammarの境界を区別。

§6 の「3 checkのみ」という旧記述も固定12 + パラメータ付き9へ更新された。
`roles/recovery_exec/defaults/main.yml` と
`roles/recovery_exec/tasks/target_setup.yml` の前回指摘箇所も実態へ同期されている。

### 解消: journal unit の pre-deploy NOTE

`roles/recovery_exec/templates/sudoers-recovery-exec-pve-target.j2` の NOTE は、
`pvesr` / `pvestatd` / `pve-cluster` / `corosync` / `pvedaemon` の全5 unitを
`systemctl list-units --all` で確認する記述へ更新された。存在しない unit が
exit 0 + 空出力になり得る注意も維持されている。

## Findings

### nit: 一部の見出し・task名が旧い「cluster/HA status」表現のまま

- `roles/recovery_exec/tasks/main.yml:56`
- `roles/recovery_exec/tasks/main.yml:158`
- `roles/recovery_exec/templates/AGENTS.md.j2:78`

上記は調査範囲が replication / VM / task / storage / ZFS / journalまで拡張された後も、
見出しまたはAnsible task名が `cluster/HA status` とだけ記述されている。本文、allowlist、
policy、実行コードは正しく、権限境界や機能に影響しないためtester移行を妨げない。
将来の読解時の誤解を避けるため、次の文書整理時に `Proxmox read-only investigation`
相当へ揃えるとよい。

## reviewer 実施確認

- requirement / 前回review / 最新implement / `git diff` の突合
- 固定12 check、パラメータ付き9 check、unit 5種、window 6種のpolicyとの一致確認
- `git diff --check`: PASS
- `bash -n`（dispatch / wrapper）: PASS
- `scripts/check-tester-gate.sh`: `[tester-gate-lint] OK (33 playbooks)`
- 実ホスト接続、Ansible APPLY、本番操作: 未実施

## tester への残存 gate

- pve1 / pve2 両方の target側 `visudo -cf` が Phase 2 ruleを受理すること。
- `command -v` と dispatch / sudoers の絶対pathが一致すること。
- 全5 journal unitが実在すること。
- forced-command + no-pty経路の `sudo -n` と、新9 / 既存12 / 初期3 checkが成功すること。
- requirement記載のinvalid matrixがsudo到達前に拒否されること。

## Next step files

- `docs/ai/reviews/autonomous_recovery/2026-07-15_019_requirement.md`
- `docs/ai/reviews/autonomous_recovery/2026-07-15_021_review.md`
- `docs/ai/reviews/autonomous_recovery/2026-07-15_022_implement.md`
- `docs/ai/reviews/autonomous_recovery/2026-07-15_023_review.md`
