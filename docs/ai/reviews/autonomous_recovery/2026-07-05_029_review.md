# Review

対象:
- docs/ai/reviews/autonomous_recovery/2026-07-05_026_requirement.md
- docs/ai/reviews/autonomous_recovery/2026-07-05_027_implement.md
- docs/ai/reviews/autonomous_recovery/2026-07-05_028_review.md
- 現在の git diff

## 結論

再レビューの結果、追加の指摘はありません。

前回レビューの suggestion 2件・nit 1件はいずれも反映されています。`homelab-reports` は `reports/recovery_investigations/<target>/` の1段ネストを扱えるようになり、`list-reports` は JSON のみを列挙し、`recovery-reports-helper` 側でも引数個数を独立して検証しています。

このまま tester 工程へ進めてよい内容です。ただし実機反映・実機テストはまだ行われていないため、次工程では quory 上での sudoers / reports / Semaphore DB / Codex execpolicy の動作確認が必要です。

## 前回指摘への確認

### 解消: `homelab-reports` が既存の自律復旧レポート階層を読めない

- `roles/recovery_exec/files/homelab-reports`
- `roles/recovery_exec/files/recovery-reports-helper`

`list-reports <playbook> [target]` / `show-report <playbook> [target] <filename>` が追加され、`target` は `NAME_RE` で検証されています。`realpath -e` による under-base 確認も維持されており、追加 path segment は1つだけです。

ansy 上の checked-in `reports/` ツリーで、`recovery_investigations` 直下に `sophos-fw/` が表示され、`recovery_investigations sophos-fw` 配下の JSON を表示できることを確認しました。

### 解消: `list-reports` は JSON 以外も列挙する

- `roles/recovery_exec/files/recovery-reports-helper`

`find` に `-name '*.json'` が追加され、`reports/radius-health/README.md` のような非 JSON ファイルは列挙されなくなっています。`show-report radius-health README.md` は従来通り `denied: bad filename` で拒否されます。

### 解消: privileged helper 側も引数個数を厳密化すると説明と揃う

- `roles/recovery_exec/files/recovery-reports-helper`

`list-playbooks` / `list-reports` / `show-report` それぞれで `#` を確認するようになり、Codex-facing wrapper だけに依存しない形になっています。

## 確認したこと

- 新規 shell 3本は `bash -n` で syntax error なし。
- `target` に `..` を渡す試行は `denied: bad target name` で拒否されました。
- `recovery_investigations/sophos-fw/20260702_113322Z.json` は新しい4引数形式で表示できました。
- `radius-health` の `list-reports` は `.json` のみを返しました。
- 追加された AGENTS.md / policy の説明は、実装された `[target]` 形式と一致しています。

## 実行した確認コマンド

```bash
bash -n roles/recovery_exec/files/homelab-reports roles/recovery_exec/files/recovery-reports-helper roles/recovery_exec/files/homelab-semaphore-query
bash roles/recovery_exec/files/recovery-reports-helper list-reports recovery_investigations
bash roles/recovery_exec/files/recovery-reports-helper list-reports recovery_investigations sophos-fw
bash roles/recovery_exec/files/recovery-reports-helper show-report recovery_investigations sophos-fw 20260702_113322Z.json | head -20
bash roles/recovery_exec/files/recovery-reports-helper list-reports radius-health
bash roles/recovery_exec/files/recovery-reports-helper show-report radius-health README.md
bash roles/recovery_exec/files/recovery-reports-helper show-report recovery_investigations '..' 'x.json'
```

## Next step files

- docs/ai/reviews/autonomous_recovery/2026-07-05_026_requirement.md
- docs/ai/reviews/autonomous_recovery/2026-07-05_027_implement.md
- docs/ai/reviews/autonomous_recovery/2026-07-05_028_review.md
- docs/ai/reviews/autonomous_recovery/2026-07-05_029_review.md
