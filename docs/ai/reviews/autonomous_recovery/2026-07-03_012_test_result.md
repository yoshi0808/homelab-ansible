# 自律復旧トリガ基盤 E-3 dispatch 修正後 drill 再テスト結果 - 2026-07-03

## 結論

quory 最新 commit `581dd44` を前提に、Claude Code から指示された dispatch 修正後の再テストを実施した。

結果は **OK**。

- `recovery_push_setup.yml -l quory` 再デプロイ: OK
- quory の `/usr/local/sbin/recovery-push-dispatch.sh` は shell builtin の `exec "${WRAPPER}"` を使っていないことを確認
- authy drill: `firing` と `session ended` を確認、lock 残留なし
- monnie drill: `firing` と `session ended` を確認、lock 残留なし
- authy / monnie とも Codex 調査は正常サービスを確認し、recover は実行していない
- 後片付け: mute なし、lock なし、`recovery-exec` process なし、実サービス active

## 前提

- commit: `581dd44`
- 既存 E-3 結果: `docs/ai/reviews/autonomous_recovery/2026-07-03_011_test_result.md`
- 修正内容: `roles/recovery_push/files/recovery-push-dispatch.sh` の wrapper 起動から shell builtin `exec` を削除

共通環境:

```bash
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local
ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote
ANSIBLE_SSH_CONTROL_PATH_DIR=/tmp/ansible-cp
ANSIBLE_SSH_COMMON_ARGS='-F /dev/null'
```

## ステップ1: dispatch.sh 再デプロイ

```bash
ansible-playbook playbooks/recovery_push_setup.yml -l quory
```

結果: OK。

```text
TASK [recovery_push : Deploy recovery-push-dispatch.sh on quory] ***************
changed: [quory]

PLAY RECAP *********************************************************************
quory : ok=31 changed=3 unreachable=0 failed=0 skipped=1 rescued=0 ignored=0
```

`changed=3` の内訳は dispatcher 更新と authy/monnie の push known_hosts 更新。

## ステップ2: 再デプロイ確認

```bash
ansible quory -b -m ansible.builtin.shell \
  -a 'grep -n "exec" /usr/local/sbin/recovery-push-dispatch.sh || echo "exec line not found"; tail -n 8 /usr/local/sbin/recovery-push-dispatch.sh'
```

結果:

```text
3:# Invoked when authy/monnie OnFailure trigger SSHes in as recovery-exec.
11:WORKSPACE="/var/lib/recovery-exec/workspace"
12:WRAPPER="/usr/local/bin/codex-exec-wrapper"
50:"${WRAPPER}" exec --cd "${WORKSPACE}" \

trap 'rmdir "${LOCK_DIR}" 2>/dev/null || true; log "session ended"' EXIT

log "firing: Codex investigation for ${TARGET}"

"${WRAPPER}" exec --cd "${WORKSPACE}" \
    "${TARGET} でサービス障害を OnFailure で検知しました。調査・復旧してください。"
```

`exec "${WRAPPER}" ...` の shell builtin 置換は消えている。
残っている `exec` は wrapper の subcommand 名とコメント / パス文字列のみ。

## ステップ3-5: authy drill 再実行

```bash
ansible authy -b -m ansible.builtin.systemd \
  -a 'name=recovery-drill-authy.service state=started'
```

Ansible module は `ExecStart=/bin/false` により failed を返した。drill として期待どおり。

dispatch.log:

```text
2026-07-03T07:15:48+09:00 PUSH authy: firing: Codex investigation for authy
2026-07-03T07:16:07+09:00 PUSH authy: session ended
```

所要時間: 19 秒。

polling 中の初回は `authy.lock` と Codex process を確認した。

```text
/var/lib/homelab-recovery/push/authy.lock
node /usr/bin/codex exec ...
```

`session ended` 後は lock なし。

authy trigger journal の Codex 最終応答:

```text
調査結果:
1. `homelab-investigate-authy status`
   - `freeradius`: active/running
   - `sshd`: active/running
復旧対応:
- 実施していません。
- 手順上、対象サービスが非 active の場合のみ `homelab-recover-authy` を実行できますが、`freeradius` と `sshd` はどちらも稼働中でした。
現在の状態:
- authy の主要サービス状態: 正常
```

確認:

```text
journalctl -u freeradius --since "2026-07-03 07:15:00" | grep -i onfailure
none

freeradius=active
```

## ステップ6: monnie drill

```bash
ansible monnie -b -m ansible.builtin.systemd \
  -a 'name=recovery-drill-monnie.service state=started'
```

Ansible module は `ExecStart=/bin/false` により failed を返した。drill として期待どおり。

dispatch.log:

```text
2026-07-03T07:16:46+09:00 PUSH monnie: firing: Codex investigation for monnie
2026-07-03T07:17:18+09:00 PUSH monnie: session ended
```

所要時間: 32 秒。

polling 中の初回は `monnie.lock` と Codex process を確認した。

```text
/var/lib/homelab-recovery/push/monnie.lock
node /usr/bin/codex exec ...
```

`session ended` 後は lock なし。

monnie trigger journal の Codex 最終応答:

```text
調査完了しました。
1. 調査結果
`monnie status` を確認し、`prometheus` / `grafana-server` / `loki` / `unpoller` はすべて `active (running)` でした。
追加で `failed`、Prometheus targets、Grafana health、Loki、ports、memory、disk を確認しました。
2. 復旧対応
対象サービスの非 active は確認されなかったため、規定に従い `homelab-recover-monnie` は実行していません。
3. 現在状態
正常です。
```

確認:

```text
grafana-server=active
prometheus=active
loki=active
unpoller=active
```

## 最終状態

quory:

```text
mute_status
TARGET     STATE        UNTIL                      REASON
authy      -            -                          -
monnie     -            -                          -
sophos-fw  -            -                          -

locks
(なし)

dispatch_tail
2026-07-03T06:34:58+09:00 PUSH authy: muted (残 10 分, E-2 push test) — skip
2026-07-03T06:35:01+09:00 PUSH monnie: muted (残 10 分, E-2 push test) — skip
2026-07-03T06:35:04+09:00 PUSH authy: push session already running — skip
2026-07-03T06:59:01+09:00 PUSH authy: firing: Codex investigation for authy
2026-07-03T07:15:48+09:00 PUSH authy: firing: Codex investigation for authy
2026-07-03T07:16:07+09:00 PUSH authy: session ended
2026-07-03T07:16:46+09:00 PUSH monnie: firing: Codex investigation for monnie
2026-07-03T07:17:18+09:00 PUSH monnie: session ended

recovery_exec_processes
(なし)
```

authy:

```text
freeradius=active
recovery-drill-authy.service is-failed: inactive
```

monnie:

```text
grafana-server=active
prometheus=active
loki=active
unpoller=active
recovery-drill-monnie.service is-failed: inactive
```

## 総合判定

`exec` 削除修正により、dispatcher の `EXIT` trap が実行されるようになった。

authy / monnie の両方で:

- `firing`
- Codex 調査完了
- `session ended`
- lock 解放

を確認できたため、E-3 push drill の未解決だった完了処理問題は解消した。
