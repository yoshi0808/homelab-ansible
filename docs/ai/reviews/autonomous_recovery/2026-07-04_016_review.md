# 自律復旧 Slack mute コマンド(自然言語)レビュー — 2026-07-04

対象:

- `docs/ai/reviews/autonomous_recovery/2026-07-04_015_implement.md`
- `roles/recovery_exec/files/homelab-mute-exec`
- `roles/recovery_exec/templates/sudoers-recovery-exec-mute.j2`
- `roles/recovery_exec/templates/codex-config.toml.j2`
- `roles/recovery_exec/templates/AGENTS.md.j2`
- `roles/recovery_exec/tasks/main.yml`

## 判定

must-fix あり。現状では commit / push しない。

## 指摘

### must-fix: `codex-config.toml.j2` が `git diff --check` に失敗する

`roles/recovery_exec/templates/codex-config.toml.j2:26` に EOF 直前の余分な空行があり、
`git diff --check` が以下で失敗する。

```text
roles/recovery_exec/templates/codex-config.toml.j2:26: new blank line at EOF.
```

このままだと通常の commit 前チェックに乗せたときに止まるため、末尾の余分な空行を削除してほしい。

## 確認済み

- `bash -n roles/recovery_exec/files/homelab-mute-exec`: OK
- `ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ansible-playbook --syntax-check playbooks/recovery_exec_setup.yml`: OK
- `scripts/check-tester-gate.sh`: OK (33 playbooks)
- sudoers 断片を `recovery-exec ALL = (root) NOPASSWD: /usr/local/bin/homelab-mute` として `visudo -cf -`: OK
- `git diff --check -- ...`: 上記 must-fix で FAIL

## 補足

設計方針自体は妥当。

- `homelab-mute-exec` を execpolicy の許可対象にし、root 昇格は sudoers で `/usr/local/bin/homelab-mute` に限定している。
- `homelab-mute` 側で target と minutes の検証があり、恒久 mute は作れない。
- AGENTS.md で mute と recovery の用途を分けて説明している点も、誤操作抑止として有効。
