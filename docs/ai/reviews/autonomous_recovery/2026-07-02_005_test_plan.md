# 自律復旧トリガ基盤 E-1 テスト計画 — 2026-07-02

対象実装: 2026-07-02_002_implement.md(レビュー: 003 = must-fix 1件対応済み、004 = 再レビュー)

**承認: Yoshinobu(2026-07-02)。全項目実施。**

## 前提: 役割分担

- **ansy(dev)への実配布は Claude Code(実装者)が実施**する(§3.2: ansy は開発環境)。
  tester は「playbook 実行時は必ず `-e tester_mode=true`」の原則(core.md §18)を維持し、
  配布済み環境の検証と drill を担当する。
- quory(本番)への配布・enable は本テスト完了後の Yoshinobu 適用判断(テスト対象外)。

## フェーズ 0(Claude Code 実施・tester の前提)

1. `ansible-playbook playbooks/recovery_probe_setup.yml -l ansy`(通常実行、dev 配布)
   - service は配置のみで enable しない(playbook の既定動作)

## フェーズ 1: 静的検証(tester)

1. `scripts/check-tester-gate.sh` → OK(31 playbooks)
2. `recovery_probe_setup.yml` / `recovery_probe_notify.yml` の syntax-check
3. `bash -n /usr/local/bin/homelab-mute`(配布物)、
   `python3 -c "import ast; ast.parse(open('/usr/local/sbin/recovery-probe.py').read())"`

## フェーズ 2: ゲート検証(tester、`-e tester_mode=true`)

| コマンド | 期待 |
|---|---|
| `recovery_probe_setup.yml -l ansy -e tester_mode=true` | tester_gate が plan 表示 → `end_host`、changed=0(配布タスク未到達) |
| `recovery_probe_notify.yml -e tester_mode=true -e nt_status=ok -e nt_title=t -e nt_message=t` | common_slack ガードで Slack 送信タスク skip |

## フェーズ 3: 配布物の検証(tester、ansy 上・read-only 中心)

1. 配置確認: /usr/local/bin/homelab-mute、/usr/local/sbin/recovery-probe.py、
   /etc/homelab-recovery/recovery-probe.json、recovery-probe.service(**enabled でない**こと)
2. homelab-mute 往復:
   - `sudo homelab-mute set authy 3 "E-1 test"` → status で MUTED 表示
   - `sudo homelab-mute set authy 1 ...` → **until が縮まない**こと
   - `sudo homelab-mute clear authy` → 解除確認
   - 不正 target(`set pve1 5`)が拒否されること
3. `RECOVERY_PROBE_CONFIG=/etc/homelab-recovery/recovery-probe.json /usr/local/sbin/recovery-probe.py --once`
   → 3 対象すべて PROBE OK(read-only)。EXTERNAL unreachable が**出ない**こと

## フェーズ 4: drill(tester、実障害なしの全経路検証)

1. mute 連動: `sudo homelab-mute set sophos-fw 10 "drill mute test"` →
   drill フラグ(`touch /var/lib/homelab-recovery/probe/drill/sophos-fw`)→
   probe を前面実行(1〜2 サイクル)→ **muted skip でカウントされない**こと → mute clear
2. drill 本番: drill フラグを置き、probe を前面実行(`timeout 600` 程度):
   - 60 秒×5 回の FAIL カウント進行
   - pvesh 確証(running)→ `recovery_vm_reboot.yml -e target=sophos-fw -e tester_mode=true`
     が実行され、L2 ゲート(実データ plan 表示)で停止、changed=0
   - [drill] ラベル付き通知がキュー → flush で Slack 実送信される(drill 通知は送る設計)
   - drill フラグが自動削除される
3. 実 reboot / start / relocate が発生していないこと、[drill] 以外の Slack 送信がないことを明記

## フェーズ 5: 後片付け(tester)

- drill フラグ・テスト mute の残骸なし、notify-queue が空、firings-sophos-fw.json の
  drill 発火記録を確認(flapping カウントに drill が乗る点は仕様。3 回以上 drill を
  連続実行しないこと)

## 実施しないこと

- quory への配布・enable(Yoshinobu の適用判断後)
- 実障害の誘発(sophos-fw の実停止等)
- stopped → start 分岐の実機検証(VM を実際に stop する必要があるため対象外。
  コードレビューと将来の計画メンテ時の drill で担保)

## 結果

- `docs/ai/reviews/autonomous_recovery/2026-07-02_006_test_result.md` に保存
