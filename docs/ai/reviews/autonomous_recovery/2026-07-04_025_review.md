# Re-review: autonomous recovery deploy runbook final fixes - 2026-07-04

対象:

- `docs/ai/prompts/autonomous_recovery_deploy_runbook.md`
- `docs/ai/prompts/autonomous_recovery_policy.md`
- `roles/recovery_io/tasks/main.yml`
- `docs/ai/reviews/autonomous_recovery/2026-07-04_024_review.md`

## 判定

must-fix なし。

024 の残り must-fix / nit は解消済み。現状の staged 内容は commit 可能と判断する。

## 確認結果

### 解消: イレギュラー対応の再セットアップ手順

target ノード停止後の再セットアップ手順は `-l quory` 固定になり、通常復旧で ansy 鍵を authy/monnie に上書きする導線はなくなった。

- `docs/ai/prompts/autonomous_recovery_deploy_runbook.md:160`
- `docs/ai/prompts/autonomous_recovery_deploy_runbook.md:165`

ansy 開発時の扱いも別記され、`targets=false` のままにするか、意図的に ansy 鍵を検証した場合は quory で戻す、という注意が入っている。

- `docs/ai/prompts/autonomous_recovery_deploy_runbook.md:170`

### 解消: 「既定」表現の誤読

1章の ansy 手順は、変数の既定値が `true` であることと、この手順では明示的に `false` を指定することが分かる表現になった。

- `docs/ai/prompts/autonomous_recovery_deploy_runbook.md:23`
- `docs/ai/prompts/autonomous_recovery_deploy_runbook.md:25`

### 確認: `recovery_io` role

`recovery_io_service_enabled=true` の場合に `recovery-io` が start される変更が入っている。

- `roles/recovery_io/tasks/main.yml:144`
- `roles/recovery_io/tasks/main.yml:148`

`ansible-playbook --syntax-check playbooks/recovery_io_setup.yml` は OK。

### 確認: policy 文面

以前の suggestion / nit も反映済み。

- §2 の ping ラダー要約は pvesh 確証後の分岐を含む表現になった。
- §5.3 の Slack 経路は `sudo -H -u recovery-exec` に修正された。
- §6 の Codex 安全設計は execpolicy の説明に寄った。
- §8 の手動レイヤーは、probe/健全性を発火条件にしないが safety gate は維持する、という実装に合う表現になった。

## 確認コマンド

- `ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote ansible-playbook --syntax-check playbooks/recovery_io_setup.yml`: OK
- `git diff --cached --check`: OK

## 残リスク

レビュー証跡ファイル `2026-07-04_022_review.md` から `2026-07-04_025_review.md` が untracked のまま見えている。commit 時には必要なレビュー証跡を staged に含めること。

## 結論

runbook / policy / `recovery_io` task の現行 staged 差分に must-fix はない。commit へ進めてよい。

Next step files:
- docs/ai/prompts/autonomous_recovery_deploy_runbook.md
- docs/ai/prompts/autonomous_recovery_policy.md
- roles/recovery_io/tasks/main.yml
- docs/ai/reviews/autonomous_recovery/2026-07-04_025_review.md
