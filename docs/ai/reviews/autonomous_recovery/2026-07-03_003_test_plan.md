# 自律復旧トリガ基盤 E-2 テスト計画 — 2026-07-03

対象実装: 2026-07-03_001_implement.md(レビュー: 002 = must-fix 1件対応済み、commit 1b74509)

**承認: Yoshinobu(2026-07-03)。フェーズ3は本番デプロイ承認済み。フェーズ4は mute あり確認のみ。**

## 前提: 役割分担

- **フェーズ 0〜2**: tester が ansy で実施(本番接続不要)
- **フェーズ 3〜4**: tester が quory / authy / monnie に接続して実施(本番デプロイ承認済み)
- フェーズ 3 完了後、drop-in が active になるため authy/monnie の各サービスに OnFailure フックが付く

## 実施しないこと

- `mute なし`での手動 push(Codex セッション実行を伴うため)
  → OnFailure → Codex 全経路検証は E-3 drill スコープ、かつ Notion の残タスクとして記録

## フェーズ 1: 静的検証(tester、ansy)

1. `scripts/check-tester-gate.sh` → OK (32 playbooks)
2. `ansible-playbook --syntax-check playbooks/recovery_push_setup.yml`
3. `bash -n roles/recovery_push/files/recovery-push-dispatch.sh`
4. `bash -n roles/recovery_push/files/recovery-push.sh`

## フェーズ 2: gate 検証(tester、`-e tester_mode=true`)

```bash
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local \
ansible-playbook playbooks/recovery_push_setup.yml \
  -l quory -e tester_mode=true
```

確認内容:
- tester_gate が plan 表示 → `end_host` で停止
- changed=0
- dispatch script / authorized_keys / delegate 先 VM タスクに未到達

## フェーズ 3: 本番デプロイ(tester、quory → 自動で authy/monnie に delegate)

```bash
ansible-playbook playbooks/recovery_push_setup.yml -l quory
```

確認内容:
1. quory に `/usr/local/sbin/recovery-push-dispatch.sh` (root:root 0755)
2. quory に `/home/recovery-exec/.ssh/authorized_keys` (recovery-exec:recovery-exec 0600)
   - authy/monnie の push key エントリが 2 行あること
3. authy / monnie それぞれ:
   - `/etc/homelab-recovery/push-key` (root:root 0600)
   - `/etc/homelab-recovery/push-key.pub`
   - `/etc/homelab-recovery/push-known_hosts`
   - `/usr/local/sbin/recovery-push.sh` (root:root 0755)
   - `/etc/systemd/system/recovery-trigger@.service` (root:root 0644)
   - drop-in: authy = freeradius.service.d/recovery-trigger.conf
   - drop-in: monnie = grafana-server/prometheus/loki/unpoller それぞれ
4. drop-in 反映確認(authy):
   `systemctl show freeradius --property=OnFailure`
   → `OnFailure=recovery-trigger@freeradius.service` と表示されること
5. drop-in 反映確認(monnie / prometheus を代表として確認):
   `systemctl show prometheus --property=OnFailure`
   → `OnFailure=recovery-trigger@prometheus.service` と表示されること

## フェーズ 4: 手動 push テスト — mute あり(SSH 経路・dispatch ログ確認)

### 4-1: authy push + mute 連動

```bash
# quory で mute
sudo homelab-mute set authy 10 "E-2 push test"

# authy で手動 push
ssh authy sudo /usr/local/sbin/recovery-push.sh
```

確認内容:
- quory の `/var/lib/homelab-recovery/push/dispatch.log` に
  「muted (残 〜 分, E-2 push test) — skip」が記録されること
- Codex セッションが起動しないこと(recovery-exec プロセスなし)

```bash
sudo homelab-mute clear authy
```

### 4-2: monnie push + mute 連動

```bash
sudo homelab-mute set monnie 10 "E-2 push test"
ssh monnie sudo /usr/local/sbin/recovery-push.sh
# → dispatch.log に monnie muted skip が記録されること
sudo homelab-mute clear monnie
```

### 4-3: push session lock 確認

```bash
# 手動でロックディレクトリを置いてから push
ssh quory sudo mkdir -p /var/lib/homelab-recovery/push/authy.lock
ssh authy sudo /usr/local/sbin/recovery-push.sh
# → dispatch.log に「push session already running — skip」が記録されること
ssh quory sudo rmdir /var/lib/homelab-recovery/push/authy.lock
```

## フェーズ 5: 後片付け確認

- mute が全て clear されていること(`homelab-mute status`)
- `/var/lib/homelab-recovery/push/authy.lock` が存在しないこと
- quory の dispatch.log を保存(証跡)

## 結果

`docs/ai/reviews/autonomous_recovery/2026-07-03_004_test_result.md` に保存
