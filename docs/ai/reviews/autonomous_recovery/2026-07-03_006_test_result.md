# 自律復旧トリガ基盤 E-2 追加デプロイ・phase 4/5 テスト結果 — 2026-07-03

## 結論

quory 最新 commit `3339ebd` を前提に、Claude Code から指示された追加デプロイと
`2026-07-03_003_test_plan.md` の phase 4/5 を実施した。

- 追加デプロイ 1: `recovery_exec_setup.yml -l quory` OK
- 追加デプロイ 2: `recovery_probe_setup.yml -l quory` OK
- 追加デプロイ 3: `recovery_push_setup.yml -l quory` OK
- authy / monnie の OnFailure は `%p` 版に更新され、二重 `.service` は解消
- authy mute あり push: dispatch.log に muted skip 記録 OK
- monnie mute あり push: dispatch.log に muted skip 記録 OK
- push session lock: dispatch.log に already running skip 記録 OK
- 後片付け: mute 全解除、lock なし、recovery-exec の Codex プロセスなし

mute なし push は計画どおり実施していない。

## 追加デプロイ

共通環境:

```bash
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local
ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote
ANSIBLE_SSH_CONTROL_PATH_DIR=/tmp/ansible-cp
ANSIBLE_SSH_COMMON_ARGS='-F /dev/null'
```

### 1. recovery-exec shell 更新

```bash
ansible-playbook playbooks/recovery_exec_setup.yml -l quory
```

結果: OK。

```text
PLAY RECAP *********************************************************************
quory : ok=24 changed=2 unreachable=0 failed=0 skipped=1 rescued=0 ignored=0
```

確認:

```text
recovery-exec:x:1004:1006:Recovery Codex runner service account:/home/recovery-exec:/bin/sh
```

### 2. homelab-mute quory 配布

```bash
ansible-playbook playbooks/recovery_probe_setup.yml -l quory
```

結果: OK。

```text
PLAY RECAP *********************************************************************
quory : ok=11 changed=6 unreachable=0 failed=0 skipped=3 rescued=0 ignored=0
```

確認:

```text
-rwxr-xr-x root:root /usr/local/bin/homelab-mute
```

`recovery_probe_service_enabled=false` のため、probe service は enable/start されていない。

### 3. recovery_push 再デプロイ

```bash
ansible-playbook playbooks/recovery_push_setup.yml -l quory
```

結果: OK。

```text
PLAY RECAP *********************************************************************
quory : ok=31 changed=4 unreachable=0 failed=0 skipped=1 rescued=0 ignored=0
```

前回まで出ていた `svc is undefined` の task name 警告は出なかった。

## デプロイ後確認

### quory

```text
recovery-exec:x:1004:1006:Recovery Codex runner service account:/home/recovery-exec:/bin/sh
-rwxr-xr-x root:root /usr/local/bin/homelab-mute
-rwxr-xr-x root:root /usr/local/sbin/recovery-push-dispatch.sh
-rw------- recovery-exec:recovery-exec /home/recovery-exec/.ssh/authorized_keys
authorized_key_lines=2
```

### authy

```text
OnFailure=recovery-trigger@freeradius.service
[Unit]
OnFailure=recovery-trigger@%p.service
-rwxr-xr-x root:root /usr/local/sbin/recovery-push.sh
-rw------- root:root /etc/homelab-recovery/push-key
-rw-r--r-- root:root /etc/homelab-recovery/push-known_hosts
-rw-r--r-- root:root /etc/systemd/system/recovery-trigger@.service
```

### monnie

```text
OnFailure=recovery-trigger@prometheus.service
[Unit]
OnFailure=recovery-trigger@%p.service
-rwxr-xr-x root:root /usr/local/sbin/recovery-push.sh
-rw------- root:root /etc/homelab-recovery/push-key
-rw-r--r-- root:root /etc/homelab-recovery/push-known_hosts
-rw-r--r-- root:root /etc/systemd/system/recovery-trigger@.service
```

`systemctl show` で二重 `.service` は出ていない。

## phase 4: 手動 push テスト

### 4-1 authy mute あり push

```bash
ansible quory -b -m ansible.builtin.command \
  -a "homelab-mute set authy 10 E-2 push test"
ansible authy -b -m ansible.builtin.command \
  -a "/usr/local/sbin/recovery-push.sh"
ansible quory -b -m ansible.builtin.command \
  -a "homelab-mute clear authy"
```

結果: OK。

dispatch.log:

```text
2026-07-03T06:34:58+09:00 PUSH authy: muted (残 10 分, E-2 push test) — skip
```

Codex セッションは起動していない。

### 4-2 monnie mute あり push

```bash
ansible quory -b -m ansible.builtin.command \
  -a "homelab-mute set monnie 10 E-2 push test"
ansible monnie -b -m ansible.builtin.command \
  -a "/usr/local/sbin/recovery-push.sh"
ansible quory -b -m ansible.builtin.command \
  -a "homelab-mute clear monnie"
```

結果: OK。

dispatch.log:

```text
2026-07-03T06:35:01+09:00 PUSH monnie: muted (残 10 分, E-2 push test) — skip
```

Codex セッションは起動していない。

### 4-3 push session lock

```bash
ansible quory -b -m ansible.builtin.command \
  -a "mkdir -p /var/lib/homelab-recovery/push/authy.lock"
ansible authy -b -m ansible.builtin.command \
  -a "/usr/local/sbin/recovery-push.sh"
ansible quory -b -m ansible.builtin.command \
  -a "rmdir /var/lib/homelab-recovery/push/authy.lock"
```

結果: OK。

dispatch.log:

```text
2026-07-03T06:35:04+09:00 PUSH authy: push session already running — skip
```

## phase 5: 後片付け確認

```bash
ansible quory -b -m ansible.builtin.shell -a '
  homelab-mute status
  find /var/lib/homelab-recovery/push -maxdepth 1 -type d -name "*.lock" -print
  find /var/lib/homelab-recovery/mute -maxdepth 1 -type f -print
  cat /var/lib/homelab-recovery/push/dispatch.log
  pgrep -a -u recovery-exec || true
'
```

結果:

```text
TARGET     STATE        UNTIL                      REASON
authy      -            -                          -
monnie     -            -                          -
sophos-fw  -            -                          -

locks: none
mute_files: none

2026-07-03T06:34:58+09:00 PUSH authy: muted (残 10 分, E-2 push test) — skip
2026-07-03T06:35:01+09:00 PUSH monnie: muted (残 10 分, E-2 push test) — skip
2026-07-03T06:35:04+09:00 PUSH authy: push session already running — skip

recovery_exec_processes: none
```

## 未実施事項

- mute なし push は実施していない(E-3 drill スコープ)。

## 総合判定

E-2 の追加デプロイと phase 4/5 は期待どおり完了した。

mute あり push と lock skip は dispatch まで到達し、Codex セッションを起動せずに
安全に skip されることを確認した。
