# 自律復旧トリガ基盤 E-3 再レビュー — 2026-07-03

対象:

- `docs/ai/reviews/autonomous_recovery/2026-07-03_007_implement.md`
- `docs/ai/reviews/autonomous_recovery/2026-07-03_008_review.md`
- `playbooks/recovery_push_drill_setup.yml`
- `roles/recovery_push/tasks/drill_setup.yml`
- `roles/recovery_push/files/recovery-drill-authy.service`
- `roles/recovery_push/files/recovery-drill-monnie.service`

## 判定

LGTM。

## must-fix 対応確認

`008_review` の must-fix だった `copy.src` 探索パス問題は解消済み。

`roles/recovery_push/tasks/drill_setup.yml` の `copy.src` が
`{{ playbook_dir }}/../roles/recovery_push/files/recovery-drill-{{ drill_target.name }}.service`
に変更され、`include_tasks` を playbook からパス指定しても unit file を解決できる。

## 確認済み

- `ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ansible-playbook --syntax-check playbooks/recovery_push_drill_setup.yml`: OK
- `ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ansible-playbook playbooks/recovery_push_drill_setup.yml -l quory -e tester_mode=true --check`: OK, changed=0, delegate 先 task 未到達
- `scripts/check-tester-gate.sh`: OK (33 playbooks)
- IP リテラル検査: 検出なし
- `ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote ANSIBLE_SSH_CONTROL_PATH_DIR=/tmp/ansible-cp ANSIBLE_SSH_COMMON_ARGS='-F /dev/null' ansible-playbook --check --diff playbooks/recovery_push_drill_setup.yml -l quory`: OK
  - authy: `recovery-drill-authy.service` copy 予定
  - monnie: `recovery-drill-monnie.service` copy 予定
  - failed=0

## レビュー観点

1. `OnFailure=recovery-trigger@%p.service` で drill unit 名から `%p` が展開され、E-2 の push 経路へ接続される設計は妥当。
2. `Type=oneshot` + `ExecStart=/bin/false` は start 時に確実に failed となり、OnFailure 発火用途として妥当。
3. tester_gate は plan 表示後に `end_play` し、delegate 先 task へ到達しないことを確認済み。
4. lint / IP check / syntax-check / delegated check-mode は上記のとおり確認済み。
