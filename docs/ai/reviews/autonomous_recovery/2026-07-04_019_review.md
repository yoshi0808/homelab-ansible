# 自律復旧 global monitoring pause リファクタレビュー — 2026-07-04

対象 staged 変更:

- `roles/recovery_exec/files/homelab-monitoring-{pause,resume,status}`
- `roles/recovery_exec/files/homelab-mute-exec` 削除
- `roles/recovery_exec/templates/sudoers-recovery-exec-mute.j2` 削除
- `roles/recovery_exec/tasks/main.yml`
- `roles/recovery_exec/defaults/main.yml`
- `roles/recovery_exec/templates/codex-config.toml.j2`
- `roles/recovery_exec/templates/AGENTS.md.j2`
- `roles/recovery_probe/files/recovery-probe.py`
- `roles/recovery_probe/templates/recovery-probe.json.j2`
- `playbooks/cert_renew.yml`
- `playbooks/recovery_exec_setup.yml`
- `docs/ai/reviews/autonomous_recovery/2026-07-04_017_test_plan.md`
- `docs/ai/reviews/autonomous_recovery/2026-07-04_018_test_result.md`

## 判定

must-fix あり。現状では commit / push しない。

## 指摘

### must-fix: staged のテスト計画・結果が現行実装ではなく削除済み `homelab-mute-exec` を検証している

今回の staged コードは `homelab-mute-exec` と sudoers を削除し、
`homelab-monitoring-pause/resume/status` と workspace 内 `monitoring-paused` flag に置き換えている。

しかし staged されている以下 2 ファイルは、どちらも旧 `homelab-mute-exec` 実装の計画・結果のまま。

- `docs/ai/reviews/autonomous_recovery/2026-07-04_017_test_plan.md`
- `docs/ai/reviews/autonomous_recovery/2026-07-04_018_test_result.md`

具体例:

- `017_test_plan.md` は `/usr/local/bin/homelab-mute-exec`、`/etc/sudoers.d/recovery-exec-mute`、
  `command = "homelab-mute-exec"` を期待値にしている。
- `018_test_result.md` は commit `f29fff4` を対象に、`homelab-mute-exec set/status/clear` が PASS
  したことを記録している。

この証跡は今回 staged されている global pause 実装を検証していない。むしろ削除対象の
旧実装が動いたことを示しており、現在の変更と矛盾している。

必要な対応:

- `017_test_plan.md` を global monitoring pause 用に更新する。
- `018_test_result.md` を現行 staged 実装の検証結果に差し替える、または新しい test_result を追加する。
- 少なくとも以下を確認対象に含める。
  - `homelab-monitoring-pause/resume/status` が `recovery-exec` から sudo なしで実行できること。
  - pause flag `/var/lib/recovery-exec/workspace/monitoring-paused` が期待どおり作成・削除されること。
  - `recovery-probe.py` が `monitoring_pause_flag` を見て全 target を skip し、counter を reset すること。
  - `recovery-probe` 実行ユーザー `yoshi` が pause flag を読めること。
  - `recovery_probe_setup.yml` による config/daemon 再配布と service restart 後に挙動が反映されること。
  - `cert_renew.yml` の pause/resume が `cert_needs_renewal=false` ではスキップされること。

## 確認済み

- `git diff --cached --check`: OK
- `bash -n roles/recovery_exec/files/homelab-monitoring-pause roles/recovery_exec/files/homelab-monitoring-resume roles/recovery_exec/files/homelab-monitoring-status`: OK
- `python3 -m py_compile roles/recovery_probe/files/recovery-probe.py`: OK
- `ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ansible-playbook --syntax-check playbooks/recovery_exec_setup.yml`: OK
- `ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ansible-playbook --syntax-check playbooks/recovery_probe_setup.yml`: OK
- `ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ansible-playbook --syntax-check playbooks/cert_renew.yml`: OK

## 補足

global pause 方向の設計自体は成立しうる。ただし、今回の変更は `recovery_exec` と
`recovery_probe` の両方を跨ぐため、旧 `homelab-mute-exec` の単体テスト結果だけでは
安全に収束したとは判断できない。
