# 自律復旧トリガ基盤 E-3 テスト計画 — 2026-07-03

対象実装: 2026-07-03_007_implement.md(commit 69c173c)

**承認待ち**

## 前提

- E-2 の push 経路がすべて quory/authy/monnie にデプロイ済みであること
- quory の recovery-exec shell が /bin/sh であること(E-2 テスト済み)
- drill は mute なしで実行し、Codex セッションを実際に起動する
  - Codex は homelab-investigate-authy status を実行し、freeradius が正常なため
    homelab-recover-authy は呼ばない(AGENTS.md §13 の禁止事項)

## 実施しないこと

- 実サービス(freeradius / grafana-server 等)の停止
- monnie/authy に対する recover-* の実行(Codex が自律的に呼ばないことを確認するのみ)

## フェーズ 1: 静的検証(ansy)

1. `scripts/check-tester-gate.sh` → OK (33 playbooks)
2. `ansible-playbook --syntax-check playbooks/recovery_push_drill_setup.yml`

## フェーズ 2: gate 検証(tester_mode=true)

```bash
ansible-playbook playbooks/recovery_push_drill_setup.yml -l quory -e tester_mode=true
```

確認: tester_gate が plan 表示 → end_play、changed=0、delegate 先タスク未到達

## フェーズ 3: drill ユニット配布(本番、Yoshinobu 承認必要)

```bash
ansible-playbook playbooks/recovery_push_drill_setup.yml -l quory
```

確認:
- authy: `/etc/systemd/system/recovery-drill-authy.service` 配置
- monnie: `/etc/systemd/system/recovery-drill-monnie.service` 配置
- `systemctl show recovery-drill-authy --property=OnFailure` → `OnFailure=recovery-trigger@recovery-drill-authy.service`

## フェーズ 4: push drill 発火(authy → Codex 全経路)

```bash
# dispatch.log を tail で監視しておく
ansible quory -b -m ansible.builtin.shell -a "tail -f /var/lib/homelab-recovery/push/dispatch.log &"

# drill 発火
ansible authy -b -m ansible.builtin.systemd \
  -a "name=recovery-drill-authy.service state=started"
```

期待する流れ:
1. `recovery-drill-authy.service` が即 fail (ExecStart=/bin/false)
2. `OnFailure=recovery-trigger@recovery-drill-authy.service` が発火
3. `recovery-push.sh` → SSH → quory dispatch.sh authy
4. dispatch.log に `firing: Codex investigation for authy` 記録
5. Codex が `homelab-investigate-authy status` を実行
   → authy に SSH、freeradius は running → 正常判定
6. dispatch.log に `session ended` 記録(Codex 完了)

確認項目:
- dispatch.log: `firing: Codex investigation for authy` の行
- dispatch.log: `session ended` の行(Codex がクラッシュせず完了)
- authy の journal: `journalctl -u recovery-trigger@recovery-drill-authy.service`
  → Codex の最終応答が確認できること(日本語の調査結果)
- authy の recover-action.sh が呼ばれていないこと
  (`journalctl -u recovery-trigger@freeradius.service` に OnFailure 発火がない)
- `firing` から `session ended` までの経過時間が数十秒以上あること
  (Codex が実際に investigate していることの傍証)

## フェーズ 4b: monnie drill(オプション、authy drill 成功後に実施判断)

```bash
ansible monnie -b -m ansible.builtin.systemd \
  -a "name=recovery-drill-monnie.service state=started"
```

同様に dispatch.log で `firing: Codex investigation for monnie` + `session ended` を確認。

## フェーズ 5: 後片付け確認

- dispatch.log: `session ended` で完了、push lock なし
- authy / monnie の実サービス(freeradius / grafana-server / prometheus / loki / unpoller)が running のまま
- `homelab-mute status`: mute なし
- `pgrep -u recovery-exec`: プロセスなし(Codex セッション終了)

## 結果

`docs/ai/reviews/autonomous_recovery/2026-07-03_011_test_result.md` に保存
