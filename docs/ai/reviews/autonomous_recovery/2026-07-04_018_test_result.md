# 自律復旧 Slack mute (自然言語) テスト結果 - 2026-07-04

## 結論

`docs/ai/reviews/autonomous_recovery/2026-07-04_017_test_plan.md` に従い、
recovery-exec からの `homelab-mute-exec` 実行経路を quory で検証した。

結果は **PASS**。

- 対象 commit: `f29fff4`
- phase 1 deploy: OK (`changed=4`, `failed=0`)
- phase 2 deploy verification: OK
- phase 3 recovery-exec manual wrapper execution: OK
- phase 4 Slack E2E: 任意項目のため未実施
- 後片付け: mute なし、mute file なし

## phase 1: デプロイ

実行:

```bash
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local \
ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote \
ANSIBLE_SSH_CONTROL_PATH_DIR=/tmp/ansible-cp \
ANSIBLE_SSH_COMMON_ARGS='-F /dev/null' \
ansible-playbook playbooks/recovery_exec_setup.yml \
  -l quory \
  -e recovery_exec_setup_targets=false
```

結果: OK。

```text
TASK [recovery_exec : Deploy Codex config.toml (model + execpolicy)] ***********
changed: [quory]

TASK [recovery_exec : Deploy AGENTS.md (tool documentation for Codex)] *********
changed: [quory]

TASK [recovery_exec : Deploy homelab-mute-exec wrapper] ************************
changed: [quory]

TASK [recovery_exec : Deploy sudoers for recovery-exec mute operations (quory-local)] ***
changed: [quory]

PLAY RECAP *********************************************************************
quory : ok=15 changed=4 unreachable=0 failed=0 skipped=2 rescued=0 ignored=0
```

`recovery_exec_setup_targets=false` により target nodes 側 setup は skipped。

## phase 2: デプロイ確認

実行:

```bash
ansible quory -b -m ansible.builtin.shell -a '
echo "=== wrapper ==="; ls -l /usr/local/bin/homelab-mute-exec
echo "=== sudoers ==="; cat /etc/sudoers.d/recovery-exec-mute
echo "=== config.toml (mute行) ==="; grep -A2 homelab-mute-exec /home/recovery-exec/.codex/config.toml
echo "=== AGENTS.md (mute section) ==="; grep -A3 "Mute operations" /var/lib/recovery-exec/workspace/AGENTS.md
'
```

結果: OK。

```text
=== wrapper ===
-rwxr-xr-x 1 root root 276 Jul  4 08:11 /usr/local/bin/homelab-mute-exec

=== sudoers ===
# recovery-exec may invoke homelab-mute as root for Slack-directed mute operations.
# Called via homelab-mute-exec wrapper from within Codex (execpolicy allows wrapper).
recovery-exec ALL = (root) NOPASSWD: /usr/local/bin/homelab-mute

=== config.toml (mute行) ===
command = "homelab-mute-exec"
policy = "allow"

=== AGENTS.md (mute section) ===
### Mute operations (homelab-mute-exec)

Use when the user asks to suppress autonomous recovery alerts during planned work.
```

期待どおり:

- `/usr/local/bin/homelab-mute-exec` は `0755 root:root`
- sudoers は `recovery-exec ALL = (root) NOPASSWD: /usr/local/bin/homelab-mute`
- Codex config は `homelab-mute-exec` を allow
- AGENTS.md に mute 操作セクションあり

## phase 3: recovery-exec ユーザーからの手動実行

### 3a. status

```bash
ansible quory -b -m ansible.builtin.shell \
  -a 'sudo -H -u recovery-exec homelab-mute-exec status'
```

結果: OK。

```text
TARGET     STATE        UNTIL                      REASON
authy      -            -                          -
monnie     -            -                          -
sophos-fw  -            -                          -
```

### 3b. set

```bash
ansible quory -b -m ansible.builtin.shell \
  -a 'sudo -H -u recovery-exec homelab-mute-exec set sophos-fw 5 "test mute from recovery-exec"'
```

結果: OK。

```text
muted: sophos-fw until 2026-07-04T08:16:58+09:00 (reason: test mute from recovery-exec)
```

### 3c. status

```bash
ansible quory -b -m ansible.builtin.shell \
  -a 'sudo -H -u recovery-exec homelab-mute-exec status'
```

結果: OK。

```text
TARGET     STATE        UNTIL                      REASON
authy      -            -                          -
monnie     -            -                          -
sophos-fw  MUTED(5m)    2026-07-04T08:16:58+09:00  test mute from recovery-exec
```

### 3d. clear

```bash
ansible quory -b -m ansible.builtin.shell \
  -a 'sudo -H -u recovery-exec homelab-mute-exec clear sophos-fw'
```

結果: OK。

```text
cleared: sophos-fw
```

### 3e. status

```bash
ansible quory -b -m ansible.builtin.shell \
  -a 'sudo -H -u recovery-exec homelab-mute-exec status'
```

結果: OK。

```text
TARGET     STATE        UNTIL                      REASON
authy      -            -                          -
monnie     -            -                          -
sophos-fw  -            -                          -
```

## phase 4: Slack E2E

未実施。

理由: test plan で任意項目とされており、Slack からのユーザー操作が必要なため。

## 後片付け確認

実行:

```bash
ansible quory -b -m ansible.builtin.shell -a '
homelab-mute status
echo mute_files
find /var/lib/homelab-recovery/mute -maxdepth 1 -type f -print 2>/dev/null | sort
'
```

結果: OK。

```text
TARGET     STATE        UNTIL                      REASON
authy      -            -                          -
monnie     -            -                          -
sophos-fw  -            -                          -
mute_files
```

mute file は残っていない。

## 総合判定

recovery-exec ユーザーから `homelab-mute-exec` を経由して:

- `status`
- `set sophos-fw 5 ...`
- `clear sophos-fw`

を実行できることを確認した。

sudoers / Codex config / AGENTS.md の配布内容も期待どおりで、Slack 自然言語 mute のための quory ローカル実行経路は利用可能。
