# 自律復旧トリガ基盤 E-1 再レビュー — 2026-07-02

## 結論

LGTM。must-fix / suggestion / nit はありません。

003_review の指摘は解消されています。このまま commit してよい状態です。

## 確認内容

- stopped 分岐は要求どおり deterministic start に修正されています。
  - `pvesh_vm_status()` が `(state, node, vmid)` を返す
  - `stopped` では `pvesh create /nodes/<node>/qemu/<vmid>/status/start` を実行
  - start 後は probe 回復ポーリングを行い、回復またはエスカレーションへ進む
  - drill 時は実 start を skip する
- 通知キューファイル名は `time.time_ns()` + PID になり、ミリ秒 timestamp 衝突による上書きリスクは解消されています。
- `roles/recovery_probe/files/__pycache__` は削除されています。

## 実行した確認

```text
bash -n roles/recovery_mute/files/homelab-mute
python3 - <<'PY'
import ast
from pathlib import Path
ast.parse(Path('roles/recovery_probe/files/recovery-probe.py').read_text())
PY
scripts/check-tester-gate.sh
git diff --check
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ansible-playbook --syntax-check playbooks/recovery_probe_setup.yml
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ansible-playbook --syntax-check playbooks/recovery_probe_notify.yml -e nt_status=ok -e nt_title=test -e nt_message=test
rg -n "([0-9]{1,3}\.){3}[0-9]{1,3}" roles/recovery_probe roles/recovery_mute playbooks/recovery_probe_*.yml docs/ai/reviews/autonomous_recovery/2026-07-02_00[123]_*.md
```

結果:

- bash / Python AST parse / lint / diff check / syntax-check は OK
- IP リテラルは検出なし
- `check-tester-gate.sh` は 31 playbooks OK

## Next step files

- `docs/ai/reviews/autonomous_recovery/2026-07-02_001_requirement.md`
- `docs/ai/reviews/autonomous_recovery/2026-07-02_002_implement.md`
- `docs/ai/reviews/autonomous_recovery/2026-07-02_003_review.md`
- `docs/ai/reviews/autonomous_recovery/2026-07-02_004_review.md`
