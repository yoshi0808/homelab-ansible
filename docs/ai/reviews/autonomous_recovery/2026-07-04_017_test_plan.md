# 自律復旧 Slack mute (自然言語) テスト計画 — 2026-07-04

対象 commit: f29fff4

## 前提

- 実行ホスト: quory
- 対象ロール変更: recovery_exec (quory ローカルのみ)
- target ノード(authy/monnie)は変更なし → `-e recovery_exec_setup_targets=false`

## フェーズ 1: デプロイ

```bash
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local \
ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote \
ansible-playbook playbooks/recovery_exec_setup.yml \
  -l quory \
  -e recovery_exec_setup_targets=false
```

期待: changed が homelab-mute-exec / sudoers-recovery-exec-mute / config.toml / AGENTS.md の 4 件前後、failed=0

## フェーズ 2: デプロイ確認

```bash
ansible quory -b -m ansible.builtin.shell -a '
echo "=== wrapper ==="; ls -l /usr/local/bin/homelab-mute-exec
echo "=== sudoers ==="; cat /etc/sudoers.d/recovery-exec-mute
echo "=== config.toml (mute行) ==="; grep -A2 homelab-mute-exec /home/recovery-exec/.codex/config.toml
echo "=== AGENTS.md (mute section) ==="; grep -A3 "Mute operations" /var/lib/recovery-exec/workspace/AGENTS.md
'
```

期待:
- `/usr/local/bin/homelab-mute-exec` が mode 0755 で存在
- sudoers に `recovery-exec ALL = (root) NOPASSWD: /usr/local/bin/homelab-mute`
- config.toml に `command = "homelab-mute-exec"` / `policy = "allow"`
- AGENTS.md に `Mute operations (homelab-mute-exec)` セクション

## フェーズ 3: recovery-exec ユーザーからの手動実行

```bash
# 3a. status (sudo 不要パス)
ansible quory -b -m ansible.builtin.shell \
  -a 'sudo -H -u recovery-exec homelab-mute-exec status'

# 3b. set (sudo → homelab-mute set として root 実行)
ansible quory -b -m ansible.builtin.shell \
  -a 'sudo -H -u recovery-exec homelab-mute-exec set sophos-fw 5 "test mute from recovery-exec"'

# 3c. 状態確認
ansible quory -b -m ansible.builtin.shell \
  -a 'sudo -H -u recovery-exec homelab-mute-exec status'

# 3d. clear
ansible quory -b -m ansible.builtin.shell \
  -a 'sudo -H -u recovery-exec homelab-mute-exec clear sophos-fw'

# 3e. 後片付け確認
ansible quory -b -m ansible.builtin.shell \
  -a 'sudo -H -u recovery-exec homelab-mute-exec status'
```

期待:
- 3a: TARGET / STATE / UNTIL / REASON ヘッダーと全ターゲット行が表示される
- 3b: `muted: sophos-fw until <timestamp>` が出力される(exit 0)
- 3c: sophos-fw が MUTED(~5m) で表示される
- 3d: `cleared: sophos-fw` が出力される
- 3e: sophos-fw が `-` 状態に戻っている

## フェーズ 4: E2E Slack テスト(任意)

Slack から以下を送信し、Codex が自然言語を解釈して homelab-mute-exec を呼ぶことを確認:

```
@Homelab sophos-fw を 5 分間ミュートして
```

期待: Slack スレッドに日本語の mute 確認応答が返る
確認: `homelab-mute status` で sophos-fw が MUTED 表示

```
@Homelab sophos-fw のミュートを解除して
```

期待: 解除確認応答 + status で `-` に戻る

## 判定基準

- フェーズ 1〜3: 全 OK → PASS
- フェーズ 4: 任意(ユーザー判断)
