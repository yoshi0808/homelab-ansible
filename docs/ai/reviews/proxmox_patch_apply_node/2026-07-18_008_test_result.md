# proxmox_patch_apply_node NAS mirror probe — dry-run テスト結果

- 実施日: 2026-07-18 (JST)
- 対象: `playbooks/proxmox_patch_apply_node.yml`
- 実施範囲: `--check --diff` のみ
- 判定: **FAIL-CLOSED**
- Ansible local temp: `/tmp/codex-ansible-local`

## 1. 結論

pve2 の read-only `pvesh` placement precheck が、control node `ansy` が pve2 上で稼働中と検出した。
playbook は設計どおり control-node safety gate で停止し、最終 recap は `failed=1` となった。

「いずれかで異常や failed があれば fail-closed で停止」の指示に従い、以後の pve1 dry-run と
cache helper 拒否経路テストは実施していない。pre-healthcheck、pre-apply predryrun、destructive block
にも到達していない。

実 APPLY、apt download/apply、reboot、NAS 書き込み、`/etc/hosts` 変更、mute、commit、push は
実施していない。

## 2. NAS mount precheck

`findmnt --mountpoint /mnt/pve/Synology-nfs` を両 node で read-only 実行した。

| node | target | filesystem | state |
|---|---|---|---|
| pve1 | `/mnt/pve/Synology-nfs` | NFS4 | mounted / rw |
| pve2 | `/mnt/pve/Synology-nfs` | NFS4 | mounted / rw |

mount の read-only precheck は PASS。

## 3. helper / mute 事前状態

両 node とも次の新規 helper は事前に存在しなかった。

- `/usr/local/sbin/proxmox-patch-apt-phase.sh`: ABSENT
- `/usr/local/sbin/proxmox-patch-cache.sh`: ABSENT

quory 上の `homelab-mute status` は `authy`、`monnie`、`sophos-fw` の全て未 mute だった。

## 4. pve2 dry-run

実行コマンド:

```text
ANSIBLE_LOCAL_TEMP=/tmp/codex-ansible-local \
ansible-playbook -i inventories/homelab/hosts.yml \
  playbooks/proxmox_patch_apply_node.yml \
  -e target_node=pve2 --check --diff
```

実行結果:

```text
localhost: ok=2 changed=0 unreachable=0 failed=0 skipped=3 rescued=0 ignored=0
pve2:      ok=17 changed=2 unreachable=0 failed=1 skipped=7 rescued=1 ignored=0
exit=2
```

確認できた事項:

- target/input assert: PASS
- recovery mute 3 task: 全て skip
- `Get cluster resources (VMs and CTs)` (`pvesh`): 実走
- control node placement gate: `ansy` が pve2 上で running と判定
- control node safety gate: fail、rescue へ遷移
- always block の post-healthcheck: 実走、結果 OK
- Slack: `skip_notifications=true` により表示のみ、外部送信 task は skip
- destructive download/apply/reboot block: 未到達

未到達のため確認できなかった事項:

- pre-apply healthcheck
- pre-apply predryrun collect
- `Copy apt phase runner`
- `Copy optional NAS cache helper`
- prepare / seed / download / publish / prune / apply / reboot の各 skip 表示

control-node gate がこれらより前にあるため、今回の placement では `failed=0` の期待条件を満たせない。

## 5. fail-closed 後の read-only 確認

- pve1/pve2 の両 helper: 引き続き ABSENT
- quory の mute status: 全対象が引き続き未 mute
- pve2 の `/mnt/pve/Synology-nfs/proxmox-patch-cache`: ABSENT
- pve2 の `/etc/hosts`: regular file のまま
- destructive block は task graph 上も未実行

`--check` でも role が controller-local report directory と final report に `check_mode: false` を
指定しているため、次のローカル診断 artifact は作成された。これは NAS/実 node の変更ではない。

```text
reports/proxmox-patch/20260718T145351_pve2_apply.json
```

## 6. 実施しなかった試験

### pve1 dry-run

pve2 で `failed=1` を検出した時点で fail-closed 停止したため未実施。

### cache helper の許可外 root 拒否

同じ理由で未実施。helper の実装上、`prepare` の引数順は次のとおり。

```text
proxmox-patch-cache.sh prepare <mount_root> <date_directory> <cache_root>
```

許可外 root は最初の `mkdir` より前の `require_mount_root` / `require_cache_root_pattern` で
rc 64 となる設計だが、今回は実行結果を取得していない。

## 7. 判定

- NAS mount read-only precheck: PASS
- pve2 pvesh placement precheck: PASS（危険配置を検出）
- pve2 healthcheck / predryrun 実走: FAIL（safety gate で未到達）
- pve2 dry-run `failed=0`: FAIL (`failed=1`)
- pve1 dry-run: NOT RUN（fail-closed）
- helper 未配備: PASS
- mute 未設定: PASS
- destructive operation 未実行: PASS
- 許可外 root rc 64 試験: NOT RUN（fail-closed）

次回は `ansy` が対象 node 上で稼働していない安全な placement を確認してから、pve2 → pve1 の順で
同じ dry-run を再実行する。
