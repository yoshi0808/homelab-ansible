# 実装内容: defaults コメント更新・retry_healthcheck_once.yml を git staging

実装日: 2026-05-31

## 対応レビュー

`docs/ai/reviews/proxmox_patch_apply_node/2026-05-31_015_review.md`

## 実施内容

### 指摘1（中）: 新規 task file がまだ untracked → staging 済み

`roles/proxmox_patch_apply_node/tasks/retry_healthcheck_once.yml` を
`git add` でステージングした。

```bash
git add roles/proxmox_patch_apply_node/tasks/retry_healthcheck_once.yml
```

確認結果:

```text
A  roles/proxmox_patch_apply_node/tasks/retry_healthcheck_once.yml
M  roles/proxmox_patch_apply_node/tasks/main.yml
```

`A`（added = staged）に変わり、untracked ではなくなった。
commit 時にこのファイルが含まれる状態になっている。

### 指摘2（低）: defaults コメントが CRITICAL のみ → 修正済み

`roles/proxmox_patch_apply_node/defaults/main.yml` のコメントを更新した。

```yaml
# 変更前
# Post-healthcheck retry on CRITICAL after reboot.
# Retried only when status is CRITICAL and reboot was performed.

# 変更後
# Post-healthcheck retry on CRITICAL/UNKNOWN after reboot.
# Retried only when status is CRITICAL or UNKNOWN and reboot was performed.
```

実装（`in ['CRITICAL', 'UNKNOWN']`）・ポリシー文書・defaults コメントが揃った。

## 作成・更新ファイル

| ファイル | 変更内容 |
|---|---|
| `roles/proxmox_patch_apply_node/defaults/main.yml` | retry コメントを CRITICAL/UNKNOWN に更新 |
| `roles/proxmox_patch_apply_node/tasks/retry_healthcheck_once.yml` | git staging（`git add`）。内容変更なし |
| `docs/ai/reviews/proxmox_patch_apply_node/2026-05-31_016_implement.md` | このファイル |

## 確認

```bash
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local \
  ansible-playbook -i inventories/homelab/hosts.yml \
  playbooks/proxmox_patch_apply_node.yml \
  -e target_node=pve2 --syntax-check
```

結果: `playbook: playbooks/proxmox_patch_apply_node.yml`（OK）

---

Next step files:
- docs/ai/reviews/proxmox_patch_apply_node/2026-05-31_001_requirement.md
- docs/ai/reviews/proxmox_patch_apply_node/2026-05-31_015_review.md
- docs/ai/reviews/proxmox_patch_apply_node/2026-05-31_016_implement.md
