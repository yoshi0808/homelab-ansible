# Final

この内容で確定。

確認者: Yoshinobu
日付: 2026-05-09

## 対象

- `playbooks/proxmox_patch_apply_node.yml`
- `roles/proxmox_patch_apply_node/`

## コメント

レビュー指摘を反映済み。
初期運用版として採用する。

## 残存リスク

VM 上で Ansible を稼働させる場合、当該ノードを登録し、複数台管理するように将来変更する。

当面は主体が VM 以外のベアメタルであり、現時点では問題なしとする。

## 指摘

将来 VM 名が変わったり、quory を control node にした時に、以下が更新されていないと検出をすり抜ける可能性がある。

```yaml
proxmox_patch_apply_control_node_name: "ansy"
```
