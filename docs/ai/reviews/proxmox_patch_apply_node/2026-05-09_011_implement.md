# proxmox_patch_policy.md 修正（v1.7 → v2.0）

実装日: 2026-05-09

---

## 概要

`docs/ai/prompts/proxmox_patch_policy.md` を v1.7 から v2.0 に更新した。
`proxmox_patch_apply_node` ロールの実装内容（手動 apply モード、明示的確認ガード、reboot 検知拡張）をポリシーに反映した。

---

## 変更箇所一覧

### 1. バージョン・日付の更新

- 版: v1.7 → v2.0
- 作成日: 2026-05-08 → 2026-05-09

---

### 2. Section 6.5 `proxmox_patch_apply_node.yml` の抽象化

**目的**

変更前:
```
`PATCH_READY` の場合のみ、指定した1ノードへパッチを適用する。
```

変更後:
```
`PATCH_READY` の自動適用、または `MAINTENANCE_REQUIRED` の手動適用を、指定した1ノードに対して行う。
```

**対象**

変更前:
```text
--limit pve2
--limit pve1
```（コードブロック）

変更後:
```
apply 対象ノードを実行時に指定する（`pve1` または `pve2` のいずれか）。
```

具体的なコマンドラインオプション名を削除し、概念的な記述に変更。

**実行条件**

変更前（末尾3行）:
```
- 事前 dry-run または apply 直前の re-dry-run により、対象ノードが `PATCH_READY` 相当であることを確認済み
- 対象ノード上の VM / CT が退避済み
- `MAINTENANCE_REQUIRED` / `BLOCKED` / `MAJOR_UPGRADE_DETECTED` ではない
```

変更後:
```
- 事前 dry-run または apply 直前の re-dry-run により、対象ノードの Status を確認済み
- 対象ノード上の VM / CT が退避済み
- `BLOCKED` / `MAJOR_UPGRADE_DETECTED` は適用禁止
- `MAINTENANCE_REQUIRED` の場合は手動 apply モードかつ明示的確認が必要
```

**処理概要**

変更前（ステップ5〜13）:
```
5. 事前 dry-run または apply 直前の re-dry-run により、対象ノードが `PATCH_READY` 相当であることを確認する
6. 対象ノード上に running VM / CT が残っていないことを確認する
7. 対象ノードに `apt-get dist-upgrade` を実行する
8〜13. （略）
```

変更後（ステップ5〜14）:
```
5. 事前 dry-run または apply 直前の re-dry-run により、対象ノードの Status を確認する
6. Status に応じた apply モードを判定する
   - `PATCH_READY`: 自動 apply モードで続行する
   - `MAINTENANCE_REQUIRED`: 手動 apply モードかつ明示的確認文字列が正しい場合のみ続行する
   - `BLOCKED` / `MAJOR_UPGRADE_DETECTED`: 停止する
7. 対象ノード上に running VM / CT が残っていないことを確認する
8. 対象ノードにパッケージを適用する
9〜14. （略、番号繰り上げ）
```

`apt-get dist-upgrade` という具体的なコマンド名を「パッケージを適用する」に抽象化。

**やらないこと**

変更前:
```
- `MAINTENANCE_REQUIRED` を適用しない
- `BLOCKED` を解除しない
```

変更後:
```
- `BLOCKED` を解除しない
- `MAJOR_UPGRADE_DETECTED` は適用対象外
```

手動 apply モードで MAINTENANCE_REQUIRED が対象になり得るため、「MAINTENANCE_REQUIRED を適用しない」を削除。

---

### 3. Section 11.7 reboot 検知の拡張

`dry-run では `reboot_expected` を推定し...` の段落の後に以下を追加:

```
reboot-required の検出は `/var/run/reboot-required` の存在確認を基本とする。ただし、カーネルパッケージの更新を伴う場合は、実行中のカーネルバージョンとインストール済みカーネルバージョンの差分によっても reboot 要否を判定する。
```

---

### 4. Section 12 MAINTENANCE_REQUIRED 手動適用の明示的確認ポリシー追加

末尾（`---` の直前）に以下を追加:

```
playbook を使って手動 apply を行う場合、明示的確認文字列を実行時変数として指定しなければならない。この確認がない場合、playbook は自動的に停止する。確認文字列は playbook が規定した形式に沿う。
```

---

### 5. Section 16.2 Codex CLI 入力の抽象化

変更前:
```
Ansible 側で事前に付与するフラグ:

- `important_component`: 重要コンポーネントリストとの照合結果
- `security_repo`: セキュリティリポジトリ出身かどうか（awk による判定結果）
- `is_new`: 新規インストールかどうか
- `has_remove`: remove を伴うかどうか
```

変更後:
```
Ansible 側で事前に判定した情報（重要コンポーネント該当性、セキュリティリポジトリ由来か、新規インストールか、remove を伴うかなど）も入力として付与する。
```

変数名・フィールド名を削除し、概念的な記述に変更。

---

### 6. Section 16.3 Codex CLI 出力の抽象化

変更前: JSON スキーマ（フィールド名・型・例を含む完全なスキーマ定義）

変更後:
```
Codex CLI は、最低限以下の情報を構造化して出力する。

- 重要コンポーネント更新の一覧と理由
- 重要コンポーネント削除の一覧
- 置換が疑われるか
- major upgrade が疑われるか
- security-sensitive な更新の一覧
- urgency 候補（LOW / NORMAL / HIGH / URGENT）
- changelog から識別した脆弱性タイプ（LPE / RCE / DoS / XSS など）
- メール件名と本文
- レポート MD

urgency 候補は Codex の識別結果に基づく候補であり、最終判定は Ansible tasks が行う。

Ansible tasks はこの出力を読み、patch policy の URGENT / HIGH 判断基準テーブルと照合して最終 status / urgency を確定する。
```

フィールド名・型・JSON 構文を削除し、出力すべき情報の概念的な列挙に変更。

---

### 7. Section 17.4 Mode C 追加

Section 17.3 の後に以下を追加:

```
### 17.4 Mode C: MAINTENANCE_REQUIRED 手動 apply（playbook 使用）

MAINTENANCE_REQUIRED が検出された場合に、手動 apply モードを使って playbook 経由で適用する。

1. 通知メールで MAINTENANCE_REQUIRED の内容を確認する
2. changelog summary を確認する
3. 必要に応じて Roadmap / Security Advisory を確認する
4. メンテナンス枠を確保する
5. 対象ノードの healthcheck を確認する
6. 対象ノード上の VM / CT の退避を確認する
7. 手動 apply モードと明示的確認変数を指定して playbook を実行する
8. apply と reboot（必要な場合）が完了したことを確認する
9. post-healthcheck を実行し、OK であることを確認する

pve1 は pve2 成功後に別途判断する。
```

---

## 変更していない箇所

- Section 7（重要コンポーネント一覧）: 変更なし
- Section 16.4〜16.9（やらせないこと、責務分離表、実行場所、セットアップ、制御ルール、テスト方針）: 変更なし
- 上記以外のすべてのセクション: 変更なし

---

Next step files:
- docs/ai/reviews/proxmox_patch_apply_node/2026-05-09_001_requirement.md
- docs/ai/reviews/proxmox_patch_apply_node/2026-05-09_010_implement.md
- docs/ai/reviews/proxmox_patch_apply_node/2026-05-09_011_implement.md
