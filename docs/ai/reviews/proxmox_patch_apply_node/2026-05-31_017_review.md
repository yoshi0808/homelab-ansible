# proxmox_patch_apply_node review 017

作成日: 2026-05-31

## レビュー対象

- `docs/ai/prompts/core.md`
- `docs/ai/reviews/proxmox_patch_apply_node/2026-05-31_001_requirement.md`
- `docs/ai/reviews/proxmox_patch_apply_node/2026-05-31_016_implement.md`
- `docs/ai/prompts/proxmox_patch_policy.md`
- `git diff`
- `git diff --cached`

## 結論

ブロッカーはありません。

前回レビューで残っていた主要な問題は解消されています。`roles/proxmox_patch_apply_node/tasks/retry_healthcheck_once.yml` は staged に含まれており、CRITICAL/UNKNOWN のリトライ対象化、defaults のコメント修正、メール変数の非空チェックも反映されています。

## 指摘

### Low: retry 後の失敗説明が CRITICAL に寄りすぎている

- 場所: `docs/ai/prompts/proxmox_patch_policy.md:659`
- 場所: `docs/ai/prompts/proxmox_patch_policy.md:660`

policy では UNKNOWN も post-healthcheck retry の対象になりましたが、リトライ後の失敗説明は「すべての試行でCRITICALのままだった場合」となっています。

実装上は、初回 post-healthcheck では healthcheck script failure / invalid JSON を UNKNOWN と判定し、`roles/proxmox_patch_apply_node/tasks/main.yml:416` で UNKNOWN も retry 対象にしています。一方、retry task 側では healthcheck command failure または JSON 未取得を `roles/proxmox_patch_apply_node/tasks/retry_healthcheck_once.yml:35` から `roles/proxmox_patch_apply_node/tasks/retry_healthcheck_once.yml:37` で CRITICAL に畳み込んでおり、最終的には `roles/proxmox_patch_apply_node/tasks/main.yml:426` から `roles/proxmox_patch_apply_node/tasks/main.yml:428` で `_post_hc_status` に反映されます。

このため、動作としては成立していますが、ドキュメントの表現は「すべての試行で OK にならなかった場合は CRITICAL として報告する」などに寄せると、UNKNOWN retry 経路との対応がより明確です。

## 確認した点

- `roles/proxmox_patch_apply_node/tasks/retry_healthcheck_once.yml` が staged されていることを確認しました。
- CRITICAL/UNKNOWN の retry 条件は `roles/proxmox_patch_apply_node/tasks/main.yml:416` から `roles/proxmox_patch_apply_node/tasks/main.yml:424` に反映されています。
- retry task は OK 到達後に block guard で後続 loop iteration の pause/run/parse を避ける構造です。
- defaults の retry コメントは CRITICAL/UNKNOWN に更新されています。
- apply 成否に関わらず summary mail 送信を試行し、mail failure を report に残した上で re-raise する構造です。
- `core.md` の差分は inventory 例と role 指定の更新であり、この要件との直接の矛盾は見当たりません。

## 検証

```text
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote ansible-playbook -i inventories/homelab/hosts.yml playbooks/proxmox_patch_apply_node.yml -e target_node=pve2 --syntax-check

playbook: playbooks/proxmox_patch_apply_node.yml
```
