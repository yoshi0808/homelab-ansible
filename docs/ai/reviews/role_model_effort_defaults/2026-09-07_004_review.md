## Code Review: Role別model / effort既定値の固定(ADR-012、coordinator.md、agent-messaging.md、status.md)

### Summary

requirement R1〜R7とAC1〜AC5を、staged diff全体・ADR-011/012・coordinator.md・agent-messaging.md・status.md・repo外`~/.agmsg/config/spawn_options.yaml`(読取)に対して照合した。`scripts/check-doc-consistency.py`とpre-commitチェック(gitleaks / tester-gate-lint / check1-3)は実際に実行し、いずれも終了コード0。旧現在値`gpt-5.6-sol` / lowを現在値として述べる規範は残っていない。blocking findingは無し。ADR-011の追記1文にscopeのやや広い表現があり、Minorとして指摘する。

### Critical Issues

なし。

### Major Issues

なし。

### Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| 1 | docs/ai/adr/011-vendor-neutral-role-allocation.md | 41 | ~~「現在のmodel / effort値はADR-012が本表をsupersedeする」は「本表」(Coordinator行を含む3行)全体を指すが、ADR-012はCoordinator行の現在値を定義しない~~ **対応済み(2026-09-07)**: 「現在のImplementer / Reviewer / Tester / Auditorのmodel / effort値はADR-012が該当行をsupersedeする」へ修正され、supersede対象がCoordinator行を含まないことが明示された。差分のみ再確認し、他行・他ファイルへの変更が無いことを確認した | 規範の精度(non-blocking) |
| 2 | docs/ai/status.md | Now冒頭 | 「Yoshinobu合意済み」はrepo内から独立に確認できない(Coordinatorとの対話記録であり本レビューの入力に含まれない)。指摘ではなく、確認できなかった旨をここに明記する | 未確認事項 |

### What Looks Good

- **AC1(Role別既定値)**: `docs/ai/roles/coordinator.md`「モデル・effort配分」に新設した「現在のagmsg配分」表がImplementer=`gpt-5.6-luna`/medium、Reviewer/Tester/Auditor=`sonnet`/mediumを一意に示す。Coordinatorはこの表に含まれず、他のどこにもmodelを固定する記述が無い(非ゴール「Coordinator自身のmodelを固定しない」と整合)。
- **AC2(引き上げ・引き下げ条件)**: ADR-012 DecisionとCoordinator.md 55行目の両方に、Implementer highの条件(複数role・複雑なcheck mode・rollback・shell/Python)とTester lowの条件(実ホスト非到達・手順完全固定・明示指定)がR2/R3どおり書かれ、混同されていない。
- **AC3(実効設定)**: `~/.agmsg/config/spawn_options.yaml`を直接読み、`claude-code: --effort medium`と`codex: -c model_reasoning_effort=medium`を確認した。implement記録の主張と一致する。
- **AC4(旧決定との関係)**: ADR-011はCLI分離の決定(作る側/検める側、Coordinatorを最低effortへ置かない)を維持したまま、テーブル前文を「決定時点の値」へ言い換え、ADR-012への参照を追加している。ADR-011自体のStatusは引き続きAccepted(010をsupersede)であり、ADR-012によってADR-011全体がsupersededになったわけではないことも文面から読み取れる。
- **AC5(文書整合性)**: `python3 scripts/check-doc-consistency.py`(check1/2/3すべてOK)と`bash scripts/git-pre-commit-check.sh`(gitleaks 0件、tester-gate-lint OK、check1-3 OK)を実行し、両方とも終了コード0を確認した。
- **旧現在値の掃引**: `gpt-5.6-sol`をrepo全体でgrepし、現在値として主張する箇所が残っていないことを確認した(ADR-011は「決定時点の値」として明示的に保持、ADR-012は経緯説明として言及、他は本案件・過去案件のrequirement記録内の引用のみ)。
- **自己整合**: `docs/ai/roles/coordinator.md`内に新設した「現在のagmsg配分」表(Role/現在のCLI/起動時指定/既定段)と、既存の「Claude Code subagent経路」表(Role/model/effort)は列名・対象経路が異なり、`scripts/check-doc-consistency.py` check2のヘッダ一致判定(`role`+`model`+`effort`の部分文字列)は新表を誤って拾わず、既存のsubagent表を引き続き参照している。両表の対象範囲は本文の「値の出どころは起動のしかたで違う」で明確に区別されている。
- **agent-messaging.mdのspawn例**: `spawn.sh codex`の唯一の例に`--model <Roleのmodel>`が追加され、runbook内に`--model`を欠く他のspawn例は無い(grep確認)。`new-session.sh`の実コードは既に`--model sonnet`をClaude Code roleへ渡しており、今回の契約と齟齬がない。
- **status.mdの整理**: 削除した「未追跡の`.codex/rules/default.rules.bak-before-coordinator-monitor-20260906`」記述は、当該ファイルが実際に存在しないことを確認済みであり、既に解消済みの記録を除いたものと判断できる(未解消のまま記録だけを消したのではない)。
- **requirement外の変更混入**: `.claude/agents/*.md`は変更されておらず(非ゴールR「`.claude/agents/*.md`のfrontmatterを変更しない」を遵守)、Coordinator自身のmodel/effortに関する記述にも変更は無い。agmsg本体への機能追加も無い。diffの範囲は計画が挙げた対象ファイル(ADR-011/012、coordinator.md、agent-messaging.md、status.md、案件記録)に収まっている。

### Verdict

Approve

### 確認範囲・未確認事項

- 確認した手段: `git diff --cached`全文の読解、`docs/ai/adr/011-vendor-neutral-role-allocation.md`全文、`docs/ai/roles/coordinator.md`該当節、`~/.agmsg/config/spawn_options.yaml`の直接読取、`.claude/agents/*.md`frontmatterの直接読取、`python3 scripts/check-doc-consistency.py`の実行、`bash scripts/git-pre-commit-check.sh`の実行、repo全体への`gpt-5.6-sol`/`spawn.sh`のgrep、`new-session.sh`内の実spawn呼び出しの確認、`.codex/rules/default.rules.bak-before-coordinator-monitor-20260906`および`new-session.sh.bak-pre-codex-coordinator-20260906-143522`の実在確認(前者は不在、後者は現存)。
- 未確認: status.mdの「Yoshinobu合意済み」という記述の裏取り(本レビューの入力範囲外)。実ホスト・本番Slack等への到達は無く、確認不要と判断した。
