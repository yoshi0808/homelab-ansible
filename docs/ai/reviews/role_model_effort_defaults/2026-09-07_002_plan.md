# 実装計画

## 対象

- `docs/ai/adr/011-vendor-neutral-role-allocation.md`
- `docs/ai/adr/012-role-model-effort-defaults.md`
- `docs/ai/roles/coordinator.md`
- `docs/ai/context/operations/agent-messaging.md`
- `docs/ai/status.md`
- `~/.agmsg/config/spawn_options.yaml`（repo外の実効設定）
- 本案件フォルダの記録

## 変更

1. ADR-012でRole別の現在値、例外条件、選択理由を記録する。
2. ADR-011のCLI分離判断を維持し、旧model / effort表だけがADR-012に置き換わったことを追記する。
3. Coordinator Roleへ現在のagmsg配分表と、modelを起動時に明示する契約を追加する。
4. agent-messaging runbookのspawn例を、Role表のmodelを明示して渡す形へ直す。
5. repo外spawn optionsのCodex effortをlowからmediumへ変更する。Claude Codeはmediumを維持する。
6. statusの移行時記述から、現物と食い違うImplementer modelと削除済みbackupの記述を除く。

## 検証

- 旧値`gpt-5.6-sol` / Implementer lowを現在値として述べる規範が残っていないことを検索する。
- `.claude/agents/*.md`とCoordinator内のClaude subagent表が一致することを機械検査する。
- repo外spawn optionsを直接読み、Codex / Claude Codeがともにmediumであることを確認する。
- 別CLIのReviewerがdocument-norm-reviewで差分を査読する。

## 実行境界

実ホストへは到達しない。repo外では`~/.agmsg/config/spawn_options.yaml`だけを変更する。subagentの起動確認は設定変更後に行うが、案件外の実装・テストは依頼しない。
