# Test result: Role別model / effort既定値の固定

実施: 2026-09-07 / Coordinator、Reviewer

本件は規範文書とローカルのagmsg起動設定だけを変更する。実ホストやサービスへは到達していない。

## AC結果

| AC | 結果 | 証拠 |
|---|---|---|
| AC1 | PASS | Coordinator Roleのagmsg配分表から4Roleのmodel / effortが一意に読め、Coordinator自身は表の対象外 |
| AC2 | PASS | Implementer highとTester lowの条件をADR-012とCoordinator Roleの双方で区別して記載 |
| AC3 | PASS | `~/.agmsg/config/spawn_options.yaml`を直接読み、Claude Codeの`--effort`とCodexの`model_reasoning_effort`がともにmediumであることを確認 |
| AC4 | PASS | ADR-011はCLI分離とCoordinatorのeffort判断を維持し、4Roleのmodel / effort該当行だけをADR-012がsupersedeすると明記 |
| AC5 | PASS | 文書整合性検査、pre-commit検査が終了コード0。独立ReviewerはApprove、blocking findingなし |

## 機械検査

- `python3 scripts/check-doc-consistency.py`: check1〜3 OK。
- `scripts/git-pre-commit-check.sh`: gitleaks、tester-gate-lint、check1〜3 OK。
- `git diff --cached --check`: 最終stage後に終了コード0。

## 残存事項

- repo外のspawn optionsはGitで追跡されないため、将来の起動時にも現物確認が必要。
- modelの明示は`spawn.sh --model`を使う運用契約であり、agmsg本体によるRole別強制ではない。
