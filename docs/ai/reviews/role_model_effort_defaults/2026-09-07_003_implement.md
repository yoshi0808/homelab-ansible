# 実装記録

## 変更内容

- ADR-012を追加し、現在の4Roleのmodel / effort、Implementer highとTester lowの例外条件を決定として記録した。
- ADR-011のCLI分離判断は維持し、決定時点のmodel / effort表だけがADR-012に置き換えられたことを明記した。
- Coordinator Roleへ現在のagmsg配分表を追加し、agmsg起動時の`--model`明示を契約化した。
- agent-messaging runbookのspawn例へ`--model`を追加した。
- repo外の`~/.agmsg/config/spawn_options.yaml`でCodex effortをlowからmediumへ変更した。Claude Codeはmediumを維持した。
- statusから、現行`new-session.sh`と食い違うImplementer modelの記述、および削除済みbackupの記述を除いた。

## 変更しなかったもの

- `.claude/agents/*.md`は変更していない。これらはClaude Code subagent経路の設定で、4RoleのfrontmatterとCoordinator内の対応表は既に一致している。
- Coordinator自身のmodel / effortは変更していない。
- agmsg本体にRole別effort機能を追加していない。

## 実効設定

変更後のrepo外設定は次の状態である。

- `claude-code`: `--effort medium`
- `codex`: `-c model_reasoning_effort=medium`

modelはRoleごとに異なるため、`spawn.sh --model`で起動ごとに明示する。

## 検証状況

- `python3 scripts/check-doc-consistency.py`: check1〜3すべてOK。
- `scripts/git-pre-commit-check.sh`: gitleaks、tester-gate-lint、check1〜3すべてOK。
- 独立Reviewer: `2026-09-07_004_review.md`でApprove、blocking findingなし。Minor 1件は採用し、ADR-011のsupersede対象を4Roleの該当行へ限定した。

## 残存リスク

repo外設定はGitで追跡されない。modelの明示も起動手順によるため、実際の起動時には生成されたセッションの設定を確認する必要がある。
