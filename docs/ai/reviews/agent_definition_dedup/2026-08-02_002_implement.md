# implement: agent定義の縮退と規範の昇格

日付: 2026-08-02 (JST) / 実施: Coordinator直接

## 変更したファイル

| ファイル | 変更 |
|---|---|
| `docs/ai/core.md` | 「Ansible変更の共通ゲート」へ decoy inventory の定義(3条件・全Role承認不要)を新設。「subagentが共通して守ること」へ2項目追加 |
| `docs/ai/roles/implementer.md` | 実ホスト到達を全面禁止へ確定。値の目視で終えない検証義務を追加 |
| `docs/ai/roles/reviewer.md` | 実ホスト到達の条項を新設(従来Role文書に一切無かった) |
| `docs/ai/roles/tester.md` | 実行前提の現物確認、通知抑止変数の付与を追加。非冪等操作の条項からホスト名を除去し `coordinator.md` 参照へ |
| `docs/ai/role-routing-index.md` | 「Agent定義との関係」へ、body に置いてよいものの限定と、書き足したくなる動機への反論を追記 |
| `.claude/agents/*.md` 4本 | body を縮退(旧 約60行 → 61行中の実質20行。1本あたり本文4〜6行) |

## AC2: 変更前 agent定義の全行の行き先

変更前は `git show HEAD:.claude/agents/<role>.md` で取得できる。

### implementer.md

| 旧行 | 内容 | 行き先 |
|---|---|---|
| L13 後半 | 承認境界ポインタ | **削除**。実ホスト全面禁止が確定し、Implementerに承認の余地が無くなったため |
| L17 | 記録へ書き切る | agent定義に維持(成果物ファイル名の対応) |
| L18 | subagent起動しない | `core.md`「subagentが共通して守ること」へ昇格(新規) |
| L19 | decoy 3条件 | `core.md`「Ansible変更の共通ゲート」へ昇格(新規) |
| L20 | 値の目視で終えない | `roles/implementer.md`「責任・権限」へ昇格。日付・事故経緯は落とした |
| L21 | 実host ansible実行しない | `roles/implementer.md`「禁止・エスカレーション」へ昇格。ホスト名の列挙は落とした |
| L22 | commit/push しない | `core.md`「人間の権限と安全境界」に既存 |

### reviewer.md

| 旧行 | 内容 | 行き先 |
|---|---|---|
| L13 後半 | 承認境界ポインタ | **削除**(同上)。代わりに `document-norm-review` SKILL を追加 |
| L17 | 記録へ書き切る | agent定義に維持 |
| L18 | subagent起動しない | `core.md` へ昇格 |
| L19 | 独立性の担保 | `roles/reviewer.md`「計画査読」「禁止」に既存 |
| L20 | 主張を鵜呑みにしない / file:line現物確認 | `core.md`「subagentが共通して守ること」へ昇格(新規) |
| L21 | decoy 3条件 | `core.md` へ昇格 |
| L22 | 実host ansible実行しない | `roles/reviewer.md`「禁止」へ昇格(**Role文書に一切無かった欠落**) |
| L23 | commit/push しない | `core.md` に既存 |

### tester.md

| 旧行 | 内容 | 行き先 |
|---|---|---|
| L17 | tester-gate / `--check` / `safe-ansible-check.sh` | `core.md`「Ansible変更の共通ゲート」、`roles/tester.md`「禁止」、`ansible_test_safety_policy.md` に既存。agent定義から policy を直接指すよう変更 |
| L18 | 非冪等操作の承認 | `roles/tester.md`「禁止」に既存(ホスト名を `coordinator.md` 参照へ置換) |
| L19 | `skip_notifications` 付与 | `roles/tester.md`「禁止」へ昇格(新規)。`skip_notifications` を先に置いた |
| L20 | 実行前提を現物で確認 | `roles/tester.md`「責任・権限」へ昇格(新規) |
| L24 | 記録へ書き切る | agent定義に維持 |
| L25 | subagent起動しない | `core.md` へ昇格 |
| L26 | decoy 3条件 | `core.md` へ昇格 |
| L27 | 範囲外は停止して報告 | `core.md`「実ホストへ触れてよい範囲は自分のRole文書が定める」「止めてCoordinatorへ返す」、`roles/tester.md`「エスカレーション」に既存 |
| L28 | 秘密情報/内部IP、commit/push | `roles/tester.md`「禁止」、`core.md` に既存 |

### auditor.md

| 旧行 | 内容 | 行き先 |
|---|---|---|
| L17 | 記録へ書き切る | agent定義に維持 |
| L18 | コールドスタートは道具 | `roles/auditor.md`「目的」「1. 再構成の検査」に既存 |
| L19 | Coordinatorの説明を入力にしない(+PMO退役の経緯と `reviews/` 参照) | `roles/auditor.md`「入力を絞る理由」に既存。**経緯と `reviews/` 参照は削除**(2026-08-01方針) |
| L20 | 記録を補完しない | `roles/auditor.md`「禁止」に既存 |
| L21 | 技術的正否は判定しない / 矛盾は指摘してよい | `roles/auditor.md`「必須Context」「禁止」に既存 |
| L22 | subagent起動しない | `core.md` へ昇格 |
| L23 | 実ホスト非接触 / commit/push | `roles/auditor.md`「禁止」に既存 |
| L24 | file:line を現物で確認 | `core.md` へ昇格 |

**対応先の無い行はゼロ。**

## 実施した検証

| AC | 手段 | 結果 |
|---|---|---|
| AC1 | 変更後の4本を目視 | body は各4〜6行(ポインタ + 成果物名) |
| AC2 | 上表を作成 | 対応先の無い行ゼロ |
| AC3 | `grep -rlnE "pve1\|pve2\|authy\|sophos-fw\|cloudkey\|monnie\|quory" .claude/agents/ docs/ai/roles/ docs/ai/core.md`(7ホスト名を網羅。当初は `sohos-fw` 1語のみで代表性が低く、review指摘3を受けて再検査した) | `docs/ai/roles/coordinator.md` と `docs/ai/core.md` のみ。うち `core.md` の `quory` は「開発と本番の境界」節の既存記述で承認境界の列挙ではない。**agent定義4本はゼロ** |
| AC4 | `grep -rn "subagentとしての事情\|実ホスト検証の安全ゲート" --include=*.md`(reviews除く) | 0件。Reviewerが拡張子を問わずリポジトリ全体で再掃引し同結果 |
| AC5 | **未達**。下記参照 | Reviewer起動が縮退前の定義で行われたため検証できず |
| 付帯 | `git diff \| grep '^+' \| grep -E '([0-9]{1,3}\.){3}[0-9]{1,3}'` | 検出なし。削除行の `127.0.0.1` は `scripts/git-pre-commit-check.sh` L44 が `^\+` のみ検査するため対象外 |

## review(003)の反映

| finding | 対応 |
|---|---|
| 1 (Critical) AC5未検証 | **受け入れた。** 下記「未解決」へ移し、`docs/ai/status.md` のWatchへ検証手段つきで載せた |
| 2 `repr`相当が落ちている | 反映。`roles/implementer.md` へ「`repr`相当で型まで確認し(文字列の空と`None`は表示上区別が付かない)」を復元した |
| 3 AC3の検証が `sophos-fw` 1語のみ | 反映。7ホスト名で再検査し、上表を差し替えた。結論は変わらず |
| 4 「Testerの役」の引用が緩い | 反映。**明文が `.claude/agents/tester.md` の frontmatter `description` にしか無かった** — 本案件が是正しようとしている「規範がharness側にしか無い」形そのもの。`role-routing-index.md` のTester行へ「実ホストへ到達してよい唯一のRoleである」を明記し、引用を実在させた |

## 未解決・残存リスク

- **AC5は未達。** Reviewerが自身の起動時システムプロンプトを `git show HEAD:.claude/agents/reviewer.md` と照合し、**一字一句一致する縮退前の内容**だったことを報告した。作業ツリーは既に縮退後になっている。つまり**同一セッション内でagent定義を編集しても、その後に起動するsubagentへは反映されない**(少なくとも今回の harness版では)。`role-routing-index.md`「セッション途中に作成した定義が登録されるかは、harnessの版に依存する」は新規作成の話で、**既存定義の編集については未記載だった**。
  - 検証手段: commit後の新しいセッションでsubagentを1体起動し、`.claude/agents/<role>.md` の現物と起動時定義が一致するか(縮退後の4〜6行構成になっているか)を報告させる。
  - この未達は差分の欠陥ではなく検証タイミングの制約である。縮退そのものの正しさ(AC1〜AC4)は独立に確認済み。
- `role-routing-index.md` のモデル配分表は frontmatter の値を写したままにした(scope外と判断)。値がずれたときに検出する手段は無い。
