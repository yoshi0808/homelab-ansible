# review: Policy群(8本)から経緯・根拠を落とす — Batch 1 + Batch 2

正本: `docs/ai/reviews/norm_docs_rationale_removal_round3/2026-08-02_001_requirement.md`
対象: `git diff docs/ai/policies/`(8ファイル、Batch 1の2本 + Batch 2の6本、うち`cert_renew_policy.md`は無編集)
参照した実装記録: `2026-08-02_002_implement_batch1.md`、`2026-08-02_003_implement_batch2.md`(いずれも検算のみに使用、対応表は独立に自分で再構築した)

## Summary

8本すべてについて `git show HEAD:<path>` と改訂後を逐行突合し、Policy IDのHTMLコメントマーカー(`<!-- XXX-nnn -->`)を全ファイルでカウント突合、`## 9. 参照`等の受け皿ファイルの実在確認、`scripts/check-doc-consistency.py`・`scripts/git-pre-commit-check.sh`の実行、リポジトリ全体への宙ぶらりんgrepを独立に行った。

**AC1〜AC5はいずれも満たされている。** 落ちているのは経緯・根拠・判断の引用・実測日付のみで、許可・禁止・停止条件・Policy IDは1件も変わっていない。実装記録が自ら「独立レビュー推奨」と挙げた3点(LOG-044統合、`§2.2 B案`ラベル削除、TIME-007の言い換え)を含めて検証し、いずれも規則の意味は不変と判断した。

唯一、`§2.2 B案`ラベル削除に伴い、`playbooks/proxmox_patch_weekly_full.yml`のコメント/fail_msgが指す先の一部がPolicy本文では字面上たどれなくなっている(Findings参照、実害は限定的)。

## Critical Issues

なし。

## Suggestions

- **`playbooks/proxmox_patch_weekly_full.yml`の`§2.2 B案`参照(L29, 414, 426-427, 465, 484, 540, 561, 637)** — ReportFindings参照。Policy本文から「B案」という語が消えたため、コメント単体からPolicyへ字面で遡れない。`requirement.md`側に同語が現存するため実害は限定的だが、次にこのplaybookへ触れる機会に、コメントの参照先を`requirement.md`明示形(既にL426-427はそう書いている)へ統一するか、Policy側に短い相互参照を残すことを検討してよい。今回のrequirement scope外の作業のため、blockingとはしない。

## What Looks Good

- **AC1(本文の日付0件)**: 8ファイルとも「変更履歴」節を除いた本文を独立にgrepし、実装記録の自己申告と一致した。ヒットが残った3箇所(`log_observability_policy.md`のLOG-067/083/061、`incident_capture_policy.md`の`## 9. 参照`内D4引用、`cert_renew_policy.md`のCERT正本ポインタ)はすべて実在ファイルパスであり、経緯としての日付ではないことを確認した(実在確認は自分でも`[ -e ]`で再検証済み)。
- **AC2(許可・禁止・停止条件が1つも変わっていない)**: 全8ファイルの`git diff`を逐行読み、削除・変更断片を「経緯・根拠・判断の引用」と「規則・Policy ID・行き先」に分類した。規則文自体が変更された形跡はどのファイルにも無い。特に念入りに見た3点は次のとおり。
  - **`log_observability_policy.md` LOG-044**: 旧LOG-044/045/046(特定時点を名指しする3条項)を「特定時点のPhase完了状態や実装状況は時点履歴として保持し、現行のnotification contractや許可条件として固定しない」という一般規則へ統合。これはrequirement OQ1が明示的に指示した処理そのものであり、元の3条項が指していた対象(notification contract固定禁止、本書の現行状態への固定禁止)を過不足なく包含している。禁止の範囲が狭まっていないことを確認した。
  - **`proxmox_operations_policy.md`の`§2.2 B案`ラベル削除**: ラベルが束ねていた規則本体(単一node運転時、退避を試みず running guest 残存なら適用を見送る/緑終了+通知/無人停止の強行はしない)はSB-012・SB-032・SB-088・§5.1手順のすべてで無改変のまま残存(Policy IDマーカーのカウント突合で確認)。ラベル自体の扱いはSuggestionsへ記載。
  - **`time_sync_check_policy.md` TIME-007**: 「2026-07-16に事象が判明した」という経緯的な文を「busybox ntpdはNXDOMAIN応答をhard failureとして扱い失敗する」という現在形の事実文へ言い換えているが、直前段落で既に確立している「quory参照はGUIから手動でIP登録する」という規則と整合しており、規則の中身(hostnameでなくIPを使う、GUI経由の手動登録)は変わっていない。
- **Policy IDの欠落なし**: 8ファイル全てで`<!-- XXX-nnn -->`マーカーを`git show HEAD`版と改訂後で集合比較した。差分が出たのは`log_observability_policy.md`のみで、LOG-045/LOG-046/LOG-073の3件が消えており、これは全てAC3で要求される退番として変更履歴に記録済み。他7ファイルは前後で完全一致(0件差分)。
- **AC3(退番の記録)**: `log_observability_policy.md`の変更履歴v4.1行に「退番: LOG-073(再利用しない)」「退番: LOG-045 / LOG-046(再利用しない)」が明記されている。他7ファイルは新規退番が無く(Policy IDマーカー突合でも裏付け済み)、既存の退番記録(SB-023等)は無改変。退番と書きながら実は本文に残っている、または逆に本文から消えたのに退番と書いていない、のいずれのパターンも見つからなかった。
- **AC4(行き先・保存先・正本の指定)**: `grep -n "正本"`等で機械的に洗い出し、全ファイルで無改変の残存を確認した。特に`incident_capture_policy.md`の`## 9. 参照`リスト(D1〜D7、IC-004が指すD4を含む)、`cert_renew_policy.md`のCERT正本ポインタ、`autonomous_recovery_policy.md`のAR-103実装・案件記録ポインタ、`log_observability_policy.md`のADR-007・provisioning YAML正本群を直接確認した。「根拠への参照」と誤判定して行き先ごと落とした例は無い。
- **AC5(宙ぶらりん参照なし)**: `check-doc-consistency.py`(check3、内部リンク切れ、91件比較でOK)、`git-pre-commit-check.sh`(gitleaks/tester-gate-lint/checkすべてOK)を実リポジトリ上で読み取り専用に実行した。加えてLOG-045/046/073、`§2.2 B案`、TIME-007旧スナップショット値を対象に独立grepを行い、Findings記載の1件を除き、現行規範として参照している箇所が無いことを確認した。
- **無編集3本の判断**: `cert_renew_policy.md`は唯一のヒット(CERT正本ポインタ)が§5「残す」列に該当するため無編集、というImplementer判断は妥当。`ubuntu_vm_patch_policy.md` / `cert_renew_cloudkey_policy.md`は`git status`上も無変更であることを確認し、R5の「参照の有無だけ確認」という指示範囲を超えていない。
- **非ゴールの遵守**: 「変更履歴」節は全ファイルで追記のみ(既存行の書き換えなし)。`docs/ai/reviews/`・`docs/ai/memory/`の既存記録・`skills/`は`git status --short`で無変更を確認。指定8ファイル以外への変更もない(Batch 1着手前から存在した`docs/ai/context-classification.md`等の変更はround2/他subagentのものであり、本案件と無関係)。

## Verdict

**Approve.** Critical Issuesなし。Suggestions 1件(blockingでない、次回の機会に対応)。

---

## Coordinatorによる対応(2026-08-02)

**Suggestion 1件を是正した。** Batch 1 が `proxmox_operations_policy.md` から「§2.2 B案」というラベルを落とした結果、`playbooks/proxmox_patch_weekly_full.yml` の6箇所(コメント5・task名1)がPolicy側で辿れない語を指していた。**これは本案件が作った宙ぶらりん参照であり、R6の趣旨に照らして放置しない。**

`§2.2 B案` → **`SB-088`**(Policy ID。ラベルと違い落ちない識別子)へ置換した。`ansible-playbook --syntax-check` 済み。

**2箇所は意図的に残した** — 426行と484行は `requirement.md` / `proxmox_auto_apply_widening` という**案件記録**を名指ししており、そちらは「B案」の語を今も持つ。案件記録への参照としては正しいままである。

なお `docs/ai/policies/` の外(playbook)へ手を入れたのは requirement §1 の対象外だが、**本案件の変更が直接生んだ不整合の是正**であり、規則にも挙動にも触れていない(task名の文字列1件を除きコメントのみ)。
