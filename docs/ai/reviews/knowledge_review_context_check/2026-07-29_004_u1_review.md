# U1差分レビュー: Context陳腐化チェック追加

対象requirement: `2026-07-29_001_requirement.md`
対象plan: `2026-07-29_002_plan.md`(U1)
対象implement記録: `2026-07-29_003_u1_implement.md`
レビュー独立性: このレビューはU1計画査読・実装に関与していないsubagentが行った。

## Summary

AC1〜AC5すべて差分上は満たされている。`job-settings.json.j2`の`Edit`/`deny`配列・`Bash`/`WebFetch`/`WebSearch`禁止は無変更、書込範囲拡張は無い。JSON構文・Jinja2レンダリングとも実際にパース/実行して確認しエラー無し。対象外3ファイル(`incident-*.j2`・`defaults/main.yml`)、規範文書の宙ぶらりん参照・自己整合の面でも問題を検出しなかった。AC1(`Read(inventories/homelab/**)`が`inventories/vars/`を実際に除外するか、Globパターンの実効性)は本レビューでは検証していない技術的前提であり、plan.md/implement.mdが正しく指摘した通りTesterのdecoy実測が必要。Verdict: Approve(AC1の実測待ちを条件に)。

## Critical Issues

なし。

## Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| 1 | roles/knowledge_review/templates/review-prompt.md.j2 | 99-100 | 「書いてはいけない」節の`playbooks/`・`roles/`・`inventory`行が「書込・実装変更はできない」の直後に読取許可範囲の括弧書きを入れており、節タイトル(禁止)と本文(許可の説明)がやや混在して読みにくい。実害はないが、読取許可の詳細は「実行環境について」節に一本化し、この節は「書けない」事実だけに絞ると将来の読者が誤読しにくい | 可読性 |

## What Looks Good

- **AC1(読み取り範囲拡張)**: `job-settings.json.j2`の`permissions.allow`に`Read(roles/**)`・`Read(playbooks/**)`・`Read(inventories/homelab/**)`の3エントリのみが追加されている。`inventories/vars/`への`Read`/`Edit`エントリは無い(diff・現物とも確認)。
- **AC2(封じ込め3条件の維持)**: `job-settings.json.j2`の`Edit`配列は`docs/ai/memory/**`・`docs/ai/context/**`・`skills/**`の3件のまま変更無し。`deny`配列(`Bash`・`WebFetch`・`WebSearch`)も無変更。`_comment`内の3条件(相対path表記/`acceptEdits`なし/`--setting-sources`空)の記述にも変更は無く、今回の追加コメントもこの3条件と矛盾しない。3ファイルへの差分は`git diff --stat`で`+12/-1`のみで、確認した実differ内容と一致する。
- **JSON構文**: `job-settings.json.j2`を`json.loads()`で実パース、エラー無し。テンプレート内にJinja変数(`{{ }}`)が無い純粋なJSONであることも確認済み。
- **Jinja2構文**: `review-prompt.md.j2`をJinja2 `Environment(FileSystemLoader).render()`で実レンダリング、エラー無し完走(出力4940文字)。既存の変数参照(`{{ knowledge_review_automemory_dir }}`)以外に変数は追加されておらず、制御構文(`{% %}`)も元から無く今回も追加無し。
- **AC3(指摘のみ・自動修正はしない)**: 新設「Context陳腐化チェック」節は要件の検出方法4項目をそのまま反映しており、末尾に「指摘は記録どうしの矛盾の指摘に留め...技術的正否の自動修正は...行わない」と明記。報告フォーマットの新小節も「不一致の指摘」または「不一致なし」の二値+未確認範囲の3パターンのみで、修正アクションを促す文言は無い。
- **promptの記述とjob-settings.json.j2の実許可の一致**: 「書いてよい/いけない場所」節・「実行環境について」節が述べる読取範囲(`docs/`・`skills/`・`roles/`・`playbooks/`・`inventories/homelab/`・auto-memory、`inventories/vars/`は不可)は、`job-settings.json.j2`の`allow`配列と1対1で一致する。promptが実際の許可より広く/狭く述べている箇所は無い。
- **AC5(role-routing-index.md・memory-classification.mdの整合)**: `role-routing-index.md`「無人実行されるCoordinator」表の「読取可」行はjob-settings.json.j2の内容と一致。`memory-classification.md`「月次振り返りの対象と手順」末尾の追記は、3系統(Incident/auto-memory/工程往復案件)とは別軸である旨を明記し、既存記述と矛盾しない。
- **対象外ファイルの不変更**: `git diff --stat`で`incident-job-settings.json.j2`・`incident-review-prompt.md.j2`・`roles/knowledge_review/defaults/main.yml`への差分がゼロであることを確認(`knowledge_review_timeout`含め無変更)。`incident-review-prompt.md.j2`は別の権限プロファイル(`reports/incidents/**`読取専用、Editなし)を持つ独立ペアであり、本requirementの対象外という記載も現物と一致。
- **秘密情報混入の懸念(独自確認)**: `Read(inventories/homelab/**)`が対象に含む`group_vars/`・`host_vars/`をgrepで確認したところ、`ansible_ssh_private_key_file`のようなキー**ファイルパスへの参照**や`ansible_ssh_pass: "{{ cloudkey_ssh_password }}"`のような**変数参照**はあるが、鍵本体・パスワード平文・vault暗号文(`$ANSIBLE_VAULT`)は含まれていない。実値は`inventories/vars/`側にあると見られ(除外済み)、`docs/ai/context/ansible/repository-overview.md`が「`inventories/vars/`のみ秘密を含み得る」と述べる記述と整合する。この点はrequirement/implement記録に明示された検証はなかったため、独立確認として記載する。
- **宙ぶらりん参照・撤回規範の残存(document-norm-review観点)**: 撤回した旧文言「実装には触らない」「読めるのは`docs/`・`skills/`とauto-memoryのみ」を`docs/ai/`・`roles/`・`skills/`・`playbooks/`全体でgrep検索したが、現行の規範文書(prompt、role-routing-index.md、memory-classification.md)に残存は無い。ヒットしたのは過去のreview記録2件(`2026-07-29_002_plan.md`自身の引用、`incident_auto_capture_step2/2026-07-28_003_adr_review.md`の当時のスナップショット引用)のみで、いずれも履歴記録であり規範本体ではないため問題無し。
- **文書内自己整合**: `review-prompt.md.j2`内で新設節と「書いてよい/いけない場所」節・「実行環境について」節・報告フォーマットの4箇所すべてが同じ新しい読取範囲を反映しており、一部だけ更新されて旧前提が残る状態(欠陥クラス5)は見られない。
- **role-context-matrix.md・incident_capture_policy.md**: 無人Knowledge振り返りの読取範囲を別の角度から記述する箇所が無いか検索したが、該当する記述は無く、更新漏れの心配は無い。

## Verdict

Approve(条件付き)。AC1の技術的前提(`inventories/homelab/**`パターンが`inventories/vars/`を実際に除外するか、および`inventories/homelab/../vars/`のような相対トラバーサル越しの読み取りが拒否されるか)は本レビューの範囲では検証しておらず、plan.md/implement.mdの申し送り通りTesterのdecoy実測が必須。それ以外の点(規範文書の整合性、書込範囲・deny配列の無変更、構文正当性、対象外ファイルの不変更)はすべて確認済みで問題なし。

## 未確認事項

- AC1: `Read()`のpermission判定における`inventories/homelab/**`パターンの実効性(部分パス許可の意味論)。ハーネスの実挙動はdecoy環境での実行でしか確定できない。
- AC1: `Read()`のみで`roles/`・`playbooks/`・`inventories/homelab/`配下のGlob/Grep相当の探索が機能するか(既存コメントは`docs/**`・`skills/**`でのみ実測済みの主張で、新規パスでの再確認はされていない)。
- AC4: 新セクション追加によるA(Incident)/B(auto-memory)/C(工程往復案件)の既存出力への回帰の有無は、実行を伴う確認(decoy環境でのフル実行)が必要で、本レビューでは静的な構造比較(既存3系統の指示文が変更されていないこと)のみ確認した。
