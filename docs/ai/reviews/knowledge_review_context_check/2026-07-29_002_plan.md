# Plan: Context陳腐化チェックの追加

対象requirement: `2026-07-29_001_requirement.md`

## 現状(調査済み)

- `roles/knowledge_review/templates/job-settings.json.j2`: 月次振り返り本体用の権限プロファイル。`Read(docs/**)`・`Read(skills/**)`のみ許可、`roles/`・`playbooks/`・`inventories/`は不許可(拒否)。
- `roles/knowledge_review/templates/review-prompt.md.j2`: 本体prompt。「対象(3系統: Incident / auto-memory / 工程往復案件)」→「3つに仕分ける」→「書いてよい/いけない場所」→「実行環境」→「報告フォーマット」の構成。
- 評価工程(`incident-job-settings.json.j2`・`incident-review-prompt.md.j2`)は別pairで対象外(障害評価であり本requirementの対象と別物)。
- `roles/knowledge_review/defaults/main.yml`に変数群。timeout等の数値はここ。

## 実行単位(1単位。並行して立てられる単位は無い — 1つの権限プロファイルとpromptに対する一連の変更のため)

### U1: 権限プロファイル・prompt・関連docsの実装(Implementer)

**変更対象**:
1. `roles/knowledge_review/templates/job-settings.json.j2`: `allow`へ`Read(roles/**)`・`Read(playbooks/**)`・`Read(inventories/homelab/**)`を追加。`_comment`にも追加理由と`inventories/vars/`を除外した理由を残す(この理由が正本のfrontmatterに書かれないと、次にこのファイルを触る人が除外の意図に気づけない)。
2. `roles/knowledge_review/templates/review-prompt.md.j2`: 新セクション「Context陳腐化チェック」を追加(`2026-07-29_001_requirement.md`「Context陳腐化チェックの検出方法」の4項目を本文へ落とし込む)。「書いてよい/いけない場所」の`playbooks/`・`roles/`・`inventory`行を「読めるが書けない」に訂正。「実行環境について」の読み取り範囲説明を更新。報告フォーマットへ「## Context陳腐化チェック」小節を追加。
3. `docs/ai/role-routing-index.md`「無人実行されるCoordinator」表の「読取可」行を更新(現状「docs/、skills/、--add-dirで渡したauto-memoryのみ」→ 今回拡張した範囲を追記)。
4. `docs/ai/memory-classification.md`に、この拡張が「月次振り返りの対象と手順」にどう影響するかの1行(新設したチェックの位置づけ)。

**未決定の設計判断**(1つ。基準の2つ未満に収まる):
- `knowledge_review_timeout`(現状1800秒)を今回のチェック追加分だけ増やすべきか。Implementerは変更せず現状維持のまま実装し、**Testerが実測で判断する**(タイムアウトに達するようなら次のTester単位で見積もりを出し、Coordinatorが変更要否を決める)。

**想定所要**: 実行単位(目安、Coordinatorの裁量。数値の上限ルールは無い — `docs/ai/reviews/process_retrospective/2026-07-29_008_coordinator_model_tier_policy.md`と同日に撤廃した`docs/ai/roles/reviewer.md`「計画査読」の基準変更に準拠)。

## 計画査読で特に確認してほしい点(層2)

- `inventories/homelab/**`という書き方で`inventories/vars/`が実際に除外されるか(pathパターンの効き方。`docs/ai/memory/lessons/claude-code-unattended-session-confinement.md`は「path は相対表記」「絶対表記だと全拒否」を実測済みだが、`inventories/homelab/**`のような部分パスの許可が期待通りに`inventories/vars/`を除外するかは、このrequirementで新たに確認する必要がある技術的前提)。
- `Read()`のみで`roles/`・`playbooks/`・`inventories/homelab/`配下のGlob/Grep相当の探索も動くか(job-settings.json.j2の既存コメントは「Read()が全ファイル読取ツールを...兼ねる」と述べているが、これは`docs/**`・`skills/**`で確認された話であり、新規パスでも同様に機能するかは未確認)。

## Testerへの委任(次単位、U1完了後)

decoy環境(実ホストに触れない、`docs/ai/memory/lessons/claude-code-unattended-session-confinement.md`と同じ実測方式)で次を確認する。

- AC1: 新しいpathで`roles/`・`playbooks/`・`inventories/homelab/`が読め、`inventories/vars/`が読めないこと。**直接指定だけでなく、`inventories/homelab/../vars/`のような相対トラバーサル越しの読み取り試行も試す**(計画査読Finding: Bashの許可判定が生文字列の前方一致で絶対パス表記に迂回された実測があり、`Read()`のpathマッチが同種の弱点を持つか未検証のため)。
- AC2: 書込範囲(`docs/ai/memory/`・`docs/ai/context/`・`skills/`)・commit/push禁止・中止条件など、既存の封じ込めが崩れていないこと(3条件の再確認)。
- AC4: decoy環境でのフル実行後、報告フォーマットのA(Incident)/B(auto-memory)/C(工程往復案件)各節が従来通り出力されること(新セクション追加による既存3系統への回帰が無いことの確認)。
- 実際にタイムアウト内でChecking処理が完走する見込みがあるか(promptの分量・想定読み込みファイル数から)。
