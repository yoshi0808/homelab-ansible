# Review: monnie monitoring healthcheck 第2回追加実装

## 結論

重大な指摘なし。

前回 `2026-05-28_005_review.md` で指摘した「retry / until 条件未達時に play が止まり、CRITICAL レポート・通知へ進めない」問題は、monitoring_servers 向け post-reboot 確認 6 タスクすべてに `ignore_errors: true` が追加されたことで解消されている。

この内容で commit してよい。

## Findings

なし。

## 前回指摘への対応状況

### [High] retry タスクが最終失敗すると、CRITICAL レポート・通知へ進めない

解消済み。

対象タスクすべてに `ignore_errors: true` が追加されている。

- `playbooks/ubuntu_nightly.yml:285` `Check prometheus service status`
- `playbooks/ubuntu_nightly.yml:296` `Check grafana-server service status`
- `playbooks/ubuntu_nightly.yml:307` `Check loki service status`
- `playbooks/ubuntu_nightly.yml:318` `Check 9090/tcp listening`
- `playbooks/ubuntu_nightly.yml:329` `Check 3000/tcp listening`
- `playbooks/ubuntu_nightly.yml:340` `Check 3100/tcp listening`

これにより、60 秒以内に service active / port listen 条件を満たせない場合でも、play は `nightly_criticals` の作成、レポート保存、CRITICAL 通知、最後の `Fail if critical` まで進める。

最終的な CRITICAL 時の playbook failed 終了も維持されるため、`ubuntu_vm_patch_policy.md` の「reboot 実行 → サービス確認 NG は CRITICAL 通知」という方針に合っている。

## 要件との照合

### OK

- `monitoring_servers` グループと `monnie` が追加されている。
- `monnie` は `monnie.internal` で定義されており、IP 直書き禁止方針に沿っている。
- `group_vars/monitoring_servers.yml` は `ansible_user: ann` / `~/.ssh/id_ann` の既存方針に沿っている。
- `monitoring-healthcheck.sh` は収集と JSON 整形のみを行っている。
- CRITICAL / WARNING 判定、通知、レポート保存、fail 制御は Ansible tasks 側にある。
- Prometheus / Grafana / Loki の service active 確認がある。
- 9090/tcp, 3000/tcp, 3100/tcp の listen 確認がある。
- `reports/monitoring-health/` への healthcheck レポート保存がある。
- `monitoring_healthcheck.yml` が専用 playbook として追加されている。
- `ubuntu_nightly.yml` に monitoring_servers 向けの reboot_required 確認、条件付き reboot、post-reboot 確認、通知が追加されている。
- post-reboot 確認には retry があり、条件未達時も CRITICAL 判定まで進む。
- `roles/systemd_timers/defaults/main.yml` に `monitoring-healthcheck` timer が要求どおり追加されている。
- 秘密鍵・認証情報・証明書秘密鍵の追加や読み出しは見当たらない。
- promtail は初回除外スコープのまま。

## 注意点

- `git diff --stat` は未追跡ファイルを表示しない。今回の新規ファイルは `git status --short` で未追跡として見えているため、commit 時は意図したファイルを明示的に stage すること。
- healthcheck role は既存 `radius_healthcheck` と同様に `/usr/local/sbin/` へ script を配置する。これは `2026-05-28_006_implement.md` の運用解釈どおり、「サービス・設定・reboot などの変更操作はしない」という意味での read-only として扱えばよい。

## 確認したコマンド

```bash
git status --short
git diff -- playbooks/ubuntu_nightly.yml
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ansible-playbook -i inventories/homelab/hosts.yml playbooks/ubuntu_nightly.yml --syntax-check
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ansible-playbook -i inventories/homelab/hosts.yml playbooks/monitoring_healthcheck.yml --syntax-check
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ansible-inventory -i inventories/homelab/hosts.yml --graph
git diff --stat
```

結果:

- `ubuntu_nightly.yml` syntax check: OK
- `monitoring_healthcheck.yml` syntax check: OK
- inventory graph: `monitoring_servers` 配下に `monnie` が表示される

## このまま commit してよいか

commit してよい。

前回までの修正必須事項は解消済み。実運用前には `monnie.internal` の名前解決と、quory 上で `systemd_timers.yml` を再実行して timer unit を反映する作業が必要。

Next step files:
- docs/ai/reviews/monitoring_healthcheck/2026-05-28_001_requirement.md
- docs/ai/reviews/monitoring_healthcheck/2026-05-28_006_implement.md
- docs/ai/reviews/monitoring_healthcheck/2026-05-28_007_review.md
