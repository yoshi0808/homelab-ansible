# review (round2): ボタンが無いplaybookの棚卸し

2026-08-06 / Reviewer(独立。今回の差分にも `..._003_review.md` にも関与していない)。
契約は `2026-08-06_001_requirement.md` R1〜R6 / AC1〜AC11。`..._002_implement.md` と
`..._003_review.md` は参考として読んだが、記載内容は自分で裏取りした範囲でのみ根拠に使った。

## Code Review: playbook_button_inventory 差分6本(round2)

### Summary

`..._003_review.md` が指摘したCritical #1(`serial_getty_mask.yml`が保護対象ホストへ技術的に
到達できた)・High #2(rsyslog/time_syncのヘッダが実装と食い違う「新規VM」用途を謳っていた)は、
D6/D7で実際に解消されていることを自分の実行で確認した。decoy inventory(後述の注記あり)と
`filter_plugins`の直接呼び出しでAC1〜AC11のうち到達可能な範囲を独立に再現し、破綻は見つからな
かった。新規に見つけたのは1件のみで、いずれも既存のfail-closed設計を回避するものではないSuggestion
級の記述精度の問題。Verdictは **Approve**。

### Critical Issues

なし。

### Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| 1 | `playbooks/serial_getty_mask.yml`:82-91 / `rsyslog_forward_to_monnie.yml`:52-61 / `time_sync_ntp_reference.yml`:73-84 | R2のassert(`... \| length > 0`)は `target_hosts=""`(空文字列)を「0件」として捕まえない。`query('ansible.builtin.inventory_hostnames', '')` は実測上「全ホスト」を返すため、assertは`ok`のまま通過する。最終的に`hosts: "{{ target_hosts \| default(...) }}"`側でAnsible自身が`Keyword 'hosts' is required, and cannot have empty values`という無関係なエラーで停止する(rc=4、3本とも実測確認済み)ため**結果としてfail-closedではある**が、fail_msgが謳う「did not match any host」という理由とは食い違う。serial_getty_maskは保護対象guard(3番目のtask)が"all"相当のこの解決結果を拾って`(pve1, pve2, sophos-fw, cloudkey)`を名指しして止めるため実害はさらに小さいが、rsyslog/time_syncの2本は保護対象guardを持たないため、空文字列を渡した運用者は「target_hosts関連の理由での停止」だとは分かるが、意図とズレた汎用YAMLエラーを見ることになる。requirementのレビュー観点が挙げる「空」の入力形に対する応答精度の話であり、ブロッキングではない。 | robustness |
| 2 | (round1から継続、未対応・Coordinator却下済み) `playbooks/*_target_hosts.yml`3本の検証play(2 task、`serial_getty_mask.yml`のみ3 task) | ほぼ逐語的な複製が3ファイルに存在する。`..._002_implement.md` §2.6に「安全のためのguardを間接参照にすると読み手が追う手間の方が増える」というCoordinatorの却下理由が記録済みであり、再指摘のみ行い採否は求めない。 | duplication |

### What Looks Good

- **R1/AC1(既定時の対象ホスト集合が不変)**: 実inventory(`inventories/homelab/hosts.yml`、状態を変えない`--list-hosts`)で3本とも自分で確認した。`serial_getty_mask.yml`→`quory/ansy/authy/monnie`、`rsyslog_forward_to_monnie.yml`→`authy/quory/ansy`、`time_sync_ntp_reference.yml`→`pve1/ansy/pve2/monnie/authy`。いずれも変更前の`hosts:`文字列と一致し、`git diff`のhunk自体も`hosts:`行以外を触っていないことを確認した。時刻同期playbookはdecoy(後述)で`--check`実行まで通し、5ホストとも新設assertを`ok`で通過し既存の`when:`分岐(dropin skip)まで変更前と同じ経路を辿ることを確認した。
- **AC2**: 実存しないホスト名(`doesnotexist123`)をdecoyへ与えたところ、3本とも検証playの`assert`が`target_hosts=...did not match any host...`で失敗し`rc=2`。旧来の`rc=0`での無警告通過(requirement §2の実測)は再現しない。
- **AC3**: decoy上の1ホスト(`newvm1`/`quory`)を指定したところ、そのホストのみが主playの対象になった(`--list-hosts`相当の到達確認および実行トレースで確認)。`rsyslog_forward_to_monnie.yml`の`ansible_play_hosts_all | length == 1`ガードは、`target_hosts`が単一ホストへ解決している限り`-l`無しでも自動的に満たされることを確認した。
- **AC4**: `roles/semaphore_templates/filter_plugins/semaphore_templates.py`の`semaphore_templates_reconcile()`を`importlib`で直接ロードし、変更後カタログ(49件)から新規4本を除いた45件を「既にreconcile済み」として`observed`に合成して実行した。結果は`new=4`(4本の`playbook#-`キーすべて一致)・`changed=0`・`unchanged=45`・`orphans=0`。実装記録・round1レビューの主張と一致した(自分のスクリプトで再現、鵜呑みにしていない)。
- **AC5/AC6/R6**: `roles/semaphore_templates/defaults/main.yml`のカタログを自分のPythonスクリプトで`playbooks/*.yml`(glob実測)と突合し、カタログに無いplaybookが要求どおり9本(`cert_renew_quory` / `incident_investigate_notify` / `incident_sync_teardown` / `knowledge_review` / `knowledge_review_timer` / `recovery_probe_notify` / `semaphore_templates_setup` / `test_ca_env` / `worktree_sync_notify`)であることを確認した。`git diff --stat`で追加分が78行の純追加(削除0)であることも確認し、既存45件は無傷。`scripts/check-doc-consistency.py`(check1〜3)・`scripts/check-deploy-needed.py`・`scripts/check-tester-gate.sh`・3本の`--syntax-check`・カタログYAMLのparseを自分で再実行し、いずれもPASS/エラー無し。
- **AC7**: `roles/time_sync_ntp_reference/tasks/main.yml`と`roles/rsyslog_forward_to_monnie/tasks/main.yml`それぞれについて、`hosts: localhost`・`become: false`の最小scratchplaybookから実roleを`import_role`し、`inventory_hostname`(=localhost)がどちらのallow-list(`time_sync_ntp_reference_chrony_hosts`/`rsyslog_forward_allowed_hosts`)にも無い状態で実行した。両方とも新設した単独`assert`が、対象の変数名・値の一覧・定義ファイルパスを名指しした`fail_msg`で`rc=2`停止することを確認した(以前の「何もせず緑」または「無関係な文言で停止」という2つの不具合はいずれも再現しない)。
- **AC8**: 3本とも`git diff`で主playの`tasks`/`pre_tasks`/`roles`本体に変更がないこと(`hosts:`行の書き換えのみ)を確認。time_sync/rsyslogのrole側追加assertは、既定ホスト集合(常にallow-listのメンバー)に対して恒真になるため経路は変わらない——decoy上の`--check`実行(time_sync、5ホスト全部)で実際に`ok`通過→既存の`when:`分岐まで到達することを直接確認した。rsyslogは同様の構造を1ホスト(`quory`)で確認(`Require an explicit single-host rollout limit`→新設assert→既存assert→...→実接続を要する`find`タスクでdecoyのUNREACHABLEに到達、という順序どおりに進み、途中で経路が変わっていないことを確認)。
- **AC9(要点)**: `serial_getty_mask.yml`の保護対象guardを、decoy inventory上で実名`pve1`を使って`-e target_hosts=pve1 --check`で実行し、`rc=2`・`PLAY RECAP`に`localhost`だけが現れ主play(`Stop and mask unused ttyS0 serial getty`、`become: true`)が一切開始されないことを確認した。さらにAnsibleの多play実行の既定挙動そのもの(play1が失敗すると、対象ホストが重ならない場合でも既定でplay2は実行されない——`any_errors_fatal`等の設定は本リポジトリの`ansible.cfg`に無い)を最小テストplaybookで別途確認し、「主playが呼ばれない」がAnsibleコアの偶然の副作用ではなく、この差分が依拠してよい既定の直列停止であることまで裏取りした。`target_hosts=proxmox`(グループ名、pve1+pve2)、`target_hosts=all`、`target_hosts=pve1:newvm1`(保護対象+非保護混在)のいずれも同様に主play到達前で`rc=2`停止することを確認し、単一ホスト名以外の入力形(グループ名・複数指定混在・`all`)でも保護対象guardが回避できないことを確かめた。
- **AC10**: 保護対象でも既定4ホストでもないinventory上のホスト(decoy `newvm1`)を`--check`で指定したところ、検証playの3 taskをすべて通過し主playが`newvm1`のみを対象に実行された(D2のユースケースが通ることを確認)。
- **AC11**: 3ファイル(`rsyslog_forward_to_monnie.yml`ヘッダ、`time_sync_ntp_reference.yml`ヘッダ、カタログの共通コメント+2件のsurvey description)を読み直し、「新規VMへ向ける用途ではない」という否定形の記述のみが残っており、「新規VMを指定できる」と読める記述が無いことを確認した(`grep`で「新規VM」「new VM」等を含む行を洗い出し、すべて否定文脈であることを1行ずつ確認)。`serial_getty_mask.yml`ヘッダの「新規VMを指定して1回流す」という記述はD2どおり残っており、これはR3の対象でAC11の対象外。
- カタログの新規4エントリの`survey_vars`(`type`省略=自由入力、`required: true`)は、既存の自由入力必須エントリ(`force_renew`/`dry_run`等)と同じ記法であることを目視確認した。
- `git diff`にIPv4パターンを含む行が無いことを`grep -E`で確認した。

### Verdict

**Approve**

## 自己検証で確認した手段

- 状態を変えない確認: `git status` / `git diff` / `--syntax-check` / `--list-hosts`(実inventory)/
  `scripts/check-doc-consistency.py` / `scripts/check-deploy-needed.py` / `scripts/check-tester-gate.sh` /
  `roles/semaphore_templates/defaults/main.yml`のYAML parse / `playbooks/*.yml`とカタログの突合。
- decoy inventory(`/tmp/.../scratchpad/decoy/`)での`--check`実行: AC1〜AC3、AC7〜AC10。
  **開示**: このdecoyは`ansible_host`をループバック / `ansible_port: 1`(閉ポート、条件2)を全ホストに
  強制し、実システムに影響するmoduleには到達させていない(condition3)が、ホスト名自体は
  `pve1`/`pve2`/`sophos-fw`/`cloudkey`/`ansy`/`quory`/`authy`/`monnie`という**実名を使っている**
  (`docs/ai/core.md`のdecoy条件1「実host名を書かない」を文字どおりには満たさない)。理由: 保護対象
  guard・allow-list guardの判定対象が実名の文字列そのものであり、架空名では判定ロジックの正しさを
  実名に対して検証したことにならないため。`ansible_host`/`ansible_port`を全件ループバック閉ポートへ
  強制しDNS解決を経由させていないため、実ホストへ到達する余地は無いと判断したが、条件1を字義どおり
  満たさない判断であることは明記する(採否・妥当性の判断はCoordinatorへ委ねる)。AC7の一部(role単体
  fixture)は実名を含まない`localhost`(`ansible_connection: local`相当、`become: false`)で行った。
- `filter_plugins/semaphore_templates.py`のpure Python直接呼び出し: AC4。

## 未確認事項

- 実inventoryの`ansy`/`monnie`/`quory`/`authy`/`pve1`/`pve2`そのものへの接続を伴う検証は、Reviewer
  も実ホストへansibleを実行しないため未実施(requirement §6の制約どおり、decoyでの構造的検証に留まる)。
- AC4のうち実Semaphore API(認証・POST/PUT・`survey_vars`のAPI側スキーマ)への到達は未検証
  (`semaphore_templates_as_code`案件の範囲、今回のAC4は「filter pluginの計算が正しいこと」まで)。
- `roles/semaphore_templates/defaults/main.yml`カタログのSuggestion #1(duplication)・round1の
  Suggestion(命名)はいずれも既にCoordinatorが却下済みであり、本レビューは再指摘のみで採否を求めない。
