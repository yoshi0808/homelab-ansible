# Grafana Alloy Phase 2 — pve2 手動適用後E2E検証

- 実施日: 2026-07-17
- 担当: tester
- 対象: `pve2`, `monnie`, Loki
- 方法: read-only Ansible ad-hoc / Loki query
- pve1: 未適用（pve1 stream不存在は正常）

## 結論

**FAIL — pve2の手動設定が参照path/contentと一致せず、monnieへ転送されていない。**

- pve2 rsyslog package: installed
- pve2 rsyslog: active / enabled
- pve2 full `rsyslogd -N1`: PASS
- journald drop-in: pathは存在するが `ForwardToSyslog=yesi` のtypo
- rsyslog forward config: 正規pathに不存在
- forward config本文: journald drop-in directoryへ誤配置
- journald: 誤配置fileのassignmentと `yesi` をparse errorとして無視
- `/var/log/syslog`, `/var/log/kern.log`, `/var/log/user.log`: 新規生成され副作用gate FAIL
- `/var/log/frr`: directoryのみ、file無しを維持
- monnie `/var/log/pve-nodes.log`: absent
- Loki pve2 stream: 0 streams / 0 entries
- pve2 journal流量: 14.3行/分相当
- Loki取り込み: 0行のため流量不整合

設定変更、service操作、test message送信、rollbackは行っていない。

## 1. 009参照設定

期待するjournald drop-in:

```ini
[Journal]
ForwardToSyslog=yes
MaxLevelSyslog=debug
```

期待するrsyslog path:

```text
/etc/rsyslog.d/49-monnie-forward.conf
```

期待するrsyslog contract:

- `omfwd`
- targetは `monnie.internal`
- UDP syslog port
- `RSYSLOG_SyslogProtocol23Format`
- action直後に `stop`

## 2. pve2側

### 2.1 package / service / syntax

| 項目 | 結果 |
|---|---|
| rsyslog package | installed, `8.2504.0-1` |
| `rsyslog.service` | active / enabled |
| `syslog.socket` | active |
| `systemd-journald` | active |
| full `rsyslogd -N1` | PASS |
| rsyslog recent error | 無し |

rsyslog自体の導入と既存構成の構文は正常。ただしremote forward fileがinclude pathに無いため、
`rsyslogd -N1` PASSは転送contractの存在を意味しない。

### 2.2 journald drop-in

実file:

```text
/etc/systemd/journald.conf.d/forward-to-syslog.conf
root:root 0644
```

実内容:

```ini
[Journal]
ForwardToSyslog=yesi
MaxLevelSyslog=debug
```

`ForwardToSyslog=yesi` は009の `yes` と不一致。journaldには次が記録された。

```text
Failed to parse ForwardToSyslog=yesi, ignoring: Invalid argument
```

vendor drop-inには別途 `ForwardToSyslog=yes` があるためsyslog socketへの転送自体は有効だが、
手動fileは参照設定と一致せず、意図した明示contractになっていない。

### 2.3 rsyslog forward fileの誤配置

期待path:

```text
/etc/rsyslog.d/49-monnie-forward.conf
```

結果: absent。

同名fileは誤って次へ配置されていた。

```text
/etc/systemd/journald.conf.d/49-monnie-forward.conf
root:root 0644
```

本文は009のrsyslog参照内容と一致するが、journald configとして読まれるpathなのでrsyslogには
includeされない。journaldは同fileについて複数行の
`Assignment outside of section. Ignoring.` を記録した。

したがってremote `omfwd` actionと後続 `stop` は実効化されていない。

### 2.4 副作用

010事前調査では不存在だったdefault local logが生成されている。

| path | 状態 |
|---|---|
| `/var/log/syslog` | present, root:adm 0640 |
| `/var/log/kern.log` | present, root:adm 0640 |
| `/var/log/user.log` | present, root:adm 0640 |
| `/var/log/messages` | absent |
| `/var/log/daemon.log` | absent |
| `/var/log/mail.log` | absent |

rsyslogのDebian default rulesが有効な一方、期待した `49-` の `stop` が無いため、重複local fileを
生成しないcontractはFAIL。

FRRは次を維持した。

- package既存
- service inactive / disabled
- `/var/log/frr` directory存在
- directory直下file 0件

FRR固有の副作用は確認していない。

### 2.5 journal流量

| window | lines | rate |
|---|---:|---:|
| 直近10分 | 262 | 26.2行/分 |
| 直近60分 | 857 | 14.3行/分 |
| 直近60分 warning..err | 9 | 0.15行/分 |

010調査のpve2基準は787行/時、13.1行/分。今回の60分平均14.3行/分は約9%増で、同程度の通常変動範囲と
判断する。送信元journalの流量は十分存在する。

## 3. monnie受信

```text
/var/log/pve-nodes.log: absent
```

期待した次の検証はfile未生成のため実施不能。

- 行頭が `error|warning|info|debug` の4値
- 2番目tokenが `pve2`
- owner `syslog:syslog`, mode `0644`
- 採取間のsize増加

monnie側はrsyslog / Alloy / Lokiがactive+enabled、Alloy ready。Alloyの直近fatal patternも0件だった。
受信file不存在の原因はmonnie runtime failureではなく、pve2側forward rule未ロードと整合する。

## 4. Loki pve stream

直近30分をread-only queryした。

| selector contract | streams | entries | 判定 |
|---|---:|---:|---|
| pve-nodes + host=pve2 + normalized level | 0 | 0 | FAIL |
| pve-nodes + host=pve1 | 0 | 0 | 正常（未適用） |

pve2には約14.3行/分のjournal入力がある一方、monnie file / Loki entryは0。end-to-end流量は整合しない。

## 5. 既存経路の非回帰

直近30分のLoki query:

| selector contract | streams | entries | 判定 |
|---|---:|---:|---|
| UniFi + fixed host | 1 | 1 | stream存在 |
| network-devices + extracted host | 4 | 146 | live |
| system + unit + normalized level | 11 | query limit 500 | live |

追加観測:

- monnie rsyslog / Alloy / Loki: active+enabled
- Alloy ready: PASS
- Alloy fatal pattern: 0件
- `/var/log/unifi-devices.log`: 前回記録値から増加
- `/var/log/unifi.log`: 前回記録値から変化なし

network-deviceとsystem journalは継続中。UniFi controller側fileは今回の短い観測窓で新着がなく、
「既存2ファイル双方が増加継続」の厳密gateは部分確認に留まる。これはpve2 E2E blockerとは別の観測事項。

## 6. 判定と再試験条件

must-fix:

1. journald drop-inの `ForwardToSyslog=yesi` を009どおり `yes` へ訂正する。
2. `49-monnie-forward.conf` をjournald directoryではなく `/etc/rsyslog.d/` の正規pathへ配置する。
3. full `rsyslogd -N1` PASS後にrsyslogを反映し、active/enabledを再確認する。
4. journaldが両fileのparse errorを新規記録しないことを確認する。
5. default local logの扱いを人間判断し、少なくとも正しい `stop` 反映後に増加停止を確認する。

修正後は次を再実証する。

- monnie `/var/log/pve-nodes.log` の生成・増加
- 全行の4値levelと2番目token `pve2`
- Loki pve2 streamのlevel付き直近entry
- pve2 journal生成rateとmonnie/Loki受信rateの概算整合
- 既存UniFi / network-device / system非回帰

今回testerはread-only制約を守り、誤設定の修正、service restart、生成log削除、test送信を行っていない。
IP address literalやsecretは本書に記録していない。

---

## 再確認: pve2 手動設定修正後

- 実施日: 2026-07-17
- 修正者: Yoshinobu
- tester操作: read-onlyのみ

### 結論

**PASS — pve2設定は009参照contractへ収束し、monnie fileとLokiまでlevel付きE2E着信した。**

- journald drop-in typo: 解消
- rsyslog forward file: 正規pathへ配置、内容一致
- 誤配置file: absent
- pve2 rsyslog / journald: active
- full `rsyslogd -N1`: PASS
- journald新規parse error: 0件
- default local log 3件: 30秒間size / mtime不変
- monnie pve file: generated, syslog:syslog 0644
- pve2行: 54、format不正0
- Loki pve2: 2 streams、直近57 entries、level付き
- pve1: 0 streams / 0 entries（未適用のため正常）
- journal / monnie受信rate: 近似整合
- 既存3系統: Lokiで継続確認

### 1. pve2設定

#### journald drop-in

```text
/etc/systemd/journald.conf.d/forward-to-syslog.conf
root:root 0644
```

内容:

```ini
[Journal]
ForwardToSyslog=yes
MaxLevelSyslog=debug
```

009参照内容と一致し、`yesi` typoは解消した。effective configにも `ForwardToSyslog=yes`,
`MaxLevelSyslog=debug` が反映されている。

#### rsyslog forward config

```text
/etc/rsyslog.d/49-monnie-forward.conf
root:root 0644
```

次のcontractを含み、009参照内容と一致した。

- canonical comment
- `action(type="omfwd")`
- target `monnie.internal`
- UDP syslog port
- `RSYSLOG_SyslogProtocol23Format`
- default file rulesより前の `stop`

以前の誤配置path `/etc/systemd/journald.conf.d/49-monnie-forward.conf` はabsent。

#### service / syntax / parse

| 項目 | 結果 |
|---|---|
| rsyslog | active / enabled |
| systemd-journald | active |
| full `rsyslogd -N1` | PASS |
| rsyslog recent error | 0件 |
| journald直近5分の旧誤配置 / typo parse error | 0件 |

修正前のparse errorはhistorical journalに残るが、restart後の新規発生は無い。

### 2. 副作用の収束

30秒差の2点観測:

| path | sample 1 | sample 2 | 判定 |
|---|---:|---:|---|
| `/var/log/syslog` | 366069 bytes | 366069 bytes | 増加停止 |
| `/var/log/kern.log` | 345332 bytes | 345332 bytes | 増加停止 |
| `/var/log/user.log` | 4166 bytes | 4166 bytes | 増加停止 |

3fileともmtimeも不変。正しい `49-` ruleの `stop` がdefault local file rulesより先に評価され、
修正前に生成されたfileへの追加書込は停止した。

既存FRR状態:

- service inactive / disabled
- `/var/log/frr` directory存在
- directory直下file 0件

FRRは従来状態を維持した。

### 3. monnie受信file

```text
/var/log/pve-nodes.log
syslog:syslog 0644
```

2回目採取時:

```text
lines=54
format_ok=54
format_bad=0
pve2=54
pve1=0
error=0
warning=1
info=53
debug=0
```

全行が次を満たした。

- 行頭levelは `error|warning|info|debug` のいずれか
- 2番目tokenは `pve2`
- pve1 token無し

fileは初回47行から54行へ増加し、mtime / sizeも更新。継続着信PASS。

### 4. Loki pve2 stream

直近10分の最終query:

| selector contract | streams | entries | labels | 判定 |
|---|---:|---:|---|---|
| pve-nodes + host=pve2 + normalized level | 2 | 57 | info / warning | PASS |
| pve-nodes + host=pve1 | 0 | 0 | — | 正常（未適用） |

file側のhost / level contractがAlloy pipelineでLoki labelへ昇格し、最新timestampまで継続している。

### 5. 流量整合

pve2の直近5分journal:

```text
131 lines / 5 min = 26.2 lines/min
warning..err = 1 line / 5 min
```

monnie pve fileは最初の行から54行目まで約134秒で、概算24.2行/分。計測窓の境界とrsyslog/Alloyの
flush timingが異なるため完全一致は要求しないが、差は約8%で同じorderかつ近似している。

010調査の長時間平均13.1行/分より今回の短時間rateは高いが、cluster / SSH / tester確認eventを含む
短時間窓のため許容できる。送信元journal、monnie file、Lokiの全段で継続増加を確認した。

### 6. 既存経路の非回帰

直近30分Loki query:

| selector contract | streams | entries | 判定 |
|---|---:|---:|---|
| UniFi + fixed host | 1 | 1 | 継続 |
| network-devices + extracted host | 5 | 152 | live |
| system + unit + normalized level | 13 | query limit 500 | live |

monnie rsyslog / Alloy / Lokiはactive+enabled、Alloy ready。system journalには正規化level実データがあり、
Alloy fatal patternは無い。

UniFi 2fileは再確認の2点採取間でsize不変だったが、双方のmtimeは直前数分内で、UniFi Loki entryと
network-deviceの複数最新entryを確認した。今回の短い窓で新規device eventが無かっただけで、既存3系統の
収集runtimeに非回帰は見られない。

### 7. 最終判定

- pve2手動設定: PASS
- pve2 local副作用の増加停止: PASS
- pve2 → monnie rsyslog: PASS
- monnie file format / growth: PASS
- Alloy pve discovery / tail: PASS
- Loki pve2 host / level: PASS
- pve2 journalとLoki ingestの概算流量: PASS
- pve1未適用: 想定どおり
- 既存3系統: PASS

testerは設定変更、service操作、生成済みlocal log削除、test送信を行っていない。
IP address literalやsecretは再確認追記にも記録していない。

---

## pve1 手動適用後 E2E 確認

- 実施日: 2026-07-17
- 修正者: Yoshinobu
- tester操作: read-onlyのみ

### 結論

**FAIL — rsyslog側は参照contractへ収束しているが、journald drop-inのtypoが残り、pve1のログはmonnie fileにもLokiにも着信していない。**

- journald drop-in: `ForwardToSyslog=yesi` のまま
- journald parse error: 直近10分に1件
- rsyslog forward file: 正規pathに存在し、009参照内容と一致
- 誤配置file: absent
- pve1 rsyslog / journald: active
- full `rsyslogd -N1`: PASS
- default local log 3件: 30秒間size / mtime不変
- monnie pve file: pve1行0、pve2行289
- Loki pve1: 0 streams / 0 entries
- Loki pve2と既存3系統: 継続

### 1. pve1設定

#### journald drop-in

```text
/etc/systemd/journald.conf.d/forward-to-syslog.conf
root:root 0644
```

実内容:

```ini
[Journal]
ForwardToSyslog=yesi
MaxLevelSyslog=debug
```

期待値 `ForwardToSyslog=yes` に対し末尾の `i` が残っている。effective configにも
`ForwardToSyslog=yesi` が現れ、systemd-journaldは次の設定行を無効として扱った。

```text
/etc/systemd/journald.conf.d/forward-to-syslog.conf:2: Failed to parse ForwardToSyslog=yesi, ignoring: Invalid argument
```

直近10分の該当parse errorは1件。したがって「typoなし」「parse error 0」の受入条件は未達。

#### rsyslog forward config

```text
/etc/rsyslog.d/49-monnie-forward.conf
root:root 0644
```

次のcontractを含み、009参照内容と一致した。

- canonical comment
- `action(type="omfwd")`
- target `monnie.internal`
- UDP syslog port
- `RSYSLOG_SyslogProtocol23Format`
- default file rulesより前の `stop`

以前の誤配置path `/etc/systemd/journald.conf.d/49-monnie-forward.conf` はabsent。

#### service / syntax / FRR

| 項目 | 結果 |
|---|---|
| rsyslog | active / enabled |
| systemd-journald | active / static |
| full `rsyslogd -N1` | PASS |
| FRR service | inactive / disabled |
| `/var/log/frr` | directory存在、直下file 0件 |

FRRは従来状態を維持した。

### 2. default local log副作用

手動適用時に次の3fileが生成されていた。

| path | sample 1 | sample 2（30秒後） | 判定 |
|---|---:|---:|---|
| `/var/log/syslog` | 368315 bytes | 368315 bytes | 増加停止 |
| `/var/log/kern.log` | 345367 bytes | 345367 bytes | 増加停止 |
| `/var/log/user.log` | 1380 bytes | 1380 bytes | 増加停止 |

3fileともmtimeも不変。正規pathのrsyslog `stop` ruleにより追加書込は停止しており、生成済みfileは
削除せずそのまま観測した。

### 3. monnie受信file

```text
/var/log/pve-nodes.log
syslog:syslog 0644
```

採取結果:

```text
lines=289
format_ok=289
format_bad=0
pve1=0
pve2=289
error=0
warning=1
info=288
debug=0
```

既存pve2行は全件format contractを満たし継続しているが、pve1行は1件も無い。

### 4. Loki

直近30分query:

| selector contract | streams | entries | labels | 判定 |
|---|---:|---:|---|---|
| pve-nodes + host=pve1 + normalized level | 0 | 0 | — | FAIL |
| pve-nodes + host=pve2 + normalized level | 2 | 289 | info / warning | 継続 |

pve1はmonnie file段階で未着信のため、Alloy / Lokiにも実データが無い。

### 5. 既存経路の非回帰

直近30分Loki query:

| selector contract | streams | entries | 判定 |
|---|---:|---:|---|
| UniFi + fixed host | 2 | 4 | 継続 |
| network-devices + extracted host | 5 | 188 | live |
| system + unit + normalized level | 11 | query limit 500 | live |

monnieのrsyslog / Alloy / Lokiはactive、Alloy ready。UniFi 2fileも存在し、再採取までに
`unifi-devices.log` のsize / mtimeが更新された。既存3系統およびjournal系に非回帰は見られない。

### 6. 最終判定と要修正点

- pve1 rsyslog forward config: PASS
- pve1 local副作用の増加停止: PASS
- pve1 journald drop-in / parse: FAIL
- pve1 → monnie rsyslog: FAIL
- monnie file pve1 host / level: FAIL（実データ0件）
- Loki pve1 host / level: FAIL（実データ0件）
- pve2継続: PASS
- 既存3系統 / journal系: PASS

pve1の `ForwardToSyslog=yesi` を009参照どおり `ForwardToSyslog=yes` へ修正し、journaldへの反映後に
同じread-only E2E確認が必要。現時点ではPhase 2のpve側を両ノード完成とは判定できない。

testerは設定変更、service操作、生成済みlocal log削除、test送信を行っていない。
IP address literalやsecretはpve1追記にも記録していない。

---

## pve1 typo修正後 最終E2E再確認

- 実施日: 2026-07-17
- 修正者: Yoshinobu
- tester操作: read-onlyのみ

### 結論

**FAIL — drop-inのtypo自体は修正されたが、修正後のjournald再起動が反映されておらず、pve1ログはmonnieにもLokiにも着信していない。**

したがって、現時点ではPhase 2のpve側を両ノード完成とは判定できない。

### 1. pve1設定と反映状態

`/etc/systemd/journald.conf.d/forward-to-syslog.conf` はroot:root 0644で、内容は期待値へ修正済み。

```ini
[Journal]
ForwardToSyslog=yes
MaxLevelSyslog=debug
```

effective configにも `ForwardToSyslog=yes` と `MaxLevelSyslog=debug` が現れる。drop-in更新時刻以降の
`Failed to parse ForwardToSyslog` は0件で、typo / 新規parse errorの条件自体はPASS。

一方、時刻関係は次のとおりだった。

| 項目 | 時刻 | 判定 |
|---|---|---|
| drop-in mtime | 2026-07-17 10:55:22 JST | 修正時刻 |
| journald ExecMainStartTimestamp | 2026-07-17 10:49:47 JST | 修正前 |
| journald ActiveEnterTimestamp | 2026-07-17 10:49:47 JST | 修正前 |

journaldのmain process / active開始時刻はいずれもdrop-in更新より前であり、「修正後に再起動済み」という
状態にはなっていない。process開始以降で見ると、修正前の `yesi` parse errorが1件残る。

rsyslogとsystemd-journaldはactive。full `rsyslogd -N1` はrc=0でPASSした。

### 2. pve1からmonnieへの着信

pve1ではdrop-in更新後にjournal 39行が生成されていたが、monnieの `/var/log/pve-nodes.log` は次の状態。

```text
lines=292
format_ok=292
format_bad=0
pve1=0
pve2=292
error=0
warning=1
info=291
debug=0
```

45秒差の追加観測でもfile size / mtimeは不変で、pve1=0、pve2=292のままだった。pve1の
`level 4値 + host=pve1` 実データ着信は未達。

### 3. Lokiと非回帰

直近30分query:

| selector contract | streams | entries | labels | 判定 |
|---|---:|---:|---|---|
| pve-nodes + host=pve1 + normalized level | 0 | 0 | — | FAIL |
| pve-nodes + host=pve2 + normalized level | 2 | 292 | info / warning | 継続 |
| UniFi + fixed host | 2 | 4 | — | 継続 |
| network-devices + extracted host | 5 | 181 | — | live |
| system + unit + normalized level | 11 | query limit 500 | error / info / warning | live |

monnieのrsyslog / Alloy / Lokiはactive、Alloy ready。pve2と既存3系統 / journal系には非回帰が無い。

### 4. 最終判定と必要対応

- pve1 drop-in typo修正: PASS
- drop-in更新後の新規parse error 0: PASS
- 修正後のjournald再起動反映: FAIL
- pve1 → monnie file: FAIL
- Loki pve1 host / level: FAIL
- pve2継続: PASS
- 既存3系統 / journal系: PASS

pve1でsystemd-journaldを実際に再起動し、`ExecMainStartTimestamp` がdrop-in更新後へ変わったことを確認したうえで、
同じread-only E2E確認が必要。

testerは設定変更、service操作、生成済みlocal log削除、test送信を行っていない。
IP address literalやsecretは最終再確認追記にも記録していない。

---

## pve1 journald再起動後 最終E2E確認

- 実施日: 2026-07-17
- 修正者: Yoshinobu
- tester操作: read-onlyのみ

### 結論

**FAIL — journaldの修正後再起動とparse error解消は確認できたが、pve1実データはmonnie fileにもLokiにも着信していない。**

Phase 2のpve側を両ノード完成とするためのE2E条件は、今回も未達。

### 1. journald再起動反映

| 項目 | 時刻 / 値 | 判定 |
|---|---|---|
| drop-in mtime | 2026-07-17 10:55:22 JST | 基準時刻 |
| journald ExecMainStartTimestamp | 2026-07-17 11:02:40 JST | 更新後 |
| journald ActiveEnterTimestamp | 2026-07-17 11:02:40 JST | 更新後 |
| `ForwardToSyslog` | `yes` | PASS |
| `MaxLevelSyslog` | `debug` | PASS |
| 再起動後parse error | 0件 | PASS |

systemd-journaldとrsyslogはactive。前回未反映だったjournald再起動は、今回はdrop-in更新後の時刻へ
変わっておりPASS。再起動後にpve1 journal 50行が存在した。

### 2. monnie file

`/var/log/pve-nodes.log` はsyslog:syslog 0644で存在し、採取結果は次のとおり。

```text
lines=446
format_ok=446
format_bad=0
pve1=0
pve2=446
error=0
warning=1
info=445
debug=0
```

pve2行は継続して増え、全行がformat contractを満たす。一方、journald再起動後にもpve1行は0件で、
`level 4値 + host=pve1` の受入条件はFAIL。

### 3. Lokiと非回帰

直近30分query:

| selector contract | streams | entries | labels | 判定 |
|---|---:|---:|---|---|
| pve-nodes + host=pve1 + normalized level | 0 | 0 | — | FAIL |
| pve-nodes + host=pve2 + normalized level | 2 | 446 | info / warning | 継続 |
| UniFi + fixed host | 2 | 5 | — | 継続 |
| network-devices + extracted host | 5 | 119 | — | live |
| system + unit + normalized level | 10 | query limit 500 | error / info / warning | live |

monnieのrsyslog / Alloy / Lokiはactive、Alloy ready。pve2と既存3系統 / journal系には非回帰が無い。

### 4. 最終判定

- pve1 journald修正後再起動: PASS
- pve1再起動後parse error 0: PASS
- pve1 journal生成: PASS（50行）
- pve1 → monnie file: FAIL（0行）
- Loki pve1 host / level: FAIL（0 streams / 0 entries）
- pve2継続: PASS
- 既存3系統 / journal系: PASS
- Phase 2 pve側両ノード完成: **未達**

pve1ではjournal生成からmonnie受信までの区間に未解消の問題が残る。送信側rsyslogのruntime処理と
network到達を切り分けた後、同じread-only E2E確認が必要。

testerは設定変更、service操作、生成済みlocal log削除、test送信を行っていない。
IP address literalやsecretは本追記にも記録していない。

---

## pve1 rsyslog再起動後 最終E2E確認

- 実施日: 2026-07-17
- 修正者: Yoshinobu
- tester操作: read-onlyのみ

### 結論

**PASS — rsyslog再起動後、pve1ログはlevel / host contractを満たしてmonnie fileとLokiへ着信した。Phase 2 pve側両ノード完成。**

### 1. pve1 rsyslog runtime反映

| 項目 | 時刻 / 結果 | 判定 |
|---|---|---|
| `49-monnie-forward.conf` mtime | 2026-07-17 10:45:17 JST | 基準時刻 |
| rsyslog ExecMainStartTimestamp | 2026-07-17 11:13:34 JST | 配置後 |
| rsyslog ActiveEnterTimestamp | 2026-07-17 11:13:35 JST | 配置後 |
| rsyslog | active | PASS |
| systemd-journald | active | PASS |
| full `rsyslogd -N1` | rc=0 | PASS |
| rsyslog再起動後のerror pattern | 0件 | PASS |

rsyslogのmain processは `49-monnie-forward.conf` 配置後に起動しており、forward ruleを読み込んだ
runtimeへ収束した。rsyslog再起動後のpve1 journalは285行で、送信元にも十分な実データがある。

### 2. monnie file

`/var/log/pve-nodes.log` はsyslog:syslog 0644で、採取結果は次のとおり。

```text
lines=898
format_ok=898
format_bad=0
pve1=293
pve2=605
error=0
warning=2
info=896
debug=0
```

pve1が293行着信開始し、pve2も605行へ増加。全898行が `level 4値 + host=pve1|pve2` のformat
contractを満たし、不正行は0件。

### 3. Lokiと非回帰

直近30分query:

| selector contract | streams | entries | labels | 判定 |
|---|---:|---:|---|---|
| pve-nodes + host=pve1 + normalized level | 2 | 293 | info / warning | PASS |
| pve-nodes + host=pve2 + normalized level | 1 | 316 | info | 継続 |
| UniFi + fixed host | 1 | 4 | — | 継続 |
| network-devices + extracted host | 2 | 34 | — | live |
| system + unit + normalized level | 9 | query limit 500 | error / info / warning | live |

monnieのrsyslog / Alloy / Lokiはactive、Alloy ready。pve1のfile件数とLoki件数はともに293で一致し、
host / level label付きE2Eが成立した。pve2と既存3系統 / journal系にも非回帰が無い。

### 4. Phase 2 pve側 最終判定

- pve1 rsyslog runtime反映: PASS
- pve1 → monnie file: PASS
- Loki pve1 host / level: PASS
- pve2継続: PASS
- 既存3系統 / journal系: PASS
- **Phase 2 pve側両ノード完成: PASS**

直前までのFAIL原因は、pve1で `49-monnie-forward.conf` 配置前から動作していたrsyslog processが
forward ruleを未ロードだったことと整合する。rsyslog再起動後に全段の実データを確認できたため、
Phase 2のpve側はpve1 / pve2とも完成と判定する。

testerは設定変更、service操作、生成済みlocal log削除、test送信を行っていない。
IP address literalやsecretは完成判定の追記にも記録していない。

---

## Sophos SFOS GUI有効化後 E2E確認

- 実施日: 2026-07-17
- 修正者: Yoshinobu
- tester操作: read-onlyのみ

### 結論

**FAIL — Sophos実データはmonnieのUDP syslog受信点へ到達しておらず、受信fileもLoki streamも未生成。**

rsyslog allowlistと `sophos-fw.internal` の実行時名前解決結果は一致している。期待する送信元からの
UDP/514 packet自体が観測されないため、現時点の障害点はSophos送信設定またはSophosからmonnieまでの
network経路側にある。

### 1. monnie受信file

```text
/var/log/sophos-fw.log: absent
```

したがって、受信templateの `level 4値 + 2番目token` formatを評価できる実データは0件。

### 2. Loki

直近60分query:

| selector contract | streams | entries | labels | 判定 |
|---|---:|---:|---|---|
| sophos-fw + host=sophos-fw + normalized level | 0 | 0 | — | FAIL |

file未生成とLoki 0件が一致しており、Alloyより前のrsyslog受信段階で未着信。

### 3. allowlist / 実送信元の切り分け

- `15-observability-sources.conf` のSophos ruleは `sophos-fw.internal` の解決先をallowlistしている
- tester確認時の `sophos-fw.internal` 実行時解決結果とallowlistは一致
- 期待するSophos送信元だけに絞った45秒間のUDP/514 capture: 0 packets
- pve1 / pve2を除外した別capture: 3 packets
- 別captureの送信元は `u7-1f` / `u7-2f` で、いずれも既存 `10-unifi.conf` allowlist対象
- Sophosの別interfaceと判断できる未登録送信元: 観測なし
- `/var/log/syslog` とjournal: Sophos / SFOS形式の未マッチ実データなし
- rsyslog service journal: 直近errorなし

monnieでは同じUDP/514にpve1 / pve2とUniFi APのpacketが継続到着しているため、listener自体は機能している。
Sophos側で送信先、UDP port、送信event種別、送信元interface / routeを再確認する必要がある。

### 4. 既存5系統の非回帰

直近60分Loki query:

| selector contract | streams | entries | 判定 |
|---|---:|---:|---|
| UniFi + fixed host | 1 | 6 | 継続 |
| network-devices + extracted host | 4 | 165 | live |
| pve-nodes + host=pve1 + normalized level | 1 | query limit 500 | live |
| pve-nodes + host=pve2 + normalized level | 1 | query limit 500 | live |
| system + unit + normalized level | 11 | query limit 500 | live |

monnieのrsyslog / Alloy / Lokiはactive、Alloy ready。既存5系統に非回帰は見られない。

### 5. 最終判定

- Sophos → monnie UDP/514: FAIL（期待送信元packet 0）
- `/var/log/sophos-fw.log`: FAIL（absent）
- Sophos file format: 未評価（実データ0件）
- Loki sophos-fw host / level: FAIL（0 streams / 0 entries）
- allowlistと名前解決: PASS（一致）
- 別interface由来のSophos packet: 観測なし
- 既存5系統: PASS

testerは設定変更、service操作、packet生成、test送信を行っていない。
IP address literalやsecretはSophos追記にも記録していない。

---

## Sophos 一時送信条件緩和中 E2E確認

- 実施日: 2026-07-17
- SFOS一時条件: severity debug、traffic log ON
- tester操作: read-onlyのみ

### 結論

**PASS — 一時条件下でSophosからmonnieへのUDP syslog、受信file、Alloy、LokiまでのE2Eが成立した。**

直前の未着信はallowlistやnetwork経路の恒常的な不備ではなく、SFOS側の送信event条件によるものと
判断できる。ただし、一時検証後に「エラー以上のみ」へ戻した通常設定では、実際のerror event発生時に
別途E2Eを確認する必要がある。

### 1. UDP/514到着

`sophos-fw.internal` の送信元だけに絞った短時間captureで、上限10 packetsへ即時到達した。

```text
captured=10
received_by_filter=10
dropped_by_kernel=0
facility_severity=daemon.info
```

allowlist対象の期待送信元からmonnieへ到着しており、別interfaceや送信元不一致ではない。

### 2. monnie受信file

`/var/log/sophos-fw.log` がsyslog:syslog 0644で生成され、採取結果は次のとおり。

```text
lines=558
format_ok=558
format_bad=0
error=0
warning=0
info=558
debug=0
```

全558行が受信templateの `level 4値 + 2番目token` contractを満たし、不正行は0件。

### 3. Loki

直近30分query:

| selector contract | streams | entries | labels | 判定 |
|---|---:|---:|---|---|
| sophos-fw + host=sophos-fw + normalized level | 1 | query limit 500 | info | PASS |

Sophos実データが `job=sophos-fw`、`host=sophos-fw`、normalized `level=info` を持ってLokiまで到達した。

### 4. 判定

- Sophos → monnie UDP/514: PASS
- `/var/log/sophos-fw.log` 生成 / format: PASS
- Alloy tail / label昇格: PASS
- Loki sophos-fw host / level: PASS
- 一時条件下のtransport E2E: **PASS**
- 通常のerror以上設定での実error E2E: event発生時に要確認

testerはSFOS設定変更、service操作、packet生成、test送信を行っていない。
IP address literalやsecretは一時検証追記にも記録していない。
