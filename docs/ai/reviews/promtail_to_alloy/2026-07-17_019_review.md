# Alloy Phase 2 拡張（Ubuntu nodes journald 集約）再レビュー

## 判定

**Approve**。`2026-07-17_018_review.md` の must-fix 3件は解消され、軽微3点も反映されている。追加の blocking finding はない。

## must-fix 再確認

1. **check mode の service state assert**: 解消。
   - `Read rsyslog active state` と `Read systemd-journald active state` の両方に `check_mode: false` が追加された。
   - read-only の `systemctl is-active` が check mode でも実行されるため、既導入ノードで skipped result の空 `stdout` を assert する問題はない。

2. **rollback validation の成功判定**: 解消。
   - 復元後 restart の条件が `rsyslog_forward_restore_validation['rc'] == 0` になった。
   - `failed_when: false` による task status と rsyslogd の実際の終了コードを混同しない。
   - rollback failure message に validation の rc / stderr が含まれ、非ゼロ時の診断情報も残る。

3. **単一ノード rollout gate**: 解消。
   - playbook の `pre_tasks` に `run_once: true` の assert が追加され、`ansible_play_hosts_all | length == 1` を要求する。
   - limit なしの ansy / quory / authy 連続 APPLY は開始前に fail-closed となる。
   - fail message に適用順、各 E2E gate、authy の quory-mediated mute が明記されている。

## 軽微指摘の再確認

- journald drop-in に role ownership marker の検証が追加され、別管理の既存ファイルを上書きしない。
- authy activation failure 時に package を自動削除しない範囲と、手動 rollback 文書への誘導が failure message に追加された。
- UniFi overlap の fail message が全 remote syslog sender を表す文言へ一般化された。

## 検証結果

- `ANSIBLE_LOCAL_TEMP=/tmp ansible-playbook --syntax-check playbooks/rsyslog_forward_to_monnie.yml`: PASS
- `ANSIBLE_LOCAL_TEMP=/tmp ansible-playbook --syntax-check playbooks/alloy_setup.yml`: PASS
- `scripts/check-tester-gate.sh`: PASS（35 playbooks）
- `git diff --check`: PASS

本レビュー範囲では approve とする。実環境 APPLY は文書どおり ansy → quory → authy の単一 host limit、各ノード後の E2E gate、authy 前の mute を維持すること。
