# Alloy Phase 2 Ubuntu nodes extension dry-run test result

- 実施日: 2026-07-17
- 対象requirement: `2026-07-17_016_requirement.md`
- 対象implementation: `2026-07-17_017_implement.md`
- reviewer判定: `2026-07-17_019_review.md` Approve
- local temp: `ANSIBLE_LOCAL_TEMP=/tmp/codex-ansible-local`
- tester操作: syntax-check、check mode、read-only snapshotのみ
- APPLY: **未実施**

## 結論

**PASS — 2 playbookのsyntax、monnie受信側check、ansy/quory/authy個別sender check、複数host fail-closed、事後非変更をすべて確認した。**

- syntax-check: 2 / 2 PASS
- monnie check: failed=0
- sender個別check: 3 / 3 failed=0
- must-fix1 check-mode active state: ansy / quoryともPASS
- authy fresh-install check contract: PASS
- limitなし実行: role開始前にfail-closed
- post-check file / package / service state: 不変
- destructive operation / restart / APPLY: 0件

## 1. syntax-check

実行結果:

| playbook | exit | 判定 |
|---|---:|---|
| `playbooks/alloy_setup.yml` | 0 | PASS |
| `playbooks/rsyslog_forward_to_monnie.yml` | 0 | PASS |

両方ともplaybook名を正常に出力し、syntax errorは無かった。

## 2. monnie受信側 check mode

実行contract:

```text
scripts/safe-ansible-check.sh playbooks/alloy_setup.yml -l monnie --check
```

recap:

```text
monnie: ok=50 changed=3 unreachable=0 failed=0 skipped=19 rescued=0 ignored=0
```

### 評価された変更

check modeのchanged予測は3件。

1. remote rsyslog route candidate
2. remote syslog logrotate candidate
3. Alloy config

次の新規contractを含むassertがPASSした。

- `ubuntu_nodes` file source
- `/var/log/ubuntu-nodes.log`
- `job=ubuntu-nodes`
- dynamic second-token host extraction
- normalized 4-value level extraction
- ansy / quory / authy sender input
- 既存pve1 / pve2 / Sophosを含む6 senderのruntime DNS解決
- sender identityの一意性
- 既存UniFi allowlistとの非重複
- monnie journal sourceの `job=ubuntu-nodes`, `host=monnie` contract

### destructive skip / 非回帰

check modeではstaging native validation、promotion、full rsyslog validation、rsyslog restart、Alloy validate、
Promtail stop/disable、Alloy restart、runtime settle/ready確認をすべてskipした。APPLYに相当する操作は無い。

既存file source `unifi` / `network_devices` / `pve_nodes` / `sophos_fw` とjournal sourceも新規sourceと
同じinput / rendered pipeline contract検査を通過した。既存UniFi config ownership/allowlist境界、既存Grafana
repository、Alloy package/service/user/group契約もPASS。runtime非回帰は設定を変更していないため、事後snapshotで
serviceと現行configが不変であることを別途確認した。

`ubuntu-nodes.log` は `require_existing_file=false` のため、sender APPLY前のabsent状態を許容するcontractとして
file check / assertがPASSした。

## 3. sender個別 check mode

各実行はsafe wrapper、明示single-host limit、`--check` を使用した。

| host | ok | changed | skipped | failed | 判定 |
|---|---:|---:|---:|---:|---|
| ansy | 22 | 2 | 13 | 0 | PASS |
| quory | 22 | 2 | 13 | 0 | PASS |
| authy | 19 | 2 | 16 | 0 | PASS |

### ansy / quory

両nodeで次がPASSした。

- single-host rollout assert
- role input / local candidate render / forward contract assert
- recon済みsnippet inventory assert
- unmanaged ownership collision gate
- rsyslog既導入baseline assert
- rsyslog enabled+started native check
- `Read rsyslog active state`: check modeでも実行され `ok`
- `Read systemd-journald active state`: check modeでも実行され `ok`
- `Require active sender services`: `ok`

must-fix1の `check_mode: false` によりread-only state taskはskipされず、空stdoutをassertする誤FAILは再現しなかった。

changed予測はjournald drop-in directoryとrsyslog staging candidateの2件。actual staging validation、promotion、
full validation、restart、activation journal gateはcheck modeのためskipした。

### authy

authyはrsyslog未導入baselineを維持したまま次がPASSした。

- single-host rollout assert
- role input / local candidate render / stop付きforward contract assert
- recon済みsnippet inventory assert
- unmanaged ownership collision gate
- fresh-install baseline assert
- rsyslog package install: changed予測
- journald drop-in directory: changed予測
- systemd-journald active state read: `ok`

native check modeはpackageを実際には導入しないため、remote rsyslog directory/binaryを必要とするstaging validation、
promotion、full validation、rsyslog state/assertを意図どおりskipした。local candidate renderとcontract assertは
skipされず、authyの `stop` contractまで評価された。

### journald drop-inのcheck mode挙動

3nodeともdrop-in directoryが事前absentのため、directory作成はchanged予測となるが実体は作られない。その後の
drop-in template taskはnative check modeではskipされる。drop-in内容自体は先行するlocal candidate render / contract
assertで検証される。この挙動はcheck modeの既知制約であり、APPLY時のremote file検証は人間gate後の対象となる。

## 4. 複数host fail-closed

limitを付けず、safe wrapperで1回実行した。

```text
scripts/safe-ansible-check.sh playbooks/rsyslog_forward_to_monnie.yml --check
```

結果:

```text
exit=2
ansy: ok=0 changed=0 unreachable=0 failed=1
assertion: ansible_play_hosts_all | length == 1
```

`Require an explicit single-host rollout limit` がrole開始前にfailし、apply順とauthy mute gateを含むfail messageを
出力した。role taskは0件、変更予測も0件。単一node rollout gateはfail-closedで機能する。

## 5. dry-run前後の非変更確認

### monnie

次の3fileはsize、mtime、owner/mode、SHA-256が事前値と完全一致した。

- `/etc/rsyslog.d/15-observability-sources.conf`
- `/etc/logrotate.d/observability-remote-syslog`
- `/etc/alloy/config.alloy`

rsyslog / systemd-journald / Alloyはactive state、enabled/static state、MainPID、ExecMainStartTimestampが
事前値と一致。restartは発生していない。

### ansy / quory

- `/etc/rsyslog.d/49-monnie-forward.conf`: 前後ともabsent
- `/etc/systemd/journald.conf.d/forward-to-syslog.conf`: 前後ともabsent
- rsyslog package version: `8.2512.0-1ubuntu4` のまま
- rsyslog / systemd-journald: state、MainPID、ExecMainStartTimestamp不変

### authy

- `dpkg -l rsyslog`: 前後とも `un`、versionなし
- rsyslog service: 前後ともinactive / not-found相当、MainPID 0
- `/etc/rsyslog.d/49-monnie-forward.conf`: 前後ともabsent
- `/etc/systemd/journald.conf.d/forward-to-syslog.conf`: 前後ともabsent
- systemd-journald: active/static、MainPID、ExecMainStartTimestamp不変

### staging residue

次のstaging pathは全対象nodeで事前・事後ともabsent。

- `15-observability-sources.conf.staging`
- `observability-remote-syslog.staging.disabled`
- `49-monnie-forward.conf.staging`

package install、設定promotion、service restart、mute操作、test message生成は実施していない。

## 6. 最終判定

- alloy_setup syntax-check: PASS
- sender playbook syntax-check: PASS
- monnie receiver check: PASS
- ansy sender check: PASS
- quory sender check: PASS
- authy fresh-install sender check: PASS
- must-fix1 check-mode service assert: PASS
- multi-host fail-closed: PASS
- post-check unchanged: PASS
- APPLY authorization: **未付与・未実行**

dry-run gateはすべてPASS。次工程はYoshinobuの明示判断後に限り、requirementどおりmonnie受信側から段階APPLYし、
senderはansy → quory → authyの順で各E2E gateを挟む。authy APPLY前のmuteはquory経由で実行する。

IP address literalやsecretは本test resultに記録していない。
