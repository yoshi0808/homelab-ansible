# Alloy Phase 2 Ubuntu nodes staged APPLY test result

- 実施日: 2026-07-17
- 対象requirement: `2026-07-17_016_requirement.md`
- 対象implementation: `2026-07-17_017_implement.md`
- dry-run: `2026-07-17_020_test_result.md` PASS
- authorization: Yoshinobu明示GO（今回限りのtester APPLY例外）
- local temp: `ANSIBLE_LOCAL_TEMP=/tmp/codex-ansible-local`
- rollout順: monnie → ansy → quory → authy

## 結論

**FAIL-CLOSED — STEP1 monnie、STEP2 ansy、STEP3 quoryは全gate PASS。STEP4 authyはE2E PASSだが、fresh install中にdefault local logが新規生成され、「syslog非生成」gateを満たさなかった。**

指示どおりFAIL判明時点で停止し、authy muteはclearせず維持した。生成済みfileの削除、設定修正、rollback、
追加APPLYは行っていない。

| step | target | APPLY | E2E | local副作用 | 最終gate |
|---|---|---|---|---|---|
| 1 | monnie receiver | PASS | PASS | 非回帰 | PASS |
| 2 | ansy sender | PASS | PASS | syslog継続 | PASS |
| 3 | quory sender | PASS | PASS | syslog継続 | PASS |
| 4 | authy sender | PASS | PASS | local log新規生成 | **FAIL** |

## STEP1: monnie receiver

### mute

quory経由で `homelab-mute set monnie 60` を実行し、APPLY前に `MUTED(60m)` を確認した。

### APPLY

```text
ansible-playbook playbooks/alloy_setup.yml -l monnie
```

recap:

```text
ok=69 changed=10 unreachable=0 failed=0 skipped=0 rescued=0 ignored=0
```

staging rsyslog / logrotate candidate validation、promotion、full rsyslog validation、rsyslog restart、Alloy config
validate/restart、15秒settle、active/ready/runtime journal assertまで全task PASS。

### gate

- `/etc/rsyslog.d/15-observability-sources.conf`: root:root 0644
- `/etc/logrotate.d/observability-remote-syslog`: root:root 0644
- `/etc/alloy/config.alloy`: root:alloy 0640
- rsyslog / logrotate staging: absent
- `alloy validate /etc/alloy/config.alloy`: rc=0
- rsyslog / Alloy: active
- Alloy ready endpoint: ready
- `/var/log/ubuntu-nodes.log`: sender未適用時点では未生成、contract上許容
- Alloy / rsyslog priority error: 0件

新しい観測epochから20秒後のLoki:

| selector | streams | entries | labels | 判定 |
|---|---:|---:|---|---|
| ubuntu-nodes + host=monnie + normalized level | 6 | 14 | error / info / warning | PASS |
| system + host=monnie | 0 | 0 | — | 新規増加停止 |

旧 `job=system, host=monnie` の過去データは残存し、直近12時間queryで取得可能だった。

既存5系統:

| selector | entries | 判定 |
|---|---:|---|
| UniFi | 6 | 継続 |
| network-devices | 236 | live |
| pve-nodes host=pve1 | query limit 500 | live |
| pve-nodes host=pve2 | 319 | live |
| sophos-fw | query limit 500 | 過去実データ保持 |

全gate PASS後、quory経由でmonnie muteをclearし、status `-` を確認した。

## STEP2: ansy sender

### APPLY

```text
ansible-playbook playbooks/rsyslog_forward_to_monnie.yml -l ansy
```

recap:

```text
ok=32 changed=7 unreachable=0 failed=0 skipped=3 rescued=0 ignored=0
```

staging validation、promotion、full `rsyslogd -N1`、rsyslog/journald restart、10秒settle、service stateと
activation journal assertまでPASS。

### gate

- `49-monnie-forward.conf`: root:root 0644、active `stop` 0行
- journald drop-in: root:root 0644、`ForwardToSyslog=yes`、`MaxLevelSyslog=debug`
- effective journald config: 期待値反映
- staging: absent
- full `rsyslogd -N1`: rc=0
- rsyslog: active / enabled
- systemd-journald: active / static
- rsyslogd AppArmor profile: loaded / enforce
- AppArmor denial: 0件
- service priority error: 0件

monnie file:

```text
ansy=70
format_bad=0
levels=error:5, info:60, warning:5
```

Loki `job=ubuntu-nodes, host=ansy`: 3 streams、62 entries、error/info/warning。host / level付き実データPASS。

`/var/log/syslog` 30秒差:

```text
18095474 bytes -> 18096162 bytes (+688)
```

stopなしで既存local loggingが継続している。STEP2全gate PASS。

## STEP3: quory sender

### APPLY

```text
ansible-playbook playbooks/rsyslog_forward_to_monnie.yml -l quory
```

recap:

```text
ok=32 changed=7 unreachable=0 failed=0 skipped=3 rescued=0 ignored=0
```

restart後、playbookとは別のread-only Ansible ad-hoc `uptime` がexit 0で応答し、quory自身へのAnsible疎通維持を確認した。

### gate

- `49-monnie-forward.conf`: root:root 0644、active `stop` 0行
- journald drop-in / effective config: 期待値反映
- staging: absent
- full `rsyslogd -N1`: rc=0
- rsyslog / systemd-journald: active
- AppArmor denial: 0件
- service priority error: 0件

monnie file:

```text
quory=51
format_bad=0
levels=info:51
```

Loki `job=ubuntu-nodes, host=quory`: 1 stream、48 entries、info。host / level付き実データPASS。

`/var/log/syslog` 30秒差:

```text
4043017 bytes -> 4043289 bytes (+272)
```

stopなしで既存local loggingが継続している。STEP3全gate PASS。

## STEP4: authy sender

### mute

quory経由で `homelab-mute set authy 60` を実行し、APPLY前に `MUTED(60m)` を確認した。

### APPLY

```text
ansible-playbook playbooks/rsyslog_forward_to_monnie.yml -l authy
```

recap:

```text
ok=33 changed=8 unreachable=0 failed=0 skipped=2 rescued=0 ignored=0
```

rsyslog fresh install、staging validation、promotion、full validation、両service restart、10秒settle、service state、
activation journal assertはPASS。

### PASSしたgate

- rsyslog package: `ii`, version `8.2512.0-1ubuntu4`
- rsyslog: active / enabled
- systemd-journald: active / static
- `49-monnie-forward.conf`: root:root 0644、active `stop` 1行
- journald drop-in / effective config: 期待値反映
- staging: absent
- full `rsyslogd -N1`: rc=0
- rsyslogd AppArmor profile: install後にloaded / enforce
- AppArmor denial: 0件
- service priority error: 0件

monnie file:

```text
authy=45
format_bad=0
levels=info:44, warning:1
```

Loki `job=ubuntu-nodes, host=authy`: 2 streams、42 entries、info/warning。E2EはPASS。

### FAILしたlocal副作用gate

fresh install前はabsentだったdefault local logが、APPLY中に新規生成された。

| path | sample 1 | sample 2（30秒後） | 状態 |
|---|---:|---:|---|
| `/var/log/syslog` | 118961 bytes | 118961 bytes | 新規生成、増加停止 |
| `/var/log/kern.log` | 94328 bytes | 94328 bytes | 新規生成、増加停止 |
| `/var/log/auth.log` | 8074 bytes | 8074 bytes | 新規生成、増加停止 |
| `/var/log/mail.log` | absent | absent | 未生成 |

30秒間size / mtime不変のため、最終runtimeでは49番 `stop` が機能している。しかしacceptanceは
「authyの `/var/log/syslog` が新規生成されていない」であり、生成済みという事実によりFAIL。

apt installによるrsyslog auto-startから管理config配置/restartまでの短いwindowで、Ubuntu default
`50-default.conf` がlocal fileを生成した挙動と整合する。

### fail-closed処理

- FAIL検出直後に次工程・cleanup・追加APPLYを停止
- 生成済みlocal fileは削除していない
- role config / package / serviceをrollbackしていない
- authy muteはclearしていない
- quory上の最終status: `MUTED(59m)`

## 最終判定

- monnie receiver APPLY / gate: PASS
- ansy sender APPLY / gate: PASS
- quory sender APPLY / connectivity / gate: PASS
- authy sender APPLY / E2E: PASS
- authy no-new-local-syslog gate: **FAIL**
- staged rollout総合: **FAIL-CLOSED**
- monnie mute: cleared
- authy mute: **activeのまま維持**

次の判断は人間gateが必要。少なくとも、authyでpackage auto-startを抑止してconfig確定後に明示startする設計へ
修正するか、生成済みだが増加停止したlocal filesを受入可能とするかを決める必要がある。testerはどちらも仮定せず、
現状態を保持した。

IP address literalやsecretは本test resultに記録していない。

---

## Yoshinobu受容後 cleanup / 最終判定

Yoshinobuは、authyの3 local filesをinstall auto-start windowで生じた良性artifactとして受容し、
指定fileの削除と再生成確認後にmuteをclearする判断を行った。この人間gateに基づき、指定範囲だけを処理した。

### 1. authy local artifact cleanup

明示指定された次の3fileだけを削除した。

- `/var/log/syslog`
- `/var/log/kern.log`
- `/var/log/auth.log`

削除直後と30秒後の2点観測:

| path | 直後 | 30秒後 | 判定 |
|---|---|---|---|
| `/var/log/syslog` | absent | absent | 再生成なし |
| `/var/log/kern.log` | absent | absent | 再生成なし |
| `/var/log/auth.log` | absent | absent | 再生成なし |

rsyslog / systemd-journaldは観測後もactive。49番の `stop` が最終runtimeで機能し、default local fileは
再生成されなかった。他のfile、package、設定、serviceには触れていない。

### 2. authy mute clear

cleanup gate PASS後、quory経由で `homelab-mute clear authy` を実行した。最終status:

```text
authy: -
monnie: -
```

authy / monnieともactive muteなし。

### 3. ubuntu-nodes 4 host分離

直近30分Loki query:

| host | streams | entries | normalized levels | 判定 |
|---|---:|---:|---|---|
| monnie | 8 | query limit 500 | error / info / warning | PASS |
| ansy | 3 | 390 | error / info / warning | PASS |
| quory | 1 | 255 | info | PASS |
| authy | 3 | 138 | debug / info / warning | PASS |

4 hostとも `job=ubuntu-nodes` の同一job意味論を持ち、`host` labelで分離され、level付き実データが継続している。

`/var/log/ubuntu-nodes.log`:

```text
owner=syslog:syslog
mode=0644
lines=783
format_ok=783
format_bad=0
```

全行が `level 4値 + sender host` contractを満たす。

### 4. 既存系統の最終非回帰

| selector | streams | entries | 判定 |
|---|---:|---:|---|
| UniFi + fixed host | 2 | 6 | 継続 |
| network-devices + extracted host | 5 | 268 | live |
| pve-nodes + host=pve1 | 1 | query limit 500 | live |
| pve-nodes + host=pve2 | 1 | 316 | live |
| sophos-fw + fixed host | 1 | query limit 500 | 実データ保持 |

monnieのrsyslog / Alloyはactive、Alloy ready。既存5系統に非回帰は無い。

### 5. 最終受容判定

- STEP1 monnie receiver: PASS
- STEP2 ansy sender: PASS
- STEP3 quory sender / connectivity: PASS
- STEP4 authy sender / E2E: PASS
- authy install-window artifact: 増加停止確認済み、Yoshinobu受容、cleanup済み
- authy local file再生成: 30秒間0件
- ubuntu-nodes 4 host分離: PASS
- 既存5系統: PASS
- authy / monnie mute: clear確認済み
- **Alloy Phase 2 Ubuntu nodes staged rollout: 最終PASS**

直前のFAIL-CLOSED判定は、明示された副作用gateに対して正しく停止した時点の記録として維持する。その後、
Yoshinobuの明示受容と限定cleanup指示を受け、再生成なしと全E2E/non-regressionを確認したため、本節が最終判定を
上書きする。

IP address literalやsecretは最終追記にも記録していない。
