# audit: syslog週次ダイジェスト

日付: 2026-09-04
対象: `docs/ai/reviews/syslog_weekly_digest/`(`_001_requirement` / `_002_implement` / `_003_review_codex` / `_004_test_result`)、commit `2eeb51c`

## 再構成できたか

**できた。** 経緯は次のとおり読み取れる。

1. Yoshinobu提起(2026-09-01)→着手前実測(loki-count/loki-window)→requirement確定(§4 AC1〜AC7、§6設計判断)。
2. Implementer初版→独立レビュー(codex)5巡。各巡の指摘・再現・是正が`_002_implement.md`に「finding表」として時系列で残り、**5回目是正の原因がCoordinator自身の4回目指示の誤りだったこと**まで含め、責任の所在ごと記録されている。
3. 途中でrequirement自体が2回訂正されている(tester-gate分類の誤り、秘密情報契約の未決着)。両方とも「誰が・いつ・何を根拠に」直したかが`_001_requirement.md`側とcommitメッセージの双方から辿れる。
4. `_003_review_codex.md`はApprove、`_004_test_result.md`は実装記録・レビューの記述を前提とせず独立に再現・再fuzzした上で一致を確認しており、その相違点(sandboxのsocket権限差)も明記されている。
5. commit `2eeb51c`の差分は、4つの記録が主張する成果物一覧(role/playbook/filter/catalog2件/.gitignore/README)と1対1で一致し、作業ツリーはclean。

以下、現物突合で確認した点。

- `roles/syslog_weekly_digest/files/syslog-weekly-digest-collect.py`: `MAX_SERIES=500`は1箇所のみ定義(記録が主張する「二重化を解消し正本を1箇所にした」を確認)。`hit_query_limit`によるfail-closed分岐(5回目是正の核心)も記述どおり実装されている。
- `roles/syslog_weekly_digest/tasks/main.yml`: 該当2taskに`no_log: true`が付いている。
- `playbooks/syslog_weekly_digest.yml`ヘッダ: `tester-gate: role-guarded`(訂正後の値)。
- `playbooks/README.md`・`roles/semaphore_templates/defaults/main.yml`(`semaphore_templates_catalog`/`semaphore_schedules_catalog`、cron `0 9 * * 1`)・`.gitignore`: いずれも記録の主張どおり1件ずつ追加されている。
- `scripts/tests/syslog_weekly_digest/run-tests.py`: 実行すると71 tests OK(記録の件数と一致)。
- `bash scripts/check-tester-gate.sh`・`python3 scripts/check-doc-consistency.py`: いずれもOK。

## 指摘

| 分類 | 内容 | クローズを妨げるか |
|---|---|---|
| 差分の外側の陳腐化 | `docs/ai/policies/log_observability_policy.md` §4 は syslog系統について「**未実装。蓄積内容の事後参照に留まる**」と書いている。requirementはこの一文を着手動機として引用しているが、本案件のclose後は「週次でSlackへ自動的に要約が届く」状態になり、**「事後参照に留まる」は事実と合わなくなる**。検知(alerting)が未実装であること自体は本案件が変えていない(requirement §3が明言)ので§4の該当部分の骨子は生きているが、この1文はPolicy側で更新されないまま残る。本案件のどの記録にもこのPolicy更新への言及が無い(「転記先の実在」以前に、転記そのものが計画されていない) | 妨げる(ただし反映は軽微)。§4の当該セルを「検知は未実装、週次ダイジェストで人手参照は行っている」等、事実に合う表現へ直せばよく、再監査は不要 |
| 状態の正本との突合 | `docs/ai/status.md`のNext表に「**syslog週次ダイジェスト(requirement合意済み・未実装)**」の行が残っている。実際には実装・独立レビュー(Approve)・テスト・commitまで完了している | 指摘ではない(Auditorは該当行の除去前に起動するため、残存自体は想定どおり)。**除去すべき行として列挙する** — Coordinatorが同一セッションで除去する実施事項 |

上記2件以外に、決定の追跡・根拠の実在・記録どうしの矛盾・受入条件の充足・未解決の明示・転記先の実在の各観点で、指摘に当たる欠落は見つからなかった。AC1〜AC7はそれぞれ充足の根拠(コード確認 or mock実測)と、未検証部分(実Slack到達、実Loki 168h窓の受理、Semaphore環境固有の挙動)が`_004_test_result.md`§4〜§5に明示されており、黙って落ちている項目は無い。

## 判定

**条件付き受入**

反映すべき事項: `docs/ai/policies/log_observability_policy.md` §4の「蓄積内容の事後参照に留まる」を、週次ダイジェスト運用開始後の実態に合わせて更新すること。反映後、再監査は不要。

`docs/ai/status.md`該当行の除去はCoordinatorが同一セッションで行う実施事項(指摘ではない)。

---

## Coordinatorの判断(2026-09-04、監査を受けて追記)

**本案件をクローズする。** 指摘の扱いは次のとおり。

- **blocking(`log_observability_policy.md` が syslog系統を「未実装。蓄積内容の事後参照に留まる」としたまま)— 是正した。** ただし**指摘より狭く直した** — 「検知は未実装」は今も正しい(本ダイジェストは閾値を持たず `level` を発火条件にしないため検知ではない)。誤りになったのは「事後参照に留まる」の側だけである。
- **同じ節にもう1件あり、あわせて是正した(Coordinatorが検出)。** `LOG-066` が検知対象として portのflapping を挙げたままだったが、**2026-09-03に「作らない」で決着している**。その判断記録は `LOG-066` を名指しして「LOG-066が想定した事象がこの期間に発生していない」と書いていたのに、Policy側へ反映されていなかった。あわせて `LOG-067` の「実データの蓄積を待つ」も、蓄積が済み判断が出た状態へ更新した。**Policy本文の改訂はEXEC-030によりYoshinobuの判断であり、範囲(表の1行だけ / LOG-066・067も含める)を提示して後者を選んでもらった。**
- **`docs/ai/status.md` の Next 行 — 除去し、初回実行の観測待ちへ移した。** あわせて**Nowに残っていた完了済みの記述を圧縮した** — Operator案件がクローズ・commit済みなのに「独立レビュー中」「進行中」と書いたままで、`SessionStart` hook の chunk が10,089文字となり**10,000の上限を超えていた**(超えると先頭2,000文字しか文脈へ届かない)。圧縮後は最大8,764文字。

**監査の指摘外だが記録しておく。** Auditorは、依頼文で渡していない事情(レビュー巡数、requirement改訂回数、Coordinator自身の誤りの所在)を成果物から自力で再構成できていた。**一方、Testerの成果物に実IPアドレスを含む実ログ行が一度書かれ pre-commit に止められて差し戻された経緯は、どの記録にも残っていない** — Coordinatorが「残せ」と指示しなかったためである。**成果物からは再構成できない事実であり、指摘されなかったこと自体は妥当**(監査対象は最終成果物であり、そこに痕跡が無い)。ここに記録として残す。
