# Code Review: Semaphore schedule activation gate removal（最終確認）

Reviewer: Codex / 2026-08-24  
Verdict: **Approve**

## Summary

前回 Finding 1 と Suggestion 1/2 は、実装記録上で正確に訂正された。§3.6 は、見落とした対象を `semaphore_templates.py` の `_target_fields()` / `_observed_fields()` と特定し、`read.yml` が非文字列値を型のまま通した後に `value or []` が `false` / `{}` 等を空配列へ畳み、誤って `unchanged` にできる実際の経路を説明している。これは前回Reviewerが現物Ansibleで再現した挙動およびHEADのコードと一致する。

既存欠陥を本案件で直さない判断も、既存由来・差分の可読性・template reconcile固有の実API受入検証が必要、という3根拠とともに明記された。`docs/ai/status.md` には対象、失敗形、本案件由来でないこと、先送り理由、参照レビューが記録されており、黙って落とす形ではない。

コードは前回確認時点から不変であり、前回までに確認したactivation predicateの修正とcanonical検査を損なう変更はない。残るAC1 / AC2 / AC4の実Semaphore API検証は、予定どおりTesterの領分である。

## Findings

なし。

## Previous Findings

| Previous item | Status |
|---|---|
| Finding 1: role横断監査の「他に無い」がtemplate側の`or []`を見落としていた | **Closed.** §3.6が誤りを明示して撤回し、対象コード、失敗機序、見落とし理由、独立レビューで発見した経緯まで現物どおりに記録した |
| Finding 1 handoff: 既存欠陥を別案件へ明示的に引き渡す | **Closed.** `docs/ai/status.md`の着手候補に、対象・影響・既存由来・本案件で直さない3根拠・参照レビューを記録した |
| Suggestion 1: URL一致filterの「型不一致は必ず失敗」が過大 | **Closed.** 同値の非文字列ペアはfilter単体で一致し得ることと、現経路では先行URI taskが停止させる境界を区別した |
| Suggestion 2: 非bool unit testの網羅範囲が記録より狭い | **Closed.** before側7ケース、desired側5ケースと、個別fixtureを持たない形をテスト現物どおりに記録した |

## Record and Handoff Check

- `_observed_fields()` は `arguments` / `survey_vars`、`_target_fields()` はcatalog側の同フィールドを `or []` で畳む。§3.6の訂正はコードと一致する。
- `read.yml` は文字列だけをJSON decodeし、`false`、数値、dict等の非文字列はその型のまま後段へ渡す。§3.6に書かれた欠陥の接続関係は正しい。
- 実装記録は、同じファイルの `_parse_marker` を見ながら近接する `_target_fields()` / `_observed_fields()` へ監査を広げなかった、と監査漏れの理由を具体化している。
- statusの着手候補は「既存だから対象外」だけで終わらず、誤った`unchanged`、必要な別受入検証、同日に見つかった同型4件まで記録している。次の担当者が修正案件を起こせる情報量がある。
- 本案件で直さないことは明示的なscope判断であり、未検出・未記録のまま残す判断ではない。

## Code Immutability Check

- `roles/semaphore_templates/filter_plugins/semaphore_templates.py`: 作業ツリー差分なし。SHA-256 `77ed331ffa0f1054cf77cc607590f8a5a4ca5cd73d2bf87d7141f5236b1b12fa`。
- `roles/semaphore_templates/tasks/read.yml`: 作業ツリー差分なし。SHA-256 `9fa3eb867816c96eb9130dbbb42e22fb91414121e640bfbfc3ebdd890be03367`。
- activation predicate本体 `semaphore_schedules.py`: 前回Reviewer確認値と一致。SHA-256 `fe19e17211e48aad559f0e97bbd040709d7d90c8b9b4fd5142bcd2a9c270741f`。
- predicate回帰テスト `test_canonical_check.py`: 前回Reviewer確認値と一致。SHA-256 `f33b7d34283b82393d5362559bf33c5d86692969b0f04dd93beaa79ce27fc5f4`。

## Verification

- filter unit tests: **93/93 PASS**。
- schedule task-flow tests: **6/6 PASS**。
- `ansible-playbook --syntax-check playbooks/semaphore_templates_setup.yml`: rc=0。
- `git diff --check`: rc=0。
- `git diff` とSHA-256により、依頼で不変とされたコードを前回Reviewer確認時点と照合した。
- 本番Semaphore API、ansy/quory、Slackには触れていない。`docs/ai/memory/`も読んでいない。

## Residual Test Scope

Testerは計画どおり、実Semaphore APIに対して次を確認する必要がある。

- AC1: 新規scheduleが1回の適用で有効になること。
- AC2: 既存21件が無変化であること。
- AC4: 有効化を伴う書き込みでcanonical接続先検査が実際に効くこと。

template側の`or []`は本案件のApproveを妨げない既知の既存欠陥としてstatusへ引き渡されている。ただし別案件では、実Semaphoreに対するtemplate reconcileの受入検証を伴って修正する必要がある。

## Verdict

**Approve** — 前回の記録不正確さは訂正され、既存欠陥は根拠と検証境界を伴って明示的に引き渡された。対象コードは不変で、既確認の修正を後退させる差分はない。Testerへ進めてよい。
