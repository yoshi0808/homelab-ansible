# proxmox_hw_check 再レビュー

## 対象

- `docs/ai/reviews/proxmox_hw_check/2026-05-08_007_requirement.md`
- `docs/ai/reviews/proxmox_hw_check/2026-05-08_011_review.md`
- `docs/ai/reviews/proxmox_hw_check/2026-05-08_012_implement.md`
- `roles/proxmox_hw_check/tasks/main.yml`
- `roles/proxmox_hw_check/files/proxmox-hw-check.sh`
- `roles/proxmox_hw_check/defaults/main.yml`

## Findings

Blocking / High / Medium の指摘はなし。

前回レビュー `2026-05-08_011_review.md` の指摘は解消されている。

| 指摘 | 状態 | 確認内容 |
|---|---|---|
| `total_packets == 0` でも error/drop があれば WARNING になる | 解消 | `roles/proxmox_hw_check/tasks/main.yml` の error/drop rate 判定に `item.total_packets | int > 0` が追加された。 |

## 確認結果

実行した確認:

```bash
bash -n roles/proxmox_hw_check/files/proxmox-hw-check.sh

ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote \
  ansible-playbook --syntax-check -i inventories/homelab/hosts.yml playbooks/proxmox_hw_check.yml

ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote \
  ansible localhost -c local -m debug \
  -a "msg={{ (0 | int > 0) and ((1 | float / (0 | int + 1) * 100) > (0.5 | float)) }}"

ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote \
  ansible localhost -c local -m debug \
  -a "msg={{ (1000 | int > 0) and ((6 | float / (1000 | int + 1) * 100) > (0.5 | float)) }}"
```

結果:

- shell 構文チェックは成功。
- playbook syntax-check は成功。
- `total_packets == 0`, `total_dropped == 1` 相当の式は `false` になり、ゼロパケット NIC が rate WARNING 対象外になることを確認した。
- `total_packets == 1000`, `total_dropped == 6` 相当の式は `true` になり、閾値超えが WARNING 対象になることを確認した。

## 良い点

- `007_requirement.md` は `total_packets == 0` の扱いと分母 `+1` の役割を分けて記述しており、実装意図が追いやすい。
- shell は収集値の追加のみで、判定は Ansible tasks 側に残っている。
- `proxmox_hw_check_nic_errors_warning_pct` / `proxmox_hw_check_nic_dropped_warning_pct` は defaults にあり、host_vars で上書き可能な構成になっている。

## 判定

この変更は commit してよい状態。

実機 `pve1` / `pve2` では、report 内の `total_packets` と `Warnings` の出方を確認する。

## Next step files

- `docs/ai/reviews/proxmox_hw_check/2026-05-08_007_requirement.md`
- `docs/ai/reviews/proxmox_hw_check/2026-05-08_012_implement.md`
- `docs/ai/reviews/proxmox_hw_check/2026-05-08_013_review.md`

