# findings の処置(P1残件のクローズ)

2026-08-05 / Coordinator。工程は requirement(`..._008`)→ Implementer(`..._009`)→ 独立Reviewer(`..._010`)→ 差し戻し → Tester(`..._011`)。**却下した指摘は無い。** 変更後の文面は commit の diff が正本であり、ここへ写さない。

## Reviewer の Critical 2件(いずれも是正)

| # | 内容 | 処置 |
|---|---|---|
| C1 | finding を組み立てる task が失敗吸収の外側にあり、AC6「いずれの場合も終了コード0」が構造として成立していなかった | probe呼び出しと組み立てを1つの `block` へ入れ、`semaphore_probe.yml` 自身に `rescue` を新設。**意図的に例外を起こす fixture で `rescued=1 / failed=0 / rc=0` と、既存の配備物findingが生き残ることを実測** |
| C2 | probe自体が失敗した finding にも「ブートストラップtemplateを押せ」という案内が出ていた(接続が成立していないときに、押しても直らない操作を案内する) | category を分離(`semaphore_probe_error` / `semaphore_template`)。前者は `playbook` が空で、既存の `report_dirs` / `hosts_file` と同じ経路で「直し方」行が出ない |

C1は「いま落ちること」ではなく**性質が構造として成立していないこと**を直した。現行の返り値の形では発火しないことは、Reviewer・Implementerとも確認している。

## Coordinatorが検出した1件(是正)

C2の修正で入った固定文言が「`SEMI-SAFE: Semaphore templates` 相当のブートストラップtemplate」と、**repoが知り得ない名前を推測で書いていた。** ブートストラップtemplateはカタログの管理外(orphan)であり、実名はquory上にしかない。**ボタン名を正確に言うことがR10の趣旨であるため、推測した名前を出すのは目的に反する。** `playbooks/semaphore_templates_setup.yml` のテンプレートである、という言い方へ変え、repoから名前を引けない旨を本文へ添えた。

## Tester が実測して分かった、差分の外の事実(是正)

`roles/semaphore_templates/defaults/main.yml` の「ansyへ向けるときの上書き例」が、**実際には通らない形を示していた**(`https://ansy.internal:3000/api` + 既定の `validate_certs: true`。CAがKeyUsage拡張を持たないためPythonの `ssl` が検証に失敗する)。次にこの例をそのまま使う人を誤らせるため、実測した通る形へ差し替え、理由を添えた。**この案件の差分の外にある既存コメントだが、1行で閉じられ、かつ本案件の検証中に実測された事実であるため、ここで直した。**

## 受入条件の到達点

| AC | 判定 | 根拠 |
|---|---|---|
| AC1 / AC2 / AC3 | PASS | 実カタログ43エントリ・実9 playbookに対しフィルタを独立に再実行(Tester) |
| AC4 | **部分PASS** | 実APIに対し2回連続で同一結果を返すことは実測。ただし**「0件一致」の状態はansyの実データに存在しない**ためfixture確認のまま |
| AC5 | PASS | ansyの実状態で `new=42` が自然発生しており、実データで確認 |
| AC6 | **部分PASS** | 非root所有tokenでのfail-closedとTLS検証失敗は実条件で実測。**TLS失敗が `probe.yml` の rescue を実際に通ったかまでは未確認**(token.ymlのroot要求で手前で止まるため。rescue自体はfixtureで実測済み) |
| AC7 | PASS | 実行前後でansy側テンプレート件数が不変(書込ゼロ)、加えて構造上POST/PUT経路が無い |
| AC8 / AC9 | PASS | AC9はtoken文字列が実行ログのどこにも出ないことを `grep` で確認 |

**quory側は全面的に未検証である。** OQ1(project / inventory / repository 名)、OQ2(become可否)、quoryのTLS/CA状態、実tokenの読取成功。**最初の観測点は本番の日次実行**であり、`docs/ai/status.md` のWatchへ移した。

## この案件が閉じたもの

`docs/ai/status.md` Next の「**同定マーカーが消されると、次回適用で重複が作られる**」を削除した。当時の対処案は「検出を `semaphore_templates` の `report.yml` へ足す」だったが、**R9はそれより広い** — マーカー消失は `new` と `orphans` の両方に現れ、**人がreconcileを流したときではなく毎日**検出される。Watch行が引き継ぐ。

## Auditor

**起動していない。** 記録は requirement・実装記録・独立レビュー・test_result・本ファイルとcommitメッセージで再構成でき、対象は既存roleへの追加に閉じている。**独立レビューがCriticalを2件検出しており、その層は機能している。**

## 工程で起きた逸脱(記録のみ)

初回のImplementerが、`ansible-lint` の既存エラーが自分の変更より前からあることを確かめるために `git stash -u` を使った。`docs/ai/core.md` が名指しで禁じ、依頼文でも境界として書いていた操作である。**作業ツリーに実害は無かった**(`git stash list` が空、他者の未commit変更は無傷であることをCoordinatorが確認)。差し戻し時に明示して再発せず、2回目は `git diff` / `grep` で同じことを確かめている。

**再発防止の規定を新設しない。** 文章として既に2箇所に書かれていて破られたので、足りないのは文章量ではない。同日、この観察を根拠に `.claude/settings.json` の `autoMode.soft_deny` へ当該操作を追加した(別commit)。**境界は文章ではなく能力の側で作る**という既存の原則の適用である。
