## Code Review: proxmox_operations_policy.md へのストレージ月次点検規範追加(規範文書レビュー)

### Summary

`docs/ai/policies/proxmox_operations_policy.md`のSB-020安全度表への行追加とSB-096〜SB-099新設、対応する`docs/ai/context/operations/proxmox-storage-monthly.md`の正本ポインタ追加、`docs/ai/status.md`の進捗行追加を確認した。新しいPolicyファイル・実装変更は無く、既存requirement/Context/実装コードの契約をPolicy本文へ正本化する変更として一貫している。AC1〜AC6を充足し、宙ぶらりん参照・規範消失・撤回根拠の残存・判定ラダーの破綻・自己整合崩れ・索引ドリフトのいずれも検出しなかった。Approveとする。

### Critical Issues

なし。

### Major Issues

なし。

### AC1〜AC6充足確認

- **AC1**: SB-020安全度表(`docs/ai/policies/proxmox_operations_policy.md:89`)に`semi-safe | proxmox_storage_monthly.yml | ZFS/NVMeのread-only収集...host/device変更なし。自動可(SB-096〜SB-099)`が追加され、Policy本文だけで自動実行可能なsemi-safe入口かつhost/device非変更と判断できる。
- **AC2**: SB-097前半「1nodeでも到達不能、収集不正、履歴破損、ローカル保存失敗、Notion公開失敗なら、得られた証拠を可能な範囲で保存・短報へ示したうえで非0終了とし、到達できたnodeだけを根拠に全体を`OK`としない」が該当。実装(`roles/proxmox_storage_monthly/module_utils/storage_monthly.py`の`build_report`が1host失敗で`collection: error`に畳み、`tasks/report.yml`の最終assertがこれを非0で止める)と一致することを、直近の独立差分レビュー(`docs/ai/reviews/proxmox_storage_monthly/2026-09-10_007_diff_review.md`・`2026-09-11_010_rereview.md`)で確認済みの実装理解と突き合わせて確認した。
- **AC3**: SB-097後半「ZFS/NVMeの健康異常はreportの`WARNING`/`CRITICAL`として通知し、収集・保存・比較・公開が完了していれば処理自体はrc0とする。健康状態と処理成否を同じ値へ畳まない」が該当し、実装のhealth/collectionフィールド分離と一致する。
- **AC4**: SB-098が`collect`/`replay`の許可範囲、巻戻し拒否、一意確認不能時の停止を規定し、実装(`storage_notion.publish`の`older_report_refused`・`creation_uncertain_manual_reconciliation_required`)と一致する。
- **AC5**: SB-099が保持削除の対象限定(比較基準参照・未公開・破損・型不明・symlinkを除外)と、native check/明示的通知抑止での外部送信禁止・check modeでの保存/保持整理停止を規定し、実装(`storage_archive.py`の`prune()`保護条件、`module.check_mode`分岐)と一致する。
- **AC6**: 文書整合検査(`python3 scripts/check-doc-consistency.py`)を実行しrc0を確認した。ただし本スクリプトは`git show :<path>`でgit indexを読む設計であり、本差分は未staged(`git diff --cached`が空)のため、この実行結果は今回の差分そのものを検査した証拠ではない。もっとも本スクリプトの3チェック(tester-gate↔README、agent frontmatter↔coordinator.md、Markdown内部リンクの存在)はいずれも本差分が触れる内容ではない(本差分はplaybookヘッダ・agent定義・新規Markdownリンクのいずれも追加していない)ため、index/working tree差異が今回の結果を左右する余地はない。独立規範レビューは本ファイルの発行をもって充足する。参照切れ・Policy内矛盾・Context競合は「掃引」節で個別に確認した。

### 掃引(欠陥クラス1〜7個別確認)

- **1. 宙ぶらりん参照**: リポジトリ全体で`proxmox_storage_monthly`を機械的に検索し、コード側(`playbooks/`、`roles/`)に規範文言の複製が無いこと、Policy/Context/statusの3ファイル以外に古い言及が残っていないことを確認した。SB番号の完全形(`SB-096`〜`SB-099`)と範囲表記(`SB-096〜SB-099`)の両方で検索し、Context・status・実装記録間で表記の不一致は無い。
- **2. 規範の消失**: 002実装記録の「規定対象」列挙(host/device非変更、部分失敗、健康状態と処理成否の分離、JSON/Notion/Slackの役割、collect/replay、保持削除、秘密情報、check/通知抑止)を、SB-096〜SB-099の各段落と逐一突き合わせ、抜け落ちた項目が無いことを確認した(秘密情報の非出力はSB-099に「認証情報をreport、通知、実行ログ、Gitへ出さない」として明記)。
- **3. 撤回した規範・根拠の残存**: 本差分は既存規範の撤回や機構変更を伴わない追加のみであり、該当なし。
- **3.5 規範が依拠する事実の未検証**: SB-096〜SB-099はいずれも実装コード(collectorのread-onlyコマンド固定、`storage_archive`/`storage_notion`のcheck_mode分岐、`prune()`の保護条件)という機械的な支えに対応しており、申告や過去作業だけに依拠した記述は無い。「control node上に限る」という記述も実際のplay構成(収集はhost上、archive/publishはlocalhost=quory上)と一致することをコードで確認済み。
- **4. 判定ラダー**: SB-020安全度表はplaybook名によるlookupであり、各行の対象playbookが重複しないため先取り・到達不能分岐は生じない。SB-096〜SB-099は既存のSB-095(healthcheck/hw_check)と対象playbookが分離しており、両者の失敗時挙動が異なる(SB-095は到達可能node継続、SB-097は1node失敗で全体非0)ことも、対象がplaybook名で排他的に分かれているため矛盾ではない。
- **5. 文書内の自己整合**: 新設節がSB-020表・変更履歴の双方に反映されており、表側だけの追記や変更履歴だけの追記という片落ちは無い。
- **6. 追跡表・索引のドリフト**: SB-020表の新規行、変更履歴表の新規行のいずれも実態(未commit差分の内容)と一致している。
- **7. 適用範囲(scope)の広狭変化**: SB-096〜SB-099は新規追加であり、既存規範の適用範囲を狭める・広げる記述ではない。SB-090(quory限定)等の既存の一般規定との重複明記は無いが、これらは§2の一般節としてこのPolicy全体に既に及んでおり、SB-096〜SB-099が上書き・除外する記述も無いため矛盾なく効いたままである。

### Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| 1 | docs/ai/policies/proxmox_operations_policy.md | 109 | 新設した見出し「#### 3.2.1 ストレージ月次点検」が「### 3.2 healthcheckとdry-run」の子節として置かれているが、月次点検はhealthcheck/dry-runの一種ではない。SB-020表(§3.1)の直下・SB-095の後という位置自体は自然だが、見出し階層だけ`3.2.1`ではなく独立した`3.2a`相当(または3.3以降への並び直し)にする方が節構造と内容が一致する。機能上の誤りではなくSB本文の到達性にも影響しないため、次回の構成見直し時のついでで良い。 | style |

### What Looks Good

- SB-096〜SB-099はいずれもSB番号として一意(既存SB-001〜SB-095、SB-080〜SB-095との衝突なし)であり、リポジトリ全体で他ファイルからの重複採番も無い。
- Context側追加行「許可・禁止・停止条件の正本は...SB-020、SB-096〜SB-099。本書は実行方法、保存形式、配備状態を記録するOperations Contextであり、実行境界を上書きしない」は、本Policy冒頭の「Contextは非規範であり、本書と競合する場合は本書を優先する」という既存の優先順位を上書きも矛盾もしていない。
- 変更履歴表の新規行は本Policyの既存の記述スタイル(「〜のため、〜を追加した。新しい機能や許可範囲は追加せず、現行契約をPolicyへ正本化した」)と一致し、根拠・経緯を本文へ書き込まず変更履歴側へ閉じ込める既存の規範文書運用(`feedback_norm_docs_no_rationale_citations`相当の方針)にも沿っている。
- P1要件「既存Policyの一般read-only点検と異なる部分だけを追加し、Contextや実装値を複製しない」を満たしている — SB-096〜SB-099は`proxmox_storage_monthly_*`変数名やNotion親ページID等の実装値を一切含まない。

### 確認範囲

- 001要件、002実装記録を全文読了。
- `git diff`で対象3ファイル(Policy、Context、status.md)の差分を全文確認。
- Policy全文を通読し、SB番号の一意性、SB-090/SB-095等の既存規定との重複・矛盾の有無、変更履歴表との整合を確認。
- リポジトリ全体を`proxmox_storage_monthly`および`SB-096`〜`SB-099`で横断検索し、宙ぶらりん参照・重複記述の有無を確認。
- `python3 scripts/check-doc-consistency.py`を実行(rc0、ただし前述のとおりgit index読み取りのため本差分自体の機械検証としては限定的)。
- 実ホスト・外部サービスへは接続していない。本タスクで変更したファイルは本review成果物のみ。

### 未解決事項

- 見出し階層(Suggestion #1)はCoordinatorの裁量で次回構成見直し時に対応で良い。
- 文書整合検査を本差分に対して機械的に再現するには、Coordinatorがcommit前に`git add`した状態で`scripts/check-doc-consistency.py`を再実行する必要がある(本Reviewerは`git add`を行わない)。

### Verdict

Approve(Critical/Major無し)。
