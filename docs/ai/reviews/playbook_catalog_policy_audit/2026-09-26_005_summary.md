# 集約: playbook一覧・Policyと実装の食い違い(2026-09-26)

作成: Coordinator。入力は `_001`(第1段、Coordinator)と `_002`〜`_004`(第2段、別モデルに役を与えず依頼した3体)。

## 件数

| 成果物 | 範囲 | 指摘 | 未確認 |
|---|---|---|---|
| `_001` | playbook一覧・Semaphoreカタログ | 3 | 0 |
| `_002` | proxmox_operations / backup_restore_verify / autonomous_recovery / incident_capture | 23 | 11 |
| `_003` | ubuntu_vm_patch / time_sync / quory_health / log_observability / unifi_backup_fetch | 10 | 7 |
| `_004` | cert_renew / cert_renew_cloudkey / ansible_test_safety / execution_boundary | 8 | 7 |

**Coordinatorが現物で確かめたもの**: `_002` F01・F02・F06、`_003` F1・F2・F5・F7・F9、`_004` F3・未確認3。いずれも引用どおりだった。それ以外は各成果物の引用に依拠しており、未照合である。

## 仕分け

重大度はCoordinatorが付け直した。「高」は、**確認や停止の仕組みが働かず、意図しない変更が本番に入りうるもの**。

### A. 実装が規定より危ない側にずれている — 実装を直す

| 重大度 | ID | 内容 |
|---|---|---|
| 高 | `_002` F06 | `UN-SAFE:Proxmox patch apply (Manual)` の確認文字列がsurveyの既定値(`MAJOR_UPGRADE_DETECTED`)で事前入力されている。既定のまま押すと確認なしで適用に進みうる |
| 高 | `_001` F1-1 | `SAFE:Prometheus update check` の選択肢で `update` / `rollback` を選ぶと、確認なしで実行される |
| 高 | `_002` F01 | 置換先の無い重要removeを含む更新が `BLOCKED` にならない(常に `MAINTENANCE_REQUIRED`)。手動applyで適用できてしまう |
| 中 | `_002` F02 | 最終Status(`MAJOR_UPGRADE_DETECTED`)とUrgencyを、AIの出力を照合せずそのまま採用している(Policyは「AIは補助、最終判断はAnsible」) |
| 中 | `_002` F03 | Codexへ渡すPolicy節を間違えている(§4「判断軸」でなく§3を渡している)。Urgencyの基準がAIに届いていない |
| 中 | `_002` F10 | 手動applyの「control nodeが対象node上にいないか」の検査が、quoryでなく `ansy` という名前のVMを探している |
| 中 | `_002` F07 | apply後のhealthcheckが見る項目がPolicyより狭い(ZFS・failed unit・root fs・GUIを見ない)。CRITICALでも正常終了する |
| 中 | `_002` F09 | 退避でHA待ち・migrationが失敗すると、Policyの「残ったguestを強制停止」に届かず中断する(Policy内のSB-026とSB-091も両立していない) |
| 中 | `_002` F05 | `BLOCKED` のとき、Policyの「自動applyのtimerを止める」が実装されていない |
| 中 | `_003` F5 | 月次full-upgradeが、authy / monnieのhealthcheckの `WARNING` を捨てている(コメントの前提「warningsを返さない」が事実と違う) |
| 中 | `_004` F3 | `cert_renew_quory.yml` は `--check` でもCA秘密鍵のtmpfs展開・鍵生成・署名を本実行する。ヘッダに意図が書かれた例外だが、Policyの `check-mode-native` の定義と合わない |
| 中 | `_004` F8 / `_002` F20 | 通知のたびに走る障害捕捉(capture)が、通知の抑止guardより前にspoolへ書く。`role-guarded` の定義(副作用はSlack通知のみ)と、incident_captureの「捕捉は被観測playの件数を変えない」の両方と合わない |
| 低 | `_002` F12 | 月次restore検証で、restore nodeを決められないとき、停止せず別nodeへ復元する |
| 低 | `_002` F14 / `_003` F10 / `_004` F1 | 実行元を「quoryのみ」とするPolicyに対し、実装はansyからの実行を許す(または検査しない)。CloudKeyはansyを明示的に許可している |
| 低 | `_004` 未確認3 | CloudKeyの参考スクリプトが、指紋が一致しなくても旧証明書の削除に進む(本番経路ではない) |

### B. 通知・記録の中身がPolicyの規定に足りない

`_002` F08(退避・復帰の失敗が通知されない、成功通知にpackage名が無い)、`_002` F18(push経路が通知もreportも残さない)、`_003` F4(nightlyが3値目の `WARNING` を `#info` / `ok` で送る)、`_004` F6(sophos_trimの通知に `--check` の分岐が無い)、`_002` F21(見直し通知の差分基準が送信前に進む)、`_002` F13(report dir作成失敗で通知なしの非ゼロ終了)。

### C. Policyの記述の方が古い・両立しない — Policyを直す(Policyの改訂はYoshinobuの領域)

| ID | 内容 |
|---|---|
| `_003` F1 / F2 | UV-048「方針2 node(ansy / quory)をAnsible管理・監視・healthcheckの対象にしない」が、月次full-upgradeや他の4 Policyの規定と両立しない |
| `_003` F3 | Statusの「上から順に当てる」表の順序と、実装の判定順序が違う |
| `_003` F7 | LOG-068のmonnieのjobが「system stream」(実際は `ubuntu-nodes`) |
| `_003` F8 | LOG-047「syslog系統からの通知は未実装」(週次ダイジェストが実装済み) |
| `_003` F6 | LOG-042「real streamがLokiへ届くことを確かめる」が、Alloyのcutoverに無い(**今日のラベル外れを捕まえられなかった理由そのもの**) |
| `_002` F04 | 「dry-run段は件数閾値を持たない」が、2026-09-20の件数超過ゲートと食い違う |
| `_002` F15 / F17 / F19 / F22 / F23 | sandboxの追加、authorized_keysの3本目(開発側調査鍵)、grafanaの15分mute、`reports/` の前提、`recovery.io` allowlist — いずれも後から入った実装にPolicyが追随していない |
| `_004` F2 / F4 | CloudKeyの検証手段(`openssl s_client`・`get_certificate` の指紋)、cert_renewの参照先ファイル名 |
| `_004` F5 / F7 | role内に `tester-gate` の理由文の複製がある(TS-036)、`safe-readonly` に `apt-get update` を含めてよいか(TS-005とTS-027) |
| `_003` F9 | UniFi backupの週次(金曜)と月次restore検証(28日)が、28日が金曜の月に同時刻に起動する(Policyは「衝突しない時間帯」) |

### D. 軽微

`_001` F1-2(チェック数の食い違い)・F1-3(READMEの列不足)、`_002` F11(ZFSの異常判定が2状態だけ)、`_002` F16(手動restartが `reset-failed` しない)。

## 進め方の案

1. **A の「高」3件を先に1案件として直す。** いずれもsurveyやStatus判定の小さな変更で、実ホストに触れずに検証できる
2. A の「中」以下とBは、Policy単位で案件を分ける(proxmoxパッチ系 / capture系 / ubuntu系)
3. C はPolicyの改訂案を起こし、Yoshinobuが判断する。実装が正しくPolicyが古いものは、文言を実装に合わせるだけで済む
