## Code Review: agmsg notification pairing

### Summary

前回までのblocking 5件とMinor 1件はすべて解消した。不可逆なsubmit前の検査、submit後の失敗開示、正常時の二重報告、Reviewer依頼の送信規範、責任分界、および追跡可能な回帰テストをAC1〜AC6へ対応付けて確認し、残存findingはない。

### Critical Issues

なし。

### Suggestions

なし。

### What Looks Good

- AC1: 要旨空、payload不正、`send.sh` / `team.sh`欠落、team欠落、固定from/to identity欠落、およびID検証ライブラリを読めない状態は、すべて不可逆なsubmitより前に停止する。team/両identity欠落のstub testではclientが一度も呼ばれない。
- AC2: 有効なIDが登録された後にagmsg送信が失敗すると、登録済み・取消不能・通知だけ未送であることと手動再送手段を示し、exit 3になる。黙った成功にはならない。
- AC3: 応答object、`request_id`の文字列型、既存ID形式、前後空白を検証する。ID契約は`roles/operator_request_channel/files/oprc/ids.py`を再利用し、正規表現を複製していない。欠落、dict、末尾LF、末尾空白、形式不正、list応答、JSON不正は通知せず、登録された可能性を明示してexit 3になる。
- AC4: 正常時は通知本文へIDと要旨だけを載せ、OPREQ本文を複製しない。標準出力は登録IDとagmsg送信済みの両方を報告する。
- AC5: `agent-messaging.md`のspawn例はboot promptを`<起動指示>`に限定し、§4/§6は依頼文を`send.sh`で送ってlocal teamでは既読を確かめるよう一貫している。旧boot prompt / tmux直接注入の回避はlive文書から消えている。
- AC6: `coordinator.md`はOperatorを起動する主体をYoshinobu、気づかせる主体をCoordinator、手段をagmsgと明記し、セッション不在を通知しない理由にしない。remote teamで既読が戻らない事実とも矛盾しない。
- 規範掃引: requirementが撤回した「返りに気づけない」課題、未応答OPREQ検出、boot prompt経由を非違反とする旧評価は、現在地の正本`status.md`から行ごと削除された。生きているReviewer実現方式の論点だけが別行へ残り、今回の責任分界を上書きしない。
- test gap: `.gitignore`は`scripts/tests/oprc_submit/`だけを打ち消し、テストは`git status --untracked-files=all`へ現れる。末尾LFはJSON escapeのままstrip分岐を通り、生LFのJSON parse失敗と分離されている。strip検査を外すと末尾LFの1件だけが失敗するnegative controlも記録と現物が一致する。
- ローカル実測: `scripts/tests/oprc_submit/run-tests.sh`はpass=15 / fail=0、`bash -n`は両shellで成功、`scripts/check-doc-consistency.py`は3検査ともOK、`git diff --check`も問題なしだった。
- セキュリティ/再利用: payloadは既存clientのschema/DLP経路へstdinで渡し、shell文字列の再解釈や`eval`を追加していない。通知はIDと要旨だけで本文を複製せず、ID形式は既存ライブラリを再利用する。
- 残存リスクは実装記録§4どおりである。wrapperを迂回した直接submit、remote側の既読を送信側で確認できないこと、要旨の内容判断は今回の契約外または人の規律に残る。次の本物のOPREQまで実経路は未観測である。
- 本番OPREQ、実ホストAnsible、SSH、remote teamへの試験送信は実行していない。`docs/ai/memory/lessons/`の6差分は対象外のまま変更していない。Reviewerによるリポジトリ変更は本成果物だけである。

### Verdict

**Approve**（blockingなし）
