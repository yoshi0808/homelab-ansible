# 第2束フェーズ3(分類の越境5件)レビュー(2026-08-25)

対象: 未commitの作業ツリー差分全体(計18ファイル + 実装記録`2026-08-25_008_implement_phase3.md`)。契約は`2026-08-25_004_fix_scope.md`「第2束」「3. 越境5件」(前提A案 + C5-1/C5-3/C5-5)、観測事実は`2026-08-25_002_findings_context.md`「5. 群固有」。Coordinator追随分(TIME-010 + v1.5)、差し戻し対応、Yoshinobu指示によるoperator.md追加修正(a)(b)を含む。使用Skill: `document-norm-review` / `code-review`。本記録は3巡(初回 / 再レビュー / 最終判定)を統合した最新状態である。**全findingsは解消またはクローズ済み**(経緯は「解消済みfindings」節)。

## Code Review: 規範文書監査・第2束フェーズ3(分類の越境の是正)

### Summary

契約6単位(A案の線引き + C5-1/C5-3/C5-5 + C5-2/C5-4の宣言是正)と追随・追加修正は、3巡を経てすべて成立した。移設された規範に消失・意味変化・条件の緩みは無く、旧住所参照はrepo全体(規範文書・playbookヘッダ・roles/コメント・scripts)で残ゼロ、新設の分類線は最初の適用対象で一意に判定でき、YAML構造はHEADと完全一致する。blocking無し。

### Critical Issues

なし(3巡目で全件解消。経緯は次節)。

### Suggestions

なし(前巡のSuggestionはすべて是正済みまたは不採用合意でクローズ。次節)。

### 解消済みfindings(経緯)

初回レビュー(1巡目):

- **旧Critical #1(semaphore.md:35のポインタ先EXEC-005に当該規範が実在しない)**: **解消**。execution_boundary_policy.md EXEC-005ブロック内(:41、次マーカーEXEC-010の手前であることを確認)へ「quory側に登録された鍵を、ansyと同じ判断で消してはならない」が新設され(v1.3、EXEC番号新設なし)、ポインタは実在する規範へ到達する。**等価性確認済み**: 禁止文は旧semaphore.md文と同義(「quory側の鍵」→「quory側に登録された鍵」は対象の明確化で広狭変化なし)。根拠文のみ「認証経路そのもの**でありうる**」→「実行能力**そのものであり**」と留保が1つ落ちたが、規範(禁止)の強度は不変で安全側。
- **旧Major #2(roles/実装コメント7箇所の旧住所参照)**: **解消**。6ファイル7箇所すべて`skills/ansible-implementation-style/SKILL.md`へ付け替え済み。差分はコメント行のみ — YAMLはHEADと`yaml.safe_load`比較で完全一致、shellは`bash -n`通過を実測。
- **旧Major #3(「Policyとの境界」の判定一意性)**: **解消**。「経路・手順の利用そのものに課す確認手続きも、手順に閉じた義務としてここに含まれる」の追加と「承認」→「実ホスト・本番の状態を変える操作の承認」への限定で、反例だったagent-messaging.md §8(送信同意・着手前確認)が「手順に閉じた義務」側へ一意に落ちることを確認した。
- **旧Suggestion #1(cron義務の逐語二重化)**: **解消**。semaphore.md:20は事実+正本ポインタへ縮約、命令文・窓の列挙・事例はカタログヘッダだけが持つ。規範の脱落なし。
- **旧Suggestion #2(「非規範」自己宣言の残存8箇所)**: **不採用合意でクローズ**。Coordinator判断(非規範宣言+Policy委譲が対で成立しており今回の線の下でも壊れていない)は妥当と認める — いずれも自文書内に手順外の禁止・義務を正本として持たない。
- **旧Suggestion #3(SKILL description)**: **解消**。「shellを使う全roleに適用される「check系shellの責務分離」の正本を含む」を追記済み。

再レビュー(2巡目、operator.md追加修正(a)(b)を含む):

- **旧Critical R1(role-context-matrix.md:34が削除済み「読むもの」節を指し、新規範と正面矛盾)**: **解消**。「Operatorはこのリポジトリを読まず、指示・手順の正本はquory側にある(`docs/ai/roles/operator.md`「この文書の位置づけ」)」へ差し替えられ、参照先の節は実在し、規範の向きも一致した。「「読むもの」」のrepo再掃引で残ゼロ。
- **旧Suggestion #1(カタログ内entryコメントが削除済みのsemaphore.md文言を引用)**: **解消**。同ファイル冒頭ヘッダコメントへの参照に差し替え(コメント行のみ、YAML構造HEAD一致を再実測)。
- **旧Suggestion #3(operator.md:11の確認主体の曖昧さ)**: **解消**。「個別のRole・Policy・実効権限で」→「quory側の実効権限で」となり、「Operatorはリポジトリを読まない」分離・(a)の正本宣言と整合した。
- **旧Suggestion #4(v1.3変更履歴の典拠がOperations Context限定の節)**: **解消**。「安全境界に触れる規範はContextを正本にせずPolicyへ置く整理に合わせて」へ一般化され、対象を覆わない節の名指しが消えた。

### What Looks Good

規範レビューでは「変わっていないこと」の確認が成果物であるため明記する。

- **C5-1(healthcheck.md §1 → SKILL.md)の規範本文は無変質**。禁止7項目(変更操作・判定・分類・期待値比較・継続中止判断・通知・保存)、責務分離図、補足3点、`proxmox_snapshot_check`の実例を逐行突き合わせ、緩み・強まり・脱落なし。「shellを使う全roleに適用」の適用範囲宣言も維持(広狭変化なし)。落としたのは移設経緯の注記のみ。core.md:109・TIME-010・roles/コメント7箇所のポインタがすべて新住所へ揃い、旧住所を正本として指す箇所はrepo全体(reviews/memory除く)で残ゼロ。
- **C5-3は両建てで成立**。「鍵を再登録しない」はEXEC-052(:117に実在)、「quory側の鍵を消してはならない」はEXEC-005(v1.3新設)へのポインタがともに実在する規範へ到達する。schedule義務2件のカタログヘッダ移設は本文同一(表照合義務、反映義務、2026-08-18事例、2026-08-24のYoshinobu決定を含め脱落なし)。semaphore.md側は事実の記録だけになり、System Contextの分類定義と整合した。
- **C5-5 + 追加修正(a)(b)は規範として消失なし**。「実装済みの能力に限る」「名称から推測しない」「経路の開通は権限の根拠にならない」は維持。(a)の正本差し替え(→quory側の実効的な制約の現物)は「Operatorはリポジトリを読まない」分離と整合し、repo文書がOperatorに届かない以上むしろ強い。(b)で消えた旧「読むもの」の列挙のうち規範性のあるもの(手順の正本はquory側/matrixが本Roleを扱わないのは漏れでない)は新「位置づけ」節が保持。core.md:3(読む主体の列挙にOperatorは元から含まれない)・core.md「運用の作業時に読む情報」とも矛盾しない。
- **C5-2/C5-4**: agent-messaging.md / sandbox-vm.mdの「非規範runbook」自己宣言は新しい線へのポインタ付き文言へ置換され、両文書の禁止・義務は線の下で正当。operator-request-channel.md無変更の判断は妥当(「非規範」宣言なしをgrepで確認、禁止・義務はchannelの手順に閉じる)。
- **新設の分類線は既存定義と矛盾しない**。Policy定義:33との関係は「runbookの手順的義務の明示的な例外化」であり、判定は最初の適用対象(agmsg §8)で一意。
- **`roles/semaphore_templates/defaults/main.yml`はコメント行のみの差分**。HEADと`yaml.safe_load`比較で構造・値の完全一致(最終状態で再実測)、カタログ21件維持。
- **overview.mdの書き換え**は002「未確認」節:207の巻き取りで契約の範囲内、semaphore.mdの性格づけと矛盾なし。**Coordinator追随分(TIME-010 + v1.5)**は参照付け替えのみで規則本文・TIME番号不変。
- **scope遵守**: `git status`は契約・是正・追随・実装記録・本成果物のみでscope外の変更なし。diff全体にIPv4リテラルなし。`scripts/check-doc-consistency.py`3チェックOK(最終状態で再実行)。`.claude/skills/ansible-implementation-style` symlink到達可。実ホストへのansible/ssh実行、git add/commit/pushは行っていない。

### 確認した手段

- `git status --porcelain` / 対象diff(各巡の着手時と返却直前に取得。3巡目の増分4箇所を逐行確認)
- 移設前後の逐語突き合わせ(healthcheck §1 / semaphore.md鍵2文・schedule義務 / operator.md旧節 / EXEC-005新設文)
- `grep -rn`掃引: 「healthcheck.md」「責務分離」「非規範」「現在の状態」「「読むもの」」「roles/operator.md」「バッチ処理工程管理表」を docs/ skills/ roles/ playbooks/ scripts/ .claude/ CLAUDE.md AGENTS.md で実施(reviews/memory除外)
- execution_boundary_policy.mdの鍵・Semaphore関連全行の逐行確認とEXECマーカー位置確認
- YAML構造のHEAD比較パース(5ファイル)、shell 2ファイルの`bash -n`、check-doc-consistency.py、IPv4 grep(いずれも最終状態で再実行)

### 未確認事項

- Operatorがquory側で実際に何を読む設計になるか(quory側の正本の実体)はこのrepoから確かめられない。(b)の規範はそれを前提とせず成立しているため判定には影響しない

### Verdict

**Approve**(blocking無し。3巡目の是正4件をすべて現物で確認し、残る指摘は無い)。
