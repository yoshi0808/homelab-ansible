# 第1束修正のレビュー(Group A 14項目 / Group B 22項目)

日付: 2026-08-25。Reviewer(subagent)。対象: 未commit作業ツリー差分(規範文書30ファイル、+84/-87行)。契約: `2026-08-25_004_fix_scope.md`「第1束」。観測事実の根拠: `_001`〜`_003` findings。implement記録: `_005`(Group A)/`_006`(Group B)。

## Code Review: 規範文書30ファイルの第1束修正

### Summary

契約36項目(A14+B22)はすべて差分に現れており、scope外の変更は無い。指し直した参照先(TS-007/TS-030、EXEC-005/010/030、AR-077、rejected-proposals.md、reviews実パス3件、各role defaults)は実在と内容一致を現物で確認した。ただし1件、**Group AとGroup Bが同一baselineで並行修正された結果、互いに矛盾する記述が同じ差分内に生まれている**(C2-2の新文がP1-5の修正後EXEC-010と食い違う)。これはblockingとする。

### Critical Issues

| # | File | Line | Issue | Severity |
|---|---|---|---|---|
| 1 | `docs/ai/context/operations/grafana-alerting-tuning.md` | 52 | C2-2の新文「`monnie`は承認区分「それ以外」= 確認不要側。正本はEXEC-010」は、**同じ差分内のP1-5修正(EXEC-010の「それ以外」行から`monnie`を除去)と矛盾する**。修正後のEXEC-010で`monnie`は「到達手段が無い」区分にのみ載る。implement記録`_006` C2-2行は「それ以外 = monnie/ansy/sandbox」と修正前の表を確認しており、Group Aの変更を取り込まないまま文言を確定した経緯が読み取れる。参照先が正しても、参照元が主張する参照先の内容が偽 — 本監査が潰そうとしている欠陥クラスそのものを新造している。修正方向の例: 区分名を書き写さず「`monnie`は保護対象ではなく、read-only参照に承認は要らない(区分の正本は`execution_boundary_policy.md` EXEC-010/EXEC-030)」の形へ(区分実値の複製をやめれば今後のドリフトも断てる) | Major(blocking) |

### Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| 2 | `docs/ai/context/operations/code-delivery-to-production.md` | 53 | 手順表の行7に「翌00:40の日次ドリフト検査」の時刻実値が残る。C1-4が落とした実値(:50/:119/:179)と同一の値の同一文書内の取り残し。implement記録`_006`は「fix_scopeの対象3箇所に含まれない」ため意図的に据え置いたと明記しており無自覚な漏れではないが、本文書は冒頭(:7)で「実値を書かない」と宣言しており、同じ束で落とすのが一貫する(値の正本`semaphore_schedules_catalog`の`cron: "40 0 * * *"`は確認済み) | 取り残し |
| 3 | `docs/ai/policies/autonomous_recovery_policy.md` | 152 | P2-4で「P4」を削除した後も「lock、flapping、`pvesh`の**4分岐**」と、数の宣言4に対し列挙が3のまま残る(実装`roles/recovery_probe/files/recovery-probe.py` `fire_ladder`ではlock→flapping→pvesh確証→VM操作段の4段)。同じ束のS1-4(「次の4つに限る」→「次に限る」)・memory-classification(「4層モデル」→「層モデル」)と同型の「数の宣言を落とす」処置が未適用。規範の消失は無い(各段の規定はAR-032等が別途持つ)ため意味の欠落としては軽微 | 一貫性 |
| 4 | `docs/ai/policies/proxmox_operations_policy.md` | 40 | SB-011へ追加した条件句の末尾「(SB-028、SB-032、§2.2)」は§2.2の内部からの自己参照になっている(§1の既存文の鏡像として契約どおりだが、SB-011側では§2.2を落としてよい) | 表現 |
| 5 | `docs/ai/policies/unifi_backup_fetch_policy.md` | 253 | UNIFI-017 §12(2026-06-15実機検証の日付付き記録)に「8 世代超の実削除は…」と世代数実値が残る。P3-2が落とした値と同一だが、過去の検証時点の記録であり現在値の主張ではない。残置は妥当と判断するが、defaultsが変わるとこの行だけ古い数字を持つことは把握しておく | 情報 |

### What Looks Good

規範本体に消失・意味変化・条件の緩みが無いことを、次の範囲で確認した。

- **契約充足**: Group A 14項目・Group B 22項目のすべてが差分に対応する変更を持つ。scope外の変更は混ざっていない(差分678行を全読し、全hunkを契約項目へ対応付けた)。implement記録が「scope外」と申告した据え置き(semaphore.md :48/:71/:73の歴史的2.19.8、code-delivery:183、pilot1)は、:53(上記#2)を除き妥当。
- **指し直した参照先の実在・内容一致**(現物で確認):
  - TS-007(分類実値はplaybookヘッダが正本)・TS-030(risk-acceptedは`--check`で停止assert) — `ansible_test_safety_policy.md`に実在し、`playbooks/unifi_backup_fetch.yml`ヘッダの文言とも一致(TIME-017 / UNIFI-019の新文と整合)。
  - EXEC-030(Policy改訂は常にYoshinobu) — incident_capture冒頭の指し直しと一致。EXEC-005(`id_ann`削除・quory Semaphoreのみ) — CCK-003の新文と一致。core.md:49の括弧書きの指し直し先(ホスト区分)もEXEC-010が実際に持つ。
  - AR-077 — 「段階単位は一律120分、weekly fullのみ360分」で、削除したTTL表の代わりの正本として成立。playbook実装(`proxmox_patch_apply_node` / `proxmox_restore_vm_placement` / `ubuntu_nightly`いずれも`recovery_mute_minutes: 120`)とも一致し、削除された表(60/90/30/45分)の側が誤りだったことを裏付ける。
  - `docs/ai/memory/decisions/rejected-proposals.md` — 実在し、worktree_sync排他(2026-08-05)・sandbox系のエントリを実際に持つ(coordinator.md / sandbox-vm.md / code-delivery の3箇所の指し直しすべて解決する)。
  - reviews実パス3件(policy_standardization 009 investigation、todo7-2_result、phase7_pilot2_setup) — すべて実在。
- **実値を落とした先の正本が値を実際に持つ**: `roles/time_sync_check/defaults/main.yml`(500ms/5000ms)、`roles/unifi_backup_fetch/defaults/main.yml`(§11表の8変数すべて)、`roles/systemd_timers/defaults/main.yml`の`cert-renew-quory`エントリ(schedule付き)、`roles/semaphore_templates/defaults/main.yml`のdrift checkスケジュール、`roles/worktree_sync/defaults/main.yml`のtimer間隔。落とした箇所の不変の意味論(週次/月次force、sophos専用の大きい閾値、Persistent=true、鮮度ガードの目的)はいずれも残っている。
- **S1-3(incident-recording運用ルールのポインタ化)で規範は消えていない**: 落とした5項目(解決済みのみ集計、滞留の別報告、繰り返しタグの直接昇格、一度きりはLesson経由)はすべて`memory-classification.md`の「3. 昇格・廃止ルール」(:82)・「月次振り返りの対象と手順」直前(:98)に実在する。指した節見出し2つも実在。
- **変更履歴表の追記**: 7 Policyすべてで追記あり。各表の既存並び順(昇順は末尾へ、降順のexecution_boundary/proxmoxは先頭へ)・採番(v2.6/v1.3/v1.4/v1.2×2)・「番号の新設・退番はない」相当の申告を含む書式が既存行と整合。
- **残存ゼロの機械掃引**(docs/policies/context/roles/skills/.claude/agents対象。memory/reviews/status/adrの履歴・記録は除外): 「載せていないもの」0件、「会話の過程は永続しない」0件、「月次強制」「00:35」は変更履歴行のみ、「AC1」「4層」「on the horizon」「新設予定」0件、cloudkey policyの「ansy(開発)」系実行経路0件、`monnie`のEXEC-010二重掲載は解消。
- **並行subagentの成果物の分離**: Group A実装はGroup Bのファイルへ触れておらず(逆も)、両implement記録の申告と差分が一致。
- **agents 4定義**: coordinator.md「Agent定義との関係」(ポインタ+成果物ファイル名対応のみ)の規定どおりの形になった。

### 未確認事項

- `_002` findingsの未確認節が挙げた「grafana-alerting-tuning.md手順1の実行経路が現在も通るか」(monnie-investigateの語彙はホスト側でrepoから確認不能)は本レビューでも未確認。#1の文言修正の際も、実行可否の保証は書き足さないこと。
- 変更履歴表の追記内容の正しさは差分と突き合わせたが、Policy改訂自体の承認(EXEC-030: Yoshinobu)は本レビューの外にある。

### Verdict

**Request Changes** — #1のみblocking。#1を直せば(あわせて#2〜#4を採るかはCoordinator判断)Approve相当。

## 確認手段

- 差分全読: `git diff`(678行)をhunk単位で契約36項目へ対応付け。
- 参照先の実在・内容: 各正本ファイルを`grep`/`sed`で直接読んだ(上記列挙)。実装突合はplaybook 3本の`recovery_mute_minutes`と`recovery-probe.py` `fire_ladder`。
- 残存掃引: 撤回文言・実値を検索語にした`grep -rn`(対象にAIが読む`.claude/agents`・skills・CLAUDE.md系を含む)。
- 実ホストへのansible / ssh実行なし。git add/commit/pushなし。本ファイル以外のリポジトリ内ファイルは変更していない。

---

## Coordinator判断(2026-08-25)

- #1(blocking): 採用。区分実値の複製をやめ、「保護対象ではない」「状態を変えない確認は承認不要」の2つの不変の性質+EXEC-010 / EXEC-040ポインタへ書き換えた。
- #2〜#4: 採用。#3はP4削除と同じ変更履歴行へ統合して記録した。
- #5: 指摘どおり残置(過去の検証時点の記録であり現在値の主張ではない)。
- 是正後 `scripts/check-doc-consistency.py` 3チェックOK。
