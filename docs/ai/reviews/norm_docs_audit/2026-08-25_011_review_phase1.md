# 第2束フェーズ1(軽い束)レビュー(2026-08-25)

対象: 未commit作業ツリー差分(P3-1 / P5-1 / P5-2 / S5-2 / P1-4)。契約は `2026-08-25_004_fix_scope.md`「第2束」1.「軽い束」、実装記録は `_010_implement_phase1.md`。使用Skill: `document-norm-review` + `code-review`(出力型)。

## Code Review: 規範文書監査・第2束フェーズ1(軽い束5項目)

### Summary

5項目とも契約どおりに実装されており、ポインタ化で消えた規範は無い。削除された本文(EXEC-080/081/083、AR-102後段、core.md git行、SKILLのReviewer制限)は、いずれも指し先の正本に同内容以上の文言が現存することを逐語で確認した。旧文言の残存掃引もゼロ、scope外の変更も無い。blocking findingは無い。

### Critical Issues

なし。

### Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| 1 | docs/ai/roles/reviewer.md | 48 | 新bulletの末尾括弧`(skills/duplication-reuse-check/SKILL.md)`は手順への参照だが、SKILL側がreviewer.mdを正本と指すため相互参照になる。SKILL側が「権限の正本を名乗らない」と明記しているので実害は無いが、将来編集時に正本の向きを迷わせないよう「検査手順は同SKILL」等と役割を添えてもよい | Nit(任意) |
| 2 | skills/duplication-reuse-check/SKILL.md | 3 | frontmatter descriptionに「全リポジトリ横断検索は行わない軽量な照合に限定する」が残る。Skill選択用のscope記述として機能しており契約違反ではないが、reviewer.md側の規範を将来変えるときは同時に直す必要がある(片側だけ直る経路が細く残る) | Consistency(任意) |

いずれも今回の受入を妨げない。修正するかどうかはCoordinatorの判断でよい。

### What Looks Good(確認済み事項)

**観点1: ポインタ化で規範が消えていないか — 消えていない。** 削除文と`docs/ai/core.md`「安全機構がブロックしたとき」(core.md:29-39)を逐語突き合わせた。

- 旧EXEC-080「別の形で同じ結果へ到達しない/止めて、ブロックされた事実をCoordinatorへ報告する」→ core.md:33。「ブロックが妥当かどうかを判定しない — 被ブロック側もCoordinatorも解除できない」→ core.md:34(core側は「Yoshinobuへ上げる」まで持ち、削除側より広い)。
- 旧EXEC-081(目的の問い直し・検証設計の組み替え・報告義務)→ core.md:35。findings 5-1が指摘していたとおり、旧EXEC-081は末尾の担保の一文(「本質かどうかを都合よく判定していないことの担保がそれしかない」)を既に落としており、core側が上位互換。欠落なし。
- 旧EXEC-083(症状ではなく設定そのもの・両方が揃って初めて機能・安全側の壊れ方)→ core.md:37。ポインタ文の括弧が機構名(`permissions.defaultMode`/`autoMode`)を自前で持つため、EXEC-083単独で読んでも指示対象が特定できる。
- 旧AR-102後段「境界は能力の不在 — 鍵・wrapper・到達先が存在しないこと — で作る」→ core.md:39「実効的な境界は文章ではなく、能力の不在(鍵・到達先・wrapperが存在しないこと)で作る」。この行は「### 安全機構がブロックしたとき」節の末尾段落にあり、ポインタの指す節名で到達できる。AR-102前段(execpolicy禁止)は無改変で残置、ADR-009:135のAR-102参照(execpolicyの事実)は前段が受けるため宙ぶらりんにならない。
- `<!-- EXEC-080 -->`/`<!-- EXEC-081 -->`/`<!-- EXEC-083 -->`/`<!-- AR-102 -->`マーカーは全て現存(マーカー形式でgrep)。EXEC-080/081が同一ポインタ文を指す状態はv1.4変更履歴と実装記録の両方に明示されている。reviews/memory以外にEXEC-080/081/083をIDで引用する規範・コードは無い(ADR-009のAR-102のみ、上記のとおり成立)。

**観点2: P3-1の統合 — 両要素とも到達できる。** 改訂後のEXEC-030表の`git commit`/`git push`行(execution_boundary_policy.md:76)に「stageした内容の分類とcommitメッセージ案を提示する」(元EXEC側)と「subagentは承認の有無にかかわらず行わない」(元core側)が揃っている。core.md「Gitの扱い」の当該行は「承認区分・実行主体はEXEC-030(`docs/ai/policies/execution_boundary_policy.md`)を正本とする」の1行ポインタで、ルールID+ファイルパスで到達可能。旧文言「都度承認を得た対話セッション」の残存はゼロ。「対話セッション」→「Coordinator」の言い換えはCLAUDE.md「この対話セッションはCoordinator」と整合し、意味の変化は無い。core.mdの隣接行(`git add`はCoordinatorだけ、playbookにGit更新をさせない)は無改変。

**観点3: P1-4は新たな矛盾を作っていない。** 「開発工程のRole(Auditorを除く)が起動時に読む」を次と突き合わせた。

- `docs/ai/role-context-matrix.md:25` — Coordinator/Implementer/Reviewer/Testerの4列すべて**起動時**のまま無改変。読む義務は残っている。
- 同:34「この表はansy側の開発工程のRoleだけを扱う」、Auditor節(:36-44)の限定列挙は本Policyを含まない — 「Auditorを除く」と整合。
- `docs/ai/roles/operator.md:15`「Operatorはこのリポジトリを読まない」 — 「開発工程の」の限定と整合。ポインタに挙げた節名「この文書の位置づけ」も実在。
- `docs/ai/roles/auditor.md:62` はcore.mdと自Role文書だけを起動時必須とし、本Policyを挙げない — 整合。
- agent定義: `.claude/agents/tester.md:14`のみ本Policyを参照(Testerは読む側なので整合)、auditor定義は参照しない — 整合。
- CLAUDE.md:8(Coordinatorが対象作業に関わらず読む)、AGENTS.md(Codexは開発工程でRole指定時にcore.md経由で誘導される) — いずれも矛盾しない。
- 旧文言掃引: 「全Role」の全用例(reviews/memory除く)を1件ずつ確認。execution_boundary_policy.mdの読み手を「全Role」と主張する箇所は変更履歴v1.4行(引用として正当)以外に残っていない。残る用例はcore.md自身の共通原則・decoy承認・memory-classification・auditor.md/agent定義のcore.md読了記述で、いずれも本Policyの読み手主張ではない。
- core.md:82とPolicy冒頭:3が同じ読み手規定を並立で持つ構造は改訂前からのもので、今回は両側を同時に同内容へ直しており分岐していない。

**観点4: S5-2の移設。** reviewer.md「禁止・エスカレーション」へ入った新bulletは、既存の責任節「指定Contextが不足する場合は追加調査し」と衝突しない(制限は「重複・再利用の確認」に限定修飾されている)。また`document-norm-review` Skillが求める機械的な全repo掃引(規範レビュー時)とも衝突しない(同上)。SKILL本文に残る「発見・指示はCoordinatorが担う」は契約が明示的に許した工程記述。SKILL側の権限規定の残骸は本文にはゼロ(descriptionの1句はSuggestion #2、契約の範囲内)。

**観点5: 掃引とscope。** 撤回文言(「都度承認を得た対話セッション」「Reviewerは照合のみ」「全リポジトリ横断検索はReviewerに行わせない」「全Roleが起動時に読む」)の残存を`docs/ skills/ .claude/ roles/ playbooks/ scripts/ CLAUDE.md AGENTS.md`でgrepし、reviews/memory(過去記録)以外ゼロ。差分のファイルは契約5項目+fix_scope.md(Coordinatorの契約記録、レビュー対象外)+実装記録`_010`のみで、scope外の変更は無い。変更履歴表はexecution_boundary(v1.4に3項目まとめ1行)・autonomous_recovery(日付行1行)とも既存書式に従い追記済み。`python3 scripts/check-doc-consistency.py` → check1/2/3すべてOK、exit 0(自分で再実行)。`.claude/skills/duplication-reuse-check` symlinkは健全。

### 確認手段

- `git status` / `git diff` を着手時と返却直前に確認(差分は着手時から不変)。
- 逐語突き合わせは現物ファイル(core.md、execution_boundary_policy.md、autonomous_recovery_policy.md、reviewer.md、SKILL.md、role-context-matrix.md、operator.md、auditor.md、agent定義4本、CLAUDE.md、AGENTS.md)を直接読んで実施。実装記録`_010`の主張は鵜呑みにせず全項目を再検証した。
- 掃引はgrep(機械的)。実ホストへのansible/ssh実行、git add/commit/pushは行っていない。

### 未確認事項

- findings原文の「未確認11件」および第2束の中2件・越境残は本レビューの対象外(契約どおり)。
- `check-doc-consistency.py`が何を比較しているかの内部仕様までは検分していない(exit 0の再現確認のみ)。

### Verdict

**Approve**(blocking findingなし。Suggestions 2件はいずれも任意)。

---

## Coordinator判断(2026-08-25)

- Suggestion #1: 採用。括弧書きへ「検査手順は」を添え、正本の向き(規範=reviewer.md、手順=SKILL)を明示した。
- Suggestion #2: 据え置き。descriptionはSkill選択用のscope記述であり正本を名乗らない。reviewer.md側の規範を変えるときに同時に直す。
