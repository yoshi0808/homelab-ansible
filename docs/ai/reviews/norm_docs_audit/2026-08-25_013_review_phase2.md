# 第2束フェーズ2(中2件)レビュー(2026-08-25)

## Code Review: 規範文書監査・第2束フェーズ2(S2-3 重大度分類の正本新設 / P1-8・P5-3 CloudKey認証契約の一本化)

- 対象: 未commitの作業ツリー差分(`docs/ai/roles/reviewer.md` / `docs/ai/policies/cert_renew_cloudkey_policy.md` / `docs/ai/policies/unifi_backup_fetch_policy.md` / `docs/ai/reviews/norm_docs_audit/2026-08-25_004_fix_scope.md` への方向追記 / 新規 `_012_implement_phase2.md`)
- 契約: `2026-08-25_004_fix_scope.md`「第2束」2.「中2件」。観測事実: `_003_findings_skills.md` 2-3、`_001_findings_policies.md` 1-8・5-3
- 適用Skill: `skills/document-norm-review/SKILL.md`(欠陥クラス)、`skills/code-review/SKILL.md`(出力型)
- 初回判定(Request Changes)後、Coordinatorの是正2箇所を受けて再判定した(2026-08-25、同日)。是正diffと他ファイルの不変(`git diff --stat`一致)を現物で確認済み

### Summary

S2-3(重大度分類)は契約どおりで、4 Skillのポインタが実際に解決することも既存レビュー記録の用法との整合も確認できた。P1-8/P5-3(CloudKey一本化)は5項目の委譲・CSRF差分の相互参照とも成立。初回判定で挙げたMajor 1件(認証ヘッダー規範の適用範囲縮小)・Minor 1件(CCK-008前置の過大表現)はCoordinatorの是正で解消を確認した。blocking findingは残っていない。

### Critical Issues

| # | File | Line | Issue | Severity |
|---|---|---|---|---|
| 1 | docs/ai/policies/unifi_backup_fetch_policy.md | 166-169(UNIFI-007ポインタ文) | **【解消済み・再判定で確認】認証ヘッダー規範の適用範囲が縮んでいた(document-norm-review 欠陥クラス2/7)。** 削除前のUNIFI-007は「認証ヘッダー: **状態に関わる要求**には付与する」だったが、委譲先のCCK-008は「**状態変更系(POST/PUT/DELETE)**」とメソッドで範囲を切るため、unifi_backup_fetchの主要求 `GET /api/backup/download`(UNIFI-002が明記する、呼び出しで新規バックアップを生成する状態影響要求。実装は`roles/unifi_backup_fetch/tasks/main.yml:107-114`でヘッダー組立、`:125-134`のGETで使用)への付与義務がどちらのPolicyからも導出できなくなっていた。**是正**: UNIFI-007固有補足の先頭へ「バックアップ生成を伴う `GET /api/backup/download`(UNIFI-002)にも、状態変更系と同じ認証ヘッダー一式(CCK-008)を付与する。メソッドがGETであることを理由に省略しない」が追加された。削除前の規範の範囲を回復しており(「省略しない」の明文化で旧文より導出が明確)、解消と判定する | Major(解消済み) |

### Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| 1 | docs/ai/policies/cert_renew_cloudkey_policy.md | 205-207(CCK-008前置) | **【解消済み・再判定で確認】**「自分の差分(CSRF導出)**だけ**を持つ」はUNIFI-007が保持する固有の補足(認証情報の保管path・アカウント種別・no_log)を捨象した過大表現だった。是正で「自分の差分(CSRF導出)と固有の補足だけを持つ」へ改められ、UNIFI側の自己記述「本ポリシー固有の補足と、CSRF導出の差分」と対称になった | Minor(解消済み) |
| 2 | docs/ai/policies/unifi_backup_fetch_policy.md | 170-175 | 残置された認証情報の保管path(`inventories/vars/cloudkey.yml`)とアカウント種別は、実際には両実装の共有物である(`playbooks/cloudkey_cert_deploy.yml:12,30` が同じVaultファイルを読む)。一方CCK-020は「credential保管pathの実値はPolicyへ記載しない」と定めており、共有物の記載場所と方針が2 Policy間でねじれている。fix_scopeの一本化対象5項目に含まれないため今回の残置判断自体は契約どおりで妥当。将来CCK-020側の方針へ寄せるか(UNIFI側もpath実値を落としてvariable/Vaultを正本とする)は別件。**Coordinatorがscope外の記録として受領済み(non-blocking、対応不要)** | 一貫性(scope外・記録のみ) |

### What Looks Good

- **S2-3: 定義は契約(fix_scope 2026-08-25追記)の文言どおりで、新しい語彙・段階の発明は無い。** 4段階の各定義・blockingの意味・Verdict 3値との対応がfix_scopeの指定と一字対応することを突き合わせた。
- **S2-3: 4 Skillのポインタが実際に解決する。** `skills/code-review/SKILL.md:33`「重大度分類・エスカレーション基準は reviewer.md を参照」/ `skills/ansible-correctness-review/SKILL.md:32` / `skills/document-norm-review/SKILL.md:116` / `skills/test-gap-review/SKILL.md:35` を現物で読み、いずれも無変更のまま新設節へ到達することを確認した。Verdict 3値の出典 `skills/code-review/SKILL.md:28-29` も実在表記(Approve / Request Changes / Needs Discussion)と一致。
- **S2-3: 既存レビュー記録の実際の用法と矛盾しない。** `docs/ai/reviews/` 全体をSeverity欄の語彙で機械集計した — Critical 72 / Major 8 / Minor 8 に加え、2026-05〜06期の古い記録に High 91 / Medium 59 / Low 12 が存在する。新設節は将来の出力を縛る規範であり、過去記録を遡って無効化する文言は無い(現行のSkill・Role文書側に High/Medium/Low を要求する箇所が無いことも確認)。「Critical=常にblocking」も過去実例と整合する(例: `proxmox_patch_dryrun/2026-07-26_005_test_result.md` — Critical 2件の解消確認後にApprove)。
- **S2-3: 文書内の自己整合。** reviewer.md既存本文の「指摘を重大度、根拠、対象箇所、必要な対応とともに整理する」「重大度別findings」と新設節は衝突しない。reviewer.md(重大度の正本)とcode-review SKILL(Verdict語彙の正本)の相互参照は担当範囲が互いに素で、循環定義になっていない。
- **P1-8/P5-3: 一本化対象5項目の逐語実在。** UNIFI-007から削られた各文について委譲先CCK-008の対応文を逐語で突き合わせた — ログインパス/ボディ形状→CCK-008表「ログイン | POST /api/auth/login | {username, password}」、TOKEN cookie→「ログイン応答の `Set-Cookie` の `TOKEN`(JWT、有効2時間)を使う」、ヘッダー3点の列挙→CCK-008の箇条書き、ホスト名接続・`validate_certs: false`→CCK-008「接続」節。消失はMajor #1の適用範囲1点のみで、是正後は残存しない(再判定で確認)。
- **P1-8/P5-3: CSRF導出の記述は実装の実挙動と一致する。** 自分で現物を読んだ — `scripts/cloudkey_cert_deploy.sh:107-111`(JWTペイロードのcsrfTokenのみ。ヘッダーを見ない)、`roles/cloudkey_cert_deploy/tasks/deploy.yml:30-46`(同じくJWTのみ)、`roles/unifi_backup_fetch/tasks/main.yml:55-91`(X-CSRF-Token優先→X-Updated-CSRF-Token→両ヘッダー空のときだけJWT fallback)。fix_scope・実装記録`_012`が挙げるfile:lineはいずれも実在し、行ズレも実質無い(`_012`のdeploy.yml:30-45はほぼ正確)。CCK-008へ足した「CSRF導出は実装ごとに異なる」とUNIFI-007側の「ヘッダー優先+JWT fallback」の記述は、この実挙動の写しとして正しい。
- **相互参照の向きに循環・宙ぶらりんは無い。** 共有部分の正本=CCK-008、CSRF差分の正本=UNIFI-007と、担当が互いに素に分割されている。UNIFI-001の比較表(認証=「同左(ローカルアカウント)」)とも整合。UNIFI-007を指す現役規範文書は他に無い(`grep -rn "UNIFI-007"` — ヒットは歴史的review記録のみ)。認証契約の重複残存も機械掃引で確認した(`auth/login`・CSRFヘッダー名でrepo横断grep — 残るのは両Policy・実装コード・playbookヘッダコメント・歴史的review記録のみで、第三の規範記述は無い)。
- **変更履歴表**: CCK v1.4 / UNIFI v1.5 とも既存書式で追記され、記載内容が実diffと一致する。ルールID(CCK-xxx/UNIFI-xxx)の新設・退番なし、`<!-- UNIFI-007 --><!-- UNIFI-021 -->`マーカーも保持。
- **scope**: 差分は契約の3ファイル+fix_scopeへのCoordinator方向追記(契約そのものの記録)+実装記録`_012`のみ。scope外変更なし。IP・認証情報の実値追加なし。`python3 scripts/check-doc-consistency.py` を自分でも再実行し check1/2/3 OK・exit 0。

### 未確認事項

- 実ホスト(CloudKey API)への接続検証は行っていない(本レビューのscope外かつ禁止。CSRF導出の確認は実装コードの静的読解による)。
- GET /api/backup/download がX-CSRF-Token/Origin無しでもCloudKey側で通るかは実測していない(Major #1の「静かに通りうる」は可能性の指摘であり、通らないとしてもPolicyから義務が導出できない事実は変わらない)。

### Verdict

**Approve** — 初回のMajor 1件(UNIFI-007の認証ヘッダー適用範囲の縮小)とMinor 1件(CCK-008前置の過大表現)は、Coordinatorの是正diffを現物で読み解消を確認した。是正で変わったのは指摘の2箇所のみで、他ファイル(reviewer.md / fix_scope / 実装記録)は初回レビュー時から不変(`git diff --stat`一致)。Suggestion #2(認証情報保管pathの2 Policy間のねじれ)はscope外の記録としてCoordinatorが受領済みで、blocking findingは残っていない。変更履歴表(UNIFI v1.5)がGET付与規範の追加行に言及しない点は、旧規範の範囲回復であり「規定文の意味は変えていない」と両立するため指摘としない。

---

## Coordinator判断(2026-08-25)

- Major #1: 採用。UNIFI-007の固有補足へ「`GET /api/backup/download` にも認証ヘッダー一式を付与する(メソッドを理由に省略しない)」を戻した。
- Minor #1: 採用。CCK-008前置を「差分(CSRF導出)と固有の補足だけを持つ」へ。
- Suggestion #2(credential保管pathの2Policy間のねじれ): scope外の記録として受領。別件化するかは次の監査サイクルで判断する。
