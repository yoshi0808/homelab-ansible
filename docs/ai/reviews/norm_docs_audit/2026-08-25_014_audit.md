# Audit: norm_docs_audit(2026-08-25)

対象: `docs/ai/reviews/norm_docs_audit/`(`_001`〜`_013`)。起点は `_004_fix_scope.md`。

## 再構成の結果

**再構成できた。** 詰まった箇所は無い。

経緯: `_001`〜`_003`(findings、Policy/Context/Skill群を横断監査、54件)→ `_004`(fix_scope、機械的修正36項目=第1束と、構造判断が要る束=第2束に分割し、第2束をさらに3フェーズへ分割)→ `_005`/`_006`(第1束の実装、Group A/B)→ `_007`(第1束レビュー、Request Changes→Coordinator是正→Approve相当)→ `_008`/`_009`(第2束フェーズ3=分類の越境、3巡でApprove)→ `_010`/`_011`(第2束フェーズ1=軽い束、Approve)→ `_012`/`_013`(第2束フェーズ2=中2件、初回Request Changes→是正→Approve)。

git履歴とも一致する。4回のcommitが4つの実装単位に対応する。

```
4f79a4f norm_docs_audit: 第2束フェーズ2、重大度分類の正本新設とCloudKey認証契約の一本化   ← _012/_013
feb3327 norm_docs_audit: 第2束フェーズ1、git規則・原則再掲の二重化を解消し読み手の範囲を実態へ絞る ← _010/_011
a0e4fad norm_docs_audit: 第2束フェーズ3、分類の越境を是正                          ← _008/_009
f5594a9 norm_docs_audit: 第1束36項目を修正                                     ← _005/_006/_007
```

`git status --porcelain` は空 — 全て確定済みで、監査時点で未commitの差分は無い。

## 現物での検証(サンプル)

記録の主張を鵜呑みにせず、以下を自分で現物照合した。

- `_007` #1是正: `docs/ai/context/operations/grafana-alerting-tuning.md` の `monnie` 区分記述とEXEC-010の実際の区分(`monnie`除去後の「それ以外」行)が一致することを確認。
- `_009`「未解決事項」で申し送られたTIME-010参照(`healthcheck.md`経由の間接参照)は、`time_sync_check_policy.md` v1.5(`_010`/`_011`のフェーズではなく別途Coordinatorが追随)で`skills/ansible-implementation-style/SKILL.md`「check系shellの責務分離」への直接参照に修正済みであることを確認(`time_sync_check_policy.md:147,240`)。
- `_011`で「解消」と申告されたEXEC-080/081/083→core.md「安全機構がブロックしたとき」への統合、AR-102後段→同節へのポインタを、core.md現物と突き合わせて確認。
- `_013`のS2-3(reviewer.md「重大度分類」新設)を現物で確認、4 Skill(`code-review`/`ansible-correctness-review`/`document-norm-review`/`test-gap-review`)のポインタが実際にこの節へ到達することを確認。
- `_013`のP1-8/P5-3是正(UNIFI-007へ「`GET /api/backup/download`にも認証ヘッダー一式を付与、メソッドを理由に省略しない」を復元)を現物で確認 — 初回レビューが指摘したMajor(認証ヘッダー義務の適用範囲縮小)が実際に解消されている。
- `_006`のC5関連(healthcheck.md §1移設)、`semaphore_schedules_catalog`ヘッダへの義務移設、EXEC-005への鍵禁止追加を現物で確認。
- `python3 scripts/check-doc-consistency.py` を自分でも実行し、3チェックとも `OK`(114/8/104件比較)を確認した(記録の申告と一致)。
- P2-4/review_007 #3(AR「P4」削除+「4分岐」の数宣言不一致)は、`autonomous_recovery_policy.md`の2026-08-25変更履歴に統合して解消されていることを確認(記録どおり)。

いずれも記録の主張どおりで、参照先の不在・内容不一致は見つからなかった。

## 決定の追跡

第2束の3フェーズ分割、各フェーズの移設先確定はすべて `_004` に「Coordinator判断」「Yoshinobu合意」として明記され、日付も付いている。第1束・第2束フェーズ2の是正はReviewer findingsへの「Coordinator判断」節が `_007`/`_013` 末尾にあり、採否と理由が1件ずつ記録されている。**辻褄が合わないものは無い。**

## 指摘

### 1. `docs/ai/status.md` 72行目の記述内容が実態より古い(non-blocking、実施事項)

該当行:

> **規範突合の第2束(構造判断)** | 2026-08-25のFable横断監査(findings 54件)のうち機械的修正36項目は適用済み。残るのは構造判断が要るもの(CloudKey認証契約の正本一本化、git規則のcore.md/EXEC-030二重化、原則の言い直し、重大度分類の正本不在、Contextの分類越境ほか)と未確認11件。

しかし `_004`〜`_013` が示すとおり、ここに列挙された構造判断項目(CloudKey認証契約の一本化=P1-8/P5-3、git規則の二重化=P3-1、原則の言い直し=P5-1/P5-2、重大度分類の正本不在=S2-3、Contextの分類越境=C5-1〜C5-5)は**すべて2026-08-25中に実装・レビュー・Coordinator是正・commitまで完了している**(上記4commit)。この行は「残る」ものとして列挙しているが、実際に残っているのは `_004` 末尾が挙げる「未確認11件」と「C3-3(現在一致している複製群)」、および `_013` Suggestion #2で新たに記録された「credential保管pathの2Policy間のねじれ(CCK-020 vs UNIFI)」のみである(いずれも各フェーズ実装記録で「対象文書に触れるときに巻き取る、独立の束にしない」とされたまま、実際には巻き取られた形跡がない — `_005`〜`_013`のどのscope外申告にも未確認11件・C3-3への言及は無い)。

**該当行が残っていること自体は指摘に当たらない**(Auditorは行を消す前に呼ばれる設計)。指摘なのは、**行の内容が「まだ手つかず」の対象を実態より広く述べている**点である。Auditor役割文書の「状態の正本との突合」検査項目に該当する。

**クローズを妨げるか**: 妨げない。Auditor役割文書は「記帳の遅れ…は、それ単独では「クローズを妨げる指摘」に当たらない」と定めており、この行の更新はCoordinatorが本監査の合否通知を受けた同一セッション内で行う実施事項として扱ってよい。更新後の内容は「未確認11件」「C3-3(複製群の現状一致)」「credential保管pathのねじれ(CCK-020 vs UNIFI-007、`_013`記録)」の3点に絞ることを推奨する。

### その他

- Reviewer記録(`_007`/`_009`/`_011`/`_013`)が指摘したCritical/Major findingsは、いずれも同一記録内でCoordinator是正→現物確認まで完結しており、未解決のまま次工程へ持ち越されたものは無い。
- `_006`と`_008`の間でC1-5(`system/semaphore.md`のバージョン実値)の扱いに軽微な記述の重複(過去の検証記録として意図的に残置)があるが、両記録とも同じ理由づけで一致しており矛盾ではない。
- 転記先の実在: `_002`/`_003`findingsが「移設した」「起票した」と述べた先(`docs/ai/memory/decisions/rejected-proposals.md`、`skills/ansible-implementation-style/SKILL.md`、`semaphore_schedules_catalog`ヘッダ、`docs/ai/roles/reviewer.md`)はすべて現物に実際の記述を持つことを確認済み。転記先が存在しないケースは見つからなかった。

## 案件をクローズしてよいかの判定

**受入**。

指摘は1件のみで、Auditor役割文書が定める「記帳の遅れは単独でクローズを妨げない」に該当するnon-blocking事項である。反映(status.md 72行目の更新)は本監査の合否通知を受けた同一セッション内でCoordinatorが行うことを推奨する。再監査は前提としない。

## 確認手段

- `_001`〜`_013` 全13ファイルを通読。
- `git log --oneline -20`、`git status --porcelain`、`git show`は行わず`grep`/`sed`で現物ファイルを直接照合。
- 現物照合先: `docs/ai/core.md`、`docs/ai/policies/execution_boundary_policy.md`、`docs/ai/policies/autonomous_recovery_policy.md`、`docs/ai/policies/time_sync_check_policy.md`、`docs/ai/policies/unifi_backup_fetch_policy.md`、`docs/ai/policies/cert_renew_cloudkey_policy.md`、`docs/ai/roles/reviewer.md`、`docs/ai/role-context-matrix.md`、`docs/ai/memory-classification.md`、`docs/ai/context/system/semaphore.md`、`skills/ansible-implementation-style/SKILL.md`、`skills/subagent-briefing/SKILL.md`、`roles/semaphore_templates/defaults/main.yml`、`docs/ai/status.md`。
- `python3 scripts/check-doc-consistency.py` を実行し3チェックOKを確認(記録の申告と一致)。
- 実ホストへの到達、git add/commit/pushは行っていない。

---

## Coordinatorのクローズ判断(2026-08-25)

受入判定を受け、本案件をクローズする。唯一の指摘(status.md行の記帳遅れ)は本クローズと同じcommitで、残る3点(未確認11件のうち未解消分、C3-3、credential保管pathのねじれ)へ絞る形で更新した。
