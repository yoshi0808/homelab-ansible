# Code Review: quory月次ヘルスレポート未commit差分

## Summary

要求001・計画002・QHM Policy・ADR-014を現物と独立照合した。quory専用入口、check/通知抑止、healthとジョブrcの分離、Notionの同月ページ再利用、既存Proxmox経路の非変更は確認できた。一方、AC1〜AC4を満たさない具体的経路が残る。

## Critical Issues

なし。

## Major Issues

| # | File | Line | Issue | Severity |
|---|---|---:|---|---|
| 1 | `roles/quory_health_monthly/module_utils/quory_health.py` | 30-38 | `df`の`use_percent_raw`を判定に使わず、`used_bytes / size_bytes`で再計算する。`df`の使用率はavailable等を踏まえた値で一致しない場合があり、要求P0・QHM-030の「df自身の使用率」「別分母で再計算しない」に反する。`use_percent_raw`が欠損・不正でも正常判定できる。`df`値を厳密に解釈して保存・閾値判定し、不正値をUNKNOWNにすること。現テスト99-103行はfixtureが同じ式で`pcent`を作るためこの欠陥を検出しない。 | Major |
| 2 | `roles/quory_health_monthly/module_utils/quory_health.py` | 65-111, 255-313 | 前回差分はNVMe error-log/media counterだけ。root/EFI使用量・メモリavailable・swap使用量の前回比が計算・本文表示されず、swap使用増や容量/メモリの悪化もissue化しない。AC3・QHM-032の「対象・現在値・比較元・差分・対応要否」を満たさない。これらの比較を保存済み成功観測から行い、数値と対応要否を本文・短報に示すこと。 | Major |
| 3 | `roles/quory_health_monthly/tasks/report.yml`, `roles/quory_health_monthly/module_utils/quory_health.py` | 70-73, 236-252 | Slackに渡す`short_summary`は正常時には対象hostもreport IDも含まず、異常時にも現在値・比較元・差分を含まない。通知本文にpublication状態とURLを足すだけなので、AC1の同一ID照合、AC3の値・差分、QHM-050の対象/対応要否が欠ける。またUNKNOWNが3件あるとCRITICALが「ほかN件」に埋もれる。重要事項を件数制限より優先し、ID・対象・主要値/差分を短報に載せること。 | Major |
| 4 | `roles/quory_health_monthly/files/quory_collect.py`, `roles/quory_health_monthly/module_utils/quory_health.py` | 76-87, 143-159 | `lsblk`が失敗・JSON破損・デバイス一覧欠落でも`namespaces=[]`となり、`nvme_transport_observed=false`、device issueなし、総合health=OKになり得る。AC2/QHM-030/031の取得不能をUNKNOWN・rc非0にする契約に反する。`lsblk`自体の取得・解釈成否を検証してreportへ渡し、不能をUNKNOWNとして扱うこと。現テスト134-138行は空deviceでOKを期待しており、失敗との区別を検証しない。 | Major |
| 5 | `roles/quory_health_monthly/module_utils/quory_health.py` | 130-151 | EFI観測が失敗すると、成功したroot結果を`entry['filesystems']`へ代入する前に例外で抜け、後続のメモリ/NVMeも処理しない。AC4の「成功した部分を可能な限り記録」を満たさず、原因も`ValueError`という型だけへ縮退する。各観測結果と具体的な失敗理由を部分保存し、最終rcだけ失敗とすること。 | Major |
| 6 | `roles/quory_health_monthly/files/quory_collect.py` | 41-56 | `df /boot/efi`はEFIが未mountでもそのパスを含むroot filesystemを返せる。実際のmount targetを読まず要求されたパスを`mountpoint`として記録するため、rootの値をEFIとして二重計上してOKにできる。AC1/QHM-030のEFI観測には実mountを検証し、不一致ならUNKNOWNにすること。 | Major |

## Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---:|---|---|
| 1 | `docs/ai/status.md` | 14 | 現行差分は「実装前・次はImplementer着手」のまま。Coordinatorが工程現在地を更新すること。 | 記録整合 |

## What Looks Good

- `groups['control_nodes'] == ['quory']`確認後の`add_host`、collectだけのquory play、replay時の非収集を確認。controllerが実際にquory自身かは配備時readback待ち。
- `--check`のcollector read-only実行、archiveの無書込preview、Notionのtoken読取前抑止、既存`common_slack`のcheck/skip抑止を確認。通知・保存経路の実Ansible結合テストは未実施。
- NVMeコマンド失敗時のUNKNOWNと最終非0 assert、WARNING/CRITICALのみならrc0という分離を確認。
- Notionは親未確定時停止、create予約、同月ページ照合、古いreport拒否、replayで再収集しない経路を確認。既存Proxmox role・monitoring healthcheckの実装変更はなく、Semaphoreは専用template追加だけでscheduleなし。ADRの隔離方針とQHM Policyの対象・禁止事項に規範の消失や拡大は見つからなかった。

## 確認・再現手段と未確認事項

- `python3 -B -m unittest discover -s tests -p test_quory_health_monthly.py -v`: 20/20成功。ただし上記反例はfixture/test未収載。最小追加テストは、df `pcent`と`used/size`を異なる値にする、`lsblk.rc != 0`/不正JSON、EFI未mount相当、EFIのみ失敗の部分保存、fs/mem/swap悪化、3件超のUNKNOWNとCRITICAL混在、正常/異常Slack本文のID・値・差分確認。
- 対象diff、要求・計画・Policy・ADR、対象role/test、`common_slack`、inventoryの`control_nodes`と`reports_base_dir`、Semaphore追加を読んだ。重複照合は指定の既存Proxmox契約との比較に限定した。Ansible実ホスト実行、Notion/Slack/Semaphore API接触は行っていない。
- AC5の実playbook統合、AC1/AC6の実収集・公開・schedule readback、NVMeツール/権限、Notion親ページ/Integration権限は未確認。scheduleと別setup入口は実測後の配備工程として未着手であり、今回コードに無いこと自体はscope逸脱としない。

## Verdict

Request Changes。Major 6件がblocking。修正後の差分再レビューと、Testerによる無害な結合検証を推奨する。
