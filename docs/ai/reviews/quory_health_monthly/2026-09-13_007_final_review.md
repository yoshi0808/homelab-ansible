# Code Review: quory月次ヘルスレポート 006指摘後の再レビュー

## Summary

006の3件は通常経路では改善したが、成功観測の選定、比較元の識別、`lsblk`行の検証に反例が残る。現行44テストは全通過したものの、この3経路は未収載。005/006は履歴として保持し、本書を現行判定とする。

## Critical Issues

なし。

## Major Issues

| # | File | Line | Issue | Severity |
|---|---|---:|---|---|
| 1（006 #1残存） | `roles/quory_health_monthly/library/quory_health_archive.py` | 13-17, 87-110 | 比較元判定はhost `collection`/`health`だけを見る。履歴破損時はreport全体が`history_error=true`/`health=UNKNOWN`でジョブ非0でも、host自身は`health=OK`となり、`usable_as_comparison_basis`はTrueを返す。破損履歴を修復して次回実行すると、この判定不能reportがfs/memory比較元・device baselineの候補になる。QHM-031/032の「成功観測」にはreport全体の判定不能/履歴破損も除外すること。`report(history_error=True)`で全体UNKNOWN・host OK・判定Trueをローカル再現。 | Major |
| 2（006 #2残存） | `roles/quory_health_monthly/module_utils/quory_health.py` | 58-68, 102-120, 358-377, 414-433 | 閾値異常の短報に現在値・前回値・差分・比較元日時は入るが、比較元report IDが短報とMarkdownに出ない。前回90%→今回95%のfixtureでJSONの`comparison_source.report_id`は保存される一方、短報・本文には前回IDがないことを再現。表示日時は分単位なので、同分内の再実行が複数あれば比較元を一意に追えず、要求P1の比較元ID・日時とAC3/QHM-030の出所追跡を満たさない。比較元IDを人向け表示にも含めること。 | Major |
| 3（006 #3残存） | `roles/quory_health_monthly/files/quory_collect.py` | 76-112 | `blockdevices`が存在しても行が`{}`なら`nvme_namespaces`は空一覧を返す。rc0・`{"blockdevices":[{}]}`をローカル再現し、`lsblk_valid=true`→transport=false→health=OKになり得る。これは「正しく列挙してNVMeなし」と「行の必須情報を解釈できない」を混同し、QHM-030/031に反する。各行の`name`/`type`の存在・型を検証し、空一覧`[]`だけを正常なゼロ件として扱うこと。 | Major |

## Suggestions

なし。

## What Looks Good

- NVMe tool失敗でhost health=UNKNOWNの場合は、fs/memory比較元とdevice baselineの両方から除外するようになった。通常の成功reportは比較元となり、WARNING/CRITICALの健康上の異常は成功観測として扱う。
- fs/memoryの絶対閾値issueにも現在・前回・差分と比較元日時が付き、UNKNOWN/CRITICALを短報の件数制限から落とさない。JSONには比較元ID・日時が残る。
- `lsblk`の空JSON・非オブジェクト行・欠損`blockdevices`は取得不能へ変換し、真に空の`blockdevices: []`はゼロ件と区別する。観測playbookのquory専用性、check/skip抑止、既存Proxmox経路の非変更も維持。

## 確認・未確認事項

- `python3 -B -m unittest discover -s tests -p test_quory_health_monthly.py -v`: 44/44成功。反例は`PYTHONPATH=tests python3 -B -c`で既存fixtureと関数を呼び、実ホスト・外部サービスに接続せず再現。追加最小テストは、履歴破損reportの比較元除外、前回IDの本文/短報表示、`blockdevices:[{}]`のUNKNOWN判定。
- AC1/AC6の実ホスト・Notion・Slack・schedule readback、AC5の実Ansible結合は未確認でTester/配備工程に残る。今回の修正でscope拡大や本番送信の経路追加は見つからない。

## Verdict

Request Changes。Major 3件がblocking。
