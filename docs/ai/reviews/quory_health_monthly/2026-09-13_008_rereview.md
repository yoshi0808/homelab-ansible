# Code Review: quory月次ヘルスレポート 007指摘後の再レビュー

## Summary

007のMajor 3件と追加の`lsblk`非0・妥当JSON反例は解消した。現行差分に新たなscope拡大や既存Proxmox経路の変更はない。ただしAC3のswap悪化短報に、現在のswap使用量が欠ける未達を確認した。

## Critical Issues

なし。

## Major Issues

| # | File | Line | Issue | Severity |
|---|---|---:|---|---|
| 1 | `roles/quory_health_monthly/module_utils/quory_health.py` | 105-121, 358-377 | swap使用量が前回比で悪化した場合、issue detailは`swap_growth`とメモリavailableの`comparison_note`を連結するだけで、前回/現在のswap使用量を短報へ出さない。ローカルfixtureでswap 50,000,000→250,000,000 bytesを与えると、Slack短報は`+200000000 bytes`と無関係な`前回50.0%→今回50.0%`を示す一方、現在値`250000000 bytes`はない。AC3が短報に要求する対象・現在値・比較元・差分のうち現在値を欠き、メモリ比率をswap比較と誤読させる。swap専用の前回/現在/差分/比較元をdetailに出すこと。 | Major |

## Suggestions

なし。

## What Looks Good

- 007 #1: `usable_as_comparison_basis(report, host)`が全体`history_error`・`health=UNKNOWN`・`collection!=ok`とhost側の失敗を除外し、fs/memory比較とdevice baselineの両方で同じ条件を使用する。履歴破損reportの後に破損を除いて再実行するfixtureも比較元未採用を確認する。
- 007 #2: fs/memory異常のdetailとMarkdownに比較元report ID・日時、現在値・前回値・差分を含め、同分内再実行の比較元も識別できる。
- 007 #3と追加反例: `nvme_namespaces`はrc非0をJSON形状に先立って拒否し、`blockdevices`欠損・`[{}]`・型違いを判定不能にする。`[]`だけを正常な空一覧として返し、collectorは失敗時`lsblk_valid=false`/transport不明、reportはUNKNOWN/collection errorとする。
- quory専用の収集経路、check/skip抑止、Notionの再送・同月再利用、既存Proxmox非変更は維持。

## 確認・未確認事項

- `python3 -B -m unittest discover -s tests -p test_quory_health_monthly.py -v`: 52/52成功。`python3 -B -m unittest discover -s tests -p 'test_*.py'`: 70/70成功。swap反例は`PYTHONPATH=tests python3 -B -c`で既存fixtureから`short_summary`を生成し、外部接続なしで確認した。最小追加テストは前回swapが非0の悪化時、短報に現在/前回swap値・差分・比較元が揃うこと。
- AC1/AC6の実ホスト観測・Notion/Slack照合・schedule readback、AC5の実Ansible結合は未確認でTester/配備工程に残る。実ホスト・Notion・Slack・実Semaphoreには触れていない。

## Verdict

Request Changes。AC3未達のMajor 1件がblocking。005〜007は履歴として保持し、本008を現行判定とする。
