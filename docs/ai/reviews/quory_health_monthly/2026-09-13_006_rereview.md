# Code Review: quory月次ヘルスレポート 005指摘後の再レビュー

## Summary

005のMajor 6件を現行の要求001・QHM Policy・コード・テストと再照合した。#1・#5・#6は解消、#2・#3・#4は主要経路が改善したものの、下記反例が残る。新たなscope拡大や既存Proxmox経路の変更は見つからなかった。

## Critical Issues

なし。

## Major Issues

| # | File | Line | Issue | Severity |
|---|---|---:|---|---|
| 1（005 #2残存） | `roles/quory_health_monthly/library/quory_health_archive.py` | 74-81, 94-101 | 「前回の成功観測」の選択がhostの`collection == 'ok'`だけを見る。NVMe取得不能ではreportのhealth=UNKNOWN・ジョブrc非0でもhost collectionはokのため、その失敗reportが次回のfs/memory/swap比較元になり、当月device baselineにも取り込まれる。実際にNVMe失敗reportを保存して次回正常reportを保存すると、その失敗report IDがrootの`comparison_source`になった。QHM-032・要求P1に合わせ、判定不能/ジョブ失敗相当のreportを比較元・baseline固定から除外すること。 | Major |
| 2（005 #3残存） | `roles/quory_health_monthly/module_utils/quory_health.py` | 52-68, 342-361 | 短報はID・hostと重要issueの非省略を満たしたが、絶対閾値による容量異常は現在率だけのissueになる。前回90%→今回95%で成長警告閾値10pt未満の場合、Slack短報は「95%」のみで比較元ID/日時・差分+5ptが出ない。AC3は異常時にも「現在値・比較元・差分」を短報へ要求するため未達。異常issueのdetailに比較元と差分を含めるか、短報に比較要約を加えること。 | Major |
| 3（005 #4残存） | `roles/quory_health_monthly/files/quory_collect.py`, `roles/quory_health_monthly/module_utils/quory_health.py` | 85-87, 219-226 | `lsblk`がrc0で`{}`を返した場合、collectorは`blockdevices`欠損を空配列として扱い、report側も「JSONがdict」のみで成功とみなす。`devices=[]`/transport=false/health=OK/collection=okを再現した。列挙不能を正常な「NVMeなし」に畳まないよう、`blockdevices`の存在・型と各行の構造を検証すること。 | Major |

## Suggestions

なし。

## What Looks Good

- 005 #1: `df`のpcentをparseし、used/sizeと異なる値・欠損/不正値のテストを追加。df自身の値で判定し、判定不能はUNKNOWNにする。
- 005 #5: root/EFI/memory/deviceを独立に処理し、EFIのみ失敗でも成功したroot・memory・deviceをreport/Markdownに保持する。
- 005 #6: `findmnt --target`の実mountを確認してからdfを実行し、EFI未mountまたは確認失敗をUNKNOWNにする。
- 005 #2/#3/#4の主要経路: fs/memory/swap差分と悪化issue、ID/hostつき短報、UNKNOWN/CRITICALの非省略、`lsblk`失敗・JSON decode失敗のUNKNOWN化は確認した。quory以外の観測・変更、check/skip抑止の解除、既存Proxmox roleの変更は見つからない。

## 確認・未確認事項

- `python3 -B -m unittest discover -s tests -p test_quory_health_monthly.py -v`: 35/35成功。未収載の反例は、`lsblk={'rc':0,'json':{}}`でOK、NVMe tool失敗reportの後の正常archiveで失敗IDを比較元に採用、前回90%→今回95%の短報で比較元・+5pt欠落。いずれも`PYTHONPATH=tests python3 -B -c`でテストfixtureを直接呼ぶローカル再現であり、外部接続なし。
- AC1/AC6の実host・Notion・Slack・schedule readback、AC5の実Ansible結合、NVMe tool/権限、専用Notion親/Integration権限は未確認。Tester/配備工程で扱う。今回、実ホスト・本番API・Slackへの接触はしていない。
- 既存Policy/ADRの安全境界変更、Proxmox/monitoring roleの差分、schedule追加はなく、別setup入口とscheduleは実測後の工程のまま。指定成果物以外は編集していない。

## Verdict

Request Changes。残存Major 3件がblocking。005は履歴として保持し、本006を現行判定とする。
