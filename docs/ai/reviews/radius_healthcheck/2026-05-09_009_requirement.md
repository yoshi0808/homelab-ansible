# 要求仕様: radius_healthcheck への通知追加

作成日: 2026-05-09

## 対象ファイル
- roles/radius_healthcheck/tasks/main.yml（既存を修正）

## 追加内容

### vars 読み込み（tasks の先頭に追加）
- ansible.builtin.include_vars
- file: "{{ inventory_dir }}/../vars/mail.yml"
- delegate_to: localhost
- become: false

### 通知 task（Print health summary の直後、Fail if critical の直前）
- 条件: radius_criticals | length > 0 or radius_warnings | length > 0
- community.general.mail で STARTTLS 送信
- delegate_to: localhost / become: false
- 添付なし（proxmox の Send email without attachment パターンを踏襲）
- 件名: [RADIUS Healthcheck] {{ radius_report.result.status }} - {{ inventory_hostname }}
- 本文: host / status / criticals / warnings を含む

## 参照ファイル
- roles/radius_healthcheck/tasks/main.yml（既存）
- proxmox の Phase 6 メール実装（パターン踏襲）
- inventories/homelab/vars/mail.yml（smtp_host / smtp_port / smtp_user / smtp_password / mail_to）

## 制約
- shell 側は触らない
- 既存の判定ロジック・レポート保存は変更しない
- Fail if critical は通知の後に残す

---

Next step files:
- docs/ai/reviews/radius_healthcheck/2026-05-09_009_requirement.md
