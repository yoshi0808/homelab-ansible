# Code Review: ubuntu_vm_apply_timeout_sigttou

日付: 2026-09-03
Reviewer: Codex
対象: requirement `2026-09-03_001_requirement.md` AC1〜AC5、指定された実装差分4ファイル、および実装記録 `2026-09-03_003_implement.md`

## Summary

`setsid -w` の内側で既定モードの `timeout` を使う実装は、制御端末を外すこととプロセスグループ全体への timeout signalを両立しており、AC1〜AC5を満たす。前回F2の回帰テストも番兵欠落時に実行前でfail closedとなり、blocking findingは解消した。

重大度別件数: Critical 0 / Major 0 / Minor 0

## Critical Issues

| # | File | Line | Issue | Severity |
|---|---|---:|---|---|
| — | — | — | なし | — |

## Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---:|---|---|
| S1 | `scripts/tests/apply_timeout/run-tests.sh` | 13-14 | 現在の自動テストはプロセス木側だけを対象とし、`setsid -w` を削除して素の `timeout` に戻しても localhost では3つの挙動ケースが通る。sandboxの実測はAC1を裏付けているためblockingではないが、可能なら擬似TTYを安全に作る回帰ケース、または端末分離wrapperの存在を確認する静的preconditionを追加すると、今回の直接原因の再導入を早期検出できる。 | Test gap |

## What Looks Good

- 旧F1: `apply.yml:125-130` の `setsid -w timeout` は、取り下げた `--foreground` と違って timeout の既定プロセスグループsignalを保つ。実際のローカル `ansible.builtin.command` でも、継続出力する子は rc=124、TERMを無視する対象は rc=137で期限内に返り、擬似TTY上の `stty sane` は停止せず rc=0になった。
- F2: `run-tests.sh:46-68` は番兵発見を `found` で保持し、未発見、空wrapper、apt/upgradeを含むwrapper、未解決JinjaのすべてをJSON出力前に拒否する。`run-tests.sh:76-87` のnegative testは抽出関数だけを呼ぶため、失敗ケースで `ansible-playbook` へ到達しない。絶対パス化と別名化のsedが実際に `apply.yml:130` を置換すること、および全体が `pass=5 fail=0` になることを独立確認した。
- AC1: `setsid` が新しいsessionを作って制御端末を外すため、apt実行は「背景プロセスグループとして制御端末に触る」状態にならない。sandboxで実際の `ansible.builtin.command` + `become: true` + `stty` を使った成功結果も実装記録 `:47-55` に残っている。
- AC2: `timeout` は既定モードのままなので子孫を含むプロセスグループへsignalを送り、`setsid -w` は待機後に rc=124/137を伝播する。継続出力する子、眠る孫、TERM無視の3経路が `run-tests.sh:121-131` で、実装から抽出したwrapperを通っている。
- AC3: `apply.yml:130-137` の `apt-get -y full-upgrade`、`Dpkg::Options::=--force-confold`、`DEBIAN_FRONTEND`、`NEEDRESTART_MODE` は保持され、defaultsの3600秒/60秒も不変である。
- AC4: rcは `apply.yml:138` にregisterされ、rc=124/137は `:161-193`、その他の非0は `:195-200` で具体的なジョブ失敗へ変換される。`setsid -w` のrc伝播も実測されている。
- AC5: destructive includeは引き続き `tasks/main.yml:398-400` の `not ansible_check_mode` で除外され、対象差分はこの境界を変更していない。
- Security: 本体は固定要素と既存変数を `command.argv` で渡しており、shell再解釈・追加の秘密情報出力はない。テスト抽出も本番コマンド境界を判定できない場合は実行しない。
- Duplication/scope: 実行用 `timeout` は対象roleの1箇所だけで、共通化対象はない。`.gitignore:49-55` の打消しは新規回帰テストを追跡対象にする変更としてscope内である。指定外の未commit差分は評価・変更対象に含めていない。
- 検証: `bash -n scripts/tests/apply_timeout/run-tests.sh`、同テストの `pass=5 fail=0`、negative fixture 2種の置換確認、`ansible-playbook playbooks/ubuntu_vm_full_upgrade.yml --syntax-check`、`scripts/check-doc-consistency.py` の3検査、`git diff --check` は成功した。

## Verdict

**Approve — blocking: no.** Critical 0 / Major 0 / Minor 0。S1はnon-blockingの残存テストギャップであり、本番aptでの初回成立確認は実装記録の未確認事項としてTesterへ引き継ぐ。
