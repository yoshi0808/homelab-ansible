# recovery_monitoring_check.yml ほか(review)

作成: 2026-08-01 / Reviewer
対象: `docs/ai/reviews/recovery_pause_daily_check/2026-08-01_001_requirement.md` §5(P0-1〜P2-1)・§6(AC1〜AC6)
対象diff:
- `playbooks/recovery_monitoring_check.yml`(新規)
- `playbooks/README.md`
- `docs/ai/policies/autonomous_recovery_policy.md`(AR-103新設)
- `docs/ai/context/operations/autonomous-recovery.md`
- `docs/ai/memory/incidents/2026-07-29_global-monitoring-pause-left-on-8-days.md`
- `docs/ai/status.md`

## Code Review: `playbooks/recovery_monitoring_check.yml` ほか

### Summary

契約のP0-1〜P0-8、AC1〜AC6を全項目、静的読解 + decoy実行(`ansible_connection=local`、閉じたスクラッチディレクトリ、実host無接続、実Slack無送信)で確認した。blocking findingは無い。「異常なのに正常と報告される」経路(未定義変数・skip・module失敗が緑で終わる組み合わせ)は作り込まれておらず、判定不能時(AC4)はSlack通知の手前で非ゼロ停止することをdecoyで実証した。誤送信経路(正常時送信・検証時の本番到達)も無い。

### Critical Issues

なし。

### Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| 1 | `playbooks/recovery_monitoring_check.yml` | 44 | `rmc_status.stdout is match('^(ACTIVE|PAUSED)')` は前方一致であり、wrapperが将来`ACTIVE`で始まりながら異常を含む出力(例: `ACTIVE_but_stale`)を返した場合、判定不能として捕捉できず健全扱いになる。現状の`homelab-monitoring-status`は`ACTIVE`/`PAUSED (all targets)`の2値のみを返す安定した既存wrapperであり、直ちに直す必要はないが、wrapper側の出力書式を変える人はこの前方一致依存を踏まえること | 保守性・将来のドリフト耐性 |
| 2 | (検証範囲) | - | AC1のうち「`recovery_probe_service_enabled`が真のhostで`recovery-probe`が実際に`active`」の組み合わせは、実systemd unitの起動が本レビューの実行境界(`/tmp`配下限定)外のため、コードパス確認(`rmc_paused=false and rmc_probe_down=false`→block skip)と代替decoy(ACTIVE + `recovery_probe_service_enabled: false`→rc=0・無通知)でのみ確認した。実active unitでの直接確認はTester側で可能なら追加を推奨 | 検証网羅性(未確認事項) |
| 3 | (検証範囲) | - | `scripts/git-pre-commit-check.sh`はstaged diffを前提とする実装(IPv4 literal検査など)のため、`git add`を行わないReviewerの立場からは素通し(no staged files)以上の実行ができなかった。同スクリプトが内部で呼ぶ`scripts/check-tester-gate.sh`・`scripts/check-doc-consistency.py`は直接実行してOKを確認し、IPv4 literalは対象6ファイルへ`grep -nE`で手動掃引しゼロ件を確認したが、wrapper自体の end-to-end 実行は未確認 | 未確認事項 |

### What Looks Good

- **P0-1**: `hosts: control_nodes`、`# tester-gate: safe-readonly`。read-only(command/slurp/stat/systemd_service/assert/set_fact/debug/include_tasks)のみで構成され、pause/muteを変更するモジュール呼び出しは無い(`ansible.builtin.command`以外に状態変更系モジュールが無いことをファイル全体で確認)。
- **P0-2**: pause flagのパスを直書きせず、probe自身が読む`/etc/homelab-recovery/recovery-probe.json`を`slurp`→`from_json`→`.monitoring_pause_flag`で取得している(L58-66)。実デプロイの`roles/recovery_probe/templates/recovery-probe.json.j2`(キー名`monitoring_pause_flag`)と`roles/recovery_probe/files/recovery-probe.py:452`(`cfg.get("monitoring_pause_flag", "")`)の現物を突き合わせ、キー名・パス(`/etc/homelab-recovery/recovery-probe.json`は`roles/recovery_probe/tasks/main.yml:97`のdestと一致)とも一致を確認。4箇所目の複製になっていない。
- **P0-3**: `when: recovery_probe_service_enabled | default(false) | bool`(L79)でhost名を直書きしていない。`grep`でも`quory`等のリテラルはコメント1箇所のみ(L4、対象記述であり条件式ではない)で確認。
- **P0-4**: 通知本文にpauseの`since`/`elapsed`(JST、`%z`+コロン挿入)と、probeの`ActiveState`(`systemctl is-active`と同じ生の値)を含む(L118-131)。decoyで実際にPAUSED/probe-inactiveそれぞれのシナリオを実行し、本文の内容を確認した(下記「検証したシナリオ」参照)。
- **P0-5**: ACTIVE + `recovery_probe_service_enabled: false`のdecoyでrc=0、`Include common_slack notify tasks`がskipされることを確認(通知ブロック条件`rmc_paused|bool or rmc_probe_down|bool`が両方false)。
- **P0-6**: `homelab-monitoring-status`が`ACTIVE`/`PAUSED`のいずれの接頭辞にも一致しない出力(`SOMETHING_WEIRD`)を返すdecoyで、`assert`タスクがfailし、rc=2で停止、以降のdebug/notifyタスクへ到達しないことを確認(判定不能を沈黙させていない)。
- **P0-7**: `playbooks/README.md`にカタログ行が追加され、`python3 scripts/check-doc-consistency.py`(check1/2/3)がすべてOK。
- **P0-8**: `docs/ai/policies/autonomous_recovery_policy.md`にAR-103を新設。既存の最終ID(AR-102)の直後で採番衝突なし(`grep -n "AR-10[0-9]"`で確認)。変更履歴表にも追記あり。
- **P1-1/P1-2**: Context(`autonomous-recovery.md`)・Incident(`2026-07-29_...md`)ともに、日次確認の存在と実施済みである旨を追記している。Incident側は「本Incidentでは実施していない」という原文を維持したまま2026-08-01の追記で現状を明示しており、契約が要求する「撤回された前提が残る」欠陥を作っていない(要求どおり、旧記述の削除ではなく追記で対応)。
- **status.md**: 旧Next行「global pauseの解除忘れを構造で防ぐ」を削除し、新Watch行(Semaphore schedule登録待ち、OQ1相当)へ差し替え済み。案件記録パスも正しく`docs/ai/reviews/recovery_pause_daily_check/`を指す。
- **AC5(`--check`)**: decoyで`--check`実行し、`command`タスクは`check_mode: false`で強制実行、`stat`/`slurp`/`systemd_service`/`assert`は`ansible-doc`で`check_mode`属性`support: full`を確認済みのため通常どおり実行され、`common_slack/notify.yml`の`ansible_check_mode`ゲートによりSlack実送信タスクがすべて`skipping`、debugタスクのみ表示されることを確認。
- **AC6**: `ansible-playbook --syntax-check`、`ansible-lint playbooks/recovery_monitoring_check.yml`(新規ファイル自体の指摘はゼロ。既存の`var-naming`/`risky-file-permissions`指摘は`roles/common_slack/tasks/notify.yml`・`capture.yml`由来で本diff対象外・未変更ファイル)、`scripts/check-tester-gate.sh`(OK, 49 playbooks)、`scripts/check-doc-consistency.py`(3 check OK)をすべて確認。
- **JST表記**: `+09:00`の直書きではなく`%z`(実オフセット)+`regex_replace`でコロン区切り整形しており、`docs/ai/status.md`が指摘する「UTCの値にJSTラベルを付ける詐称」パターンではない。decoy実行で`2026-07-29T23:35:47+09:00`のような正しい形式の出力を確認。
- **秘密情報・IP**: 対象6ファイルへ`grep -nE '([0-9]{1,3}\.){3}[0-9]{1,3}'`を実施し該当なし。実ホスト名の直書きも無い(コメント中の説明的言及を除く)。
- **実行境界**: 検証はすべて`/tmp`配下のスクラッチディレクトリ・`ansible_connection=local`のdecoy inventory・モックした`homelab-monitoring-status`で行い、実quory等への接続は発生していない。Slackは`skip_notifications=true`または`--check`のいずれかで常に抑止し、`common_slack/notify.yml`の実送信ブロック(`Load Slack variables`以降)がすべて`skipping`になることを毎回確認した。実webhookの参照・実送信は一度も発生していない。

### 検証したシナリオ(decoy)

1. ACTIVE + `recovery_probe_service_enabled: false` → rc=0、通知なし(AC1相当)。
2. PAUSED(3日超経過) + probe check無効 → rc=0、`alerts`宛の警告文面(`since`/`elapsed`/解除手順)をdebugで確認(AC2相当)。
3. ACTIVE + `recovery_probe_service_enabled: true`(実unit不在→`ActiveState: inactive`) → rc=0、probe-down分の警告文面を確認(AC3相当)。
4. `homelab-monitoring-status`が`ACTIVE`/`PAUSED`いずれにも一致しない出力 → assertでfail、rc=2、通知タスクへ未到達(AC4)。
5. 上記2の状態で`--check`実行 → read-onlyタスクは実行され、Slack実送信タスクはすべてskip、debugのみ表示(AC5)。

### Verdict

**Approve**(blocking findingなし。Suggestionsは3件、いずれも非ブロッキング・追加確認の余地として記載)。

## 追記(2026-08-01, Suggestion #1対応の再照合)

対象: `playbooks/recovery_monitoring_check.yml` のみの差分。requirementに変更なし。

### 変更内容

判定を正規表現前方一致(`rmc_status.stdout is match('^(ACTIVE|PAUSED)')`)から、`trim | split(' ') | first`で先頭の語を取り出し`in ['ACTIVE', 'PAUSED']`で厳密突き合わせる形へ変更(L41-50)。

### 再照合結果

- **Suggestion #1は解消**。decoyで`ACTIVE_but_stale`・`PAUSEDXYZ`をそれぞれ`homelab-monitoring-status`の出力として与え、いずれも`assert`がfail・rc=2で停止し、以降のdebug/notifyへ到達しないことを確認した(以前の実装ではこれらは前方一致で健全誤判定していたはずの入力)。
- **新しい穴(正常出力が異常判定される回帰)は無い**。実際の2つの正常出力を再検証した。
  - `ACTIVE` → `split(' ') | first` = `ACTIVE` → assert通過、rc=0、通知なし(回帰なし)。
  - `PAUSED (all targets)` → `split(' ') | first` = `PAUSED`(カッコ以降は`first`で捨てられる) → assert通過、`rmc_paused=true`、通知本文の`since`/`elapsed`/`解除`もJST表記で正しく生成されることを確認(回帰なし)。
  - 空文字列出力 → `''`と評価され、以前同様に判定不能としてrc=2で停止(回帰なし、むしろ以前の前方一致でも同じくfailしていた入力と同じ挙動を維持)。
- **`\b`(バックスペース解釈)問題の残存は無い**。現在のファイルに`\b`という文字列は存在しない(`grep -n '\\b'`で0件)。今回の実装は正規表現ではなく文字列の`split`/`first`/`in`という別の機構であり、Jinjaの文字列リテラルにエスケープシーケンスを埋め込む構造そのものが無いため、この種の問題が入り込む余地が無い。
- **前回Approveの根拠のうち崩れるものは無い**。P0-6/AC4(判定不能時の非ゼロ停止)は本変更によりむしろ強化された。P0-4/AC2/AC3(通知本文の内容)、AC5(`--check`時の抑止)は同一ロジックのままであり、decoy再実行で内容・抑止とも従来どおりであることを確認済み。
- 機械チェック再実行: `ansible-playbook --syntax-check`、`ansible-lint playbooks/recovery_monitoring_check.yml`(本ファイル起因の指摘なし)、`scripts/check-tester-gate.sh`(OK, 49 playbooks)、`scripts/check-doc-consistency.py`(3 check OK)、IPv4 literal・`\b`の`grep`掃引(いずれも0件)をすべて再確認し、全てpass。

### 検証したdecoyシナリオ(追加分)

1. `ACTIVE` → rc=0、通知なし。
2. `PAUSED (all targets)` → rc=0、`rmc_paused=true`、通知本文の`since`/`elapsed`がJST表記で正しく生成。
3. `ACTIVE_but_stale`(前回のSuggestion #1が指摘した反例) → assert fail、rc=2。
4. `PAUSEDXYZ`(同型の反例) → assert fail、rc=2。
5. 空文字列 → assert fail、rc=2。
6. `PAUSED (all targets)` + `--check` → 通知はdebug表示のみ、Slack実送信タスクは全て`skipping`。

いずれも実host無接続・本番Slack無送信(`/tmp`配下のスクラッチ + `ansible_connection=local` + `skip_notifications=true`または`--check`)で実施。

### 追記時点のVerdict

**Approve(変更なし)**。Suggestion #1はクローズ。Suggestion #2・#3(前回記載)は本追記の対象外であり、状態も変わらず未確認事項として残る。
