# proxmox_patch_dryrun 追加実装内容（apt-get update 追加）

実装日: 2026-05-09
対応要求仕様: 2026-05-09_009_requirement.md

---

## 対応概要

apt-get update を dry-run フローに追加した。

---

## 変更内容

### 1. docs/ai/prompts/proxmox_patch_policy.md

**安全度:**

```
semi-safe
apt-get update あり（パッケージリストの更新のみ、パッケージ本体は変更しない）
実パッチ適用なし
```

**処理概要:**

step 2 に `apt-get update` を追加し、以降を 1 つずらした（step 2〜20）。

### 2. roles/proxmox_patch_dryrun/files/proxmox-dryrun-collect.sh

`apt-get check` の前に以下を追加した。

```bash
# apt-get update (refresh package lists only, no package installation)
apt-get update -qq 2>&1
```

---

## 検証結果

- `bash -n roles/proxmox_patch_dryrun/files/proxmox-dryrun-collect.sh`: OK

---

Next step files:
- docs/ai/reviews/proxmox_patch_dryrun/2026-05-09_009_requirement.md
- docs/ai/reviews/proxmox_patch_dryrun/2026-05-09_010_implement.md
