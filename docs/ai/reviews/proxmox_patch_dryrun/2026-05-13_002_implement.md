# 実装内容: dry-run メール reboot_expected 明示

## 実装概要

`scripts/codex-classify.sh` を修正し、Codex CLI へのプロンプト・出力スキーマ・Python 検証部分に `reboot_expected` を追加した。

## 作成・更新したファイル一覧

| ファイル | 種別 |
|---|---|
| `scripts/codex-classify.sh` | 更新 |
| `docs/ai/reviews/proxmox_patch_dryrun/2026-05-11_001_requirement.md` | 新規 |
| `docs/ai/reviews/proxmox_patch_dryrun/2026-05-11_002_implement.md` | 新規 |

## 変更内容の要約

### 1. プロンプトへの判定指示追加（指示番号 4 として挿入）

Codex CLI への指示に以下を追加した。

```
4. 以下の条件で reboot_expected を判定する：
   - updates にパッケージ名に "kernel" を含むものがある → true
   - changelog を読んで reboot が必要と判断した → true
   - それ以外 → false
   reboot_expected が true の場合、mail_body に
   「⚠️ kernel 系パッケージが含まれるため、apply 後に reboot が必要になる可能性があります。」
   を含めること。
```

既存の指示番号 4〜6 は 5〜7 にシフト。

### 2. 出力スキーマへの reboot_expected 追加

`status_inputs` スキーマに以下の2フィールドを追加した。

```json
"reboot_expected": false,
"reboot_expected_reason": "<reboot が必要と判断した理由。不要な場合は空文字>"
```

### 3. Python 検証部分の対応

`reboot_expected` / `reboot_expected_reason` はオプション扱いとし、後方互換性を保った。
存在する場合のみ型チェックを行う。

```python
if 'reboot_expected' in status_inputs and not isinstance(status_inputs['reboot_expected'], bool):
    print('Error: status_inputs.reboot_expected must be a boolean', file=sys.stderr)
    sys.exit(1)

if 'reboot_expected_reason' in status_inputs and not isinstance(status_inputs['reboot_expected_reason'], str):
    print('Error: status_inputs.reboot_expected_reason must be a string', file=sys.stderr)
    sys.exit(1)
```

## 実行した確認コマンド

```bash
# 構文チェック
bash -n scripts/codex-classify.sh
```

実行結果: エラーなし。

## 未対応事項

- Ansible tasks 側での `reboot_expected` の利用（要求仕様の初回除外スコープ）
- apply 後の実際の reboot 判定改善（PENDING 1b）

## 注意点

- `reboot_expected` はオプションフィールドのため、古い形式の Codex 出力でも検証が通る
- `reboot_expected_reason` が空文字の場合も有効とする（false の場合に空文字を期待）
- Ansible tasks 側での利用は今回対象外。今後 `proxmox_patch_dryrun` role 側で `reboot_expected` を参照する場合は別の require として追加する

---

Next step files:
- docs/ai/reviews/proxmox_patch_dryrun/2026-05-11_001_requirement.md
- docs/ai/reviews/proxmox_patch_dryrun/2026-05-11_002_implement.md
