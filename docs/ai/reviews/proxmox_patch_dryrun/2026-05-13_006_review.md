# Review: reboot_expected Python側強制補正の追加

## 対象

- `scripts/codex-classify.sh`

## 参照した前提

- `docs/ai/prompts/core.md`
- `docs/ai/reviews/proxmox_patch_dryrun/2026-05-13_004_requirement.md`
- `docs/ai/reviews/proxmox_patch_dryrun/2026-05-13_005_implement.md`
- `docs/ai/reviews/proxmox_patch_dryrun/2026-05-13_003_review.md`
- `docs/ai/prompts/proxmox_patch_policy.md`

## Findings

なし。

## 確認内容

### 1. 要求仕様との整合

要求仕様では、`scripts/codex-classify.sh` の Python ブロック内で、`output_json` 書き込み直前に以下の補正を行うことになっていました。

- `input_data` の `updates` リストを走査する
- `name` に `"kernel"` を含むパッケージを抽出する
- 該当パッケージがある場合、`status_inputs.reboot_expected` を `true` に強制上書きする
- `status_inputs.reboot_expected_reason` が空または未設定の場合のみ、`kernel packages included: ...` を設定する
- 既存の reason がある場合は残す

実装では、`json.dump(data, ...)` の直前に以下の補正が追加されています。

```python
kernel_pkgs = [u.get('name', '') for u in updates if 'kernel' in u.get('name', '')]
if kernel_pkgs:
    status_inputs['reboot_expected'] = True
    if not status_inputs.get('reboot_expected_reason'):
        status_inputs['reboot_expected_reason'] = 'kernel packages included: ' + ', '.join(kernel_pkgs)
```

`status_inputs` は `data['status_inputs']` への参照なので、ここでの補正は保存される JSON に反映されます。

### 2. 補正位置

補正は Python ブロック内の検証処理後、`output_json` への書き込み直前に配置されています。

要求仕様の「補正は output_json への書き込み直前に行う」と一致しています。

### 3. 既存 reason の保持

`if not status_inputs.get('reboot_expected_reason'):` の条件により、Codex がすでに非空の `reboot_expected_reason` を設定している場合は上書きされません。

未設定または空文字の場合のみ、`kernel packages included: <パッケージ名リスト>` が設定されます。

### 4. スコープ

今回要件の新規追加箇所は Python ブロック内に収まっています。

なお、現在の `git diff` 全体には前回レビュー済みの `reboot_expected` プロンプト・スキーマ・型検証追加も含まれています。これは未コミット差分が累積しているためで、今回の Python 側強制補正そのものは要求スコープ内です。

### 5. 実行確認

実行した確認:

```bash
bash -n scripts/codex-classify.sh
```

結果: エラーなし。

## 残リスク・注意点

`kernel` 判定は要求仕様どおり部分一致です。Proxmox / Debian 系の実パッケージ名は通常小文字なので実用上は問題ありません。

`updates` の各要素が dict であることまではこの追加コードでは検証していませんが、既存の dry-run merge 出力では `updates` はパッケージ情報 dict のリストであり、今回の変更で新たに前提が増えたわけではありません。

## 判定

この差分は要求仕様を満たしており、現時点で修正必須の指摘はありません。
