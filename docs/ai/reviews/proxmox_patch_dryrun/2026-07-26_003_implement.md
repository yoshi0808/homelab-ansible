# Proxmox Patch Dry-run 単一ノード対応 — 実装記録(P0)

- 日付: 2026-07-26(JST)
- Role: Implementer
- 対応requirement: `docs/ai/reviews/proxmox_patch_dryrun/2026-07-26_002_requirement.md`(§5 P0)
- 対応調査: `docs/ai/reviews/proxmox_patch_dryrun/2026-07-26_001_single_node_dryrun_investigation.md`
- 対応ADR: `docs/ai/adr/002-proxmox-patch-dryrun-single-node.md`
- 状態: P0の4ファイル改修完了。local-only検証済み。実ホスト実行、commit、pushは未実施。**AC4(両node到達不能時の明示fail)は設計変更を要する既知の制約が残る(下記§3参照、Coordinator/Tech Leadへの確認事項)**。

## 1. 対応内容(4ファイル)

### 1-1. `roles/proxmox_patch_dryrun/tasks/main.yml`

- Phase 2冒頭に`_expected_nodes`(`ansible_play_hosts_all`)/`_reached_nodes`(`ansible_play_hosts`)を`set_fact`する task を追加。以降のPhase 2-6全体でこの2変数を単一の正本として使う。
- `hostvars['pve1']`/`hostvars['pve2']`固定参照3箇所をすべて置換した。
  - merge入力生成(`Save node dryrun data to temp file for merging`): `[hostvars['pve1'].node_dryrun, hostvars['pve2'].node_dryrun]` → `_reached_nodes | map('extract', hostvars, 'node_dryrun') | list`。
  - pre-status判定(`Determine pre-Codex cluster status`): pve1/pve2決め打ちの6項目列挙 → `unified_dryrun.node_summaries.values()`をnamespace付き`{% for %}`でループする形へ変更(`proxmox-dryrun-merge.py`側が既にnode数非依存でkeyingしているため、Ansible側だけの修正で足りる)。
  - 最終レポート生成(`Build final report JSON`)の`reboot_required_pve1`/`_pve2`: `hostvars['pve1'].node_dryrun.reboot_required`から`unified_dryrun.node_summaries['pve1'].reboot_required if 'pve1' in unified_dryrun.node_summaries else none`へ。**キー名(`reboot_required_pve1`/`_pve2`)自体はrequirement.md P1スコープのため維持し、値の算出だけをnode非依存にした**(指示どおり)。該当nodeが今回未参加なら`null`を返し、`false`(reboot不要)と区別する。
- 到達node 0件のガードtaskを追加(`Fail if no proxmox node reached the merge phase`、`when: _reached_nodes | length == 0`)。詳細な制約は§3参照。
- 通知文面(NO_UPDATES件名・本文、BLOCKED本文)の`groups['proxmox']`を`_reached_nodes`へ置換。
- `_unreachable_nodes`(`_expected_nodes - _reached_nodes`)/`_limit_excluded_nodes`(`groups['proxmox'] - _expected_nodes`)を計算し、`_node_reachability_note`(AC1/AC3で異なる文言)を生成するtaskを追加。全status分岐(Codex分類結果/NO_UPDATES/BLOCKED)の後段で`_notify_body`へ無条件に追記する`when: _node_reachability_note | length > 0`taskを1つ追加し、分岐ごとの重複実装を避けた。両node健全時は`_unreachable_nodes`/`_limit_excluded_nodes`が両方空になり`_node_reachability_note`は空文字列になるため、この追記taskはno-opでAC2の完全同一出力を壊さない。
- NO_UPDATESのMDレポート内`pve1/pve2 ともに更新候補なし。`は、`_node_reachability_note`が空(=fixed pair成功時)のときだけこの原文を出力し、そうでない場合は`_reached_nodes`ベースの動的文へ切り替える条件分岐にした(AC2の文字列完全一致を保つため)。BLOCKEDのMDレポートは元々`unified_dryrun.node_summaries.items()`でループしており既にnode数非依存だったため無修正。

### 1-2. `playbooks/proxmox_patch_dryrun.yml`

- `any_errors_fatal: true`を削除した(ADR-002 decision a-1)。
- コメントで削除理由と、Phase 2冒頭のガードtaskの存在、および§3の既知の制約を記録した。
- `hosts: proxmox`は変更していない(§3参照、`hosts: proxmox:localhost`への変更を試みたが規模外の副作用が判明したため revert 済み)。

### 1-3. `docs/ai/policies/proxmox_patch_policy.md`

- SB-023(`dry-runはpve1 / pve2固定pairを対象とし、単一node実行を許可しない`)を廃止し、新規SB-094へ置換した。番号は既存最大値SB-093の次番(廃止番号SB-049/083/084/086は再利用しない既存慣行に倣い、SB-023も再利用しない)。
- SB-094の内容: 到達・healthcheck OKなnodeが1node以上あれば開始する条件文、両node到達可能時は従来どおり両node healthcheck OK必須、片方が通信断/`--limit`対象外なら単一node dry-runとして開始し通知で理由を明示、両node到達不能なら開始せず明確なエラーで停止(空report・サイレント成功にしない)、package本体は変更しない旨(既存文言維持)、単一node dry-runの`PATCH_READY`はapply側(SB-027、SB-028、SB-032)のfixed pair gate条件を満たさない旨、をすべて1項目にまとめた。
- SB-027/SB-028/SB-032本文は無変更(apply側のdrift回避要求はそのまま)。
- §8変更履歴に2026-07-26行を追加(廃止: SB-023、新設: SB-094、apply側不変の旨を明記)。

## 2. requirement.md上のP1項目をP0実装に含めた点(スコープ観察)

Coordinatorからの直接指示(本caseの「実装スコープ(P0のみ)」項目3)は、ADR-002 decision d-2に基づく「意図しない通信断」/「`--limit`による意図的単一node実行」の通知文言区別(AC1とAC3で異なる文言)を明示的に含んでいた。requirement.md §5では同項目は形式上P1(Should)に分類されているが、直接指示が技術詳細まで踏み込んで明記していたため、これを額面通りP0の一部として実装した。ADR-002のDecision/Consequences節もこの区別をP0/P1で区切らず一体として記述しており、内容面での矛盾はない。念のため本観察をここに記録する。

## 3. AC4(両node到達不能時の明示fail)の既知の制約 — Coordinator/Tech Leadへの確認事項

### 3-1. 経緯

初回実装では`run_once: true` + `delegate_to: localhost`の単純なガードtaskを追加したが、decoy inventory(127.0.0.1閉ポート/`ansible_connection: local`、実host名・実IPなし)による実地検証で、**同一play内の全hostが失敗/到達不能になった時点でAnsibleのlinear strategyがそのplayの残りtaskを一切スケジュールしなくなり、delegate先がlocalhostであってもこのガードtask自体が発火しないdead codeになる**ことが判明した(`ansible.builtin.ping`/`ansible.builtin.fail`のいずれで全host失敗させても同じ、`meta: clear_host_errors`を挟んでも同じ)。一方、pve1/pve2の片方だけが失敗する場合(AC1/AC3の実際の想定シナリオ)はこの問題が起きず、ガードtaskも通知文言区別も正しく動作することを確認済み(§4参照)。

Coordinatorの指示を受け、この制約を解消する設計を2案検討・実地検証したが、いずれも本caseの4ファイル限定scopeでは実装できないことが判明したため、単純なガードtask設計(dead codeの制約を残す)へ戻した。

### 3-2. 検討・却下した案

**案A: `hosts: proxmox:localhost`(単一play内にlocalhostを追加)**

localhostを常に生存するplay memberとして加えれば、pve1/pve2が両方失敗してもrun_onceタスクを引き継ぐhostが残るはずだった。両node健全時はrun_onceが引き続き`groups['proxmox'][0]`(通常pve1)を選ぶことも確認済み。しかし実地検証の結果、**`roles/proxmox_healthcheck`(本caseのscope外)の`Build Semaphore health summary` task(`run_once`、delegate_toなし、`{% for host in ansible_play_hosts %}`で`hostvars[host].proxmox_report`を参照)が、localhostがhealthcheck role自体をskip(`when`で除外)しているだけで`ansible_play_hosts`には残り続けるため、`hostvars['localhost'].proxmox_report`未定義でJinja renderingが失敗する**ことが判明した。これは異常系だけでなく**両node健全時の通常運行でも毎回発生する**致命的な回帰であり、`roles/proxmox_healthcheck`はscope外かつ他playbook(`proxmox_healthcheck.yml`、`proxmox_patch_weekly_full.yml`)共有roleのため修正できない。案Aは却下。

**案B: play分離(collect playとmerge playを分ける)**

`hosts: proxmox`のままplay 1(healthcheck + Phase 0/1)を無変更で残し、play 2(Phase 2-6)を独立したplayとして追加する案。play 2で`gather_facts: false` + 全task `delegate_to: localhost`にすれば、pve1/pve2へ実際に接続する task が皆無になるため、play 2単体では両nodeとも「失敗していない」扱いのまま run_once が機能する、という仮説を実地検証した。

しかし検証の結果、**Ansibleは host の失敗/到達不能状態を playbook 実行全体を通じて(play境界をまたいで)保持しており**、play 1で両node が失敗すると、同じ `hosts: proxmox` を対象とする play 2 は(play 2自身が一度もpve1/pve2へ接続を試みていなくても)**開始すらされない**(`PLAY [...]`のbannerすら出力されない)ことを確認した。`meta: clear_host_errors`をplay 2の最初のtaskに置いても同じ結果だった。

play 2の`hosts:`を`localhost`単体にすれば play 1 の失敗と無関係に必ず開始されるが、その場合 `_final_status` 等の `run_once` factの格納先が `hostvars[groups['proxmox'][0]]`(通常pve1)から`hostvars['localhost']`へ変わる。`playbooks/proxmox_patch_weekly_full.yml:130-132`のコメントが明記するとおり、同playbookの`_status`変数は`hostvars[groups['proxmox'][0]]['_final_status']`を直接参照しており、この変更は同ファイル(scope外、4ファイル限定の対象外)の修正なしに壊れる。案Bも却下。

### 3-3. 現在の挙動(採用した設計)

- pve1/pve2の**片方**が失敗/到達不能な場合(AC1)、または`--limit`で片方のみを対象にした場合(AC3): ガードtaskは正しく機能し(`_reached_nodes | length == 0`は偽)、生き残った側のnodeだけでdry-runが完走し、通知文言も正しく区別される。**これが本feature本来の主目的(夏季pve1平日シャットダウン運用時の日次dry-run欠測解消)であり、正しく動作する。**
- pve1/pve2が**両方**失敗/到達不能な場合(AC4): 本ガードtask自体は発火しない(dead code)。しかし、Ansible自身が両hostを`UNREACHABLE`または`FAILED`として明示的に報告し(host別のfatalメッセージ、`PLAY RECAP`)、playbook全体が非ゼロ終了する(decoy検証実測: UNREACHABLEはrc=4、FAILEDはrc=2)。merge処理へは進まず、reportファイルも生成されない。AC4が求める「空reportやサイレント成功にしない」という安全性の実質は、本ガードtask自身のメッセージではなく、この**Ansible標準の失敗報告**によって満たされる。

### 3-4. 確認事項

上記のとおり、AC4を**独自メッセージ付きで**確実に発火させるには、本case外(`roles/proxmox_healthcheck`または`playbooks/proxmox_patch_weekly_full.yml`)への波及を伴う設計変更が必要である。以下いずれかの判断をCoordinator/Tech Leadに仰ぎたい。

1. 現状(Ansible標準の失敗報告でAC4の安全性を担保し、本ガードtaskは片方失敗ケースのみ機能する)を受け入れる。
2. `playbooks/proxmox_patch_weekly_full.yml`の`_status`変数定義(1行、`hostvars[groups['proxmox'][0]]['_final_status'] | default('UNKNOWN')`を`hostvars['localhost']`も見るfallback付きに変更する程度の小さい修正)への影響許容を明示したうえで、scopeを5ファイルへ拡張し案Bを実装する。

## 4. 検証(local-only)

以下をlocal-onlyで確認した。実ホスト、Slack通知、実際の`--limit`起動、実際のUNREACHABLE検知は未実施(Testerの担当)。

- `ansible-playbook playbooks/proxmox_patch_dryrun.yml --syntax-check`: PASS
- `ansible-playbook playbooks/proxmox_patch_dryrun.yml --list-hosts`: `hosts: proxmox`のまま(pve1、pve2の2host)であることを確認
- `ansible-playbook playbooks/proxmox_patch_weekly_full.yml --syntax-check`: PASS(本playbookを`import_playbook`する側の構文にも影響がないことを確認)
- Ansible標準Templar(`ansible.template.Templar`)による`select('in', ...)`/`difference()`のfilter order-preservation単体検証: PASS(AC2回帰確認に必要な文字列完全一致の前提)
- decoy inventory(`127.0.0.1`閉ポートまたは`ansible_connection: local`、実host名・実IPなし)による末端シナリオのend-to-end検証、4パターンすべてPASS:
  - fixed pair両node健全(AC2相当): `expected=reached=[node1, node2]`、`_node_reachability_note`空文字列
  - 片方node失敗(AC1相当): `reached`が生存側のみ、`_unreachable_nodes`に失敗側、通知文言は「意図しない通信断」
  - `--limit`で片方明示指定(AC3相当): `_limit_excluded_nodes`に非対象側、通知文言は「`--limit`による意図的単一node実行」
  - 両node失敗(AC4相当): 本ガードtaskは発火せず、Ansible標準のUNREACHABLE/FAILED報告とplaybook全体の非ゼロ終了で停止することを確認(§3参照)
- `roles/proxmox_healthcheck`の`Build Semaphore health summary`相当taskを模したdecoyテストで、案A(`hosts: proxmox:localhost`)が通常運行時から壊れることを確認(§3-2)
- decoy inventoryはすべて`/tmp/claude-1000/.../scratchpad/`配下で作成・検証後に削除済み。実ホストへのansibleコマンド実行、commit、pushは行っていない。

## 5. 未検証事項・残存リスク

- **§3のAC4既知の制約**(最重要、上記参照)。
- 実Proxmox host(pve1、pve2)上での`proxmox-healthcheck.sh`/`proxmox-dryrun-collect.sh`の実際の出力・挙動は本実装で変更していないため未検証(元々の役割分担どおりTesterが担当)。
- 実際のSlack通知配信(`common_slack`との統合)は本実装で構造を変えていないため大きな懸念はないが、`_node_reachability_note`追記後の本文がSlack上で読みやすいかは実地確認が望ましい。
- requirement.md §7オープンクエスチョンの「`reboot_required_pve1`/`_pve2`固定キーのrepo外消費者有無」は本実装のスコープ外(P1)で未解決のまま。今回は値の算出だけをnode非依存化し、キー自体・schemaは変更していないため、この点に関する追加リスクは発生させていない。
- `--limit pve2`指定時に`pve2`自体も同時に失敗する複合ケース(オペレータが明示的に単一node指定し、かつその node が同時に不調になるケース)は、`_expected_nodes`が`--limit`適用後の時点で既に`['pve2']`のみとなるため、そのケースでも本ガードtaskは(pve2 1台のみのplayとして)発火せず、§3-2で述べた「全host失敗でplay全体が停止しdead codeになる」制約が同様に当てはまる。AC3の定義自体が「`--limit pve2`実行時にpve2が正常に完走する」ことを前提としており、この複合ケースはrequirement.mdのいずれのACにも該当しないため新たな要件逸脱ではないが、念のため残存リスクとして記録する。

## Coordinator判断(2026-07-26)

§3-4の確認事項について、**選択肢1(現状維持)を採用**する。理由: AC4は稀なケース(pve1/pve2同時ダウン)であり、Ansible標準のUNREACHABLE/FAILED報告+非ゼロ終了で「サイレント成功にしない」という安全性の実質は満たされている。案2(weekly_full.ymlへのscope拡張)は、より本番影響の大きい実パッチ適用playbookへ波及するリスクに対しカスタムメッセージという限定的な利益しか得られず、requirement.md §3の非ゴール(apply/weekly full側は対象外)の趣旨とも整合しない。カスタムメッセージが必要になった場合は、weekly_full.ymlを明示的にscopeへ含めた別案件として改めて起案する。

「並行セッションかもしれない」という観察について: これはCoordinator自身が本セッション前半で行った意図的な編集(Codex撤退に伴うdocs/ai配下の改訂、2026-07-26)であり、並行セッションではない。

## 7. Reviewレビュー対応(2026-07-26、`2026-07-26_004_review.md`)

独立Reviewerによるレビューでblocking findingはなかったが、Major finding 2件についてYoshinobu/Coordinatorと相談のうえ、以下3件の修正指示を受けて対応した。

### 7-1. 修正1: 到達性ノートの原因誤表示(Critical Issues #1、対応: 文言の是正)

`_node_reachability_note`(`roles/proxmox_patch_dryrun/tasks/main.yml`)が、真のUNREACHABLEとhealthcheck task失敗(SB-022の定める実障害)を区別せず、後者も一律「意図しない通信断」と誤表示していた。Reviewer提案のうち**最小修正案(原因を断定しない文言への変更)を採用**した。

- `{{node}} は今回到達できませんでした(意図しない通信断)。` → `{{node}} は今回の結果に含まれていません(到達不能または健全性チェック失敗)。`へ変更。
- 同ファイル内の関連コメント(Phase 2冒頭の`_expected_nodes`/`_reached_nodes`説明、`Append node reachability note`taskの説明)も、原因を断定していた英語コメント("unintended communication failure"、"unintended UNREACHABLE")を、両方の原因がありうる旨へ修正した。
- `ansible_unreachable_hosts`/`ansible_failed_hosts`による完全な原因分離(Reviewer提案のもう一つの案)は、Coordinatorの判断により複雑化回避のため見送った。
- `docs/ai/policies/proxmox_patch_policy.md`のSB-094へ、単一node dry-runへ入る経路として「通信断」「`--limit`」に加え「healthcheck失敗による除外」を3つ目の経路として明記し、通知文言がこの2つ(通信断/healthcheck失敗)を断定的に区別しない旨も追記した(Suggestions #2対応)。

### 7-2. 修正2: weekly_full.ymlのapply安全ゲート非対称性(Critical Issues #2、scope 4→5ファイルへ拡張、Yoshinobu承認済み)

`playbooks/proxmox_patch_weekly_full.yml`のStep 1b(両node healthcheck OK gate)通過直後・Step 2b実行前の競合windowでpve2側だけが脱落すると、pve1単独のdry-run結果がそのままfixed-pair PATCH_READYとして`hostvars[groups['proxmox'][0]]['_final_status']`経由で読まれてしまう非対称な保護漏れが、Reviewerのdecoy実証で確認されていた。これは今回の変更(`any_errors_fatal`除去)がdry-run側で新たに許容した「片方node脱落」状態が、apply側gateの暗黙の前提(fact帰属先=`run_once`の副次的挙動)を突き破ってしまったために生じたギャップであり、ADR-002の「apply側の両node要求は変更しない」という前提を実装レベルで裏切っていた。

Reviewer提案(Suggestions #3)どおり、Step 2bのgate task 1つに絞った最小修正で対応した。

- `_dryrun_missing_nodes`変数(`vars:`1行追加)を新設し、`hostvars[groups['proxmox'][0]]['_final_report']['node_summaries']`のkey集合と`groups['proxmox']`の差分を取る。
- 既存の`Fail if dry-run status is a genuine error or unexpected`taskの`when`条件へ`or _dryrun_missing_nodes | length > 0`を追加し、Statusが`PATCH_READY`でも両node分のnode_summariesが揃っていなければgateをfailさせる(安全側)。
- メッセージも「missing node summaries」を明示する形へ拡張した。
- 他のtask・ロジックには触れていない(1 task、vars 1行+when拡張+message拡張の最小差分)。

decoy inventory(`ansible_connection: local`、実host名・実IPなし)で以下3パターンを検証しPASS:

1. 通常のfixed-pair PATCH_READY(両node分のnode_summaries揃い)→ gate通過(`GATE_BLOCKS=False`、回帰なし)。
2. Reviewerが実証した非対称ケース(Statusは`PATCH_READY`だが`node_summaries`に1node分しかない)→ gateが正しくblock(`GATE_BLOCKS=True`)することを確認、Critical Issues #2が解消された。
3. 既存の安全側ケース(`_final_status`未定義→`UNKNOWN`)→ 従来どおりgateがblockすることを確認(無回帰)。

`ansible-playbook playbooks/proxmox_patch_weekly_full.yml --syntax-check`もPASS。

### 7-3. 修正3: ADRのtraceabilityギャップ(Suggestions #4、任意対応・実施済み)

`docs/ai/adr/002-proxmox-patch-dryrun-single-node.md`のDecision (b)節に、「P0は値のみnode非依存化する最小修正(b-1の機構+`none`明示でb-1の懸念を解消)にとどめ、完全な動的schema化(b-2の原意)はP1へ再委任」という趣旨の一文を追記し、ADR本文と実装(`main.yml`の`reboot_required_pve1`/`_pve2`固定キー維持)との不一致を解消した。

### 7-4. 対応不要とされた項目

- Suggestions #5(Codex分類結果の通知件名にも単一node実行を反映、minor)は今回見送り。
- AC4はCoordinator判断済み(選択肢1)のため本ラウンドでは再検討していない。

### 7-5. 検証(local-only、追加分)

- decoy inventoryはすべて`/tmp/claude-1000/.../scratchpad/`配下で作成・検証後に削除済み。
- `ansible-playbook playbooks/proxmox_patch_dryrun.yml --syntax-check`: PASS(再実行)。
- `ansible-playbook playbooks/proxmox_patch_weekly_full.yml --syntax-check`: PASS。
- 実ホストへのansibleコマンド実行、commit、pushは行っていない。

### 7-6. 更新後の残存リスク

- §5の「AC4既知の制約」は変更なし(Coordinator判断どおり選択肢1のまま)。
- Critical Issues #1・#2はいずれも本ラウンドで解消。ただし#1の文言は「原因を断定しない」ため、healthcheck失敗による除外がSB-022レベルの実障害(ZFS縮退、quorum喪失等)であっても、通知本文だけでは重大度が伝わらない。人間側は`_reached_nodes`に含まれないnodeについて、必要に応じてhealthcheck結果を個別に確認する運用が引き続き必要(Slack通知本文はあくまで「対象から外れたnode名」を知らせるものであり、原因の切り分けは行わない設計とした)。
- weekly_full.ymlの修正はStep 2bの1 task内に閉じており、Step 1a/1b(healthcheck)、Step 2a(dry-run本体)、Step 2c以降(evacuate/apply/restore)のロジックには触れていない。

## 6. 次の工程

- 独立Reviewerによる4ファイル逐行照合は完了済み(`2026-07-26_004_review.md`、Verdict: Request Changes → 本ラウンドの修正で対応)。
- 本ラウンドの修正(§7)についてReviewerの再確認、またはCoordinator/Tech Leadによる最終判断。
- 判断確定後、Tester検証(実ホスト)へ。

## 8. 実ホストTesterによるblockingバグ発見と修正(2026-07-26、`2026-07-26_005_test_result.md`)

独立Testerによる実ホスト(pve1、pve2、ともに健全)実行で、§7までの修正では検出できていなかった新規のblockingバグが発見された。§7の対応後にReviewerが再検証していなかった箇所であり、Reviewer/Implementerいずれのdecoy検証も「値をprintして目視確認する」にとどまり、実際に`| length`フィルタへ通す後続taskまで実行していなかったため見逃されていた。

### 8-1. バグの内容

`_node_reachability_note`(`roles/proxmox_patch_dryrun/tasks/main.yml`の`Build node reachability note text`task)は、`_unreachable_nodes`・`_limit_excluded_nodes`が両方空(=fixed-pairで両ノードとも正常参加、AC2が想定する通常時であり、日次実行で毎回通る経路)の場合、`{%- else -%}{%- endif -%}`という「静的文字列を一切含まないJinja制御構造のみ」のテンプレートになる。Ansibleの Templar はこの場合、レンダリング結果を空文字列`""`ではなく**Python の `None`** として`set_fact`する(Ansibleの既知の仕様: テンプレート全体がJinjaマークアップのみで構成され結果が空文字列になる場合の`None`変換)。この`_node_reachability_note`(値`None`)に対し`| length`フィルタを適用していた3箇所すべてで`object of type 'NoneType' has no len()`により例外終了していた。Tester実測での実際のクラッシュ地点は`Save minimal MD report for NO_UPDATES or BLOCKED`task(`content:`内の`{% if _node_reachability_note | length == 0 %}`)。

これは**両ノードが健全な最も普通の日次実行(AC2が回帰なしを保証すべき経路)で、Statusに関わらず(NO_UPDATES / BLOCKED / NEEDS_CLASSIFICATIONいずれでも)毎回発生する**、本番影響の大きいbugだった。

### 8-2. 修正内容

Coordinator指示に従い、原因側の是正と使用側の防御の両方を実施した。

1. **原因側**: `Build node reachability note text`task直後に、`_node_reachability_note`を`| default('', true)`で明示的に空文字列へ再coerceする新task(`Coerce None (from an empty pure-Jinja template) back to a real empty string`)を追加した。`default(default_value, boolean=true)`の第2引数`true`は「`None`もdefault適用対象に含める」ためのAnsible/Jinja既定filterの仕様であり必須。
2. **使用側(防御)**: `| length`を適用する3箇所すべてを`| default('', true) | length`へ統一した。
   - `roles/proxmox_patch_dryrun/tasks/main.yml`の`Save minimal MD report for NO_UPDATES or BLOCKED`(content内の`{% if %}`)
   - 同ファイルの`Set notification content for NO_UPDATES`(`_notify_body`内の`{% if %}`)
   - 同ファイルの`Append node reachability note to notification body`の`when`条件

これにより、たとえ将来同種の原因(別のテンプレートがNoneを生む等)で`_node_reachability_note`が再びNoneになったとしても、使用側で単独にクラッシュを防げる二重の防御になっている。

### 8-3. 検証(local-only、今回は`| length`フィルタまで実行して確認)

Coordinatorの指示どおり、今回は値を目視するだけでなく、実際に`| length`を適用する後続処理まで含めて動かして確認した。

1. **単体レベル**: decoy(`hosts: localhost`限定、実ホスト非接続)で、修正前のtemplateがelseブランチで`type_debug=NoneType`・`is none=True`になることを再現し、修正後(`| default('', true)`のcoerce task追加)で`type_debug=str`・`is none=False`になることを確認した。さらに、`_node_reachability_note | length > 0`という実際の`| length`呼び出しを、if/elif/else全3分岐それぞれで実行し、修正前は空分岐でのみクラッシュ、修正後は全分岐でクラッシュせず正しい真偽値を返すことを確認した。
2. **統合レベル(今回追加した検証)**: `roles/proxmox_patch_dryrun/tasks/main.yml`のPhase 2以降(Phase 0/1の実Proxmoxツール依存部分を除く、行40以降を`sed`で無編集のまま抽出)と実物の`proxmox-dryrun-merge.py`(無変更)をdecoy role配下に配置し、`common_slack/tasks/notify.yml`はdebugスタブに置換したうえで、`hosts: proxmox`相当のdecoy inventory(`ansible_connection: local`、実host名・実IPなし)を使い、`ansible-playbook`を**最後まで実行**して以下3パターンを確認した。いずれも`failed=0`で完走し、Slack本文(スタブのdebug出力)の内容も期待どおりだった。
   - fixed-pair両ノード参加・NO_UPDATES(§8-1の実クラッシュと同一条件): 完走、通知本文は`pve1/pve2 ともに更新候補がありません。パッチ適用は不要です。`のみ(追記なし、AC2どおり)。
   - 片方ノードが実際にfailするAC1相当: 完走、通知本文に`decoy_pve1 は今回の結果に含まれていません(到達不能または健全性チェック失敗)。decoy_pve2 のみの結果です。`が追記される。
   - 実際の`--limit decoy_pve2`フラグを使ったAC3相当: 完走、通知本文に`--limit により decoy_pve1 を明示的に除外した単一ノード実行です(decoy_pve2 のみ対象)。`が追記される。
3. `ansible-playbook playbooks/proxmox_patch_dryrun.yml --syntax-check`、`ansible-playbook playbooks/proxmox_patch_weekly_full.yml --syntax-check`: 再実行しPASS。
4. decoy一式はすべて`/tmp/claude-1000/.../scratchpad/`配下で作成・検証後に削除済み。実ホストへのansibleコマンド実行、commit、pushは行っていない。

### 8-4. 教訓・今後の確認事項

- decoy検証において「値の型・内容を目視確認する」ことと「実際にその値が使われる後続フィルタ・条件式まで動かして確認する」ことは別物であり、後者を欠くと今回のような型変換由来のバグを見逃す。今後同種の`set_fact` + Jinja条件分岐タスクを追加・変更する場合は、値を使用する側の`when`/`length`/`join`等のフィルタまで実際に通す検証を標準手順とすべきという教訓が得られた。
- この`None`変換は、テンプレート全体が静的文字列を含まない制御構造のみになりうる箇所であればどこでも起こりうる一般的なAnsibleの落とし穴である。本diff内の他のJinja式(`_pre_status`、`_final_status`等)はいずれも全分岐で何らかの静的文字列を返す設計になっており、空文字列になりうる分岐を持つのは`_node_reachability_note`のみであることをコード読解で再確認した(他箇所への同種修正は不要)。

## Next step files

- `docs/ai/reviews/proxmox_patch_dryrun/2026-07-26_001_single_node_dryrun_investigation.md`
- `docs/ai/reviews/proxmox_patch_dryrun/2026-07-26_002_requirement.md`
- `docs/ai/adr/002-proxmox-patch-dryrun-single-node.md`
- `docs/ai/reviews/proxmox_patch_dryrun/2026-07-26_003_implement.md`(本ファイル)
- `docs/ai/reviews/proxmox_patch_dryrun/2026-07-26_004_review.md`
- `docs/ai/reviews/proxmox_patch_dryrun/2026-07-26_005_test_result.md`
