# 再レビュー結果: proxmox_patch_dryrun / P2 対応確認

- 日付: 2026-05-22
- レビュー対象: git diff
- 参照:
  - `docs/ai/prompts/core.md`
  - `docs/ai/reviews/proxmox_patch_dryrun/2026-05-13_004_requirement.md`
  - `docs/ai/reviews/proxmox_patch_dryrun/2026-05-22_003_implement.md`
  - `docs/ai/prompts/proxmox_patch_policy.md`

## Findings

新たな指摘事項はありません。

## 結論

`proxmox_patch_dryrun` 側の変更として残っている `scripts/codex-classify.sh` の差分は、前回レビュー時と同じく妥当です。

```diff
-if ! codex exec "$(cat "$PROMPT_FILE")" > "$CODEX_OUTPUT" 2>&1; then
+if ! codex exec < "$PROMPT_FILE" > "$CODEX_OUTPUT" 2>&1; then
```

`codex exec --help` で、PROMPT 未指定時は stdin から instructions を読む仕様であることを確認しました。ARG_MAX 回避として成立しています。

前回レビュー `2026-05-22_002_review.md` の P2 指摘である `ubuntu_vm_patch_policy.md` の混在については、ユーザー注記により「Ubuntu 側との分離は許容し、別コミットとする」ことが明示されています。また、`2026-05-22_003_implement.md` でも Ubuntu 側の独立レビュートラック作成と別コミット方針が記録されています。

そのため、本再レビューでは P2 は運用上解消済みとして扱います。

## 確認したこと

### `scripts/codex-classify.sh`

- git diff 上の変更は `codex exec` 呼び出しの stdin 化のみ。
- stdout / stderr の出力先は従来通り `"$CODEX_OUTPUT"`。
- `cd "$REPO_ROOT"` 後に実行する流れは維持されている。
- Python ブロック内の kernel パッケージ検出と `status_inputs.reboot_expected` 強制補正は、output JSON 保存直前に存在している。
- 要求仕様 `2026-05-13_004_requirement.md` の補正条件を満たしている。

### Ubuntu 側の分離

現在の working tree には引き続き `docs/ai/prompts/ubuntu_vm_patch_policy.md` の差分が含まれています。

ただし、今回のユーザー注記と `2026-05-22_003_implement.md` により、以下の扱いで問題ないと判断します。

- `scripts/codex-classify.sh` と Proxmox dry-run レビュートラックは proxmox_patch_dryrun commit に含める。
- `docs/ai/prompts/ubuntu_vm_patch_policy.md` と `docs/ai/reviews/ubuntu_vm_patch_policy/` 配下は別 commit に分離する。
- 本レビューでは Ubuntu ポリシー本文の内容妥当性はレビュー対象外とする。

## テスト・検証

実施:

- `git status --short`
- `git diff -- scripts/codex-classify.sh`
- `git diff -- docs/ai/prompts/ubuntu_vm_patch_policy.md`
- `git diff --stat`
- `git diff --name-status`
- `codex exec --help`
- `nl -ba scripts/codex-classify.sh`
- `docs/ai/reviews/ubuntu_vm_patch_policy/2026-05-22_001_requirement.md` の確認
- `docs/ai/reviews/ubuntu_vm_patch_policy/2026-05-22_002_implement.md` の確認

未実施:

- `scripts/codex-classify.sh` の実行テスト
  - 理由: 実際の Codex CLI 分類実行を伴うため、今回は CLI 仕様確認と差分確認に留めた。
- `shellcheck`
  - 理由: ローカルに `shellcheck` が見つからなかった。

## 最終判断

このまま進めてよいです。

commit 時は、`2026-05-22_003_implement.md` の方針通り、Proxmox dry-run 側と Ubuntu policy 側を別 commit に分けてください。

Next step files:
- `docs/ai/reviews/proxmox_patch_dryrun/2026-05-13_004_requirement.md`
- `docs/ai/reviews/proxmox_patch_dryrun/2026-05-22_003_implement.md`
- `docs/ai/reviews/proxmox_patch_dryrun/2026-05-22_004_review.md`
