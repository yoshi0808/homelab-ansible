# Review: proxmox_patch_dryrun third follow-up

レビュー日: 2026-05-09

## 対象

- `docs/ai/prompts/core.md`
- `docs/ai/prompts/proxmox_patch_policy.md`
- `docs/ai/reviews/proxmox_patch_dryrun/2026-05-09_001_requirement.md`
- `docs/ai/reviews/proxmox_patch_dryrun/2026-05-09_007_review.md`
- `docs/ai/reviews/proxmox_patch_dryrun/2026-05-09_008_implement.md`
- `playbooks/proxmox_patch_dryrun.yml`
- `roles/proxmox_patch_dryrun/tasks/main.yml`
- `roles/proxmox_patch_dryrun/files/proxmox-dryrun-collect.sh`
- `roles/proxmox_patch_dryrun/files/proxmox-dryrun-merge.py`

## 結論

commit してよい。

`2026-05-09_007_review.md` の指摘 1・2 は解消されている。core.md の shell / Ansible 責務分離にも反していない。

## 指摘事項

なし。

## 解消確認

### 1. 片系 healthcheck NG 時の全体停止

`playbooks/proxmox_patch_dryrun.yml:6` に `any_errors_fatal: true` が追加されている。

これにより、`proxmox_healthcheck` または `proxmox_patch_dryrun` 冒頭の healthcheck gate で pve1 / pve2 のどちらかが fail した場合、他方のホストだけ dry-run collect へ進むリスクは抑止される。

確認箇所:

- `playbooks/proxmox_patch_dryrun.yml:6`
- `playbooks/proxmox_patch_dryrun.yml:8-10`
- `roles/proxmox_patch_dryrun/tasks/main.yml:6-9`

### 2. policy の単一ノード実行可表記

`docs/ai/prompts/proxmox_patch_policy.md:418-420` で、対象が pve1 / pve2 固定ペア実行に修正されている。

`--limit` 単一ノード実行可の記載は削除され、現在の pve1 / pve2 固定参照実装と整合している。

### 3. policy と実装の整合

以下の点も整合している。

- `docs/ai/prompts/proxmox_patch_policy.md:425-427`
  - `apt update あり` ではなく、変更操作なし / 実パッチ適用なしに修正済み。
- `docs/ai/prompts/proxmox_patch_policy.md:432-434`
  - healthcheck OK と `apt-get check` が dry-run 手順に含まれている。
- `docs/ai/prompts/proxmox_patch_policy.md:465-469`
  - changelog text file 個別保存ではなく、unified JSON / final JSON / MD report 方針に修正済み。

## shell / Ansible 責務分離

守られている。

`proxmox-dryrun-collect.sh` は、`apt-get check` / `apt-get -s dist-upgrade` / changelog diff の収集と JSON 整形に留まっている。`BLOCKED` / `PATCH_READY` / `MAINTENANCE_REQUIRED` などの判定は Ansible tasks 側で行っている。

## PVE1/PVE2 固定について

ユーザー希望に基づき、`pve1` を主系、`pve2` を従系・先行検証ノードとして固定扱いする前提は妥当。

policy も固定ペア実行へ修正されているため、この点は commit 阻害要因ではない。

## 検証結果

- `bash -n roles/proxmox_patch_dryrun/files/proxmox-dryrun-collect.sh`: 成功
- `python3` compile check for `roles/proxmox_patch_dryrun/files/proxmox-dryrun-merge.py`: 成功
- `ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote ansible-playbook -i inventories/homelab/hosts.yml playbooks/proxmox_patch_dryrun.yml --syntax-check`: 成功

## commit 可否

commit 可。

Next step files:

- `docs/ai/reviews/proxmox_patch_dryrun/2026-05-09_001_requirement.md`
- `docs/ai/reviews/proxmox_patch_dryrun/2026-05-09_007_review.md`
- `docs/ai/reviews/proxmox_patch_dryrun/2026-05-09_008_implement.md`
- `docs/ai/reviews/proxmox_patch_dryrun/2026-05-09_009_review.md`
