# Review 002: `skills/delegation-tier/SKILL.md` 2軸判定・1巡目Critical対応の独立照合(2巡目)

- 日付: 2026-07-26
- Role: Reviewer(独立照合)
- 対象: 作業ツリーの`skills/delegation-tier/SKILL.md`、`docs/ai/roles/reviewer.md`、`docs/ai/roles/coordinator.md`(いずれも001レビュー後にCoordinatorが修正した状態、未commit)。
- 照合基準: 依頼文が絞った3点(1. 軸Aはしごの全域性=判定不能・判定揺れの反例探索、2. 001のCritical #1〜#4が実際に解消したか、3. 「引き上げ条件」廃止後の残骸・軸A/軸B/`+R`用語の一貫性)。
- 実施した検証: 対象3ファイルの全文逐行読解、`git diff`で001以降の変更範囲を特定、`grep -rn 引き上げ条件`で残骸の有無を確認(ヒットなし)、001レビューが提示した9ケースを実際に手順へ当てはめるシミュレーション、9ケース以外の反例を自作して同様にシミュレーション(下記Critical参照)、`docs/ai/roles/tester.md`・`docs/ai/role-routing-index.md`は参照のみ(変更なしのため深追いせず)。
- 実ホストへの操作は行っていない(ローカルのgit/grep/Readのみ)。

## Code Review: `skills/delegation-tier/SKILL.md` ほか(2巡目)

### Summary

001のCritical #1・#2(軸Aはしごの全域性欠如、冒頭一文の矛盾)は解消されている。`引き上げ条件`という語は完全に除去され、軸A/軸B/`+R`の用語は判定手順節を通じて一貫している。Critical #3・#4(`reviewer.md`・`coordinator.md`が`+R`を知らない)も、返却先節とTier列挙に追記が入り一次的には解消した。しかし**軸Aのはしご自体に、001とは別種の全域性・一意性の欠陥が新たに存在する**。具体的には (a) 項目1の「runtimeを変えないdoc変更」条件が、項目4が名指しする「安全境界の再定義・Role間の責任分界の変更」を先取りしてしまい、項目4が単一ファイルのdoc編集に対して事実上到達不能になっている(この2巡目レビューの対象である`reviewer.md`/`coordinator.md`自身の今回の追記がまさにこの反例になっている)、(b) 項目1の第2節「実行経路への影響が自明でfixtureで確認しきれる変更」が、001の9ケースに含まれる「filter_plugin複雑リファクタ→3」という模範解答と字面上矛盾する、(c) 項目2の「ロジック自体は自明で実ホスト確認のみ必要」が、項目3の「破壊的操作」を明示的に除外しておらず、trivialだが破壊的な変更で判定が項目の並び順に依存して揺れる。さらに`reviewer.md`は返却先節にだけ`+R`例外を追記し、同じ文書内の「禁止・エスカレーション」節が旧来通り「担当Tech Lead」を無条件の宛先として書いたままであり、自己整合が崩れている。Request Changesとする。

### Critical Issues

| # | File | Line | Issue | Severity |
|---|---|---|---|---|
| 1 | `skills/delegation-tier/SKILL.md` | 23, 26 | **項目1(Tier1)が項目4(Tier4)の対象を先取りし、項目4が単一ファイルのdoc編集に対して到達不能になる。** L23「静的検査だけで確認が完結するか(runtime挙動を変えないdoc / コメント / Policy / Contextの変更…)」は、ラダーが上から順に評価され最初のYesで停止する方式(L21)である以上、**内容が何であれ**「doc変更でruntimeに影響しない」ことさえ満たせば即Tier1として確定してしまう。ところがL26の項目4は「安全境界の再定義・Role間の責任分界の変更」自体をTier4の理由に挙げており、これらは大抵の場合まさに`docs/ai/roles/*.md`や`skills/*.md`という**doc**の変更として実現される(runtimeのコードは変えない)。反例は今回の2巡目レビュー対象そのものである: `docs/ai/roles/reviewer.md`の返却先例外追記(L19-20)と`docs/ai/roles/coordinator.md`のTier列挙追記(L10-15)は、単一ファイルずつの、runtimeに影響しないdoc変更であり、かつ文字通り「Role間の責任分界の変更」(Reviewerの返却先という責任分界を再定義している)である。項目1を先に問えば「Yes→Tier1」、項目4の文言だけを独立に読めば「Role間の責任分界の変更に該当→Tier4」であり、**同一の変更に対して手順のどこで判定を止めるかで答えが割れる**。これは001のCritical #1と同じ「判定不能・判定が揺れるケース」であり、しかも今回の一連の修正作業そのものが実例になっている(この2ファイルへの追記がTier1相当の軽い扱いで直接なされたのか、Tier4相当の調査・受入を経ているのかが、はしごの文言からは一意に読み取れない)。`docs/ai/roles/coordinator.md` L35の「Yoshinobuへ上げるもの…安全境界そのものの変更」という別節の存在も、doc変更だからといって安全境界変更が自動的に軽いTierへ落ちてよいという設計ではないことを傍証しており、項目1と項目4の優先関係を明示しない限りこの矛盾は解消しない。 | Critical |
| 2 | `skills/delegation-tier/SKILL.md` | 23 vs 25 | **項目1第2節(「実行経路への影響が自明でfixtureで確認しきれる変更」)と項目3(「本番影響のある実ロジック変更」)の境界が「専門性の要否」を問わないため、複雑だが単体テスト可能なロジック変更が両方に該当し得る。** 軸Aの問い自体はL14「実装に専門性・実機検証が要るか」だが、項目1の運用基準(L23)はこれを「fixtureで確認しきれるか」という検証可能性だけに翻訳しており、「専門性が要るかどうか」を直接は問わない。この結果、コード側で完結し実ホスト不要で、かつfixture/unit testで検証しきれる複雑なロジック変更(例: filter_pluginの複雑なリファクタ)は、項目1の文言を字面通り適用すればYesとなりTier1に落ちる。しかしCoordinatorが001で自己検証したとされる9ケースの1つ「filter_plugin複雑リファクタ→3」は同じ状況をTier3としており、**項目1の文字通りの適用結果と、Coordinator自身が正解として提示した分類が食い違う**。両者を両立させるには項目1に「ただし専門性を要する複雑な実ロジック変更は該当しない」等の除外文言が要るが、現状のL23にはこの除外がない。読み手が項目1だけを読んで機械的に判定すると、Coordinatorの想定と異なりTier1に倒してしまう余地が残る。 | Critical |
| 3 | `skills/delegation-tier/SKILL.md` | 24 vs 25, 47 | **項目2(Tier2)が「破壊的操作」を明示的に除外していないため、ロジックが自明な破壊的操作は項目2と項目3のどちらでも成立し、並び順(項目2が先)によってTier2に確定してしまう。** L24「変更ロジック自体は自明で、実ホストでの確認だけが必要か」は、変更が破壊的かどうかに一切触れていない。一方L25は「…破壊的操作、セキュリティか」を独立のOR条件としてTier3に含めており、L47の例外注記(「3.の『破壊的操作』は本来…Tier2に留まる場合」)は、**例外を除けば破壊的操作は原則Tier3である**という前提を明言している。しかし当のL47の例外はあくまで「Yoshinobuが実行内容を直接指定した」場合に限定されており、それ以外の一般的な「ロジックは単純だが破壊的な変更」(例: 単一ホストの設定ファイルを削除するtaskを新規実装する、ロジックはtrivialで実ホストでの動作確認だけが必要)については、はしごを上から順に問うと項目2で先にYesが成立し、項目3の「破壊的操作」条件に到達する前にTier2で確定してしまう。項目2に「ただし破壊的操作を除く」という一文がなく、L47の例外条件(Yoshinobu直接指定)に該当しない一般ケースでの優先順位が未規定である。 | Critical |
| 4 | `docs/ai/roles/reviewer.md` | 34, 37(「禁止・エスカレーション」節) | **`+R`例外が返却先節(L19-20)にしか反映されておらず、同じ文書内の別節が矛盾したまま残っている。** L34「修正はfindingとして担当Tech Leadへ返す」、L37「blocking finding…を見つけた場合は担当Tech Leadへエスカレーションする」は、いずれも無条件に「担当Tech Lead」を宛先としている。ところが`+R`工程(Tier1/2にReviewerだけを付加する経路)にはTech Leadが存在せず、L20で追記された例外は「返却先はCoordinatorとする」と明記している。`+R`で起動されたReviewer subagentがL37を字面通り読むと、実在しない「担当Tech Lead」へエスカレーションしようとして経路に迷う。001のCritical #3はL19-20(返却先節)のみを修正対象として扱ったため、同一文書内の禁止・エスカレーション節への伝播が漏れている。これは依頼文item3の「文書内の自己整合」観点そのものの欠陥であり、`+R`という新設概念が1つのRole文書の中で部分的にしか反映されていない。 | Critical |

### Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| 1 | `skills/delegation-tier/SKILL.md` | 23 | 「静的検査だけで確認が完結するか」という項目1の見出しと、括弧内の「Policy / Contextの変更」という例示は、`docs/ai/roles/coordinator.md` L35の「安全境界そのものの変更」がYoshinobuへ上げる対象である点と緊張関係にある(Critical #1参照)。項目1の括弧内に「ただし安全境界・Role間の責任分界を再定義する変更を除く」等、項目4への逃げ道を明示すると解消できる。 | correctness / 判定曖昧性 |
| 2 | `skills/delegation-tier/SKILL.md` | 47 | Critical #3で指摘した「一般の破壊的操作(Yoshinobu直接指定でない)」の項目2/項目3の優先順位を、L47の例外注記の前段(3.の解説部分)に一文追加して明示すると、はしご全体の一意性が回復する。 | correctness / 判定曖昧性 |
| 3 | `docs/ai/roles/reviewer.md` | 9〜13(「責任・権限」節) | Critical #4と同根で、責任・権限節も「指摘は…担当Tech Leadへ」という前提の記述がないか確認済みだが(該当なし)、将来この節に類似の宛先記述を足す場合は`+R`例外を都度確認する運用上の注意として残す。 | 保守性 |

### 反例を探した範囲(見つからなかった観点の記録)

以下は実際に手順へ当てはめてみたが、一意に判定できることを確認した(反例にならなかった)ケース:

- 001が提示した9ケース全て(core.md退役→1+R、単一playbookコメント修正→1、Policyのseverity入替→1、role実装で実ホスト確認要→2、Policy構造標準化10文書横断→1+R、複数roleへ実ロジック追加→3、proxmox_patch_dryrun単一ノード対応→4、Yoshinobu直接指定の破壊的操作→2、filter_plugin複雑リファクタ→3)について、はしごの各項目に個別に当てはめ直した。このうち「filter_plugin複雑リファクタ」はCritical #2の反例の起点になった(項目1第2節の文言と模範解答が食い違う)。
- 「Policy文書がhookや自動化機構から直接読み込まれ実行時判断に使われる場合」を想定し、項目1が「Policy変更はruntimeに影響しない」と決め打ちしている点に矛盾がないか確認した。現状リポジトリ内でPolicyが機械的に読み込まれ実行時分岐へ使われている例は見当たらず(`grep`で`docs/ai/policies/`を読み込むコードを探索したが未検出)、現時点では反例として成立しないため今回はSuggestionにも計上しなかった。
- 「セキュリティに関わるが単一行の設定値変更」(例: あるroleのdefaultの`become: true/false`切替)を想定し、項目1(doc/comment扱いではない、コード変更)・項目3(セキュリティ)のどちらで止まるか検討したが、これはコード変更である時点で項目1の「runtime挙動を変えないdoc」に該当せず項目3で一意に止まるため反例にならなかった。
- 「多ファイルだが専門性も要る」複合ケース(001のCritical #1が解消したはずの領域)を再度シミュレーションし、軸Aは項目3/4、軸Bは`+R`非該当(Tier3/4は`+R`を付けない、L40)という一意な結果になることを確認した。矛盾なし。

### What Looks Good

- 001のCritical #1(軸Aはしごの全域性欠如)は解消されている。ファイル数を軸Aから明示的に除外し(L17)、項目1を「静的検査で完結するか」という基準へ変えたことで、core.md退役(43ファイル・非実ロジック)は軸A=Tier1、軸B=`+R`として一意に決まる(L43の実証記述どおり)。
- 001のCritical #2(冒頭一文の矛盾)は解消されている。`引き上げ条件`という語自体がリポジトリ全体から消えており(`grep`で無ヒット確認済み)、L12の冒頭文は現在の2軸独立構造と整合している。
- 「迷ったら軽い側へ倒す」の適用範囲が軸Aのみである旨(L21、L88)は、判定手順節と判断根拠節の両方で一貫して明記されており、001のSuggestion #2が示していた「宣言位置が末尾すぎる」問題も、L21で前倒しする形で改善されている。
- `docs/ai/roles/reviewer.md`・`docs/ai/roles/coordinator.md`とも、返却先/Tier列挙という001が名指しした箇所自体には`+R`への言及が追加されており、001のCritical #3・#4が指摘した「文書がその概念を知らない」状態は該当箇所では解消している(ただしCritical #4で指摘の通り、reviewer.mdは同一文書内での伝播漏れが残る)。
- 「軸Bを引き上げ条件から独立させ、対象範囲の選定という1つの判別軸に統合した」という今回の示された方針(L30-38)は、001のSuggestion #1(引き上げ条件と軸Bの関係が未定義)への直接の回答になっており、単一Policy文言修正の`+R`対象外という具体例(L38)も含めて自己整合している。

### Verdict

Request Changes

**根拠**: 依頼された3観点のうち、001のCritical #1〜#4は該当箇所に限れば解消しているが(観点2)、依頼が最優先視した「軸Aはしごの全域性」(観点1)について、9ケース以外の反例が複数成立する。特にCritical #1は今回の一連の修正作業(reviewer.md/coordinator.mdへの追記)自体が反例になっており、皮肉にも「1巡目Criticalの修正を`+R`なしで行ったこと自体のTier判定が、はしごの文言からは一意に決まらない」という状態を作っている。Critical #4は観点3(自己整合)で明確な実害があり、`+R`工程で起動されたReviewerが誤った宛先(存在しないTech Lead)へエスカレーションしようとする経路が残ったままである。いずれも軽微な文言修正では済まず、はしごの優先関係(項目1と項目4、項目2と項目3)を明示する追加設計判断が必要なため、Request Changesとする。
