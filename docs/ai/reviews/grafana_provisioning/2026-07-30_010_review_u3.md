# review: Grafanaダッシュボード/アラートのrepo正本化 — U3(Step 2、provider明示化 + alerting配備 + restart)

作成: 2026-07-30 / Reviewer(独立subagent)
対象: `docs/ai/reviews/grafana_provisioning/2026-07-30_004_plan.md` §3 U3 の実装差分
参照した実装記録: `2026-07-30_009_implement_u3.md`
参照した規範: `2026-07-30_001_requirement.md`(R2/R4/R5/R6/R8/R14、§11)、`004_plan.md`§1-3〜1-8、`002_investigation.md`(R1-1/R1-4)、`003_ui_export_alert_rules.yaml`(原本)、`docs/ai/policies/ansible_test_safety_policy.md`(TS-014/015)

## 検証方法

`git status`/`git diff`/`cat`で対象差分・関連ファイルを現物確認したうえで、実ホストへ一切接続せず以下を実行した(すべてrepo内read-only、または scratchpad配下のみ)。

1. `ansible-playbook playbooks/grafana_provisioning.yml --syntax-check` / `bash scripts/check-tester-gate.sh` — いずれも成功。
2. `ansible-playbook playbooks/grafana_provisioning.yml --list-tasks` を6パターン(タグ無し、`dashboards`、`provider`、`alerting`、`provider,alerting`、`dashboards,alerting`)で実行し、タグの選択結果を全タスク単位で突合(実ホスト接触なし)。
3. `diff docs/ai/reviews/grafana_provisioning/2026-07-30_003_ui_export_alert_rules.yaml roles/grafana_provisioning/files/alerting/unifi-switch-port-errors.yaml` を独立に実行。
4. `roles/grafana_provisioning/files/dashboards-provider-unifi.yml` の内容を`002_investigation.md` R1-1の生値と目視突合。
5. scratchpad配下に、role本体を経由しない独立の最小playbook(2本)を作成・実行し削除:
   - (a) `never`タグ除外時と`when: false`時それぞれで`register`変数の`is defined`/`changed`/`skipped`の実際の値を観測。
   - (b) Grafana Go-template構文(`{{ $labels.name }}`、`{{ $values.A.Value }}`)を含むYAMLファイルを`lookup('file') | from_yaml`で読み、Jinjaによる再評価が起きないことを観測。
6. `grep`でrecovery_muteの`include_tasks`呼び出しパターンをrepo全体から収集し、既存実装との一貫性を確認。

## 確認した項目(現物・実行で裏付け済み)

**1. タグの全組み合わせで意図しない実行経路は残っていない(依頼の問い1)。** `--list-tasks`を6パターンで実行し表にした。

| 実行形態 | `deploy_dashboards` | `deploy_provider` | `deploy_alerting`(3task) | restart判定〜block |
|---|---|---|---|---|
| タグ無し(フル実行) | 実行 | 実行 | **選択外**(`never`) | 選択される。alerting未実行のためregister未定義→**skip-noticeが出る**(Coordinator差し戻し済みの経路) |
| `--tags dashboards` | 実行 | 選択外 | 選択外 | **選択外**(restart判定task自体が`dashboards`タグを持たない) |
| `--tags provider` | 選択外 | 実行 | 選択外 | 選択される。alerting未実行→skip-notice |
| `--tags alerting` | 選択外 | 選択外 | 実行(`alerting`要求で`never`解除) | 選択される。alerting実行→DB/ログ確認まで到達 |
| `--tags provider,alerting` | 選択外 | 実行 | 実行 | 選択される。alerting実行→DB/ログ確認まで到達 |
| `--tags dashboards,alerting` | 実行 | 選択外 | 実行 | 選択される。alerting実行→DB/ログ確認まで到達 |

Coordinatorが差し戻した「providerだけ配ってrestartしたのに手順5でfailする」経路は解消されていることを確認した。**それ以外の組み合わせで新たな誤検出経路は見つからなかった。** `--tags dashboards`ではrestart判定task自体が選択対象から外れる(タスクに`dashboards`タグが付いていないため)ため、dashboard単独配布時にrestart関連のロジックへ触れることはない。

**2. `grafana_provisioning_alerting_result is defined`判定は、実際に機能する設計になっている(依頼の問い2)。** scratchpad上の独立playbookで検証(下記実測)。

```
tag_excluded defined=False
when_false defined=True changed=False skipped=True
```

- `never`タグで選択外になったタスクは`register`が**未定義のまま**になる(`is defined` = False)。
- `when: false`でskipされたタスクは`register`が**定義され**(`skipped: True`、`changed: False`)。

`restart_and_verify.yml`の`grafana_provisioning_alerting_deployed_this_run`判定は前者(タグ除外)のみに依存する設計であり、これは正しい。`deploy_alerting.yml`のcopyタスクは`when: not ansible_check_mode`も持つため理論上は後者の経路(`--check`実行時のskip)もあり得るが、**restart block自体が`not ansible_check_mode`でゲートされているため、`--check`実行時はこの判定自体に到達しない**。したがって「skip経由でもregisterはchanged:false付きで存在するため素通りする」という実装コメントの記述は、到達しない経路についての説明であり実害はないが、**発生しうる条件の書き方としては正確でない**(発生するのは`--check`実行時だが、その時点でblock自体がすでにskipされている)。Nitpickとして下記Suggestionsに記載。

R8ゲートがfailした場合はplay自体がfatalで停止するため、restart_and_verify.ymlの`grafana_provisioning_alerting_result is defined`判定に到達する時点で「copyタスクが実行され終えている」という実装記録の主張も、上記(a)の結果と整合する。

**3. alerting YAMLは原本と`annotations`以外で完全一致している(依頼の問い3前半)。** `diff`の実行結果、差分は4箇所すべて`annotations`の値のみで、UID・folder・PromQL・比較値・`for`・`interval`・`notification_settings`は一切変わっていない。実装記録の主張と一致する。

**4. `lookup('file')`経由でGrafana Go-template構文がJinjaにより誤評価されるリスクは無い(依頼の問い3後半)。** scratchpad上のテストで、`{{ $labels.name }}`・`{{ $values.A.Value }}`を含むYAMLを`lookup('file') | from_yaml`で読み込んだ結果、当該文字列は**リテラルのまま保持され**、Jinjaによる再評価・エラーのいずれも起きなかった(`from_yaml`後のdictをdebug出力し目視確認)。加えて、alerting/provider双方の配備タスクは`ansible.builtin.copy`の`src:`(ファイルコピー)であり`template`モジュールを使っていないため、配備経路そのものにもJinja評価は介在しない。`deploy_alerting.yml`のtop-levelキー検査タスクも`from_yaml`後に`dict2items | map(attribute='key')`しかしておらず、annotationsの値を評価する箇所は無い。安全と判断する。

**5. `deploy_provider.yml`は現行`unifi.yaml`の値(name/folder/type/path/orgId)を1文字も変えていない(依頼の問い4)。** `002_investigation.md` R1-1の生値と`roles/grafana_provisioning/files/dashboards-provider-unifi.yml`を突合し、追加されているのは`allowUiUpdates: false`と`updateIntervalSeconds: 10`の2行のみであることを確認した。

**6. dashboard/providerのowner/modeは実測値と一致する(依頼の問い5の一部)。** `deploy_dashboards.yml`(`grafana:grafana` `0644`)・`deploy_provider.yml`(`root:grafana` `0640`)は、いずれも`002_investigation.md`のR1-1/R1-2実測ディレクトリlistingと一致する。

**7. R8ゲートの`--check`skip設計は、requirement R7訂正・計画§1-7で明示的に検討された意図的な例外であり、Policyと矛盾しない(依頼の問い6)。** TS-014は「read-onlyな診断taskには`check_mode: false`」「破壊的taskには`when: not ansible_check_mode` + `tags: [destructive]`」の2分類を定めるが、R8ゲート(`assert`)はそのどちらとも厳密には一致しない第三の性質(状態を読まない人間確認フラグの検査)を持つ。この扱いをrequirement/planが独立に理由づけている(「`--check --diff`だけでこのフラグ無しに動作を見られるようにする」)ことを`001_requirement.md` R7訂正・`004_plan.md`§1-7の現物で確認した。**見落としではなく記録された設計判断であり、check-mode-nativeの核(read-onlyな診断は`--check`でも常時実行し、破壊的操作だけをゲートする)を損なっていない** — このゲート自体はrepo↔hostの差分を明らかにする診断ではないため、`--check`でskipしても「配備前に差が読める」というAC6の趣旨に影響しない。

**8. セキュリティ観点(`skills/ansible-security-review`)で変数注入リスクは見つからなかった。** `restart_and_verify.yml`内の`command`モジュール利用のうち、テンプレート変数を埋め込んでいるのは`journalctl -u grafana-server --since "{{ grafana_provisioning_restart_started_at.stdout }}" --no-pager`のみで、値は固定書式の`date '+%Y-%m-%d %H:%M:%S'`出力であり外部入力・シェルメタ文字を含まない。`command`モジュールはシェルを介さないためインジェクション不成立。`python3 -c`のワンライナー2本はUIDのハードコードリストのみを埋め込んでおり、変数展開箇所は無い。機密情報(Slack webhook・Grafana資格情報)を扱うタスクも無く`no_log`の要否は該当しない。

**9. `--syntax-check`・`check-tester-gate.sh`とも独立実行で成功を再確認した。**

## Critical Issues

| # | File | Line | Issue | Severity |
|---|---|---|---|---|
| 1 | `roles/grafana_provisioning/tasks/deploy_alerting.yml` / `docs/ai/reviews/grafana_provisioning/2026-07-30_004_plan.md`§1-8 | deploy_alerting.yml:21-24、plan.md:249 | **owner/mode `root:grafana 0640`の根拠citationが誤っている。** plan §1-8は「同ディレクトリの`sample.yaml`が`root:grafana 0640`であることをR1-4のlistingで確認」と書くが、**R1-4はalert_ruleテーブルのDBダンプ節であり、ディレクトリlistingを含まない**。task file側のコメントは出典をR1-1としているが、R1-1の`ls -la`は`/etc/grafana/provisioning/dashboards/`(providerディレクトリ)のものであり、`/etc/grafana/provisioning/alerting/`(alertingディレクトリ)とは**別ディレクトリ**である。`002_investigation.md`は`cat /etc/grafana/provisioning/alerting/sample.yaml`を実行した記録は持つ(使用した特権の一覧、6行目)が、その`ls -la`(owner/mode)は本文のどこにも記載されていない。**「同ディレクトリのsample.yamlからの類推」という主張自体が事実と異なる**(実際には別ディレクトリの値を借用している)。値そのもの(Grafanaパッケージの慣習からroot:grafana 0640である可能性は高い)が誤っているとは断定できないが、**記録が主張する確度(「同ディレクトリの実測からの類推」)は実際には存在しない**。plan・実装コメントの双方を「alertingディレクトリの実測は取得していない。dashboardsディレクトリの`sample.yaml`(別ディレクトリ)の値からの類推であり、alerting配下自体のlistingは無い」という正確な記述へ訂正すべき。 | Moderate(値そのものは機能する可能性が高いが、記録の確度表示が事実と異なる。この案件は「検証済み」の誤表記を繰り返し問題視してきた経緯があり、同種の欠陥として扱う) |

## Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| 1 | `roles/grafana_provisioning/tasks/restart_and_verify.yml` | 34-39 | `recovery_mute/tasks/set.yml`の`include_tasks`が`{{ role_path }}/../recovery_mute/tasks/set.yml`という新パターンを使っているが、既存repoの慣習(`playbooks/proxmox_restore_vm_placement.yml`・`ubuntu_nightly.yml`・`proxmox_patch_weekly_full.yml`・`proxmox_evacuate_node.yml`、および**role内から呼ぶ場合も含め**`roles/ubuntu_vm_full_upgrade/tasks/apply.yml`)は全て`{{ playbook_dir }}/../roles/recovery_mute/tasks/set.yml`である。実装記録は「本タスクファイルがrole内にあるためplaybook_dir基点ではなくrole_path基点にした」と理由づけているが、**`ubuntu_vm_full_upgrade/tasks/apply.yml`はまさに同じ条件(role内のtaskファイルからrecovery_muteを呼ぶ)で`playbook_dir`を使い、実際に機能している既存の先例がある**。今回の`role_path`ベースの経路もこのrepoのレイアウト(`playbooks/`と`roles/`が兄弟ディレクトリ)である限り同じ絶対パスに解決されるため機能上の欠陥ではないが、既存の統一されたパターンからの理由なき逸脱であり、`duplication-reuse-check`の観点で指摘する。次回以降は既存パターン(`playbook_dir`基点)に揃えるか、`role_path`を選ぶ具体的な技術的理由を記録に残すべき。 | Duplication/Reuse |
| 2 | `roles/grafana_provisioning/tasks/restart_and_verify.yml` | 70-80 | `alert_configuration.configuration_hash`の期待値(`c6c0ff263e3ebf225d478e187c42cd99`)がハードコードされているが、**この値が将来正当な理由(Yoshinobuが意図的にcontact point/notification policy treeをUIで変更する)で変わった場合の運用手順がコードにもrequirement/planにも書かれていない**。このassertは「restartが起きるたび」(providerのみの変更を含む、将来のあらゆる変更で)無条件に評価されるため、正当な変更の後は**次にこのplaybookでgrafana-serverをrestartするたびに毎回failし続ける**ことになる。値の更新手順(誰が・どこで・どう確認して書き換えるか)をタスクファイルのコメントに追記することを推奨する。 | Maintainability |
| 3 | `roles/grafana_provisioning/tasks/deploy_alerting.yml` | 47-58 | R8確認ゲート(`Require explicit acknowledgement...`)は`when: not ansible_check_mode`でゲートされる点でTS-014の「破壊的task」の扱いに近いが、`tags: [alerting, never]`のみで`destructive`タグを持たない。このタスク自体は状態を変更しないため`destructive`分類が完全に正しいわけではないが、TS-026(マーカー分類名と実ガードの一致確認)の観点では、`--check`でゲートされる他のタスク群(`destructive`タグ付き)と扱いが不揃いに見える。既存の`destructive`慣習と合わせるか、「confirmation-gate」的な性質を持つ非破壊task固有の扱いである旨をPolicyまたはコードコメントで明記するとよい。 | Consistency(Nitpick) |
| 4 | `roles/grafana_provisioning/tasks/restart_and_verify.yml` | 11-15(冒頭コメント) | 「タグ経由の未実行と、`when: not ansible_check_mode`によるskip経由の両方に対応」というコメントの書き方は、後者の経路が実際には(restart block自体が`ansible_check_mode`でゲートされているため)到達不能であることを明示していない。確認した項目2のとおり実害はないが、コメントを「`--check`実行時はこの判定自体に到達しない(block全体がskipされるため)」と正確化した方が、将来block構造を変更した際に誤読を招きにくい。 | Documentation accuracy |

## What Looks Good

- タグの全組み合わせ(タグ無し / `dashboards` / `provider` / `alerting` / `provider,alerting` / `dashboards,alerting`)を`--list-tasks`で実際に検査し、Coordinatorが差し戻した経路以外に新たな誤検出経路が無いことを確認した。
- `grafana_provisioning_alerting_result is defined`の判定ロジックを、role本体を経由しない独立の最小テストで実際に実行し、`never`タグ除外時は未定義・`when: false`時は定義済み(`skipped: True`)という前提が正しいことを実測で確認した。
- alerting YAMLの原本との差分は`diff`コマンドで実際に4行(`annotations`のみ)であることを確認した。目視ではなく機械的な差分検査。
- `lookup('file') | from_yaml`がGrafanaのGoテンプレート構文(`{{ }}`)を再評価しないことを実際に実行して確認した。加えて配備経路(`copy`)自体がJinja評価を経由しないことも構造的に確認した。
- provider yamlの内容が現行`unifi.yaml`の実測値(name/folder/type/path/orgId)を一切変えていないことを直接突合した。
- dashboard/provider双方のowner/modeが投資記録の実測値と一致することを確認した(alerting側は上記Critical 1のとおり出典に誤りがあるため除く)。
- R8ゲートの`--check`skip設計はrequirement/planに明示的な理由づけがあり、見落としではなく記録された意図的な例外であることを確認した。
- セキュリティレビュー観点(変数注入・`no_log`・`delegate_to`・`lookup()`)でリスクは見つからなかった。
- `--syntax-check`・`scripts/check-tester-gate.sh`の再実行が成功した。

## 未確認のまま残る項目(推測を確認済みとして書かない)

- **`restart_and_verify.yml`のblock内部(mute/systemd restart/`wait_for`/DBクエリ/journalctl)は実行検証していない。** Implementerが実行しなかったのと同じ理由(decoyサンドボックスにpasswordless sudoと`grafana`グループが存在し、実サービスのrestartを誘発するリスクがある)により、本レビューでもこの部分は静的レビューに留めた。以下は静的確認の域を出ない:
  - `python3 -c`ワンライナーの実行時の構文的正しさ(構文は目視・確認項目4の周辺ロジックのみ実行確認、DBクエリ自体は未実行)。
  - `journalctl --since`の日時フォーマットがmonnieの実際のjournalctlバージョンでパースされるか。
  - `wait_for: port: 3000, timeout: 30`が実際のGrafana起動時間に対して十分か。
  - `alert_configuration`テーブルの「id降順の最新行が現行構成」という前提(Coordinatorが既に「未検証だが妥当」と判定済みの項目で、本レビューはこの判定を追加検証していない)。
- alerting配置先(`/etc/grafana/provisioning/alerting/`)の実際のowner/modeは、投資記録に`ls -la`が存在しないため未確認(Critical 1参照)。
- `journalctl`のエラー検出パターン(`provisioning`と`error`を同一行に含む)が実際の失敗ログで機能するかは、実際の失敗ログが存在しないため本レビューでも検証できていない(Implementer記録が明記済みの制約と同じ)。

## Verdict

**Request Changes**(1件、Critical Issues #1のみ)。

理由: タグ配線・register判定・alerting YAMLの無編集性・Jinja評価の安全性・provider yamlの無変化・R8ゲートの`--check`設計・変数注入耐性は、いずれも現物確認または実行検証で裏付けが取れており、機能的な欠陥は見つからなかった。**唯一の指摘は、alerting側owner/modeの根拠citationが事実と異なる点**である。値そのもの(`root:grafana` `0640`)を変える必要は無い可能性が高いが、「同ディレクトリの実測からの類推」という主張は誤りであり、`004_plan.md`§1-8・`roles/grafana_provisioning/tasks/deploy_alerting.yml`のコメントの双方を「alertingディレクトリ自体のlistingは取得していない。dashboardsディレクトリ(別ディレクトリ)の`sample.yaml`からの類推である」という正確な記述に訂正することを求める。この修正はドキュメントのみで機能変更を伴わないため、小さな差し戻しである。Suggestions 1〜4は次回以降の改善点であり、本差し戻しの解消条件には含めない。

---

## 追記(2026-07-30、再照合): 前回3件の対応を検証した結果 — Approve

Coordinatorから「前回の指摘3件すべてに対応が入った」との差し戻しを受け、**申告を鵜呑みにせず現物確認・実行検証で再照合した。** 検証方法は`git diff`による差分確認、実ホストに触れないscratchpad上での独立実行(2件)、`--list-tasks`の再実行(6パターン)。restart block内部(mute/systemd/DB/journalctl)は今回も実行していない(静的レビューのみ、Implementer/前回レビューと同じ制約)。

### 1件ずつの解消判定

**Critical 1(owner/modeのcitation誤り) — 解消を確認した。** `git diff docs/ai/reviews/grafana_provisioning/2026-07-30_002_investigation.md`で、`R1-4b`節が実際に追補されていることを確認した。中身は`ls -la /etc/grafana/provisioning/alerting/`の生値で、`sample.yaml`が`root:grafana`・mode`640`、ディレクトリが`755`であることを示す実測listingである(前回私が「存在しない」と指摘した種類の証拠が、今回は実際に存在する)。`004_plan.md`§1-8の表・`deploy_alerting.yml`のコメント双方が引用先を`R1-4b`へ更新しており、旧版の「同ディレクトリからの類推」という誤った記述も「実際には別ディレクトリ(dashboards側)の値を誤って引用していた」という経緯として明記されている。**値(`root:grafana` `0640`)は変わっていない。** citationの誤りだけが解消対象であり、それが解消された。IPアドレス等の混入も無いことを確認済み。

**Suggestion 1(`recovery_mute` includeのパス方式) — 解消を確認し、かつ独立に再実行検証した。** `restart_and_verify.yml`は`{{ role_path }}/../recovery_mute/tasks/set.yml`から`{{ playbook_dir }}/../roles/recovery_mute/tasks/set.yml`へ変更され、既存の repo 全体の慣習(`playbooks/*.yml`複数+ `roles/ubuntu_vm_full_upgrade/tasks/apply.yml`)と統一された。**「role内部から呼ばれる文脈で実際に解決するか」という問い(依頼文の問い1)は目視で終わらせず、scratchpad上に実repoと同じ相対階層(`playbooks/`と`roles/`が兄弟、`roles/<role>/tasks/main.yml`から`import_role`経由で呼ばれる構造)を再現した独立playbookを作り、実際に実行して確認した。**

```
TASK [mytest : Mute monnie before grafana-server restart (mirrored)] ***
included: .../scratchpad/pathcheck/roles/recovery_mute/tasks/set.yml for localhost

TASK [mytest : recovery_mute stand-in: prove this file was actually reached] ***
ok: [localhost] => {
    "msg": "REACHED recovery_mute/tasks/set.yml with recovery_mute_target=monnie"
}
```

`playbook_dir`は`import_role`経由でネストされたrole task内から評価しても、常にトップレベルplaybookのディレクトリ(このrepoでは`playbooks/`)を指すことを実行で確認した。**この案件で past に実在した「パス解決の思い込みで誤った前例」(`lookup('file')`のrstrip)と同じ轍を踏んでいないことを、目視でなく実行で確認済み。** 検証後にscratchpadのテストディレクトリを削除し、`git status`に影響が無いことを確認した。

**Suggestion 2(`configuration_hash`のハードコード) — 解消を確認した。** `grep -rn "c6c0ff263e3ebf225d478e187c42cd99"`をrole全体+playbookに対して実行し、ヒットは`defaults/main.yml`の変数定義1箇所のみであることを確認した(`restart_and_verify.yml`側に旧ハードコード値の残存は無い)。`assert`の`that:`・`fail_msg:`双方が`grafana_provisioning_expected_configuration_hash`変数を参照しており(依頼の問い2)、`defaults/main.yml`のコメントには「assertを外すのではなく値を更新する」という3段の手順(意図確認→read-onlyで新hash取得→変数更新)が明記されている。

**Suggestion 3(任意、R8ゲートのタグ一貫性) — 対応を確認した。** `destructive`タグは追加されなかったが、「このタスク自体は状態を変更しないため`destructive`分類には該当しない。`when`でゲートしているのは別軸の理由による」という判断がコード中に明記され、TS-026(マーカーと実ガードの一致確認)の観点を満たす形になっている。任意対応であり、この選択(タグを足さず理由を書く)は妥当と判断する。

**Suggestion 4(ヘッダコメントの正確性) — 未対応。Coordinatorの問い(blockingかどうか)に回答する: blockingではない。** `restart_and_verify.yml`冒頭のコメント(11-15行目、変更なし)は今回も「skip経由はregisterが`changed: false`付きで存在するため素通りする」という記述を残しており、この経路が実際には到達不能(restart block自体が`not ansible_check_mode`でゲートされているため)であることを明示していない。**しかしこれはコメントの精度の問題であり、コードの挙動に影響しない。** 前回のレビューで確認した「到達不能である」という事実は今回のdiffで変わっておらず(該当コメントブロックはbyte-for-byte不変)、実害の無いドキュメント上のnitpickのまま。Coordinatorが必須リストから外したのは見落としというより妥当な優先度判断だったと考えてよい。次回この付近を触る機会があれば直せばよく、今回の差し戻し・承認判断には影響させない。

### 前回の実行確認済み項目が崩れていないかの再検証(依頼の問い5)

`--list-tasks`を同じ6パターン(タグ無し/`dashboards`/`provider`/`alerting`/`provider,alerting`/`dashboards,alerting`)で再実行し、タスク名・タグの並びが前回と完全に同一であることを確認した(diffではなく全量再掲・目視突合)。`--syntax-check`・`scripts/check-tester-gate.sh`も再実行し成功。`register`のskipセマンティクス(タグ除外→未定義、`when: false`→定義済みだが到達不能)は、コードの当該部分(`restart_and_verify.yml`105-198行目)がbyte-for-byte変更されていないことを`git diff`で確認済みであり、崩れていない。

### 依頼の問い3(defaults/main.ymlの変更がU1確定済み変数に影響していないか)

`git diff roles/grafana_provisioning/defaults/main.yml`で確認した。既存の6変数(`grafana_provisioning_dashboards_dir`/`_provider_dir`/`_alerting_dir`/`_dashboard_files`/`_alert_rule_file`/`_mute_minutes`)は1文字も変更されておらず、ファイル末尾に`grafana_provisioning_expected_configuration_hash`が追加されただけである。U1でReviewerが確認した`grafana_provisioning_dashboard_files`等への影響は無い。

### 到達していないことの確認(今回分)

- 実ホスト(monnie等)へのansibleコマンド実行: 一度も行っていない。
- `grafana-server`のrestart: 発生させていない。restart block内部は今回も静的レビューのみ。
- scratchpadでの検証(recovery_mute pathの再現テスト)は`localhost`/`connection: local`で完結し、実サービス・実ファイルへは一切触れていない(自作のdebugタスクのみで実際のrecovery_mute/set.ymlは呼んでいない)。検証後にディレクトリごと削除、`git status`に残存なし。
- `git add`/`commit`/`push`: 行っていない。
- 作業ツリー: 本ファイル(既存レビュー記録への追記)以外に変更を加えていない。
- IPアドレス: 追記内容に含まれない。

### 最終判定

**Approve。** 前回のCritical 1件は実測の追補により解消を確認した(citationの誤りが是正され、値自体は変わらず正しいことも確認済み)。Suggestion 1・2は解消を確認し、Suggestion 1については独立再現実行によりパス解決の正しさも実証した。Suggestion 3は任意対応が妥当な形で入った。Suggestion 4は非blockingのドキュメントnitpickとして残存するが、今回の承認判断を妨げない。**restart block内部(mute/systemd/DB/journalctl)は今回も実行検証していない** — この部分の実行はTesterの工程であり、静的レビューの範囲を出ない点は前回と同じ制約として明示する。
