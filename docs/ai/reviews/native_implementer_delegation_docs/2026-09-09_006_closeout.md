# Closeout: Implementer委任経路の規範を現行化する

## 結果

Implementerの現行経路をCodex native subagentへ統一し、agmsg monitor / spawnを要求する旧規範を
現行文書から除去した。agmsgはCodex Coordinator、Claude CodeのReviewer / Tester / Auditor、
remote Operatorの連絡経路として維持する。

## 受入条件

| AC | 結果 | 根拠 |
|---|---|---|
| AC1 | PASS | Coordinator Roleがnative subagent・非agmsg・非常駐を明記 |
| AC2 | PASS | agent-messaging Contextが登録rosterとactive経路を区別し、旧`implementer`登録を使わないことを明記 |
| AC3 | PASS | Coordinator RoleとADR-012でmodel / effort値を維持し、native delegationとagmsg spawnへ指定元を分離 |
| AC4 | PASS | statusからImplementer monitor確認を除去し、現行体制へ更新 |
| AC5 | PASS | 旧経路断定の現行規範残存0件、doc consistency rc=0 |

## レビュー

- 初回レビュー `_004_review.md`: Request Changes（Major 2件）。旧`claude`識別子と、残存する
  `implementer`登録を存在しないと読める表現を検出。
- 再レビュー `_005_rereview.md`: Approve。両指摘の解消、両teamの`coordinator`登録、旧
  `implementer`登録の残存と非利用を実測確認。
- Suggestionだったplanの除去的な表現は、登録rosterとactive経路を分ける記述へ更新した。

## 非変更・残存事項

- agmsg teamの旧`implementer`登録は削除していない。登録の存在は起動・配送経路を意味しない。
- model / effort値、作る側 / 検める側の独立性、Reviewer / Tester / AuditorとOperatorのagmsg配送、
  Coordinator monitorは変更していない。
- launcher、agmsg本体、機能コード、実ホストは変更していない。

## 計画外事象

旧語掃引で検索語中のバッククォートをshellが評価する既知事故クラスが再発した。想定外のrepo変更が
無いことを確認したうえで、2026-09-08 / 2026-09-09の両Incidentを
`docs/ai/memory/lessons/shell-search-patterns-must-not-be-evaluated.md`へ昇格した。
