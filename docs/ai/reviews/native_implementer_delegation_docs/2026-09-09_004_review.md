## Code Review: Implementer委任経路の規範を現行化する(未commit diff)

### Summary

対象4ファイル(`docs/ai/roles/coordinator.md`、`docs/ai/context/operations/agent-messaging.md`、`docs/ai/adr/012-role-model-effort-defaults.md`、`docs/ai/status.md`)の差分は、requirement AC1〜AC4の文面上の要求(Implementerのnative subagent化、roster表からの除去、model/effort節の分離、statusの現在地更新)を満たす。ただし、`agent-messaging.md`自身が編集したroster表(§1)と、同一ファイル内の別節(§7)の記述が新たに矛盾した状態のまま残っており、また同ファイルの「Implementerはこのrosterへ登録せず」という断定を実際のagmsg team roster(`team.sh homelab`)と突き合わせると事実と食い違う。旧経路語の掃引(P1-1/AC5)は現行文書の範囲でヒット無しを確認した。

### Critical Issues

なし。

### Major Issues

| # | File | Line | Issue | Severity |
|---|---|---|---|---|
| 1 | `docs/ai/context/operations/agent-messaging.md` | 104(§7) | 本diffが§1のroster表を`claude`/`implementer`から`coordinator`(codex)へ書き換えたが、§7の「`coordinator` \| ansy \| 人が直接使っているセッション。team `homelab`では移行が済むまで`claude`を名乗る(team ごとに識別子は別である)」は未修正のまま残った。§1が既に`coordinator`を現在の識別子として記載しているため、同一ファイル内で「`homelab`では`claude`を名乗る」と「`homelab`の識別子は`coordinator`」が同時に成立し得ない。実測(`~/.agents/skills/agmsg/scripts/team.sh homelab`)でも登録されているのは`coordinator`(codex)であり`claude`は存在しない。読み手はこの1文書だけでは`homelab`側の現在の識別子を一意に確定できない(document-norm-review 欠陥クラス5: 文書内の自己整合、新概念の部分反映) | Major |
| 2 | `docs/ai/context/operations/agent-messaging.md` | 22-23(§1) | 「Implementerはこのrosterへ登録せず、Codex native subagentとして案件ごとに起動する。」は今後の運用規範として妥当だが、実測(`team.sh homelab`)では`implementer`(codex)が現在も14メンバーの1つとして登録されたままである。非ゴール「agmsg本体、team登録、tmux launcherを変更しない」によりこの登録の削除自体は求められていないが、文書は現在形で「登録されていない」と読める断定をしており、実態(登録は残存)を確認せずに読むと識別子が既に消えたと誤認しうる(document-norm-review 欠陥クラス3.5: 規範が依拠している事実の未検証) | Major |

### Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| 1 | `docs/ai/context/operations/agent-messaging.md` | 22-23 | 上記Major #2に対応する形で、「`implementer`(codex)の登録は残存しているが新たにjoinしない」等、残存の事実を明記すると、将来このrosterを読む者が実態と規範の差を自分で確かめずに済む | doc-accuracy |

### What Looks Good

- **旧経路語の掃引(P1-1/AC5)**: `grep -rn` で「Implementerはagmsg経由」「agmsg経由で`implementer`として起動する」等の断定形を`docs/ai/`・`skills/`・`.claude/`・`CLAUDE.md`・`AGENTS.md`・code/comments(`roles/*/filter_plugins/*.py`、`scripts/tests/**`等)全域で検索し、ヒットは案件記録(`docs/ai/reviews/`配下、当時の証跡としてP2で書き換え対象外)と、今回追加された「native Implementerはagmsg対象外」という新しい否定文だけであることを確認した。断定の残存は無い。
- **AC1(Coordinator Role)**: `docs/ai/roles/coordinator.md`の起動表・model/effort節は、Implementer=Codex native subagent(委任時指定)、Reviewer/Tester/Auditor=agmsg spawnという区別を一意に記述している。
- **AC3(model/effort値)**: `docs/ai/roles/coordinator.md`とADR-012はいずれもImplementer=`gpt-5.6-luna` medium(複雑案件high)、Reviewer/Tester/Auditor=`sonnet` mediumを一致して記載し、値そのものの変更は無い(非ゴール「model/effort値を変更しない」を維持)。
- **AC4(status)**: `docs/ai/status.md`から存在しないImplementer monitor確認の行は削除済みで、Now節の他の行にも同種の残タスクは残っていない(全文確認)。
- **作る側/検める側の独立性**: `docs/ai/roles/coordinator.md`「起動できるRoleと、その実現方式」の作る側/検める側の定義、`docs/ai/roles/reviewer.md`の計画査読と差分レビューの別体要件は変更されていない。
- **`scripts/check-doc-consistency.py`**: 独立実行でcheck1/2/3すべてrc=0(OK)を確認した(model/effort frontmatterとの整合を含む)。
- **`git diff --check`**: 独立実行でエラー無し。
- **scope**: 未commit差分は対象4ファイルとレビュー対象自身の成果物のみであり、requirement/planが列挙した対象外のファイルへの変更は無い(`git status --porcelain=v1 --untracked-files=all`で確認)。

### 確認範囲

- requirement(`_001`)、plan(`_002`)、implementation(`_003`)全文。
- 対象4ファイルの現在の全文(diffだけでなく`docs/ai/roles/coordinator.md`、`docs/ai/context/operations/agent-messaging.md`は全文読了)。
- `docs/ai/adr/011-vendor-neutral-role-allocation.md`(ADR-012がsupersedeする範囲の整合)。
- `docs/ai/role-context-matrix.md`、`docs/ai/policies/execution_boundary_policy.md`、`docs/ai/memory-classification.md`、`docs/ai/context/operations/healthcheck.md`中のImplementer言及(いずれもroute断定ではなくRole責務の一般言及であり対象外と判断)。
- `~/.agents/skills/agmsg/scripts/team.sh homelab`の実行結果(実際の登録identity一覧、状態を変えない確認)。
- `python3 scripts/check-doc-consistency.py`、`git diff --check`の独立再実行。
- 旧経路語のリポジトリ横断`grep`(docs/ai、skills、.claude、CLAUDE.md、AGENTS.md、code/コメント)。

### 未確認事項

- `docs/ai/reviews/native_implementer_delegation_docs/`以下の過去案件(`agent_skills_reorganization_plan.md`等)の全件は、grepヒット箇所のみ確認し全文は読んでいない(P2「過去の案件記録は書き換えない」の対象であり判定に影響しないため)。
- `new-session.sh`はgitignore対象でrepo外にあり、現物は読んでいない。観測事実として依頼文で示された「Implementerはresident paneではない」は、agmsg team roster実測(`implementer`はいるが`claude-code`常駐先の3 Roleに`implementer`が含まれないこと)と矛盾しないことのみ確認した。

### Verdict

Request Changes(Major 2件がblocking)。
