# Plan: Implementer委任経路の規範を現行化する

## 対象

- `docs/ai/roles/coordinator.md`
- `docs/ai/context/operations/agent-messaging.md`
- `docs/ai/adr/012-role-model-effort-defaults.md`
- `docs/ai/status.md`
- 本案件の成果物

## 変更

1. Coordinator RoleのRole表とmodel / effortの値の出どころをnative Implementerとagmsg checking Roleへ分離する。
2. agent-messaging Contextで登録rosterとactive経路を分離し、旧Implementer登録は残っていても
   起動・配送には使わないこと、native subagentは本Contextの対象外であることを明記する。
3. ADR-012はmodel / effort値を変えず、起動時指定の経路だけを現行化する。
4. statusからImplementer monitor確認を削除し、現行体制とCodex権限の実測記述を更新する。
5. 現行規範の旧語掃引、doc consistency、diff checkを実行し、別CLI Reviewerへ規範レビューを依頼する。

## 停止条件

- native subagentの実在またはlauncherの役構成と矛盾する場合。
- 作る側 / 検める側のCLI分離やmodel / effort値の変更が必要になる場合。
- agmsgの実装変更が必要になる場合。
