# audit: `role-routing-index.md` から経緯・根拠を落とす

日付: 2026-08-02 (JST)
検査対象: `docs/ai/reviews/norm_docs_rationale_removal/`(001_requirement〜003_review)
参照先の現物確認: `docs/ai/role-routing-index.md`(未staged改訂)、`docs/ai/adr/010-role-model-effort-allocation.md`(新設)、`docs/ai/status.md`、`docs/ai/reviews/incident_investigate_trigger/`、`scripts/check-doc-consistency.py`

## 再構成できたか

**できた。** 001_requirementの問題定義・R1〜R5・AC1〜AC5・「Testerを立てない」判断・リスク3件から、002_implementの対応・自己検証、003_reviewの独立確認まで、経緯と判断根拠が単線で辿れる。003_reviewは002_implementの主張を鵜呑みにせず独立に再検算しており、Reviewer役の独立性(`docs/ai/roles/reviewer.md`)も記録上満たされている。

## 現物突合で確認した事項

| 確認 | 結果 |
|---|---|
| `role-routing-index.md`の実差分(`git diff HEAD`)がAC1〜AC3の主張と一致するか | 一致。日付付き注記3箇所・根拠列4セル・経緯段落3つ・lessons参照文の削除のみで、値(Role/model/effort表、現行体制表、正本の優先順位、解決手順)は字句レベルで不変 |
| 改訂後ファイルに`2026-`が残っていないか(AC1) | `grep -n "2026-"` 0件を実行確認 |
| `docs/ai/memory/lessons/`参照が0件、`docs/ai/reviews/`言及がR5の3箇所のみか(AC2) | `grep`で確認。3箇所(証跡の扱い/正本の優先順位/作業開始時の解決手順)と一致 |
| ADR-010が`skills/architecture-decision-record/SKILL.md`の型に沿い、索引から落とした根拠を単体で再構成できるか(AC4) | Status/Context/Options Considered/Decision/Trade-off Analysis/Consequencesの6節構成を確認。Decisionが引用する`incident_investigate_trigger`案件の具体的記述(走査起点のOSError握りつぶし、`chmod 000`とPermissionErrorの技術的誤りとその訂正)は`docs/ai/reviews/incident_investigate_trigger/2026-07-31_002_implement.md`・`_004_test_result.md`に実在し、内容も一致 |
| `check-doc-consistency.py`(AC5前半) | 実リポジトリ(staging済みの他案件差分を含む現状)で直接再実行し、check1〜3すべてOKを確認(002/003が隔離コピーで確認した結果と一致) |
| ADR番号衝突なし | `docs/ai/adr/`は001〜010が一意に存在。並行staged中の`tester_mode_full_removal`はADRを新設していないことを`grep`で確認 |
| `docs/ai/memory-classification.md:103`の「同節」参照が今回の削除で宙ぶらりんにならないか | 現物は既に`claude-code-unattended-session-confinement.md`への直接参照であり(`norm_trim_role_routing_claude_md`案件で2026-07-29に張り替え済み)、今回の削除対象(索引側の参照文)とは別の記述。影響なし |

## 指摘

なし。

## `docs/ai/status.md`との突合

L55「規範文書から経緯・根拠・lessons参照を落とす見直し(Phase 2)」行は、本案件が対象とした`role-routing-index.md`単独の完了を指しており、現物(改訂後ファイル)と整合している。**該当行が残っていること自体は指摘にしない**(Auditor起動は該当行を消す前の設計)。除去はCoordinatorが本監査結果を受けた同じセッションのうちに行う実施事項であり、監査上の指摘ではない。

## 判定

**受入** — そのままクローズしてよい。

---

## Coordinatorによるクローズ判断(2026-08-02)

**受入。指摘0件のためそのままクローズする。**

`docs/ai/status.md` の該当行は、完了に伴い「残りの規範文書へ広げるか」という**未決の問い**へ書き換えた。`memory-classification.md` / `context-classification.md` / Policy群 / `skills/` が未適用のまま残っており、広げるかどうかはYoshinobuの判断である。

本案件で確立した型を1行で残す — **落とす前に受け皿の実在を確認し、受け皿が無いものは先に作ってから落とす。** `role-routing-index.md` のmodel/effortの根拠は他のどこにも無く(`docs/ai/reviews/incident_investigate_trigger/` に `medium` の記述は0件だった)、ADR-010を新設してから落とした。**確認を省いていれば、規範文書を軽くする作業がそのまま判断根拠の消失になっていた。**
