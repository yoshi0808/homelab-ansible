# Code Review: `docs/ai/role-routing-index.md`改訂 + ADR-010新設

正本: `docs/ai/reviews/norm_docs_rationale_removal/2026-08-02_001_requirement.md`
検証対象: `docs/ai/role-routing-index.md`(未staged改訂)、`docs/ai/adr/010-role-model-effort-allocation.md`(新設)
実装記録: `docs/ai/reviews/norm_docs_rationale_removal/2026-08-02_002_implement.md`(独立に再検算、鵜呑みにしていない)

### Summary

`git show HEAD:docs/ai/role-routing-index.md`との逐行突合、ADR-010単体の再構成可能性の判定、リポジトリ全体でのdangling reference検索、`check-doc-consistency.py`/`git-pre-commit-check.sh`の隔離コピー再実行の4系統を独立に実施した。**指示・値の欠落、宙ぶらりん参照、ADR型崩れ、番号衝突のいずれも検出しなかった。** R1〜R5・AC1〜AC5は充足していると判定する。

### Critical Issues

なし。

### Suggestions

なし。

### What Looks Good

- **AC1(日付なし)**: 改訂後全文を読み、`2026-`を含む記述が無いことを確認した(実装記録のgrep主張を独立に再読して確認、grepの誤検査ではない)。
- **AC2(lessons参照なし/reviews言及は3箇所のみ)**: `docs/ai/memory/lessons/`への参照は0件。`docs/ai/reviews/<target>/`言及は「証跡の扱い」節(L59)、「正本の優先順位」表(L71)、「作業開始時の解決手順」5.(L80)の3箇所のみで、R5が残すと定めた箇所と一致する。
- **AC3(逐行突合、指示が1つも失われていない)**: `git show HEAD:...`と現行ファイルを自分で並べて読み、削除された5断片(Coordinator行の日付注記、effort表「根拠」列4セル、mediumの試行段落、Implementer据え置き段落、Sonnet/Opus切り分け段落、lessons参照文)を1つずつ「経緯・根拠・日付」か「指示・値」かに分類した。すべて経緯・根拠側であり、各Roleのmodel/effortの値(表)、実現方式(現行体制表)、正本の優先順位、解決手順の本文はいずれも改訂前と字句レベルで同一。特に「Opus級の判断が要るのは規範のドリフト検出でありCoordinatorの領分」という一節は、`docs/ai/roles/coordinator.md`に同等の記述が無く一見instructionにも読めたが、requirement R1が明示的に移設対象として列挙している内容であり、実行可能な指示(「〜をせよ」)ではなく配分の理由づけに留まるため、根拠側への分類は妥当と判断した。
- **AC4(ADRが再構成先として成立)**: ADR-010をrole-routing-index.mdを参照せず単体で読み、Context/Options Considered/Decisionが自己完結していることを確認した。ADRが引用する`incident_investigate_trigger`案件は実在(`docs/ai/reviews/incident_investigate_trigger/2026-07-31_002_implement.md`等)。索引から落とした根拠断片5件すべてがADRのいずれかの節に写っていることを実装記録の対応表と別に自分で突合し、一致を確認した。
- **AC5(宙ぶらりん参照なし)**: `/tmp`配下へ独立にリポジトリを`cp -r`し`git add -A`した上で`check-doc-consistency.py`(check1/2/3すべてOK)と`git-pre-commit-check.sh`(gitleaks/tester-gate-lint/check1-3すべてOK、exit=0)を実行し、実装記録の主張を再現した。加えて、削除された文言そのもの(`mediumの試行`、`Implementerを既定のままにしている`、`claude-code-unattended-session-confinement`への参照)をリポジトリ全体でgrepし、`docs/ai/reviews/norm_docs_rationale_removal/`配下の案件記録以外に参照が残っていないことを確認した。`docs/ai/memory-classification.md:103`が「無人実行されるCoordinator」節の**表**を指す参照は改訂で変化しておらず、lessonsファイルへの直接参照は同ファイル内で独立して張られているため、いずれもdanglingではない。
- **ADR番号衝突なし**: `docs/ai/adr/`の既存最大は`009`、新設は`010`のみで重複ファイルは存在しない。並行staged中の`tester_mode_full_removal`案件はADRを新設していない(grep確認)。
- **ADRの型**: `skills/architecture-decision-record/SKILL.md`が定めるStatus/Context/Options Considered/Decision/Trade-off Analysis/Consequencesの6節構成に一致。IPアドレス・VLAN ID・VM ID・認証情報の実値は含まれない。
- **非ゴール遵守**: 差分は`docs/ai/role-routing-index.md`と新設ADRのみで、`docs/ai/roles/*.md`・Policy群・`memory-classification.md`・`context-classification.md`・`skills/`には触れていない。`docs/ai/reviews/`・`docs/ai/memory/`の既存記録も参照のみで書き換えなし。Roleの責務・権限・実現方式の値も不変。

### Verdict

Approve
