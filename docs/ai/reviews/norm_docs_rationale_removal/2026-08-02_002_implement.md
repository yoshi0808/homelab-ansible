# implement: `role-routing-index.md` から経緯・根拠を落とす

正本: `docs/ai/reviews/norm_docs_rationale_removal/2026-08-02_001_requirement.md`

## 変更・新設したファイル

| ファイル | 種別 | 内容 |
|---|---|---|
| `docs/ai/role-routing-index.md` | 変更(未staged) | R1〜R5に沿って経緯・根拠・日付・lessons参照を削除。R5が残すと定めた3箇所の`docs/ai/reviews/`言及はそのまま保持 |
| `docs/ai/adr/010-role-model-effort-allocation.md` | 新設 | R1で移す根拠(モデル/effort配分表の「根拠」列、mediumの試行と確定の経緯、Implementerを既定に据えた理由、Sonnet/Opusの切り分け)を`skills/architecture-decision-record/SKILL.md`の型で再構成 |

`docs/ai/status.md`は変更していない(着手前から`M`でstageされていた既存差分で、私の変更範囲外)。

## 要件充足状況

| ID | 状態 | 備考 |
|---|---|---|
| R1 | 充足 | 現行L23〜34相当の内容(根拠列4件、mediumの試行段落、Implementer据え置き理由、Sonnet/Opus切り分け)をADR-010のContext/Options Considered/Decision/Trade-off Analysisへ再構成した |
| R2 | 充足 | 「モデル・effort配分」節は、配分を守る操作を説明する導入段落(既存のまま)+ Role/model/effortの3列表(根拠列を削除)+ 品質低下時に`high`へ戻す運用指示 + ADR-010への1行ポインタ、のみになった |
| R3 | 充足 | 日付つき注記3箇所(Coordinator行の`(2026-07-31、Yoshinobu明示)`、Reviewer行根拠内の`2026-08-01に試行を終了し確定した`、mediumの試行段落冒頭の`2026-08-01に終了し`)をすべて削除した。改訂後ファイルに`2026-`は0件(grep確認) |
| R4 | 充足 | 「無人実行されるCoordinator」節から`docs/ai/memory/lessons/claude-code-unattended-session-confinement.md`への参照文を削除し、「本節は現在の許可範囲だけを述べる」に置き換えた。直後の許可範囲表(起動/書込可/読取可/Bash/auto-memory/commit-push/中止条件)はそのまま残しており、これが「現在の許可範囲」の実体 |
| R5 | 充足 | 削除・追加を通じて`docs/ai/reviews/`への言及は3箇所のまま変化なし(「証跡の扱い」節、「正本の優先順位」表、「作業開始時の解決手順」5.) |

## AC別の自己検証

### AC1(日付が無い)— 確認済み

```
grep -n "2026-" docs/ai/role-routing-index.md
```
マッチ0件(grep終了コード1)。改訂前は3箇所(L13, L27, L30相当)あった。

### AC2(lessons参照無し、reviews言及は3箇所のみ)— 確認済み

```
grep -n "docs/ai/memory/lessons" docs/ai/role-routing-index.md   -> 0件
grep -n "docs/ai/reviews" docs/ai/role-routing-index.md
  -> 59行目「証跡の扱い」節、71行目「正本の優先順位」表、80行目「作業開始時の解決手順」5.
```
3箇所とも改訂前から存在した箇所で、今回の編集で増減していない。R5が残すと定めた箇所と一致する。

### AC3(改訂前後の突合、指示が1つも失われていない)— 確認済み

`git diff -- docs/ai/role-routing-index.md`を全文読み、削除した各断片を「経緯・根拠・日付」と「指示・値」に分類した。

| 削除した断片 | 分類 | 指示・値の保存先 |
|---|---|---|
| Coordinator行の`(2026-07-31、Yoshinobu明示)` | 日付つき出典表示のみ | 直前の指示文「どこまで分解し誰へ委任するかはCoordinatorの裁量である」はそのまま残存。日付を落としても指示は変わらない |
| effort表の「根拠」列4セル(Auditor/Implementer/Reviewer/Tester) | 根拠(なぜその値か) | 値そのもの(sonnet/medium, sonnet/high, sonnet/medium, sonnet/medium)は3列表として残存。根拠本文はADR-010 Decision節へ移設 |
| 「mediumの試行は2026-08-01に終了し…」段落 | 経緯(試行の実績・確定の理由) | ADR-010 Decisionへ全文相当を移設。段落末尾の運用指示「品質低下が見えたら`high`へ戻す方針自体は維持する」は、索引側に「品質低下が観測されたら、該当Roleのeffortを`high`へ戻す」として残した(指示を落とさず、根拠だけをADRへ渡した) |
| 「Implementerを既定のままにしているのは…」段落 | 根拠 | ADR-010 Decisionへ移設。索引側にImplementerを`high`に据え置くという値自体は3列表に残っている |
| 「subagentをSonnetで回して本番影響前に…」段落 | 根拠(モデル選択の切り分け理由) | ADR-010 Options ConsideredおよびDecisionへ移設。索引側のモデル指定(Coordinator=Opus以上、Implementer/Reviewer/Tester=Sonnet)は表と導入段落にそのまま残存 |
| 「制約の仕組み・根拠・実測結果の正本は`docs/ai/memory/lessons/...`」文 | 根拠への参照(R4で明示的に削除対象) | 直後に続く許可範囲表(起動/書込可/読取可/Bash/auto-memory/commit-push/中止条件)は無改変で残存。これが「現在の許可範囲」の実体であり、R4はこの表を残すことを前提にしている |

**落ちているのは経緯・根拠・日付だけで、指示(各Roleのモデル・effortの値、実現方式、正本の優先順位、解決手順)は改訂前と同一であることを確認した。**

### AC4(ADRから根拠が再構成できるか、型に従っているか)— 自己検証のみ(独立性の限界あり)

- ADR-010は`skills/architecture-decision-record/SKILL.md`の型(Status / Context / Options Considered / Decision / Trade-off Analysis / Consequences)に従って書いた。
- 索引から削除した根拠断片(上表)がADR-010のどの節に対応するかを1件ずつ突き合わせ、全件を確認した(上表右列に記載)。
- **未検証**: AC4は「読む」側の主観評価を伴う受入条件であり、requirement §5のリスク#2が「独立レビューを必ず入れる」と明記している。自己検証の範囲では「移設先が存在し内容が対応している」ことまでしか確認できず、独立の目でADRの質(再構成しやすさ)を判定していない。Reviewer起動が必要。

### AC5(check-doc-consistency.py / git-pre-commit-check.sh がOK、宙ぶらりん参照が無い)— 確認済み(隔離コピー経由)

Implementerは`git add`を行えないため、実リポジトリでは両スクリプトの対象(gitインデックス)に変更が反映されない。そのため`/tmp/claude-1000/.../scratchpad/repo_check_copy`へリポジトリ全体を`cp -r`し、そのコピー内でのみ`git add -A`して検証した(実リポジトリの`git add`・staging状態には触れていない)。

```
[check-doc-consistency.py]
[check1] OK (98 compared)
[check2] OK (8 compared)   # .claude/agents/*.md の model/effort と本index表の一致。根拠列を削除してもRole/model/effort列は保持しているため一致は崩れない
[check3] OK (91 compared)  # markdown内部リンク切れ検査。ADR-010・role-routing-index.mdの新規/削除リンクを含めて0件
exit=0

[git-pre-commit-check.sh]
gitleaks: no leaks found
[tester-gate-lint] OK (49 playbooks)
[check1] OK / [check2] OK / [check3] OK
[pre-commit] OK
exit=0
```

また、削除前に`role-routing-index.md`「無人実行されるCoordinator」節を指す既存文書(`docs/ai/memory-classification.md:103`、複数の`docs/ai/reviews/knowledge_review_context_check/`成果物、`docs/ai/reviews/process_retrospective/2026-07-29_007_...md`)を`grep`で洗い出したが、いずれも「無人実行されるCoordinator」節の**表**(書込可/読取可等)またはこの節の存在自体を指しており、今回削除したlessons参照文そのものを指すものはなかった。宙ぶらりん参照は確認できなかった。

## 非ゴールの遵守

- `docs/ai/roles/*.md`・Policy群・`memory-classification.md`・`context-classification.md`・`skills/`には触れていない。
- `docs/ai/reviews/`・`docs/ai/memory/`の既存記録は書き換えていない(参照元として`grep`で読んだのみ)。
- Roleの責務・権限・実現方式の値は変更していない(AC3の突合で確認済み)。

## 未解決事項

1. **AC4の独立検証が未実施。** 自己検証は「対応関係の存在」までで、Reviewerによる独立読解(ADR単体で根拠を再構成できるか)が要る。requirement §5リスク#2の指示どおり、独立レビューをCoordinatorが手配すること。
2. **AC5は隔離コピーでの検証。** 実リポジトリでの最終確認は、Yoshinobuまたは次工程が`git add`した時点で改めて`scripts/git-pre-commit-check.sh`を走らせることを推奨する(コピーとの差異は無いはずだが、実行時点はcommit直前が最も確実)。
3. ADR番号は既存最大`009`の次として`010`を採番した。並行してstageされている別案件(`tester_mode_full_removal`)がADRを新設する場合、番号衝突が起きないかはCoordinator側で確認が要る(私の変更範囲では確認不可)。
