# test_result: slack_send_optin

対象requirement: `docs/ai/reviews/slack_send_optin/2026-08-02_001_requirement.md`(AC1〜AC7)
参照(判定は引き継がず現物で再確認): `2026-08-02_003_implement.md`(実装記録)、`2026-08-02_004_review.md`(差分レビュー、Approve）
検証者: Tester、2026-08-02
実ホストへの到達: なし。全検証はscratchpadのdecoy inventory(`ansible_connection: local`、実host名・実IP・`vars/slack.yml`を含まない)で行った。

## 検証方法

`roles/common_slack/tasks/notify.yml`を`include_tasks`する最小playを自分で新規に作成した(Implementerの検証記録を引き継がず独立に構築)。

- 場所: `/tmp/claude-1000/-home-yoshi-homelab-ansible/88fcc24c-2630-458e-a1ec-0e62d96e2d9b/scratchpad/slack_optin_test/`(`inventory.ini`、`play.yml`、各AC実行ログ`ac*.log`)
- inventory: `decoyhost ansible_connection=local`の1ホストのみ。実host名・実IPを含まない。
- `vars/slack.yml`はscratchpad配下に存在しない(`inventory_dir/../vars/slack.yml`を実際に検索し不在を確認済み)。そのため「送信task側へ進んだ」場合でも`Load Slack variables`が`include_vars`の読み込み失敗(ローカルファイル不在、ネットワーク不使用)で止まり、`rescue`で吸収される。`community.general.slack`モジュール自体は一度も実行されていない(全ログで`Send Slack notification`taskが`skipping`になっているか、そこへ到達する前に`Load Slack variables`で`fatal`→`rescue`になっている)。
- 環境変数は`env -u`/明示代入で都度制御し、`extra-vars`で`skip_notifications` / `slack_force_send` / `--check`を切り替えた。

**本番Slackへ到達したリクエストは0件。** `hooks.slack.com`を含むいかなるURLへも接続を試みるtaskへは到達していない(到達させないための構成そのものが上記)。

## AC別判定

### AC1 — エージェントセッション発では送らない: **PASS**

Given `CLAUDECODE=1`(本セッションのBashツール自体がこの値を持つことを`echo $CLAUDECODE`で確認済み)、`--check`なし、`skip_notifications`なし。
実測(`ac1.log`): `[suppressed] Show notification instead of sending`が`ok`(trigger3=True)、`Send Slack notification`ブロック配下の全task(`Load Slack variables`以降5つ)が`skipping`。`community.general.slack`のtask自体に到達していない。rc=0。

### AC2 — 本番相当の環境では従来どおり送る: **PASS**

Given `CLAUDECODE`未設定・`INVOCATION_ID=abc123`。
実測(`ac2.log`): `[suppressed]`taskが`skipping`(=抑止されない)。送信blockへ進み`Load Slack variables`が`fatal`(ローカルvars file不在、no_logのため内容は隠蔽)→`rescue`で吸収。これは「送信taskが実行された」ことの観測であり、`community.general.slack`モジュール自体へは到達していない(要求どおり実ネットワーク不使用)。`debug`の抑止メッセージは出ていない。rc=0。

### AC3 — 判別失敗は送信側へ倒れる: **PASS**

Given `CLAUDECODE`・`INVOCATION_ID`とも未設定。
実測(`ac3.log`): AC2と同型の結果(`[suppressed]`が`skipping`、送信blockへ進む)。取得失敗が沈黙ではなく送信側へ倒れることを確認。rc=0。

### AC4 — 既存2条件の意味が変わっていない: **PASS**

エージェントセッション外(`CLAUDECODE`未設定・`INVOCATION_ID`設定)の環境で2パターン実測。

- AC4-a(`--check`付与、`ac4a.log`): `trigger2 ansible_check_mode: True`、他はFalse。抑止された。
- AC4-b(`skip_notifications=true`、`ac4b.log`): `trigger1 skip_notifications: True`、他はFalse。抑止された。

いずれも`debug`に該当条件が名指しで出力され、rc=0。

### AC5 — 明示的送信の逃げ道と、上書きできない2条件: **PASS**

Given `CLAUDECODE=1`。3パターンすべて実測。

- ①`slack_force_send=true`のみ(`ac5_1.log`): `[suppressed]`が`skipping`。送信blockへ進んだ(=送信taskが実行された)。
- ②`slack_force_send=true` + `skip_notifications=true`(`ac5_2.log`): `trigger1=True`、抑止された。`slack_force_send=True`と表示されるが上書きされていない。
- ③`slack_force_send=true` + `--check`(`ac5_3.log`): `trigger2=True`、抑止された。**TS-031の不変条件(check mode下で送信する例外を作らない)を実測で確認**。送信されていれば実装差し戻し対象だったが、③は抑止されている。

rc=0(全パターン)。

### AC6 — 本番の通知経路に挙動差分が出ていない: **PASS**

- `git diff -- playbooks/ | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -vE '^[+-]#'` → **0件**(`playbooks/`配下の全差分行が`#`始まりのコメント行のみであることを機械的に確認)。
- `git diff --stat -- roles/` → `roles/common_slack/tasks/notify.yml`のみ(他role差分なし)。
- `git status --short`で変更ファイル一覧を確認し、`roles/common_slack` / `playbooks/`9本のコメント行 / 規範文書4本 / 新規Incidentファイル以外に差分がないことを確認した。

### AC7 — 宣言と実装が一致している: **PASS(ただしrequirement文言どおりの厳密な意味では未充足の残存箇所を1件検出)**

R6の3ファイル(`docs/ai/context/operations/healthcheck.md` L49 / `docs/ai/roles/tester.md` L37 / `docs/ai/policies/ansible_test_safety_policy.md` TS-005)、R11のヘッダコメント、TS-031本文は、いずれも`notify.yml`の実`when:`式(3条件構造)と一致していることを目視で確認した。`playbooks/`9本のマーカー説明文も3条件表記へ更新済み(diff全文で確認済み、AC6の項参照)。

**ただし、`docs/ai/status.md` L55 に「2条件だけを名指しする記述」が現存する。**

```
docs/ai/status.md:55: 「`roles/common_slack/tasks/notify.yml` は `skip_notifications` /
`ansible_check_mode` のいずれかが真のときだけ送信を抑止する」
```

これは「AとBのいずれかが真の**ときだけ**」という排他的・網羅的な表現であり、第3条件追加後は事実と反する。AC7の文言は「2条件だけを名指しする記述が**リポジトリ内に1つも残っていない**(`docs/ai/reviews/`配下の過去の案件記録は当時の事実なので対象外)」であり、`docs/ai/status.md`はこの除外対象に含まれていない。

実装記録(`2026-08-02_003_implement.md`「未解決事項」2)・差分レビュー(`2026-08-02_004_review.md`「未解決事項」)ともにこの行を認識しており、「R1〜R12に含まれておらずCoordinatorの職掌」という整理で共通しているが、**AC7という受入条件そのものの充足可否とは別問題**である。Testerとしては、契約文言に照らして機械的に検出できる残存を「未検出」として通すことはできないため、AC7を「実装差分としては充足」「requirement文言の字義どおりでは未充足の残存1件あり」の両面で報告する。

**参考(除外対象と判断した項目)**: 同じgrepで`docs/ai/memory/incidents/2026-07-29_...` `2026-08-01_...`にも`skip_notifications`の言及がヒットするが、いずれも「当時(`tester_mode`撤廃前)の事実の記述」であり、現在の`notify.yml`の挙動を現在形で言い切ってはいない。`docs/ai/reviews/`と同じ性質(過去の事実の記録)と判断し、AC7の対象に含めなかった。`scripts/check-tester-gate.sh` L10の「skip_notificationsガードで抑止される」は1条件しか名指ししておらず「2条件だけを名指し」という定義に厳密には該当しないため対象外としたが、この変更前から存在した既存の簡略表記であり、今回の変更が原因で生じたdriftではない(参考情報として付記)。`playbooks/cert_renew_quory.yml`等6箇所の`skip_notifications: "{{ ansible_check_mode }}"`はrequirementが明示的に対象外としているcaller側の値指定であり、ガード説明文ではない。

## R9(Incident起票)の確認

`docs/ai/memory/incidents/2026-08-02_tester-slack-decoy-domain-quirk-recurrence.md`を`skills/incident-recording/SKILL.md`の型と突き合わせ、日付/状態/対象/種別/原因分類/症状/原因/修正内容/確認方法の全項目が揃っていることを確認した。状態「解決済み」の根拠はImplementerの自己検証だったが、本test_resultのAC1・AC5③の独立した実測により、修正内容(第3条件の追加)が実際に機能していることを裏付けられた。

## 静的検証

- `ansible-playbook --syntax-check`を、変更された9本のplaybook全てに対して実行し、エラーなしを確認(実ホスト非対象、通常のsyntax-checkのみ)。
- `bash scripts/check-tester-gate.sh` → `[tester-gate-lint] OK (49 playbooks)`、rc=0。

## 未実施・未検証

- **OQ1(Semaphore実行環境に`CLAUDECODE`が無いことの実地確認)は未実施。** repo外でありAIから検証できない(requirement記載どおり)。`docs/ai/status.md` Watchへの追加はCoordinatorの職掌であり、実装記録・差分レビューいずれもこの点を積み残しとして明記している。
- **systemd timer起動時に実際に`CLAUDECODE`未設定・`INVOCATION_ID`設定になることの実地確認は未実施。** AC2は「systemd起動の再現」ではなく環境変数の条件そのものとして検証した(Coordinatorの指示どおり)。実機のsystemd timer実行での実測は、次回の月次Knowledge振り返り(2026-08-26予定)がOQ1と合わせて兼ねられる。
- **R7・R8(TS-031改訂・core.md decoy節改訂)はYoshinobu承認前の案文のまま。** 本test_resultはPolicy/core.mdの内容が実装(`notify.yml`)と整合していることを確認したのみで、承認自体はTesterの職掌外。
- `docs/ai/status.md` L55の是正、OQ1のWatch追加はいずれも未実施(Coordinatorの職掌、上記AC7参照)。

## 残存リスク

1. **AC7の字義どおりの完全充足には、`docs/ai/status.md` L55の是正が必要。** 現状のまま案件をクローズすると、「2条件だけを名指しする記述」が1件repoに残る。実害は低い(status.mdは運用状況の追跡メモであり、`notify.yml`の実装を直接参照する規範文書ではない)が、次にこの行を読んだ者が誤った前提(既定は送信のまま)で判断する可能性がある。
2. **OQ1が唯一残る沈黙経路である**(requirement記載のとおり)。Semaphore環境に`CLAUDECODE`が誤設定されていた場合、本番通知が沈黙する。AIからは検証不能。
3. R7・R8はYoshinobu承認前の案文であり、正式なPolicy改訂ではない。

## 結論

AC1〜AC6・R9は現物の実測でPASS。AC7は実装差分自体(`notify.yml`とR6対象3ファイル・R11・TS-031)は充足しているが、requirement文言が要求する「リポジトリ内に1つも残っていない」を字義どおり満たすには`docs/ai/status.md` L55の是正が要る。Coordinatorの判断で、この1行を案件のスコープに含めて是正するか、既知の残存事項として記録し別途扱うかを決めること。
