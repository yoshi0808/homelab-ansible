# review: slack_send_optin 作業ツリー差分

対象: `/home/yoshi/homelab-ansible` の未commit差分(`git status`/`git diff`で確認できる範囲、未追跡ファイル含む)
契約: `docs/ai/reviews/slack_send_optin/2026-08-02_001_requirement.md`(R1〜R12、AC1〜AC7、§9)
参照(判定・根拠は引き継がず現物で再確認): `2026-08-02_002_review.md`(計画査読)、`2026-08-02_003_implement.md`(実装記録)
査読者: Reviewer
日付: 2026-08-02

## Summary

`roles/common_slack/tasks/notify.yml`のwhen式を実際に評価し、AC1〜AC5が定める5通りの入力すべてで期待どおりの真偽になることを確認した。R1〜R9・R11・R12・AC6・AC7の要件は現物で充足を確認できた。規範文書側(core.md decoy節、TS-031、TS-005、healthcheck.md、tester.md)の記述は実装と一致しており、requirementが要求していない変更の混入も見つからなかった。

1件、実装記録`2026-08-02_003_implement.md`のAC7自己検証欄が主張する「grep結果0件」が、記載どおりのコマンドを実行すると実際には9件ヒットし再現しないことを見つけた。これはnotify.yml側の実装やドキュメント本文の欠陥ではなく(該当9行はいずれも正しく3条件化済みで、AC7が禁じる「2条件だけを名指しする記述」には該当しない)、自己検証コマンドの精度不足による誤った「0件」主張である。契約の充足そのものは揺るがないためBlocking設定はしないが、記録の正確性の問題としてMinorで報告する。

## Findings

### Minor

**[M1] 実装記録のAC7自己検証コマンドが主張通りの結果を再現しない**
- File: `docs/ai/reviews/slack_send_optin/2026-08-02_003_implement.md:128`
- 内容: 「`grep -rn "skip_notifications/ansible_check_mode\b\|skip_notifications/ansible_check_modeで\|skip_notifications/ansible_check_mode)"`(略)を`docs/ai/reviews/`除外で全repoに対して実行し、いずれも**0件**であることを確認した」とあるが、このコマンドを実際に再現すると`playbooks/`配下9ファイル(R12で更新した全ファイル)がヒットする。理由は`\b`が「mode」の直後・「/」の前でも成立するため、`...ansible_check_mode/AIエージェントセッション検出(3条件)...`のような**3条件化済みの文字列も部分一致でヒットしてしまう**、正規表現側の精度不足。
- 実害の有無: 現物のAC7要求(「2条件だけを名指しする記述が1つも残っていない」)自体は、9行すべてが3条件を明記する文言に更新済みであることを別途確認しており(下記「自己検証」参照)、**満たされている**。この指摘は実装の欠陥ではなく、自己検証記録に書かれた「確認手段で確認した」という主張が実際には再現しないという記録の正確性の問題。
- 影響: 将来この記録を読んだ者が「このgrepコマンドは2条件記述の検出に使える」と誤って信じ、別の変更時に再利用すると、3条件化済みの正しい記述まで誤検知する。是正するなら「2条件のみ」を検出する否定先読み等に直すか、記述を「目視突合で確認した(コマンドは参考程度)」に弱めるべき。
- 対応要否: 案件クローズ前にCoordinatorが実装記録側へ訂正を入れるか、既知の誤りとして記録に注記を追加することを推奨。ブロッカーではない。

## 契約充足状況(R1〜R12 / AC1〜AC7)

| ID | 判定 | 根拠 |
|---|---|---|
| R1 | 充足 | `notify.yml`に`_slack_agent_session_detected`のset_fact(`CLAUDECODE`判定)を追加し、skip/send両taskの`when:`へ第3項として組み込み済み |
| R2 | 充足 | `lookup('env','CLAUDECODE') \| default('', true) \| trim \| length > 0`。未設定・空文字はいずれも`False`となり、trigger3は不成立→抑止されない側(送信側)へ倒れる。式として評価し確認(下記) |
| R3 | 充足 | `playbooks/`側の差分はコメント行のみ(全9ファイル、`git diff -- playbooks/`で確認)。task定義・`when:`・`vars:`への変更はゼロ |
| R4 | 充足 | 抑止時の`debug`にtrigger1/2/3を個別の真偽値として出力 |
| R5 | 充足 | `slack_force_send`は`_slack_agent_session_detected and not (slack_force_send...)`の内側にのみ出現し、trigger1・trigger2には一切関与しない構造。AC5の3パターンで実際に評価し確認(下記) |
| R6 | 充足 | `docs/ai/context/operations/healthcheck.md` L49、`docs/ai/roles/tester.md` L37、`docs/ai/policies/ansible_test_safety_policy.md`(TS-005)の3ファイルすべてが3条件を反映する文言へ更新済み |
| R7 | 充足(承認前の案文として) | TS-031へ第3条件と`slack_force_send`の上書き範囲を追記、改訂履歴表に「Yoshinobu承認前の案文」と明記して追加行あり。§9の要求どおり承認前扱いが可視化されている |
| R8 | 充足(承認前の案文として) | `docs/ai/core.md` decoy節へ「完走しただけでは足りない、宛先の決め方を確かめない限り成立と仮定しない」旨を1行追加 |
| R9 | 充足 | `docs/ai/memory/incidents/2026-08-02_tester-slack-decoy-domain-quirk-recurrence.md`を新規作成。`skills/incident-recording/SKILL.md`の型(日付/状態/対象/種別/原因分類/症状/原因/修正内容/確認方法)を満たしている |
| R11 | 充足 | ヘッダコメントの「suppression is now exactly these two triggers」を削除し、3トリガの説明・R1参照・実装記録参照に置き換え済み |
| R12 | 充足 | 9本すべて(`monitoring_healthcheck` / `proxmox_healthcheck` / `proxmox_snapshot_check` / `time_sync_check` / `proxmox_patch_dryrun` / `incident_investigate_notify` / `radius_healthcheck` / `recovery_probe_notify` / `recovery_monitoring_check`)のマーカー説明文がコメント行のみで更新済み |
| AC1〜AC5 | 充足 | when式を5通りの入力で評価し、いずれも契約どおりの真偽になることを確認(下記「when式の評価」) |
| AC6 | 充足 | `git diff -- playbooks/`の全hunkが`#`行のみ。`roles/`配下の差分は`roles/common_slack/tasks/notify.yml`のみ |
| AC7 | 充足 | R6の3ファイル・R11のヘッダコメント・TS-031本文がnotify.ymlの実装と一致。`docs/ai/reviews/`を除いた範囲で「2条件だけを名指しする記述」を独自にgrepし、該当ゼロを確認(M1参照、実装記録側のコマンドとは別の方法で確認) |

R10はP2(不採用の記録のみ)であり対象外。

## when式の評価(AC1〜AC5、自己検証)

現物の式(抑止側):
```
skip_notifications | default(false) | bool
or ansible_check_mode | bool
or (_slack_agent_session_detected and not (slack_force_send | default(false) | bool))
```
送信task側はこの否定。`_slack_agent_session_detected`は`CLAUDECODE`の非空判定。

| ケース | skip | check_mode | CLAUDECODE検出 | force_send | 抑止式の値 | 期待(AC) | 一致 |
|---|---|---|---|---|---|---|---|
| AC1 | F | F | T | F | `F or F or (T and not F)=T` → 抑止 | 抑止される | ○ |
| AC2 | F | F | F | F | `F or F or (F and ...)=F` → 送信 | 送信される | ○ |
| AC3 | F | F | F | F | 同上=F → 送信 | 送信される(沈黙しない) | ○ |
| AC4-a(`--check`) | F | T | F | F | `F or T or ...=T` → 抑止 | 抑止される | ○ |
| AC4-b(`skip_notifications`) | T | F | F | F | `T or ...=T` → 抑止 | 抑止される | ○ |
| AC5-① | F | F | T | T | `F or F or (T and not T)=F` → 送信 | 送信される | ○ |
| AC5-② | T | F | T | T | `T or ...=T` → 抑止 | 抑止される(trigger1が上書き不可のまま優先) | ○ |
| AC5-③ | F | T | T | T | `F or T or ...=T` → 抑止 | 抑止される(trigger2が上書き不可のまま優先、TS-031不変条件維持) | ○ |

5通りすべてで期待どおりの真偽になることを式の評価で確認した(実ホスト・実ネットワークには触れていない)。

## 規範文書との整合(自己検証)

- `docs/ai/context/operations/healthcheck.md` L49: 「3条件のいずれか」という表現でnotify.ymlの式構造と一致。
- `docs/ai/roles/tester.md` L37: 「検出は取得失敗時に送信側へ倒れる設計であり、`skip_notifications`の付与を省略してよい理由にはしない」— R2の向き(取得失敗=送信)と矛盾せず、Testerに引き続き`skip_notifications`付与を求める内容も後退していない。
- `docs/ai/policies/ansible_test_safety_policy.md` TS-031: 第3条件追加と`slack_force_send`の上書き範囲(第3条件のみ)を明記。「check mode下で送信する例外を作らない」という既存の不変条件と矛盾しない(AC5-③の評価と一致)。TS-005(`role-guarded`)はTS-031への参照を追加しただけで、条件を独自に列挙し直していない(重複記述を避ける方針と整合)。
- `docs/ai/core.md` decoy節: 「完走しただけでは足りない、宛先の決め方を確かめない限り成立と仮定しない」という1文追加は、#3・#4が「完走したのに実送信された」事象を踏まえた趣旨と一致し、根拠・日付・案件名などの引用は本文に含めていない(規範文書に経緯を書かない方針と整合)。
- いずれも§9のとおり「Yoshinobu承認前の案文」である旨がTS-031改訂履歴表に明記されている。

## requirementが要求していない変更の混入確認

- `git diff --stat`の14ファイル + 未追跡2件(Incidentファイル、reviewフォルダ)が変更の全体。このうち`docs/ai/status.md`・`scripts/`・`roles/common_slack`以外の`roles/`・`inventory/`等への差分はゼロ(`git status`で確認)。
- `playbooks/`側の差分はコメント行のみで、task/`when:`/`vars:`への影響がないことをdiff全文の目視で確認。R12対象外の6箇所(`skip_notifications: "{{ ansible_check_mode }}"`を含む`cert_renew_quory`等)にも差分がないことを確認。
- notify.yml内の変更は、第3条件の追加(R1)・ヘッダ更新(R11)・debug文言更新(R4)・逃げ道(R5)・task名変更(`[skip_notifications]`→`[suppressed]`、R3の範囲内の内部変更)に限られており、`Load Slack variables`以下の送信block本体・capture block・webhook組み立てロジックには手を加えていない。
- 混入は見つからなかった。

## 未解決事項(Coordinatorへの申し送り、実装記録からの引き継ぎ)

以下は実装記録`2026-08-02_003_implement.md`「未解決事項」に記載済みで、本レビューでも現物相当であることを確認した。差分としては妥当な状態(Implementerの範囲外)であり、blocking findingとしては扱わない。

- `docs/ai/status.md` L55が2条件のみを名指ししたまま(本案件のR1〜R12に含まれず未更新)。
- OQ1(Semaphore実行環境の`CLAUDECODE`不在の検証不能)の`docs/ai/status.md` Watchへの追加が未実施。
- R7・R8はYoshinobu承認前の案文(§9のとおり、承認後に確定させる必要がある)。
- Tester役による正式なAC1〜AC7受入確認は未実施(本レビューは差分自体の契約充足を確認したもので、Testerの受入確認の代わりではない)。
- R12の requirement本文が当初「6本」としていた数と実装対象(9本)の不一致は、requirement側で訂正が必要(実装は9本を正しく実施済み)。

## 確認範囲(自己検証)

- 現物を読んで確認: `roles/common_slack/tasks/notify.yml`(全文)、9本のplaybookのdiff全文、`docs/ai/core.md`(decoy節)、`docs/ai/policies/ansible_test_safety_policy.md`(TS-005/TS-031/改訂履歴表)、`docs/ai/roles/tester.md`、`docs/ai/context/operations/healthcheck.md`、`docs/ai/memory/incidents/2026-08-02_tester-slack-decoy-domain-quirk-recurrence.md`、`skills/incident-recording/SKILL.md`、`2026-08-02_001_requirement.md`(全文)、`2026-08-02_002_review.md`(全文)、`2026-08-02_003_implement.md`(全文)。
- 実行して確認: `git status` / `git diff --stat` / `git diff`(対象ファイルごと)、`grep`による2条件記述の横断確認(`docs/ai/reviews/`除外)、`docs/ai/status.md`内の関連記述確認。when式は実ホスト・実ansible-playbookを起動せず、式として手計算で5パターン評価した(実ネットワークには一切触れていない)。
- 未確認: systemd timer実行時に実際に`CLAUDECODE`が未設定・`INVOCATION_ID`が設定されることの実測(002査読のSuggestion #3が指摘済み、Reviewer権限外)。Semaphore側の環境変数状況(OQ1、repo外)。Tester役による実ホスト相当の受入確認(別工程)。

## Verdict

Approve — Blocking issueなし。Minor 1件(M1、実装記録の自己検証コマンドの精度不足)はCoordinatorの裁量で記録訂正の要否を判断してよい。契約(R1〜R12、AC1〜AC7)はいずれも現物で充足を確認した。R7・R8はrequirement §9のとおりYoshinobu承認が別途必要。
