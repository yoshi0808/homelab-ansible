# plan_review: slack_send_optin requirement

対象: `docs/ai/reviews/slack_send_optin/2026-08-02_001_requirement.md`
査読者: Reviewer(計画査読)
日付: 2026-08-02

## Summary

requirementが挙げるfile:line引用・現物の記述・過去Incident/review記録への参照は、確認した範囲すべてで実在し内容も一致していた(下記「自己検証で確認したこと」)。技術的前提の誤りは見つからなかった。一方、R5(明示送信の逃げ道)のスコープが未確定なままだとTS-031が明示的に禁じている「check mode下で送信する例外」を再び作りかねない欠陥経路が1つ残っており、これはCritical相当としてImplementer着手前に文言を確定させるべきと判断する。そのほかAC7の機械チェック根拠、notify.yml自身のヘッダコメント欠落、引用数値の不一致をSuggestionとして挙げる。

## Critical Issues

| # | File | Line | Issue | Severity |
|---|---|---|---|---|
| 1 | `docs/ai/reviews/slack_send_optin/2026-08-02_001_requirement.md` | R5(67行目)、AC5(106-109行目) | `slack_force_send`が**何に対する**逃げ道かがスコープされていない。文面は「エージェントセッションから意図的に実送信したい場合の逃げ道」「`skip_notifications`が真ならそちらが優先」としか書いておらず、**`ansible_check_mode`との関係が未定義**。TS-031(`docs/ai/policies/ansible_test_safety_policy.md` L110)は「check mode下で送信する例外を作らない — 例外を1つ作れば、それが唯一の抜け道になる」と明記しており、`slack_force_send`がcheck_modeまで上書きする実装になれば、本案件が守ろうとしているTS-031の不変条件を自ら破ることになる。AC5は`CLAUDECODE`設定・`skip_notifications`との組合せしか検証せず、`ansible_check_mode=true`と`slack_force_send=true`を同時に与えた場合の期待値がAC1〜AC7のどこにも無いため、**Implementerがこの組合せをどちらに倒しても、既存のACはPASSしてしまう**。実装前に「`slack_force_send`は第3条件(エージェントセッション判定)のみを上書きし、`skip_notifications`にも`ansible_check_mode`にも影響しない」と本文で確定し、その組合せをACへ1件追加することを推奨する。 | Critical |

## Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| 1 | `docs/ai/reviews/slack_send_optin/2026-08-02_001_requirement.md` | AC7(116-119行目) | `scripts/check-doc-consistency.py`は現物を確認したところ、チェック対象が①`# tester-gate:`ヘッダ↔`playbooks/README.md`②agent frontmatterの`model`/`effort`↔`role-routing-index.md`③Markdown内部リンクの3種類のみで(スクリプト冒頭docstringに明記、`notify.yml`/`healthcheck.md`/`tester.md`/`ansible_test_safety_policy.md`のいずれの文字列もスクリプト中に出現しない)、**R6の3ファイルとnotify.ymlのwhen式の整合はこのスクリプトの検査対象に含まれていない**。したがってAC7の「`scripts/check-doc-consistency.py`が新たなエラーを出さない」という条件は、R6が正しく実装されているかどうかに関わらず常に成立し、検証の裏付けとして機能しない。AC7の実質的な充足は前半の手動突合(「3ファイルの記述が実装と一致している」)だけで担っており、スクリプトへの言及は誤った安心感を与える。文言を「このスクリプトはR6の整合を検査しない」と明示するか、AC7から削るかのいずれかを推奨する。 | correctness / test-coverage |
| 2 | `roles/common_slack/tasks/notify.yml` | 8-21行目(ヘッダコメント) | 現物のヘッダコメントは「2026-08-02: suppression is now exactly these two triggers. A former third, now-removed trigger variable has no effect if passed」と明記しており、第3条件を追加すると**この行自体がmarker driftになる**。R4は`debug`出力の文言更新を要求しているが、このファイル冒頭の説明コメントの更新はR1〜R9のどこにも明示されておらず、AC群(AC1〜AC7)もこのコメント行の内容を検査対象にしていない。R3が「変更はnotify.ymlの内部に閉じる」としている以上、このヘッダコメントの更新もR1やR4の付随作業として明示し、可能ならAC7の対象(「3ファイル」→「3ファイル+notify.yml自身のヘッダコメント」)に含めることを推奨する。 | consistency |
| 3 | `docs/ai/reviews/slack_send_optin/2026-08-02_001_requirement.md` | R1(63行目) | 「実測: Claude Codeセッションでは設定あり、systemd timer 起動時は未設定」のうち、前半(`CLAUDECODE=1`)は本査読でも同じCoordinator/Reviewerセッションのbash環境で再確認できた。後半(systemd timer起動時に`CLAUDECODE`が未設定・`INVOCATION_ID`が設定)は、本ドキュメントの他の全ての事実主張と異なり出典となる一次記録・review/incidentファイルへの参照が無い。技術的にはsystemdがサービス起動時に`INVOCATION_ID`を設定し`CLAUDECODE`を設定しないのは一般的な挙動で妥当性は高いが、本ドキュメントが他の箇所で貫いている「file:line/一次記録での裏付け」の水準からは外れている。ブロッカーではないが、AC2の実行時(Tester)にこの前提自体も実測で確認されることを明記しておくとよい。 | documentation |
| 4 | `docs/ai/reviews/slack_send_optin/2026-08-02_001_requirement.md` | §3非ゴール(46行目) | 「40ファイル/70参照」という数値は、`grep -rn "common_slack/tasks/notify.yml" playbooks/ roles/`(`roles/common_slack/`自身を除く)で再現すると28ファイル・48箇所程度であり、記載の数値と一致しない。結論(「1箇所で完結させる」)自体への影響は無いが、数値の根拠(集計範囲にdocs/を含めたか等)が書かれておらず再現できない。 | accuracy |

## What Looks Good

- R1〜R9が引用するfile:line(`roles/common_slack/tasks/notify.yml`、`docs/ai/policies/ansible_test_safety_policy.md` TS-005/TS-031、`docs/ai/context/operations/healthcheck.md` L49、`docs/ai/roles/tester.md` L37、`docs/ai/core.md` decoy節L103-109)はすべて現物と突き合わせ、引用内容が一字一句正確であることを確認した。
- R6が挙げる「guard式を写している非review文書」3件は、`grep -rn "notify.yml" docs/`の全ヒットから他候補(`time_sync_check_policy.md`、`unifi_backup_fetch_policy.md`、`cert_renew_cloudkey_policy.md`、`autonomous_recovery_policy.md`、ADR-003/004/009)を確認したが、いずれも「`notify.ymlを呼ぶ」という言及に留まり、実際のguard式(`skip_notifications | default(false) | bool or ansible_check_mode | bool`)を転記していなかった。R6の3件で網羅は取れている。
- #2(2026-07-29)・#3(2026-08-01)のIncidentファイルおよび`tester_mode_full_removal`の`006_test_result.md`§残存リスク1・`007_audit.md`指摘2は、requirementの引用どおりの内容であることを確認した。#4が未起票である事実(R9の根拠)も、Incidentディレクトリの現物一覧(2026-07-24/07-29/08-01の3件のみ)で確認できた。
- OQ1の「既存Watch 2件が同じ制約を記録している」は、`docs/ai/status.md`のWatch表に`Semaphoreの定期ジョブが risk-accepted へ --check を渡していないか`・`Semaphore実行環境でfact cachingが無効か`など「repo外のためAIから確認できない」を明記する行が複数存在することで裏付けが取れた(正確には2件以上あるが、趣旨に反する不一致ではない)。
- R2の「取得失敗＝送信」の向きと、OQ3が単独判別子を推奨する理由(AND/ORいずれでも沈黙経路が生まれうる)は、R1〜R3・AC1〜AC3の記述と矛盾なく一貫している。
- R3(callerを変えない)とAC6(`playbooks/`・`roles/common_slack`以外に通知関連差分がないことを`git diff`で機械確認)は対応が取れており、非ゴール(callerごとのopt-in化をしない)とも整合している。

## Verdict

Needs Discussion — Critical Issue #1(`slack_force_send`のスコープ未確定によるTS-031不変条件の再侵害リスク)をrequirement本文へ反映してからImplementerへ渡すことを推奨する。Suggestionsはブロッカーではないが、AC7は実装着手前に文言修正しておくと後工程の手戻りを避けられる。

## 確認範囲(自己検証)

- 現物を直接読んで確認: `roles/common_slack/tasks/notify.yml`(全文)、`docs/ai/policies/ansible_test_safety_policy.md`(TS-005/TS-018/TS-031/改訂履歴表)、`docs/ai/core.md`(decoy節)、`docs/ai/roles/tester.md`(禁止・エスカレーション節)、`docs/ai/context/operations/healthcheck.md`(L49周辺)、`docs/ai/status.md`(Watch表)、`scripts/check-doc-consistency.py`(全文のdocstringとgrep)、`docs/ai/memory/incidents/2026-07-29_tester-slack-notify-misfire.md`、`docs/ai/memory/incidents/2026-08-01_tester-slack-decoy-did-not-contain-request.md`、`docs/ai/reviews/tester_mode_full_removal/2026-08-02_006_test_result.md`(残存リスク節)、`docs/ai/reviews/tester_mode_full_removal/2026-08-02_007_audit.md`(指摘2節)。
- 実行して確認: `env | grep -i claudecode`(本セッションで`CLAUDECODE=1`を確認)、`env | grep -i invocation`(未設定を確認)、`grep`によるrepo横断検索(R6候補文書の洗い出し、`notify.yml`参照ファイル数の再集計、`CLAUDECODE`/`INVOCATION_ID`の既存参照有無)。
- 未確認・確認できなかったこと: systemd timer実行時に実際に`CLAUDECODE`が未設定・`INVOCATION_ID`が設定されることの実測(ansyの実行環境への到達が要るためReviewerの権限外。Suggestion #3として記録)。Semaphore(quory)側の環境変数状況はrepo外でありOQ1が自認するとおり検証不能。R5のスコープ確定は本査読が求める是正であり、現時点では実装されていないため検証もできない。
