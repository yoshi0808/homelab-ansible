# Review: semaphore_upgrade_automation(セキュリティ・規範整合)

作成: 2026-09-05 / Reviewer(独立体、正当性側レビューとは非同期・非参照)

対象: `docs/ai/reviews/semaphore_upgrade_automation/2026-09-05_001_requirement.md` に対する下記diff(すべて未commit)。

- `roles/semaphore_upgrade/`(新設一式)
- `playbooks/semaphore_upgrade.yml`(新設)
- `playbooks/README.md`(変更)
- `roles/semaphore_templates/defaults/main.yml`(変更)

観点: セキュリティ(`skills/ansible-security-review/SKILL.md`)、規範整合(`docs/ai/policies/execution_boundary_policy.md` / `docs/ai/policies/ansible_test_safety_policy.md` / `docs/ai/core.md`「公開情報と秘密情報」、`skills/document-norm-review/SKILL.md`)。正当性(冪等性・再実行・観測可能性等)は別体レビュー範囲のため扱わない。

## Summary

Critical/Majorに該当する欠陥は見つからなかった。P0-10(Slack webhookが`systemctl show`に出ない)は、`--setenv`を使わず0600 rootファイル経由で渡し使用後に削除する設計・実装が要求どおりで、実際にwebhook値が到達する経路(controller上の`no_log`付き読み込み→`copy`でtarget上0600ファイルへ→`systemd-run`のargvには config path のみ→スクリプトが読み終えて`unlink`)を追ったが、`systemctl show`に載る余地は無い。IPアドレス直書きも無い。Minor 3件を報告する。

## Critical Issues

該当なし。

## Major Issues

該当なし。

## Minor Issues

| # | File | Line | Issue | Severity |
|---|---|---|---|---|
| M1 | `roles/semaphore_upgrade/tasks/prepare_detached.yml` | 25-29(該当箇所は`upgrade.yml`側の走行中ジョブ確認) | `awk -F'|' '$3 != "playbooks/semaphore_upgrade.yml"'` による自己除外は、job idではなくplaybookパスで除外している。同一playbookの**別の**同時実行(2つ目のボタン押下やヒューマンエラーでの二重起動)が走っている場合、その別ジョブも自分自身と同様に除外されてしまい、P0-2「走行中ジョブ無しの確認」が本来検出すべき並行実行を見逃す。実害は`prepare_detached.yml`側の`systemctl is-active {{ unit }}.service`によるunit重複チェック(host単位で1つのtransient unit名しか持てない)が最終防御として機能するため、二重の`systemd-run`起動そのものは防がれるが、preflight〜install手前まで2プロセスが並走しうる余地は残る | Minor |
| M2 | `roles/semaphore_upgrade/files/semaphore-upgrade-detached.py` | 76-78 (`consumer_query_ok`) | `pwd.getpwnam(user).pw_uid in (0, os.stat(command).st_uid)` は、`homelab-semaphore-query`がroot:root 0755で配備される現行環境では`os.stat(command).st_uid == 0`のため実質`pw_uid == 0`のみを弾くチェックに退化している。P0-8が求める「所有者(`yoshi`)では通さない」という制約を、この検出スクリプト自身は`user`文字列を直接`"yoshi"`と比較して確かめておらず、Ansible側(`upgrade.yml`の`awk`フィルタで`yoshi`/`root`を除外)の正しさに全面的に依存している。P0-4は本スクリプトを「この窓で唯一の判定主体」と位置付けており、判定主体自身が`yoshi`を名指しで排除しないのは一貫していない | Minor |
| M3 | `roles/semaphore_upgrade/tasks/upgrade.yml` / `rollback.yml` | `semaphore_upgrade_backup_root`配下に世代ごとのbackupディレクトリ(DBフルコピー含む)が作成されるが、削除・世代数上限のtaskが無い | 版上げ・rollbackを繰り返すたびに`/var/backups/semaphore-upgrade`が無制限に増える。セキュリティ上の欠陥ではないが、rollback.ymlの「最新一致を選ぶ」ロジックが古い世代を拾わないことを保証するのは運用側の記憶だけになる | Minor |

## Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| S1 | `roles/semaphore_upgrade/defaults/main.yml` | 6 | `semaphore_upgrade_dry_run: "{{ dry_run \| default(true) \| bool }}"`は、P0-11本文が求める「明示のフラグを与えない限りinstallの手前で止まる」という**挙動**そのものには一致するが、`roles/prometheus_update_check/`は`dry_run is defined`をassertし無指定を明示エラーにする(暗黙の既定値を持たない)規約であり、**規約の実装方式**としては一致していない。AC1が無指定での完走を要求しているため今回の実装がAC1に矛盾するわけではないが、P0-11の「フラグ名と意味論を合わせる」の読み方に幅があるため、Coordinatorが両者の差を意図した選択として確認しておくとよい | 規範整合(参考) |
| S2 | `roles/semaphore_upgrade/tasks/rollback.yml` | 24-25 | `semaphore_upgrade_rollback_to`は`prometheus_update_check_rollback_to`と異なりバージョン形式のregex検証を持たない(値はJinja比較にのみ使われ、shell/SQLへは渡らないため injection耐性は成立しているが、フォーマットチェックが無い分だけ利用者側の入力ミスがサイレントに「一致候補ゼロ」でしか顕在化しない) | 堅牢性 |

## 確認したこと(自己検証)

- `git status`で対象5パスすべてが未commitであることを確認。実装差分は`git diff` / `Read`で全文読了(`roles/semaphore_upgrade/`一式、`playbooks/semaphore_upgrade.yml`、`playbooks/README.md`・`roles/semaphore_templates/defaults/main.yml`の`git diff`)。
- Slack webhookの流通経路を追跡: `include_vars`(controller側、`no_log: true`)→`copy content:`(`no_log: true`、target側 root:root 0600、`/run`配下)→`systemd-run`のargvは script path と config path のみ(`--setenv`不使用)→Pythonスクリプトの`notify()`が使用後`finally`で`unlink`。`transaction.json` / `rollback.json`にwebhook実値が含まれないことをフィールド列挙で確認。**P0-10・AC10を満たすと判断する。**
- `roles/semaphore_upgrade/`・`playbooks/semaphore_upgrade.yml`・`roles/semaphore_templates/defaults/main.yml`・`playbooks/README.md`をIPv4パターンで機械的に`grep -RnE`し、該当なし(rc=1)を確認。`docs/ai/core.md`「公開情報と秘密情報」に抵触なし。
- shell/commandの変数展開箇所を洗い出し、shell注入面を確認: `semaphore_upgrade_target_version` / `target_edition`は`main.yml`のassertで`^[0-9]+[.][0-9]+[.][0-9]+$` / `['community','pro']`に検証されてから使用される。sqlite3呼び出しはすべて`argv:`リスト形式でSQL文字列は固定(変数は`.backup`のパスのみで、パス構成要素は既に検証済みの`current_version`と固定prefix)。`homelab-semaphore-query` / token pathを使う2箇所の`shell:`は固定defaultのコマンド・パスを`quote`フィルタ通しで使用。Pythonの`run()`は常にargvリストで`subprocess.run`し、`runuser -u user --`もリスト形式。injectionの成立箇所は見つからなかった。
- `playbooks/semaphore_upgrade.yml`のヘッダ`# tester-gate: check-mode-native`と`roles/semaphore_upgrade/tasks/main.yml`の実装(`ansible_check_mode`時は`upgrade.yml`/`rollback.yml`を丸ごとskipし、確認メッセージのみ表示)を突き合わせ、AC2(`--check`でdownload/install/restartのいずれも起きない)と整合していることを確認。TS-014が示す「read-only診断は常時実行、破壊的操作だけゲート」という典型パターンとは異なり全体skip型だが、要求AC2自体がこの挙動を明示的に定義しているため、Policy逸脱ではなくrequirement側で選択された実装として扱った。
- `docs/ai/policies/execution_boundary_policy.md`EXEC-050/EXEC-051「Reviewerは実ホストへansibleを実行しない、状態を変えない確認も含む」を順守し、実ホスト(ansy/quory)へのansible実行は一度も行っていない。読んだのはrepo内ファイルのみ。
- `playbooks/README.md`・`roles/semaphore_templates/defaults/main.yml`のdiffを、`playbooks/semaphore_upgrade.yml`ヘッダおよび`roles/semaphore_upgrade/`実装と突き合わせ、記述の不一致は見つからなかった。カタログ新規2エントリ(`variant: apply` / `variant: rollback`)に`schedule:`キーは無く、非ゴール§3「scheduleへの登録をしない」と一致することを確認。`class: UN-SAFE` + `tester-gate: check-mode-native`の組み合わせは既存の`proxmox_patch_apply_node.yml`等と同型であり、`class_reason`(R8)が無いことも整合している(両者が食い違う場合にのみ要る)。

## 未解決事項

- M1(自己除外フィルタの粒度)・M2(consumer_query_okの`yoshi`明示排除の欠如)は、いずれも既存の別防御(unit重複チェック/Ansible側ACLフィルタ)で実害は抑えられていると判断したためMinorに留めたが、P0-4「この窓ではAnsibleが死んでおり判定主体が他に存在しない」という設計方針に照らすと、判定スクリプト自身が独立して`yoshi`/`root`を排除する形へ寄せる価値はある。Blockingとはしない。
- S1(dry_runの既定値の有無)はrequirement文書内でP0-11本文とprometheus_update_check参照の間に読み方の幅があることに起因し、実装の欠陥ではなくrequirement解釈の確認事項として記載した。
- ansyでのリハーサル実行、quoryでの実機AC検証はいずれもTester工程であり本レビューの範囲外(未実施)。

## Verdict

**Approve**(blocking findingなし)。Minor 3件・Suggestion 2件は次工程またはフォローアップでの対応で足り、承認を妨げない。
