# implement: Proxmox read-only点検3本の単一ノード対応

Implementer / 2026-07-30(2026-07-30 差分レビュー`2026-07-30_005_review.md`のRequest Changesを受け、Coordinatorが契約(plan §1-1)を2点改訂したことに追随し、本ファイルを更新)
契約の正本: `2026-07-30_002_plan.md` §1(逐語仕様)。本記録は契約からの逸脱と自己検証のみを書く。以下は改訂後の契約に対する最終状態を示す(履歴は積み増さず、結論を上書きしている)。

## 対象パス

- 新規: `roles/proxmox_reachable_nodes/tasks/main.yml`、`roles/proxmox_reachable_nodes/defaults/main.yml`(`handlers`/`templates`/`files`は作成していない — 契約どおり)
- 変更: `playbooks/proxmox_healthcheck.yml`、`playbooks/proxmox_hw_check.yml`、`playbooks/proxmox_snapshot_check.yml`
- 変更: `roles/proxmox_healthcheck/tasks/main.yml`、`roles/proxmox_hw_check/tasks/main.yml`、`roles/proxmox_snapshot_check/tasks/main.yml`
- 変更: `playbooks/proxmox_patch_weekly_full.yml`(Step 1b)
- 変更: `docs/ai/policies/proxmox_patch_policy.md`(§3.2 SB-095新設、§8変更履歴に1行)
- 新規: 本ファイル
- `playbooks/proxmox_patch_dryrun.yml` / `roles/proxmox_patch_dryrun/` は未変更(`git diff --stat`で確認、差分0件)

## Q2の結果: `meta: end_host`はrole内のタスクとして機能する

実装着手の最初に、decoy(scratch領域のみ。`ansible_connection: local`のreachableホスト1台 + ループバック閉ポート`127.0.0.1:1`のunreachableホスト1台。実host名・実IPは使っていない)で、plan §1-1のT1〜T6と同型のタスク列を持つ最小roleを`roles:`から呼び出して確認した。

結果: `decoy_unreachable`は`T6 end_host`到達後、そのhostに対する**以降の全タスク(roleのMARKERタスクと、playの`post_tasks`のdebugを含む)が実行されなかった**。PLAY RECAPにも`decoy_unreachable`の`post_tasks`側の出力が出ない。`decoy_reachable`側は最後まで実行され、`q2_marker_ran=True`を確認した。

**結論: 機能する。契約どおりU1〜U4を実装した(fallbackへの切替は不要)。**

## 契約との対応

- §1-1(新role `proxmox_reachable_nodes`): T1〜T6を逐語どおり実装。T4は`_prn_reachable_calc`という task-level `vars:`へ式を1つだけ括り出し、`prn_reachable_nodes`と`prn_unreachable_nodes`の両方から参照する形にした(plan「同じ式を2回書くのを避けるため`vars:`へ括り出してよい」に沿う。`vars:`の複数回評価は純粋なJinja式のみで副作用が無いため無害)。T1・T5の`become: false`はplan §1-1の理由どおり明記した。
  - **2026-07-30改訂反映**: 差分レビュー(`2026-07-30_005_review.md` Critical #1)が、T1の厳密一致assert(`ansible_play_hosts_all | sort == (groups[prn_candidate_group] | sort)`)は`ansible-playbook playbooks/proxmox_healthcheck.yml --limit <node>`(SB-021が明示的に許可する既存能力)を確実に壊す回帰であることをdecoyで独立検証・指摘。Coordinatorが契約(plan §1-1)側を修正したため、それに追随した。**T1**: 厳密一致を`ansible_play_hosts_all | difference(groups[prn_candidate_group]) | length == 0`(部分集合)かつ`ansible_play_hosts_all | length > 0`(非空)へ緩和し、`fail_msg`も「exactly」から「a non-empty subset of」へ書き換えた。**T2**: `ansible.builtin.ping`に`become: false`を明示した(差分レビューSuggestion #1「sudo障害が到達不能へ畳み込まれる」への対応。plan側の理由説明どおり、probeは接続性判定に純化)。T3〜T6、変数表、出力表は無変更。
  - **2026-07-30 コメント修正(Coordinator再指示)**: 上記のコード改訂後、`roles/proxmox_reachable_nodes/tasks/main.yml`冒頭の「呼び出し規約」コメントが「候補ノード**全体**を hosts: に持つ Play」という厳密一致時代の記述のまま残っており、`defaults/main.yml`の`prn_candidate_group`コメントも「hosts: と**一致**させること」「この**一致**をassertする」という同種の食い違いを持っていたため、両ファイルの該当コメントのみを「候補groupの部分集合であり、かつ空でないこと」「`--limit <node>`による単一node実行も正しく通る」と読める記述へ書き換えた。**コード(YAML実行部)は一切変更していない**(`git diff`でコメント行のみの差分であることを確認済み)。
- §1-2(Q2): 上記のとおり実施・機能確認。
- §1-3(3 playbook適用): `roles:`リスト先頭に`proxmox_reachable_nodes`を追加、play-levelに`ignore_unreachable: true`を追加。`tester-gate:`行の文言は変更していない。各playbook冒頭に1〜2行のコメントを追加し、詳細はADR-008を参照させる形にした。
- §1-4(summary): healthcheck/hw_checkは`Result=`append直後に条件付き`Unchecked=`appendを追加(plan snippetそのまま)。snapshot_checkはplan_review反映後の§1-4(`:125`単一行を差し替える形)をそのまま採用した。3 roleとも`prn_unreachable_nodes | default([])`を使用。
- §1-5(weekly full gate): `playbooks/proxmox_patch_weekly_full.yml`のStep 1b、既存の`Fail if healthcheck is not OK`タスクの直前へ、plan §1-5のタスクをmsg・vars・when含め逐語で追加した。
- §1-6(Policy): SB-095を`docs/ai/policies/proxmox_patch_policy.md` §3.2のSB-094直後へ、plan §1-6の文面をそのまま追加した。§8変更履歴に既存慣行(1行、退番なし)どおり1行追加した。Yoshinobu承認前提でcommitはしていない。

**契約からの逸脱: なし。** plan §1(2026-07-30改訂後)の各要素は上記のとおりすべて実装に存在する。`roles/proxmox_exec_node`は今回も未変更(非ゴール、`git diff --stat`で確認、差分0件)。

## 自己検証

実行環境: `ansy`のリポジトリ作業ツリー内、および`/tmp`配下のscratchのみ。**pve1 / pve2 / quory / monnie / authy / sophos-fw / cloudkeyへは一切接続していない。** decoyは`ansible_connection: local`のホストとループバック閉ポート(`127.0.0.1:1`、`127.0.0.1:2`)のホストのみで構成し、実host名・実IPは使っていない。

### 1. `--syntax-check`(4 playbook)

```
ansible-playbook playbooks/proxmox_healthcheck.yml --syntax-check      → RC=0
ansible-playbook playbooks/proxmox_hw_check.yml --syntax-check         → RC=0
ansible-playbook playbooks/proxmox_snapshot_check.yml --syntax-check   → RC=0
ansible-playbook playbooks/proxmox_patch_weekly_full.yml --syntax-check → RC=0
```

### 2. `roles/proxmox_reachable_nodes`単体をdecoyで実行(実roleコード。scratch playbookから`roles: [proxmox_reachable_nodes]`で呼び出し、post_tasksでfactをdebug表示するのみ。copy/command等の実システムに影響するモジュールは使っていない)

**T1/T2改訂後に同じ3シナリオを再実行し、結果が変わらないことを確認済み(下表は再実行後の実測値)。** 加えて、改訂の動機になった`--limit`(AC7)と、部分集合への緩和後もT1が守るべき前提(候補group外ホストの混入)の2シナリオを新たに確認した。

| decoyシナリオ | 期待 | 実測 |
|---|---|---|
| 候補2台中1台unreachable(closed port) | rc=0、`prn_reachable_nodes`=到達側のみ、`prn_unreachable_nodes`=不能側のみ、unreachable側はend_hostで以降スキップ | **rc=0**(改訂前後で同一)。`prn_reachable_nodes=['decoy_reachable']` `prn_unreachable_nodes=['decoy_unreachable']`(型はJinjaのlist、`None`や空文字列ではないことをdebug出力のPython repr表記で確認) |
| 候補2台とも到達不能(2つの閉ポート) | 非ゼロで停止、msgに両ノード名 | **rc=2**(改訂前後で同一)。fail msg: `"...no node in group 'proxmox' is reachable via SSH ping... Candidate reachability: decoy_unreachable1=False, decoy_unreachable2=False."` |
| 候補2台とも到達可能(`ansible_connection: local`×2) | rc=0、`prn_unreachable_nodes`が空list | **rc=0**(改訂前後で同一)。`prn_unreachable_nodes=[]`(空list、`length`フィルタで`0`になることを次項3で別途確認) |
| **AC7: `--limit <1ノード>`、候補2台とも到達可能** | 部分集合緩和によりT1を通過、rc=0、指定した1ノードだけの結果が出る | **rc=0**。`--limit decoy_r1`で`ansible_play_hosts_all=['decoy_r1']`のみとなりT1(部分集合チェック)を通過。`prn_reachable_nodes=['decoy_r1']` `prn_unreachable_nodes=[]`。指定していない`decoy_r2`はplayに現れない |
| **候補group外のホストがplayに混じる**(`hosts: proxmox:other`で`decoy_outsider`を追加) | 部分集合へ緩めてもT1が拒否し、非ゼロで停止 | **rc=2**。fail msg: `"...calling play's hosts must be a non-empty subset of groups['proxmox'] (play hosts: ['decoy_p1', 'decoy_p2', 'decoy_outsider'], group members: ['decoy_p1', 'decoy_p2'])."` |

### 3. summary Jinja(3 role、`Unchecked=`挿入部)をdecoyで検証(T1/T2改訂の影響を受けないため再実行はしていない。T3〜T6・summary側は無変更)(該当task blockを実ファイルからverbatimコピーし、`proxmox_report`等を`set_fact`で合成データ投入して実行。実host・実システムへは影響しない)

`prn_unreachable_nodes`を(A)`['pve1']`(非空)、(B)`[]`(空list)、(C)未定義(dryrunの`include_role`経路を模す)の3パターンで実行し、いずれも**rc=0**、`| length`フィルタを含む消費側まで通して例外が出ないことを確認した。

- (A) 非空: `SUMMARY proxmox_healthcheck | Result=OK | Unchecked=pve1 | Next=none | ...`
- (B) 空list: `SUMMARY proxmox_healthcheck | Result=OK | Next=none | ...`(`Unchecked=`セグメント自体が出ない)
- (C) 未定義: (B)と**完全に同一の文字列**(`default([])`によりdryrun経路の出力が変わらないことを実測で確認 — R5・AC5相当)

hw_check・snapshot_checkも同様に3パターンとも確認し、(B)と(C)が完全一致することを確認した(snapshot_checkの例: (A)`SUMMARY proxmox_snapshot_check | Result=OK | Unchecked=pve1 | decoy_single=OK(...) | Threshold=7d`、(B)=(C)`SUMMARY proxmox_snapshot_check | Result=OK | decoy_single=OK(...) | Threshold=7d`)。

### 4. weekly full Step 1bゲート(該当taskをverbatimコピーしてdecoyで実行)

| decoyシナリオ | 期待 | 実測 |
|---|---|---|
| 候補2台中1台`prn_reachable=false` | 到達不能ノード名を含むmsgで停止、非ゼロ | **rc=2**。msg: `"...but decoy_unreachable did not answer during the healthcheck play. Apply-side drift avoidance (SB-027 / SB-028 / SB-032) requires the full fixed pair."` |
| 候補2台とも`prn_reachable=true` | タスクがskipし完走、rc=0 | **rc=0**(タスクは`skipping`) |

### 5. pre-commit(gitleaks + tester-gate-lint)

改訂反映後、`git add -A` 後 `scripts/git-pre-commit-check.sh` を再実行し、`gitleaks: no leaks found`・`[tester-gate-lint] OK (44 playbooks)`・`[pre-commit] OK`を確認した(**commitは実行していない**)。IPv4アドレスはリポジトリ側diffに含まれないことを`grep -E`で個別にも確認した(マッチ0件)。**コメント修正後も同様に`--syntax-check`(4 playbook全てRC=0)・pre-commit(gitleaks/tester-gate-lint)を再実行し、結果は変わらないことを確認した。** コメントのみの変更のため、role単体・summary Jinja・weekly fullゲートのdecoy再実行は行っていない(§2〜4の実測値はコード改訂後のものが最終値のまま有効)。

## 自己検証で確認できなかったこと(未解決事項・Testerへの申し送り)

1. **3 playbook本体(copy/command部分を含む実際のtasks)をdecoyやlocalhost経由で通しては検証していない。** `roles/proxmox_healthcheck`等は`/usr/local/sbin/`へのscript配置(`become: true`)と実コマンド実行を伴うため、`ansible_connection: local`のdecoyであっても実行すればansy自身の実ファイルシステムに書き込むことになり、「実システムに影響するモジュールを使わない」という許可範囲を超える。そのため、今回の自己検証は**新規role単体**・**summary Jinjaの該当断片**・**weekly fullの新規ゲートtask**の3つを個別にdecoyで実測する形に留めた。AC1〜AC7(playbook全体としてのrc・summary文言・report JSON保存・weekly full連携・`--limit`)の最終確認はTesterの担当であり、requirement §9のとおりpve1停止中の今日(2026-07-30)の平日日中がAC1〜AC3の実地検証の窓である。
2. Q1(SB-095のYoshinobu承認)は未承認。commitはYoshinobuが行う前提で差分のみ用意した。
3. `docs/ai/status.md`・`docs/ai/adr/008-proxmox-readonly-check-unreachable-node.md`のStatus更新(Proposed→Accepted等)は本タスクのscope外のため行っていない。
4. **差分レビュー(`2026-07-30_005_review.md`)のCritical #1(`--limit`回帰)は本更新で解消済み**(上記「契約との対応」§1-1の改訂反映を参照。AC7として再検証済み)。**Suggestion #1(T2のbecome非対称)も本更新で解消済み**(T2へ`become: false`を明示)。同レビューの他の指摘(dead code化回避、`equalto false`成立、AC5バイト一致、dryrun非波及、重複許容の判断根拠)はいずれも指摘時点で問題なしと確認されており、対応不要。

## リポジトリ状態

`git status --porcelain`は許可パス一覧、および元々untrackedだったCoordinator/Reviewer側の既存成果物(ADR-008、requirement/plan/plan_review、差分レビュー`2026-07-30_005_review.md`)のみが対象で、それ以外の変更は無い。`git add -A`でstageのみ実施(`git commit`/`git push`は未実行、現在16ファイルがstaged)。scratch領域(`/tmp/claude-1000/.../scratchpad/decoy_q2`、`decoy_u1`、`decoy_u2`、`decoy_u3`)は本ファイルの検証記録として残した以外、repo内には何も残していない。`roles/proxmox_exec_node`は今回の改訂でも一切変更していない(非ゴール、差分0件)。
