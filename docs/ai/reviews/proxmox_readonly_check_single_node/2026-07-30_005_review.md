# review: Proxmox read-only点検3本の単一ノード対応(差分レビュー)

作成: 2026-07-30 / Reviewer(差分レビュー、計画査読とは別subagent)
対象: `git diff --cached`(15ファイル、719行追加)。実装本体8ファイル(依頼文の一覧)。
契約: requirement `2026-07-30_001_requirement.md` / plan `2026-07-30_002_plan.md` §1 / ADR-008。
参照(照合のみ、鵜呑みにしない): implement記録 `2026-07-30_004_implement.md`。

**追記(2026-07-30、再照合)**: Critical #1・Suggestion #1への修正(`roles/proxmox_reachable_nodes/`のみ、他ファイルは変更なし)を再照合した。契約側もT1・T2の仕様変更(部分集合assert、`become: false`明示)とAC7新設で追随済み。**両findingsとも解消を確認、新たな穴も検出しなかった。Verdictを Approve へ更新する。** 変更履歴は本ファイル末尾「再照合ログ」節に記す(節の本体はResolvedへ書き換え、経緯として旧記述は再照合ログに残す)。

## 確認方法

repo内現物の直接読み合わせに加え、**指示された検証観点(`selectattr('prn_reachable', 'equalto', false)`の成立、到達不能nodeの除外、全滅ガードのdead code化、AC5のバイト一致)を、実際に`ansible-playbook`を完走させて独立に確認した**(Implementerの自己検証記録は出発点にせず、自分で再実行)。

- decoyはすべてscratch領域(`/tmp/claude-1000/.../scratchpad/prn_verify/`)に作成し、実host名・実IPは使っていない。`ansible_connection: local`(reachable)とループバック閉ポート`127.0.0.1:1`/`:2`(unreachable、`ConnectTimeout=1`)の組み合わせのみ。
- 実行したのは①scratch上の最小playbookで到達性判定ロジックとweekly full gateの断片(verbatimコピー)、②**リポジトリの実ファイル`roles/proxmox_reachable_nodes/tasks/main.yml`をそのまま`ANSIBLE_ROLES_PATH`経由で呼び出す**decoy playbook、の2種類。後者はImplementerの自己検証とは独立に、レビュー側で新規に組んだもの。
- pve1 / pve2 / quory / monnie / authy / sophos-fw / cloudkeyには一切接続していない。`git add` / `commit` / `push`は実行していない(現在のstaging areaは変更していない)。実装ファイルも変更していない。

## Code Review: `roles/proxmox_reachable_nodes` ほか(proxmox read-only点検3本の単一ノード対応)

### Summary

新role `roles/proxmox_reachable_nodes`のT1〜T6、weekly fullのStep 1bゲート、3 roleのsummary条件付きappendは、plan §1の逐語仕様と1対1で対応しており、依頼で指定された検証観点(到達不能nodeの除外、全滅ガードのdead code化回避、`equalto false`の成立、AC5のバイト一致)はいずれも独立実行で再現・確認できた。**2026-07-30の再照合で、Critical #1(`--limit`回帰)・Suggestion #1(T2 become非対称)への修正を独立実行で検証し、両方とも解消を確認した。新たな穴(候補group外ホストの混入、`--limit`単一node不到達時のサイレント成功)は検出しなかった。**

### Critical Issues

**すべて解消(2026-07-30再照合)。**

| # | File | Line | Issue | Severity | Status |
|---|---|---|---|---|---|
| 1 | `roles/proxmox_reachable_nodes/tasks/main.yml` | 51-52(T1 assert、再照合後の行番号) | ~~T1の呼び出し規約assertが厳密一致のため`--limit`単一node実行(SB-021が許可)を壊す~~ → **Resolved**。T1は`ansible_play_hosts_all \| difference(groups[prn_candidate_group]) \| length == 0` かつ `ansible_play_hosts_all \| length > 0`(部分集合+非空)へ緩和された。plan §1-1に「T1を部分集合にする理由」として明記され、ADR-008 Consequencesにも追随している。 | ~~Critical (blocking)~~ | **Resolved** |

**再照合ログ(独立実行、リポジトリの実ファイル`roles/proxmox_reachable_nodes/tasks/main.yml`を`ANSIBLE_ROLES_PATH`経由でdecoy実行。前回のCritical #1発見時とは別のdecoyを新規に組んだ)**:

1. **`--limit`で1台(到達可能)に絞った場合 — 通ることを確認(rc=0)**:
   ```
   TASK [... Assert required inputs and calling convention] ***  ok: [decoy_a -> localhost]
   TASK [... Fail if no candidate node is reachable] ***  skipping: [decoy_a]
   TASK [... Drop unreachable nodes from the play] ***  skipping: [decoy_a]
   TASK [post marker] ***  msg: "post_tasks reached on decoy_a, prn_reachable=True"
   RC=0
   ```
2. **`--limit`で1台(到達不能)に絞った場合 — サイレント成功にならないことを確認(rc=2)**:
   ```
   TASK [... Fail if no candidate node is reachable] ***
   fatal: [decoy_unreachable -> localhost]: FAILED! => {"msg": "prn_verify_test: no node in group 'proxmox' is
     reachable via SSH ping, cannot continue the check on any node. Candidate reachability: decoy_unreachable=False."}
   RC=2
   ```
   T5(全滅判定)が`--limit`で縮小した1台構成でも正しく発火する。片肺どころか「点検対象0」を明確なfailで止めており、サイレント成功は再現しなかった。
3. **候補group `proxmox`外のホストがplayに混じった場合 — 引き続き拒否されることを確認(rc=2、新たな穴なし)**:
   ```
   TASK [... Assert required inputs and calling convention] ***
   fatal: [decoy_a -> localhost]: FAILED! => {"assertion": "ansible_play_hosts_all | difference(groups[prn_candidate_group]) | length == 0",
     "msg": "...play hosts: ['decoy_a', 'decoy_outsider'], group members: ['decoy_a']"}
   RC=2
   ```
   部分集合チェックへ緩めても、候補group外ホストの混入は従来どおり拒否される(`difference(...) | length == 0`は「play hostsがgroupに含まれる」ことを要求し、逆方向の混入は防ぐ)。

### Suggestions

**解消(2026-07-30再照合)。**

| # | File | Line | Suggestion | Category | Status |
|---|---|---|---|---|---|
| 1 | `roles/proxmox_reachable_nodes/tasks/main.yml` | 64-68(T2 `ansible.builtin.ping`) | ~~T2に明示`become`が無く呼び出し側の`become: true`を継承し、sudo障害が「到達不能」に畳み込まれる~~ → **Resolved**。T2に`become: false`が明示された。`ping`はSSH接続性の確認のみでrootを要さないため妥当。plan §1-1に「T2を`become: false`にする理由」として明記され、「SSHは通るがsudoが壊れているノードは点検role本体のタスクで失敗する — それは『未点検』ではなく本物の障害であり、そちらの形で出るのが正しい」という設計意図が記録されている。差分(`git diff --cached`)で`become: false`が該当タスクに付いていることをfile:lineで確認した。 | correctness / observability | **Resolved** |

### What Looks Good

- **`meta: end_host`のrole内動作、および全滅時ガードのdead code化回避(T5→T6順)**: リポジトリの実role code(`ANSIBLE_ROLES_PATH`経由、自分で新規に組んだdecoy)で独立に確認した。混在(1台unreachable)ケースはrc=0、`prn_unreachable_nodes=['decoy_unreachable']`、unreachable側は以降のタスク(`post_tasks`含む)へ進まない。全滅(2台ともclosed port)ケースはrc=2、`Fail if no candidate node is reachable`が正しく発火し、msgに両node名と`=False`が入る(`ガードがdead code`という懸念は再現しなかった)。
- **`selectattr('prn_reachable', 'equalto', false)`(`playbooks/proxmox_patch_weekly_full.yml:139`)の成立**: repo内に前例の無いパターンだが、weekly fullのStep 1b該当タスクをverbatimコピーしたdecoy(Step1aで1node unreachable→end_host、Step1b新規playで`prn_reachable`をhostvars越しに参照)で独立実行し、rc=2・`_wf_unreachable=['decoy_unreachable']`・想定どおりのmsgを確認した。`set_fact`がcontroller側で完結しplay境界を越えて持続すること、`fail`/`assert`がconnection-less action pluginでunreachable nodeへ新規接続しないことも合わせて確認できた。
- **AC5(バイト単位一致)**: 3 roleのsummary Jinjaを読み合わせた。healthcheck/hw_checkの追加ブロックは`{% if -%}...{% endif -%}`のtrimマーカー付きで、`prn_unreachable_nodes`が空のとき文字どおり何も出力しない。snapshot_checkの`:126`は`{% if %}...{% endif %}`をSUMMARY行内にインラインで置いており、falseのとき除去される範囲はUnchecked区間のみで前後の`" | "`区切りは変わらない。既存の`Result=`直後append・WARNING/CRITICAL分岐と同じ慣行に沿っており、構造上バイト不一致は生じない。
- **dryrun経路(R5/§1-4)への非波及**: `playbooks/proxmox_patch_dryrun.yml`・`roles/proxmox_patch_dryrun/`に`prn_unreachable_nodes`/`prn_reachable`への言及が無いことをgrepで確認(0件)。3 roleとも`| default([])`を使っており、`include_role: proxmox_healthcheck`経路の出力は不変。
- **weekly fullの`:46`(`hosts: "{{ groups['proxmox'][0] }}"`、ADR-006関連の非ゴール)とU3の追加箇所(Step 1b、`:127`以降)は別のplayで衝突しない**ことをファイル現物で確認した。
- **重複・再利用(duplication-reuse-check)**: probeロジック(ping+set_fact)が`roles/proxmox_exec_node`と重複しているが、ADR-008 Options (a) に「消費者が3つ目に増えるまでは共通化しない」という明示的な判断根拠があり、受容範囲と判断した。
- **セキュリティ観点**: 新規・変更タスクはすべて`ping`/`assert`/`fail`/`set_fact`/`meta`のみで、`shell`/`command`/`lookup()`を使わない。`prn_caller`はplaybook側`vars:`のリテラル文字列で外部入力を経由しない。`no_log`要否・機密変数の露出・`delegate_to`と信頼できない変数の組み合わせのいずれにも該当なし。
- **Policyの一貫性(SB-095 vs SB-021・SB-022・SB-094・SB-027/028/032)**: SB-095の**文面**自体はSB-027/028/032(apply側の両node要求)を明示的に緩めない旨を書いており、SB-094(dry-run側の単一node許容)とも役割が分離されていて文言上の矛盾は無い。**T1の緩和により、実装とSB-021(`--limit`許可)の矛盾も解消した**(AC7で再照合ログ1・2のとおり確認)。
- **(再照合)コメント3箇所の記述とT1の実際の要求の一致**: `tasks/main.yml`冒頭の呼び出し規約コメント(「候補group(既定 prn_candidate_group)の**部分集合**を hosts: に持つ Play」「`--limit <node>`による単一node実行...も正しく通る」)、`defaults/main.yml`の`prn_candidate_group`コメント(「呼び出し側Playの hosts: はこのgroupの部分集合であること」「厳密一致ではない」)は、いずれもT1のassert式(`difference(...) | length == 0` かつ `length > 0`)が実際に要求している内容(部分集合・非空)と一致することをfile:lineで確認した。plan §1-1の「T1を部分集合にする理由」「T2を`become: false`にする理由」の追加節、requirement AC7、ADR-008 Consequences(:68行)もすべて同じ設計変更を指しており、齟齬は無い。
- **AC7(`--limit <1ノード>`実行、requirement §7)**: decoyでの独立実行により、到達可能な1台への`--limit`はrc=0で通り、到達不能な1台への`--limit`はT5がサイレント成功にせずrc=2で明確に止まることを確認した(再照合ログ1・2)。

## 契約との対応(plan §1 U1〜U4、逐語照合)

- **U1(§1-1新role・§1-2 Q2)**: T1〜T6は逐語どおり実装されている(file:line照合済み)。Q2(`meta: end_host`のrole内動作)は独立検証で機能を確認、fallbackは不要と判断してよい。**T1(部分集合assert)・T2(`become: false`)は、前回差分レビューのCritical #1・Suggestion #1を受けてplan §1-1が改訂され、実装がそれに逐語で追随している。再照合により両方の解消を確認した(上記Critical Issues / Suggestions節)。**
- **U2(§1-3・§1-4)**: 3 playbookの`roles:`先頭挿入・`ignore_unreachable: true`・summary条件付きappendはすべて契約どおり。plan_reviewでCritical化されたsnapshot_checkの挿入仕様は、`2026-07-30_003_plan_review.md`の指摘どおり修正された形(`:125`単一行の差し替え)で実装されており、齟齬は無い。
- **U3(§1-5)**: weekly full Step 1bのゲートは、msg・vars・`run_once`/`delegate_to: localhost`/`become: false`/`when:`を含め逐語一致。停止条件を変えずmsgだけを足すという設計意図どおりに機能することを独立検証で確認した。
- **U4(§1-6)**: SB-095の文面はplan §1-6のとおり一字一句一致。変更履歴1行も追加されている。Yoshinobu未承認のままcommitされていないことも確認した(`git status`相当はCoordinator側の管理下にあり、本レビューでは変更していない)。

## 未確認事項(Testerへの申し送り)

- 3 playbook本体(実際のcopy/command部分を含むtasks)を通した実機AC1〜AC3・AC5は、pve1停止中の実環境でのみ確認可能であり、本レビューでは実施していない(実施対象外、Testerの担当)。
- AC7(`--limit`)は本レビューでロジック断片(role単体のdecoy実行)としては確認したが、`proxmox_healthcheck.yml`本体を`--limit`付きで通す最終確認はTesterの担当。
- Suggestion #1は解消したが、broken-sudo(SSHは通るがsudoが壊れている)という周辺ケースが「点検role本体のタスクで本物の障害として出る」という設計意図どおりに見えるかどうかは未検証のまま(意図的に壊れたsudo環境を要し、本レビューのdecoy方針の範囲外と判断した)。findingとしては解消済みのため今回はTesterへの必須申し送りとしない。

## 自己検証(reviewer.md「成果物と返却先」への対応)

- Critical Issues #1(解消): file:line実在確認済み(`roles/proxmox_reachable_nodes/tasks/main.yml:51-52`、`docs/ai/policies/proxmox_patch_policy.md:93`、`docs/ai/reviews/proxmox_readonly_check_single_node/2026-07-30_002_plan.md:50`、`docs/ai/adr/008-proxmox-readonly-check-unreachable-node.md:68`)。根拠は独立実行ログ3件(再照合ログ1〜3、上記)。
- Suggestions #1(解消): file:line実在確認済み(`roles/proxmox_reachable_nodes/tasks/main.yml:64-68`)。根拠はdiff上の`become: false`明記とplan §1-1「T2を`become: false`にする理由」の読み合わせ(broken-sudoの実行検証は範囲外、上記「未確認事項」に明記)。
- plan §1のU1〜U4各要素について、file:line単位で1件ずつ契約と差分を照合した(上記「契約との対応」節)。

## Verdict

**Approve。**

理由: 前回のRequest ChangesはCritical #1(`--limit`回帰)1件のみが根拠であり、今回の差分(`roles/proxmox_reachable_nodes/`のみ、T1を部分集合assertへ緩和、T2に`become: false`明示、関連コメント3箇所を追随)と契約側の改訂(plan §1-1追加節、requirement AC7、ADR-008 Consequences)を独立実行で再照合した結果、Critical #1・Suggestion #1とも解消を確認した。緩和によって新たな穴(候補group外ホストの受理、`--limit`単一node不到達時のサイレント成功)が開いていないことも独立検証で確認した(再照合ログ2・3)。前回確認済みだったU1(T1〜T6本体ロジックのうちT1・T2以外)・U2(3 playbook適用・summary)・U3(weekly fullゲート)・U4(SB-095文面)には変更が無く、再検証の必要も無い。

**blocking findingsの有無**: なし。
**重大度別件数**: Critical 0(前回1件、解消) / Suggestion 0(前回1件、解消)。
**Testerへ進めてよいか**: 可。AC1〜AC7の実機・decoy検証へ進めてよい。
