# Review: norm_docs_slimming (core.md / role-routing-index.md / CLAUDE.md / status.md)

対象: 未staged diff 4ファイル(`CLAUDE.md`、`docs/ai/core.md`、`docs/ai/role-routing-index.md`、`docs/ai/status.md`)。
実装者: Coordinator自身。独立レビュー(別subagent)。観点: `skills/document-norm-review/SKILL.md`。
対象実装は変更していない。git addもcommitも行っていない。

## Summary

blocking 1件。旧`docs/ai/core.md`「安全機構がブロックしたとき」節にあった、`permissions.defaultMode`と`autoMode`が両方揃わないと機能せず、片方欠落時の症状が「安全側に壊れて気づかれにくい」という規範(2026-07-28の実インシデントに基づく)が、新版core.mdから移設先無しで消滅している。しかも`docs/ai/roles/coordinator.md:148`が今もこの内容をcore.mdの当該節から読めると明示的に主張しており、宙ぶらりん参照になっている。

その他はnon-blocking 2件(手順循環の軽い読みにくさ、role-routing-index.mdとcoordinator.mdの過渡的な主張の食い違い)。「作業開始時に何を読むか」の3文書間の順序(core.md→自分のRole文書→role-routing-index.md)は一致しており矛盾なし。旧体制節・Tier常駐表現などの掃引も、4ファイル内および他文書からの参照ともに残存ゼロを確認した。

## Blocking

### B1. `docs/ai/roles/coordinator.md:148`が指す内容がcore.mdから消えている(宙ぶらりん参照 + 規範消失)

- 旧core.md(`git show HEAD:docs/ai/core.md`)「安全機構がブロックしたとき」節の最終段落:
  > **この機構は `.claude/settings.json` の `permissions.defaultMode` と `autoMode` の両方が揃って初めて機能する**(値そのものは同ファイルが正本であり、ここへ写さない)。片方が欠けたときの症状は「確認プロンプトが増える」という**安全側の壊れ方**で、2026-07-28に実際に `defaultMode` の書き漏らしが約2日間気づかれなかった。**壊れていても異常に見えない**ため、この層を変更したときは症状ではなく設定そのものを確認する。
- 新core.md該当節(`docs/ai/core.md:26-35`)にはこの段落が無い。残っているのは以下1文のみ:
  > 根拠と実例: `docs/ai/memory/lessons/permission-boundaries-must-be-designed-not-prompted.md`。強制している設定そのものは `.claude/settings.json` が正本であり、値を文書へ写さない。
- 引用先のlessonファイルを確認したが(`docs/ai/memory/lessons/permission-boundaries-must-be-designed-not-prompted.md:91`)、そこには「`docs/ai/core.md`「安全機構がブロックしたとき」も参照」と**core.md側へ差し戻す**記述しかなく、実体を持たない。
- リポジトリ全体を該当文言("壊れていても異常に見えない"、"両方が揃って初めて機能する"、"症状ではなく設定そのものを確認")でgrepし、他のどこにも残っていないことを確認した(移設先なし)。
- しかし`docs/ai/roles/coordinator.md:148`(今回のscope外だが現行の正本)は今もこう書いている:
  > `.claude/settings.json` — 上記の承認境界を実際に強制している機構(`permissions.defaultMode` と `autoMode`)。**設定そのものが正本**であり、値を文書へ写さない。両方が揃わないと機能しないこと、および欠けたときの症状が気づかれにくいことは `docs/ai/core.md`「安全機構がブロックしたとき」
- 指し先の節見出しは現存するが、指されている内容自体が消えている。**「守るべきこと」の中でも実際に2日間気づかれなかった実インシデントに基づく行動規範("この層を変更したときは症状ではなく設定そのものを確認する")であり、理由説明ではなく規範本体**。ユーザー指示は「理由を多く書く必要はない」であって、規範そのものの削除は求めていない。
- 対応案: 新core.mdの当該節へ1〜2文で復元するか(例: 「`permissions.defaultMode`と`autoMode`は両方揃って初めて機能する。片方が欠けても症状に出ない — 設定変更時は症状でなく設定そのものを確認する。」)、coordinator.md:148の参照先をlessonファイル単体に張り替えたうえでlessonファイル側に実体を持たせる。どちらでもよいが、現状は両方とも実体を持たない状態。

## Non-blocking

### N1. core.md「作業時に読む情報」のstep 2とstep 3の順序が字義通りには循環する

- `docs/ai/core.md:65`(step 2)「自分のRole文書 `docs/ai/roles/<role>.md` を読む」がstep 3(`docs/ai/role-routing-index.md`でidentity→Role対応を確認)より先に来ている。自分のRoleが何かを解決する前に自分のRole文書を読めという順番になっている。
- 実害は無い: Coordinator自身は`CLAUDE.md`が直接「この対話セッションはCoordinator」と埋め込んでいるため解決不要であり、subagentは`docs/ai/role-routing-index.md`「作業開始時の解決手順」4(`docs/ai/role-routing-index.md:79`)によりCoordinatorが起動時に対象Role文書を読み込ませて渡すため自己解決の必要がない。
- 参考までに指摘。文言を「(Coordinatorから渡されている場合はそれを読む。無い場合はrole-routing-index.mdで解決してから読む)」のように1句足すと自己完結する。

### N2. role-routing-index.mdの「Coordinatorの裁量」表現と、coordinator.mdの現行Tier必須シーケンスが過渡的に食い違う

- 新`docs/ai/role-routing-index.md:13`「Coordinator」行: 「**どこまで分解し誰へ委任するかはCoordinatorの裁量である**(2026-07-31、Yoshinobu明示)」。
- 一方`docs/ai/roles/coordinator.md`(今回scope外、未改訂)は「Tier 3/4 必須シーケンス(省略しない)」「計画受領ゲート(5項目)」など、Tier番号に紐づいた強制手順を今も規定している。文字通り読むと「裁量」と「必須シーケンス」が矛盾する。
- 致命ではない: `docs/ai/role-routing-index.md`自身の「正本の優先順位」表(`docs/ai/role-routing-index.md:70`)が「Role本文(責任・権限・成果物・禁止事項)は`docs/ai/roles/<role>.md`が正本、本indexはその要約」と明記しており、競合時はcoordinator.mdが勝つと読める。また`docs/ai/status.md:54,58`がこの未反映状態を次の作業単位として既に追跡している。
- ただし「本indexの要約」という位置づけを知らずにこの1行だけを読むと、Tierゲートが撤廃されたと誤読しうる。次の作業単位(roles/*.md削減)でcoordinator.mdを改訂する際、この行の文言を先に書き換えたことによる過渡的な読み違いリスクとして申し送っておく。

## 掃引で確認し、残存ゼロだったもの(What Looks Good)

- `grep -rn` で以下の撤回文言が4ファイルおよび`docs/`, `skills/`, `.claude/`全体に残っていないことを確認: 「常駐マルチプロセスtrio体制」「旧体制」(規範文書としての現用箇所には無し、過去の`docs/ai/reviews/`記録内のみ=許容)、「Tierに応じてその場で実行」、「Tierと呼び出し方針」(delegation-tier行削除後の再言及なし)。
- `docs/ai/core-migration-map.md`・`docs/ai/role-context-matrix.md`は現存し、今回の削除箇所を指す壊れた参照は無い(`core.md`のstep 5が指す`core-migration-map.md`はそのまま現存・無変更)。
- `skills/subagent-briefing/SKILL.md:77`の`docs/ai/core.md`「安全機構がブロックしたとき」引用は、新core.mdに残っている部分(別の形で到達しない/事実を報告に含める)だけを要約しており、これは壊れていない(B1で指摘した消えた段落とは別の内容)。
- 「作業開始時に何を読むか」の記述はCLAUDE.md/core.md/role-routing-index.mdの3箇所とも「core.md → 自分のRole文書(+Coordinatorはstatus.md) → role-routing-index.md」の順で一致しており、矛盾はない。
- 人間の権限と安全境界節から削除された「承認済みscope内で個々の操作がその範囲に収まっているかを判断する責任はCoordinatorにある」「scope外または判断がつかない場合はCoordinatorもYoshinobuへ上げる」は、文言としては消えたが実体は`docs/ai/roles/coordinator.md`「実ホストへの非冪等操作の承認」表(特に「scope外/不明なら停止してYoshinobuへ」の行)に既存であり、core.md側の1文「判断の3分類は`docs/ai/roles/coordinator.md`「実ホストへの非冪等操作の承認」を正本とする」で正しく到達可能。消失ではなく移設として妥当。
- status.mdの`Reviewer / Testerの effort=medium 試行`行の「次にTier 4を回すとき」→「次に逐行照合を伴うレビュー・検証を回すとき」への言い換え、および`proxmox_hw_check.yml`行・`context-classification.md`行末尾の「Tier 1」除去は、Tier概念廃止の指示と整合し内容の実質を損なっていない。
- core.md末尾の「変更履歴」表の削除と「本ファイルへ事故の経緯や日付つきの履歴を書かない」という新規則は、Yoshinobuの明示指示(「理由を多く書く必要はない」)と一致し、この指示はcore.md限定である(role-routing-index.mdは対象外)ため、role-routing-index.md側に残る日付付き根拠(2026-07-31など)との不整合ではない。

## 未解決事項

- B1の復元方法(core.md本文へ短く書き戻すか、lessonファイルへ実体を持たせるか)はCoordinatorの判断に委ねる。
- N2は次の作業単位(`docs/ai/roles/*.md`削減)で自然に解消される見込みだが、それまでの間にこの1行だけを読んで「Tierゲートは任意になった」と誤解しないよう、状況を認識しておくこと。
