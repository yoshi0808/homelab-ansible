# audit: `tester_mode` の完全撤廃 — R10(移行assertの撤去とTS-003/TS-004の削除)

日付: 2026-08-02 (JST)
対象: `docs/ai/reviews/tester_mode_full_removal/2026-08-02_008_requirement_r10.md` 〜 `2026-08-02_011_test_result_r10.md`
位置づけ: 001〜007(前半)は`007_audit.md`で条件付き受入済み・クローズ済み。本監査は後半(R10)のみを再検査対象とする。前半は背景として参照した。
確認した手段:

- `008_requirement_r10.md`〜`011_test_result_r10.md`を番号順に全文読解。`007_audit.md`(前半のクローズ判定とCoordinatorのクローズ対応)を背景として読解。
- `git status --short` / `git diff --staged --stat`で、記録が主張する変更範囲(34ファイル: playbook29 + Policy1 + 案件記録3新規 + status.md)と実差分を突合。
- `git diff --staged -- playbooks/`全体、`docs/ai/policies/ansible_test_safety_policy.md`、`docs/ai/status.md`の実差分を通読し、削除境界が疑わしい`proxmox_evacuate_node.yml`(`quiet: true`混入修正の主張)・`ubuntu_vm_full_upgrade.yml`(play丸ごと削除の主張)・`ca_trust_deploy.yml`/`recovery_ha_failover.yml`(pre_tasksキー削除/task単独削除の主張)を個別に確認(状態を変えない確認)。
- `grep -rn "tester_mode" playbooks/ roles/ inventories/ scripts/ docs/ai/policies/ docs/ai/context/ docs/ai/roles/ skills/`を自分で実行し、AC1(ヒットは各Policyの変更履歴節のみ)を実測確認。
- `bash scripts/check-tester-gate.sh`(AC2)、`bash scripts/git-pre-commit-check.sh`(AC5)を自分で実行し、記録どおりOK(rc=0)であることを確認。
- Python `yaml.safe_load_all`で対象29ファイルを独立に再パースし、`pre_tasks`/`tasks`/`post_tasks`/`handlers`/`roles`の空リスト化・None化がないことをAC3として独立確認。
- `grep -rln "TS-003\|TS-004"`をrepo全体(policies/roles/skills/playbooks)へ実行し、Policy本文以外に節削除後の残存参照が無いことを確認。
- `docs/ai/memory/incidents/2026-08-02_recovery-probe-drill-blocked-by-tester-mode-assert.md`(前半で「解決済み」化された記録)を再読し、内容が前半監査の指摘反映と矛盾しないことを確認。
- `docs/ai/status.md`全文と、staged diffの該当箇所を確認(状態の正本との突合)。
- 実ホストへは到達していない。

## 再構成可否

**再構成できた。** requirement(008、前提条件の充足を明記)→implement(009、削除パターン3分類・自己発見した`quiet: true`混入ミスの修正過程まで記録)→review(010、Approve・findings 0)→test_result(011、両記録の「検証済み」主張自体を検査対象として全AC再実行、PASS)という経緯が、各ファイルの相互参照だけで辿れた。008の前提条件表は007のCoordinatorクローズ判断が`docs/ai/status.md` Nextへ記録した条件(「撤去してよいのは、撤廃済みのコードが本番へ反映され、tester_modeを渡す経路が存在しないことを確認した後」)を正しく引き継ぎ、確認済みにしたうえで着手している。

静的検証(`grep`件数、YAML構造、`check-tester-gate.sh`/`git-pre-commit-check.sh`の実行結果、Policy節削除後の参照残存有無)はすべて記録の主張どおりに再現し、実質的な食い違いは無かった。

## 指摘

### 1. `009_implement_r10.md`のAC1自己検証、件数表記と列挙内容が食い違う — **クローズを妨げない**

009の「2. 自己検証」表AC1行は「ヒット2件」としながら、括弧内に`ansible_test_safety_policy.md`の2026-07-06〜07行・今回追加した2026-08-02行・`proxmox_backup_restore_verify_policy.md`の2026-07-26行の**3件**を列挙している。実測(本監査・010・011いずれも一致)は3件であり、正しいのは列挙の方で「2件」という数字が誤り。

後続の010(独立再検証で「変更履歴節の3行(既存2行+今回追加1行)」と正しく計上)と011(実測3件を示し、009の「2件」との差を「対象パス集合の差ではなく、実装時点でまだ存在しなかった追加行を含むため」と説明)がこの食い違いを認識・reconcileしており、AC1のPASS判定自体は3記録とも実測と一致した正しい結論に至っている。実害は無く、記録の自己完結した文中の計数ミスに留まるため、クローズを妨げない。

## 状態の正本との突合

`docs/ai/status.md`のNext表に、007のCoordinatorクローズ判断が追加した行「**29本の移行assertの撤去(R10)と、それに伴うTS-003の削除**」が、R10着手前の内容(「撤去してよいのは…確認した後である」という条件待ちの記述)のまま残っている。R10はrequirement→implement→review→test_resultの全工程を経てAC1〜AC5全てPASSしており、現状と一致していない。**これは指摘ではない**(Auditorは該当行を消す前に起動するため、除去は次のとおりCoordinatorが同一セッションで行う)。

同じNext表内の「通知抑止の既定を反転させるか」行は、今回のstaged diffで`tester_mode`撤廃を反映済みの記述へ更新されており、現物(`roles/common_slack/tasks/notify.yml`に`tester_mode`が無いこと)と一致することを確認した。「`pvesh`が関わる本番への検証方法」行はR10と無関係で、現状も変化なく整合している。

## 除去すべき行(実施事項、指摘ではない)

- `docs/ai/status.md` Next「**29本の移行assertの撤去(R10)と、それに伴うTS-003の削除**」— クローズと同時に削除。

## 総合判定

**受入。**

クローズを妨げる指摘は無い。指摘1は記録済みの事実として残すのみでよい(009本文の訂正は要求しない)。除去すべき行1件はCoordinatorが同一セッションで対応する。

---

## Coordinatorによるクローズ判断(2026-08-02)

**受入。R10をもって案件全体をクローズする。**

| 指摘 | 対応 |
|---|---|
| 1(`009_implement_r10.md` のAC1自己検証の件数が「2件」と誤記) | **訂正しない。** 後続の010・011が独立に実測して3件へreconcileしており、記録全体としては正しい結論に着地している。実装記録を遡って書き換えると、**独立検証が誤りを捕まえたという経緯そのものが消える**。誤記が残っていること自体が、この工程が機能した証拠である |
| 除去すべき行(`docs/ai/status.md` NextのR10行) | **削除した。** |

### 本番反映で確認したこと

R10の前提(assertを外しても`tester_mode`を渡す経路が無い)は、リポジトリだけでなく**配備物で確認した**。

| 対象 | 撤去前 | 撤去後 |
|---|---|---|
| quory `/usr/local/sbin/incident-capture-collector.py` | `tester_mode` 1件(旧版) | 0件(2026-08-02 10:13配備) |
| ansy / quory `/usr/local/sbin/recovery-probe.py` | `tester_mode` 4件 / `drill` 23件(mtime 07-29) | ともに0件(2026-08-02 10:20配備) |

**「repoを直しても配備物は古いまま」という形が、この案件だけで2回出た。** どちらも `docs/ai/status.md` のWatchで拾えたのではなく、Coordinatorが実機を見て気づいた。撤廃系の案件では受入条件に**配備物の棚卸し**を含めるべきで、`skills/requirements-analysis/` への追加候補として `docs/ai/status.md` Next に残す。

### 残存リスク

**Semaphoreのテンプレートは repo外でAIから確認できない。** assertは2026-07-08から存在し、`-e tester_mode=true` を渡すテンプレートがあれば当該ジョブはその日から毎回rc=2で赤くなっていたはずであるため、定期ジョブ側には無いと判断した。**残るのは2026-07-08以降一度も実行されていない手動テンプレートのみ**で、そこに該当があれば、撤去後は停止せずに本適用となる。Yoshinobuへ申し送り済み。
