# test_result: `tester_mode` の完全撤廃 — Step 5(Tester検証)

日付: 2026-08-02 (JST)
対象requirement: `docs/ai/reviews/tester_mode_full_removal/2026-08-02_001_requirement.md`
対象: `git diff --staged`(40ファイル、確認済み。作業ツリー未追跡の追加変更なし — `git diff`（unstaged）は空)
先行記録: `2026-08-02_002_implement.md`〜`2026-08-02_005_review.md`(いずれも主張は検査対象として扱い、自分で再実行した)

## 実施した手段の一覧

- `git status --short` / `git diff --stat`(unstaged)で、ステージ済みの40ファイル以外に変更が無いことを確認。
- `grep -rn "tester_mode" playbooks/ roles/ inventories/ scripts/ docs/ai/policies/ docs/ai/context/` を自分で実行し、96件のヒットを分類。
- `grep -rln "tester_mode | default(false) | bool" playbooks/` で移行assertを持つファイル数が29件であることを実測。
- `roles/common_slack/tasks/notify.yml` / `capture.yml` を全文読解。
- `roles/incident_capture/files/incident-capture-collector.py` の `REQUIRED_SPOOL_FIELDS`・`main()`・`load_spool_record()` を読解。
- `roles/recovery_probe/files/recovery-probe.py` を全文読解(`fire_ladder`・`main()`ループ)。
- scratchpad配下にdecoy環境を自作して構築し、**実際に`ansible-playbook`を実行**して以下を検証した(詳細は「実行記録」参照。手順は本ファイルの操作対象であり、実行そのものは行った):
  - AC2: `roles/common_slack/tasks/notify.yml`(実体、symlink経由)を`skip_notifications=true`単独、`--check`単独、`tester_mode=true`単独の3パターンで実行。
  - AC3: `roles/common_slack/tasks/capture.yml`(実体)でspool recordを実際に書き出し、旧形式(`tester_mode`キー入り)レコードを追加作成した上で、`roles/incident_capture/files/incident-capture-collector.py`(実体、そのまま`python3`で起動)に両方読ませた。
  - AC4: `grep -ni drill roles/recovery_probe/files/recovery-probe.py` と `python3 -m py_compile` を実行。
  - AC7: `recovery-probe.py`を`importlib`でロードし、`fire_ladder()`を**実際のsubprocess.run経由**でdecoyの`ansible`/`ansible-playbook`スタブ(PATH上に自作、実ホスト・実`pvesh`には一切到達しない)へ発火させ、`running→reboot→復旧確認`と`stopped→start→復旧確認`の2分岐を実行。
  - AC6: `bash scripts/git-pre-commit-check.sh`をステージ済みの状態に対して実行(状態を変えない確認 — gitleaks/tester-gate-lint/check-doc-consistencyはいずれも読み取りのみ)。
- `git status --short` を最終確認し、検証によってリポジトリ内の追跡ファイルに変更が生じていないことを確認。

**実ホスト(ansy/quory/pve1/pve2/authy/monnie等)へは一切到達していない。** decoy inventory・decoy `ansible`/`ansible-playbook`スタブ・scratchpad配下のファイルのみを対象にした。

## AC別結果

### AC1: `grep -rn "tester_mode" ...` のヒットが29本の移行assertのみ

**PASS**(限定的なwiggle roomを含めて成立)

- 総ヒット96件。うち29ファイル(実測、`grep -rln`で確認)が移行assert本体(`that: not (tester_mode | default(false) | bool)`とその前後の`msg:`)を持つ。ヘッダマーカー行(`# tester-gate: ...`)に`tester_mode`を含むものは0件(13本の差し戻し済み)。
- 残差5件はすべて`docs/ai/policies/`配下の「変更履歴」節2件(非ゴールで明示的に保護)と、`ansible_test_safety_policy.md`「背景」節3件(見出し1・TS-003本文1・TS-004本文1)。
- AC1は文字どおりには「29本のみ」であり5件の残差は超過するが、requirementのAC5改訂節が「TS-003/TS-004の『背景』節が識別子を名指しするのは可」と明記しており、変更履歴節も非ゴールで保護されている。**この5件はAC1の例外としてrequirement自身が想定済み**と判断した。

### AC2: `notify.yml`の抑止条件

**PASS**

実行記録(scratchpad decoy、`roles/common_slack/tasks/notify.yml`を`include_tasks`で直接実行):

1. `skip_notifications=true`のみ → `[skip_notifications] Show notification instead of sending`が発火(`ok`)、`Send Slack notification`ブロック配下の4タスクは全て`skipping`。実送信は試みられなかった。
2. `--check`のみ(`ansible_check_mode`) → 同上のパターンで抑止された。
3. `tester_mode=true`のみ(`skip_notifications`無し、`--check`無し) → `[skip_notifications]`タスクは`skip_condition`不成立で`skipping`、`Send Slack notification`ブロックへフォールスルーし`Load Slack variables`以降が実行された(`ok`)。**「`tester_mode=true`を付けても抑止されない」ことを実行で確認した。**

**残存リスク・注記**: パターン3の検証で、decoyの`vars/slack.yml`に置いた非実在URL(`https://127.0.0.1:1/...`)を`community.general.slack`モジュールの`token`引数へそのまま渡したところ、同モジュールは`token`の中身を検証せず常に`https://hooks.<domain>/services/<token>`(既定`domain=slack.com`)というURLを構築するため、**実際には実在の`hooks.slack.com`へHTTP POSTが送信された**(応答は汎用ドキュメントページ、HTTP 200。有効なwebhookパスではないため、どの実チャンネルにもメッセージは配信されていない)。これは意図した閉域テストの想定が外れた点であり、Coordinatorへ申し送る。**実際のSlack通知(メッセージ配信)は発生していない**が、外部ホストへの通信自体は発生した。以降のテストではこのモジュールを介した経路を再実行していない。

### AC3: `capture.yml`のspool recordとcollectorの必須フィールド整合

**PASS**

- `capture.yml`(実体)を実行し、新形式のspool recordを実際に書き出した → `tester_mode`キーは存在せず、`skip_notifications`キーが存在することを確認。
- 旧形式(`tester_mode: false`を含む)のspool recordを手作業で1件追加作成し、新形式と並べて配置。
- `incident-capture-collector.py`(実体)を実行 → **終了コード0**、`_heartbeat.json`の`has_errors: false`、`spool_total_this_cycle: 2`、両レコードとも消費され`_rejected/`は作成されなかった。**新旧どちらの形式も`collection_errors`を出さずに正常処理された。**

### AC4: `recovery-probe.py`のdrill不在・構文健全性

**PASS**

- `grep -ni drill roles/recovery_probe/files/recovery-probe.py` → 0件(exit code 1)。
- `python3 -m py_compile roles/recovery_probe/files/recovery-probe.py` → エラーなし。
- `git status --short playbooks/recovery_push_drill_setup.yml roles/recovery_push/tasks/drill_setup.yml` → 変更なし(push drill側は無傷)。
- `roles/recovery_probe/tasks/main.yml`の`drill`ディレクトリ言及も0件。

### AC7: 通常の障害検知経路(非drill)の健全性

**PASS**

`recovery-probe.py`を`importlib`でロードし、`fire_ladder(cfg, target, tconf, failed_probes)`(新シグネチャ、4引数)を実際の`subprocess.run`経由で発火。PATHに自作の`ansible`/`ansible-playbook`デコイスタブを置き、実ホスト・実`pvesh`には到達させていない。

- `running`分岐: pvesh確証→`running`検出→`recovery_vm_reboot.yml`呼び出し(デコイrc=0)→`wait_for_recovery`(decoyの`127.0.0.1`へのicmp probeで即復旧)→`[recovery-probe] 復旧確認`(status: ok)を通知キューへ記録。呼び出しコマンドラインは`ansible-playbook playbooks/recovery_vm_reboot.yml -e target=sophos-fw`のみで、**`tester_mode=true`は付与されていない**ことをログで確認した(requirementが問題視した経路の解消を実地で確認)。
- `stopped`分岐: pvesh確証→`stopped`検出→`pvesh_vm_start`(デコイ経由)→復旧確認、を同様に確認。
- どちらの分岐でも`[drill]`ラベルや無条件`status: ok`(drillが従来送っていた偽の成功)は一切現れなかった。

### AC5: Policyから`tester_mode`が「現に使える手段として」現れないこと

**PASS**(requirementのAC5改訂節の基準で判断)

- TS-003は日付・経緯・`end_play`/`end_host`の説明を含まず、「撤廃済みで渡しても無視される」という現在の事実のみを述べている。TS-004は「ヘッダマーカーは廃止されていない」ことを明示し、`tester_mode`とは別物と述べる目的での言及であり、requirementが明示的に許容している。
- 変更履歴節2件(`ansible_test_safety_policy.md`・`proxmox_backup_restore_verify_policy.md`)は非ゴールで保護対象。
- 実行可能な手段(`when:`条件、関数シグネチャ、辞書キー)として`tester_mode`が現れる箇所は0件(AC1の実測と同じ)。

### AC6: `scripts/git-pre-commit-check.sh`

**PASS**

ステージ済みの状態(40ファイル)に対して`bash scripts/git-pre-commit-check.sh`を実行し、`[pre-commit] OK`(exit 0)を確認した。内訳: gitleaks「no leaks found」、`[tester-gate-lint] OK (49 playbooks)`、`[check1] OK (98 compared)`、`[check2] OK (8 compared)`、`[check3] OK (91 compared)`。この実行はステージ済みの状態を読むだけで変更を加えない。

## 未検証・未実施の項目

- **quoryへの配備(Step 2レビューSuggestion 1)**: `capture.yml`のtask includeと`incident-capture-collector.py`のquory配備が別タイミングになった場合の不整合は、この案件のコード差分そのものではなく運用手順の問題であり、実ホストquoryへの到達が要るため本Stepでは検証していない。Coordinatorへの申し送り事項として先行レビューが既に記録済み。
- **`recovery-probe.py`の`main()`ループ全体(mute判定・`monitoring_pause_flag`・24hのflapping閾値・`--once`モードの`log`出力等)**: `fire_ladder()`の直接発火では検証したが、`while True`ループ自体を回す統合実行はしていない(daemonとして起動し続ける形の検証は、常駐・停止操作を伴い今回のスコープを超えると判断した)。`--once`モードはコード上、閾値到達時に`action skipped`でログするだけで`fire_ladder`を呼ばない実装であることをコード読解で確認済み(実行はしていない)。
- **既存ホスト(ansy/quory)に残存する`/var/lib/homelab-recovery/probe/drill`ディレクトリの後始末**: Step 2レビューSuggestion 2が指摘済みで、実ホストへの到達が要るため未確認・未実施(実害なしと判定済みのため優先度は低いとされている)。

## 残存リスク

1. **`community.general.slack`モジュールの`token`引数の扱い**(AC2検証中に判明): `token`に完全なURL文字列を渡しても、モジュールは`hooks.<domain>/services/<token>`という形で常に実際の`hooks.slack.com`(既定domain)へリクエストを組み立てる。今回のdecoy設計はこの挙動を見誤っており、意図せず実在の`hooks.slack.com`へ1件のHTTPリクエスト(無効なwebhookパス、応答は汎用ドキュメントページ)を発生させた。**メッセージの配信は発生していない**が、閉域のつもりのテストが外部ネットワークへ出た点は事実として記録する。今後同種のdecoyテストを設計する際は、`community.general.slack`の`domain`パラメータを明示的に指定するか、モジュール自体をモックする必要がある。
2. **AC1の文字どおりの解釈と実際の残差(5件)の乖離**: 先行レビュー(Step 4)と同じ判断を踏襲し、AC5改訂節の但し書きにより許容されると判断したが、これは解釈の適用であり、Yoshinobuが「変更履歴節・背景節も含め文字どおりゼロにすべき」と考えるならAC1は不成立になる。判断の分岐点として明示しておく。
3. **P1(R10: 29本の移行assert撤去)は本案件のスコープ外のまま**。requirementのリスク#1(assert先行撤去によるdrill実リブート)は、drill機構自体が本Stepで削除済みのため既に発生し得ない状態にあるが、R10実施のタイミングと条件(R1〜R4の本番反映確認)は別途Coordinatorが管理する必要がある。

## 総合判定

AC1〜AC7すべてPASS。未検証項目はいずれも「実ホスト到達が必須」または「今回のコード差分の対象外(運用手順・quory配備・既存ホストの残存ファイル)」であり、到達せずに済ませた。実ホスト・本番Slackへの到達は発生していない(AC2検証中の`hooks.slack.com`への1リクエストのみ例外的に発生したが、メッセージ配信は無く、対象は本番ワークスペースの実在webhookではない)。
