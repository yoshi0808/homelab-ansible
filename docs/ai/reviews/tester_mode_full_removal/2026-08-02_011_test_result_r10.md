# test_result: R10 — 移行assertの撤去とTS-003/TS-004の削除

日付: 2026-08-02 (JST)
対象requirement: `docs/ai/reviews/tester_mode_full_removal/2026-08-02_008_requirement_r10.md`
先行記録: `2026-08-02_009_implement_r10.md`(実装)、`2026-08-02_010_review_r10.md`(レビュー、findings 0件)。両記録の「検証済み」という記述はそれ自体を検査対象として扱い、全AC を自分で再実行した。
対象: `git diff --staged`(34ファイル、`docs/ai/status.md`と`docs/ai/reviews/tester_mode_full_removal/2026-08-02_008/009/010`を含む。playbook29本+Policy1本がR10の実装対象)。`git status --short`で staged 以外の変更が無いことを確認、`git diff`(unstaged)は空。

## 実施した手段の一覧(状態を変えない確認のみ)

- `grep -rn "tester_mode" playbooks/ roles/ inventories/ scripts/ docs/ai/policies/ docs/ai/context/ docs/ai/roles/ skills/` を自分で実行(AC1)。
- `bash scripts/check-tester-gate.sh` を実行(AC2)。
- Python `yaml.safe_load_all` で `git diff --staged --name-only` から抽出した playbooks 29本を独立に再パースし、`pre_tasks`/`tasks`/`post_tasks`/`handlers`/`roles` の空リスト化・None化を機械チェック(AC3前半)。
- `ansible-playbook --syntax-check -e target_node=pve1` を `playbooks/*.yml` 全49本へ実行(AC3後半、実ホストへの接続を伴わない構文解析のみ)。
- AC4: `git show HEAD:playbooks/<file>` で撤去前の内容をscratchpadへ書き出し、撤去後(作業ツリー、staged内容と一致)との `ansible-playbook --list-tasks` 出力を比較。`--list-tasks` はplay/taskの列挙のみでホスト接続・モジュール実行を一切行わないため、実ホストへ到達しない。
- `bash scripts/git-pre-commit-check.sh` を実行(AC5)。`git diff --cached` を前提とするスクリプトであり、対象は既にstage済みのため追加のgit操作なしにそのまま実行できた。
- `git diff --staged -- docs/ai/policies/ansible_test_safety_policy.md` を通読し、TS-003/TS-004節の削除・変更履歴追記・退番記法を確認。
- `grep -n "退番"` で `log_observability_policy.md` / `proxmox_operations_policy.md` の先例と突き合わせ、書式一致を確認。

**実ホスト(ansy/quory/pve1/pve2/authy/monnie等)へは一切到達していない。** 本番Slackへの通知も発生していない(`--list-tasks`はタスクを実行しないため、通知系task含め一切のモジュールが起動しない)。

## AC別結果

### AC1: `grep -rn "tester_mode" ...` のヒットが各Policyの「変更履歴」節のみ

**PASS**

実測ヒット3件、すべて変更履歴節内:
- `docs/ai/policies/ansible_test_safety_policy.md:160`(2026-07-06〜07行、既存)
- `docs/ai/policies/ansible_test_safety_policy.md:166`(今回R10で追加した2026-08-02行)
- `docs/ai/policies/proxmox_backup_restore_verify_policy.md:303`(2026-07-26行、既存)

実装・レビュー記録の主張(前者は「ヒット2件」、後者は独立再検証で同数)と概ね一致。件数の差(実装記録の2件 vs 今回の3件)は対象パス集合の差ではなく、実装・レビュー時点でまだ存在しなかった今回追加の変更履歴行(TS-003/TS-004退番の1行)が今回の実行対象に含まれているためで、内容面の齟齬ではない。

### AC2: `scripts/check-tester-gate.sh` がOK(49 playbooks)

**PASS**

`[tester-gate-lint] OK (49 playbooks)`、rc=0。`# tester-gate:`マーカーの喪失なし。

### AC3: YAML構文エラーなし、`pre_tasks`/`tasks`に空リストが生じていない

**PASS**

- 独立実装のPythonスクリプトで対象29ファイル全てパース成功、`pre_tasks`/`tasks`/`post_tasks`/`handlers`/`roles`いずれも空リスト・None化なし。
- `ansible-playbook --syntax-check`を49本全て(変更対象29本+未変更20本)へ実行し全て成功。`proxmox_evacuate_node.yml`/`proxmox_restore_vm_placement.yml`は`target_node`未指定だと`hosts:`のJinja評価でエラーになる既存仕様のため、`-e target_node=pve1`を付与して実行(本変更と無関係の前提条件)。

### AC4: 代表的なplaybookを`--check`相当で実行し、撤去前と同じ挙動(assertが無くなった分だけtask数が減る以外の差が無い)。実ホストへ到達しない範囲で行う

**PASS(`--check`本体ではなく`--list-tasks`による構造比較で成立させた)**

3種類の削除パターンすべてを代表に含めた:

| パターン | 代表playbook | 結果 |
|---|---|---|
| ① `pre_tasks:`キーごと削除(13本) | `ca_trust_deploy.yml` | 撤去前は`[migration] tester_mode is deprecated...`task1本＋roleのtask3本。撤去後はrole task3本のみ。差分はassert1本の消失のみ |
| ② playごと削除(1本) | `ubuntu_vm_full_upgrade.yml` | 撤去前はplay #1(`hosts: localhost`、assertのみ)+play #2(本体)の2play構成。撤去後はplay #2のみ、task内容・順序は完全一致 |
| ③ assert taskのみ削除(15本、うち`ubuntu_nightly.yml`は2箇所) | `cert_renew.yml`、`ubuntu_nightly.yml` | `cert_renew.yml`は7play・移行assert1箇所(play #1内)のみが消え、他6playのtask列は1行も変わらず完全一致。`ubuntu_nightly.yml`は`diff`で2箇所とも`[migration] tester_mode is deprecated...`の1行のみが差分、他は完全一致 |

判断根拠: `--check`を実ホストへ向けて実行すると、`homelab_cert_renew`のようなroleは`ansible.builtin.slurp`/`stat`等で対象ホストへの実接続を要求し、要件の禁則(実ホストへ到達しない)に抵触する。一方`--list-tasks`はplaybookをパースしてplay/task構造を列挙するだけで、対象ホストへの接続やモジュール実行を一切行わない(inventory groupの実在確認すら行わない)。AC4が求める「assertが無くなった分だけtask数が減る以外の差が無い」という主張は、実行順序・task構成という構造レベルの主張であり、`--list-tasks`による構造比較で過不足なく検証できる。`--check`が本来意味する「モジュールが実際に何をシミュレートするか」まではAC4の文言上求められておらず、かつそこへ踏み込むには実ホスト接続が必須になるため踏み込んでいない。

decoy inventoryによる`--check`実行は、`docs/ai/memory/incidents/2026-08-01_tester-slack-decoy-did-not-contain-request.md`が記録する通り「対象モジュールが宛先をどう決めるかを確認しない限り成立を仮定できない」既知の欠陥クラスであり、同incidentは`tester_mode_full_removal`案件そのものの先行AC検証中(2026-08-02、R10以前)にも再発している。今回はこの経路を採らず、ネットワーク到達を構造的に持たない`--list-tasks`を選んだ。

### AC5: `scripts/git-pre-commit-check.sh` をstaged状態で実行しOK

**PASS**

```
[pre-commit] OK
```
gitleaks(staged、leaks 0件)、`check-tester-gate.sh`(OK 49 playbooks)、check1〜3(YAML構文・vault先頭行等、staged差分に対して実行)すべて通過。

**実装記録の記述の訂正**: `2026-08-02_009_implement_r10.md`は「Implementerはgit addを行わないためstage状態を作れず、スクリプトを文字どおりには実行していない」としているが、これはImplementer工程の制約説明として正しい。**Tester工程の時点では対象が既にstage済み**であり、追加のgit操作なしにスクリプトをそのまま実行できた。「AC5は誰も文字どおり実行していない未検証事項」という含みで読むと誤りで、Tester工程で解消される設計として機能している。

## 残存リスク・未検証事項

なし。5件のAC全てを状態を変えない確認手段で自分で再実行し、PASSを確認した。実ホストへの接続、本番Slackへの通知はいずれも発生していない。

## 対象ファイル一覧(自分が読み書きした範囲)

- 読解のみ: `playbooks/*.yml`(全49本、うち差分対象29本)、`docs/ai/policies/ansible_test_safety_policy.md`、`docs/ai/policies/log_observability_policy.md`、`docs/ai/policies/proxmox_operations_policy.md`、`scripts/check-tester-gate.sh`、`scripts/git-pre-commit-check.sh`
- 作業ファイル(scratchpad、リポジトリ外、検証後削除済み): `/tmp/claude-1000/.../scratchpad/before/{ca_trust_deploy,ubuntu_vm_full_upgrade,cert_renew,ubuntu_nightly}.yml`
- 新規作成: 本ファイル
