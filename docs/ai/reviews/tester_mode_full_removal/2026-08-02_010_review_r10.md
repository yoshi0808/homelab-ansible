# review: R10 — 移行assertの撤去とTS-003/TS-004の削除

日付: 2026-08-02 (JST)
対象requirement: `docs/ai/reviews/tester_mode_full_removal/2026-08-02_008_requirement_r10.md`
対象実装記録: `docs/ai/reviews/tester_mode_full_removal/2026-08-02_009_implement_r10.md`
Role: Reviewer
併用Skill: `skills/code-review/SKILL.md`, `skills/duplication-reuse-check/SKILL.md`, `skills/document-norm-review/SKILL.md`

## 対象

未commit差分: playbooks 29本 + `docs/ai/policies/ansible_test_safety_policy.md`。`docs/ai/status.md`と`docs/ai/reviews/`配下は対象外(Coordinator/Implementer記述のため)。

## 確認した手段(状態を変えない確認のみ)

- `git diff -- playbooks/` 全640行を通読(29ファイル30箇所の削除ハンクを個別確認)。
- `git diff -- docs/ai/policies/ansible_test_safety_policy.md` を通読。
- 削除の境界が疑わしい3ファイル(`ubuntu_vm_full_upgrade.yml`の削除play全文、`proxmox_evacuate_node.yml`の`quiet: true`混入修正箇所、`recovery_ha_failover.yml`/`recovery_vm_reboot.yml`のpre_tasks構造)は`Read`で削除後の全文を確認。
- `grep -rn "TS-003\|TS-004"` を`docs/ai/`(reviewsを除く)・`skills/`・`playbooks/`・`roles/`へ実行し、Policy本文・他規範文書への残存参照なしを確認(`docs/ai/memory/incidents/`内の1件は非ゴールで保護された過去記録であり対象外)。
- `grep -n "^#\|<!-- TS-"` でPolicy全体の見出し構造を確認し、節削除後にTOCや相互参照の欠落がないことを確認。
- 独立にPython `yaml.safe_load_all`で対象29ファイルを再パースし、`pre_tasks`/`tasks`/`post_tasks`/`handlers`/`roles`キーの空リスト化・None化がないことを実装者と別経路で確認(結果: 該当なし)。
- `grep -rn "tester_mode" playbooks/ roles/ inventories/ scripts/ docs/ai/policies/ docs/ai/context/ docs/ai/roles/ skills/` を実行し、ヒットが変更履歴節の3行(既存2行+今回追加1行)のみであることを確認(AC1相当の独立再検証)。
- `bash scripts/check-tester-gate.sh` を実行し `OK (49 playbooks)` を確認(AC2相当の独立再検証)。
- `git diff --stat` で30ファイル・1追加/326削除であることを確認し、実装記録の数値と一致することを確認。
- `awk`で削除箇所前後の連続空行(削除漏れ・削除しすぎの兆候)を機械チェックし、該当なし。
- 退番の記法(`退番: XXX(再利用しない)`)を`docs/ai/policies/log_observability_policy.md`・`docs/ai/policies/proxmox_operations_policy.md`の先例と突き合わせ、書式が一致することを確認。

未確認: 実ホストへの到達、`scripts/git-pre-commit-check.sh`のstage状態での実行(制約により行わない。実装記録に記載の通りCoordinator/Testerの領分)。

## 結果

- 29本すべてで、削除は要件の3分類(①pre_tasksキーごと13本 ②play丸ごと1本 ③taskのみ15本)通りに実施されており、削除箇所前後に断片・空セクション・意図しない属性混入は見当たらない。
- `proxmox_evacuate_node.yml`の`quiet: true`混入は実装記録の記述通り修正済みで、直前task(`Assert target_node is pve1 or pve2`)は`fail_msg`行で正しく終端している。
- `pre_tasks:`キーごと削除した13本は、削除前も`pre_tasks`の内容がassert1本のみであったため、削除後のplay実行順序(roles/tasksの先頭からの実行)に変化はない。handler flushタイミングへの影響なし(assert自体がhandlerを持たない)。
- `ubuntu_vm_full_upgrade.yml`で削除したplay(`hosts: localhost`, `connection: local`)は、assertのみを含み`add_host`・`set_fact`・後続playへの変数受け渡しを一切持たない。play削除後もホスト集合・変数の欠落はない。
- `# tester-gate:`マーカーは`check-tester-gate.sh`でOK(49 playbooks)、1本も失われていない。
- Policyの節削除(TS-003/TS-004)後、他の規範文書・Policy本文中に当該節への参照は残っていない。退番の記法は既存2件の先例と一致。
- requirementの非ゴール(`docs/ai/reviews/`等の不変更、`# tester-gate:`マーカー不変、assert削除以外のロジック変更なし)は守られている。

## Findings

なし。
