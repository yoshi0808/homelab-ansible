# review: `tester_mode` の完全撤廃 — Step 2(R1〜R4の差分レビュー)

日付: 2026-08-02 (JST)
対象requirement: `docs/ai/reviews/tester_mode_full_removal/2026-08-02_001_requirement.md`
対象implement記録: `docs/ai/reviews/tester_mode_full_removal/2026-08-02_002_implement.md`
対象差分: `git diff`(未commit)のうち以下6ファイル

- `roles/common_slack/tasks/notify.yml`
- `roles/common_slack/tasks/capture.yml`
- `roles/incident_capture/files/incident-capture-collector.py`
- `roles/recovery_probe/files/recovery-probe.py`
- `roles/recovery_probe/tasks/main.yml`
- `inventories/homelab/group_vars/all.yml`

`docs/ai/status.md` の差分と `docs/ai/reviews/tester_mode_full_removal/`・`docs/ai/memory/incidents/` の未追跡ファイルはCoordinator記録のためレビュー対象外(指示どおり)。

## 確認した手段

- `git diff`(対象6ファイルそれぞれ個別に)を全文読んだ。
- `git diff --stat` で変更ファイル集合が指定の6ファイル+`docs/ai/status.md`のみであることを確認(範囲超過なし)。
- `python3 -m py_compile roles/recovery_probe/files/recovery-probe.py roles/incident_capture/files/incident-capture-collector.py` を実行しエラーなしを確認(状態を変えない確認)。
- `python3 -c "import ast; ast.parse(...)"` で `recovery-probe.py` の構文木を確認。
- `grep -rn "tester_mode"` を対象6ファイルへ個別に実行し0件を確認。
- `grep -rn "tester_mode"` をリポジトリ全体(playbooks/roles/inventories/scripts/docs/ai/policies/docs/ai/context)へ実行し、ヒットが全て(a) 29本の移行assert関連(`[migration]` task名・`that:`・`fail_msg`本文)、(b) R5〜R9対象のPolicy/Contextやplaybookコメント(本Stepの範囲外と契約が明記)のいずれかであることを確認。**実行に影響する箇所(`when:`条件・関数シグネチャ・辞書キー)からは1件も出ないことを確認した。**
- `grep -rn "drill_requested\|clear_drill\|fire_ladder("` をリポジトリ全体へ実行し、呼び出し側が新シグネチャ(4引数)に揃っていることを確認。
- `grep -rni "drill"` を `roles/recovery_probe/` 全体へ実行し0件を確認。`playbooks/README.md`・`docs/ai/policies/autonomous_recovery_policy.md` の「drill」がpush drill側(対象外)であることを確認。
- `roles/recovery_probe/files/recovery-probe.py` の `fire_ladder()` 全文を読み、旧`drill`分岐の削除後も各ステータス分岐(`pve-unreachable`/`stopped`/`not-found`/通常ラダー/HA failover)が正しくreturnしていることを確認。
- `roles/common_slack/tasks/notify.yml` 全文を読み、抑止条件が`skip_notifications`と`ansible_check_mode`の2条件のみになっていること、`tester_mode=true`単独では`Send Slack notification` blockへフォールスルーする(=抑止されない)ことを確認。
- `roles/common_slack/tasks/capture.yml` の`_capture_ctx`辞書リテラルと3箇所のコメントから`tester_mode`が削除されていることを確認。`record_version: 1`のまま据え置かれていることを確認(R11/OQ2は本Stepの範囲外)。
- `roles/incident_capture/files/incident-capture-collector.py` の`REQUIRED_SPOOL_FIELDS`から`"tester_mode"`が外れ、`capture.yml`側の辞書キー集合と一致することを確認。`roles/incident_investigate/files/incident-investigate.py`など他の消費者に`tester_mode`参照が無いことを確認。
- `inventories/homelab/group_vars/all.yml`から`tester_mode: false`定義削除後、リポジトリ内の全`tester_mode`参照(29本のassert含む)が`| default(false)`を経由しており、グローバル変数の消滅で壊れる箇所が無いことを確認。
- requirementの「リスク」表(#1〜#4)を照合軸に、それぞれこの差分でどう扱われているかを確認(下記What Looks Good参照)。

未確認事項(自分では実施していない、実装記録の主張の裏取りに留まる範囲):
- 実ホスト(ansy/quory)への到達・decoy実行はしていない(implement記録のdecoy検証結果はログを直接見ておらず、コードの静的な整合性からのみ裏取りした)。
- `ansible-playbook --syntax-check`は自分では再実行していない(implement記録の主張を静的読解で代替検証した)。

## Code Review: `tester_mode_full_removal` Step 2(R1〜R4)

### Summary
6ファイルの差分はrequirementのR1〜R4を過不足なく満たしており、範囲超過・取り残しは見つからなかった。`fire_ladder`のシグネチャ変更・drill分岐削除・spoolフィールド削除は呼び出し側まで一貫しており、静的に壊れた経路は無い。リスク表の#1(assert先行によるdrill実リブート)は本Stepでassertを外していないため回避されている。#3(呼び出し側の残存)は該当4playbook(`alloy_setup.yml`等)にtester_mode関連コードが元々無いことを確認し、解消を確認した。#2(capture/collector不整合)はコード自体は整合しているが、**quoryへの配備手順そのものはコードの外にあり、この差分だけでは強制できない**(下記Suggestions #1)。

### Critical Issues
なし。

### Suggestions
| # | File | Line | Suggestion | Category |
|---|---|---|---|
| 1 | `roles/common_slack/tasks/capture.yml` / `roles/incident_capture/files/incident-capture-collector.py` | - | requirementのリスク#2は「同一playbook実行で配備する」運用手順に依存しており、コード側にはそれを強制する仕組みが無い。`capture.yml`はtask includeであり実行ホストのgit checkout次第で有効なコードが変わる一方、collectorは`incident_capture_setup.yml`でquoryへcopyされる。二つの反映タイミングが分離した状態(例: quoryの手動pullが先、`incident_capture_setup.yml`実行が後、またはその逆)が生じると、その間に発生した通知のspool recordが古い/新しいどちらかのスキーマ不一致で全件`collection_errors`に落ちる可能性がある。本Stepのコード自体に欠陥は無いが、実配備時にこの順序をCoordinator/Testerへ明示的に申し送ることを推奨する | 運用手順・引き継ぎ |
| 2 | `roles/recovery_probe/tasks/main.yml` | 68行付近 | `loop:`から`/var/lib/homelab-recovery/probe/drill`を外したことで新規配備分は作られなくなるが、**既にdrill機構が稼働していたホスト(ansy/quory)に残っている同ディレクトリを削除するタスクは無い**。`drill_requested()`/`clear_drill()`が削除された以上この既存ディレクトリは完全に不要になるが、放置しても実害は無い(参照するコードが無い)ため優先度は低い。次回このroleへ触れる際にクリーンアップを検討してよい | 整理・クリーンアップ |

### What Looks Good
- **R1**: `notify.yml`の抑止条件が`skip_notifications | default(false) | bool or ansible_check_mode | bool`の2条件のみになっており、要件どおり。`tester_mode=true`単独を渡した場合に`[skip_notifications]`debugタスクの`when:`が不成立となり、`Send Slack notification`blockの否定条件も不成立(=blockが実行される)ことを条件式から確認した。これはAC2が要求する「`tester_mode=true`を付けても抑止されない(無視される)」と一致する。
- **R2**: `recovery-probe.py`のdrill関連コード(docstring行、`fire_ladder`の`drill`引数・`label`変数・`tester_mode=true`付与・`stopped`分岐のdrillショートカット・VM reboot分岐の無条件`status: ok`送信・`drill_requested`/`clear_drill`関数・main loopの`drill`変数とその分岐)がすべて削除されている。`fire_ladder`呼び出し元は新シグネチャ(4引数)に揃っており、`grep -rni drill roles/recovery_probe/`は0件。この削除は、requirementが問題視した「drillがassertで落ちながら`status: ok`を送る」経路そのものを消しており、リスク#1(assert先行によるVM実リブート)はassertを外していない現状で構造的に発生し得ない。
- **R3**: `capture.yml`のspool record辞書リテラルと`incident-capture-collector.py`の`REQUIRED_SPOOL_FIELDS`から`tester_mode`が同時に外れており、フィールド集合が一致している。`record_version`は1のまま据え置かれており、requirementのOQ2(R11)が「未決」としている扱いと整合する。
- **R4**: `group_vars/all.yml`から`tester_mode: false`と説明コメントが削除され、`reports_base_dir`のみ残った。29本の移行assertはすべて`| default(false)`を経由しているため、グローバル変数消滅によって壊れる箇所は無い。
- **範囲**: `git diff --stat`は指定6ファイル+`docs/ai/status.md`のみで、R5〜R9(Policy/Context/playbookコメント/scriptコメント)やP1(29本のassert撤去)に踏み込んでいない。requirementの非ゴール・別Step切り分けと一致している。
- **push drillとの独立性**: `playbooks/recovery_push_drill_setup.yml`・`roles/recovery_push/tasks/drill_setup.yml`・`docs/ai/policies/autonomous_recovery_policy.md`の「drill」記述はいずれもpush drill(対象外)であり、この差分は触れていない。

### Verdict
Approve

## 未解決事項
なし。Suggestions 2件はいずれもStop-shipに当たらない(運用申し送り・低優先度クリーンアップ)。
