# implement: `tester_mode` の完全撤廃 — Step 3 (R5〜R8)

日付: 2026-08-02 (JST)
対象requirement: `docs/ai/reviews/tester_mode_full_removal/2026-08-02_001_requirement.md`
scope: R5・R6・R7・R8のみ。R1〜R4(Step 1で実施済み、`2026-08-02_002_implement.md`)、R9(`docs/ai/status.md`、Coordinator担当)、P1(R10、29本の移行assert撤去)は対象外。

## 変更したファイル

1. `docs/ai/policies/ansible_test_safety_policy.md` (R5)
2. `docs/ai/policies/proxmox_backup_restore_verify_policy.md` (R6)
3. `docs/ai/policies/incident_capture_policy.md` (R6)
4. `docs/ai/context/operations/healthcheck.md` (R6)
5. `playbooks/proxmox_patch_dryrun.yml` (R7)
6. `playbooks/proxmox_healthcheck.yml` (R7)
7. `playbooks/time_sync_check.yml` (R7)
8. `playbooks/recovery_monitoring_check.yml` (R7)
9. `playbooks/proxmox_snapshot_check.yml` (R7)
10. `playbooks/recovery_probe_notify.yml` (R7)
11. `playbooks/incident_investigate_notify.yml` (R7)
12. `playbooks/monitoring_healthcheck.yml` (R7)
13. `playbooks/radius_healthcheck.yml` (R7)
14. `scripts/check-tester-gate.sh` (R8)
15. `playbooks/recovery_vm_reboot.yml` (Coordinator差し戻し#1)
16. `playbooks/recovery_service_restart.yml` (Coordinator差し戻し#1)
17. `playbooks/recovery_ha_failover.yml` (Coordinator差し戻し#1)
18. `playbooks/ubuntu_nightly.yml` (Coordinator差し戻し#1)
19. `playbooks/sophos_trim.yml` (Coordinator差し戻し#1)
20. `playbooks/proxmox_restore_vm_placement.yml` (Coordinator差し戻し#1)
21. `playbooks/proxmox_evacuate_node.yml` (Coordinator差し戻し#1)
22. `playbooks/proxmox_patch_weekly_full.yml` (Coordinator差し戻し#1)
23. `playbooks/cert_renew_quory.yml` (Coordinator差し戻し#1)
24. `playbooks/proxmox_patch_apply_node.yml` (Coordinator差し戻し#1)
25. `playbooks/proxmox_backup_restore_verify.yml` (Coordinator差し戻し#1)
26. `playbooks/unifi_backup_fetch.yml` (Coordinator差し戻し#1)
27. `playbooks/cloudkey_cert_deploy.yml` (Coordinator差し戻し#1)

`git status` で確認済み: 上記27ファイル以外に自分で変更したファイルはない。Step 1差分(6ファイル)、`docs/ai/status.md`、`docs/ai/reviews/tester_mode_full_removal/`、`docs/ai/memory/incidents/2026-08-02_recovery-probe-drill-blocked-by-tester-mode-assert.md`(未追跡)はすべて未変更のまま(差し戻し対応後も再確認済み)。

## R5: `ansible_test_safety_policy.md`

- **TS-003**(2回改訂): 初版では「2026-07-06〜07に廃止され」「2026-08-02、撤廃が完了した」という日付つき経緯と、`tester_gate` roleが`end_play`/`end_host`する設計だったという廃止理由の説明を書いた。Coordinatorの差し戻し(下記「Coordinator差し戻し対応」参照)により、日付・経緯・`end_play`/`end_host`の説明を全て削除し、「読み手がいま行動するために要る事実」だけへ縮小した: `tester_mode`/`tester_gate`は廃止済みで`roles/tester_gate`は実在しないこと、ゲート機構は`--check`(`ansible_check_mode`)であること、`tester_mode`識別子への参照は全経路から撤去済みで渡しても無視されること、現在この識別子が現れるのは29本の移行assertのみであること。
- **TS-026**: marker driftの例示から`tester_mode`固有の言及を削除。「過去に理由文が廃止済みの`tester_mode` guardを指しながら実guardは`skip_notifications`だった」→「理由文が指すguard名と実装上のguardが乖離する『marker drift』は複数playbookで実在した」に変更(要求どおり「実例なしで規則だけを述べる」形)。
- **TS-004**(未変更、Coordinator確認済み): 「`# tester-gate:`ヘッダマーカーは廃止されていない。名前が似ているだけの別物であり、廃止済みの`tester_mode`と混同しない」という記述は、Coordinatorの差し戻し指示で明示的に「そのまま残してください」と確認されたため変更していない。

## R6: 3ファイルの `tester_mode` 記述を現行の抑止手段へ置き換え

- `docs/ai/policies/proxmox_backup_restore_verify_policy.md`(BRV-011直後の段落、非`変更履歴`): 「`tester_mode`/`tester_gate`は廃止済みの概念であり本Playbookでも参照しない(`tester_mode=true`を指定した場合はassertでfailする)」という文を、実際の抑止手段の記述に置き換えた。「`risk-accepted`は`--check`を安全な実行手段として提供しない(TS-030)。`--check`を指定すると`ansible_check_mode`を検査する停止assertでplayが止まり、指定しなければ本実行になる」とした。`playbooks/proxmox_backup_restore_verify.yml`の実コード(L70/L238に`that: not (ansible_check_mode | bool)`のTS-030 assertが実在)を確認した上での記述。line 303の`変更履歴`エントリ(`tester_mode`廃止後の実態に合わせて改訂、と記す2026-07-26の履歴)は非ゴールにより不変更。
- `docs/ai/policies/incident_capture_policy.md`(IC-009): 「`tester_mode` 等の抑止フラグは捕捉をスキップする理由にせず」→「`skip_notifications` 等の抑止フラグは...」。Step 1で`capture.yml`のspool recordから`tester_mode`フィールド自体が消えている(記録すらされない)ため、例示を現存するフィールド名へ差し替えた。
- `docs/ai/context/operations/healthcheck.md`(§3): guard式の引用`tester_mode | default(false) | bool or skip_notifications | default(false) | bool`を、Step 1後の`notify.yml`実装に合わせて`skip_notifications | default(false) | bool or ansible_check_mode | bool`へ更新。

## R7: playbookコメント9件

いずれも「`tester_mode`/`skip_notifications`で抑止する」type の現状不一致claimを「`skip_notifications`/`ansible_check_mode`で抑止する」へ書き換えた(Step 1で`notify.yml`の抑止条件が`skip_notifications or ansible_check_mode`の2つになったことに合わせる)。

- `proxmox_patch_dryrun.yml:2`、`proxmox_healthcheck.yml:2`、`time_sync_check.yml:2`、`proxmox_snapshot_check.yml:2`、`monitoring_healthcheck.yml:2`、`radius_healthcheck.yml:2`: ヘッダの`# tester-gate:`理由文内。
- `recovery_monitoring_check.yml:24`: ヘッダブロック内の説明行。
- `recovery_probe_notify.yml:2`: `role-guarded`の理由文(「`tester_mode`ガードで抑止される」→「`skip_notifications/ansible_check_mode`ガードで抑止される」)。
- `incident_investigate_notify.yml:9`: Policy TS-005の引用に添えた括弧注記。

要求文が例示した3本(`proxmox_snapshot_check.yml` / `proxmox_patch_dryrun.yml` / `recovery_monitoring_check.yml`)に加え、grep洗い出しで見つかった残り6本も同一パターンとして修正した。

## R8: `scripts/check-tester-gate.sh`

- ヘッダコメントの`(旧 tester_mode 不変条件 lint)`を削除。
- 「単なる tester_mode 文字列やコメントでは通らない」を「単なるコメントでは通らない」に変更(この注記自体、`tester_mode`という語を挙げる必然性が既になくなっている)。
- 判定ロジック本体(`grep -Eq '^# tester-gate: ...'`等)は元々`tester_mode`という語に一切依存しておらず、変更していない。

## Coordinator差し戻し対応(2026-08-02、同日中)

初回提出後、Coordinatorから2件の判断が返り、以下を追加で実施した。前回提出済みの契約要素(scope、対象外ファイル)は変更なし。

### 差し戻し#1: 13本のヘッダ行を修正

初回提出では「AC1の『29本の移行assert』はassertのtask本体を指すのかヘッダのマーカー行を含むのか未確定」として、13本のplaybookが持つ`# tester-gate: <種別> — tester_mode/tester_gate を廃止。...`というヘッダ理由文を未変更のまま残していた。Coordinatorの判断: **「29本の移行assert」はassertのtask本体のみを指し、ヘッダのマーカー行は含まれない。** 経緯部分(`tester_mode/tester_gate を廃止。`)を落とし、そのplaybookがその分類である理由だけを残すよう指示された。

対象13本(いずれもマーカーは`# tester-gate: <種別> — <理由>`の1行形式を保ち、`—`の直後から実質的な理由が始まる形へ書き換えた。既存の続き行(何をゲートし、何を`--check`でも本実行するかの説明)は内容を変えずそのまま活用し、経緯部分だけを除去した):

- `recovery_vm_reboot.yml` / `recovery_service_restart.yml` / `recovery_ha_failover.yml`(check-mode-native、自律復旧ラダー各段の説明を活用)
- `ubuntu_nightly.yml`(check-mode-native、reboot-required判定と実rebootのゲート分離の説明を活用)
- `sophos_trim.yml`(dry-run-aware、fstrimのネイティブdry-run差し替えの説明を活用)
- `proxmox_restore_vm_placement.yml` / `proxmox_evacuate_node.yml`(check-mode-native、VM識別とmigrate/HA操作のゲート分離の説明を活用)
- `proxmox_patch_weekly_full.yml`(check-mode-native、オーケストレータとしてimport_playbook先のゲートを信頼する説明を活用)
- `cert_renew_quory.yml`(check-mode-native、quory本番Semaphoreへの影響を理由とする説明を活用)
- `proxmox_backup_restore_verify.yml` / `unifi_backup_fetch.yml` / `cloudkey_cert_deploy.yml`(risk-accepted、dry-run区分がなくTS-030停止assertで代替する説明を活用)
- `proxmox_patch_apply_node.yml`(check-mode-native、英語コメント。Coordinatorが名指しした「経緯を落とすと理由が空になる」ケース。マーカー行に`pilot migration away from tester_mode/tester_gate.`しかなく、続き行の`Gating now relies on Ansible's native --check (ansible_check_mode): read-only / diagnostic command tasks force check_mode: false so they run for real under --check, while the destructive block (apt-get dist-upgrade, reboot) stays gated by ansible_check_mode.`という実装済みの説明をそのままマーカー行の理由として繰り上げた。実装(L52/L60/L68の`when: not ansible_check_mode`)と照合し記述が正確であることを確認した)

### 差し戻し#2: TS-003の日付つき経緯を除去

TS-003(上記R5節参照)から「2026-07-06〜07に」「2026-08-02」という日付と、`tester_gate`が`end_play`/`end_host`する設計だったという廃止理由の説明を削除し、「読み手がいま行動するために要る事実」(`tester_mode`は撤廃済みで渡しても無視されること、29本のplaybookは渡されると停止するassertを持つこと)だけへ縮小した。TS-004は指示どおり未変更。

### 再確認条件(Coordinator指定の5項目)

1. `grep -rn "^# tester-gate:.*tester_mode" playbooks/` → 0件(exit code 1、ヒットなし)。
2. 13本全てでマーカー行が1行形式(`# tester-gate: <種別> — <理由>`)を保ち、理由が空でないことを`grep -n "^# tester-gate:"`で目視確認。
3. `bash scripts/check-tester-gate.sh` → `[tester-gate-lint] OK (49 playbooks)`。
4. TS-003に日付(`2026-07-06`/`2026-07-07`/`2026-08-02`)と`end_play`/`end_host`の説明が無いことを確認(`grep -n "2026-07-06\|2026-07-07\|2026-08-02" docs/ai/policies/ansible_test_safety_policy.md`の残り2件はTS-029と`変更履歴`節であり、TS-003とは無関係)。
5. Step 1の差分・`docs/ai/status.md`・`docs/ai/reviews/`と`docs/ai/memory/`の他ファイルは`git status`で再確認し、変更なし。

追加で`python3 scripts/check-doc-consistency.py`(playbook tester-gate文言とREADME等の突合)を再実行し、`[check1] OK / [check2] OK / [check3] OK`を確認した。13本のYAML構文も`yaml.safe_load`で再確認済み。

## 自己検証

**静的確認**

- `grep -rn "tester_mode" playbooks/ roles/ inventories/ scripts/ docs/ai/policies/ docs/ai/context/` を実行し、29本の移行assert関連行を除いた残差を全て手動で分類した(下記「AC1の掃引結果」)。
- `python3 -c "import yaml; ..."` で変更した9本のplaybookを全てパース → 全てOKで構文エラーなし。
- `bash scripts/check-tester-gate.sh` → `[tester-gate-lint] OK (49 playbooks)`。
- `python3 scripts/check-doc-consistency.py` → `[check1] OK / [check2] OK / [check3] OK`(playbook tester-gate文言とREADME、agent frontmatter、Markdown内部リンクの整合。今回のコメント書き換えによるドリフトなし)。

**未実施(理由あり)**

- `scripts/git-pre-commit-check.sh` のフル実行(AC6): このスクリプトは`git diff --cached`(staged)を対象にする。Implementerは`git add`を行わない規則のため、ステージされていない現状では「staged files無し」で早期exitするだけで意味のある検証にならない。スクリプト内でstagingに依存しない2つの構成要素(`check-tester-gate.sh`本体、`check-doc-consistency.py`)は個別に実行して上記のとおりOKを確認した。gitleaks・IPv4リテラル検査・vault検査・YAML構文検査(staged版)はCoordinatorがstageした後にTesterが確認する必要がある。

## AC1の掃引結果

`grep -rn "tester_mode" playbooks/ roles/ inventories/ scripts/ docs/ai/policies/ docs/ai/context/` の全ヒットを分類した。Coordinator差し戻し対応前は110件(残差18件)だったが、13本のヘッダ行を修正した結果、**96件(残差5件)** へ縮小した。

**29本の移行assert本体(想定どおり残存、対象外)**: 各ファイルの `[migration] tester_mode is deprecated...` / `Reject deprecated tester_mode` / `Assert tester_mode is not used` というtask名・`that:`条件・`msg:`文言。`playbooks/`配下29ファイルに1本ずつ(`ubuntu_nightly.yml`のみ2箇所)。`grep -rl` で対象ファイル数が正確に29件であることを確認済み。Coordinatorの判断により、これはassertのtask本体のみを指し、ヘッダのマーカー行は含まれないと確定した(差し戻し#1)。

**残差(5件、いずれも非ゴールにより保護されている、またはCoordinatorが明示的に残す判断をしたもの)**:

1. **3件**: `ansible_test_safety_policy.md`の「背景」節(見出し1箇所・TS-003本文1箇所・TS-004本文1箇所)。TS-003は日付・経緯を除去した上で、`tester_mode`識別子そのものを名指しする現在の指示的事実(撤廃済みで渡しても無視される、29本のassertのみに残る)として書いており、識別子名を伏せて書くことはできない。TS-004はCoordinatorが明示的に「そのまま残してください」と指示した(差し戻し#2)。
2. **1件**: `ansible_test_safety_policy.md`の`変更履歴`節(L168)。非ゴールにより保護。
3. **1件**: `proxmox_backup_restore_verify_policy.md`の`変更履歴`節(L303)。非ゴールにより保護。

## R5〜R8それぞれの充足状況

| ID | 状況 |
|---|---|
| R5 | 充足。TS-003は撤廃完了を述べる文へ改訂、TS-026は`tester_mode`固有の例示を除去した。TS-004は意図的に未変更(上記) |
| R6 | 充足。3ファイルとも`skip_notifications`/`ansible_check_mode`という現行の手段へ置き換えた |
| R7 | 充足。要求が例示した3本を含む、該当パターンを持つ全9本のplaybookコメントを修正した |
| R8 | 充足。`check-tester-gate.sh`のコメントから`tester_mode`言及を除去し、判定ロジックが元々依存していないことも確認した |

## 未解決事項

1. ~~AC1の厳密解釈~~ → **解決済み(差し戻し#1)**。Coordinatorが「29本の移行assert」はassert本体のみを指すと確定し、13本のヘッダ行を修正した。AC1の残差は5件のみで、いずれも非ゴール保護(変更履歴節)またはCoordinator確認済み(TS-004)。
2. **AC5の文言と`ansible_test_safety_policy.md`の「背景」節**: AC5は「`tester_mode`は変更履歴節にのみ現れ、現行の指示としては現れない」と定める。「背景」節は`tester_mode`を名指しするが(見出し1・TS-003本文1・TS-004本文1の計3件)、TS-003は日付・経緯を除去済みで内容は「今どう扱うべきか」という現在の事実の記述であり、TS-004はCoordinatorが明示的に残す判断をした(差し戻し#2)。厳密な文字列ゼロ化(見出しも含め一切名指ししない)は識別子の説明そのものを不可能にするため、AC5は「現在有効な機構であるかのような誤読を招く記述がない」という趣旨で満たされていると判断する。文字どおりの解釈を取るかはCoordinator確認事項として残る。
3. **AC6の完全実行**: 上記「自己検証」のとおり、`scripts/git-pre-commit-check.sh`本体はstaged前提のため未実行。stagingに依存しない構成要素(`check-tester-gate.sh`、`check-doc-consistency.py`)は今回の追加修正後も再実行しOKを確認したが、gitleaks・IPv4リテラル検査・YAML構文検査(staged版)はCoordinatorがstageした後の確認が必要。
