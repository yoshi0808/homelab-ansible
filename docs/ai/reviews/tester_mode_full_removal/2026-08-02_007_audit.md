# audit: `tester_mode` の完全撤廃

日付: 2026-08-02 (JST)
対象: `docs/ai/reviews/tester_mode_full_removal/`(001_requirement〜006_test_result)
確認した手段:

- `2026-08-02_001_requirement.md`〜`006_test_result.md`を番号順に全文読解。
- `git status --short` / `git diff --staged --stat` / `git diff --staged -- <各ファイル>`で、記録が主張する変更範囲(40ファイル: 34 diff + 5 review記録 + status.md)と実差分を突合。
- `git diff --staged`で`notify.yml`・`capture.yml`・`incident-capture-collector.py`・`recovery-probe.py`・`group_vars/all.yml`の実差分全文を読み、implement/review記録の記述と一致することを確認(状態を変えない確認)。
- `grep -rn "tester_mode" playbooks/ roles/ inventories/ scripts/ docs/ai/policies/ docs/ai/context/`を自分で実行し96件(29 assert + 5残差)を実測、記録の主張と一致を確認。
- `bash scripts/check-tester-gate.sh`・`bash scripts/git-pre-commit-check.sh`を自分で実行し、記録どおりOK(exit 0)であることを確認(状態を変えない確認)。
- `docs/ai/status.md`の差分と、参照されている`docs/ai/memory/incidents/2026-08-02_recovery-probe-drill-blocked-by-tester-mode-assert.md`・`2026-08-01_tester-slack-decoy-did-not-contain-request.md`・`skills/incident-recording/SKILL.md`を確認(非ゴールが参照するため)。
- 実ホストへは到達していない。

## 再構成可否

**再構成できた。** requirement→Step1実装→Step2レビュー(Approve)→Step3実装(Coordinator差し戻し2件を含む)→Step4レビュー(Approve)→Step5 Tester(AC1〜7全PASS)という経緯が、各ファイルの相互参照(implement記録が対象requirementとscopeを明記、review記録が対象implement記録を明記、test_resultが先行記録を「検査対象として扱い自分で再実行した」と明記)だけで辿れた。差し戻しの理由・対応・再確認条件も004に記録されており、Coordinatorの判断(「29本の移行assertはtask本体のみを指しヘッダは含まない」「TS-004は残す」)が誰の判断かまで含めて追える。

静的検証(`grep`件数、diffの内容、`check-tester-gate.sh`/`git-pre-commit-check.sh`の実行結果)はすべて記録の主張どおりに再現し、食い違いは無かった。

## 指摘

### 1. Incident記録`2026-08-02_recovery-probe-drill-blocked-by-tester-mode-assert.md`が、この案件で修正されたにもかかわらず`調査中`/`未着手`のまま — **クローズを妨げる**

この案件のR2(`recovery_probe`のdrill機構削除)は、まさにこのIncidentが記録した不具合(drillがassertで落ちながら`status: ok`の「drill完了」を送る)への修正であり、AC4・AC7でTesterが実地確認まで行っている(006)。しかしIncidentファイル自身は`状態: 調査中`、`## 修正内容`は「未着手。`tester_mode`の最終的な扱いを決めてから、drill経路をあわせて直す」のままである。

`skills/incident-recording/SKILL.md`は「捕捉→完成」の2段階を定め、完成段階の条件を「原因が判明し**修正の確認が取れた時点**」、`状態`を`解決済み`にすると明記している。本件は原因(`tester_mode`廃止への追随漏れ)・修正内容(drill機構削除)・確認方法(AC4/AC7のTester実地確認)がすべて揃っており、スキルの定める完成条件を満たしているのに、記録側が追随していない。

requirement.md の非ゴール「`docs/ai/memory/incidents/`と`docs/ai/memory/lessons/`を書き換えない。同じ理由(過去の作業の一次記録)」は、`docs/ai/reviews/`の案件記録や`docs/ai/adr/004`と同列に扱っているが、**この2つは性質が違う**。ADRや案件記録は決定・実施時点のスナップショットで書き換え不要が正しいのに対し、Incidentの`状態`欄はスキルが「原因判明・修正確認後に更新する」ことを前提にした生きたフィールドであり、「過去の一次記録だから触らない」という理由は成立しない。この非ゴールの適用範囲設定そのものが、記録を陳腐化させた原因になっている。

この案件の差分そのものに欠陥は無いが、**この案件の変更によって、案件フォルダの外にある正本(Incident記録)が古いまま残る**状態であり、Auditorの検査表「差分の外側の陳腐化」に直接該当する。

### 2. Tester検証中に発生した`hooks.slack.com`への実リクエストが、前日の同型Incidentと紐付けられていない — **クローズを妨げない**

006(test_result)の「残存リスク」1は、`community.general.slack`が`domain`を明示しない限り常に実在の`hooks.slack.com`へリクエストを組み立てるため、閉域のつもりのdecoyから1件の実HTTPリクエストが漏れたことを記録している(メッセージ配信なし)。

同一の欠陥クラス・同一の原因(`community.general.slack`の`domain`既定値)が、1日前の`docs/ai/memory/incidents/2026-08-01_tester-slack-decoy-did-not-contain-request.md`にIncidentとしてすでに記録されている。同Incidentは「Testerの検証実行が本番Slackへ到達した事象は3回目」として系列表まで作り、再発防止の必要性を明示していた。本件はこの4回目の再発に当たるが、006はこの前例を参照しておらず、新規Incidentとしても起票されていない。

記録の再構成自体は006単体で完結しており(何が起きたか・実害の有無は明記されている)、この案件のクローズを妨げるものではない。ただし前例と同じ構造の再発が、前例からわずか1日後に、前例を参照せずまた起きている事実は、Coordinatorへの申し送り価値があると判断し記録する。

## 状態の正本との突合

`docs/ai/status.md`の「Now(進行中)」#1行は、Step 1着手前の内容(「Step 1から順に実施」)のまま残っている。R1〜R9・AC1〜AC7すべて完了・全PASSの現状と一致していない。**これは指摘ではない**(Auditorは該当行を消す前に起動するため、除去は次のとおりCoordinatorが同一セッションで行う)。

`Watch`表の該当項目はすでに「`pvesh`が関わる本番への検証方法」(OQ1決定を反映した申し送り)へ更新済みで、これは現状と一致している。

## 除去すべき行(実施事項、指摘ではない)

- `docs/ai/status.md` Now(進行中)#1「`tester_mode`の完全撤廃」— クローズと同時に削除。

## 総合判定

**条件付き受入。**

指摘1(Incident記録の状態欄更新)を反映すればクローズしてよい。反映は`docs/ai/memory/incidents/2026-08-02_recovery-probe-drill-blocked-by-tester-mode-assert.md`の`状態`を`解決済み`にし、`## 原因`・`## 修正内容`・`## 確認方法`をこの案件(R2、AC4/AC7)を根拠に埋める、というIncident記録側の更新であり、**tester_mode_full_removal案件のrequirement/実装そのものへの後戻りではない**。再監査は前提としない。

指摘2は記録済みの事実として申し送るのみで、クローズ判定には影響しない。

---

## Coordinatorによるクローズ判断(2026-08-02)

**条件付き受入の指摘1を反映してクローズする。**

| 指摘 | 対応 |
|---|---|
| 1(Incident記録の状態欄が`調査中`のまま) | **是正した。** `docs/ai/memory/incidents/2026-08-02_recovery-probe-drill-blocked-by-tester-mode-assert.md` を `解決済み` にし、`原因分類: #テスト不足`・`原因`(移行漏れ。`740f8fe`で入り`f925905`で壊れた)・`修正内容`(drill機構ごと削除、代償として配線の訓練経路が無くなった)・`確認方法`(AC4/AC7の実測、ただしdrill自体を回した観測は最後まで無い)を埋めた。**あわせてrequirementの非ゴールを訂正した** — Incidentの`状態`/`原因`/`修正内容`/`確認方法`は`skills/incident-recording/SKILL.md`が定める生きたフィールドであり、一律保護の対象にしたのが誤りだった |
| 2(`community.general.slack` のdecoy漏れ再発) | **本案件では扱わない。** 新規Incidentも起票しない。同じ原因分類が繰り返し検出されている段階であり、`skills/incident-recording/SKILL.md` の運用ルールは「Lessonを経由せず直接Skill新設・改訂を検討する」と定める。4回目である事実と、再発条件(Testerが前例を参照する経路を持たない)を `docs/ai/status.md` Next の Lesson昇格の行へ書き込んだ |

`docs/ai/status.md` の Now 行は削除し、本案件から派生した申し送りを次のとおり移した。

- **Next**: R10(29本の移行assert撤去とTS-003削除)。**先に撤去すると、かつてdrillがそうであったように、止まるはずだった経路が本番適用になる。**
- **Next**: `pvesh` が関わる本番への検証方法(drill削除の代償)。
- **Watch**: spool recordの必須フィールド変更が、quoryへ2経路で届くことによる不整合窓。
