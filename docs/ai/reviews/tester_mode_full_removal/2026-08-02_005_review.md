# review: `tester_mode` の完全撤廃 — Step 4(規範文書・コメント掃引)

日付: 2026-08-02 (JST)
対象requirement: `docs/ai/reviews/tester_mode_full_removal/2026-08-02_001_requirement.md`(R5・R6・R7・R8、AC1・AC5・AC6)
対象実装記録: `docs/ai/reviews/tester_mode_full_removal/2026-08-02_004_implement.md`

## Code Review: 規範文書とコメントの掃引(`tester_mode_full_removal` Step 4)

### Summary

対象は以下14ファイル。`roles/`・`inventories/`配下(Step 1、別レビューでApprove済み)、`docs/ai/status.md`、`docs/ai/reviews/tester_mode_full_removal/`、`docs/ai/memory/`配下は対象外として扱った。

- `docs/ai/policies/ansible_test_safety_policy.md`(R5)
- `docs/ai/policies/proxmox_backup_restore_verify_policy.md`・`incident_capture_policy.md`・`docs/ai/context/operations/healthcheck.md`(R6)
- `playbooks/`配下13本のヘッダ行 + 9本のコメント修正(R7、実体は重複ファイルを含む差し戻し#1の13本と初回提出の9本で計13本)
- `scripts/check-tester-gate.sh`(R8)

独立に`grep -rn "tester_mode" playbooks/ roles/ inventories/ scripts/ docs/ai/policies/ docs/ai/context/`を実行し、実装記録の「AC1掃引結果(96件、残差5件)」を再現・確認した。残差5件はすべて非ゴールで保護された変更履歴節2件、またはPolicyの「背景」節が識別子を名指しする現在の指示的事実3件(TS-003見出し・TS-003本文・TS-004本文)で、AC5の但し書きと一致する。29本の移行assert(`ubuntu_nightly.yml`のみ2箇所、他28ファイルは1箇所)も実カウントと一致した。

13本のヘッダマーカーは全て`# tester-gate: <種別> — <理由>`の1行形式(TS-006)を保ち、`check-tester-gate.sh`の正規表現にマッチすることを確認した。risk-accepted 3本(`proxmox_backup_restore_verify.yml`・`unifi_backup_fetch.yml`・`cloudkey_cert_deploy.yml`)は`tester-gate-condition2:`行も無傷で残っている。マーカーの理由文と実装(`ansible_check_mode`停止assertの行番号、`notify.yml`の抑止条件式、`recovery_probe_notify.yml`のTS-005 role-guarded要件)を個別に照合し、marker driftは見つからなかった。

`bash scripts/check-tester-gate.sh`→`[tester-gate-lint] OK (49 playbooks)`、`python3 scripts/check-doc-consistency.py`→3チェックともOKを再実行して確認した(AC6のうち`git-pre-commit-check.sh`本体はstaged前提のため未実行、実装記録の記載どおり)。

非ゴールが保護する範囲(`docs/ai/adr/004-notify-capture-insertion.md`、`docs/ai/reviews/`の他案件、`docs/ai/memory/`、各Policyの「変更履歴」節、`playbooks/README.md`・`skills/`・`roles/recovery_exec/templates/AGENTS.md.j2`・`docs/ai/policies/autonomous_recovery_policy.md`のdrill言及)はいずれも`git diff --stat`で無変更を確認した。

### Critical Issues

なし。

### Suggestions

なし。

### What Looks Good

- **TS-003の改訂(R5)**: 日付つき経緯と`end_play`/`end_host`の廃止理由説明が削除され、「読み手がいま行動するために要る事実」だけへ縮小されている。撤回した規範を根拠として引用する残存(欠陥クラス3)は見当たらない。
- **TS-026の改訂(R5)**: marker driftの例示が`tester_mode`固有の言及を含まない一般的な規則へ書き換えられており、要求どおり「実例なしで規則だけを述べる」形になっている。TS-004は指示どおり未変更。
- **R6の3ファイル**: いずれも旧記述(`tester_mode`/`tester_gate`は廃止済みで参照しない、`tester_mode | default(false) | bool or skip_notifications`等)を、Step 1後の実装(`notify.yml`の`skip_notifications | default(false) | bool or ansible_check_mode | bool`、`proxmox_backup_restore_verify.yml`のTS-030停止assert)と突き合わせて正確に書き換えている。宣言と実装の乖離なし。
- **R7の9件+差し戻し#1の13件、計13本のplaybook**: 機械的な置換(`tester_mode/skip_notifications`→`skip_notifications/ansible_check_mode`、経緯部分の削除)が実コードと整合している。特に`proxmox_patch_apply_node.yml`は「経緯を落とすと理由が空になる」ケースで、続き行の実装済み説明をマーカー行へ繰り上げる対応が取られており、理由文が空にならず実装(`when: not ansible_check_mode`)とも一致する。
- **R8**: `check-tester-gate.sh`の判定ロジック本体はそもそも`tester_mode`という語に依存しておらず、コメント整理のみで機能に影響がないことを確認した。
- **欠陥クラス4(判定ラダー)**: TS-005の5分類定義自体は変更されておらず、全域性・一意性に変化なし。
- **欠陥クラス6(追跡表・索引)**: `docs/ai/policies/ansible_test_safety_policy.md`は個別playbookの分類一覧を複製しない設計(TS-007)のため、本変更でドリフトする追跡表は存在しない。
- **欠陥クラス7(適用範囲)**: R6で書き換えた各Policy・Contextの適用範囲(BRV-011の対象playbook、IC-009の捕捉範囲、healthcheck.md §3の対象)は書き換え前後で変わっていない。
- **非ゴールの遵守**: ADR-004、他案件の`docs/ai/reviews/`、`docs/ai/memory/`全般、各Policyの変更履歴節、drill関連4箇所(`autonomous_recovery_policy.md`・`playbooks/README.md`・`skills/ansible-implementation-style/SKILL.md`・`roles/recovery_exec/templates/AGENTS.md.j2`)はすべて無変更を確認した。

### Verdict

Approve
