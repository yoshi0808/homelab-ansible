# review: 切り離しの境界を直す(案件A)最終レビュー(独立・4本目)

作成: 2026-09-06 / Reviewer(独立、先行レビュー3本の結論は引き継がず現物で判定)

対象: `roles/semaphore_upgrade/`(defaults/main.yml、tasks/main.yml、tasks/capture_origin.yml、tasks/upgrade.yml、tasks/rollback.yml、tasks/prepare_detached.yml、files/semaphore-upgrade-detached.py、files/test_detach_boundary.py、files/test_capture_origin.yml)。すべて未commit(`git status`で確認)。

## Summary

直近3件の直修正(rollbackのregister参照、テストが実体を通らない、capture_originの同名register上書き)は、現物のdiffと`/tmp`上のfixture実行で個別に再現・確認し、いずれも解消されている。一方、**AC11(待機が例外で失敗したときの安全宣言)がrollbackモードでは満たされていない**ことを実行して確認した。upgradeモードの同種失敗は正しく安全宣言が出るのに、rollbackモードの同種失敗は`status: failed`のまま何の安全宣言も出ない — これはAC11自身が名指しで禁じている「中止の理由によって安全宣言が出たり出なかったりする」の実例である。Verdict: **Request Changes**。

## Critical Issues

なし。

## Major Issues(blocking)

| # | File | Line | Issue | Severity |
|---|---|---|---|---|
| M1 | `files/semaphore-upgrade-detached.py` | 379-389(`if not boundary_crossed:`ブロック) | **AC11違反(rollbackモード)。** `wait_for_jobs`がタイムアウト以外の例外(DB読み取り不可・API応答不正など、AC11が名指しする「DBが読めない等」そのもの)で失敗したとき、安全宣言は①`mode=="upgrade"`かつ`undo_install_without_stop`実行時に付く`binary_restored`フラグ、②`isinstance(error, BoundaryTimeout)`による`status="aborted"`、の2経路でしか出ない。rollbackモードは①の対象外(このトランザクションでは何もインストールしていないため`undo_install_without_stop`が呼ばれない)であり、②はタイムアウト以外の例外では成立しない。結果、rollbackモード+非タイムアウト例外は`status: "failed"`のまま、「Semaphoreを停止していない」「何も変更していない」という文言が一切通知に出ない。実行して確認(下記「3の具体」参照)。**タイムアウト例外の場合は`isinstance(error, BoundaryTimeout)`が効くため、rollback+タイムアウトは正しく「中止」表示になる(`test_rollback_timeout_does_not_touch_binary`で確認済み)。差が出るのは非タイムアウト例外のときだけ**、まさにAC11が禁じる「理由によって出たり出なかったりする」状態そのもの。 | Major |

## Minor Issues(non-blocking)

| # | File | Issue | Severity |
|---|---|---|---|
| m1 | `files/test_capture_origin.yml` | `capture_origin.yml`の4象限(Semaphore起動×API可/不可、CLI起動×API可/不可)のうち、実行しているのは「Semaphore起動×API可」「CLI起動×API不可(DBフォールバック)」の2つだけ。「Semaphore起動×API不可(DBフォールバック)」と「CLI起動×API可」は未実行。後者はansyでのCLIリハーサル(reading path自体はansyにも存在しうる)で実際に通る組み合わせであり、未検証のまま残っている。 | Minor |
| m2 | `tasks/rollback.yml` | ロールバック候補選定(`Inspect irreversible-point rollback payloads`〜`Require a matching rollback generation`、mtime降順ソートと`rollback_to`一致フィルタ)を実行するテストが無い。過去に一度不具合が出た領域(依頼文が挙げる「ロールバック候補の選定」)だが、本diffはここへ手を入れておらず、退行の兆候も見ていない。 | Minor |
| m3 | `files/semaphore-upgrade-detached.py` | `version()`関数(バージョン文字列パース、過去に後方参照の不具合があった領域)を直接叩く単体テストが無い。`test_rollback_main_reports_observed_source`等でmockにより間接的に通っているのみ。 | Minor |

## Suggestions

なし(この案件のscopeでは不要)。

## 3. テスト2本の具体(実行して確認した範囲)

`python3 -m unittest roles/semaphore_upgrade/files/test_detach_boundary.py`(9件全pass)と、fixtureを`/tmp`配下に作った`ansible-playbook roles/semaphore_upgrade/files/test_capture_origin.yml`(localhost/`connection: local`、実system不可侵)を実際に実行して確認した。

- **踏んでいる**: `wait_for_jobs`のAPI経路・DB経路それぞれでの「origin終端+他ジョブ0」判定(`test_terminal_then_other_job_finishes`、`test_api_observes_origin_and_all_jobs`)、タイムアウト時のバイナリ復元(upgrade: `test_timeout_restores_without_stop_or_post`、rollback: `test_rollback_timeout_does_not_touch_binary`)、非タイムアウト例外時のupgradeの安全宣言(`test_non_timeout_failure_reports_observed_safe_state`)、通知本文の要素(`test_notification_shapes`)、`capture_origin.yml`の実体include(Semaphore起動×API可、CLI起動×DBフォールバックの2象限、`test_capture_origin.yml`)。
- **踏んでいない**: **rollbackモード+非タイムアウト例外**(M1の直接原因。upgrade版に相当するテストがrollback版に存在しない — これを実行して初めてM1が再現した)。`capture_origin.yml`の残り2象限(m1)。ロールバック候補選定ロジック(m2)。`version()`の直接ユニットテスト(m3)。AC10(Semaphore起動のはずが行が0件)はコード読解では正しく見えたため、自分でfixtureを組んで実行し、`assert`が実際に失敗することを確認した(既存テストには無い経路)。
- **quory実測が要る範囲(このレビューでは踏んでいない)**: AC1(実ジョブがsuccessで確定して残ること)、AC4(停止直前に他ジョブが走っている実際のレース)。requirement §7 R1/工程の通り、Testerがquoryで検証する対象であり、本レビューは`--check`を含め実hostへ一切触れていない。

## P0 対応表

| # | 要件 | 判定 | 根拠 |
|---|---|---|---|
| P0-1 | 起動元ジョブ確定後に停止 | 対応 | `wait_for_jobs`が`stop()`より前に必須(main.py 328-333) |
| P0-2 | 切り離し受理確認が遅延ありでも成立 | 対応 | `prepare_detached.yml`が`active`/`activating`両方を受理、コメントで意味論明記 |
| P0-3 | 停止直前に他ジョブ再確認、上限まで待つ | 対応 | `wait_for_jobs`ループが`active==0`を繰り返し確認 |
| P0-8 | 中止時にバイナリを戻す | 対応(upgradeモードのみ該当。rollbackはこのトランザクションで未installのため対象外、整合) | `undo_install_without_stop` |
| P0-4 | 通知が日本語・旧→新版/エディション等 | 対応 | `notification_text` |
| P0-5 | 但し書きの宛先を直す | 対応 | Slack本文から除去、`prepare_detached.yml`のAnsible debugへ移設 |
| P0-6 | resultの生ダンプ禁止 | 対応 | `notification_text`はフィールド個別整形、`test_notification_shapes`で`result=`不在を確認 |
| P0-7 | dpkg -V説明はロールバック時のみ | 対応 | 成功時upgradeのresultに`dpkg_bookkeeping`キー無し |

## AC 対応表

| AC | 判定 | 根拠 |
|---|---|---|
| AC1 | 設計上は対応(未実測) | quory実測はTester範囲。設計はwait_for_jobsの順序で支持される |
| AC2 | 対応 | `main.yml`の1〜600範囲assert |
| AC3 | 対応 | `active`/`activating`両方成功 |
| AC4 | 設計上は対応(未実測。ロジックはP0-3と共通) | 同上 |
| AC5 | 対応 | `test_notification_shapes`で確認 |
| AC6 | 対応 | `test_notification_shapes`で確認(復帰先・dpkg説明) |
| AC7 | 対応 | upgrade/rollback双方のタイムアウト経路で確認(`test_timeout_restores_without_stop_or_post`、`test_rollback_timeout_does_not_touch_binary`) |
| AC9 | 対応 | タイムアウトで`aborted`・後始末・通知(upgrade/rollback双方) |
| AC10 | 対応 | 自分でfixtureを実行し、0件マッチでassertが失敗することを確認(既存テスト無し) |
| AC11 | **未対応(rollbackモード)** | M1。upgradeモードは`test_non_timeout_failure_reports_observed_safe_state`で対応済みだが、rollbackモードの同型失敗を再現実行すると安全宣言が一切出ない |
| AC8 | 対応(前案件から不変、影響なし) | 抑止ロジックは本diffで変更なし |

## 前案件(semaphore_upgrade_automation)からの回帰

回帰は確認していない。P0-2(不可逆点まではAnsible担当)、P0-5〜P0-8(ヘルスウェイト・検証項目・識別)、P0-9(ansy CLIリハーサル)、P0-11(fail-closed既定)、P0-12(rollback CLI起動)に対応する箇所を現物で確認したが、いずれも本diffで壊れていない。

## 自己検証で確認したこと

- `git status` / `git diff --stat`で対象ファイルとscopeを確認。
- `python3 -m unittest roles/semaphore_upgrade/files/test_detach_boundary.py`を実行、9件全pass。
- `ansible-playbook roles/semaphore_upgrade/files/test_capture_origin.yml`を`/tmp`配下のfixture(実host名・実IP不使用、`connection: local`)で実行、全task pass。
- 上記に加え、`/tmp`配下で以下を独自に作成・実行して確認:
  - rollbackモード+DB読み取り不可(存在しないパス)での`main()`実行 → `status: "failed"`、安全宣言なしを確認(M1の直接証拠)。
  - `capture_origin.yml`をSemaphore起動形状+0件マッチのfixtureで単独実行 → `assert`が期待通り失敗することを確認(AC10)。
  - `"{{ bool_fact }}"`形の値渡しが`when:`句でネイティブ型(文字列化されない)を保つことをこの環境のansible-core(2.20.1)で確認(when句の文字列強制によるbranch誤動作の懸念を排除)。
- 実host(ansy/quory含む)へは一切接続していない。`--check`実行も行っていない。レビュー対象の実装は変更していない。

## 未解決事項

- M1の修正方法はCoordinator/Implementerの判断に委ねる。ただし`isinstance(error, BoundaryTimeout)`にモード非依存の安全宣言を紐付ける(またはrollbackモードの「boundary未到達」を明示するフラグを設ける)のが最小差分に見える。
- m1〜m3はblockingではないが、次回の同種案件でこの领域に触れるときは併せて塞ぐのが妥当。
- AC1/AC4はquory実測が前提であり、本レビューの範囲外(Testerの担当)。
