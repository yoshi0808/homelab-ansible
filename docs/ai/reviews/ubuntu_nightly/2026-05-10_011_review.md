# Review: ubuntu_nightly.yml レポート保存追加

Review date: 2026-05-10

## Summary

`2026-05-10_009_requirement.md` の要求に対して、`playbooks/ubuntu_nightly.yml` のレポート保存追加は適切に実装されている。

- reboot 要否にかかわらず JSON report を保存する構成になっている
- 保存先は `reports/ubuntu-nightly/{{ inventory_hostname }}_{{ timestamp }}.json`
- reboot 不要時は `End play if reboot is not required` の直前で保存している
- reboot 実行時は `Send result notification` の直前で保存している
- 保存処理は `delegate_to: localhost` / `become: false`
- report JSON は `.gitignore` の `reports/**/*.json` により Git 管理対象外

`playbooks/ubuntu_nightly.yml` に commit を止める問題は見つからなかった。

## Findings

なし。

## Notes

- `git diff` は未追跡ファイルの本文を表示しないため、本レビューでは `playbooks/ubuntu_nightly.yml` を直接読んで確認した。
- `playbooks/ubuntu_nightly.yml` と `docs/ai/reviews/ubuntu_nightly/*` は未追跡ファイルとして表示されているため、commit 時は意図したファイルを `git add` する必要がある。
- `scripts/ai-next.sh` の tracked diff は、前回確認済みのユーザー側ヘルパー修正として扱った。今回の `ubuntu_nightly.yml` レポート保存追加のブロッカーではない。

## Verification

以下を実行した。

```text
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ansible-playbook --syntax-check playbooks/ubuntu_nightly.yml
bash -n scripts/ai-next.sh
```

結果:

```text
playbook: playbooks/ubuntu_nightly.yml
```

どちらも構文エラーなし。
