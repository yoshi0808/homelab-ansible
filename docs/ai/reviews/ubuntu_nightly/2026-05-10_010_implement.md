# 実装内容: ubuntu_nightly.yml レポート保存追加

実装日: 2026-05-10

---

## 実装概要

`playbooks/ubuntu_nightly.yml` にレポート保存機能を追加した。

reboot の要否にかかわらず毎回 JSON を保存する。
`roles/radius_healthcheck/tasks/main.yml` の Save report タスクのパターンを踏襲し、
`ansible.builtin.copy` + `to_nice_json` + `delegate_to: localhost` / `become: false` で実装した。

---

## 作成・更新ファイル一覧

| ファイル | 種別 |
|---|---|
| `playbooks/ubuntu_nightly.yml` | 更新 |
| `docs/ai/reviews/ubuntu_nightly/2026-05-10_009_requirement.md` | 新規作成 |
| `docs/ai/reviews/ubuntu_nightly/2026-05-10_010_implement.md` | 新規作成（本ファイル） |

---

## 変更内容の要約

### play vars にレポートディレクトリを定義

```yaml
vars:
  nightly_report_dir: "{{ playbook_dir }}/../reports/ubuntu-nightly"
```

`playbook_dir` は `playbooks/` に解決されるため、`reports/ubuntu-nightly/` がリポジトリルート直下になる。
既存の `.gitignore` ルール `reports/**/*.json` により JSON は git 管理対象外。

### 追加 tasks 一覧

#### Ensure report directory exists（条件なし・常に実行）

```yaml
- name: Ensure report directory exists
  ansible.builtin.file:
    path: "{{ nightly_report_dir }}"
    state: directory
    mode: "0755"
  delegate_to: localhost
  become: false
```

reboot 判定直後に配置。両パス共通で1回だけ実行する。

#### reboot 不要パス

```yaml
- name: Build skipped report
  ansible.builtin.set_fact:
    nightly_report:
      host: "{{ inventory_hostname }}"
      executed_at: "{{ ansible_facts['date_time']['iso8601'] }}"
      reboot_required: false
      rebooted: false
      result: "SKIPPED"
  when: not reboot_required

- name: Save skipped report
  ansible.builtin.copy:
    content: "{{ nightly_report | to_nice_json }}"
    dest: "{{ nightly_report_dir }}/{{ inventory_hostname }}_{{ ansible_facts['date_time']['iso8601_basic_short'] }}.json"
  delegate_to: localhost
  become: false
  when: not reboot_required
```

`End play if reboot is not required` の直前に配置。
`executed_at` は play 開始時点の `ansible_facts['date_time']['iso8601']`。

#### reboot 実行パス

```yaml
- name: Build result report
  ansible.builtin.set_fact:
    nightly_report:
      host: "{{ inventory_hostname }}"
      executed_at: "{{ ansible_facts['date_time']['iso8601'] }}"
      reboot_required: true
      rebooted: true
      freeradius: "{{ freeradius_active.stdout.strip() }}"
      port_1812: "{{ 'listening' if port_1812_output.stdout | length > 0 else 'not listening' }}"
      port_1813: "{{ 'listening' if port_1813_output.stdout | length > 0 else 'not listening' }}"
      criticals: "{{ nightly_criticals }}"
      result: "{{ 'CRITICAL' if nightly_criticals | length > 0 else 'OK' }}"

- name: Save result report
  ansible.builtin.copy:
    content: "{{ nightly_report | to_nice_json }}"
    dest: "{{ nightly_report_dir }}/{{ inventory_hostname }}_{{ ansible_facts['date_time']['iso8601_basic_short'] }}.json"
  delegate_to: localhost
  become: false
```

`Gather facts after reboot` の後・`Send result notification` の直前に配置。
`executed_at` は reboot 完了・サービス確認後に再収集した `ansible_facts['date_time']['iso8601']`。

### tasks 全体の順序（変更後）

```
Check reboot-required flag
Check if needrestart is available
Run needrestart in batch mode
Evaluate reboot requirement
Ensure report directory exists      ← 新規追加（常に実行）
Build skipped report                ← 新規追加（when: not reboot_required）
Save skipped report                 ← 新規追加（when: not reboot_required）
End play if reboot is not required
Load mail variables
Send reboot start notification
Reboot host
Check freeradius service status
Check 1812/udp listening
Check 1813/udp listening
Evaluate service and port results
Gather facts after reboot
Print post-reboot result
Build result report                 ← 新規追加
Save result report                  ← 新規追加
Send result notification
Fail if critical
```

### 生成される JSON の例

reboot 不要時:
```json
{
    "host": "authy",
    "executed_at": "2026-05-10T02:00:00+09:00",
    "reboot_required": false,
    "rebooted": false,
    "result": "SKIPPED"
}
```

reboot 実行時（正常）:
```json
{
    "host": "authy",
    "executed_at": "2026-05-10T02:03:45+09:00",
    "reboot_required": true,
    "rebooted": true,
    "freeradius": "active",
    "port_1812": "listening",
    "port_1813": "listening",
    "criticals": [],
    "result": "OK"
}
```

---

## 実行した確認コマンド

```
ansible-playbook --syntax-check playbooks/ubuntu_nightly.yml
```

## 実行結果

```
playbook: playbooks/ubuntu_nightly.yml
```

構文エラーなし。

---

## 未対応事項

要求仕様の「初回除外スコープ」に従い、以下は未実装。

- レポートのローテーション・削除
- MD 形式でのレポート出力

---

## Next step files

- docs/ai/reviews/ubuntu_nightly/2026-05-10_009_requirement.md
- docs/ai/reviews/ubuntu_nightly/2026-05-10_010_implement.md
