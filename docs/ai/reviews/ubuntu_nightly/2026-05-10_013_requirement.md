# 要求仕様: ubuntu_nightly.yml executed_at JST修正

## 目的
レポートの executed_at がUTC（末尾Z）で出力されている。
radius_healthcheck と統一し、JST（+09:00）で出力されるよう修正する。

## 対象
- グループ: radius_servers
- ホスト: authy

## 作成・更新ファイル
- playbooks/ubuntu_nightly.yml

## 実施内容
- Build skipped report の executed_at をJSTに修正する
- Build result report の executed_at をJSTに修正する
- lookup('pipe', 'date +%Y-%m-%dT%H:%M:%S+09:00') を使う

## 参照ファイル
- なし

## 制約
- ubuntu_nightly.yml のみ修正する
- 他の task は一切変更しない
- 秘密情報を扱わない

## 初回実装スコープ
- executed_at の2箇所のみ修正

## 初回除外スコープ
- その他の修正

## shell / Ansible 責務分離
- shell: なし
- Ansible tasks: 担当
