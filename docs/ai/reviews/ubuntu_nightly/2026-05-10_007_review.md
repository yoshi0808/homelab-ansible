# Review: ubuntu_nightly.yml 追加実装 2回目

Review date: 2026-05-10

## Summary

`2026-05-10_006_implement.md` に記載された `playbooks/ubuntu_nightly.yml` の追加修正は、前回レビューの Low 指摘を解消している。

- `needrestart` の存在確認と実行パスが `/usr/sbin/needrestart` に統一されている
- reboot 後、結果通知の前に `setup` で `date_time` facts を再収集している
- 要求仕様の reboot_required 確認、条件付き reboot、freeradius / UDP 1812 / UDP 1813 確認、OK / CRITICAL 通知の流れは維持されている

`playbooks/ubuntu_nightly.yml` 自体に commit を止める問題は見つからなかった。

## Findings

### Info: `scripts/ai-next.sh` の変更はユーザー側の意図的なヘルパー修正

- File: `scripts/ai-next.sh`
- Lines: 206-218, 241-253, 291-321

今回の実装メモの「作成・更新ファイル一覧」には `playbooks/ubuntu_nightly.yml` と `docs/ai/reviews/ubuntu_nightly/2026-05-10_006_implement.md` のみが記載されている。一方で、実際の `git diff` には `scripts/ai-next.sh` の変更も含まれている。

ユーザー確認により、この変更は AI レビュー・実装依頼用のヘルパー関数に対するユーザー側の意図的な修正であることを確認した。変更内容は、Codex / Claude Code 向けプロンプトの誤投入防止文言追加と表示整形で、構文上の問題は見つからない。

そのため、これは `ubuntu_nightly.yml` 実装へのブロッカーではない。commit の切り方だけ判断すればよい。

- `scripts/ai-next.sh` の変更も今回 commit に含めるなら、implement / final 側に明記する
- もしくは `ubuntu_nightly.yml` 系の変更とは別 commit に分ける

## Notes

- `playbooks/ubuntu_nightly.yml` と `docs/ai/reviews/ubuntu_nightly/*` は未追跡ファイルとして表示されているため、commit 時は `git add` 対象に含める必要がある。
- `git diff` は未追跡ファイル本文を表示しないため、本レビューでは `playbooks/ubuntu_nightly.yml` を直接読んで確認した。
- `scripts/ai-next.sh` は `bash -n` を通過している。

## Verification

以下を実行した。

```text
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ansible-playbook --syntax-check playbooks/ubuntu_nightly.yml
bash -n scripts/ai-next.sh
```

結果:

```text
playbook: playbooks/ubuntu_nightly.yml
```

どちらも構文エラーなし。
