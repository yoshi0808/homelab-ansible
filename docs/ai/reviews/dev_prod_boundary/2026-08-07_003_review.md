# review: `journal-unit` に行数 operand を足す(D8)

日付: 2026-08-07 (JST)
Role: Reviewer(独立レビュー、実装には関与していない)
対象:
- `roles/dev_investigate/files/recovery-investigate-dispatch-quory.sh`(`journal-unit` case ブロック)
- `docs/ai/reviews/dev_prod_boundary/2026-08-03_008_phase3_check_catalog.md`(Q9 行 + `## 8.` 新設)
- 検査対象(記述の真偽を検査しただけで正本としない): `docs/ai/reviews/dev_prod_boundary/2026-08-07_002_implement.md`

requirement: `docs/ai/reviews/dev_prod_boundary/2026-08-07_001_requirement.md`(R1〜R7、AC1〜AC10)

## 検査方法

`git diff`で対象2ファイルの差分を読み、`docs/ai/reviews/dev_prod_boundary/2026-08-03_008_phase3_check_catalog.md`全文（§0不変条件、§1 Q9、§4/§6.3/§7.5総数表、§7 journal-ssh節、§8新設）を読んだ。スクリプト全体（384行）も通読し、`journal-unit`以外のcaseアーム・`read`によるarity検査・`deny_count`/`deny_invalid`ヘルパーの実装を確認した。

**実装記録の主張を鵜呑みにせず、ansy上でスクリプトを直接実行して再現した**（Reviewer役の許容範囲内のローカル検証。実ホストへは一切接続していない）。自分の作業用scratchpad配下に`journalctl`/`systemctl`のecho shimを作り、`SSH_ORIGINAL_COMMAND=<cmd> bash <script>`でAC1〜AC8相当を含む18パターンを実行し、argv・stderr・終了コードを直接観測した。検証後にshimディレクトリを`rm -rf`で削除し、`git status --porcelain`で対象2ファイル以外に変更が残っていないことを確認済み。

実行したケース（すべて再現、実装記録の記述と一致）:
- AC1（既定300・rc=0）、AC2（`lines=5000`が効く・rc=0）
- AC3/AC4/AC5相当（`10001`、`0`、`abc`、`1e4`、`-5`、`300;id`、`+300`がすべて`denied: invalid lines for journal-unit`・rc=1）
- AC6（4 operand目`extra`が`denied: too many parameters for 'journal-unit'`）
- AC7（`journal-ssh 24h 300`・`unit-cat <unit> x`がそれぞれ`denied: invalid parameter count`、`journal-ssh 24h`単体は引き続き`-n 300`固定で行数operandを受け付けない）
- AC8（unit不正・window不正がlines検証より先に`denied`）
- 追加で自分の判断で加えた境界値: `lines=018`（先頭ゼロ）が10進18として扱われ8進誤読が起きないこと、`lines=1`/`10000`の下限・上限受理、`lines=99999`（5桁regexは通るが範囲外）が拒否されること

## 契約（R/AC）充足状況

| # | 内容 | 判定 | 根拠 |
|---|---|---|---|
| R1 | 第3 operand、省略時300 | 充足 | 実測AC1/AC2 |
| R2 | regex+範囲、拒否文言・非ゼロ終了 | 充足 | 実測AC3/AC4/AC5、文言完全一致を確認 |
| R3 | `read`行不変、宣言arity3のまま | 充足 | diffで当該行(37行目)が無変更であることを自分で確認。`p3`は元々`report-show`が使う既存の宣言operandであり、新規の4番目は増えていない |
| R4 | 発行コマンドのオプション不変 | 充足 | diffで`-u`/`--since`/`-n`/`--no-pager`以外の追加が無いことを確認。operand文字列が直接渡るのは`-n`の値のみで、数値検証を通過済み |
| R5 | カタログに§8新設、総数不変を明記 | 充足 | §4/§6.3/§7.5の総数表は本diffで変更されておらず、§8本文も「総数は変わらない」と明記 |
| R6 | `docs/ai/status.md`のNext行削除 | **未充足・スコープ外** | 依頼文の書込先指定により本Implementerは対応せず、Coordinatorへ持ち越し。**本レビュー時点でもstatus.md:69の当該行は残存**（下記「未解決事項」） |
| R7 | 上限10000の根拠をスクリプム内コメントに残す | 充足 | `journal-unit`ケース直前のコメントに1〜2行で記載、requirement §5の算出根拠と数値が一致 |

AC1〜AC10はすべて実測または差分読解で確認済み、いずれもPASS相当。AC10（カタログとの一致）は文言レベルで突き合わせ、既定値300・上限10000・拒否メッセージ・operand順序（unit→window→lines）のすべてが requirement §5〜6・カタログ§8.1・実装コメントの3者で一致していることを自分で確認した。

## Critical Issues

なし。

## Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| 1 | `roles/dev_investigate/files/recovery-investigate-dispatch-quory.sh` | 12-13 | ヘッダコメントが「24 total」と記載しているが、`git log`で確認したところ`journal-ssh`（D7、commit `b0b6058`）追加時点でカタログ側の総数は25（§7.5）へ更新済みであり、このヘッダだけが24のまま取り残されている。**本D8の diff が作った不整合ではない**（D7時点からの既存drift）が、カタログとスクリプトの総数記述が一致していない状態は「カタログが実装より広い/狭い」の一種であり、次にこのファイルへ触れる機会にあわせて直すことを推奨する | ドキュメント整合性（規範文書レビュー観点6: 追跡表・索引のドリフト） |
| 2 | `docs/ai/status.md` | 69 | R6が要求する削除がまだ行われていない。「`journal-unit`が`-n 300`固定で...」という記述は本D8で解消済みの症状を指しており、そのまま残ると読み手に誤解を与える。Coordinator側の対応が必要 | 未解決事項（下記も参照） |

## What Looks Good

- **arity(I-4)は実質的に広がっていない**。`read -r check p1 p2 p3 extra`の宣言arity検査(37行目)は1文字も変更されておらず、4番目以降のtoken(`extra`)は従来どおり dispatch全体（journal-unit以外の全check含む）で拒否される。`journal-unit`ケース内の変更は「`p3`必須」を「`p3`任意」に緩めただけで、`p1`/`p2`の必須性は維持されている。他checkのarityへの影響はゼロ（`journal-ssh`・`unit-cat`を実測して確認）。
- **検証順序（unit→window→lines）が実装どおり**。`_is_valid_unit`→windowの`case`→lines検証、の順で並んでおり、unit/window不正時はlinesの正当性を評価する前に`deny_invalid`で終了する。実測でも確認した。
- **8進誤読の回避が正しく効いている**。`^[0-9]{1,5}$`は`00300`のような先頭ゼロを通すが、`10#$p3`でbase-10算術評価を強制しているため、先頭ゼロを持つ値が意図せず8進として解釈されクラッシュ（bashの`(( ))`は`018`のような8を含む先頭ゼロ数値を素で評価すると"value too great for base"エラーになる）することがない。この対策が無い状態を想定して自分で`lines=018`を実行し、正しく10進18として扱われることを確認した。
- **書込語彙・新しいcommand・operand文字列を直接コマンドへ渡す経路は増えていない**。diff全体を読んだ限り、I-1が挙げる語（`systemctl start/stop/restart/enable`等）や`eval`は導入されておらず、`journalctl`のオプション自体も増えていない。operandが素通りするのは検証済み整数の`-n`引数のみ。
- **カタログ§8の記述と実装が文言レベルで一致**（拒否メッセージ、既定値、上限、operand順序、arity不変の説明）。requirement→カタログ→実装の3者間で数値・文言のずれは見つからなかった。
- **`journal-ssh`（D7）・class G/P（journal-unit形自体を持たない/対象外）への影響がゼロ**であることを実測・diff読解の両方で確認した。カタログ§8.2の「変わらないこと」節の記述はすべて現物と一致している。
- Implementerの自己検証記録（AC1〜AC10の実測ログ）は、私が独立に再実行した結果と完全に一致した。事後に作られた`/tmp`のargv観測用shimも残存が無いことを確認した。

## Verdict

**Approve**

R6のみ未充足だが、これはrequirement自体が「本Implementerの書込先指定に含まれない・Coordinator側の対応事項」と明記しているスコープ外項目であり、本diff（スクリプト+カタログ）自体の欠陥ではない。対象2ファイルの変更は授権境界を広げず、arityも広げず、カタログとの記述も一致している。

## 未確認事項

- `docs/ai/status.md`のNext行削除（R6）はCoordinator側の対応待ち。本レビューはこの状態を確認しただけで、自分で編集していない。
- ヘッダコメントの「24 total」drift（Suggestion #1）は本D8とは独立した既存の齟齬であり、D8のクローズを妨げるものではないと判断したが、確定的な「直すべきか」の判断はCoordinatorに委ねる。
- quoryへの実配備後の動作（`SSH_ORIGINAL_COMMAND`を経由した実際のforced command経路、`systemd-journal`グループの実効性)は本レビューの範囲外（requirementが明記するとおりscope外）であり、配備後はTesterによる実ホスト検証が必要。
