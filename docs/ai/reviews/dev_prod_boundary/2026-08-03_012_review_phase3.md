# review: Phase 3 実装差分(独立レビュー)

日付: 2026-08-03 (JST)
対象: 未commit差分(`git status`時点)。3 subagent並行実装(Step1/2/3、`..._009`/`_010`/`_011`)
基準: `2026-08-03_008_phase3_check_catalog.md`(D5 承認済み正本)
検査者: Reviewer(実装には関与していない独立体)

## 確認範囲(状態を変えない確認のみ)

- `git status --porcelain` で対象差分の全ファイルを確認
- 変更ファイルは `git diff`、新規ファイルは `Read` で全文読了。対象:
  `roles/dev_investigate/` 一式、`roles/recovery_exec/files/{incident-bundle-helper,homelab-incident-bundle,recovery-reports-helper,homelab-reports,claude-investigate.pub,claude-investigate-pve.pub}`、
  `roles/recovery_exec/{tasks/main.yml,tasks/target_setup.yml,templates/AGENTS.md.j2,templates/authorized_keys.j2,templates/authorized_keys-pve.j2,templates/recovery-investigate-dispatch.sh.j2,templates/recovery-investigate-dispatch-pve.sh.j2}`、
  `roles/deployment_drift_check/defaults/main.yml`、`playbooks/README.md`、`playbooks/dev_investigate_setup.yml`
- 書込語彙の静的走査: `grep -nE` で `pvesh (create|set|delete)` / `systemctl (start|stop|restart|enable|disable|reload)` / `qm (start|stop|reset|destroy)` / `tee` / `rm -` / `mv` / `cp -` / `eval` / リダイレクト / `dd` / `truncate` / `chmod` / `chown` / `kill` / `ln` / `sed -i` / `-delete` を対象ファイル全体へ実施(コメント文字列以外の実マッチ0件)
- カタログ §1〜§3 の各 check と実装の case 文を1件ずつ突合(operand 検証・enum・実行内容)
- 実ホストへは一切接続していない。実装記録(`_009`〜`_011`)の「確認済み」は根拠として使わず、上記の現物突合のみで判定した

## Critical Issues

なし。

## Major Issues

| # | File | Line | Issue | Severity |
|---|---|---|---|---|
| M1 | `roles/dev_investigate/tasks/main.yml` | 15-24 | `dev-investigate` ユーザーを `systemd-journal` グループへ入れているが、カタログ §1 は ACL 項目として `/home/yoshi` traverse と `semaphore.db` read の2件しか承認していない。journal read 権限の付与はカタログのどこにも明記されていない。**Q-C(journal-system/dmesg)・Q9(journal-unit)が機能するには必要な権限**であり、`roles/recovery_exec/tasks/target_setup.yml:59` の既存パターン(authy/monnie の recovery-exec ユーザー)を踏襲した実装判断としては筋が通るが、**D5 で承認されたのは「20 checks の表」であり、その表を実現するための ACL/グループ付与そのものは明示的な承認対象になっていない**。実害は forced command が唯一の授権境界(I-7)であるため無制限ではなく、dev-investigate は `no-pty` でシェルを持てず、journal 全体への読み取り能力はスクリプトが許可する `journal-system`/`dmesg`/`journal-unit`(固定 unit enum)の3経路でしか行使できない。とはいえ「グループ付与」というカタログ非記載の権限拡張が実装に混入している事実は、Yoshinobu が見た表からは読み取れない。カタログへの追記または再確認を推奨 |

## Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| S1 | `roles/dev_investigate/templates/recovery-investigate-dispatch-quory.sh.j2` | 全体 | Jinja 構文が0件(`grep -c '{{\|{%'` で実測)。`template` モジュールで配備しているため、`roles/deployment_drift_check/defaults/main.yml` の「①copy配備物」(内容の自動突合対象)から外れ、Tier 2(保留)へ落ちる。中身が完全に静的である以上 `copy` + `files/` で配備すれば今すぐ①へ入れられた。ドリフト検査の対象を広げる機会を逃している | 保守性/duplication-reuse |
| S2 | `roles/deployment_drift_check/defaults/main.yml` | ①(17-38行付近) | 今回 `copy` で新規配備した `incident-bundle-helper`(`/usr/local/sbin/incident-bundle-helper`)と `homelab-incident-bundle`(`/usr/local/bin/homelab-incident-bundle`)が①の一覧に追加されていない。既存の `recovery-reports-helper`/`homelab-reports` も同じく①に無く、これは新規のリグレッションではなく既存の欠落を引き継いだだけだが、この Phase で同種のファイルを4本足しながら①を触らなかった点は指摘しておく | duplication-reuse / 網羅性 |
| S3 | `FILE_RE='^[a-zA-Z0-9_+-]+\.json$'` | `roles/recovery_exec/files/homelab-reports:28`, `roles/recovery_exec/files/recovery-reports-helper:26`, `roles/dev_investigate/templates/recovery-investigate-dispatch-quory.sh.j2:59` | 同一の正規表現が3箇所に文字列として複製されている(`BUNDLE_ID_RE`/`ID_RE` も `incident-bundle-helper`/`homelab-incident-bundle`/dispatch-quory.sh.j2 の3箇所)。各層が独立に再検証するという設計意図(plan §1.2 / catalog I-7 の defense-in-depth)自体は正しく、コメントで相互参照もされているが、**将来どちらか一方だけ変更されると層ごとに許可集合がズレる**。今回は3箇所とも値が一致していることを確認済み。定数化はできない構造(独立した実行ファイル)なので、次に触るときに3箇所を横断更新する運用上の注意点として記録に残す | duplication-reuse |

## What Looks Good

- **AC9(書込ゼロ)**: `roles/dev_investigate/templates/recovery-investigate-dispatch-quory.sh.j2` の20 check、`recovery-investigate-dispatch.sh.j2` の追加2 check(G1/G2)、`recovery-investigate-dispatch-pve.sh.j2` の追加2 check(P1/P2)、`incident-bundle-helper`/`homelab-incident-bundle`、`recovery-reports-helper`/`homelab-reports` の全経路を読み、書込語彙(`pvesh create/set/delete`、`systemctl start/stop/restart/enable`、`qm start/stop`、リダイレクト、`tee`/`rm`/`mv`/`cp`、`eval`)が1つも到達不能であることを現物で確認した。静的 grep でも実マッチ0件
- **カタログとの一致**: class Q の20 check(Q1-Q4/Q5-Q7/Q-C 8種/Q8-Q12)、`_is_valid_unit` の15 unit enum、`deployed-hash` の8 name→パス表、class G の G1/G2、class P の P1/P2 を1件ずつ突合し、**カタログに無いチェックの混入は無い**(quory dispatch の case 文は正確に20件、G/Pはそれぞれ2件の純追加)
- **`bundle-grep` 不在**: 初期リリースから外す判断どおり、任意文字列を operand に取るチェックはどこにも存在しない
- **operand 検証の境界**: `BUNDLE_ID_RE`/`NAME_RE`/`FILE_RE` はいずれも `^...$` の完全一致で `/` を許さず、traversal が構造的に組めない。`under_base()` による realpath 二重防御も両 helper に存在する。enum 系(`bundle-show` のファイル名、`investigation-show` の拡張子、unit 一覧、`semaphore-query` のクエリ名)はすべて `case` 文のリテラル比較で、宣言外の値は `deny_invalid`/`usage` へ落ちることを確認した(通す側・落とす側の双方を読んだ)
- **AC10(Codex/Claude Code 同一出力)**: `authorized_keys.j2`/`authorized_keys-pve.j2` は新エントリも既存エントリと**同一の** `command="..."` を指しており、dispatch script 側に identity によるコード分岐は無い。class Q は元々 Codex 鍵が無いクラスなので、代わりに `incident-bundle-helper`/`recovery-reports-helper` を Codex 用 wrapper(`homelab-incident-bundle`/`homelab-reports`)と dispatch の両方が同一実体として呼んでいることを確認した(plan §3 AC10 の代替確認方針と一致)
- **G1(FILE_RE 拡張)**: `+` の1文字のみを追加し `.` を足していないことを3ファイルすべてで確認。`.` を許さない理由のコメントも3箇所に存在し矛盾していない
- **鍵は公開鍵のみ**: `claude-investigate.pub`/`claude-investigate-pve.pub`/`dev-investigate.pub` はいずれも `ssh-ed25519 ...` の公開鍵1行のみで、秘密情報は含まれない
- **`deployment_drift_check_forced_command_keys`**: authy/monnie を `entries: 2→3`、pve1/pve2 を `entries: 1→2` へ更新済みで、新規エントリ追加と数値が一致している(G2 と合致)
- **sudo境界**: class Q の新 arm(`deployed-hash`/`unit-cat`)は対象パスがすべて world-readable な root:root ファイルか dispatch 自身が所有する `authorized_keys` であり、sudo なしで整合している。class G/P の `authorized-keys` deployed-hash も同様に自ファイル読み取りで sudo 不要

## 未解決事項

- **M1**: `systemd-journal` グループ付与がカタログの明示的な承認対象に含まれるかは、Reviewer の判断範囲を超える(read-only 拡張であり実害は限定的だが、D5 承認プロセスの外で足された capability である事実は残る)。Coordinator から Yoshinobu へ確認を推奨
- Tier 2(template配備物のドリフト内容比較)は本Phaseでも据え置き。S1で述べたとおり、今回の新規テンプレートは静的内容にもかかわらずTier 2側に留まっており、この判断自体はplanの既定路線どおりで問題はないが、機会損失として記録した
- 受入検証(AC8/AC9/AC10/AC19/AC20 の実機確認)はTesterの担当であり本レビューでは実施していない

## Verdict

**Request Changes**(M1の扱いをCoordinator/Yoshinobuに確認してから次工程へ進めることを推奨。Critical無し、AC9の書込露出は確認した範囲で無し)

---

## Coordinator の処置(2026-08-03)

| finding | 処置 |
|---|---|
| **M1**(`systemd-journal` グループ付与がカタログの ACL 行に無い) | **Yoshinobu へ上げる。** 承認済みの Q9 / `journal-system` / `dmesg` は journal 読取なしには成立せず、代替は「sudo を与える」(この identity の契約そのものを壊す)か「3チェックを落とす」しかない。ただし**グループ所属が与えるのは enum で絞った unit ではなく全 journal の読取**であり、カタログの表と粒度が一致しない。推奨は「受け入れ、カタログの ACL 行へ明記して追認」 |
| **S1**(quory dispatch は Jinja 0 件なので `copy` にできる) | **反映した。** `roles/dev_investigate/files/recovery-investigate-dispatch-quory.sh` へ移し、task を `copy` へ変更。これで**ドリフト検査 Tier 1 の適格になる** — Phase 4 は配備物の版ずれを常時検出できることを前提に鍵を捨てるため、調査面そのものが検査対象から外れているのは前提の欠落だった。将来 Jinja を持ち込むと検査から外れる旨をコードのコメントに残した |
| **S2**(新設 helper 2本がドリフト検査のカタログに無い。既存3本も同様) | **反映した。** 新設2本 + quory dispatch + **既存の漏れ3本**(`recovery-reports-helper` / `homelab-reports` / `homelab-semaphore-query`)を `deployment_drift_check_files` へ追加。あわせて `dev-investigate` の `authorized_keys`(エントリ1本)を `deployment_drift_check_forced_command_keys` へ追加した(`become: true` で読むため 0600 でも採取できることを `tasks/collect.yml` で確認済み)<br><br>追加時点の実測: `recovery-reports-helper` と `homelab-reports` は **repo と quory で hash が異なる**(本Phaseで repo 側を変更し、まだ配備していないため)。`homelab-semaphore-query` は一致。**配備前に日次検査が回ればこの2件が正しく検出される** |
| **S3**(`FILE_RE` / `BUNDLE_ID_RE` が3ファイルずつ重複) | **却下する。** この重複は意図的な多層防御である — wrapper が検証し helper が独立に再検証する構造は既存 `homelab-reports` → `recovery-reports-helper` から続く形で、片方を信頼しないことがこの設計の要点である。単一の定義を共有させると、共有した側が壊れたとき全層が同時に壊れる。**残る risk は「片方だけ編集して判定が食い違う」ことだが、食い違いの向きは常に安全側**(どちらか厳しい方で落ちる)であり、緩む方向へは倒れない。検査で塞ぐ案は、正規表現の意味的な同値判定が要るため `scripts/check-doc-consistency.py` が扱えないクラスと同型で、`docs/ai/status.md`「載せていないもの」が既に同じ理由で退けている |

**S1 / S2 は Coordinator が直接実装した。** 変更は機械的(`template`→`copy` の移設と、カタログへの行追加)で、`--syntax-check` と `ansible-lint`(production profile、0 failure / 0 warning)を通している。
