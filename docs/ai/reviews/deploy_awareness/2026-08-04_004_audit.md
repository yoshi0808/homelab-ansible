# audit: 配備が要ることに気づける仕組み(A: 検出後の案内 / B: commit時の予告)

日付: 2026-08-04 (JST)
対象: `docs/ai/reviews/deploy_awareness/`(`2026-08-04_001_requirement.md` / `_002_implement.md` / `_003_review.md`)

## 再構成できたか

**できた。** requirement(R1〜R10、AC1〜AC8、OQ1/OQ2)→ implement(実装・OQ確認・R1-a判断・R9/R10追補・自己検証)→ review(独立照合・Approve、Suggestion 2件のうち1件をCoordinatorが直接反映)という流れに矛盾はなく、途中の方針転換(R8撤回→R9/R10追加)もimplement §9・requirement §5に整合して記録されている。

現物との突合を行った。

- `git status --porcelain` / `git diff --stat` は成果物一覧(implement §1、review「検査方法」)と一致した(変更5ファイル、新規3件)。
- `roles/deployment_drift_check/defaults/main.yml` / `tasks/evaluate.yml` / `tasks/report.yml`、`inventories/homelab/host_vars/quory.yml`、`scripts/git-pre-commit-check.sh` の実diffを読み、implement §3・§4・§9の記述(delegate_to 5エントリへの `run_host: quory`、R10の2 unit、R1-a除外の理由コメント)と食い違いなし。
- `roles/recovery_push/tasks/sender_setup.yml` の `delegate_to`、`playbooks/recovery_push_setup.yml`(`hosts: quory`)/`playbooks/recovery_exec_setup.yml`(`hosts: dev_nodes:control_nodes`)を直接読み、implement §3.1・§9.1の「`-l authy` は hosts(0)」という主張の前提を裏取りした。
- `scripts/tests/fixtures/deploy_needed/run-fixture-checks.sh` を実行し、AC4/AC5/AC6/AC8 と `r9_run_host_override` の5件すべてPASSを確認した(implement §5・review「検査方法」の主張どおり)。
- `ansible-playbook playbooks/deployment_drift_check.yml --syntax-check` はPASS。
- `git worktree add --detach` の使い捨てコピーへ本diffを反映し `git add -A` した状態で `python3 scripts/check-deploy-needed.py` を実行し、**実カタログの全 `playbook:` が実在すること(R5)を独立に再現した**(exit 0)。
- review が Approve とともに指摘した Suggestion 1(`check_r4` のdocstringに対象範囲の非対称を明記)は、Coordinator対応表のとおり `scripts/check-deploy-needed.py` L161-166 に実際に反映されていることを確認した。Suggestion 2(Jinja空白)は見送りの記録どおり未着手。

## 指摘

| # | 分類 | 内容 | クローズを妨げるか |
|---|---|---|---|
| 1 | 差分の外側の陳腐化 | `docs/ai/context/operations/code-delivery-to-production.md` §2(requirementが問題提起の根拠として直接引用している正本)は、「変更する人が気づく手段が無い」「検出しても直し方が分からない」という記述のまま更新されていない。本案件はこの2つの穴をまさに埋めるものだが、`code-delivery-to-production.md` にはSlack通知の「直し方」行(R3)も、commit時予告・pre-commitブロック(R4〜R6)も一切触れられていない。「突合の手段」節は `deployed-hash` の手動比較のみを案内しており、今後この文書だけを読む人は新しい仕組みの存在を知れない。 | 妨げない — 内容に誤りは無く、追記で閉じられる |
| 2 | 状態の正本との突合 | requirement §8は「効き目の確認は `status.md` の Watch へ置く」と明言しているが、現時点の `docs/ai/status.md` に本案件のWatch行は無い(検証は decoy/fixture のみで完結しており、実運用でSlack通知に「直し方」行が意図どおり出るかは未観測)。commit前のため未反映であること自体は自然だが、クローズ前に追加されないと「新たに生じた観測待ちが落ちている」状態のままになる。 | **クローズを妨げる** — status.mdへのWatch行追加は他の類似案件(worktree_sync、cert_renew等)でも同じセッション内でCoordinatorが行っている実施事項であり、本案件だけそれを欠くと運用側が本番での初回ドリフト発生時に「直し方」行の有無を確認する手がかりを失う |

## 受入条件の充足確認

AC1〜AC8はいずれもimplement §5・review「検査方法」に対応する検証記録があり、上記の独立再現(fixture実行・syntax-check・R5再現)でも矛盾は出なかった。AC7(host_vars記載)は現物を直接確認し一致した。

## 未解決の明示

- implement §6・§9のとおり、当初の申し送り2件(delegate_to 5エントリ、R2同型の2エントリ)はR9/R10で解消済みとして閉じられている。現物のカタログにこの2種の未解決は残っていない(defaults/main.ymlのdiffで確認済み)。
- requirementの非ゴール(自動配備・Tier 2内容検査・カタログ拡張)は実装に含まれておらず、逸脱もない。

## 判定

**条件付き受入。**

指摘2(`docs/ai/status.md` へのWatch行追加)を反映すればクローズしてよい。追加すべき内容は、requirement §8・AC1の趣旨(次回ドリフト発生時にSlack本文へ「直し方」行が実際に出るか)を検証手段として明記したものであり、既存の「日次ドリフト検査が `drift: 0` へ戻るか」行と統合するか別行にするかはCoordinatorの裁量。指摘1は妨げにしないが、次にこの文書を読む人のために追記を推奨する。

再監査は前提としない。

---

## Coordinatorのクローズ判断(2026-08-04)

指摘2件とも反映し、本案件を**クローズする**。再監査は行わない。

| 指摘 | 対応 |
|---|---|
| 1. `code-delivery-to-production.md` が新機構を反映していない | **反映した**。§2 へ「気づくための2点」を新設し、commit時の予告とドリフト時の「直し方」が同じカタログを読むこと、および**「直し方」が載らないfindingが存在すること**(流しても直らないものには書かない)を明記した。この節を落とすと、行が無いことを異常と誤読する読み手が出る |
| 2. `status.md` に本案件のWatch行が無い | **追加した**。発火条件は「次にドリフトが検出されたとき」。**decoyでは観測済みだが本番の通知では一度も出ていない**ことを検証手段へ明示した |

指摘1をクローズ阻害でないと判定した点についてはAuditorと同意見だが、**先送りしない**判断を採った。この案件の目的が「気づけるようにすること」である以上、経路を説明する正本が古いままでは目的の一部が未達である。
