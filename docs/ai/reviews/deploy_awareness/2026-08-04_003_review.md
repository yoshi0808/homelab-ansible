# review: 配備が要ることに気づける仕組み(A: 検出後の案内 / B: commit時の予告)

日付: 2026-08-04 (JST)
対象: `2026-08-04_001_requirement.md`(R1〜R10、AC1〜AC8)に対する未commit差分
参照した実装記録: `2026-08-04_002_implement.md`(検査対象であり、判定の根拠として直接は使わず、記載の主張は現物で独立に確かめた)

## 検査方法

- `roles/deployment_drift_check/{defaults/main.yml,tasks/evaluate.yml,tasks/report.yml}` の差分全文、`inventories/homelab/host_vars/quory.yml` の差分、`scripts/git-pre-commit-check.sh` の差分、`scripts/check-deploy-needed.py` 全文、`scripts/tests/fixtures/deploy_needed/` 全ファイルを読んだ。
- カタログの `playbook:` を持つ全21エントリについて、対応する `playbooks/*_setup.yml` の `hosts:` 宣言と、配布taskの `delegate_to` の有無を現物(role tasks)から individually 確認した(推測・implement記録の転記に頼らない)。
- `roles/deployment_drift_check` 単体・リポジトリ全体で `ansible-lint` と `--syntax-check` を実行した。
- `scripts/tests/fixtures/deploy_needed/run-fixture-checks.sh` を実行し、AC4/AC5/AC6/AC8/R9の5fixtureが全てPASSすることを確認した。
- `python3 scripts/check-deploy-needed.py`(working tree読み、参考実行)と、defaults/main.ymlを直接パースして全 `playbook:` の実在をfilesystem上で確認するスクリプトを実行した。
- `/tmp` 配下に decoy inventory(`ansible_connection: local`、実ホスト名を使わない `decoyhost1`)を作り、`playbooks/deployment_drift_check.yml` を `-e @vars.yml -e skip_notifications=true` で2回実行した(①ハッシュ不一致1件+playbook無し1件の混在、②単発)。Slack本文の suppressed debug 出力と `reports/drift/latest.json` を直接確認した。**実ホスト(quory/ansy含む)へのansible/ssh到達はゼロ。** decoy作業ディレクトリは検証後に削除し、`git status --porcelain` が検証前後で同一(未追跡/未ステージのみ、`git add`なし)であることを確認した。
- pre-commitチェーンは `bash -n` によるshell構文検査と `py_compile` のみに留め、実際の `git add`/`git commit` は一切実行していない(役割上の禁止事項に該当するため)。

## Summary

R1〜R10・AC1〜AC8のいずれについても、実装記録の主張を現物で裏取りでき、矛盾や復元しない案内は見つからなかった。特に懸念していた「`playbook:` を持つエントリが本当に復元するか」「持たないエントリの除外が過剰でないか」の両方向を、対応する `playbooks/*_setup.yml` の `hosts:` と `delegate_to` を全21エントリぶん個別に照合して確認したが、すべて正しい。通知本文・JSONレポート・commit時予告の3経路も同じ `run_host` フォールバックロジックを使っており、decoy実行で実際に一致した値が出ることを確認した。pre-commitの既存5系統・R5のブロック条件も、fixtureと実カタログの両方で意図通りに動作した。

## Critical Issues

なし。

## Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| 1 | `scripts/check-deploy-needed.py` | `check_r4` (L160-167) | R4は `deployment_drift_check_files` のみを見ており、units/forced_command_keysのsrcに相当するものが無いため対象外なのは妥当。ただし将来 `playbook:` を持つが `src` を持たないentry種別が増えたとき、この関数のdocstring/コメントは「filesのみ」という前提を明示していない(呼び出し元のコメントには書いてあるが関数自体には無い)。実害はないが、関数定義の直下に1行足すと将来の変更者に親切。 | Readability |
| 2 | `roles/deployment_drift_check/tasks/report.yml` | L96-98 | `{% if f.playbook %}` ブロックの前後で `trim_blocks`/`lstrip_blocks` の設定に依存した余白が出うる(今回のdecoy実行では実害となる崩れは確認できず、視認性への影響は無視できる範囲)。次にこのテンプレートを触るときに気に留める程度でよい。 | Style |

## What Looks Good

- **R1-a(復元しない案内を書かない)の判断範囲が過不足なく現物確認されている。** OQ1/OQ2で名指しされた3パターンに加え、implement記録が独自に見つけた5+2パターンの除外理由(`delegate_to`のhosts不一致、`enabled`/`state`の非対称なgate)を、該当role task(`recovery_probe/tasks/main.yml`、`recovery_io/tasks/main.yml`、`incident_capture/tasks/main.yml`、`incident_investigate/tasks/main.yml`、`recovery_push/tasks/sender_setup.yml`、`recovery_exec/tasks/target_setup.yml`)を実際に読んで独立に裏取りしたが、すべて主張どおりだった。
- **R9の `run_host:` が必要な5エントリと不要なエントリの切り分けが正確。** `playbooks/recovery_push_setup.yml`(`hosts: quory`)・`playbooks/recovery_exec_setup.yml`(`hosts: dev_nodes:control_nodes`、quoryは`control_nodes`に属する)を確認し、`delegate_to` を伴う配布のみに `run_host: quory` が付与され、直接配布(quory自身のplayでdelegate_to無し)のエントリには付与されていないことを確認した。逆方向(過剰な除外)も確認 — `recovery_probe_setup.yml`/`recovery_io_setup.yml` は `hosts: dev_nodes:control_nodes` だが `delegate_to` を使わない直接配備であり、quoryが `control_nodes` に含まれるため `-l quory` がそのまま届く。`run_host` を付けなかったのは正しい。
- **3経路(Slack本文・JSONレポート・commit時予告)が同じ値を使うことを実測で確認した。** decoyの `run_host: some-other-host` フィクスチャに相当する検証(実際は本レビューで別途、file findingのrun_host無し/ありパターンをdecoy実行)と、`r9_run_host_override` fixtureの `must_not_contain: "(authy, monnie)"` 検査により、`evaluate.yml`(finding生成)・`report.yml`(Slack本文)・`check-deploy-needed.py`(R4)の3箇所が同一の「`run_host`優先、無ければfindingのhost」ロジックで揃っていることを確認した。
- **R5(ブロック)とR4/R6(警告のみ)の分離が正しい。** `check_r5`はカタログ全体を毎回検査し(当該commitでstagedかどうかに関係なく)非ゼロで終了、`check_r4`/`check_r6`は常にexit 0で終わる構造になっており、`main()`の `exit_code` 変数もR5のみが書き換える。fixture `ac6_missing_playbook` で実際にexit 1・エラー行に壊れたエントリ名が出ることを確認した。
- **既存pre-commit4系統(gitleaks/dangerous-files/IPv4/vault-header/YAML構文/tester-gate-lint/doc-consistency)への影響なし。** 新規check(check-deploy-needed.py)は既存チェックの後ろに追記されているだけで、既存ロジックへの変更は無い。bash構文チェック・python compileともにPASS。
- **AC1/AC2/AC3をdecoy実行で直接再現した(implement記録の主張を鵜呑みにしていない)。** ①ハッシュ不一致findingに `直し方: ansible-playbook playbooks/deployment_drift_check.yml -l decoyhost1` が付き、playbook実行自体はfailed=0で終わること、②playbook無しfindingには「直し方」行が出ず、JSONの`playbook`が`""`であること、を実際のSlack本文(suppressed debug)とJSONファイルの両方で確認した。
- **IPアドレス直書きなし。** 新規・変更ファイル全体を `grep -E '([0-9]{1,3}\.){3}[0-9]{1,3}'` で走査し、ヒットなし。
- **時刻表記規約に抵触なし。** 変更差分に新規の時刻生成コードは無く(既存の `report.yml` L11-12 のJST生成ロジックは未変更)、fixture/README内の時刻言及もリテラル貼付けではない。
- **shell/commandモジュールの変数注入対策も既存のまま。** `collect.yml` の `systemctl show {{ item.name | quote }}` は変更前から `quote` フィルタを使っており、今回の `playbook:`/`run_host:` フィールド追加はcommand構築に影響しない(コマンド文字列を構成する式に triggersされていない)。
- **重複再利用チェック**: `check-deploy-needed.py` がカタログをパースするロジックを `check-doc-consistency.py` に統合しない判断(Won't節)はrequirement自身の指示どおりであり、独自の理由も明記されている(パース処理を二重に持たない設計判断の一部として、既存の `check-doc-consistency.py` の `git_ls_files`/`read_index_content` の実装パターン(git indexから読む、同じ規約)を踏襲しているのは確認済みで、コード自体の重複ではなく設計方針の踏襲)。

## Verdict

**Approve**

---

## Coordinatorの対応(2026-08-04)

| # | 対応 | 理由 |
|---|---|---|
| Suggestion 1(`check_r4` の対象範囲コメント) | **採用**。Coordinatorが直接修正した | 「R4はfilesのみ、R5は全section」という対象範囲の非対称は、次に種別を足す人が最も踏みやすい。関数の直下に前提を書いた |
| Suggestion 2(`report.yml` のJinja空白) | **見送り**。修正しない | decoy実行で実害となる崩れが出ておらず、Slack本文の視認性にも影響していない。**実害の観測が無い状態でテンプレートの空白制御を触ると、通知本文という「壊れても静かな出力」に回帰を持ち込む側のリスクが上回る**。次にこのテンプレートを触るときに併せて見る |

Verdictは**Approve**であり、上記1件の修正は差し戻しではない。
