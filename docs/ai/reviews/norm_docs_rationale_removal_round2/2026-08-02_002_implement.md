# implement: 分類・索引3本から経緯・根拠を落とす

正本: `docs/ai/reviews/norm_docs_rationale_removal_round2/2026-08-02_001_requirement.md`

## 変更したファイル

| ファイル | 種別 |
|---|---|
| `docs/ai/memory-classification.md` | 変更(未staged) |
| `docs/ai/context-classification.md` | 変更(未staged) |
| `docs/ai/role-context-matrix.md` | 変更(未staged) |

ADR等の新設は行っていない(理由はR2節を参照)。上記3本以外は変更していない。

## 要件充足状況

| ID | 状態 | 備考 |
|---|---|---|
| R1 | 充足 | §5「落とす」列に該当する日付つき注記・改訂の経緯・過去の実例/事故の説明・計画書の足場をすべて除去した。詳細はAC3節の突合表 |
| R2 | 該当なし(受け皿確認は実施済み) | 落とした断片はいずれも、除去後も残る現在の規則(「〜しない」「〜する」という指示文そのもの)から独立して存在していた経緯・実例・引用であり、値そのものの根拠がどこにも無くなるケースは無かった。詳細は本節末尾 |
| R3 | 充足 | `TODO6-0`〜`TODO6-3`、`TODO2-1`〜`TODO2-4`、`TODO5-2`、`TODO7-2`、`Phase6`、`Phase2`、`pilot`/`pilot1`/`pilot2`/`pilot3`、`agent_skills_reorganization_plan.md`への参照をすべて見出し・本文から除去した。改訂後、3本に`TODO`/`Phase[0-9]`/`pilot`/`agent_skills_reorganization_plan`の文字列は0件(grep確認、下記) |
| R4 | 充足(2件STALE→削除、1件は元々該当なしと確認) | 詳細は下記「R4の確認結果」 |
| R5 | 充足 | 3本間の相互参照(4箇所)はすべて保持。外部文書からの参照は`scripts/check-doc-consistency.py` check3(内部リンク切れ検査、91件比較・0件不一致)で確認。手動でも該当箇所を洗い出し、壊れていないことを確認(下記) |

### R2: 受け皿確認の結果

前案件(`role-routing-index.md`)では、model/effort配分の「根拠」列がリポジトリ中どこにも存在せず、ADR-010を新設してから落とした。今回、同様の「落とすと値の根拠が消える」ケースが無いかを、削除した断片1つずつについて確認した。

削除した断片は次の3種に分類でき、いずれも受け皿の新設を要しなかった。

1. **現在の規則が断片除去後も自己完結している**(例: `memory-classification.md`の「昇格時は根拠を先に移送してから縮約する」— `(2026-07-27追加)`という日付注記を落としても、規則本文は指示として完結している)。
2. **規則の対象範囲が別の行で既に定義済み**(例: 月次振り返りの対象を広げた経緯段落を落としても、直後の「**対象**: ...」行が現在の範囲を独立して定義している)。
3. **削除対象が既存のlessons/reviewsファイルへの重複引用であり、そのファイル自体は削除していない**(例: `memory-classification.md`の`lessons/always-loaded-summaries-are-the-least-current.md`引用、`context-classification.md`の`[[feedback_no_ip_in_repo]]`引用、`role-context-matrix.md`の`process_retrospective/2026-07-28_003_pmo_retirement.md`・`2026-07-29_005_techlead_retirement.md`引用)。これらは「なぜこの規則があるか」を補強する根拠への参照であり、規則自体(「auto-memoryに残タスクを書かない」「Coordinatorの説明は入力にしない」等)は参照が無くても指示として成立する。ファイル自体は現存しており(`ls`で確認)、必要なら誰でも辿れる。

**該当したら落とさず報告する」に当たる項目は無かった。**

## R4の確認結果(陳腐化の確認)

requirement R4が名指した3箇所を現物と突き合わせた。

### `docs/ai/memory-classification.md`「pilot」節 — STALE、削除した

節本文: 「`incidents/`・`decisions/`・`temporary/`は現時点で書き出す実例がないため空(README.mdのみ)。」

現物確認(`ls -la docs/ai/memory/incidents/ decisions/ temporary/`):
- `incidents/`: **14件**のIncidentファイルが存在(2026-07-24〜2026-08-02)。
- `decisions/`: **1件**(`approval-authority-for-real-host-operations.md`)が存在。
- `temporary/`: `README.md`のみで空(ここだけは記述と一致)。

3項目中2項目が事実と食い違っており、STALE。節全体(3件のlessonsファイルへの書き出し実績の記述を含む)を削除した。3件のlessonsファイル自体は現存を確認済み(`rollback-cli-argument-convention.md`・`destructive-operation-classification-criteria.md`・`multilayer-escaping-and-novel-stack-verification.md`)が、この節が無くても1節・2節の判定ルールから独立して存在し続けるため、判定ルールの喪失は無い。

### `docs/ai/role-context-matrix.md`「未解決・持ち越し事項」節 — STALE、削除した

節本文: 「Policy本体(`docs/ai/policies/`配下)は...個別Policyファイルの本実装は...後に着手する。」(未着手を示唆)

現物確認(`ls docs/ai/policies/`): `ansible_test_safety_policy.md`・`autonomous_recovery_policy.md`・`cert_renew_cloudkey_policy.md`・`cert_renew_policy.md`・`incident_capture_policy.md`・`log_observability_policy.md`・`proxmox_backup_restore_verify_policy.md`・`proxmox_operations_policy.md`・`time_sync_check_policy.md`・`ubuntu_vm_patch_policy.md`・`unifi_backup_fetch_policy.md` の**11ファイルが既に実装済み**。「未着手」という記述は事実と全く一致しない。STALEと判定し削除した。

### `docs/ai/role-context-matrix.md`「完了条件の確認」節 — 判断基準の性質上、非アクショナブルと判定し削除した

節本文は「各Roleについて...説明できる」「新しい情報を追加するとき...1行追加するだけで済む」という、設計時の完了判定チェックリストである。事実として誤っているわけではないが(=「STALE」という言葉の字義どおりの陳腐化ではない)、requirement §2が「完了条件の確認」節を計画書の足場の具体例として明示的に挙げており、この節は運用中に誰かが参照して行動を変える性質の記述ではなく、初版設計が完了したことを確認するための一度きりのゲートである。**「いま従うべき指示ではない」という§3ゴールの基準に照らし、機能的に陳腐化(もう参照されない)していると判断し削除した。**

なお`docs/ai/context-classification.md`にも同名の「完了条件の確認」節があった(R4が名指ししたのは`role-context-matrix.md`側のみだが、requirement §2は「完了条件の確認」節を3本共通の計画書の足場として一般的に列挙している)。こちらもR1(§5の「計画書の足場」列)の対象として同じ理由で削除した。ここはR4の陳腐化確認の対象ではなくR1の直接適用として扱った。

## AC別の自己検証

### AC1(`grep -c "2026-"`が3本とも0件)— 確認済み

```
docs/ai/memory-classification.md: 0
docs/ai/context-classification.md: 0
docs/ai/role-context-matrix.md: 0
```

なお`context-classification.md`の`docs/ai/reviews/<feature>/`の例に実在ファイル名`radius_healthcheck/2026-07-21_020_final.md`があり、日付そのものは正当な命名規則の例示だったが、AC1を満たすため`radius_healthcheck/<日付>_020_final.md`へ書き換えた(規則の意味は変わらない)。

### AC2(`TODO`/`Phase[0-9]`/`pilot`/`agent_skills_reorganization_plan`が3本とも無し)— 確認済み

```
grep -n -E "TODO|Phase[0-9]|pilot|agent_skills_reorganization_plan" <3ファイル>
→ いずれも0件
```

### AC3(改訂前後の逐行突合、規則が1つも失われていないこと)— 確認済み

`git diff`を3本それぞれで全文読み、削除した断片を1つずつ「経緯・根拠・計画書の足場」か「指示・値・行き先・正本」かに分類した。

**`memory-classification.md`**(15箇所)

| 削除した断片 | 分類 | 現在の規則の保存先 |
|---|---|---|
| タイトル`(Phase6 TODO6-0〜6-3)`、L3の`agent_skills_reorganization_plan.md`参照 | 計画書の足場 | 「`docs/ai/memory/`配下のKnowledgeをどう分類し...決める」という目的文はそのまま残存 |
| 見出し`(TODO6-0)`〜`(TODO6-3)`(4箇所) | 計画書の足場 | 見出しテキスト自体(「4層モデル」等)は保持 |
| `(2026-07-27に第0段を追加)` | 日付つき注記 | 「判定は2段階で行う」という規則は不変 |
| auto-memory矛盾の実例(2026-07-27時点の索引矛盾)+lessons引用 | 過去の実例・根拠への参照 | 「auto-memoryに残タスク等を書かない」という規則本文は保持。lessonsファイル自体は現存 |
| 第1段の改訂履歴(2026-07-26/2026-07-29の判定基準変遷) | 改訂の経緯 | 判定基準の現在の文言(「Coordinator自身の判断、またはsubagentの判断が変わるか」)は保持 |
| Incident記録の旧ルール撤回経緯 | 改訂の経緯 | 「気づいた時点で開始し、原因判明後に完成させる2段階」という現行規則は保持 |
| `(2026-07-27追加)`(根拠先送りルール) | 日付つき注記 | 規則本文(「根拠を先に移送してから縮約する」)は保持 |
| 月次振り返り対象拡大の経緯(2026-07-26のフル工程案件の実例) | 過去の実例の説明 | 「対象は`incidents/`だけに限らない」という現在の範囲規則は保持し、直後の「対象:」行が具体的範囲を独立して定義 |
| `(Yoshinobu提案、2026-07-26)` | 日付つき注記 | 「3つに仕分ける」という規則は保持 |
| `(2026-07-27追加)`(状態突合ルール) | 日付つき注記 | 規則本文は保持 |
| timer起動の経緯(当初MEMORY.md先頭行だった) | 改訂の経緯 | 現行の起動方式(timer、`ansible-knowledge-review.timer`)は保持 |
| cloud routine無効化の実例(2026-07-27時点) | 過去の実例 | 「期日を他所へ書かない」という規則は保持 |
| `(2026-07-27)`(無人実行の書込制限) | 日付つき注記 | 規則本文・許可範囲表への参照は保持 |
| Context陳腐化チェックの`(2026-07-29)`+読取範囲拡張の経緯への参照 | 日付つき注記+経緯への参照 | チェックの存在と内容の説明、および`review-prompt.md.j2`への正本ポインタは保持 |
| Tech Lead廃止の経緯(Coordinator行) | 改訂の経緯 | 現在の割当(`lessons/`全般もCoordinator自身が確認)という値は保持 |
| 「5. pilot」節全体(R4でSTALE確認済み) | 計画書の足場+STALE | (上記R4節を参照) |
| 関連節の`agent_skills_reorganization_plan.md`行 | 計画書の足場 | 残る1件(`context-classification.md`への相互参照)は保持 |

**`context-classification.md`**

| 削除した断片 | 分類 | 現在の規則の保存先 |
|---|---|---|
| タイトル`(TODO2-1)`、L3のTODO2-2/2-3/2-4/pilot3羅列+日付 | 計画書の足場 | 目的文「`docs/ai/`配下の情報をどの分類へ置くかを決める」は保持 |
| Repository Context対象の地図廃止経緯+reviews参照 | 過去の実例・経緯 | 「地図を複製せず現物を直接参照する」という規則は保持 |
| `pilot3で先行実装済み` | 計画書の足場 | `healthcheck.md`の内容説明そのものは保持 |
| `pilot1/2で見つかった...`、`(Phase6で設計)` | 計画書の足場 | 作成条件の規則文、および[[Knowledge]]への行き先指定は保持 |
| Policy例の`(将来作成予定)`+`(未作成)` | 計画書の足場、かつ現物確認でSTALE(両ファイルとも実在) | 例示ファイル名自体は現存する実ファイルなのでそのまま保持、注記のみ除去 |
| `TODO7-2で` | 計画書の足場 | 例示内容(df Use%意味論の誤り)自体は保持 |
| `[[feedback_no_ip_in_repo]]`+日付の引用 | 根拠への参照 | 「実値を書かない」規則、既存規約に従う旨は保持 |
| `TODO2-2/2-3の成果物は...確認済み` | 計画書の足場 | 直前の表現規則は保持 |
| `(2026-07-25追加)` | 日付つき注記 | 節見出しの内容は保持 |
| 「Why」段落全体(2026-07-25のtimer記載齟齬の実例) | 過去の実例・事故の説明 | 「変化の速い値」テーブルと「書き方」段落(規則本体)は保持 |
| 5節見出しの`(機能別37フォルダ+新設12ファイル、2026-07-22時点)` | 日付つき注記 | 見出しテキストは保持 |
| reorg関連ファイル行の`agent_skills_reorganization_plan.md`例示 | 計画書の足場 | 分類行自体(種別・目的・更新頻度)は保持し、例を匿名化 |
| `(pilot3のhealthcheck.md作成がこの実例)` | 計画書の足場 | 「結論だけを転記してよい」という規則は保持 |
| `Phase6(Knowledge運用改善)で`、`TODO2-1の時点では` | 計画書の足場 | 「Lesson/Decision該当分はKnowledgeへ昇格させることがある」「一括移行はしない」という規則は文言を保ったまま保持 |
| 「6. 完了条件の確認」節全体 | 計画書の足場(R1直接適用) | 元々チェックリストであり規則を含まない(下記) |
| 「6. Skillの配置と...」見出しの`(TODO5-2、2026-07-23確定。...歴史的経緯として残す)` | 計画書の足場+経緯の自己言及 | 見出しテキストは外部文書(`subagent_briefing`案件記録)が節タイトルで disambiguation に使っているため変更していない(下記R5参照) |
| 「pilot」段落(Reviewerの出力フォーマットでの実証) | 計画書の足場 | 実証内容は規則ではなく実施記録であり、規則の喪失なし |
| 関連節の`TODO2-4`、`agent_skills_reorganization_plan.md`行、`pilot3` | 計画書の足場 | 相互参照先(role-context-matrix.md、healthcheck.md)自体は保持 |

**`role-context-matrix.md`**

| 削除した断片 | 分類 | 現在の規則の保存先 |
|---|---|---|
| 冒頭の`TODO2-4`+統合確定日 | 計画書の足場 | 「状態: 正本。誰が・いつ・どの深さで・何を読むか定義する」は保持 |
| Tech Lead廃止経緯(2箇所、System Context行・inventory行) | 改訂の経緯 | 「Coordinatorが直接確認する」「現物を直接参照」という現在の割当・方針は保持 |
| Auditor節見出しの`(2026-07-28新設。退役したPMOの節を置き換え)` | 日付つき注記 | 見出しテキストは保持 |
| `progress.md`撤廃の`2026-07-29に` | 日付つき注記 | 撤廃済みという規則は保持 |
| PMO退役の経緯+reviews引用 | 過去の実例・根拠への参照 | 「Coordinatorの説明は入力にしない」規則は保持 |
| 判断の原則見出しの`(計画書からの引き継ぎ、変更なし)` | 計画書の足場(出典注記) | 5つの判断原則の箇条書きは保持(R1一般適用、AC2の機械チェックには掛からないが§2の趣旨に合わせて除去) |
| Coordinator行の`(2026-07-29、Tech Lead廃止に伴い...統合)` | 改訂の経緯 | 「全体像を理解し、必要Contextを選ぶ」規則は保持 |
| Coordinatorの扱い節見出しの`(2026-07-22、pilot2/3レビューの指摘を反映)` | 計画書の足場 | 見出しテキストは保持 |
| pilot3/TODO2-2レビュー経緯+Tech Lead前後比較+reviews引用 | 計画書の足場+改訂の経緯+根拠への参照 | 現在の規則(「対象領域のSystem/Ansible Contextは着手時にCoordinator自身が読む」)は保持し、上表への参照(`(上表)`)も維持 |
| `docs/ai/status.md`追加の`2026-07-27に` | 日付つき注記 | 直後の「subagentは都度コールドスタート...」という運用原則(現在も有効)は保持 |
| 「完了条件の確認」節全体(R4で機能的陳腐化と判定) | 計画書の足場 | (上記R4節を参照) |
| 「未解決・持ち越し事項」節全体(R4でSTALE確認) | 計画書の足場+STALE | (上記R4節を参照) |

**結論**: 3本とも、削除した断片はすべて日付つき注記・改訂の経緯・過去の実例や事故の説明・根拠への参照・計画書の足場のいずれかに分類でき、判定基準・行き先・正本の指定・タイミングの定義は1件も失われていない。

### AC4(§5「残す」列の行き先・正本の指定がすべて残っている)— 確認済み

```
grep -n "が正本" <3ファイル>
```
- `memory-classification.md`: `incidents/`のファイル形式は`skills/incident-recording/SKILL.md`が正本(2箇所)、Auditorの参照範囲は`role-context-matrix.md`「Auditorの参照範囲」が正本
- `context-classification.md`: 「コードが正本のまま」(該当しない情報の原則)

「行き先」テーブル(`memory-classification.md`「仕分け」表、`skills/`・`lessons/`・`decisions/`の3行)、`docs/ai/reviews/<target>/`への言及(Temporaryの削除条件、月次振り返り対象)、`docs/ai/memory/lessons/claude-code-unattended-session-confinement.md`への正本ポインタ(3条件の確認先)は、いずれも改訂後もそのまま残存している。

### AC5(`check-doc-consistency.py`・`git-pre-commit-check.sh`がOK、宙ぶらりん参照が無い)— 確認済み(隔離コピー経由)

Implementerは`git add`を行えないため、`/tmp/claude-1000/.../scratchpad/repo_check_copy`へリポジトリ全体を`cp -r`し、そのコピー内でのみ`git add -A`して検証した(実リポジトリのstaging状態には触れていない)。

```
[check-doc-consistency.py]
[check1] OK (98 compared)
[check2] OK (8 compared)
[check3] OK (91 compared)   # markdown内部リンク切れ検査。docs/ai直下(3対象ファイル含む)が対象範囲
exit=0

[git-pre-commit-check.sh]
gitleaks: no leaks found
[tester-gate-lint] OK (49 playbooks)
[check1] OK / [check2] OK / [check3] OK
[pre-commit] OK
exit=0
```

check3は標準markdownリンク構文`[text](target)`のみを対象とし、3本で使われている`[[wikilink]]`記法(`[[role-context-matrix.md]]`、`[[feedback_no_ip_in_repo]]`、`[[Knowledge]]`)は対象外(正規表現`_LINK_RE`で確認)。これらは改訂前から機械チェック対象外の非公式表記であり、今回のテキスト変更(`TODO2-4`等の注記除去)でも参照先ファイル名自体は変えていないため、手動確認でも壊れていない。

### R5(3本間の相互参照・外部文書からの参照)— 確認済み

**3本間の相互参照**(4箇所、いずれも保持):
1. `memory-classification.md`→`context-classification.md`(「関連」節)
2. `memory-classification.md`→`role-context-matrix.md`(Auditor行、全Role起動時読込の原則行)
3. `context-classification.md`→`role-context-matrix.md`(`[[role-context-matrix.md]]`、「関連」節)

**外部文書からの参照**: `grep -rl`で3ファイル名を参照する全文書を洗い出し、削除候補セクション(「完了条件の確認」「未解決・持ち越し事項」「pilot(2026-07-23)」)を指すものが無いことを確認した。唯一の懸念は`context-classification.md`「6. Skillの配置とCodex/Claude Codeへの公開方法」の見出しで、`docs/ai/reviews/subagent_briefing/progress.md:79`・`2026-07-28_002_plan.md:224,275`・`docs/ai/status.md:80`が、同ファイルに`## 6.`見出しが2つある問題(既知のバグ、`docs/ai/status.md`に係属中)への回避策として**節タイトルの文字列そのもの**で当該節を指している。このため、この見出しテキストは変更せず、末尾の`(TODO5-2、2026-07-23確定。...)`という注記部分だけを除去した。

**副次的な効果**: 「6. 完了条件の確認」節を削除した結果、`context-classification.md`内の`## 6.`重複(`docs/ai/status.md:80`に記録されている既知の課題)が解消された。この課題行の除去自体は本案件のscope外(状態ファイルの更新)のため行っていないが、Coordinatorへの報告事項とする。

## 非ゴールの遵守

- 分類のルール・判定基準・行き先・正本の指定は変更していない(AC3で確認)。
- `docs/ai/policies/`・`skills/`・`docs/ai/roles/*.md`には触れていない。
- `docs/ai/reviews/`・`docs/ai/memory/`の既存記録は書き換えていない(参照のみ)。

## 未解決事項

1. **`context-classification.md`の`## 6.`重複解消は`docs/ai/status.md:80`に未反映。** 今回「完了条件の確認」節を削除した副次的効果として重複が解消されたが、`docs/ai/status.md`の該当行(および`docs/ai/reviews/subagent_briefing/progress.md`のJ-1)は本案件のscope外のため更新していない。Coordinatorが該当行のクローズを検討すること。
2. **`context-classification.md`「6. Skillの配置とCodex/Claude Codeへの公開方法」の「Codex側」段落は、実質的に陳腐化している可能性があるが独自に削除していない。** `AGENTS.md`が「Codexは開発工程から外れ、`recovery_exec`とProxmoxパッチ適用の実行エンジンとしてのみ稼働している」と明記し、`role-routing-index.md`にCodexへの言及が0件であることから、この段落が説明するSkill探索・明示参照の仕組み(Codex向け)は現在どのRoleからも使われていない可能性が高い。ただし該当箇所は原文自身が「歴史的経緯として残す」と自己申告していた注記であり、requirement R4が名指しした3箇所には含まれず、独自の陳腐化判定は本案件のscopeを超えると判断し、注記(TODO5-2・日付・自己申告部分)のみ除去して段落本体は保持した。Coordinatorの判断を仰ぐ。
3. **AC1の`radius_healthcheck/2026-07-21_020_final.md`例示は日付部分を`<日付>`へ書き換えた。** 実ファイルは現存する正当な例だが、AC1(grep `2026-`が0件)を満たすための機械的な書き換えであり、規則上の実害は無いと判断している。
