# review: 分類・索引3本から経緯・根拠を落とす(round2)

正本: `docs/ai/reviews/norm_docs_rationale_removal_round2/2026-08-02_001_requirement.md`
対象実装記録: `docs/ai/reviews/norm_docs_rationale_removal_round2/2026-08-02_002_implement.md`
適用Skill: `skills/document-norm-review/SKILL.md`

## Code Review: docs/ai/memory-classification.md / docs/ai/context-classification.md / docs/ai/role-context-matrix.md

### Summary

改訂前後を3本とも逐行で突合し、`docs/ai/memory/incidents/`(15件、README除き14件)・`docs/ai/memory/decisions/`(1件)・`docs/ai/policies/`(11件)を実地でカウントしてR4の陳腐化判定(pilot節、未解決・持ち越し事項節)を検算した。AC1〜AC5をすべて独立に再実行し、実装記録の対応表を鵜呑みにせず自分で確認した。**規則・判定基準・行き先・正本の指定の消失、宙ぶらりん参照、非ゴール違反のいずれも検出されなかった。** Critical/Major findingsは0件。

### Critical Issues

なし。

### Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| 1 | `docs/ai/reviews/norm_docs_rationale_removal_round2/2026-08-02_002_implement.md` | 189-191 | `context-classification.md`の`## 6.`見出し重複解消が`docs/ai/status.md`未反映であることを実装記録が自己申告している(未解決事項1)。レビュー時点で確認したところ`docs/ai/status.md`から該当行は既に消えている(git diffで確認済み、本レビュー対象外のファイルだが整合は取れている)。Coordinatorが当該Watch行のクローズを確認すること | doc-consistency |
| 2 | `docs/ai/context-classification.md` | 94-100 | 「Codex側」段落は`docs/ai/status.md`「リポジトリ直下`AGENTS.md`の要否判断」と同根の未決事項として、本案件のrequirementが明示的に「落とさない」と判断済み。今回の改訂はその判断どおり日付注記のみ除去し本文を保持しており契約どおりだが、次回この段落に手を入れる案件が来たときのために、未決事項が`status.md`側にあることをこの段落自体には書いていない(あえて経緯を書かない方針と整合するため問題ではないが、参考情報として記載) | 情報 |

### What Looks Good

- **AC1**: 3本とも`grep -c "2026-"`が0件であることを独立に再実行して確認した。
- **AC2**: `TODO|Phase[0-9]|pilot|agent_skills_reorganization_plan`のいずれも3本に残存しないことを独立に再実行して確認した。
- **AC3(逐行突合、本レビューの中核)**: `git diff`を3本それぞれ独立に読み、削除された全断片を「経緯・根拠・計画書の足場」か「規則・判定基準・行き先・タイミング」かに自分で分類した。実装記録の対応表(AC3節、15+16+13箇所)と突き合わせた結果、**分類の食い違いは無かった**。具体的には:
  - `memory-classification.md`: 日付つき注記(2026-07-27追加、Tech Lead廃止経緯等)・改訂履歴・過去の実例説明(auto-memory矛盾実例、月次振り返り対象拡大の経緯)が落ち、規則本文(判定は2段階、auto-memoryに残タスクを書かない、昇格時は根拠を先に移送、月次振り返りの対象・手順・起動方式)はすべて保持されている。
  - `context-classification.md`: TODO2-1〜2-4・pilot1/2/3・Phase6・agent_skills_reorganization_plan.mdへの参照が落ち、4分類の定義・実値を書かない規則・変化の速い値の正本表・Skill配置方式(Codex側含む)は保持されている。
  - `role-context-matrix.md`: TODO2-4・pilot2/3レビュー経緯・Tech Lead廃止の経緯が落ち、読むタイミングのマトリクス・Auditorの参照範囲・判断の原則・Coordinatorの扱いは保持されている。
- **AC4(行き先・正本の指定)**: `grep -n "が正本"`を独立に再実行し4箇所すべての残存を確認。加えて「行き先」テーブル(`memory-classification.md`の仕分け表、Knowledge内部分類表)、`Temporaryの削除条件`、`skills/incident-recording/SKILL.md`への正本ポインタ(2箇所)、`role-context-matrix.md`「Auditorの参照範囲」への正本ポインタが、いずれも改訂後に残存していることを目視確認した。「根拠への参照」と誤って「行き先の指定」を落とした形跡は無い。
- **R4の陳腐化確認の検算**: `ls docs/ai/memory/incidents/ decisions/ temporary/`・`ls docs/ai/policies/`を自分で実行した。`incidents/`14件・`decisions/`1件(README除く)、`policies/`11件(実装済み)であり、削除された「pilot」節・「未解決・持ち越し事項」節の記述(「現時点で書き出す実例がない」「個別Policyファイルの本実装は…後に着手する」)が事実と一致しないことを独立に確認した。**削除判断は妥当。**「完了条件の確認」節(role-context-matrix.md・context-classification.md両方)は、実装記録が「STALEというより機能的陳腐化(計画時の一度きりのゲート)」と性質を分けて説明しており、requirement §2が計画書の足場の具体例として明示しているため、削除は契約どおり。
- **宙ぶらりん参照(document-norm-reviewの掃引作法)**: 完全パス・略記の両方で`agent_skills_reorganization_plan`・`TODO2-4`・`完了条件の確認`・`未解決・持ち越し事項`・`pilot(2026-07-23)`をリポジトリ全体(`docs/`・`skills/`・`roles/`・`playbooks/`・`inventories/`・`CLAUDE.md`・`AGENTS.md`)から検索し、`docs/ai/reviews/`配下の既存記録以外に該当なしを確認した。`scripts/check-doc-consistency.py`を実リポジトリで独立に再実行し`check1〜3`すべてOK(check3=91件比較、内部リンク切れ0件)。
- **撤回した規範の残存確認**: 「修正して正常動作の確認が取れた時点で1回記録する」(旧Incidentルール)・「Codex系Role」(旧第1段の判定基準)をリポジトリ全体で検索し、`docs/ai/memory/incidents/`内の1件(過去の事実説明としての言及、レビュー対象外のファイル)以外に現行規範として引用している箇所は無いことを確認した。
- **非ゴールの遵守**: 変更されたファイルは対象3本のみ(`docs/ai/status.md`はCoordinator/Implementerの成果物でレビュー対象外、案件フォルダの新設ファイルは記録)。分類のルール自体(第0段/第1段の判定基準、4分類の定義、昇格・廃止フロー)に変更は無い。`docs/ai/policies/`・`skills/`・`docs/ai/reviews/`・`docs/ai/memory/`の既存記録は触れられていない。
- **「Codex側」段落の扱い**: requirement addendum(2026-08-02、Coordinator判断)の指示どおり、日付つき注記(`TODO5-2、2026-07-23確定`等)のみ除去し、段落本体(Codexのネイティブ自動検出の制約、明示参照方式の理由)はそのまま保持されている。

### Verdict

**Approve**

## 検証範囲(状態を変えない確認、実施した手段)

- `git diff -- docs/ai/memory-classification.md docs/ai/context-classification.md docs/ai/role-context-matrix.md` を全文読み、逐行突合。
- `git show HEAD:<path>` 相当として上記diffの `-` 側を改訂前として読んだ(3本とも)。
- `Read` で改訂後の3本を全文通読。
- `ls docs/ai/memory/incidents/ docs/ai/memory/decisions/ docs/ai/memory/temporary/ docs/ai/policies/` を実行し件数を数えた。
- `grep -c "2026-"` / `grep -n -E "TODO|Phase[0-9]|pilot|agent_skills_reorganization_plan"` を3本に対して独立に再実行(AC1・AC2)。
- `grep -n "が正本"` を3本に対して独立に再実行(AC4)。
- `grep -rln`/`grep -rn` でリポジトリ全体(`docs/` `skills/` `roles/` `playbooks/` `inventories/` `CLAUDE.md` `AGENTS.md`)を対象に、削除された節・見出し番号・撤回済みルール文言の残存を検索(R5、宙ぶらりん参照)。
- `python3 scripts/check-doc-consistency.py` を実リポジトリで実行(check1〜3、AC5の一部)。
- `git diff docs/ai/status.md` を読み、実装記録が報告した副次効果(`## 6.`重複解消)と整合しているかを確認した(内容自体はレビュー対象外のため判定はしていない)。

## 未確認事項

- `scripts/git-pre-commit-check.sh` は未staged状態では`[pre-commit] no staged files`で終了し、gitleaks/tester-gate-lintの実行結果を実リポジトリ上で再現できなかった(git addは本レビューの許可範囲外の操作と判断し実施せず)。実装記録が隔離コピー上での実行結果(OK)を報告しており、AC5の実体である「宙ぶらりん参照が無いこと」は`check-doc-consistency.py`の独立再実行で別途確認済みのため、判定への影響は無いと判断した。
