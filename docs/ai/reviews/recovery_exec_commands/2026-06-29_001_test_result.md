# Test Result: recovery_exec_commands

Tester: Codex `tester`
Date: 2026-06-29
Request: agmsg from `claude` at 2026-06-29T09:46:29Z
Target commit: `8a4c4f6`

## Conclusion

PASS.

`codex-exec-wrapper` rejected all invalid argument patterns before launching Codex, each with `rc=1` and the expected denial message. `authorized_keys` on both target nodes contains exactly 2 entries.

## Environment

Wrapper tests were executed over SSH on `quory` as `ann`:

```bash
ssh -F /dev/null -i /home/yoshi/.ssh/id_ann ann@quory.internal ...
```

The direct `quory -> authy/monnie` SSH hop as `ann` did not have a usable public-key identity in this shell path, so the `authorized_keys` count was collected directly from the controller with the configured Ansible key. The checked file and target hosts are the same as requested.

## T1: Too Many Arguments

Command on `quory`:

```bash
sudo -H -u recovery-exec /usr/local/bin/codex-exec-wrapper exec --cd /var/lib/recovery-exec/workspace test --dangerously-bypass-approvals-and-sandbox
```

Result: PASS

```text
rc=1
denied: exactly 4 arguments required (exec --cd <workspace> <message>)
```

## T2: Missing `--cd`

Command on `quory`:

```bash
sudo -H -u recovery-exec /usr/local/bin/codex-exec-wrapper exec /var/lib/recovery-exec/workspace test
```

Result: PASS

```text
rc=1
denied: second argument must be '--cd'
```

## T3: Wrong Workspace Path

Command on `quory`:

```bash
sudo -H -u recovery-exec /usr/local/bin/codex-exec-wrapper exec --cd /tmp test
```

Result: PASS

```text
rc=1
denied: workspace path must be '/var/lib/recovery-exec/workspace'
```

## T4: Non-`exec` Subcommand

Command on `quory`:

```bash
sudo -H -u recovery-exec /usr/local/bin/codex-exec-wrapper chat
```

Result: PASS

```text
rc=1
denied: first argument must be 'exec'
```

## T5: `authorized_keys` Entry Count

Commands:

```bash
ssh -F /dev/null -i /home/yoshi/.ssh/id_ann ann@authy.internal 'sudo wc -l /home/recovery-exec/.ssh/authorized_keys'
ssh -F /dev/null -i /home/yoshi/.ssh/id_ann ann@monnie.internal 'sudo wc -l /home/recovery-exec/.ssh/authorized_keys'
```

Result: PASS

```text
authy:  2 /home/recovery-exec/.ssh/authorized_keys
monnie: 2 /home/recovery-exec/.ssh/authorized_keys
```

## Notes

- T1-T4 are rejection-path tests and did not launch Codex.
- No production apply or state-changing recovery action was executed.
- Initial `ssh quory.internal` without `-F /dev/null` failed locally because `/etc/ssh/ssh_config.d/20-systemd-ssh-proxy.conf` has bad owner or permissions. The tests used `ssh -F /dev/null` to avoid that local client-config issue.
