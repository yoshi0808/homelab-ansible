# review: 主力serviceへの影響を通知の先頭へ出す(UV-084の充足)

対象:
- requirement: `docs/ai/reviews/ubuntu_vm_notify_impact_placement/2026-08-22_001_requirement.md`
- 差分: `roles/ubuntu_vm_full_upgrade/tasks/main.yml`(未commit)

## Summary

R1〜R6・AC1〜AC4を満たす。判定ロジック(`classify.yml`)は無変更(diffは`main.yml`のみ)。新設した`ubuntu_vm_full_upgrade_product_impact_block`は既存の`ubuntu_vm_full_upgrade_notify_detail_block`と同じ設計(版は`inst_detail`/`remv_detail`から都度引く・二重に持たない、空入力は`default('', true) | trim`で吸収)を踏襲しており、独立した欠陥は見つからなかった。Critical Issuesなし。

## Critical Issues

なし。

## Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| 1 | roles/ubuntu_vm_full_upgrade/tasks/main.yml | 233-264 | 新ブロックにも既存の`_i.length > 0`探索と同様、`_i`と`_r`の両方に該当した場合(同名が`inst_detail`・`remv_detail`双方に載る)は`inst_detail`側が優先される。既存の`notify_detail_block`はこのケースが構造上起きない(1パッケージは`inst`/`remv`いずれか一方のみに属する)ため既存踏襲で問題ないが、コメントに前提として一言書いておくと次に触る人が「これは想定外入力への防御か、意図的な優先順位か」を迷わない。任意対応。 | Clarity |

## What Looks Good

- **R1(先頭移動)**: `slack_message`の1行目が`{{ ubuntu_vm_full_upgrade_product_impact_block }}`になっている(旧位置は末尾のfor-loop、diffで削除を確認)。
- **R2(版併記)**: install候補は`name old → new`(`old`が空文字なら`(新規)`)、remove候補は`name version (remove)`。`old`/`version`が空・未定義でもクラッシュしない(`if d.old else`スタイル)。
- **R3(該当なしを残す)**: `p.packages | length == 0`のとき`該当なし`を出す分岐は変更なしで維持。
- **R4(4ホスト共通)**: `ubuntu_vm_full_upgrade_products`はauthy/monnie/ansy/quoryの全4ホストに定義済みで、`ubuntu_vm_full_upgrade_product_impact`が空リストになるホストは無い(=先頭行が消えるケースが構造的に無い)。
- **R5(版の出所)**: `selectattr('name', 'equalto', name)`で`inst_detail`/`remv_detail`を都度検索するのみで、値を別の変数へ複製していない。
- **R6(Policy不変)**: `git diff -- docs/ai/policies/ubuntu_vm_patch_policy.md`は差分なし。
- **判定ロジック不干渉**: `classify.yml`は無変更。`ubuntu_vm_full_upgrade_important_regex`・Status昇格・non-aptレジストリ(`ubuntu_vm_full_upgrade_nonapt_per_node`)はいずれも新ブロックから参照されるだけで書き換えられていない。
- **`apt外:`行の位置**: 一覧(`notify_detail_block`)より前という既存位置は変更なし(diffで確認)。
- **3種のdetailのキー取り違えなし**: 新ブロックは`inst_detail`→`old`/`new`、`remv_detail`→`version`のみを参照し、`phased_detail`(`installed`/`candidate`)には触れていない。これは要件どおり(R5は`inst_detail`のみを出所と定めており、`phased_detail`は元々`classify.yml`の判定対象外)。既存の`notify_detail_block`は3種を`'new' in d` / `'version' in d` / それ以外(`phased_detail`)の順でキー形状により判別しており、新ブロックとは独立した実装で、混線していない。

## 自己検証で確認したこと

状態を変えない確認のみ。実ホスト(monnie/authy/quory/pve)へは触れていない。

1. **`git diff --stat` / `git status --porcelain`** — 変更ファイルは`roles/ubuntu_vm_full_upgrade/tasks/main.yml`のみ。Policy・classify.yml・defaults/main.ymlはすべて無変更。
2. **AC1/AC2/AC3の描画確認** — `main.yml`から`_impact`テンプレートと`slack_message`テンプレートをYAML/Jinja2としてそのまま抽出し(手書き転記ではなくファイル現物)、Coordinatorが取得済みの#800実レポート(`monnie800.json`/`authy800.json`/`quory800.json`/`ansy800.json`)を投入して描画。
   - monnie: 1行目が`Prometheus/Grafana/Loki/unpoller監視基盤:`で始まり、`unpoller 3.3.4+git → 4.0.0+git`を含む(AC1充足)。
   - authy: 先頭が`FreeRADIUS/WPA3認証基盤: 該当なし`(AC2充足、該当パッケージ無し)。
   - quory: 3ラベル(本番Ansible実行基盤/QDevice/NTP基準)がすべて先頭に出て全て該当なし(AC3の「空欄にしない」を含めて充足)。
   - ansy: `Ansible開発環境(apt管理: 判定対象): 該当なし`。
3. **AC4** — `ansible-playbook --syntax-check playbooks/ubuntu_vm_full_upgrade.yml -i inventories/homelab` を実行、rc=0。
4. **ansible-lint差分** — 変更前後のlint出力(scratchpad `lint_before.txt`/`lint_after.txt`)を比較。行番号のずれ以外に新規の指摘なし。
5. **合成入力によるテンプレート単体テスト**(壊れる入力の洗い出し) — `_impact`テンプレートを実ファイルから抽出し、以下を個別に投入して例外・不正表示がないことを確認:
   - remove候補(`remv_detail`側、`version`あり) → `name version (remove)`
   - `packages`にあるがname一致が`inst_detail`にも`remv_detail`にも無い(データ不整合) → 版なしで`name`のみ表示(クラッシュしない)
   - `packages`内の名前重複 → 該当行がそのまま2回出る(クラッシュしないが冪等な表示ではない。実データでは`inst`/`remv`が`| unique`済みのため`classify.yml`側で重複は発生しない設計 — 新ブロックはその前提に乗っている)
   - `old`が空文字列(新規インストール) → `(新規) → new`
   - `remv_detail`の`version`が空文字列 → `? (remove)`
   - 同名パッケージが`inst_detail`と`remv_detail`の両方にある(構造上は起きない想定) → `inst_detail`側が優先されるだけでエラーにはならない(Suggestions #1)
   - `product_impact`が空リスト → 空文字列(実データでは発生しない、R4確認済み)

## 未解決事項

- **AC5(Yoshinobu実施)は本レビューの対象外。** requirement OQ1のとおり、ansyは2026-08-19の鍵削除によりmonnie/authy/quory/pveへ到達しないため、Slack実送信・「もっと見る」展開前の見え方はこのレビューでは検証できない。手動起動での確認はYoshinobu側に残る。
- Suggestions #1は任意対応。判定・表示のいずれにも影響しないため、Request Changesの理由にはしていない。

## Verdict

**Approve**(AC5はYoshinobu実施待ちのままでよい)。

---

## Coordinatorの対応(2026-08-22)

Suggestions #1 を**反映した**。`roles/ubuntu_vm_full_upgrade/tasks/main.yml` の新ブロックのコメントへ、探索が排他であること、両方に載った場合に inst 側を採るのは**優先順位の設計ではなく想定外入力への防御**であることを明記した。反映後に `--syntax-check`(rc=0)と #800 実データでの再描画を通し、AC1 の出力が変わっていないことを確認した。

本件はこれでクローズし、AC5(Slack実送信での見え方)は Yoshinobu が quory の Semaphore から手動起動して確定する。
