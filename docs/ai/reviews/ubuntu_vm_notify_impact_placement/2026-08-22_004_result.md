# result: 主力serviceへの影響を通知の先頭へ出す(クローズ)

実行: Yoshinobu(quory の Semaphore、2026-08-22)
記録: Coordinator

## 結論

**受入条件はすべて充足した。案件をクローズする。**

| AC | 内容 | 結果 |
|---|---|---|
| AC1 | monnie の先頭に影響行が出て `unpoller 3.3.4+git → 4.0.0+git` を含む | **PASS**(#800 の実レポートで描画確認、#801 の実通知で確定) |
| AC2 | authy の先頭に `FreeRADIUS/WPA3認証基盤:` が出る | **PASS** |
| AC3 | 該当なしを空欄にしない | **PASS** |
| AC4 | `--syntax-check` rc=0 | **PASS** |
| AC5 | 「少なく表示する」を開く前に影響行と版が読める。終了コード0 | **PASS**(ジョブ #801 が正常終了し、5行が欄外に表示された) |

## 開発側で確かめられた範囲

**ansy からは実ホストへ到達できないため、開発側の検証はテンプレートの描画までである**(requirement OQ1)。実施したのは次の2つ。

- `roles/ubuntu_vm_full_upgrade/tasks/main.yml` を YAML として読み、該当タスクの `_impact` と `slack_message` の**文字列そのもの**を取り出して描画した。テンプレートを書き写すと自分のコピーを検証することになるため、現物を対象にした。
- 入力は #800 が quory へ保存した4ホストの実レポートである。

**AC5 だけは、この経路では原理的に確定しなかった。** Yoshinobu が quory の Semaphore から手動起動して初めて閉じた。次の月次(9月)を待たずに済んだのはそのためである。

## Auditor を起動していない

**Coordinator の判断でクローズした。** 変更が通知テンプレートの並べ替えに閉じており、判定ロジック・Policy本文・defaults のいずれにも触れていないこと、独立レビューが Approve を返していること、ACが実通知まで含めて全て充足していることによる。記録は requirement / review / 本ファイルの3本で、経緯は再構成できる。

## 残っているもの

**無い。** ただし本件が生まれた発端である **monnie の unpoller 3.3.4+git → 4.0.0+git は未適用**であり、当てるかどうかとその時期は Yoshinobu の判断である(本案件のscope外)。
