# implement: `loki-window` の埋め込み改行正規化

日付: 2026-09-04

## 変更

- `roles/recovery_exec/files/recovery-loki-helper`
  - `cmd_window` で entry 本文を `" ".join(line.splitlines())` により1論理行へ正規化してから `MAX_LINE_LENGTH` を適用するようにした。
  - `cmd_errors` の既存コメントを、同じ正規化が `cmd_window` にも適用され、本文を出力しない `cmd_count` だけが対象外である現状へ同期した。
- 定数、切り詰め通知、`cmd_count` / `cmd_errors` の処理、operand検証、認可境界は変更していない。

## 自己検証

本番Lokiへ問い合わせず、`loki_get` を差し替える使い捨てスクリプト `/tmp/verify_loki_window_embedded_newline.py` で検証した。HEAD版は `git show HEAD:roles/recovery_exec/files/recovery-loki-helper`、作業ツリー版は同パスの現物をそれぞれ別namespaceへ読み込み、同じfixtureを渡した。スクリプトの終了コードは0だった。

### AC1 / AC2

本文fixtureは LF / CRLF / 単独CR / VT / FF / NEL / LS / PS / FS / GS / RS / 先頭LF / 末尾LF / これらの混在の14種類、entry件数は 0 / 1 / 299 / 300 / 301 / 500 の6種類とし、全84組合せを実行した。

- 判定値: 各stdoutの `len(stdout.splitlines())` と `stdout.count("\n")`
- 結果: いずれも300以下。観測した最大値は双方300。全呼び出しが例外なく戻り、検証プロセス終了コードは0。
- 301件と500件の全28組合せでは末尾物理行が切り詰め通知であり、同じ入力で実行したHEAD版の末尾通知と完全一致した。全件で `earliest 299 of >300 lines` を含んだ。

### AC3

改行相当文字を含まない本文 `plain text` を 0 / 1 / 3 / 299 / 300 / 301 / 500 件返し、HEAD版と作業ツリー版のstdoutを比較した。7組合せすべてで完全一致した。

### AC4

- `cmd_count`: level label有り(`error`) / 無し / 空文字列の3系列を同時に返し、HEAD版と作業ツリー版のstdoutが完全一致した。
- `cmd_errors`: AC1と同じ14本文種類 × 6件数の84組合せを実行し、HEAD版と作業ツリー版のstdoutがすべて完全一致した。

### AC5

全14種類の改行相当文字を混在させた本文2件を逆時系列で返し、出力本文が `" ".join(line.splitlines())` の期待値と完全一致すること、各entryが1物理行であること、JST時刻と `[host-a]` prefixが保持されること、時刻昇順になることを確認した。さらに同じ本文の300件では通知なし、301件では末尾に通知ありとなり、切り詰め判定がentry件数から変わらないことを確認した。LF 300文字だけからなる本文1件では、正規化後に長さ上限300文字が適用され、stdoutが1物理行になることも確認した。

### AC6

`PYTHONPYCACHEPREFIX=/tmp/loki-window-pycache python3 -m py_compile roles/recovery_exec/files/recovery-loki-helper` は終了コード0だった。`git diff --check -- roles/recovery_exec/files/recovery-loki-helper` も終了コード0だった。

### AC7

自分が変更したrepo内ファイルは、許可されたhelperと本ファイルだけである。案件フォルダ内にはCoordinatorが作成したrequirementもある。`git status --short --untracked-files=all` にはこのほか、着手前から存在した次のscope外の他者差分が出るため、作業ツリー全体を字義どおり対象にしたAC7は未充足である。このファイルは変更していない。

- `M docs/ai/memory/lessons/verification-referent-and-path-mismatch.md`

## 未解決事項・残存リスク

- 実ホストおよび本番Lokiでは検証していない。実機配備もscope外であり未実施。
- 原文の改行位置が必要な運用への影響はrequirement §8のオープンクエスチョンのまま。本実装では判断していない。
