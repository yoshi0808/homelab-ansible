# playbooks/README.md と現物の突合結果（3列）

2026-09-05 / Implementer。依頼: `docs/ai/reviews/playbook_readme_consistency/2026-09-05_000_briefing.md`。

## 範囲・結論

`git ls-files 'playbooks/*.yml'` の **59本すべて**について、READMEの「対象」「用途」「主な role / 実装」を現物と突合した。未照合は0本。各playの本体を読み、`import_playbook` の接続先、role呼び出し・`tasks_from`・`include_tasks`、適用先の判断に必要なrole内の委譲・既定値を追った。実ホストへの接続・Ansible実行は行っていない。

記録は **A: 誤記・実際の適用先欠落13件（13行）**、**B: 要約による省略13件（13行、誤りとは判定しない）**。Aの内訳は対象列12件、用途列1件。「主な role / 実装」に存在しないroleを記載した相違は見つからなかった。Bは複数列の省略を同じカタログ行ごとにまとめた。A行に付随する補助roleの省略はAの根拠中に示し、Bへ重複計上していない。

「対象」は既定の実行で実際に操作・観測するリモート先も含めて評価した。controller上の引数検証、レポート保存、通知のためだけの`localhost`省略は対象の誤記とはしていない。可変の`hosts`について既定値を載せた記述も、その既定値が正しければB相当とした。一方、通常の処理で別ホストへ配備・接続する委譲先の欠落、到達性次第で切り替わる実行ノードの固定表記はAとした。

以下は相違・省略を確認した行だけであり、一致行の一覧ではない。tester-gate分類、そのPolicy上の妥当性、カタログの掲載有無など依頼の対象外事項は判定していない。README・実装の修正も行っていない。

## A. 誤記・実際の適用先の欠落

### A01 recovery_exec_setup.yml — 対象

- README: `dev_nodes:control_nodes`（`playbooks/README.md:67`）。
- 現物: `playbooks/recovery_exec_setup.yml:79` は `hosts: control_nodes`。さらに `roles/recovery_exec/tasks/main.yml:557` は条件付きで `target_setup.yml` を呼ぶ。配布は既定有効（`roles/recovery_exec/defaults/main.yml:49`）で、対象は同ファイルの `:66`（authy / monnie）、`:106`（pve1 / pve2）。実際の委譲は `roles/recovery_exec/tasks/target_setup.yml:64`、`:261`。PVE配布は到達可能な対象に限定される（同 `:228`）。
- 判断: **READMEが古い**。commit `f7694af` の現物diffで `dev_nodes:control_nodes` → `control_nodes` の変更を確認した。加えて、既定で行うリモート配布先も対象列に載っていない。runner配備先と委譲配布先を区別する必要がある。

### A02 recovery_io_setup.yml — 対象

- README: `dev_nodes:control_nodes`（`playbooks/README.md:69`）。
- 現物: `playbooks/recovery_io_setup.yml:38` は `hosts: control_nodes`。
- 判断: **READMEが古い**。commit `f7694af` の当該playbook差分でdev_nodes除外を確認した。

### A03 recovery_probe_setup.yml — 対象

- README: `dev_nodes:control_nodes`（`playbooks/README.md:72`）。
- 現物: `playbooks/recovery_probe_setup.yml:34` は `hosts: control_nodes`。probeとmute CLIはこのplayで配備する（同 `:42`、`:49`）。
- 判断: **READMEが古い**。commit `f7694af` の当該差分でdev_nodes除外を確認した。probeの監視対象とdaemonの配備先は別である。

### A04 incident_inspect_setup.yml — 対象

- README: `dev_nodes:control_nodes`（`playbooks/README.md:85`）。
- 現物: `playbooks/incident_inspect_setup.yml:48` は `hosts: control_nodes`。
- 判断: **READMEが古い**。commit `f7694af` の当該差分でdev_nodes除外を確認した。

### A05 recovery_ha_failover.yml — 対象

- README: `pve1`（`playbooks/README.md:68`）。
- 現物: `playbooks/recovery_ha_failover.yml:48` で `proxmox` を対象に `proxmox_exec_node`（同 `:52`）を呼び、`:58` の `recovery_exec_target` で実行する。選定は到達可能候補から1台（`roles/proxmox_exec_node/tasks/main.yml:96`）。復旧対象は `target`（playbook `:68`）、HAの移動先は別のonlineノードから決める（`roles/recovery_ha_failover/tasks/main.yml:97`）。
- 判断: **READMEが古い**。commit `beff0f5` の差分で `hosts: pve1` から動的選定への変更を確認した。選定ノード・対象VM・移動先をpve1の1語では表せない。

### A06 recovery_service_restart.yml — 対象

- README: `pve1`（`playbooks/README.md:77`）。
- 現物: `playbooks/recovery_service_restart.yml:45` は `proxmox`、`:49` で `proxmox_exec_node`、`:55` は `recovery_exec_target`。実際のサービスrestartは `target` へ委譲する（`roles/recovery_service_restart/tasks/main.yml:92`、`:99`）。許容targetはplaybook `:65` の authy / monnie / sandbox。
- 判断: **READMEが古い**。commit `beff0f5` で固定pve1からの変更を確認した。実行ノードだけでなく、サービス操作先も対象列から欠落している。

### A07 recovery_vm_reboot.yml — 対象

- README: `pve1`（`playbooks/README.md:78`）。
- 現物: `playbooks/recovery_vm_reboot.yml:49` は `proxmox`、`:53` で `proxmox_exec_node`、`:59` は `recovery_exec_target`。対象VMは `target`（同 `:69`）で決め、roleがクラスタからVM情報を取得して停止・起動する（`roles/recovery_vm_reboot/tasks/main.yml:60`、`:84`、`:168`）。
- 判断: **READMEが古い**。commit `beff0f5` の差分で固定pve1から動的選定への変更を確認した。

### A08 unifi_backup_fetch.yml — 対象

- README: `pve1`（CloudKeyへ接続）（`playbooks/README.md:117`）。
- 現物: `playbooks/unifi_backup_fetch.yml:40` は `proxmox`、`:44` で `proxmox_exec_node` に `pen_prefer: pve1`（`:46`）を渡す。本体は `unifi_backup_fetch_target`（`:54`）。共通roleの選定式（`roles/proxmox_exec_node/tasks/main.yml:99`）では優先先が到達不能なら別の到達可能候補となる。
- 判断: **READMEは現行挙動を十分に表していない**。pve1は固定実行先でなく優先先。commit `beff0f5` の変更前にもpve2へのfallbackが存在することを確認したため、そのcommitを相違の発生日とはしない。READMEの更新漏れか当初からの過度な省略かは断定できない。

### A09 recovery_push_drill_setup.yml — 対象

- README: `quory`（`playbooks/README.md:75`）。
- 現物: playの `hosts` はquory（`playbooks/recovery_push_drill_setup.yml:24`）だが、`:28` のincludeは authy / monnie（`:33`）をloopする。unitの配備とdaemon reloadは `roles/recovery_push/tasks/drill_setup.yml:14`、`:22` で各 `drill_target.name` に委譲される。
- 判断: **READMEの配備対象が不足**。quoryはplayの実行文脈であり、drill unitの配備先はauthy / monnie。新旧関係は今回の現物比較だけでは断定できない。

### A10 recovery_push_setup.yml — 対象

- README: `quory`（`playbooks/README.md:76`）。
- 現物: `playbooks/recovery_push_setup.yml:29` のquoryから `recovery_push` を呼ぶ（`:34`）。roleは `sender_setup.yml` をloop（`roles/recovery_push/tasks/main.yml:40`）し、既定のauthy / monnie（`roles/recovery_push/defaults/main.yml:13`）へscript・unit・drop-in等を配備する（`roles/recovery_push/tasks/sender_setup.yml:65`、`:73`、`:85`、`:113`）。quory側にもdispatch等を配備する（`roles/recovery_push/tasks/main.yml:22`）。
- 判断: **READMEの適用先が不足**。通常の配備先はquoryだけではない。新旧関係は断定できない。

### A11 time_sync_check.yml — 対象

- README: `quory:pve1:pve2:ansy:monnie:authy:sophos`（`playbooks/README.md:104`）。
- 現物: `playbooks/time_sync_check.yml:73` の `hosts` は一致するが、呼び出すrole（`:79`）は `roles/time_sync_check/tasks/main.yml:190` でCloudKeyにもSSH接続する。宛先は同 `:197` の `cloudkey_host`、実行は `delegate_to: localhost`（`:205`）。参照ホストが同期済みなら実施する条件（`:212`）であり、単なる未使用taskではない。
- 判断: **READMEの観測対象が不足**。CloudKeyはinventoryのhosts式外から実際に観測される。追加時期とREADMEの新旧関係は断定できない。

### A12 proxmox_restore_vm_placement.yml — 対象

- README: `localhost`, `target_node`（`playbooks/README.md:60`）。
- 現物: `playbooks/proxmox_restore_vm_placement.yml:28`、`:63` のplay対象は一致するが、呼び出すrole（`:78`）のQEMU移動・LXC移動は元の配置ノード `item.node` へ委譲される（`roles/proxmox_restore_vm_placement/tasks/main.yml:116`、`:125`、`:128`、`:136`）。移動対象はtarget_nodeに属すべきなのに他ノードにあるVMから選ぶ（同 `:87`）。
- 判断: **READMEの実際の操作先が不足**。配置を戻す先に加えて、戻す元のノードでも移動コマンドを実行する。新旧関係は断定できない。用途の「対象ノードへ戻す」自体は正しい。

### A13 incident_investigate_setup.yml — 用途

- README: 一次調査本体の内容に「同期起動鍵生成」を含めている（`playbooks/README.md:89`）。
- 現物: `playbooks/incident_investigate_setup.yml:68` が呼ぶroleの全taskを確認したが、同期起動鍵生成・同期用taskのimportは存在しない。現在の配備はscript（`roles/incident_investigate/tasks/main.yml:167`）、config（`:178`）、unit（`:189`、`:201`）等。残った同期鍵を除去する別入口は `playbooks/incident_sync_teardown.yml:106`。
- 判断: **READMEが古い**。commit `7904ba2` の差分で、当該roleの `sync_trigger.yml` importと鍵生成taskファイルの削除を確認した。処理の省略ではなく、既に行わない処理が用途に残っている。

## B. 要約による省略（誤りとは判定しない）

各行とも、現物が追加の処理・補助実装を持つことだけでは「どちらかが古い」とは判定できない。以下の判断欄に別記のないものは新旧不明であり、README修正を必須とはしない。通知用`common_slack`、報告用localhostなど反復する補助処理の省略は、個別の修正事項として網羅列挙しない。

| ID / カタログ行 | READMEの記述 | 現物の追加内容と根拠 | 判断 |
| --- | --- | --- | --- |
| B01 `codex_update_check.yml` | 用途「Codex CLIの更新確認・更新」 | `playbooks/codex_update_check.yml:52` のroleはnpm自身のmajor更新も扱う（`roles/codex_update_check/tasks/main.yml:169`、`:184`）。 | Codexの用途説明は正しいが、npmの変更もあるという言い落とし。 |
| B02 `deployment_drift_check.yml` | 対象は既定のホスト群、用途の列挙はhash / unit状態 / forced command構造 / reports所有権 / /etc/hosts、実装は `deployment_drift_check` | 対象は上書き可（`playbooks/deployment_drift_check.yml:35`）。Semaphore probe（`:58`）とlocalhost集約（`:76`）があり、`semaphore_templates` のnoopも呼ぶ（`:85`）。roleにはowner/group/mode等の比較もある（`roles/deployment_drift_check/tasks/evaluate.yml:188`）。 | 「配備物がrepoとずれていないか」という用途は合う。列挙と主なroleの省略。 |
| B03 `semaphore_templates_setup.yml` | 対象 `quory`、用途は押せるボタンのreconcile、削除なし | `playbooks/semaphore_templates_setup.yml:124` は `semaphore_target`（既定quory）で、既知のSemaphoreホストに限る（`:121`）。`roles/semaphore_templates/tasks/main.yml:121` 以降はscheduleも扱い、`:142` でapplyする。 | quoryは正しい既定値。scheduleの作成・更新・active反映も用途に加わるが、ボタン管理という記述の否定ではない。`roles/semaphore_templates/tasks/schedules_apply.yml:13`、`:19` は作成・更新であり、「削除なし」との矛盾は認めない。 |
| B04 `prometheus_update_check.yml` | 対象 `monnie`、用途は更新確認と承認された更新 | `playbooks/prometheus_update_check.yml:40` にlocalhost前提確認。roleは手動rollbackも扱う（`roles/prometheus_update_check/tasks/main.yml:434`、`:443`）。 | 適用先monnieは正しい。前提確認と復元機能の省略。 |
| B05 `knowledge_review.yml` | 用途は月次見直しのきっかけをSlackへ通知 | `playbooks/knowledge_review.yml:30` のroleは次回期日を計算してMEMORY.mdへ反映する（`roles/knowledge_review/tasks/main.yml:37`、`:41`）。通知はplaybook `:34`。 | 無人LLMを起動しない通知入口という説明は合う。付随する期日更新の省略。MEMORY.md自体は読んでいない。 |
| B06 `proxmox_evacuate_node.yml` | 用途「対象ノードからVMを退避」、主なroleは `proxmox_evacuate_node`, `proxmox_healthcheck` | `playbooks/proxmox_evacuate_node.yml:36` 等でmute taskもincludeする。roleの移動対象にはLXCもある（`roles/proxmox_evacuate_node/tasks/main.yml:149`）。 | 「VM」をゲストの総称として読める要約。muteやCTの明示省略を誤りとはしない。 |
| B07 `proxmox_patch_apply_node.yml` | 対象 `target_node`、用途は単一ノードのパッチ適用、実装 `proxmox_patch_apply_node` | `playbooks/proxmox_patch_apply_node.yml:29` のlocalhost前提確認と `:40` 等のmute include。roleにはhealthcheck呼び出し（`roles/proxmox_patch_apply_node/tasks/main.yml:161`）、必要時reboot（`:823`）もある。 | 主たる適用先・処理は一致。補助処理と従属roleの省略。 |
| B08 `proxmox_backup_restore_verify.yml` | 主な実装 `proxmox_backup_restore_verify`、用途は月次の実restore検証 | `playbooks/proxmox_backup_restore_verify.yml:52` に選定用 `proxmox_exec_node`、`:64` のquery playを挟んで `:210` のrestore対象へ進む。roleは最新backupを選ぶ（`roles/proxmox_backup_restore_verify/tasks/main.yml:177`）。 | 主処理と動的restore対象の記述は正しい。補助roleの省略。「月次」は検証周期の要約として読める。 |
| B09 `semaphore_db_backup.yml` | 主な実装 `semaphore_db_backup` | `playbooks/semaphore_db_backup.yml:83` の `proxmox_exec_node`、`:96` 等の通知taskも呼ぶ。 | 対象列には既に選定が明記され、用途も正しい。「主な」実装の省略。 |
| B10 `proxmox_healthcheck.yml` | 主な実装 `proxmox_healthcheck` | `playbooks/proxmox_healthcheck.yml:14` は先に `proxmox_reachable_nodes`、`:17` で記載roleを呼ぶ。 | 到達性処理の補助roleが未記載だが、主処理は一致。 |
| B11 `proxmox_hw_check.yml` | 主な実装 `proxmox_hw_check` | `playbooks/proxmox_hw_check.yml:14` は先に `proxmox_reachable_nodes`、`:17` で記載roleを呼ぶ。 | 同上。 |
| B12 `proxmox_snapshot_check.yml` | 主な実装 `proxmox_snapshot_check` | `playbooks/proxmox_snapshot_check.yml:14` は先に `proxmox_reachable_nodes`、`:17` で記載roleを呼ぶ。 | 同上。 |
| B13 `proxmox_patch_weekly_full.yml` | 用途は退避・パッチ・healthcheck・配置復元の週次オーケストレーション、実装は関連playbookの順次import | `playbooks/proxmox_patch_weekly_full.yml:96` は `proxmox_exec_node` を直接呼ぶ。`:247`、`:328`、`:547`、`:564`、`:621` 等のimportに加え、直接healthcheck（`:596`）、mute（`:511`）もある。両ノード時のみ退避する条件は `:551`。 | import中心という説明は正しい。直接task・補助role・単一ノード分岐の省略であり、全条件で必ず退避するという記述ではない。 |

## 自己検証・制限

- 着手時の `git status --short` は案件ディレクトリの未追跡表示だけで、追跡済みファイルの差分はなかった。依頼文は既存入力として保持した。
- 全59本の入口と3列を照合。import先も59本の照合集合内で確認し、必要な委譲先・既定値・role内処理を追加確認した。引用した変更履歴は `git show` の実差分で確認した。
- 本書内の `file:line` は既存ファイルの行番号範囲をローカルscriptで検査し、参照先の内容も再確認した。短縮した `:行番号` は同段落で示したファイルに属する。削除済みファイルは現物の `file:line` 根拠として使っていない。
- 成果物作成前後の追跡・未追跡ファイルのhash比較で、変更は本書1本だけと確認した。実ホスト検証、Ansible、identity昇格、stage / commitは実施していない。処理を起動したまま残していない。
- 未解決: Aのうち履歴で確定していない項目は、READMEが後から古くなったのか最初から省略していたのかを断定していない。現物の適用先・処理の相違自体は確認済み。本書は照合結果であり、実装を変更すべきかという要件・運用判断は含まない。
