# review: playbooks/README.md「対象」列是正 + check1拡張

日付: 2026-09-05 / Reviewer。対象: `git diff --cached`(`playbooks/README.md`、`scripts/check-doc-consistency.py`)。入力: `2026-09-05_002_requirement.md`、`2026-09-05_001_findings.md`、`2026-09-05_003_implement.md`。

## Summary

R1〜R4を自分で再検証した。R1の凡例文言は指定どおり、R2/R3の修正はA01〜A13の13行だけに限定されており、B01〜B13は1バイトも変わっていない(diffで確認)。check1拡張(R4)は自分で作った独立の検証で、AC2相当(変更後59行=118件比較で指摘0)とAC3相当(`HEAD`のREADMEに対してA01〜A08の8行だけを検出、過不足なし)を再現した。実装記録の主張を鵜呑みにせず、モジュールを直接importしてREADME部分だけを`HEAD`版に差し替える形で自分で検証した。blockingな指摘は無い。

## Critical Issues

なし。

## Major Issues

なし。

## Minor / Suggestions

| # | File | Line | Issue | Category |
| --- | --- | --- | --- | --- |
| 1 | `scripts/check-doc-consistency.py` | `_normalize_host_patterns`呼び出し周辺(旧行 `hosts mismatch` 判定直前) | `localhost`除外ヒューリスティック(`'localhost' in actual_hosts and len(actual_hosts) > 1`)は、`deployment_drift_check.yml`等の既存B行(controller専用の集約play・報告playが対象列に出ない慣行)を通すために要る一方、この振る舞いはR1で新設した凡例にもコード内docstringにも「なぜlocalhostだけ例外か」を書いていない。今回の再検証では非対称な悪化(見逃し)は作らないことを個別に追った(actual側にlocalhostが無い側の比較は素通しされない)が、次にこの分岐へ触る人が意図を復元しづらい。 | Suggestion |
| 2 | `scripts/check-doc-consistency.py` | `_playbook_host_patterns`内 `brv_restore_targets` → `'動的restore対象'` の直書き | R4は「対応の付け方は実装者に一任」だが、この1行はinventoryグループ名と自然文ラベルを1:1でハードコードする、対象1playbookに特化した対応である。正しく動作することは確認したが、汎用規則ではなく個別許可リストに近い。将来同種の自然文カタログ表記が増えると、この方式では1行ずつ追記することになる。 | Suggestion |
| 3 | `scripts/check-doc-consistency.py` | `_playbook_host_patterns`のJinja対応(`variable`/`destination`の2パターンのみ) | 意図どおり「対応外は`AnalysisError`で止める」設計であり、判定不能を素通りさせない原則には合致している。ただし今後、既存2パターン以外の動的`hosts:`(例: 3ノード以上のgroupから選ぶ新しいselect roleの追加)を書くと、その瞬間にpre-commitが割れる。設計として妥当だが、対応表記が増えたときの拡張手順(このファイルへパターンを足す)がコード外のどこにも明文化されていない。 | Suggestion |

## What Looks Good

- R1の凡例文言はrequirement本文と一字一句一致し、指定どおり「安全区分の見方」と同階層に置かれている。
- R2/R3の修正はfindings A01〜A13の13行に限定され、diffで確認した範囲内でB01〜B13のセル文字列に変更は無い。
- R4の実装は`git show :path`によるindex読み取りという既存設計(作業ツリーでなくstage内容を見る)を踏襲し、標準ライブラリのみで完結しており、`ansible-core`実行やネットワークアクセスを持ち込んでいない(既存の設計方針`Design notes`と整合)。
- 新しい`_inventory_host_groups`・`_playbook_host_patterns`はいずれも、解析できない入力に対して`AnalysisError`で止まる(空集合や不明構文を握りつぶして0件比較にならない)。実際に「不明構文」「循環import」など複数ケースで例外に落ちることを確認した(下記の自己検証参照)。
- Jinjaの2パターン(`{{ var | default(...) }}`、`{{ 'a' if x == 'b' else 'c' }}`)は`proxmox_patch_apply_node.yml`・`proxmox_evacuate_node.yml`の実際のコードと文字どおり一致しており、架空のケースに備えた先回り実装ではない。

## 自己検証(Reviewerが独立に実施したもの)

- `python3 scripts/check-doc-consistency.py`(index=stage内容)を実行し、`[check1] OK (118 compared)` を含む全チェックPASS(exit 0)を確認した。AC1相当は`.git/hooks/pre-commit`が呼ぶスクリプトと同一呼び出し(引数無し)であることをスクリプト自身で確認済み。
- モジュールを直接importし、`read_index_content`を`playbooks/README.md`だけ`HEAD`版に差し替えた状態で`check_playbook_tester_gate`を呼び出し、`recovery_exec_setup.yml` / `recovery_ha_failover.yml` / `recovery_io_setup.yml` / `recovery_probe_setup.yml` / `recovery_service_restart.yml` / `recovery_vm_reboot.yml` / `incident_inspect_setup.yml` / `unifi_backup_fetch.yml` の8件(A01〜A08と完全一致)だけが検出されることを確認した。9件目以降の混入・件数不足は無い。
- `git diff --cached -- playbooks/README.md`の全ハンクを目視し、変更行がA01〜A13の13行のみであること、B01〜B13に該当する行がハンクに一切出現しないことを確認した。
- 新規コードが依存する実データ(`inventories/homelab/hosts.yml`の`all.children`構造、`proxmox_exec_node`/`pen_target_group`の呼び出し箇所、`brv_restore_targets`のadd_host、`{{ 'pve1' if target_node == 'pve2' else 'pve2' }}`の実在)をgrepで裏取りし、コード中のコメントが指す実装が実在することを確認した。
- `git status --short`で、指定2本(`playbooks/README.md`、`scripts/check-doc-consistency.py`)と本reviewを含む案件記録以外に変更が無いことを確認した(AC4相当)。
- `python3 -c "py_compile.compile(...)"`で構文確認OK。

## 未確認・範囲外

- AC1(`.githooks`実体である`.git/hooks/pre-commit` → `scripts/git-pre-commit-check.sh`全体の実行)そのものはCoordinatorの担当であり、本レビューでは実行していない。ただし本Policyの対象であるcheck-doc-consistency.py単体はstage内容に対してexit 0であることを確認済みで、pre-commit失敗要因としては残らないと判断する。
- `scripts/git-pre-commit-check.sh`内の他チェック(vaultヘッダ、IPv4検査等)は本diffの変更対象外であり、レビュー範囲に含めていない。
- `_inventory_host_groups`・`_playbook_host_patterns`が対応しないinventory/Jinja表記が今後どの程度の頻度で出るかは、将来のplaybook追加傾向次第であり判定していない。

## Verdict

Approve
