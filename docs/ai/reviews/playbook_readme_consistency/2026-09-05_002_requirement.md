# requirement: playbooks/README.md「対象」列の明文化・是正と、機械検査の追加

日付: 2026-09-05
決定: Yoshinobu(案A + 素のhosts部分の機械検査)
前提となる照合結果: `2026-09-05_001_findings.md`(A13件 / B13件)

## 決定の背景(観測事実)

`playbooks/README.md` の59行を実測した内訳。

| | 行数 |
|---|---|
| 「対象」列が play の `hosts:` の転記のみ | 44 |
| `hosts:` 外の語を含む | 15(うち**8行は誤記**=findings A01〜A08、**7行は意図的な補記**) |

意図的な7行の補記は3つの型に収まる。①group名では実体が分からないので展開する ②実行先が到達性・引数で決まるので選び方を書く ③play の外へ接続・委譲する先がある。**`semaphore_db_backup.yml` の行(`pve1`/`pve2`（選定）、quory(delegate_to))がこの流儀の手本である。**

**規則は既にファイルの中にあり、書かれていない。** findings の A09〜A12 は、この規則を適用し忘れた行である。

## 契約

### R1 — 凡例に「対象」列の定義を書く

`playbooks/README.md` の「安全区分の見方」と同じ階層に、次の内容の節を置く。文言は下記のとおりとし、増やさない。

> **対象**は play の `hosts:` を転記する。複数playなら順に並べる。次のときだけ補う。
> - group名だけでは実体が分からない → 実体を添える
> - 到達性や引数で実行先が決まる → 選び方を添える
> - play の外へ接続・委譲する先がある → 添える

### R2 — 誤記9行を現物に合わせる

findings の **A01〜A08、A13**。いずれも現物と食い違っている。A13は用途列(既に行わない処理が残っている)、他8件は対象列。

### R3 — 補記が抜けている4行に補う

findings の **A09〜A12**。play の `hosts:` は正しいので**そこは変えない**。R1 の規則に従って補記を足す。書き方は `semaphore_db_backup.yml` の行に倣う。

### R4 — check1 を拡張する

`scripts/check-doc-consistency.py` の check1(playbook `# tester-gate:` ↔ README カタログ)へ、**対象列の「素のhosts部分」が play の `hosts:` 集合と対応していること**の照合を足す。

- **素のhosts部分**とは、対象セルから補記(丸括弧内、および補記であることが読み取れる部分)を除いた残りを指す。
- `hosts:` が Jinja 式や変数の playbook がある(例: `{{ target_hosts | default('control_nodes') }}`、`{{ 'pve1' if target_node == 'pve2' else 'pve2' }}`)。**対応の付け方は実装者が決めてよい。** 判定できないものを黙って通す側へ倒さない。

### B項目は変更しない

findings の B01〜B13 は要約として正しいと判定済みである。**1行も変更しない。**

## 受入条件

- **AC1**: `.githooks` の pre-commit が全項目で通る。
- **AC2**: R4 の新しい照合が、**R1〜R3 適用後の59行すべてで指摘ゼロ**になる。
- **AC3**: **同じ照合を `HEAD` の README(修正前)に対して走らせると、findings A01〜A08 の8行を検出する。** 検出できない照合は入れない。この確認結果を成果物へ書く。
- **AC4**: `playbooks/README.md` と `scripts/check-doc-consistency.py` の2本以外に、自分が変更したファイルが1つも無い。

## 到達してはいけない状態

- 上記2本と成果物1本以外に、自分が変更したファイルがある状態で報告が返ること。
- 実ホストへ触れた状態、実行identityを昇格した状態に到達すること。
- stage / commit が行われた状態。

## 成果物

`docs/ai/reviews/playbook_readme_consistency/2026-09-05_003_implement.md`。変更した行の一覧、AC2/AC3の確認結果、未解決事項。

## 報告

対象パス、ACの充足状況、自己検証で確認したこと、未解決事項。中間ログと長い引用は載せない。
