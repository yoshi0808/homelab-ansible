## Code Review: proxmox_storage_monthly(独立再レビュー、007 Major1/2・009 locale欠陥・Semaphore catalog追加への対応確認)

### Summary

007のMajor#1(通知抑止式の未検証)・Major#2(`prune()`未検証)・Suggestion#1(token防御未検証)はいずれも解消を確認した。009 Testerが検出したlocale依存の日時解析blocking欠陥も解消を確認し、本セッションの実際のshell locale(`ja_JP.UTF-8`)下で自分で再実行して再現しないことを独立に確かめた。追加された`roles/semaphore_templates/defaults/main.yml`のcatalog/schedule追加は既存の命名規約・実行可否設計と整合する。Approveとする。

### Critical Issues

なし。

### Major Issues

なし。

### 007 Major1/2・Suggestion1への対応確認

- **Major#1(通知抑止式)**: `test_actual_report_suppression_expression`が`tasks/report.yml`から`storage_notion.suppress`の実際のJinja式文字列を`yaml.safe_load`で読み出し、`ansible-playbook`へ渡して`skip_notifications`/`proxmox_storage_monthly_notion_force_send`/`slack_force_send`/`ansible_check_mode`/`CLAUDECODE`環境変数の全組合せ(2^5=32通り)を評価し、期待式`check or skip or (agent and not force)`と一致することをassertしている。ハードコードした複製ロジックではなく現物の式を直接検証しており、将来の式改変への回帰検知が機能する。`slack_force_send`がNotion抑止に影響しないことも組合せの一部として確認される。
- **Major#2(`prune`)**: `test_prune_boundaries_and_protection`が実ファイルシステム上に、削除対象(cutoff未満+公開済)、境界月(cutoff同値、保持)、比較基準保護、未公開、symlink化、directory化の7パターンを作り、`archive.prune`実行後の生死を全パターンで検証している。symlinkケースでは`outside.txt`(symlink先)が無傷であることも確認しており、破壊的操作の安全側動作が検証されている。
- **Suggestion#1(token防御)**: `test_token_file_defenses`が0644での`token_permissions`、symlink経由アクセスでの`OSError`(`O_NOFOLLOW`)、空tokenでの`token_empty`をそれぞれ確認している。

### 009 locale欠陥への対応確認

- `module_utils/storage_monthly.py`の`scan_timestamp()`が`datetime.strptime`の曜日・月名解析を廃し、固定の英語月名リスト照合と数値引数によるdatetime構築へ置き換えられている。プロセスlocaleの変更は行わない。
- `test_scan_timestamp_ignores_process_locale`が`C`/`ja_JP.UTF-8`両locale下で同一入力から同一ISO時刻を得ること、不正な月名・不正な日付(2/31)が`ValueError`になることを確認している。
- **独立検証**: 本セッションのshell既定localeは`ja_JP.UTF-8`(009 Testerが実際に失敗を再現した条件と同一)。この状態で`python3 -m unittest discover -s tests -p test_storage_monthly.py -v`を自分で実行し、`test_ansible_controller_collect_check_and_replay`を含む15件全てPASSを確認した(009時点はこの1件がrc=2で失敗していた)。

### Semaphore catalog/schedule追加の整合確認

- `roles/semaphore_templates/defaults/main.yml`へ追加された`playbook: playbooks/proxmox_storage_monthly.yml`エントリ(`class: SEMI-SAFE`, `title: "Proxmox storage monthly"`)と、対応する`semaphore_schedules_catalog`の`template: "SEMI-SAFE: Proxmox storage monthly"`を確認した。`filter_plugins/semaphore_templates.py`の`semaphore_templates_render_name()`(`name = "{class}: {title}"`)に照らすと生成名は`"SEMI-SAFE: Proxmox storage monthly"`となり、schedule側の参照と一致する(不一致による孤立scheduleにはならない)。
- `survey_vars`の`operation`(enum: collect/replay)・`report_id`の形式は、既存の`proxmox_patch_apply_node`等の記法と一致する。
- scheduleは`cron: "0 8 15 * *"`(毎月15日08:00 JST)、`active: false`。006実装記録は工程管理表と照合し衝突する開始時刻は無いが後続処理の終了時刻は未確認としており、`active: false`のまま止めている点は001要件・003計画の「時刻照合後に確定、初期active=false」と一致する。

### Suggestions

なし(前回Suggestion#2の実機検証はTester側検証事項として残置。今回の差分で状態は変わっていない)。

### What Looks Good

- 007で確認済みの項目(check mode分離、抑止優先順位、冪等再送、baseline凍結、部分失敗、ロック競合、secret非露出)は今回の差分でも変更がなく維持されている。
- 機械チェックを自分で再実行し記録どおりであることを確認: `bash scripts/check-tester-gate.sh`(61 playbook OK)、`python3 scripts/check-doc-consistency.py`(3チェックOK)、`ansible-playbook playbooks/proxmox_storage_monthly.yml --syntax-check`(構文エラー無し、動的group未定義警告のみで想定どおり)。

### 確認範囲

- 006(更新後)・007・008・009・本Incident記録(`docs/ai/memory/incidents/2026-09-11_storage-monthly-locale-and-review-interference.md`)を読了。
- `git status`/`git diff --stat`で対象範囲を再確認(前回からの差分: `roles/semaphore_templates/defaults/main.yml`が追加、`tests/test_storage_monthly.py`に3テスト追加、`storage_monthly.py`に`scan_timestamp()`追加)。
- `tests/test_storage_monthly.py`の新規3テストおよび`scan_timestamp`回帰テストを現物で通読し、fixtureが主張する分岐を実際に踏んでいることを確認。
- 前回007の対象外とした`roles/semaphore_templates/defaults/main.yml`は今回スコープに含まれたため差分を通読し、命名整合をfilter_pluginの実装から裏取りした。
- ユニットテスト・tester-gate lint・doc-consistency・syntax-checkを自分で再実行し、記録の主張を独立に検証した(いずれもrepo作業ツリー内のローカル検証で、実ホスト・外部サービスへの接触なし)。

### 未解決事項

- Notion API実機呼出、実inventory(pve1/pve2)での`zpool status -P -p`/`nvme`出力の実地一致確認、Semaphore長時間処理との時間帯衝突有無の最終確認はTester・配備工程の担当であり未確認(006が明示する未完了事項から変化なし)。
- 前回Suggestion#2(spare/cache/log vdev構成でのzfs正規表現)は実装変更が無いため再掲しない。Testerの実機検証で確認を推奨する。

### Verdict

Approve(Critical/Major無し。007 Major1/2は解消確認、009 blockingは解消し独立再現テストで反証確認)。
