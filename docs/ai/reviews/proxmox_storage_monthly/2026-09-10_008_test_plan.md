# 月次ストレージ点検 test_plan

正本: 001要件、003計画、006実装記録。対象diff: `playbooks/proxmox_storage_monthly.yml`、`roles/proxmox_storage_monthly/`(defaults/tasks/files/library/module_utils)、`tests/test_storage_monthly.py`、`playbooks/README.md`、`docs/ai/context/operations/proxmox-storage-monthly.md`、`docs/ai/status.md`。

対象は`# tester-gate: check-mode-native`。破壊的task(`storage_notion`本体呼び出し)は`ansible_check_mode`に依存しない([tags: destructive]だが`when`ゲートは無く、代わりにmodule内部の`suppress`/`check_mode`ゲートに依存する設計)ため、`--check`実行に加えて`skip_notifications=true`を常に明示する(TS-031、Tester禁止事項)。

## 静的検証(独立再実行)

- S1: `python3 -m unittest discover -s tests -p test_storage_monthly.py -v` (既存11件の非回帰)。
- S2: `ansible-playbook playbooks/proxmox_storage_monthly.yml --syntax-check`。
- S3: `bash scripts/check-tester-gate.sh`(61+1本のtester-gate lint)。
- S4: `git diff --check`。
- S5: `python3 scripts/check-doc-consistency.py`(存在すれば)。

## Fixture/decoy動的検証(すべて`/tmp`のscratch report dirへ保存、実host・実Notion・実Slackへ触れない、`skip_notifications=true`を常に付与)

既存tests/test_storage_monthly.pyの`test_ansible_controller_collect_check_and_replay`と同型パターン(`--limit localhost`+inventory host varへ`storage_monthly_observation`を直接注入し、`storage_monthly_targets`groupの実行自体を起こさない)を踏襲する。scratch inventoryとreports_base_dirは`/tmp`配下に作る。

- D1 (AC7/AC2): 同月内で2回実行し、2回目のreport IDが1回目をcomparison_sourceとして参照し、月次baselineが1件のまま更新されることを確認。
- D2 (AC5): `storage_monthly_targets`扱いのhostを2台(1台はhostvarへ正常observationを注入、もう1台は`storage_monthly_observation`を注入しない=収集未達)にして本実行し、最終rcが非0、保存されたreportに両hostのレコードがあり片方が`error`のまま残ること、Slackが`skip_notifications=true`で送信0件であることを確認する。
- D3 (AC3): `comparison_source`が指す過去reportファイルを破損させたのち再実行し、`history_error`が立ち`comparison`が`history_corrupt`、healthがUNKNOWNになることを確認(月次baselineファイルの破損)。
- D4 (AC3): 同一hostで前回と異なるserialのdeviceを注入し`replacement_or_added`が付くことを確認(独立Pythonでarchive()を2回呼ぶfixture、build_report経由ではなくarchive相当のfreeze_devicesとbuild_reportの結合を再現)。
- D5 (AC4): pool状態がDEGRADED、READ/WRITE/CKSUM非ゼロ、scrub in progress、no history(`none requested`)の4バリエーションでissuesの分類(CRITICAL/WARNING)を独立に確認。
- D6 (AC6): fake HTTP transportで(a)応答消失後の再送→POST1回のみ、(b)重複ページ検出で停止、(c)`collected_at`が新しいstateへ古いreportの`older_report_refused`、(d)token権限不正(0644)で`Client`初期化が静的コード`token_permissions`で失敗しtoken内容を例外へ出さないことを確認(既存testに無いケースのみ追加)。
- D7 (AC8): `report.yml`の`suppress:`Jinja式をそのまま切り出したlocalhost/connection:local使い捨てplaybookで、`ansible_check_mode`/`skip_notifications`/`CLAUDECODE`/`proxmox_storage_monthly_notion_force_send`の組合せ(最低6通り)を`assert`し、期待どおりの真偽になることを確認する(実`storage_notion`は呼ばない、副作用ゼロ)。検証後に使い捨てplaybookを削除する。
- D8 (AC9): 保存失敗(reports_base_dirを書込不可にする)で`storage_archive`が例外を出し、report/bodyがコミットされず、例外メッセージに実データを含まないことを確認。

## 対象外・Not Run

- 実quory/Semaphore経由の配備、実Notion API疎通、実Slack送信(EXEC-052外、Tester権限外)。
- AC1/AC10の本番初回実行相当(quory実機、Integration token配備前提のためNot Run — Step5はYoshinobu)。
- `roles/semaphore_templates`のcatalog登録はこの差分にまだ含まれない(未実装)ため対象外。

## 完了条件

各Dシナリオの実測結果と、静的検証のrc・件数を`2026-09-10_009_test_result.md`へ記録し、Verdict(PASS/FAIL/Not Run)をAC別に付す。使い捨てplaybook・fixtureファイルは`/tmp`に閉じ、検証後に残さない。
