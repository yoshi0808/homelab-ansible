# 本番検証前提の確認準備

2026-09-11に再開。git statusはclean、mainとorigin/mainは一致、実装commitはcda643d、HEADはccc4661だった。010のApproveと011のPASSを読了。statusのcommit/push承認待ちは現物と一致しないため更新した。

次のread-only OPREQを作成し、repoのschemaとDLPでPASSを確認した。対象はquoryのコード取得、Semaphore template/schedule、tokenファイルのメタデータと実行identityの読取可否、既存レポートの有無、pve到達性。token本文読取と本番状態変更は依頼しない。

## 送信結果

初回wrapperは同期engine停止の事前検査でrc2となり登録されなかった。Yoshinobuの起動済み連絡後もsandbox内では同じ結果だったが、正規のrequire_escalatedで同一statusを実行すると同じPIDがengine running、直近同期成功と表示された。sandbox内のstale表示から実プロセスの停止とは断定できない。

同じ送信wrapperをrequire_escalatedで実行しrc0。OPREQ `req-20260911T170033+0900-5eadffb4d1a941ed` を登録、homelab-opsのOperatorへ通知済み。安全検査や設定の変更、別送信経路の使用は行っていない。

## 回答受領

OPRES `req-20260911T171216+0900-45d070d1570eb0c0` をclient getで受領。quory HEADはccc4661でcda643dを含む。playbookが存在し、対象templateとscheduleは各1件登録済み、collect/replay surveyの既定はcollect、cronは0 8 15 * *、active=false。実行履歴0件、report保存先なし。tokenは通常ファイル、yoshi:yoshi、0600でSemaphore実行identityからreadable(本文未読)。pve両nodeにread-only SSHで到達できた。

次はSemaphoreからcollectの手動初回実行を行い、JSON/Notion/Slack結果を確認する。Integrationの対象ページ権限・tokenの有効性・実pve出力一致は実行前には未確認。scheduleは初回受入まで無効のまま。ジョブ起動や投稿は本照会では行っていない。
