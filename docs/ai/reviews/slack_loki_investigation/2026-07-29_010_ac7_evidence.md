# AC7 一次証跡 — Slack → Codex → execpolicy の通し

## このファイルの位置づけと限界

**これは Slack 上の Codex 応答を Yoshinobu が対話へ転記し、それを Coordinator がファイル化したものである。** Slack の原文でも、recovery-io / Codex のログでもない。転記の過程で欠落・改変が生じていないことは保証できない。

`2026-07-29_007_test_result.md` は AC7 を「到達不能」とし、Tester の identity では recovery-exec の SSH 鍵を持てないため Yoshinobu の手動実行が必要とした。本ファイルはその実行結果の記録である。

**より強い証跡が必要な場合**は、quory の `journalctl -u recovery-io` に該当時刻の実行記録が残っているはずなので、そちらを一次とすること(本案件では取得していない)。

## 実行1 — 広い窓(誤った結論に至った回)

依頼: `@Homelab 2026-07-29 06:00頃のログを見て`

応答(転記):

```
2026-07-29 06:00頃のログを確認しました。

1. 調査結果
05:55-06:10 の Loki 集約ログを確認。
主な異常系は以下です。

- 05:55:08 quory: homelab-incident-capture.service が一度 exit-code で失敗
- 06:00:01 quory: 同サービスは再実行され、正常終了
- 05:58-06:03 の範囲では error はなし
- 06:00 前後、quory の監視 probe は monitoring paused (global) — skip で authy / monnie / sophos-fw をスキップ
- UniFi AP では 06:02 頃にクライアントローミング関連ログあり
- Sophos FW は DHCP renew と system health の information ログのみ

2. 復旧対応
サービス停止は確認していないため、復旧コマンドは実行していません。

3. 現在ステータス
homelab-monitoring-status の結果は PAUSED (all targets) です。
現在の状態は 異常 / 確認必要 です。意図した停止でなければ、監視を再開する必要があります。
```

**判定**: 経路としては PASS(execpolicy の default deny に阻まれず、Codex がラッパーを呼び、日本語で要約を返した)。**内容としては誤り** — 06:07:07 以降の daemon-reexec と recovery-probe 再起動に触れていない。切り詰め通知にも言及していない。

報告された範囲が「05:58-06:03」で止まっている点が、要求窓(05:55-06:10)の後半を読めていなかったことの徴候である。詳細は `progress.md` C-5。

## 実行2 — 窓を絞った回(PASS)

依頼: `@Homelab 2026-07-29 06:05 から10分間の ubuntu-nodes のログを見て`

応答(転記):

```
2026-07-29 06:05 JST から 10 分間の ubuntu-nodes ログを確認しました。

調査結果:
- quory で 06:05:01 に homelab-incident-capture.service が実行され正常終了。
- 06:05:29 / 06:06:29 / 06:07:14 に authy / monnie / sophos-fw の監視が monitoring paused (global) のためスキップ。
- 06:06:58 から apt-daily-upgrade.service / apt-daily.service が開始。
- 06:07:07 頃に systemd の reexec が発生し、その後 chrony, corosync-qnetd, cron, multipathd,
  fwupd, ModemManager, polkit, recovery-io, recovery-probe, sshd, rsyslog, systemd-networkd
  などが停止・再起動されています。
- 06:07:15 に recovery-probe が EXTERNAL unreachable (ISP または FW 断) — 監視のみ、発火しない を記録。
- 06:07:29 から ansy で ansible-incident-sync.service が開始し、quory から incident bundle の同期を実行。

注意:
- 該当窓は 300 行超で、取得結果は先頭 299 行までで打ち切られています。
- error だけの絞り込みではなく any で確認しました。

復旧操作:
- 実施していません。今回はログ確認のみです。

現在ステータス:
- このログ窓では、主に quory の apt/systemd reexec と監視グローバル停止中の記録が目立ちます。
- 状態判定はログ確認範囲だけでは 異常確定 ではありませんが、06:07:15 の EXTERNAL unreachable は要注意です。
```

**判定: PASS。** requirement §6 の成功指標「人が scratch playbook を書いた場合と同じ結論へ到達できること」を満たす。

`2026-07-29_001_measurement.md`(Tester が scratch playbook で得た結果)との突合:

| 事象 | measurement | 実行2 | 一致 |
|---|---|---|---|
| `apt-daily-upgrade.service` 開始 | 06:06:58 | 06:06:58 | ○ |
| systemd reexec | 06:07:07 | 06:07:07 頃 | ○ |
| 多数サービスの Stop→Start(recovery-probe 含む) | 06:07:14 | 記載あり | ○ |
| `EXTERNAL unreachable` | 06:07:14 | 06:07:15 | ○(秒の丸め差) |

**切り詰め通知を Codex が利用者へ伝えている**点も確認できる(「該当窓は 300 行超で、取得結果は先頭 299 行までで打ち切られています」)。ただしこれは改修**前**の通知であり、読めた範囲は示されていない。改修後の通知の検証は `2026-07-29_008_test_result_followup.md`。

## 実行3 — 監視再開前の健全性確認(本 AC の対象外、Incident 対応)

依頼: `@Homelab authy と monnie の status を確認して`

応答(転記):

```
確認しました。

1. 調査結果
   - authy: freeradius と sshd はどちらも active (running)
   - monnie: prometheus / grafana-server / loki / unpoller はすべて active (running)

2. 復旧対応
   - 非 active のサービスは確認されなかったため、復旧操作は実行していません。

3. 現在状態
   - 正常です。エスカレーション不要です。
```

`docs/ai/memory/incidents/2026-07-29_global-monitoring-pause-left-on-8-days.md` の「修正内容」が根拠として挙げている確認は、これである。
