# plan_review — Slack/Codex から Loki 横断ログを調査できるようにする(計画査読)

Reviewer(計画査読)、独立subagent。対象: `2026-07-29_002_requirement.md`(以下「計画」)。入力: `2026-07-29_001_measurement.md`(以下「実測」)。両方とも現物を読んだ。実ホストへは接続していない(不要)。

## 層1(数えるだけで判定できるもの)

**判定: PASS(差し戻し不要)。**

計画§8の申告:
- 単位1(Policy改訂・Context同期): 0件
- 単位2(dispatch引数機構・2 check・mirror・defaults・AGENTS.md): 1件(JSON整形処理の置き場)

いずれも基準「1単位に未決定の設計判断が2つ以上」を超えない。

**申告の妥当性の確認方法と結果**: 計画本文を通読し、申告以外に「未決定」「検討中」「TBD」等の語または実質的な二択が残っている箇所が無いかを`grep`と目視の両方で確認した。単位2の実装は既存のPVE系(`homelab-investigate-pve.sh.j2` / `recovery-investigate-dispatch-pve.sh.j2`)のパラメータ検証パターン(`p1`/`p2`変数、`read -r`分割、`deny_count`/`deny_invalid`関数)を土台にできる。ただしPVE系は最大2パラメータ(`check + p1 + p2`、`argc>3`で拒否)の構造であるのに対し、`loki-window`は4パラメータ(`job, since, window, level`)を要し、`recovery-investigate-dispatch.sh.j2`(monnie側)は現状パラメータ分割を一切行わない純粋な文字列一致の`case`である。この差分は**設計判断ではなく機械的な拡張**(`p1 p2`を`p1 p2 p3 p4`に増やす、`case "$cmd"`を`case "$check"`+`read`前処理に置き換える)と判断した。計画のRK1「引数機構の新設が既存の無引数checkを壊す」およびAC6が同じ懸念を既にカバーしており、申告漏れとして層1側の指摘には計上しない(層1/層2どちらに属するか未確定な事案には該当しなかった)。

以上より、層1申告(単位1=0、単位2=1)は妥当と判定した。

## 層2(技術的前提の反証) — findings

**件数: 4件。内訳: Blocker 1、Major 1、Minor 0、Informational(確認結果の記録) 2。**

### [Blocker] F1: Policy新設節のAR番号(AR-052〜AR-058)が既存条項と全面衝突する

計画§9-2は`docs/ai/policies/autonomous_recovery_policy.md`へ`AR-052`〜`AR-058`を新設節として挿入する逐語案を提示している。しかし現物確認の結果、**AR-052〜AR-058は既に別内容で存在する**(いずれも`## 4. 判断軸`および`## 5. ライフサイクル・処理フロー`節):

| 既存番号 | 既存の内容(現物、行番号) |
|---|---|
| AR-052 | L133 `pull probeは全対象を60秒間隔で継続監視する。` |
| AR-053 | L75 `sophos-fw`のflapping閾値(icmp/dns 5回連続失敗) |
| AR-054 | L78 `authy`のflapping閾値 |
| AR-055 | L81 `monnie`のflapping閾値 |
| AR-056 | L136 `各targetは短縮名でなく明示FQDNで解決する。` |
| AR-057 | L84 `ladder lockを取得済みなら重複実行をskipする。` |
| AR-058 | L87 `直近24時間で3回以上発火していればflappingと判定し...` |

計画案をそのまま適用すると、同一の`<!-- AR-0NN -->`アンカーが文書内に2箇所存在することになり、他条項や過去記録からの`AR-052`等の参照が指す内容が曖昧になる(現行の`AR-055`は「monnieのflapping閾値」だが、計画適用後は「Loki横断ログ調査のwrapper/dispatch二層検証」を指すことになり、既存の参照先が意味的に破壊される)。

現物確認: `grep -on "AR-[0-9]\{3\}" docs/ai/policies/autonomous_recovery_policy.md | sed 's/.*AR-//' | sort -n -u` で使用済み番号の最大値は**AR-094**。未使用の連番は**AR-095以降**。

**対応**: §9-2の新設番号を`AR-095`〜`AR-101`など未使用域へ振り直す必要がある(番号の中身自体、AR-052〜058案の技術内容は他条項と矛盾しない — 番号だけの問題)。既存節のAR-052〜058は変更不要。**Yoshinobuがcommitする前に必ず修正が要る**(計画§9冒頭に「Yoshinobuがcommitするため妥当性判断はYoshinobuに属する」とあるが、番号衝突は妥当性判断以前の客観的事実誤りであり、Reviewerとして指摘する)。

### [Major] F2: `docs/ai/policies/autonomous_recovery_policy.md`の変更履歴追記(§9-3)がAR番号を含まない

軽微だが記録しておく。F1修正後、§9-3の変更履歴文言中に「AR-052〜AR-058」という表記が残ると、修正後の実際の番号(例: AR-095〜AR-101)と食い違う。F1の修正時に§9-3も同時に直す必要がある(F1の一部として扱ってもよいが、Implementer/Yoshinobuが§9-2だけ直して§9-3を直し忘れる実務上のリスクがあるため独立に記載する)。

### [Informational] F3: file:line引用の逐行照合 — 確認した範囲はすべて現物と一致

計画・実測が引用する以下のfile:lineを現物で確認し、引用内容と実際のコード/設定が一致することを確認した(不一致なし):

- `roles/recovery_exec/templates/recovery-investigate-dispatch.sh.j2`(monnie側dispatch、`case "$cmd"`で純粋な文字列一致、引数分割なし) — 現物確認
- `roles/recovery_exec/templates/homelab-investigate.sh.j2`(monnie側wrapper、`case "${1:-status}"`、単一token) — 現物確認
- `roles/recovery_exec/templates/homelab-investigate-pve.sh.j2` / `recovery-investigate-dispatch-pve.sh.j2`(AR-046/047/048/088が実装されているPVE系precedent。`read -r check p1 p2 extra`、`deny_count`/`deny_invalid`、二層独立検証) — 現物確認。計画がここを土台にする前提は妥当
- `roles/recovery_io/templates/recovery-io.py.j2:97` — `say(text=f"<@{user_id}>\n{response[:3800]}", ...)`、3800文字打ち切りの記述と一致
- `roles/recovery_probe/files/recovery-probe.py:34`(`def log(msg):`)、`:107-118`(`def external_reachable(cfg):`、次の`def`は124行目)— 一致
- `roles/recovery_probe/defaults/main.yml:8`(`recovery_probe_interval_s: 60`)、`:23`(`recovery_probe_external_check_url`)— 一致
- `roles/alloy/defaults/main.yml:60-135`(`alloy_file_sources`/`alloy_journal_sources`のラベル定義。`network_devices`ソースに`host`ラベルが無い=level無し系列の実測と整合)— 一致
- `roles/alloy/templates/config.alloy.j2:65-102`(`extract_level_best_effort`ブロック、レベル不一致時はlabel未設定のまま次段へ)— 一致
- `roles/rsyslog_forward_to_monnie/defaults/main.yml:2-4`(`rsyslog_forward_allowed_hosts`に`quory`含む)、`templates/forward-to-syslog.conf.j2`の`MaxLevelSyslog=debug`— 一致
- `docs/ai/policies/autonomous_recovery_policy.md`のAR-024(L218-219)、AR-026(L224-225)、AR-046/047/048(L282-289)、AR-088(L323)— 内容・行番号とも一致
- `docs/ai/context/system/autonomous-recovery.md:15`(`service recovery対象`の説明が`target内の許可serviceを扱う`と記述し、Loki横断ログ参照点であることが落ちている)— 一致、R8の前提は妥当

### [Informational] F4: `codex-config.toml.j2`(execpolicy)は変更不要という省略が正しい

計画の単位2ファイル一覧(§8)は`recovery-investigate-dispatch.sh.j2` / `homelab-investigate.sh.j2` / `defaults/main.yml` / `AGENTS.md.j2`を挙げ、`roles/recovery_exec/templates/codex-config.toml.j2`を挙げていない。現物確認したところ、execpolicyのルールは`command = "homelab-investigate-{{ target.name }}"`という**コマンド名単位の許可**であり、引数パターンでの制限は行っていない(`homelab-investigate-pve1 <check> <param1> [<param2>]`が既にexecpolicy変更なしで機能している前例と整合)。したがって`loki-count`/`loki-window`をラッパー・dispatch側へ追加しても、execpolicy側の変更は不要というファイル一覧の省略は正しい。AC7(execpolicyのdefault denyに阻まれないこと)は設計上自然に満たされる公算が高いが、**Tester による実ホストでの実行確認は依然として必要**(AC7の文言どおり)。

## 未解決事項

- F1・F2はYoshinobuがcommitする前に修正が必要(§9冒頭の断り書きにより逐語案の妥当性自体はYoshinobu判断だが、番号衝突という客観的誤りは実装前に解消すべき)。Coordinatorへ差し戻す。
- 層1/層2のどちらに属するか未確定な事案(`docs/ai/status.md`のNext「計画査読の正本に足りない1点」)には、今回**該当する事案が発生しなかった**(F1・F2はいずれも純粋に層2、技術的事実誤りであり、曖昧判定を要さなかった)。
- 単位2の未決定(JSON整形処理の置き場)自体は査読対象外(Coordinatorが計画時点で未決定と明示済み。実装着手前に判断材料が計画本文に揃っていることは確認した)。
