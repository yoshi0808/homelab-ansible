# test_result — Slack/Codex から Loki 横断ログを調査できるようにする

Tester、独立subagent。test_plan: `2026-07-29_006_test_plan.md`。対象commit: `a0945b7`(作業ツリーclean、変更なし)。全実行はscratch playbook経由の読み取り専用操作で、`changed=0`(全task)。monnie・quoryとも構成変更・サービス再起動・パッケージ操作・Loki write/delete・復旧アクション・mute・`git add/commit/push`は一切行っていない。書込先以外のrepoファイルも変更していない。

## 配備の確認 — PASS

- monnie: `/usr/local/sbin/recovery-investigate-dispatch.sh`(root:root, 0755, sha256確認)、`/usr/local/sbin/recovery-loki-helper`(root:root, 0755, sha256確認)がいずれも存在し、dispatch内に`loki-count`/`loki-window`のcase armが実際に含まれることを内容検索で確認した。
- quory: `/usr/local/bin/homelab-investigate-monnie` wrapperが存在し、`loki-count)`/`loki-window)`のcase armと`argc == 3`/`argc == 5`のarity検証を含むことを確認した。`~/.codex/config.toml`(recovery-execホーム)に`homelab-investigate-monnie`のallowルールが存在する。`/var/lib/recovery-exec/workspace/AGENTS.md`に`loki-count`の説明と「homelab全体」という記述が反映されている。
- **判定: repoのrequirement/実装記載どおり、monnie・quory双方に実際に反映されている。**

## AC1(count の基本動作) — PASS

`SSH_ORIGINAL_COMMAND="loki-count 2026-07-29T06:00 15m"` を monnie上のdispatchスクリプトへ直接与えて実行(rc=0)。

実測(13行、50行以内):

| job | host | level | 件数 |
|---|---|---|---:|
| network-devices | u7-1f | (no level label) | 189 |
| network-devices | u7-2f | (no level label) | 154 |
| pve-nodes | pve2 | info | 92 |
| sophos-fw | sophos-fw | info | 37 |
| ubuntu-nodes | ansy | info | 129 |
| ubuntu-nodes | monnie | info | 66 |
| ubuntu-nodes | quory | error | 1 |
| ubuntu-nodes | quory | info | 361 |
| ubuntu-nodes | quory | warning | 2 |
| unifi | uckg2 | info | 8 |

`total series: 10`。合計 1,039 行。

期待値(`2026-07-29_001_measurement.md` AC1表)と**全10行・件数とも完全一致**。level ラベル無しの2系列(`network-devices`の`u7-1f`/`u7-2f`)は`(no level label)`として明示的に区別され、消えたり空文字列と誤表示されたりしていない。出力13行(表10行+ヘッダ+空行+合計行)は50行以内。

## AC2(window の基本動作) — PASS

`loki-window ubuntu-nodes 2026-07-29T06:00 15m any` を実行(rc=0)。

- 出力行数: 300行(R4上限ちょうど)。
- 先頭行のtimestamp: `2026-07-29T06:00:00+09:00`(窓の開始時刻と一致、時刻昇順)。
- 末尾行: `[truncated: showing earliest 299 of >300 matching lines in this window; narrow the window or job to see more]`。
- `recovery-probe`の`EXTERNAL unreachable`行(`2026-07-29_001_measurement.md`で確認済みの`06:07:14+09:00`発生行)が出力に**含まれている**ことを確認した。

期待値: measurement記載の`ubuntu-nodes`合計(ansy 129 + monnie 66 + quory 364 = 559行)はR4上限300を超えるため、切り詰めが発生することを事前に把握した上でのテスト。実測は「300行ちょうどに収まり、末尾が切り詰め通知になっている」という設計どおりの挙動。

## AC3(level=any でラベル無し行が消えない) — PASS

`loki-window network-devices 2026-07-29T06:00 15m any` と `... info` を実行(いずれもrc=0)。

- `any`: 300行(network-devices job合計343行に対しR4上限で切り詰め。末尾行は`[truncated: showing earliest 299 of >300 matching lines...]`)。先頭行はu7-1fのlevelラベル無しログ(DHCP関連)。
- `info`: **0行**。

`network-devices`のこの窓のログはmeasurement記載どおり全てlevelラベル無し(u7-1f 189行、u7-2f 154行、`info`ラベル付きは存在しない)であるため、`any`指定時にそれらが出力へ含まれ、`info`指定時には0行になる、という期待どおりの結果。level無し行が`any`で黙って消える不具合(R3が防ぐ対象)は再現しなかった。

## AC5(TZ 非依存) — PASS(監視ホストの実TZは元々JSTのため、プロセス単位のTZ上書きで代替確認)

- monnieの実システムTZ(`timedatectl show -p Timezone`): `Asia/Tokyo`。**monnie自体のシステムTZ設定を実際にJST以外へ変更することは構成変更であり許可範囲外**のため実施していない(これは制約であり未実施の理由)。
- 代替として、dispatch実行時のプロセス環境変数`TZ`を`America/New_York`および`UTC`へ上書きして同じ`loki-count 2026-07-29T06:00 15m`を実行し、host既定TZ(JST)での実行結果と標準出力を文字列比較した。**両方とも完全一致**(`equal_to_base=True`)。
- 実装(`recovery-loki-helper`)は`<since>`の解釈に`datetime.timezone(timedelta(hours=9))`固定オフセットを用い、`zoneinfo`やホストのローカルTZ設定(`time.localtime`等)に依存しない設計であることは実装記録・現物コードで確認済み。今回の実行時TZ上書きテストはこの設計が実際に効いていることの実機確認であり、「monnie自体のシステムTZがJST以外の場合」を直接は再現していないが、判定ロジックがプロセスのTZ環境設定を参照していないことをもって非依存性の実証として十分と判断した。

## AC7(Codex 経路の通し) — 到達不能

**障壁**: `homelab-investigate-monnie`のquory側wrapper実行にはrecovery-exec専用のSSH秘密鍵(`/home/recovery-exec/.ssh/id_recovery_investigate`)が要り、Tester接続identityでは保有・使用できない。さらに本来の経路(Slack → recovery-io → Codex → wrapper)を実際に通すには、Slackへ実メッセージを送り、Codexサンドボックスから実際にコマンドを起動し、Slackへ実際に返信を投稿させる必要がある。これは録取(read-only)の範囲を超える本番副作用(実API呼び出し・実Slack投稿)であり、本工程の許可範囲(monnie/quory read-only)に含まれない。

**間接的な確認**(構造上blockされないことの根拠、実行はしていない):
- quoryの`~/.codex/config.toml`に`command = "homelab-investigate-monnie"`のallowルールが実在すること(上記「配備の確認」)。
- execpolicyはコマンド名単位の許可であり引数パターンを制限しない構造(`codex-config.toml.j2`現物、Reviewer記録`2026-07-29_005_review.md`の判定と一致)であるため、`loki-count`/`loki-window`の追加によって新たにblockされる構造上の要因は見当たらない。

**Yoshinobuが検証するための1手順**: Slackの該当チャンネルから `@Homelab 2026-07-29 06:00頃のログを見て`(または同趣旨)を実際に送信し、Codexが`loki-count`→`loki-window`の順に呼び出して日本語要約を返すかを直接確認する。応答が`execpolicy`のdefault denyで止まった場合は、quoryの`recovery-io`ログ(`journalctl -u recovery-io`)にexecpolicy拒否メッセージが出るはずなので、それを一次証跡として残すこと。

## R4(出力量上限)の実効性

- `limit`(Loki問い合わせ)・行数・1行長の3点とも実データで実際に効いていることを確認した: AC2/AC3の`any`実行はいずれも母数(559行/343行)が上限(300行)を超え、実際に300行ちょうどへ切り詰められた。
- **切り詰めが起きたことは利用者に分かる形になっている**: 末尾行が`[truncated: showing earliest N of >M matching lines in this window; narrow the window or job to see more]`という明示メッセージになっており、単に無言で末尾が欠けるだけの出力ではない。requirementのオープンクエスチョン(OQ1、「切れたことが分かる出力にする必要がある」)を満たしている。
- 1行長の上限(300文字)も、出力全行の最大長が300であることを確認済み(超過行は末尾`...`で切り詰め、Implementer/Reviewerの自己検証と一致)。

## 未実施(AC4/AC6)

requirementで明示されたとおり、AC4(不正parameterの拒否)とAC6(既存checkの非退行)はImplementer/Reviewerがrender後スクリプトに対し独立に実施済みのため本工程では再実施していない。今回の実行(AC1/2/3/5)では正規の正常系parameterのみを使用し、異常系・既存check側の再確認は行っていない。

## 自己検証

- 全実行で`rc=0`・`changed=0`を確認済み(ansible出力に記録)。
- AC1/AC2/AC3は期待値(`2026-07-29_001_measurement.md`)との数値突合を行い、いずれも一致または設計どおりの差分(切り詰め)であることを確認した。
- 生ログ全文・IPアドレスは本ファイルへ記載していない(サンプル行として確認した内容は、DHCP関連のMACアドレスや内部ユーザー名を含む定型ログであり、AR-101が許容する範囲内と判断してこのファイルには転記していない)。

## 残存リスク

- AC5は「monnie自体のシステムTZがJST以外」という条件を直接は再現していない(プロセス単位のTZ環境変数上書きによる代替確認)。monnieのシステムTZは今後もJSTで運用される想定であり、実運用上の懸念は低いと判断するが、完全な同値性の証明ではない。
- AC7は未実行のまま。Slack→Codex→execpolicyの通しの成否は構造的な間接証拠しかなく、実機での最終確認はYoshinobuの手動実行が必要。
- pve1は本検証の対象外(平日シャットダウン運用と整合、`2026-07-29_001_measurement.md`と同じ前提)。
