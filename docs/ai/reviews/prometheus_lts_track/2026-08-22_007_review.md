# review: prometheus の運用 track 対応（5回目の文書確認）

Reviewer: Codex / 2026-08-23  
対象: `_006_review.md` Findings 1-2 の文書修正  
Verdict: **Request Changes**

## Summary

前回の2文は正しく直っている。requirement §8はmonthly機械の役割を「候補を検知・提示するが、採否を判定せず版も指定しない」とし、UV-087と一致する。実装記録もnon-apt weeklyを「機械は検知まで、適用は人が`dry_run=false`を明示」とし、UV-086と一致する。コード6ファイルとPolicyのSHA-256は前回と全件同一である。

ただし全文突合で、別の古い現在形が2箇所残っている。requirement §6は改訂前UV-032を今も「現行文」、改訂済み本文を「改訂案」と呼び、実装記録末尾は実際に改訂されたR4を含む§4要求表を「未更新」とする。Approveにはまだできない。

## Findings

| # | File | Lines | Finding | Severity |
|---|---|---:|---|---|
| 1 | `docs/ai/reviews/prometheus_lts_track/2026-08-22_001_requirement.md` | 60-70 | **UV-032の改訂状態が現在のPolicyと逆のまま。** 62行はStatusを`REVIEW_REQUIRED`へ昇格する旧文を「現行文」、66行はStatusを上げない現行UV-032を「改訂案」とする。問題定義12/19行は案件着手前の問題を説明する履歴として読めるが、§6は時点を限定せず現在形でPolicyを参照するため、現行Policy 159行と両立しない。「起案時の旧文 / 承認・反映済みの現行文」など、履歴と現在を明示すること。 | Medium |
| 2 | `docs/ai/reviews/prometheus_lts_track/2026-08-22_002_implement.md` | 152 | **「requirement本体（§4要求表）は未更新」が現物と一致しない。** R4はinstalled-version例外を含む形へ実際に更新済みで、実装記録18/24/149行もその更新を解消済みとしている。H2/H3が独立したR番号を持たない、という追跡性の指摘を残すなら、その事実だけに限定し、「§4未更新」または「Coordinatorが改訂するか未定」という現在形を除くこと。 | Medium |

## Requested Checks

### `_006` Finding 1

解消。requirement 90行のmonthly列と94行はいずれも、機械はupgrade候補を検知・提示するが、採否と版指定は人に留保すると定める。UV-087の「機械は検知して提示するところまで」「採否・版指定は人」と一致し、monthly非apt事実提示（UV-032）とも矛盾しない。

### `_006` Finding 2

解消。implement 61行はweekly non-aptについて機械の役割を検知までに限定し、manual applyを`dry_run=false`の人間明示へ留保する。requirement 33/90行、UV-086、UV-035〜038と一致する。

### Other allowed documents

- requirement / implement / Policy / status.mdを全文確認し、weekly/monthly、apt/non-apt、検知/提示/採否/適用、Status昇格、系統外通知例外の語彙を横断検索した。Findings 1-2以外に同型の現在形矛盾は見当たらない。
- `status.md` 61行は「apt以外の機械適用」を将来候補として置き、現在は検知まで・人が明示適用、Policyへ現在形で書けばUV-035〜038と矛盾すると明記する。現行境界と将来案を区別できている。
- `status.md` 68行のSemaphore更新検知も自動適用を否定し、Prometheus roleをmanual applyの雛形として参照するため、現行境界を広げない。

## Knowledge Scope

依頼に含まれた `docs/ai/memory/decisions/rejected-proposals.md` は検査していない。`docs/ai/core.md:86` が「Knowledge（`docs/ai/memory/`）を読むのはCoordinatorだけ。subagentは読まない」と禁止しており、Reviewer Roleからこの境界を越えられないためである。

このファイルについてはCoordinator自身が全文確認し、結果を監査証跡へ残す必要がある。Knowledgeは規範の正本ではないためReviewerのPolicy/code承認範囲からは除外できるが、「指定5文書すべてをReviewerが確認した」とは報告できない。

## Immutability Check

前回 `_006_review.md` 時点と今回のSHA-256は同一。

| File | SHA-256 prefix |
|---|---|
| `roles/prometheus_update_check/defaults/main.yml` | `1c00b730...` |
| `roles/prometheus_update_check/tasks/main.yml` | `25fc2e88...` |
| `roles/prometheus_update_check/tasks/notify.yml` | `86215d0a...` |
| `roles/prometheus_update_check/tasks/upgrade.yml` | `446521a2...` |
| `roles/ubuntu_vm_full_upgrade/tasks/classify.yml` | `b18f852b...` |
| `playbooks/ubuntu_vm_full_upgrade.yml` | `666cefd4...` |
| `docs/ai/policies/ubuntu_vm_patch_policy.md` | `9a9caf6a...` |

コードとPolicyは変更されていない。

## Verification

- requirement / implement / Policy / status.md全文確認。
- stale-boundary語彙の横断検索。
- `git diff --check`: rc=0。
- KnowledgeはCORE-086相当の明示禁止により未読。

## Verdict

**Request Changes** — 前回2件は解消したが、現在のPolicy改訂状態を逆に示す文がrequirementとimplement記録に1箇所ずつ残る。コード・Policyの変更は不要。Coordinatorは併せて、Reviewerが読めない`rejected-proposals.md`を自身の権限で確認すること。
