# review: prometheus の運用 track 対応（最終・全文一貫性検査）

Reviewer: Codex / 2026-08-23  
対象: requirement / implement / Policy / status の全文一貫性、および `_007_review.md` Findings 1-2 の修正  
Verdict: **Approve**

## Summary

前回2件は解消した。requirement §6は旧UV-032を「改訂前」、現行UV-032を「改訂後(現行)」として正しく区別し、実装記録152行もR4/AC3が更新済みであることを示す。

指定4文書を全文で突き合わせた結果、現行の要求・受入条件・Policy・実装記録・statusに矛盾は無い。撤回済みの設計は、変更前の問題定義、明示された改訂前引用、または時系列の訂正記録としてのみ残っており、現行仕様として述べる箇所には残っていない。コード6ファイルとPolicyのSHA-256は前回と全件同一である。

## Findings

なし。

## Full Consistency Matrix

| 論点 | requirement | Policy | implement / status | 結果 |
|---|---|---|---|---|
| aptの担い手 | §8でUbuntu Pro等の対象は自動、その対象外はmonthly | UV-018 / UV-085 / UV-086 | statusはapt自動、non-aptは将来候補として分離 | 一致 |
| non-apt weekly | 検知まで、適用は人が`dry_run=false`を明示 | UV-035〜038 / UV-086 | implement 61行、status 61行が同じ境界 | 一致 |
| monthly upgrade | 機械は候補を検知・提示するが採否と版指定は人 | UV-087 | implementのR5説明と一致 | 一致 |
| 系統外版 | 上流取得版は週次通知へ出さない。mismatch時のinstalled版だけ許容 | UV-088 | implement 51〜67行が同じ区別を記録 | 一致 |
| non-aptとStatus | 月次は版・reasonを出すがStatusを昇格しない | UV-032 / UV-094 | classify変更記録と一致 | 一致 |
| track異常 | 該当releaseなしを更新なしと区別し、系統外applyを止める | UV-089 | `track_not_found` / `track_installed_mismatch`の記録と一致 | 一致 |

## Current State and History Classification

- requirement 7〜22行の`releases/latest`比較と`REVIEW_REQUIRED`昇格は、§1「問題定義」に置かれた着手前の状態である。現行要求は§4、現行ACは§5、現行Policy引用は§6、現行の判断軸は§8に分離されている。
- requirement §6は見出しを「承認のうえ反映済み」とし、引用を「改訂前」「改訂後(現行)」に分けた。旧UV-032を現行規範と誤読する状態は解消した。
- implement 20〜24行、36行以降のFinding対応、91行以降の旧コメント説明は、日付・review番号・「当初」「誤りだった」「訂正」により時系列の履歴として読める。
- implement 137〜144行の未確認事項は「開発側で確かめられなかったこと」として自己検証時点の境界を記録しており、現行Policyや承認状態の主張ではない。Testerへ残す事項も具体的に列挙されている。
- status 61行のnon-apt自動適用はNext候補であり、現行は人の`dry_run=false`明示、Policyへ現在形で書けばUV-035〜038と矛盾すること、実現時はPolicyと実装を同時改訂することを明記する。将来案を現行規範として扱っていない。

## Withdrawn-design Sweep

| 撤回済み設計 | 結果 |
|---|---|
| 全体最新との2版併記 | 現行要求・通知仕様には無し。implementでは撤回・削除の履歴としてのみ登場 |
| non-apt結果によるStatus昇格 | 現行R5 / AC6 / UV-032 / 実装記録はいずれも昇格しない。旧文は問題定義と改訂前引用だけ |
| weekly non-aptを機械が適用 | 現行§3 / §8 / UV-086 / statusはいずれも人の明示適用。statusの機械適用は将来候補として分離 |
| 系統外版の絶対禁止 | R4 / AC3 / UV-088はいずれもinstalled-version mismatchの例外を持つ。implementも上流候補版とinstalled版を区別 |

## Immutability Check

`_007_review.md`時点と今回のSHA-256は同一。

| File | SHA-256 prefix |
|---|---|
| `roles/prometheus_update_check/defaults/main.yml` | `1c00b730...` |
| `roles/prometheus_update_check/tasks/main.yml` | `25fc2e88...` |
| `roles/prometheus_update_check/tasks/notify.yml` | `86215d0a...` |
| `roles/prometheus_update_check/tasks/upgrade.yml` | `446521a2...` |
| `roles/ubuntu_vm_full_upgrade/tasks/classify.yml` | `b18f852b...` |
| `playbooks/ubuntu_vm_full_upgrade.yml` | `666cefd4...` |
| `docs/ai/policies/ubuntu_vm_patch_policy.md` | `9a9caf6a...` |

コードとPolicyは変更されていない。PolicyのUV識別子も定義コメント単位で重複なし。

## Verification

- 指定4文書を全文確認。
- 現在状態語、撤回済み設計語、weekly/monthly、apt/non-apt、検知/提示/採否/適用、Status、系統外版を横断検索し、各出現箇所の時制と節を確認。
- requirement §4 / §5 / §6 / §8とPolicy UV-018 / UV-032〜038 / UV-085〜094を相互照合。
- `git diff --check`: rc=0。
- コード6ファイルとPolicyのSHA-256を `_007_review.md` の記録と照合。
- `docs/ai/memory/`は依頼どおり対象外・未読。

## Verdict

**Approve** — 文書間の現行境界は一致し、撤回済み設計は履歴として識別可能な箇所にのみ残る。コードとPolicyも前回から不変。Testerへ渡してAC1 / AC2 / AC4 / AC5のfixture検証へ進めてよい。
