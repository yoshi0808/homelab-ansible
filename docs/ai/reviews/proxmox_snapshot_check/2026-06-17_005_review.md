# 再レビュー結果: proxmox_snapshot_check（003 review 指摘反映後）

対象:
- `docs/ai/reviews/proxmox_snapshot_check/2026-06-17_001_requirement.md`
- `docs/ai/reviews/proxmox_snapshot_check/2026-06-17_003_review.md`
- `docs/ai/reviews/proxmox_snapshot_check/2026-06-17_004_implement.md`
- `playbooks/proxmox_snapshot_check.yml`
- `roles/proxmox_snapshot_check/*`

## 結論

指摘事項はありません。

003 review の must-fix / suggestion はどちらも反映されています。このまま commit して問題ありません。

## 前回指摘の確認

### 1. must-fix: `pvesh` 失敗時に stale なしとしてサイレント成功する

解消済みです。

`roles/proxmox_snapshot_check/files/proxmox-snapshot-collect.sh` は `pvesh_json()` の失敗を
`data=None, error=<message>` として扱い、出力 JSON に `collection_ok` と `errors` を含める
形に修正されています。`/cluster/resources` の取得失敗は `collection_ok: false`、個別ゲストの
snapshot 取得失敗は `errors` に記録されます。

`roles/proxmox_snapshot_check/tasks/main.yml` 側では、`collection_ok == false` または
`errors | length > 0` を WARNING 条件に含めています。Slack 通知も `result.status == 'WARNING'`
で発火するため、収集不能が false OK になる問題は解消されています。

shell は収集結果とエラー情報の JSON 整形に留まり、WARNING 判定と通知制御は Ansible tasks 側に
残っているため、core.md §7 の責務分離にも沿っています。

### 2. suggestion: 7日ちょうどの境界が「以上」ではなく「より古い」になっている

解消済みです。

stale 抽出が `selectattr('snaptime', 'le', _snap_threshold_epoch | int)` に修正されており、
要求仕様の「7日（604800秒）以上前」と一致しています。

## 確認したこと

- read-only 方針は維持されています。スナップショット削除・変更操作はありません。
- 収集、JSON 整形、判定、通知制御の責務分離は要求仕様と core.md §7 に沿っています。
- `current` pseudo-entry の除外は構造的な除外として維持されています。
- IP リテラルの混入は確認できませんでした。
- `ansible-playbook --syntax-check playbooks/proxmox_snapshot_check.yml` は成功しました。

## 補足

対象ファイルが untracked のため `git diff -- playbooks/proxmox_snapshot_check.yml roles/proxmox_snapshot_check`
は空でした。レビューでは実ファイルの内容を直接確認しています。

構文チェックは sandbox の都合で Ansible のローカル tmp を `/tmp` に向けて実行しました。

```text
env ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote ansible-playbook --syntax-check playbooks/proxmox_snapshot_check.yml
```

Next step files:
- `docs/ai/reviews/proxmox_snapshot_check/2026-06-17_001_requirement.md`
- `docs/ai/reviews/proxmox_snapshot_check/2026-06-17_004_implement.md`
- `docs/ai/reviews/proxmox_snapshot_check/2026-06-17_005_review.md`
