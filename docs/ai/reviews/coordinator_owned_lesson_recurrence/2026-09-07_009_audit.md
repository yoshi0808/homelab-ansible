# Audit: lessons再発記録をCoordinator管理へ移す

監査: 2026-09-07 / Auditor(Claude Code、identity `auditor_lesson`)
対象: `docs/ai/reviews/coordinator_owned_lesson_recurrence/`(001〜008)と、そこから参照されている成果物
返却先: Coordinator

## 0. 監査自身の規範違反(先に申告する)

**`docs/ai/memory/`配下を読んだ。** `docs/ai/core.md:86`「Knowledge(`docs/ai/memory/`)を読むのはCoordinatorだけである。subagentは読まない」に反する。Auditorはsubagentであり、`docs/ai/roles/auditor.md`の必須Contextにも`docs/ai/memory/`は無い。

- 読んだもの: `docs/ai/memory/lessons/`の`## 再発記録`節(12本のポインタ文と表)、`docs/ai/memory/knowledge-review-log.md`のdiff、`docs/ai/memory/decisions/`へのgrepヒット行。
- 経緯: AC4の実体(12本という本数、30行という既存行数、置換文言)を案件記録どおりか確かめようとして、境界を確認する前にgrep/diffへ入った。
- 影響: 下記 §3 の「参考(境界外)」に置いた内容は、**本来Auditorが持ってはいけない入力**である。Coordinatorはこれを監査の成果として扱わず、自分で確認したうえで採否を決めること。
- **本監査は`docs/ai/memory/`を変更していない。**(read-onlyのgrep/diff/sedのみ。`git status`上、案件開始時からlessonsのstaged/unstaged状態に変化なし)

この事象は`docs/ai/memory-classification.md`「`lessons/`の「再発記録」節」の判定条件3(規範文書に書いてあることをしなかった)に当たる。**記録の採否はCoordinatorの責任**であり、Auditorは判定しない。本件は皮肉にも、この案件が新設した契機「subagentの成果物を統合するとき」の最初の適用対象になる。

## 1. 再構成できたか

**できた。** 001→008を順に読むだけで、次を自力で再構成できる。

1. 問題(Roleを区別しないSessionEnd hookがsubagent終了時に共有worktreeへ差分を残す) → 2. ゴールと非ゴール → 3. P0 7件のrequirementとAC 6件 → 4. plan 4 step → 5. 実装 → 6. 初回レビューRequest Changes(Major 1件: 記録契機がplanより狭い) → 7. 反映 → 8. 再レビューApprove(non-blocking 3件) → 9. AC全PASS → 10. closeout。

**詰まった箇所は1つ**(§2 指摘1)。それ以外は、判断の根拠・却下の理由・未検証の所在がいずれも記録側に書かれており、説明を要しなかった。

### 記録と現物の突合(Auditorが自分で確認したもの)

| 記録の主張 | 確認手段 | 結果 |
|---|---|---|
| `.claude/settings.json`のhook keyは`SessionStart`のみ4件、top-level`env`は消えた | `python3 -c "json.load(...)"` でkey列挙 | 一致(keys=`permissions`/`hooks`/`autoMode`、SessionStart 4) |
| `scripts/session-recurrence-record.py`は削除済み | `ls` | 存在しない(`git status`で`D`) |
| 撤回語・script名・timeout名のlive残存0 | `docs/ skills/ scripts/ .claude/ .codex/ AGENTS.md CLAUDE.md`をgrep(`docs/ai/reviews/`除外) | 0件 |
| CodexのSessionEndはlessonsへ書かない | `.codex/hooks.json` | agmsg `session-end.sh` のみ |
| 責任主体は`memory-classification.md`と`coordinator.md`の2箇所に一意 | 「再発記録」の全live参照をgrep | 規定しているのはこの2文書のみ。後者は前者を正本として指すポインタ |
| lesson 12本が1挿入1削除、既存表行の増減なし | `git diff --cached --numstat` | 12本すべて1/1。表行の追加・削除は差分に無い |
| doc consistency rc=0 | `python3 scripts/check-doc-consistency.py` | rc=0 |
| 参照先の実在(`norm_docs_audit/2026-08-25_003_findings_skills.md`、`coordinator_platform_migration/`、`scripts/session-context.py`、`role-context-matrix.md`、`skills/document-norm-review`) | `ls` | すべて実在 |
| AC6「対象外の既存差分はcommit候補に含まれない」 | `git status --short` | staged 25件はすべて本案件path。`node_modules/`・`package.json`・`package-lock.json`・`.codex/rules/default.rules.bak-...`はuntrackedのまま |

## 2. 指摘

| # | 分類 | 指摘 | クローズを妨げるか |
|---|---|---|---|
| 1 | 記録どうしの矛盾 | **`2026-09-07_003_implement.md`の`## 未検証`節に、検証済みの事実が3件並んでいる。**「lesson全19本のSHA-256とmtimeが完全一致した」「独立ReviewerはApprove」「全文書整合性、staged diff check、pre-commit検査はすべて成功した」はいずれも完了形で書かれ、`2026-09-07_007_test_result.md`でAC2・AC6のPASS根拠として使われている。見出しどおりに読むとこの3件は未検証となり、007と正面から食い違う。3か月後の読み手はどちらが正しいか判定できない | **妨げる** |
| 2 | 決定の追跡 | **稼働中の機構を撤去するという決定の起点と承認が、案件記録のどこにも無い。** 001 §7が「今回の未commit行だけを採用しない方針はYoshinobu確認済み」と書くのは**未commit1行の採否**についてであり、**hookとscriptそのものの撤去**をいつ誰が決めたかは書かれていない。旧機構は2026-08-25に計測開始された(`knowledge-review-log.md`に節がある)ため、これは既存の合意を取り消す決定にあたる。`docs/ai/roles/coordinator.md`の「着手前に計画を出し、合意してから着手する」に対して、その合意の所在が記録から辿れない | **妨げる** |
| 3 | 根拠の実在 | **AC4のうち「未commitの2026-09-06行が消えたこと」の証拠が記録に無い。** 007はAC4のPASS根拠を「`## 再発記録`節は12本、正本ポインタも12本、commit済み表行は30行でソート後SHA-256も一致」と書くが、これはすべて**残ったものの同一性**であり、**消したものの同一性**ではない。当該行はHEADに存在しないためgitからは検証できず、変更前の作業ツリーの写しも記録されていない。001 §1が「Reviewerの終了に由来する再発記録1行が未commit差分として残っていた」と述べる以上の裏付けは無い | 妨げない(いま再現不能。ACの表現を実際に確認できた範囲へ狭めるのが正しい直し) |
| 4 | 根拠の実在 | **`2026-09-07_006_review.md`が引く行番号が現物とずれている。** `memory-classification.md:50`は現在51行目、同`:58`は61行目。指摘後の編集で位置が動いたためで、内容は一致している | 妨げない |
| 5 | 未解決の明示 | **`2026-09-07_008_closeout.md`の`## Auditor`が「未実施」のまま。**「状態: Auditor確認待ち」も同様 | 妨げない(監査の実施そのものが生む記帳の遅れ) |

### 指摘にしないもの

- `docs/ai/status.md:28`の進行中行が「統合時に記録する形へ変更中」と旧い(2契機のうち1つしか書いていない)こと、および「実装後の新規Claude Reviewer終了時に…確認する」が充足済みであること。**`006`のSuggestion #3として既に挙がり、`005`で「クローズ時に削除する」と決まっている。**Auditorは該当行を消す前に起動するため、残存自体は指摘にしない(下記 §4 の実施事項)。

## 3. 状態の正本との突合

- `docs/ai/status.md`の本案件行(28行目)は現況と食い違う(上記のとおり、指摘ではなく実施事項)。
- **新たに生じた観測待ちは無い。** 本案件は自動機構を撤去する変更であり、稼働を待つものが無い。AC2は既に実測済みで、`005`と`007`が結果を持つ。
- 同ファイルの他の現在地(Codex移行、sandbox自動パッチ、ansy自己対象playbook)は本案件の変更で古くなっていない。本案件はClaude Codeの`SessionEnd`だけを触っており、`SessionStart` 4本・permissions・autoModeは逐語不変であることを確認した。

### 参考(境界外 — §0のとおりAuditorが持ってはいけない入力)

`docs/ai/memory/decisions/`の2文書が、**この案件が撤去した機構を現在形で前提にしている**可能性がある。`docs/ai/roles/auditor.md`の「差分の外側の陳腐化」が本来見る対象(Policy / ADR / Context / status / Incident)には含まれず、Knowledge境界により**Auditorが判定すべきものでもない**ため、確認事項としてのみ渡す。Coordinatorが現物を読んで採否を決めること。

- `improve-prompts-by-deleting-not-adding.md` — 再発記録を「削除の根拠」を支える**計測手段**として位置づけている。本案件は行数の意味を「確認できた事例数」へ定義し直した。
- `rejected-proposals.md` — 見送りの理由と再開条件が「分類器は同じlessonへ1回だけ割り当てる」という**撤去済みの実装挙動**を根拠にしている。

## 4. 実施事項(指摘ではない。Coordinatorが同じセッションで行う)

1. `docs/ai/status.md:28`の本案件の進行中行を削除する。
2. `2026-09-07_008_closeout.md`の「状態」と「## Auditor」を本監査の結果で更新する(指摘5)。

## 5. 判定

**条件付き受入**

指摘1と指摘2を反映すればクローズしてよい。**再監査は前提としない。**

- 指摘1: `003`の`## 未検証`を実態に合う見出しへ直す(3件はいずれも検証済みであり、`007`が根拠を持つ)。
- 指摘2: 撤去の決定の起点と承認の所在を、`001`または`008`へ1〜2文で記録する。

指摘3〜5および §3「参考」は、クローズを妨げない。指摘3はACの表現を確認できた範囲へ揃えるのが正しい直しであり、行を復元して検証し直すことではない。
