# Review: lessons再発記録をCoordinator管理へ移す

レビュー: 2026-09-07 / Reviewer(Claude Code、identity `reviewer_lesson`)
対象: `2026-09-07_001_requirement.md`、`2026-09-07_002_plan.md`、`2026-09-07_003_implement.md`、および現在の未commit diff

## Code Review: lessons再発記録のCoordinator移管

### Summary

機構の撤去(`SessionEnd` hookと分類script)は完全で、`SessionStart` 4本は逐語一致で保存されている。旧script名・旧timeout名・旧責任主体の文言はlive範囲に残っていない。ただし**記録が起きる契機がplanから狭まっており**、subagentを起動しないCoordinatorセッションでの再発が、どの規範からも記録対象にならない状態になっている(Major、blocking)。

### Critical Issues

| # | File | Line | Issue | Severity |
|---|---|---|---|---|
| 1 | `docs/ai/memory-classification.md` / `docs/ai/roles/coordinator.md` | 50 / 102 | **記録の契機が「統合するとき」だけになり、subagentを起動しないセッションが契機を持たない。** 撤去した機構は`SessionEnd`(`/clear`を含む)で毎セッション発火し、Coordinator単独セッションも対象だった。新しい規範は両文書とも「自分の実行結果とsubagentの成果物を統合するとき」と書いており、統合する成果物が無いセッションには当たらない。requirement §5リスクレジスタの緩和策は「**closeout・統合時**のCoordinator責任として明文化し」、plan Step 2は「統合時**および自分の作業終了前に**」と書いていたが、実装ではこの第2の契機が落ちている。結果としてgoal 2(採否と追記責任の一本化)は満たすが、旧機構が持っていた適用範囲を回復していない | Major |

対応案(Coordinatorが決める): 両文書の契機を「統合するとき、および統合が発生しないセッションでは作業を終える前に」の形へ揃える。`docs/ai/roles/coordinator.md`「`docs/ai/status.md` の維持」が既に「終わる前に」を契機として使っており、同じ粒度で書ける。

### Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| 1 | `2026-09-07_001_requirement.md` | 83 (AC1) | AC1の検査対象が`.claude/settings.json`1本に閉じているが、goal 1(subagent終了が`docs/ai/memory/`を変更しない)は設定scopeが1つであることに依存する。**本レビューで`~/.claude/settings.json`、`~/.claude/settings.local.json`、`.claude/settings.local.json`の3scopeを実測し、再発記録hookはどこにも無いことを確認した**(残る`SessionEnd`はagmsgの`session-end.sh`のみで、lessonsへ書かない)。ACの参照先を「有効な全settings scope」へ広げると、この確認が次回も再現できる | Minor |
| 2 | `docs/ai/memory-classification.md` | 52-56 | 判定条件2が名指しする安全機構(permission classifier / `permissions.deny` / `autoMode`)はClaude Code固有であり、最終行の除外も「Claude CodeのSessionEnd hook」を名指ししている。**Coordinatorのプラットフォーム移行(`docs/ai/reviews/coordinator_platform_migration/`)が進むと、書き手がCodexのときに条件2が指す機構が存在しない。** 本diffが導入した問題ではなく、責任主体をCoordinatorへ寄せたことで新たに露出した。「harnessの安全機構」を製品非依存の語で書けるかは別案件で判断するのが妥当 | Suggestion |
| 3 | `docs/ai/memory-classification.md` | 121 | 月次振り返りは「まず前回の節の**次月に見るもの**を開いて突き合わせる」ところから始まる。旧機構のログ(repo外`~/.claude/`)とその件数が`docs/ai/memory/knowledge-review-log.md`の前月節で次月の測定対象に指定されている場合、次回(2026-09-26)にたどれない指示が残る。**この確認はKnowledge境界により本レビューの範囲外**(未確認、下記参照) | Minor |
| 4 | (作業ツリー) | — | AC6「対象外の既存差分はcommit候補に含まれない」に対し、`node_modules/`・`package.json`・`package-lock.json`・`.codex/rules/default.rules.bak-before-coordinator-monitor-20260906` がいずれも未追跡かつ`.gitignore`対象外である。`git add -A`相当を使うと混入する。stageは明示pathで行う | Minor |

### What Looks Good

**規範の消失は無い(document-norm-review 欠陥クラス2)。** 旧契約の各項目を逐行で新旧照合した。「話題が似ていることは記録の理由にならない」「調べた・検証した・見つけたは記録しない」「反した規範の所在を書けない項目は記録しない」「回数だけを昇格の根拠にしない」、および3分類(P1-1)はすべて新文へ到達している。落ちたのは旧機構の実装詳細を述べる語(「機械が落とす」「分類器はLLMであり」)だけで、規範としての内容は保存されている。節の内容規定は「見出しと表だけ」→「見出し、正本へのポインタ、表だけ」へ変わったが、これは現物のlessonファイルおよび撤去した`docs/ai/status.md`の記述と一致する方向の是正である。

**旧機構・旧根拠のlive残存は0件(欠陥クラス3)。** `session-recurrence-record` / `session_recurrence_record`、`SESSIONEND` / `SessionEnd`、「機械だけが書く」「機械が追記」「人は手で書かない」「機械が落とす」を`docs/` `skills/` `roles/` `playbooks/` `scripts/` `.claude/` `.codex/` `AGENTS.md` `CLAUDE.md` 横断でgrepした。liveなヒットは(a)本案件フォルダ、(b)`docs/ai/reviews/norm_docs_audit/2026-08-25_003_findings_skills.md`(過去review、AC3の除外対象)、(c)`docs/ai/memory-classification.md`の**禁止文**と`docs/ai/status.md`の**進行中記述**の3種のみ。`roles/` `playbooks/` `scripts/`の「再発」ヒットはすべて無関係な用語(証明書再発行、再発防止のコメント)。

**AC1充足。** `.claude/settings.json`はJSONとして解析でき、hook keyは`SessionStart`のみ・4エントリ。HEADとの構造比較で`SessionStart`配列は**逐語一致**(順序・内容とも不変)、除去された top-level key は`env`のみ(中身は`CLAUDE_CODE_SESSIONEND_HOOKS_TIMEOUT_MS` 1件のみだった)、`permissions`と`autoMode`は不変。`scripts/session-recurrence-record.py`は削除済み、コンパイル済み`.pyc`は`.gitignore`の`__pycache__/`配下でrepo内容ではない。

**SessionStart側の知識は失われていない。** `docs/ai/status.md`から削除した節には、`SessionStart`が4エントリに分かれている理由(hook出力の10,000文字制限、`scripts/session-context.py`による節境界分割)が含まれていた。これは非ゴールで維持すると宣言した機構の説明だが、**同じ事実が`scripts/session-context.py`のdocstringと定数コメントに書かれている**ため、宙ぶらりんにも消失にもなっていない(`docs/ai/core.md`「影響先のコードに書ける → そこへ書く」に沿う)。

**責任主体は一意(AC5)。** live範囲で`## 再発記録`の書き手を規定するのは`docs/ai/memory-classification.md`(正本)と`docs/ai/roles/coordinator.md`(正本を指すポインタ)の2箇所だけで、後者は判定・形式の正本を前者へ委ねており値を二重に持っていない。`docs/ai/core.md`「Knowledgeを読むのはCoordinatorだけ」と`docs/ai/role-context-matrix.md`は未変更で、subagent側にこの責務を読める記述は無い。追加された1行の置き場(`docs/ai/roles/coordinator.md`「Coordinator固有の作法」)も、正本を別に持つ作法という節の性格と一致する。

**機械検査。** `python3 scripts/check-doc-consistency.py` → rc=0(check1 120件 / check2 8件 / check3 112件、いずれもOK)。

### Verdict

**Request Changes**(Critical Issues #1 がblocking)

## 確認範囲

- 入力3文書、`docs/ai/core.md`、`docs/ai/roles/reviewer.md`、`skills/code-review`、`skills/duplication-reuse-check`、`skills/ansible-security-review`、`skills/document-norm-review`。
- `git status --short` / `git diff` / `git diff --numstat`(全変更ファイル)、`.claude/settings.json`のHEADとの構造比較。
- 上記grepによる撤回語・script名・timeout名の横断掃引。
- 有効な全settings scope(user / user local / project local)のhook列挙。
- `scripts/check-doc-consistency.py`の実行。

## 未確認事項

- **`docs/ai/memory/`配下の内容**(lesson 12本の置換文言、既存表行の保存、`knowledge-review-log.md`の次月測定指示)。**依頼文でKnowledge境界として読むことを禁じられたため読んでいない。** `git diff --numstat`で12本すべてが1挿入1削除であることだけは確認しており、定型1行の置換と、未commitの2026-09-06行がHEADに存在しないため差分に現れないという説明とは矛盾しない。AC4の実体判定はCoordinatorが行う。
- **`## 再発記録`節を持つlessonが12本で全部かどうか。** 同じ理由でファイル一覧の照合をしていない。13本目があれば旧文言が残る。
- **AC2(新規Claude Reviewerセッション終了時にlessonsが変わらない)。** 本セッションがその実測対象であり、Reviewer自身では判定できない。**本レビューで`docs/ai/memory/`配下および対象実装を一切変更していない**ことは申告できる。書き出したのは本ファイル1本だけである。
- **AC6のpre-commit検査。** stagedファイルが0件のため実行しても空回りする。commit直前にCoordinatorが行う。
- 実ホストへは一切到達していない。残置プロセスは無い(本セッションのagmsg watcherを除く)。

## 対象実装への変更

無し。本ファイル以外にリポジトリ内ファイルを変更していない。
