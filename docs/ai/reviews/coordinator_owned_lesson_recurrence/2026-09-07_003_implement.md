# Implement: lessons再発記録をCoordinator管理へ移す

実装: 2026-09-07 / Coordinator
対象: `2026-09-07_001_requirement.md`、`2026-09-07_002_plan.md`

## 変更内容

- `.claude/settings.json`から`SessionEnd` hookと専用timeout環境変数を撤去した。
  `SessionStart` 4本とpermission / autoModeは変更していない。
- `scripts/session-recurrence-record.py`を削除した。
- `docs/ai/memory-classification.md`の再発記録責任をCoordinatorへ移し、subagentの
  報告は自動採用せず現物確認する契約にした。既存3分類は維持した。
- `docs/ai/roles/coordinator.md`に、統合時の再発判定と該当時の記録責任を追加した。
- 再発記録節を持つlesson 12本の定型文を新しい正本へのポインタへ更新した。
  commit済みの表行は維持し、旧hookが残した未commitの2026-09-06行だけを除いた。
- `docs/ai/status.md`から旧hookの観測待ちを除き、本案件の進行中状態を記録した。
- 過去のreviewsと`docs/ai/memory/knowledge-review-log.md`の測定値は履歴として
  変更していない。後者の将来向け測定指示だけは下記のとおり撤回注記を加えた。

Reviewer指摘の反映:

- Major #1を受け、記録契機を「各対話セッションを終える前」と「subagent成果物の
  統合時」の2つに揃えた。Coordinator単独セッションも対象になる。
- Minor #1を受け、AC1を有効なClaude settings全scopeの確認へ広げた。
- Suggestion #2を同一scopeで解消し、安全機構の例を特定製品に依存しない語へ改めた。
- Minor #3を確認したところ、月次記録の「次月に見るもの」に旧hookの測定指示が
  残っていた。過去の測定値は維持し、その指示へ2026-09-07の撤回注記を加えた。

## Coordinator自己検証

- `.claude/settings.json`はJSONとして解析でき、hook keyは`SessionStart`だけ、件数は4。
- top-levelの専用`env` keyは消えている。
- `scripts/session-recurrence-record.py`は存在しない。
- live範囲からscript名、専用timeout名、旧「機械が追記」「人は手で書かない」の
  文言が消えている。過去review・時点付き測定記録は検索対象から除外した。

## 検証結果

- 新規Claude Reviewer終了前後でlesson全19本のSHA-256とmtimeが完全一致した。
- 独立Reviewerはblocking解消後にApprove。規範消失・旧根拠残存はなく、責任主体は
  Coordinatorへ一意に割り当てられている。
- 全文書整合性、staged diff check、pre-commit検査はすべて成功した。
