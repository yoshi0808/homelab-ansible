# Re-review: proxmox_restore_vm_placement HA VM relocate 追加

対象:

- `roles/proxmox_restore_vm_placement/tasks/main.yml`
- `playbooks/proxmox_restore_vm_placement.yml`
- `docs/ai/prompts/proxmox_patch_policy.md`
- 現在の git diff

参照:

- `docs/ai/prompts/core.md`
- `docs/ai/reviews/proxmox_restore_vm_placement/2026-06-13_001_requirement.md`
- `docs/ai/reviews/proxmox_restore_vm_placement/2026-06-13_006_implement.md`
- `docs/ai/prompts/proxmox_patch_policy.md`

## Findings

追加指摘はありません。

`2026-06-13_005_review.md` の Low Finding は解消されている。

- Mode A の pve2 restore 手順に `LRM待機 → HA relocate → HA復帰待機` が追記されている
- 省略されていた pve1 restore 手順も pve2 と対称な内容へ更新されている
- `17.1 Mode A 標準手順` の pve1 / pve2 restore も同じフローへ更新されている
- role の Phase 3 見出しは、maintenance disable と HA relocate の両方を示す表現へ更新されている

## 実装確認

全差分は要求仕様と整合している。

- maintenance mode を disable し、LRM が active / idle になるまで待機する
- `hacritical` + `prefer<target>` を持ち target node 以外にいる QEMU / LXC を抽出する
- 各対象へ `ha-manager crm-command relocate vm:<vmid> <target_node>` を発行する
- 既存ポーリングで target node への配置と `running` 状態を確認する
- 運用ポリシー、playbook コメント、role コメントが実際のフローと一致している

秘密情報、runtime report、無関係な変更は差分に含まれていない。

## 確認結果

```text
git diff --check
=> OK
```

```text
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local \
ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote \
ansible-playbook -i inventories/homelab/hosts.yml \
  playbooks/proxmox_restore_vm_placement.yml \
  --syntax-check -e target_node=pve2
=> OK
```

`ansible-lint` と `yamllint` は環境に存在しないため未実施。

## 判定

レビュー上の追加修正は不要。この内容で commit してよい。

## 残リスク

- 実機で maintenance disable 後に複数の HA VM / CT へ relocate を連続発行する動作確認は未実施。
- `_ha_vms_to_relocate` は Phase 1 で取得した `_restore_all_resources` を使う。取得後に配置やタグが外部変更された場合は最新状態を反映しないが、要求仕様で認識済みかつ許容された設計判断である。

---

Next step files:

- `docs/ai/reviews/proxmox_restore_vm_placement/2026-06-13_001_requirement.md`
- `docs/ai/reviews/proxmox_restore_vm_placement/2026-06-13_006_implement.md`
- `docs/ai/reviews/proxmox_restore_vm_placement/2026-06-13_007_review.md`（本ファイル）
