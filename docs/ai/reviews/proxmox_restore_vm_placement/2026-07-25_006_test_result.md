# Test Result: proxmox_restore_vm_placement external reachability gate

## 1. 判定

**BLOCKED**

Exact Actual `--check` commandはexec commandのCreateProcess前approvalで拒否され、
Ansibleプロセスは開始しなかった。承認された停止条件に従い、再試行、pve1への切替、
ad-hoc確認、通常実行は行っていない。

## 2. Scope

- identity / Role: `tester2` / Tester
- 対象候補: `pve2`のみ
- tester-gate: `check-mode-native`
- 通常実行 / APPLY: 0
- Actual wrapper試行: 1回
- Actual Ansible起動: 0回

Actual前のscope確認では、承認時から次の対象に変化はなかった。

- `roles/proxmox_restore_vm_placement/defaults/main.yml`
- `roles/proxmox_restore_vm_placement/tasks/main.yml`
- `roles/proxmox_restore_vm_placement/files/`
- `docs/ai/reviews/proxmox_restore_vm_placement/2026-07-25_001_requirement.md`
- `docs/ai/reviews/proxmox_restore_vm_placement/2026-07-25_002_investigation.md`
- `docs/ai/reviews/proxmox_restore_vm_placement/2026-07-25_003_implement.md`
- `docs/ai/reviews/proxmox_restore_vm_placement/2026-07-25_004_review.md`
- `docs/ai/reviews/proxmox_restore_vm_placement/2026-07-25_005_test_plan.md`

## 3. Static results

### tester-gate lint

Command:

```bash
scripts/check-tester-gate.sh
```

- rc: `0`
- result: `PASS`
- sanitized output: `[tester-gate-lint] OK (37 playbooks)`

### syntax-check

Command:

```bash
ansible-playbook -i inventories/homelab/hosts.yml \
  playbooks/proxmox_restore_vm_placement.yml \
  -e target_node=pve2 \
  --syntax-check
```

- rc: なし
- result: `NOT RUN`
- reason: CreateProcess前approval rejection
- 再試行: なし
- 代替静的根拠:
  - `2026-07-25_003_implement.md`記録のsyntax-check PASS
  - `2026-07-25_004_review.md`のApproveと独立静的確認

### diff check

Command:

```bash
git diff --check
```

- rc: `0`
- result: `PASS`
- output: なし
- scope: 承認時から不変

## 4. Actual check

承認されたexact command:

```bash
scripts/safe-ansible-check.sh \
  playbooks/proxmox_restore_vm_placement.yml \
  -i inventories/homelab/hosts.yml \
  -e target_node=pve2 \
  --check
```

結果:

- wrapper command試行: 1回
- process start: なし
- rc: なし
- Ansible recap: なし
- pve2接続: なし
- task実行 / skip記録: なし
- controller report: 生成なし
- controller filesystem write: 0
- Ansible transport一時処理: 0
- recovery mute action: 0
- recovery action: 0
- helper copy / run / remove: 0 / 0 / 0
- external HTTP attempt: 0
- healthcheck: 0
- migration / maintenance / relocate / start: 0 / 0 / 0 / 0
- 通知: 0

Tool rejection output全文（秘密情報なし）:

```text
Script error:
exec_command failed for `/bin/bash -lc 'scripts/safe-ansible-check.sh playbooks/proxmox_restore_vm_placement.yml -i inventories/homelab/hosts.yml -e target_node=pve2 --check'`: CreateProcess { message: "Rejected(\"rejected by user\")" }
```

拒否はcommand approval / CreateProcess前であり、sandbox内process、wrapper、Ansible、
SSH、networkの各段階には到達していない。

## 5. Report合格条件

Actualが開始されずcontroller reportも生成されなかったため、次はすべて未検証である。

- `result == "PLAN_ONLY"`
- `external_reachability_status == "NOT_RUN"`
- `external_probe_attempts == 0`
- `external_probe_elapsed_seconds == 0`
- `maintenance_disabled == false`
- `vms_migrated_back == []`
- `vms_started == []`
- `post_healthcheck_status == "UNKNOWN"`
- `error_msg == ""`

## 6. 未検証事項と残存リスク

- pve2の到達性、facts収集、read-only `pvesh`実行。
- VM / CT識別だけがcheck modeで実行されること。
- 親not-check destructive blockにより、新gate、helper lifecycle、外部HTTP、healthcheck、
  migration、maintenance、relocate、startがすべてskipされること。
- controller reportが`PLAN_ONLY`と期待するsanitized fieldsを持つこと。
- recovery action 0と通知skipをActual Ansible logで確認すること。
- Ansible recapとtask単位のok / skipped件数。

静的構造上は005 test planおよび004 reviewの根拠と整合するが、Actualの実測品質は
保証できていない。再実行には新たなTech Lead承認とcommand approvalが必要である。

## 7. 秘密・runtime識別情報・成果物

- guest name、ID、配置、内部address、秘密情報の取得・転記: 0
- runtime report: 未生成
- helper artifact: 未生成
- cleanup対象: なし
- 本ファイル以外の追加編集: なし
