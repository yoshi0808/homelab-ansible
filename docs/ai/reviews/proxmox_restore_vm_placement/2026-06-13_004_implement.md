# 追加実装報告: proxmox_patch_policy.md / playbook ヘッダーコメント 更新

日付: 2026-06-13
参照:

- `docs/ai/reviews/proxmox_restore_vm_placement/2026-06-13_001_requirement.md`
- `docs/ai/reviews/proxmox_restore_vm_placement/2026-06-13_003_review.md`

---

## 対応した Finding

### Medium: 運用ポリシーと playbook の処理説明が旧挙動のまま

`003_review.md` が指摘した箇所:

| ファイル | 箇所 | 問題 |
| --- | --- | --- |
| `docs/ai/prompts/proxmox_patch_policy.md:243` | タグ分類表の restore 動作列 | 「maintenance mode disable により HA が自動復帰」と記載 |
| `docs/ai/prompts/proxmox_patch_policy.md:250` | restore ロール説明文 | 「HA が自動復帰させる」と記載 |
| `docs/ai/prompts/proxmox_patch_policy.md:812-814` | 6.7 処理概要 | maintenance disable → HA 復帰待機の間に relocate 発行ステップが欠落 |
| `playbooks/proxmox_restore_vm_placement.yml:14` | Steps コメント | 「HA restores hacritical VMs automatically」と記載 |

---

## 作成・更新したファイル一覧

| 操作 | ファイル |
| --- | --- |
| 更新 | `docs/ai/prompts/proxmox_patch_policy.md` |
| 更新 | `playbooks/proxmox_restore_vm_placement.yml` |
| 新規 | `docs/ai/reviews/proxmox_restore_vm_placement/2026-06-13_004_implement.md`（本ファイル） |

---

## 変更内容の詳細

### 1. `docs/ai/prompts/proxmox_patch_policy.md` — タグ分類表（旧 line 243）

**変更前:**

```text
| `hacritical` あり | HA管理 VM/CT（タグが印） | maintenance mode enable により HA が自動退避 | maintenance mode disable により HA が自動復帰 |
```

**変更後:**

```text
| `hacritical` あり | HA管理 VM/CT（タグが印） | maintenance mode enable により HA が自動退避 | maintenance mode disable 後、`ha-manager crm-command relocate` を明示的に発行して復帰 |
```

---

### 2. `docs/ai/prompts/proxmox_patch_policy.md` — restore ロール説明文（旧 line 250）

**変更前:**

```text
restore ロールは、non-HA VM/CT を手動 migrate で home node に戻した後、`ha-manager crm-command node-maintenance disable <node>` で maintenance mode を無効化し、HA が `hacritical` + `prefer<target>` を持つ VM/CT を自動復帰させる。HA VM/CT が home node に戻り running になるまで待機する。
```

**変更後:**

```text
restore ロールは、non-HA VM/CT を手動 migrate で home node に戻した後、`ha-manager crm-command node-maintenance disable <node>` で maintenance mode を無効化する。Proxmox HA はメンテナンス解除後に稼働中 VM/CT を自動リロケートしないため、`hacritical` + `prefer<target>` を持ち target node 以外にいる各 VM/CT に対して明示的に `ha-manager crm-command relocate vm:<vmid> <target_node>` を発行する。その後、HA VM/CT が home node に戻り running になるまで待機する。
```

---

### 3. `docs/ai/prompts/proxmox_patch_policy.md` — 6.7 処理概要（旧 lines 812-814）

**変更前:**

```text
6. `ha-manager crm-command node-maintenance disable <target_node>` で maintenance mode を無効化する
7. maintenance mode が inactive になるまで待機する（`ha-manager status` でポーリング）
8. `hacritical` + `prefer<target_node>` を持つ HA VM / CT が target_node に戻り running になるまで待機する
```

**変更後:**

```text
6. `ha-manager crm-command node-maintenance disable <target_node>` で maintenance mode を無効化する
7. maintenance mode が inactive になるまで待機する（`ha-manager status` でポーリング）
8. `hacritical` + `prefer<target_node>` を持ち target_node 以外にいる HA VM / CT を特定する
9. 各 HA VM / CT に `ha-manager crm-command relocate vm:<vmid> <target_node>` を発行する
10. `hacritical` + `prefer<target_node>` を持つ HA VM / CT が target_node に戻り running になるまで待機する
```

---

### 4. `playbooks/proxmox_restore_vm_placement.yml` — Steps コメント（line 14）

**変更前:**

```yaml
#   4. Disable maintenance mode (HA restores hacritical VMs automatically)
#   5. Wait for HA VMs to return to target_node
```

**変更後:**

```yaml
#   4. Disable maintenance mode and wait for LRM to become active
#   5. Issue ha-manager crm-command relocate for each hacritical+prefer<target> VM
#   6. Wait for HA VMs to return to target_node
```

（以降のステップ番号を 5→6、6→7 へ繰り下げ）

---

## 確認コマンドと結果

```bash
git diff docs/ai/prompts/proxmox_patch_policy.md playbooks/proxmox_restore_vm_placement.yml
```

変更は意図した 4 箇所のみ（+ ステップ番号繰り下げ 1 箇所）。コードの動作変更なし（ドキュメント・コメント更新のみ）。

```bash
ansible-playbook -i inventories/homelab/hosts.yml \
  playbooks/proxmox_restore_vm_placement.yml \
  --syntax-check -e target_node=pve2
```

```text
playbook: playbooks/proxmox_restore_vm_placement.yml
```

syntax-check: OK

---

## 未対応事項

なし。`003_review.md` の Finding をすべて対応した。

---

## 注意点

- `proxmox_patch_policy.md` の 6.7 処理概要はステップ番号が 8→10 に増えた。
  他の箇所（5.x / 17.x など）でステップ番号を参照している記述はないため、影響なし。
- 今回の変更は**ドキュメントおよびコメントのみ**の更新であり、role / playbook の実行ロジックは変更しない。
  実行ロジックの修正は `2026-06-13_002_implement.md` で完了済み。

---

Next step files:

- `docs/ai/reviews/proxmox_restore_vm_placement/2026-06-13_001_requirement.md`
- `docs/ai/reviews/proxmox_restore_vm_placement/2026-06-13_003_review.md`
- `docs/ai/reviews/proxmox_restore_vm_placement/2026-06-13_004_implement.md`（本ファイル）
