# 追加実装報告: Mode A フロー記述 / Phase 3 見出し 更新

日付: 2026-06-13
参照:

- `docs/ai/reviews/proxmox_restore_vm_placement/2026-06-13_001_requirement.md`
- `docs/ai/reviews/proxmox_restore_vm_placement/2026-06-13_005_review.md`

---

## 対応した Finding

### Low: 正本ポリシーの Mode A 手順と role の Phase 見出しに旧フローが残っている

`005_review.md` が指摘した箇所:

| ファイル | 箇所 | 問題 |
| --- | --- | --- |
| `docs/ai/prompts/proxmox_patch_policy.md:1253` | 11.3 Mode A 実行フロー（pve2 restore 行） | `maintenance disable → HA復帰待機` のみで relocate 発行が欠落 |
| `roles/proxmox_restore_vm_placement/tasks/main.yml:158` | Phase 3 見出しコメント | `triggers HA restore` という記述が maintenance disable 自体が HA restore を開始するように読める |

---

## 作成・更新したファイル一覧

| 操作 | ファイル |
| --- | --- |
| 更新 | `docs/ai/prompts/proxmox_patch_policy.md` |
| 更新 | `roles/proxmox_restore_vm_placement/tasks/main.yml` |
| 新規 | `docs/ai/reviews/proxmox_restore_vm_placement/2026-06-13_006_implement.md`（本ファイル） |

---

## 変更内容の詳細

### 1. `docs/ai/prompts/proxmox_patch_policy.md` — 11.3 Mode A 実行フロー（旧 line 1253・1258）

pve2 と pve1 の restore 行を対称的に更新した。

**変更前:**

```text
11. pve2 を restore（non-HA migrate back → 停止VMを起動 → maintenance disable → HA復帰待機 → post-restore healthcheck）
...
16. pve1 を restore
```

**変更後:**

```text
11. pve2 を restore（non-HA migrate back → 停止VMを起動 → maintenance disable → LRM待機 → HA relocate → HA復帰待機 → post-restore healthcheck）
...
16. pve1 を restore（non-HA migrate back → 停止VMを起動 → maintenance disable → LRM待機 → HA relocate → HA復帰待機 → post-restore healthcheck）
```

**補足**: 17.1 Mode A 標準手順（旧 lines 1821・1826）も同様に更新した。

---

### 2. `roles/proxmox_restore_vm_placement/tasks/main.yml` — Phase 3 見出し（旧 line 158）

**変更前:**

```yaml
    # =========================================================================
    # Phase 3: Disable maintenance mode (triggers HA restore)
    # =========================================================================
```

**変更後:**

```yaml
    # =========================================================================
    # Phase 3: Disable maintenance mode and relocate HA VMs home
    # =========================================================================
```

---

## 確認コマンドと結果

```bash
ansible-playbook -i inventories/homelab/hosts.yml \
  playbooks/proxmox_restore_vm_placement.yml \
  --syntax-check -e target_node=pve2
```

```text
playbook: playbooks/proxmox_restore_vm_placement.yml
```

syntax-check: OK

```bash
git diff --stat
```

```text
 docs/ai/prompts/proxmox_patch_policy.md           | 20 +++++++++--------
 playbooks/proxmox_restore_vm_placement.yml        |  7 +++---
 roles/proxmox_restore_vm_placement/tasks/main.yml | 26 ++++++++++++++++++++-
 3 files changed, 40 insertions(+), 13 deletions(-)
```

---

## 未対応事項

なし。`005_review.md` の Low Finding をすべて対応した。

---

## 注意点

- 今回の変更は**コメント・ドキュメントのみ**の更新であり、role の実行ロジックは変更しない。
- pve1 restore 行（旧 line 1258 の `16. pve1 を restore`）は単純な省略表記だったが、
  pve2 と対称的に詳細フローを追記した。

---

Next step files:

- `docs/ai/reviews/proxmox_restore_vm_placement/2026-06-13_001_requirement.md`
- `docs/ai/reviews/proxmox_restore_vm_placement/2026-06-13_005_review.md`
- `docs/ai/reviews/proxmox_restore_vm_placement/2026-06-13_006_implement.md`（本ファイル）
