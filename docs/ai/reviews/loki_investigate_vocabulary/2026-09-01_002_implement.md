# implement: Loki調査語彙の追加と、調査手段の所在の是正

日付: 2026-09-01
requirement: `docs/ai/reviews/loki_investigate_vocabulary/2026-09-01_001_requirement.md`
review: `docs/ai/reviews/loki_investigate_vocabulary/2026-09-01_003_review.md`(Claude)、`2026-09-01_004_review_codex.md`(Codex)。**requirement AC1〜AC8に変更は無い。**

- 1回目: 両者からRequest Changes(blocking 4件: B1〜B4)。是正済み(§1〜§5、以下「1回目是正」として残す)
- 2回目(`2026-09-01_004_review_codex.md` 再レビュー版): **Claude側はApprove、Codex側はRequest Changes(blocking: Codex Major#1、以下C1)で判定が割れ、Coordinatorの判断でCodex側を採った。** C1は「entry数でMAX_LINES予算を組んでも、entry本文の埋め込み改行で物理stdout行数がMAX_LINESを超えうる」というもので、**1回目是正で行った299/300/301の境界テスト(entry数ベース)はこの経路を通っておらず、AC2/AR-098が要求する「stdout行数の上限」を証明していなかった。** 本記録はこの事実を残したまま、C1の是正と物理行数ベースの再検証を追記する(§2.1、§3.1)

## 1. 変更ファイル

- `roles/recovery_exec/files/recovery-loki-helper` — `cmd_errors()` を新設し、`main()` の subcommand 分岐へ `errors` を追加。冒頭docstringへ新語彙を反映。切り詰め通知を `_truncation_notice()` へ抽出し `cmd_window` / `cmd_errors` で共有(`cmd_window` の出力は旧版とbyte-identical)。**B1是正**: `cmd_errors` はAC4のnote行を `MAX_LINES` の予算の外に置いていなかった(note+entries+切り詰め通知で最大301行になっていた)。note用に1行を先に予約し(`entry_budget = MAX_LINES - 1`)、切り詰め時はさらに通知用の1行を予約する形へ直した。**B4是正**: AC4のnote文言から「(e.g. network-devices)」という job 名指しの例示を外し、「level が付くかどうかは job/host ではなく行ごとに決まる」という言い方へ改めた。同様の例示・コメントを docstring・selectorの根拠コメントからも除去した。**C1是正(2回目、§2.1/§3.1)**: `cmd_errors` の出力ループで、entryの本文 `line` に含まれる CR/LF(を含む `str.splitlines()` が認識する物理改行相当の文字全般)を `" ".join(line.splitlines())` で1論理行へ正規化してから `MAX_LINE_LENGTH` の切り詰め・`print()` を行うよう変更した。**`cmd_window`/`cmd_count` はこの変更の対象外**(既存2語の出力処理は変更していない — Codex側指摘のとおり、blockingの根拠は新設`cmd_errors`自身がAC2を満たさないことだけであるため)
- `roles/recovery_exec/templates/recovery-investigate-dispatch.sh.j2` — `loki-errors|loki-errors\ *)` ケースを追加(引数検証→`exec recovery-loki-helper errors`)。ヘッダコメント(冒頭8-13行)を2語→3語に更新し、あわせて「唯一のparameterized case」という古い主張も是正(`journal-ssh`/`deployed-hash`/`unit-cat` も既にparameterized caseであるため、「several parameterized cases のひとつ」に改めた。**B2是正**)
- `roles/recovery_exec/templates/homelab-investigate.sh.j2` — `loki-errors)` ケースを追加(既存 `loki-count`/`loki-window` と同じ形の事前検証→`run_remote`)。ヘッダコメントへ用法を追加
- `roles/recovery_exec/templates/AGENTS.md.j2` — **B2是正(新規)**。運用Codexが読むツール説明が2語のままだった欠落を埋めた。usageブロックへ `loki-errors <since> <window>` を追加し、「Loki横断ログ調査」節を3語の使い分け(count→当たりをつける、window→1jobの本文、errors→全job横断でerrorのみ最大24h)、`<window>`のallowlist(`1h`/`6h`/`12h`/`24h`)、level無し行の扱いと到達経路(`loki-count`/`loki-window ... any`)、切り詰め時の指示(job operandが無いため窓を狭めるかjob別`loki-window`へ切り替える)へ更新した。あわせて既存の「実運用でnetwork-devicesに多い」という例示も、B4と同じ理由でjob非依存の言い方へ直した
- `roles/recovery_exec/defaults/main.yml` — **B2是正(新規)**。59行目のコメントを「loki-count / loki-window」→「loki-count / loki-window / loki-errors」に更新(挙動・値は無変更)
- `docs/ai/reviews/dev_prod_boundary/2026-08-03_008_phase3_check_catalog.md` — §9として `loki-count`/`loki-window`/`loki-errors` を問い合わせ語彙として追加。§2の `loki-helper`(deployed-hash対象)行と混同しないことを明記。**B3/B4是正**: 「level無しの行はL1〜L3のいずれでも取得できない」という誤った断定(`loki-window ... any` がlevel無し行の唯一の到達経路であることと矛盾)を削除し、L1(集計)/L2の`any`(生ログ)がlevel無し行への到達経路として残っていることを明記。job名指しの例示(network-devices)も外し、`extract_level_best_effort` の実際の挙動(job単位ではなく行単位でlevelが付く)へ根拠を差し替えた。**non-blocking#5是正**: §9.1の総数表(§4/§6.3/§7.5)が「D5〜D8の承認時点で閉じたスナップショットであり、以後のカタログ改訂で追随させる仕組みではない」ことを明記し、L3が表の数え方では`+7`に相当するが本節では改めない理由を1文で説明した。**Minor#2是正(2回目)**: 「`QUERY_LIMIT`/`MAX_LINES`/`MAX_LINE_LENGTH`はL1〜L3で共通」という誤った記述(L1=`cmd_count`はこの3定数を一つも参照せず、`MAX_SERIES`で系列数だけを制限する)を、「L2/L3が3定数を共有し、L1は`MAX_SERIES`を使う」という実装どおりの説明へ直した。`ast`で `cmd_count`/`cmd_window`/`cmd_errors` それぞれが参照する定数名を機械的に抽出して確認した(§3.1)
- `docs/ai/policies/execution_boundary_policy.md` — EXEC-003の`monnie`行から、カタログ全体(Loki横断ログ調査は§9)へのポインタを追加。変更履歴表のv1.5を最終形へ更新。**non-blocking#6是正**: ポインタを`§9`単体ではなく「カタログ(Loki横断ログ調査は§9)」の形にし、`monnie-investigate`の大半のチェックが載る§2/§6/§7へも辿れるようにした

## 2. AC充足(契約は不変。是正後の実装がAC1〜AC8を満たすことを再確認)

| AC | 内容 | 状態 |
|---|---|---|
| AC1 | 全jobのerror行を時刻昇順で返す | 実装済み。selector `{job=~".+", level="error"}`、`entries.sort(key=lambda e: e[0])` で時刻昇順に統一。複数jobにまたがる昇順をunit testで確認(§3) |
| AC2 | `QUERY_LIMIT`/`MAX_LINES`/`MAX_LINE_LENGTH` を緩めない | 実装済み。3定数とも既存値のまま(各300)。**B1是正後も定数自体は変更していない** — `cmd_errors` は `MAX_LINES` の内側でnote行の予算を確保する形にしただけで、新しい定数は作っていない。**C1是正(2回目)**: entry数ベースの予算だけでは「stdout行数上限」自体を証明できていなかった(entry本文の埋め込み改行で物理行が展開されるため)。本文をCR/LF正規化してから出力する形に直し、物理行数ベースで `len(stdout.splitlines()) <= MAX_LINES` を再検証した(§2.1/§3.1) |
| AC3 | 切り詰め時、loki-windowと同じ形式で「どこまで読めたか」を出力 | 実装済み。`_truncation_notice()` を両コマンドが共有し、文言の書式(`[TRUNCATED — read ... of requested ... JST; earliest N of >M lines. ...]`)は完全一致。N/Mの値は各コマンドの実際の予算に応じて渡す(cmd_window: 299/300、cmd_errors: 298/299)ため、**是正後は数値が実態と正確に一致する**(B1是正の副産物) |
| AC4 | levelラベル無しの行は取得できないことを出力自身が明示 | 実装済み。`cmd_errors` は結果が空でも常に先頭で note 行をstdoutへ出す。**B4是正後の文言**: `note: loki-errors only sees lines that had a level label attached during collection (this varies line by line, not by job); lines without one are invisible here and are NOT counted as error-free by this result.`(job名を挙げない言い方に修正) |
| AC5 | dispatch側でallowlist検証→位置渡し、helper側でも同じallowlistを再確認 | 実装済み(前回report時点から変更なし)。dispatchが `since`/`window` を正規表現・case文で検証し `exec ... errors "$since" "$window"` で位置渡し。helper側も `ALLOWED_ERRORS_WINDOWS` で再検証 |
| AC6 | 不正な `since`/`window`/余分operandは `denied:` で拒否、helper起動しない | 実装済み(前回report時点から変更なし)。dispatch script側の挙動テストで確認(§3) |
| AC7 | カタログに問い合わせ語彙としてloki-count/loki-window/loki-errorsが載る。既存 `loki-helper`(deployed-hash)行と混同しない | 実装済み。§9は維持しつつ、**B3/B4是正で本文の正確性を修正**(§1参照) |
| AC8 | EXEC-003のmonnie行から、何ができるか(Loki横断ログ調査を含む)とカタログへのポインタが辿れる | 実装済み。**non-blocking#6是正でポインタの形を修正**(§1参照)、AC8の意図(何ができるか全体への到達)により忠実になった |

### 2.1 C1(2回目差し戻し)の経緯と是正

**1回目是正でのB1対応・境界値テスト(299/300/301)は、entryの「個数」でMAX_LINES予算を組み、その個数のままstdout行数と同一視していた。** この前提は、Lokiが返すentry本文(`line`)が改行を含まない場合にしか成立しない。Codex側再レビューが、本文を `"a\nb"` とした299 entriesを与えると、note行を含む物理stdoutが**599行**になることを実測で示した(1 entryの本文に含まれる `\n` がそのまま `print()` され、物理行としては2行に展開されるため)。Coordinatorも同じ現象(1 entryが物理2行になること)を独立に再現している。

**`MAX_LINE_LENGTH` による切り詰めはこの経路を防がない** — 文字数だけを見る切り詰めであり、短い本文に改行が含まれていれば発火すらしない。

**是正**: `cmd_errors` の出力ループで、entry本文を `" ".join(line.splitlines())` により1論理行へ正規化してから `MAX_LINE_LENGTH` の判定・`print()` を行うよう変更した(`str.splitlines()` はCR/LF/CRLFに加え、他の物理改行相当の文字も分割対象にするため、CR/LF限定の`str.replace`より広く安全側)。これにより、entriesの件数と実際に出力される物理stdout行数が一致することが保証される。**`cmd_window`/`cmd_count` は対象外** — 同じ性質(埋め込み改行がそのまま物理行として展開される)を持つことは認識しているが、Coordinatorの指示どおり本案件のblocking根拠は新設`cmd_errors`自身がAC2を満たさないことに限られ、既存2語の出力処理は変更していない(触るなら別案件)。

実測結果は§3.1に記す。

## 3. 自己検証

- `python3 -m py_compile roles/recovery_exec/files/recovery-loki-helper` — 構文OK(是正後も継続)
- **B1境界値テスト(1回目是正時、entry数ベース。§3.1参照)**: Python単体テスト(importlib経由でモジュールをロードし `loki_get` をモック)で、Lokiが返すentry数を `0, 1, 299, 300, 301, 500` の6パターン(**本文は改行を含まない単純な文字列**)で与え、以下を確認した。
  - `len(stdout.splitlines()) <= MAX_LINES`(300)が**このパターン群では**成立
  - note行(`note: loki-errors only sees lines...`)が**全パターンでstdout先頭に出る**(0件でも出る)
  - `n=299`: 切り詰め通知なし、stdout合計300行(note 1 + entries 299)
  - `n=300`/`n=301`: 切り詰め通知あり、stdout合計300行(note 1 + entries 298 + 通知1)、通知本文は `earliest 298 of >299 lines`
  - **このテストは「entryの個数」を物理stdout行数と同一視しており、entry本文に埋め込み改行がある場合を経路として通していなかった。C1(2回目差し戻し)はこの欠落を指摘したもので、指摘は妥当だった。** 物理行数ベースの再検証は§3.1

### 3.1 C1是正の実測(物理行数ベース、今回追加)

Python単体テストを、`len(stdout.splitlines())`(**物理行数**)で検証するよう組み直した。

- **再現(是正前の状態を確認)**: entry本文を `"a\nb"` とした299 entriesを与えると、noteを含む物理stdoutが**599行**になることを確認した(Codex側の再現と一致)。同様に、1 entryあたり10個の改行を含む本文(`"seg0\nseg1\n...\nseg9"`)を299 entries与えても、是正前のロジックでは`MAX_LINES`を超える
- **是正後・埋め込み改行ありでの再検証**:
  - `"a\nb"` を本文とする299 entries → 物理stdout行数 **300**(`<= MAX_LINES`)。改行は正規化され、各行に `\n` を含まないことを確認した(サンプル: `2025-01-01T09:00:00+09:00 [pve-nodes/pve1] a b`)
  - 1 entryあたり10行の本文(`seg0`〜`seg9`)を299 entries → 物理stdout行数 **300**
  - `"part1\r\npart2\rpart3\npart4"`(CRLF・CR・LFを混在させた本文)を5 entries → 物理stdout行数 **6**(note 1 + entries 5)、いずれの行も `part1 part2 part3 part4` へ正規化されており、CR単独・CRLF・LFのいずれも1論理行へ畳み込まれることを確認した
- **是正後・境界値の再検証(埋め込み改行入り本文で、物理行数を直接assert)**: entry数 `0, 1, 299, 300, 301, 500` のそれぞれに `"line-with-\nembedded-\r\nnewlines\r"` という本文を与え、`len(stdout.splitlines()) <= MAX_LINES` と note行の存在を同時に確認した。結果: 物理行数は順に `1, 2, 300, 300, 300, 300`、`n=300`以降で切り詰め通知が出る — **1回目是正時と同じ数値**(entry数ベースのテストと物理行数ベースのテストが、正規化後は一致することを確認できた)
- **既存2語への非退行(埋め込み改行入り)**: `cmd_window` に本文 `"a\nb"` の1 entryを与えると、出力は `"2025-01-01T09:00:00+09:00 [pve1] a\nb\n"` のまま(`\n` が正規化されず物理2行になる)であることを確認した。**これは意図した挙動である** — 本案件は`cmd_window`を変更対象にしていない(§2.1)。`cmd_count`は本文を扱わない(件数のみ)ため、この経路自体が存在しない
- **回帰確認**: 改行を含まない本文での従来テスト(entry数 `0,1,299,300,301,500`)、`cmd_window`の基本出力、`cmd_count`の基本出力を再実行し、いずれも変更前と同一の結果であることを確認した

- **`cmd_window`の非退行(既存2語の再確認)**: `_truncation_notice()`のシグネチャ変更後も、`cmd_window`のnormal出力(`2025-01-01T09:00:00+09:00 [pve1] x`)と、n=301時の切り詰め通知(`earliest 299 of >300 lines`、is-identical to pre-B1-fix values)が**旧版と同一**であることを確認した。B1修正はcmd_errors側の予算配分のみを変え、cmd_windowへ渡す引数(`len(entries)`=299, `MAX_LINES`=300)は数値として従来と同じになるよう実装した
- **B4文言確認**: `cmd_errors`のnote出力に `"network-devices"` という文字列が含まれないこと、`"line by line, not by job"` という訂正後の言い回しが含まれることを実行して確認した
- dispatch/wrapper/AGENTS.md.j2の3テンプレートをJinja2で monnie/authy 両方の実際の変数(`recovery_exec_targets`)でレンダリングし、dispatch/wrapperは `bash -n` で構文確認(4パターンとも通過)。authy(loki無効)側のレンダリング結果に `loki-errors` の実行分岐が一切現れないことを確認(冒頭ヘッダコメントに一般論として1箇所出るのみ、既存2語の記載も元から同様の書き方だったため新規の非対称ではない)。AGENTS.md.j2は変数を持たないテンプレートで、レンダリング結果に `loki-errors` が8箇所含まれることを確認した
- dispatch/wrapperの挙動テストは前回report時点のもの(正常系・不正since・不正window・operand過不足・既存2語・未知コマンド)を再実行し、是正後も結果が変わらないことを確認した(コメント変更のみで挙動に影響する変更が無いため)。**この検証では実ホストへのssh接続もLokiへの問い合わせも一切発生していない**
- `python3 scripts/check-doc-consistency.py` — 是正後の7ファイル(`recovery-loki-helper`、dispatch/wrapper/AGENTS.md.j2 の3 template、`defaults/main.yml`、catalog、EXEC policy)を一時的な使い捨てclone(`git clone` local、`/tmp` scratchpad配下)へコピーし、そのclone内でのみ `git add` してから実行し、3チェックすべてPASS(check1: 114、check2: 8、check3: 104)を確認した。**C1/Minor#2是正後に再実行**しても同じ結果(PASS)であることを確認済み。**このclone操作は本作業ツリー・実リポジトリのindexには一切影響しない** — clone自体を検証後に削除した。本作業ツリーでは `git add` を一度も行っていない

## 4. quory側wrapper(`homelab-investigate.sh.j2`)の扱い

requirement §5「既存2語がwrapperにも登録されているなら同じ規律で追加する」に従い、実装対象へ含めた(前回reportから判断は変わらない)。確認手順:

1. `roles/recovery_exec/templates/homelab-investigate.sh.j2` を読み、`loki-count`/`loki-window` が既にケースとして存在すること(operand形式の事前検証のみ、`run_remote` でSSH経由の文字列をdispatchへそのまま渡す設計)を確認した
2. helperの冒頭docstring(「dispatchを正本、wrapper(homelab-investigate.sh.j2)をmirrorとする」)のとおり、wrapper側には `QUERY_LIMIT` 等の出力量定数を一切持たせていない — 追加した `loki-errors` ケースも同様に、`since`/`window` の形式検証だけを行い、Lokiへの問い合わせも出力量制御も持たせていない
3. したがって、`loki-errors` を追加してもwrapperの設計(事前検証のみ、Loki問い合わせ責務はdispatch/helperにのみ存在)は崩れていない。二重に検証するのはoperand形式のみで、AR-097の「quory側wrapperを一次filter、対象host側dispatchを権限の本gate」という規律もそのまま踏襲した
4. **B2で追加したのはquoryの `homelab-investigate-monnie` バイナリではなく、`recovery-exec`ワークスペースへ配備される `AGENTS.md`(運用Codexへのツール説明)である。** 両者は別物(前者はoperand検証、後者はドキュメント)で、`homelab-investigate.sh.j2` 自体への変更は前回reportから増えていない

## 5. 未解決事項

- なし。requirement §6のとおり、実機到達性の検証(実際にmonnieのLokiへ問い合わせが通ること)はCoordinatorが配備後に行う
- 1回目差し戻しのblocking B1〜B4、およびnon-blocking #5/#6はすべて本記録に反映して是正した。#8(切り詰め通知の文言)・#9(AR-099とのPolicy上の関係)はCoordinatorの判断により不採用(前者はAC3の「同一形式」要求と衝突、後者はrequirementのACに無くPolicy本文改訂になるため別途判断)
- 2回目差し戻し(Claude Approve / Codex Request Changes、Codex側を採用)のblocking C1、および併せて指摘されたMinor#2はいずれも本記録に反映して是正した(§2.1/§3.1、§1のcatalogの行)。**1回目是正時点の「境界テスト済み」という記述はentry数ベースであり、embedded改行の経路を検証できていなかった** — この事実は§3の該当bullet(取り消し線ではなく注記を追加する形)と§2.1に残した。C1是正後、埋め込み改行を含む入力での物理行数ベースの再検証をもってAC2/AR-098の「stdout行数上限」が新設`cmd_errors`について成立することを確認している
