# findings の処置(案件クローズ)

2026-08-04 / Coordinator。Yoshinobu承認済み。

Reviewer 3本(002 / 003 / 004)の findings を **Critical 7件・Suggestion 3件**として統合し、7件すべてを現物で確認したうえで処置した。**却下した指摘は無い。** 変更後の文面は commit の diff が正本であり、ここへ写さない。

## Critical(7件)

| # | 対象 | 処置 |
|---|---|---|
| C1 | `docs/ai/roles/coordinator.md` 承認境界の表 | 行3に「このうち4ホストへは到達手段が無く次行が優先する」を追記。**行は残した** — 文言は `.claude/settings.json` が同じルールへ既に書いているものを借りており、両者を対応させて維持する約束(表の下の注記)を守るため |
| C2 | `docs/ai/role-routing-index.md` Tester行 | 到達してよい範囲は coordinator.md が定める旨を追記。ホスト名は写さない |
| C3 | `docs/ai/context/system/semaphore.md` | template の正本が `roles/semaphore_templates/` にあることへ書き換え |
| C4 | `proxmox_operations_policy.md` SB-090 | 実行端末を quory に限定。**ただし ansy に閉じた実行(decoy / `--syntax-check` / ansyのSemaphore)はテスト目的で可、を明記した** — Yoshinobu指摘(2026-08-04)。当初案は「quoryに限定する」のみで、ansy に閉じたテストまで禁じていた |
| C5 | `proxmox_backup_restore_verify_policy.md` 3箇所 | 実行元から ansy を除去。**BRV-043 は退番にせず主語を「monthly schedule以外の手動実行」へ置換** — 重複の懸念は実行元によらず残るため |
| C6 | `time_sync_check_policy.md` TIME-002 | 実行元から ansy を除去 |
| C7 | `unifi_backup_fetch_policy.md` UNIFI-003 | 実行元から ansy を除去 |

**C4 の但し書きを C5〜C7 へ展開しない**と決めた。あの3つは「本番実行がどこから出るか」を書く欄であり、ansy に閉じた検証は `docs/ai/core.md`「開発と本番の境界」が既に許している。4箇所へ同じ文を増やさない(Yoshinobu合意)。

## Suggestion(3件)

| # | 処置 |
|---|---|
| S1 | **次工程へ送った**(Yoshinobu合意)。調べ直したところ Reviewer の指摘より範囲が広く、`role-context-matrix.md` は Context 14本のうち4本しか名指ししておらず `operations/` は6本すべてが表に無い。表の設計を決める必要があるため本案件から切り出した。申し送りは `docs/ai/status.md` Next |
| S2 | `AGENTS.md` 末尾の「本ファイルの要否は未判断。」を削除。**ファイルは残す**(Yoshinobu判断「害はないはず」)。この判断により `docs/ai/status.md` Next の「`AGENTS.md` の要否判断」行は用済みとなり削除した |
| S3 | **見送り。** 矛盾ではなく、直すと行が増える |

## レビューが拾えなかったもの(Coordinatorが実装中に検出)

`time_sync_check_policy.md` の TIME-002 直後の本文が「quory実行時・ansy実行時いずれでも自己比較にならない」と書いており、C6 を適用した結果と食い違った。**掃引の対象は「実行元」を名乗る欄に寄っており、設計の説明文に埋まった同じ前提を拾えなかった。** 「どのホストで実行しても」へ置換して解消した。

同じ理由で `unifi_backup_fetch_policy.md:19` の `localhost（quory / ansy）` も確認したが、こちらは比較表のうち `cloudkey_cert_deploy` 側を説明するセルであり、**現在も正しい**(あちらは `connection: local` で CloudKey の API を直接叩き、Vault パスワードも意図的に ansy へ残してある)。003 のレビューが同じ区別をしている。

## Auditor

**起動していない。** 記録は本ファイルと3本のレビュー成果物、および commit メッセージで再構成でき、対象が文書の文面変更に閉じているため、工程を積み増さない判断をした(`docs/ai/roles/coordinator.md`「委任するときの独立性」に基づくCoordinatorの裁量)。
