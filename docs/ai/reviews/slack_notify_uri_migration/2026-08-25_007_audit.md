# Audit: Slack notify URI migration

Auditor / 2026-08-25
対象: `docs/ai/reviews/slack_notify_uri_migration/`(001〜006)
関連commit: `5cdaf7f`〜`a069177`(`3315c62`, `fabdb97`, `494df72`, `7989f27`, `105cae6`, `a069177`)

## 再構成できたか

**できた。** requirement(001)→implement(002)→review(003, Request Changes)→review是正/implement/requirement更新(fabdb97/494df72)→再review(004, Approve、AC7未検証を明示してTesterへ残す)→test_result(005)→review addendum(006, AC7 PASS支持)→AC2の本番確認(a069177、005とstatus.mdへ反映)という一直線の経路が、番号付き成果物とcommit logの両方から一致して辿れる。詰まった箇所はなかった。

### 突き合わせて確認した事項

- 全commit(`5cdaf7f`, `3315c62`, `fabdb97`, `494df72`, `7989f27`, `105cae6`, `a069177`)が実在し、各reviewの「対象」欄の親子関係と一致する。
- 現物コード(`roles/common_slack/tasks/notify.yml`, `playbooks/incident_investigate_notify.yml`)は、implement/review/test_resultが述べる内容(`ansible.builtin.uri`、`timeout: 10`、`link_names: 1`のtop-level配置、token抽出taskの削除、`community.general.slack`の実行箇所ゼロ)と一致した。コード上に残るのはコメント内の旧モジュール名への言及のみで、実行はしていない。
- `playbooks/README.md`の該当行は`ansible.builtin.uri`表記へ更新済み(P0-1)。
- 参照されているPolicy根拠(TS-005, TS-014, TS-022, TS-026, TS-031, EXEC-042)、`docs/ai/memory/knowledge-review-log.md`の「#テスト不足 13件中5件」、`docs/ai/memory/decisions/grant-read-access-rather-than-police-speculation.md`は実在し内容も記述と整合する。
- `docs/ai/memory/incidents/2026-08-25_decoy-slack-reached-real-endpoint-during-its-own-fix.md`は実在し、implement(002)が述べるP0-3訂正の経緯・「移行前コードを実行しない」という再発防止と一致する。
- `docs/ai/status.md`の該当2エントリ(Now冒頭「観測待ち: Slack送信のuri移行が…」、およびそこから参照されるAC2/AC3/AC6/回帰テスト保留の記述)は、test_result(005)・a069177コミットの内容と一致する。AC3が「意図的に起こせない」観測待ちとして残っている点も、test_resultの記述と食い違わない。
- review 003→004間で指摘された2件(timeout競合、`link_names`脱落)は、diffとしても該当行に反映されており「Closed」の主張は現物で裏付けられる。

## 指摘

### 1. P2-1の判断が記録されていない(非ブロッキング)

requirement §5 P2-1は「`docs/ai/context/operations/`に…1行残すか検討する。**本案件では判断のみ行い**、実施は別」と、判断そのものを本案件のscopeに含めている。しかし implement(002)の「未解決事項」、review(003/004)、test_result(005)のいずれにもP2-1への言及がない。判断が行われた形跡もなければ、「やらなかった」という明示もない。

Auditorの基準(「未解決の明示」「黙って落ちている項目がないか」)に照らすと、これは黙って落ちた項目にあたる。ただしP2-1はP2(最低優先度)であり、案件のゴール(AC1〜AC7)そのものには無関係で、機能・安全境界に影響しない。**クローズを妨げない。**

## 状態の正本との突合

`docs/ai/status.md`は本案件の現況(AC1/AC4/AC5/AC7/`link_names`構造=PASS、AC2=PASS実測済み、AC3=観測待ち、AC6=配備待ち、回帰テスト保留)を正しく反映している。除去すべき行はない — 案件はまだAC3の観測待ちが残っているため、該当行は消さずに残すのが正しい状態である。

## 判定

**受入。**

指摘は1件のみで、非ブロッキングと明示した。記録間の矛盾、根拠の不実在、転記先の欠落、反証の未反映は見つからなかった。AC3の観測待ちと回帰テスト保留は、意図的な未解決として記録されており「黙って落ちている」ものではない。

---

## クローズの判断(Coordinator、2026-08-25)

**クローズする。**

指摘されたP2-1は、requirementへ判断を書いて解消した(「書かない」。移行後は `url:` が唯一の宛先入力でコードから自明であり、Contextへ1行置くと同じ主張が2箇所になる)。**指摘のとおり、判断すると書いておきながらどの成果物にも残していなかった。**

**AC3だけが未充足のまま閉じる。** `incident_investigate_notify.yml` は Semaphore ジョブが失敗して一次調査が完了したときにしか動かず、**意図的に起こせない。** `docs/ai/status.md` の観測待ちへ置いた。次にそれが起きたときが唯一の確認機会である。

**回帰テストを置かなかったことは承知の上の残存リスクである。** `community.general.slack` へ戻す差分や `link_names` / `timeout` の脱落を機械的に止める仕組みは無い(Reviewerの Suggestion 2、Coordinatorが保留)。
