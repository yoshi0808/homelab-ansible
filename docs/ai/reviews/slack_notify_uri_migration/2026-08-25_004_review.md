# Code Review: Slack notify URI migration（再レビュー）

Reviewer: Codex / 2026-08-25
対象: `fabdb97..494df72`（前回対象 `3315c62` からの是正を併せて照合）
Verdict: **Approve**

## Summary

前回のblocking finding 2件は閉じた。両`uri` taskへ明示された`timeout: 10`は、旧`community.general.slack`が使っていた`fetch_url()`既定10秒と一致し、incident経路の親timeout 30秒より十分短い。両payloadには旧モジュールと同じ整数の`link_names: 1`がbody直下へ入り、attachments経路・plain text経路の双方で旧message parsingを維持する。

前回Approve済みの完全URLによる宛先決定、URL秘匿、rescueの経路別終了コード、抑止3条件、check-mode境界、N2、Python実行ロジックには追加変更がない。実Slackで`@here`等を発火させず、decoyが捕捉したJSONの構造で`link_names`を検証する契約上の限定も妥当である。

## Critical Issues

なし。

## Previous Findings

| Previous item | Status |
|---|---|
| Finding 1: `uri`既定30秒と親playbook timeout 30秒の競合 | **Closed.** 両taskに`timeout: 10`を明示し、旧実効値を維持した。incident経路では親30秒に対して20秒の余白を持つ |
| Finding 2: 旧payloadの`link_names: 1`脱落 | **Closed.** commonは`body.link_names`と`body.attachments`が兄弟、incidentは`body.link_names`と`body.text`が兄弟で、値はいずれも整数1 |
| Suggestion 1: P0-3がCoordinator判断待ちと残る | **Closed.** 充足表と§「差し戻し前に指摘した事項の現状」でrequirement訂正済み・解消済みへ更新した |
| Suggestion 2: checked-in regression test | **Deferred by Coordinator.** 現時点で実装欠陥ではないが、4回再発した境界を一回限りの検証に依存する残存リスクは変わらない |
| Suggestion 3: success時の`changed: true`→`false` | **Not addressed / Non-blocking.** 呼出側にregister/handler依存はなく、機能影響は確認されていない |

## Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---:|---|---|
| 1 | `docs/ai/reviews/slack_notify_uri_migration/2026-08-25_001_requirement.md` | 47, 50-53, 97-101 | `link_names`の要求はP0-1配下に置かれながら「両経路」と書き、P0-2はbodyを`{"text": ...}`とだけ記す。実装とCoordinator判断は明確だが、将来の読者向けにはP0-2にも`link_names: 1`を明記し、実チャンネルで描画効果を検証しない限定がAC2だけでなくAC3にも適用されることを明示すると境界が一意になる。 | Contract clarity |
| 2 | `docs/ai/reviews/slack_notify_uri_migration/2026-08-25_002_implement.md` | 78 | 「requirementのAC2/AC3自体は差し戻しで変更されていない」「Coordinatorに確認したい」は現物とずれている。AC2には既に「`link_names`の描画効果を実チャンネルで確かめない」というCoordinator判断が追加済みである。Testerへ渡す前に、この未解決表現を現在の契約へ揃えるとよい。 | Record accuracy |

## What Looks Good

- YAMLを独立parseし、common側は`timeout=10`, `link_names=1`（int）, body keys=`attachments/link_names`、incident側は`timeout=10`, `link_names=1`（int）, body keys=`link_names/text`であることを確認した。
- `timeout: 10`は旧経路の実効値を厳密に再現し、2経路で値を二重に異ならせていない。
- `link_names`はattachment要素内ではなくpayload top-levelに置かれ、旧`build_payload_for_slack()`と同じ階層である。
- `link_names`の効果確認に本番`@here`を使わない判断は、全員通知という不要な副作用を避けつつ、decoyのrequest body捕捉で実装事実を判定できる。
- `playbooks/README.md`と`incident-investigate.py`は`3315c62`から不変。前回確認したURL秘匿の多層防御とPython実行ロジックを後退させていない。
- 新たなshell/command、秘密値の表示、delegate先変更、`no_log`後退、check-mode迂回はない。

## Verification

- `git rev-parse HEAD`: `494df72b3c39e4c1db92cddbb32a7a503782b5d9`。親は`fabdb97`。
- 着手時・返却直前とも、成果物作成前の対象作業ツリーはclean。対象commitはレビュー中不変。
- YAML構造評価: 両`timeout`は整数10、両`link_names`は整数1、body直下の位置を確認。
- `git diff --check fabdb97 494df72`: PASS。
- `scripts/check-tester-gate.sh`: PASS（57 playbooks）。
- `ansible-playbook --syntax-check playbooks/incident_investigate_notify.yml`: PASS。
- `ansible-lint`: 前回同様の既存9件のみ。今回追加した`timeout` / `link_names`に新規violationなし。
- Reviewer独立の無応答socket fixture実行は未実施。sandboxがlisten socket作成を拒否し、sandbox外実行の承認も得られなかったため、規則どおり別手段へ迂回していない。実装者記録の約11秒・rescue完走という実測は独立に引き継がず、**AC7はTesterが無応答fixtureで実行確認する必要がある。** 静的には10秒のuri timeoutと30秒の親timeoutの関係が成立することを確認した。
- AC2/AC3の実Slack表示、AC6の配備hashは未確認。requirementどおりTester・配備工程の領分である。
- `docs/ai/memory/`は読んでいない。

## Residual Risks / Tester Handoff

- AC7は、接続拒否ではなくaccept後に応答しないfixtureを使い、親30秒より前に両rescueが完走することを確認する。
- `link_names: 1`はdecoy HTTP serverが捕捉したJSON bodyで両経路を確認し、実Slackへ`@here`等を送らない。
- AC2/AC3は通常の安全な本文で、commonのicon/title/text/colorとincidentのattachments無しplain textだけを確認する。
- checked-in回帰テストを保留したため、将来`community.general.slack`へ戻る差分や`link_names`/timeout脱落を機械的に止める仕組みはまだない。

## Verdict

**Approve** — 前回の2件はコードと契約の双方で解消した。実行不能だったAC7の独立確認はTesterへ明示的に残るが、現物のtimeout関係とpayload構造は正しく、レビュー段階で残るblocking defectはない。
