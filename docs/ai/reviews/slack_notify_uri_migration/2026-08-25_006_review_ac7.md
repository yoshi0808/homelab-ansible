# Review Addendum: Slack notify URI migration — AC7

Reviewer: Codex / 2026-08-25  
対象: `docs/ai/reviews/slack_notify_uri_migration/2026-08-25_005_test_result.md` の AC7 証拠  
Verdict: **AC7 PASS を支持する**

## 結論

Tester の証拠は AC7 の成立を十分に支える。追加で必要な証拠はない。

## 契約との対応

| AC7 の条件 | Tester の証拠 | 判定 |
|---|---|---|
| 接続後に無応答となる fixture | loopback listener が `accept()` のみ行い、`recv` / `send` を行わない。接続拒否 fixture ではない | 充足 |
| `incident_investigate_notify.yml` 経路 | 実 playbook を、実装と同じ `subprocess.run(argv, cwd=repo_dir, capture_output=True, text=True, timeout=30)` の形で起動 | 充足 |
| 親 timeout より先に rescue が完走 | `TimeoutExpired` なし、rc=2、10.88秒で終了 | 充足 |
| playbook の非ゼロ終了として観測 | 親ラッパーが rc=2 を取得 | 充足 |
| `common_slack` 経路 | 同じ fixture で rc=0、`rescued=1`、10.983秒 | 充足 |
| 両経路の `uri` timeout と親 timeout の余白 | `uri` は両方10秒、親は30秒。実測でも約19秒の余白を残す | 充足 |

incident 経路は本体 Python 関数そのものではなく、同じ playbook argv と timeout 境界を持つ使い捨てラッパーから起動している。ただし AC7 が検証するのは、子 playbook の `uri` failure と rescue が親の30秒より先に終了し、親から `TimeoutExpired` ではなく非ゼロ return code として見えることにある。記録されたラッパーはこの観測境界を再現しており、この一点の判定には十分である。

`common_slack` の `rescued=1` は rescue 到達を直接示す。incident 経路の rc=2 と約11秒での完了は、当該 playbook の rescue が送信失敗を再送出する設計と整合し、親 timeout に奪われず playbook 自身の失敗として完走したことを示す。

## Verification Scope

- Reviewer は fixture を再実行していない。前回確認済みの sandbox 制約と、今回の依頼範囲に従い、Tester のコマンド形・観測値・AC7 との対応を検査した。
- 現 HEAD `7989f27` と実装 commit `494df72` の間で、両通知 task、`incident-investigate.py`、関連 defaults に差分がないことを確認した。
- staged の Tester 成果物は変更していない。実装変更、`git add`、commit、push は行っていない。
- checked-in 回帰テストがないという `2026-08-25_004_review.md` の残存リスクは変わらないが、今回の AC7 PASS を否定しない。

## Verdict

**AC7 PASS を支持する。** accept 後無応答の fixture、両経路、親30秒境界、`TimeoutExpired` 非発生、経路ごとの終了コードと rescue 到達、実測時間がすべて揃っている。
