# findings の処置(案件クローズ。配備まで完了)

2026-08-05 / Coordinator。工程は requirement(`..._001`)→ Implementer(`..._002`)→ 独立Reviewer(`..._003`)→ 差し戻し。**却下した指摘は無い。** 変更後の文面は commit の diff が正本であり、ここへ写さない。

## Reviewer の指摘2件(いずれも是正)

| # | 重大度 | 内容 | 処置 |
|---|---|---|---|
| S1 | Medium | **R3 の制約確認が、quory の Semaphore から押される場合しか考慮していなかった。** `playbooks/semaphore_templates_setup.yml` は ansy を対象にした適用モードを自ら文書化しており、その経路でカタログを適用すると ansy にも同じボタンができる。押すと `connection: local` が無条件に効き、**quory 向けのユニット定義が ansy 自身へ配備される** | `playbooks/systemd_timers.yml` の先頭へ実行ホストの guard play を追加(既存の同型 guard を持つ playbook に倣った形)。**R3 は落としていない** |
| S2 | Minor | 捕捉スクリプトが `incident_capture` 側の spool ディレクトリの存在に暗黙に依存している | スクリプトへ依存と、前提が崩れたときの出方(クラッシュではなく無言の no-op)を明記。**ディレクトリを自分で作る形にはしていない** — T1 と同じ「書くだけ」の面を保つため |

**S1 は「鍵が無いから届かない」という安全の前提が効かない経路だった。** `connection: local` は SSH を使わないため、ansy が quory の認証情報を持たないことは何の防御にもならない。**能力の不在で境界を作るこのリポジトリの設計に、素直に穴が開く形**である。

## Coordinator が確認したこと(差し戻し後)

guard は `ansible_facts['hostname']` が `groups[target_hosts]` に含まれることを assert する。**決め手は「短ホスト名と inventory 名が一致しているか」**であり、そこを独立に確かめた。

| 確認 | 結果 | 手段 |
|---|---|---|
| ansy の短ホスト名 | `ansy` | `hostname` |
| `control_nodes` / `dev_nodes` の実体 | `['quory']` / `['ansy']` | `inventories/homelab/hosts.yml` |
| quory の短ホスト名 | `quory` | 2026-08-04 に取得した journal 出力(`quory systemd[1]`) |

したがって **ansy から既定のまま押すと止まり、`-e target_hosts=dev_nodes` の正当な用途は通る。** quory では通る。

**再レビューは行っていない。** 差分が assert 1 play とコメントに閉じており、判断の決め手(ホスト名の対応)をCoordinator が独立の証拠で確かめたため。

## 受入条件の到達点

| AC | 判定 | 根拠 |
|---|---|---|
| AC1(失敗が捕捉に残る) | PASS(実ホスト外) | 実スクリプトと**実物の収集器**を複製して走らせ、レコードが書かれ `source: "spool"` のバンドルになることを Implementer・Reviewer が別々に再現 |
| AC2(排他を失敗としない) | **部分PASS** | `flock -n -E 75` が実際に 75 を返すことは再現済み。**systemd が `failed` へ遷移しないことの確認は、root systemd が無く未実施** |
| AC3(収集器を壊さない) | PASS | 収集器の既存分岐(対応する Semaphore 行が無い場合)を**現物で読み、実行して**確認。exit 2(collection_errors)であって 3(内部エラー)ではない |
| AC4 / AC5(ドリフト検査) | **未検証** | quory 実機が要る。**配備後の初回でしか埋まらない** |
| AC6(reconcile の `--check`) | **未検証** | 同上 |
| AC7(既存の挙動不変) | PASS | 既存エントリに差分が無いことを `git diff` で確認、`--syntax-check` / lint / fixture とも通過 |

## 残っているもの

- **`enabled` / `active` の実値が未確認のまま、ドリフト検査のカタログへ入っている。** 根拠は間接証拠(このtimerが実際に月次で発火している事実)であり、`systemctl is-enabled` の直接照会ではない。**外れていれば配備後の初回で finding が立つ** — 誤検知として現れるので気づける
- **landing unit 自身はドリフト検査に入れていない。** `[Install]` を持たない OnFailure 専用ユニットの期待状態を実ホスト無しに確定できず、**推測で書くと AC5 を破る**ため(Implementer 判断、Coordinator 同意)
- **OQ2(`Persistent=false` のため quory 停止中に月初を跨ぐと、その月の更新自体が飛ぶ)は範囲外のまま。** 飛んだことを検出する手段は無い

## Auditor

**起動していない。** 記録は requirement・実装記録・独立レビュー・本ファイルと commit メッセージで再構成でき、**独立レビューが実ホスト無しでできる範囲を実行で確かめ、Medium を1件検出している**(その層は機能している)。

## 配備(2026-08-05 実施済み)

repo の変更だけでは何も起きない。**quory への反映は、Yoshinobu が Semaphore でボタンを2回押して完了した。**

1. `playbooks/semaphore_templates_setup.yml` のテンプレート — 今回足したカタログ項目を反映し、**`systemd_timers` のボタンを作る**
2. できた `systemd_timers` のテンプレート — `.service` の `OnFailure` と landing unit を quory へ配備する

**順序が逆にできない**(2のボタンは1を押すまで存在しない)。両方とも正常終了し、現物で確認した(下記)。

## 配備後に見つかった穴(2026-08-05、同日中に是正)

Yoshinobu が2つのボタンを押した直後、Coordinator が配備を確認しようとして気づいた。**新しく配備した2つ(捕捉スクリプトと landing unit)は、開発側から存在を確かめる手段が1つも無かった。**

- `ssh quory-investigate "unit-cat ansible-timer-failure-capture@.service"` → `denied: invalid unit for unit-cat`(dispatch のunit名は列挙式)
- `deployed-hash` の対応表(8件)にも無い
- ドリフト検査にも入っていない

**捕捉の仕組みそのものが「壊れても静か」な側にある。** 配備が欠けていても、次に実際の失敗が起きるまで誰も気づかない。

**是正**: スクリプトは `copy` 配備なので、`deployment_drift_check_files`(Tier 1、内容ハッシュ)へ加えた。これで存在も内容ドリフトも日次で見える。**landing unit 本体は `template` 配備のため Tier 2 で、同カタログの但し書きにより載せられない** — ここは穴のまま残る。

**原因はrequirementの側にある。** R2 を「timer を units へ加える」とだけ書き、**同じ案件で新規に配備する `copy` 成果物を対象に含めていなかった。** 実装側の見落としではない。

**確認できた配備の事実**(`ssh quory-investigate`):

- `unit-cat ansible-cert-renew-quory.service` に `OnFailure=` / `flock -n -E 75` / `SuccessExitStatus=75` が入っている
- `semaphore-query template-list` が 45件を返し、`id=47` に `SEMI-SAFE: Systemd timers` がある
