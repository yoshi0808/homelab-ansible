# Slack通知共通化 再レビュー

## 対象

- `docs/ai/prompts/core.md`
- `docs/ai/reviews/slack_notify/2026-06-08_001_requirement.md`
- `docs/ai/reviews/slack_notify/2026-06-09_008_implement.md`
- `docs/ai/prompts/ubuntu_vm_patch_policy.md`
- 現在の git diff

`inventories/vars/slack.yml` は Ansible Vault 形式で暗号化されていることだけを確認し、復号や秘密値の読取りは行っていない。

## Findings

追加の指摘なし。

## 前回指摘の確認

`2026-06-09_007_review.md` の指摘は解消されている。

- `inventories/vars/mail.yml` は現在も Git 管理対象として存在するが、Slack移行対象タスクからは参照しない、という実態に合う記述へ修正された。
- 文書タイトル、更新日、版番号が `v1.3`、`2026-06-09` に更新された。
- Ubuntu運用ポリシーの通知条件、チャンネル割り当て、best-effort 方針は実装と一致している。

## 要求仕様との整合

- 共通通知タスクは `roles/common_slack/tasks/notify.yml` に集約されている。
- `slack_channel` に応じて info / alerts / patches の Webhook を切り替えている。
- status ごとの色とアイコンは要求仕様どおりである。
- Webhook URLを含む変数読込・選択・token生成・送信タスクには `no_log: true` が設定されている。
- メール移行対象9ファイルから `community.general.mail` と `mail.yml` の読込みが削除されている。
- `proxmox_healthcheck` は WARNING / CRITICAL の場合のみ `#alerts` へ通知し、OKでは通知しない。
- 各 playbook / role の正常・異常チャンネルは要求仕様の割り当てと一致している。
- `proxmox_hw_check` は初回除外スコープのままである。

## 確認結果

実行した確認:

```bash
git diff --check

ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote \
  ansible-playbook --syntax-check -i inventories/homelab/hosts.yml <対象 playbook>

ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote \
  ansible-doc community.general.slack
```

結果:

- `git diff --check` は成功。
- 変更対象を呼び出す playbook 10本の syntax-check はすべて成功。
- インストール済み `community.general.slack` のドキュメントで、Webhook URL末尾の token 部分と attachments の利用方法を確認した。
- `inventories/vars/slack.yml` は Ansible Vault 形式である。
- Slackへの実送信は行っていない。

## 残余リスク

- 構文チェックでは Vault 内の変数名・Webhook到達性・実際のチャンネル配送までは確認できない。確定後の本番反映前に、各 Webhook へのテスト送信で確認する必要がある。
- `inventories/vars/slack.yml` と `roles/common_slack/` は現在未追跡ファイルである。commit 時にレビュー対象の新規ファイルとして含める必要がある。

## 判定

前回までの指摘はすべて解消されている。現在の差分について、コードレビュー上の blocking issue は見つからなかった。

新規ファイルをcommit対象へ含め、実環境でSlackテスト送信を確認することを前提に、この内容で確定してよいと判断する。

## Next step files

- `docs/ai/reviews/slack_notify/2026-06-08_001_requirement.md`
- `docs/ai/reviews/slack_notify/2026-06-09_008_implement.md`
- `docs/ai/reviews/slack_notify/2026-06-09_009_review.md`
