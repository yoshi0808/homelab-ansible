# Plan Review: Proxmox weekly full 件数超過ゲート

対象: `docs/ai/reviews/proxmox_weekly_full_count_gate/2026-09-20_001_requirement.md`

## Summary

件数超過をStep 2bでcluster全体のclean-skipへ変える方向は、現行のStep 2c mute、evacuate、apply、restoreの順序と整合し、§1の障害経路を閉じる。非ゴールの切り方も、件数によるpre-change停止だけを扱う案件として成立している。

ただし、再利用するforecast factの契約と受入試験の観測方法が未確定である。このままでは正常な「超過nodeなし」を判定材料欠落として落とす実装と、通知を実送信せずにAC2をPASS扱いする実装の双方が要件上成立するため、実装前の修正が必要である。

## Critical Issues

Critical findingはない。

## Major Issues

| # | File | Line | Issue | Severity |
|---|---|---:|---|---|
| 1 | `2026-09-20_001_requirement.md` | 49-50, 76, 103 | R2/R3/AC4が再利用する「予告側のfact」の名前・型・正常時の空値を定義していない。現物の`apply_forecast.yml`が持つnode判定結果は`_apply_forecast_node_warnings`という人向け文字列listであり、超過nodeが無い正常週には空listになる。一方R3はfactが「空」ならfailを要求するため、これをそのまま再利用するとR6の閾値以下経路が必ず停止する。逆に別factを追加する場合も、要求に出力契約がないため、通知文のparse、boolだけの保持、node/count/thresholdの再計算のどれでもR2を満たしたと解釈できる。forecast側が出す構造化factの名前・要素(`node`、`update_count`、`threshold`、超過判定)とhost scopeを定め、「定義済みの空listは超過なしとして有効」「未定義・型不正・必要フィールド欠落は判定不能」と区別する必要がある。 | Major |
| 2 | `2026-09-20_001_requirement.md` | 69-79 | AC1〜AC5の観測契約が、安全境界内の検証方法と一致していない。AC2は「Slackへ1通出る」と「本番Slackへ送らない」を同時に要求するが、現行`common_slack`は抑止時にdebugとcaptureを行って送信taskをskipするため、実送信件数では確認できない。AC1/AC3はfixture入力を受ける入口がないweekly full chain本体の実行を要求し、既存の同系統テストは対象taskの逐語コピーまたは直接includeで局所検証しており、import境界を含むE2Eではない。AC5は件数・statusを指定しておらず、超過時のclean-skipと閾値以下の現行check-mode完走のどちらを期待するか決まらない。ACを、(a)対象task/includeをそのまま使うfixtureでよい範囲、(b)通知includeの呼出し回数・渡したchannel/title/bodyと実送信taskのskipを証跡にすること、(c)超過・境界一致/以下・fact欠落・既存status各枝、(d)check modeの超過/非超過それぞれの期待値、に分けて書く必要がある。 | Major |

## Minor Issues

| # | File | Line | Issue | Severity |
|---|---|---:|---|---|
| 1 | `2026-09-20_001_requirement.md` | 15-19 | §1はmuteの根拠を`proxmox_evacuate_node.yml:35-57`の120分muteだけに置いているが、weekly fullはevacuateより前のStep 2c (`playbooks/proxmox_patch_weekly_full.yml:495-532`)で360分muteを張る。今回防ぐべき最初の副作用はこちらであり、AC1の`Set recovery mute`もStep 2cを指す。R1がStep 2bを指定しているため実装位置は結果として正しいが、因果モデルと引用を現物へ合わせるべきである。dry-run側の`major_upgrade_suspected`参照も現在は`main.yml:286`で、`:284`は条件分岐の開始行である。 | Minor |

## Open Questions

§7の2件は実装前にCoordinatorが方式を固定しなくてもよい。

1. 通知位置はStep 2b内とStep 11のskip分岐のどちらでも、R4、R6、修正後AC2の「通知意図が1回だけ」を満たせればよい。実装者が接続範囲の小さい方を選べる設計判断である。
2. R7は文脈非依存文言と呼出元による出し分けのどちらでも受入可能であり、P1の意味を変えない。

一方、Major 1のfact契約はオープンクエスチョンとして残さず、実装前に要求へ確定する必要がある。ここを実装者判断にするとR2/R3/R4の境界が変わる。

## Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---:|---|---|
| 1 | `2026-09-20_001_requirement.md` | 78 | AC6は値の重複だけでなく、Step 2bがdefaultsファイルを再lookupせずforecast factだけを参照することも静的に確認すると、R2の後半を直接観測できる。 | Test coverage |

## What Looks Good

- 現行Step 2bは`PATCH_READY`またはremoveなし`MAINTENANCE_REQUIRED`を通し、Step 2c muteの後にevacuate、apply、restoreへ進む。対象nodeのいずれかが超過した時点でStep 2bからcluster単位でskipするR1/R5は、件数超過時に副作用へ入る経路を閉じる。
- apply側の比較は`_sim_updates | length > proxmox_patch_apply_major_upgrade_threshold | int`、forecast側もnode別`update_count > _apply_forecast_major_upgrade_threshold`で、node単位と厳密不等号は一致している。
- 閾値の正本をapply role defaultsへ保ち、既存forecast判定を再利用する方針は重複を増やさない。新しいroleや通知経路は不要である。
- apply実処理失敗後のrestore、per-node除外、survey追加、2種類の`MAJOR_UPGRADE_DETECTED`統合、閾値変更を非ゴールにした切り分けは、本案件のpre-change件数ゲートを成立させたまま独立している。
- `common_slack`再利用が明記され、shell/commandへの新規変数埋込み、secretのログ出力、別の通知先追加を求める計画ではない。計画段階のセキュリティ上のblocking findingはない。
- 既存のclean-skip/fail各枝と閾値以下の通常経路を回帰対象に含める考え方は妥当である。

## Confirmed / Not Confirmed

確認済み:

- requirementが引用するStep 2b、Step 2c、evacuate/apply/restore/最終通知の現行接続。
- `apply_forecast.yml`の閾値読込、node単位比較、factのhost scopeと正常時の空list。
- apply roleの再dry-run件数判定と閾値defaults。
- `proxmox_operations_policy.md`のSB-032/SB-041、`ansible_test_safety_policy.md`のcheck-mode-native境界。
- 既存記録におけるジョブ#1164の経路。実ジョブは再観測していない。

未確認:

- 実ホスト、Semaphore本番ジョブ、本番Slackの挙動。Reviewerの権限境界に従い実行していない。
- template #7の当日実状態。requirement記載の観測を引き継がず、repo側カタログにsurvey定義が無いことだけを確認した。
- 実装差分がまだ無いため、具体的なfact探索・通知分岐・fixtureの成立性。

## Verdict

**Request Changes**

Major 1とMajor 2を要求書で解消してからImplementerへ渡す。blocking findingが残るためApproveしない。

---

## Re-review 1 (2026-09-20)

対象: 初回査読後に更新された`2026-09-20_001_requirement.md`。

### Summary

初回のMajor 1・Major 2・Minor 1は解消した。§5.1は正常な空listと判定不能を分離し、fact名・主要フィールド・host scopeを確定している。§6もE2Eを前提から外し、抑止下の通知証跡とcheck modeの超過・非超過を観測可能にした。

一方、新設するfactの**生成処理**を入力から検証するACがない。現在のAC1/AC4は生成済みの`_apply_forecast_apply_gate`をfixtureから直接与えるため、`apply_forecast.yml`が誤ったfactを作ってもconsumer側の試験だけが通る。中核となる判定の欠陥を出荷前に検出できないため、追加修正が必要である。

### Resolved Findings

| 初回finding | 判定 | 根拠 |
|---|---|---|
| Major 1: forecast fact契約が未定義 | 解消 | §5.1が`_apply_forecast_apply_gate`、`evaluated`、`threshold`、`blocking_nodes`、host scopeを定義し、空の`blocking_nodes`を正常値と明記した。R2/R3もこの契約を参照する。 |
| Major 2: ACの観測契約が安全な検証方法と不整合 | 解消 | §6は局所fixtureと静的確認を明記した。AC2はincludeへ渡す値と送信taskのskipを証跡とし、AC7はcheck modeの超過・非超過を分けた。AC8は2bからのdefaults再lookup禁止も観測する。 |
| Minor 1: 最初の副作用とfile:lineが不正確 | 解消 | §1はStep 2cの360分muteを最初の副作用として記載し、dry-run側参照を`:286`へ直した。 |

### Major Issues

| # | File | Line | Issue | Severity |
|---|---|---:|---|---|
| 3 | `2026-09-20_001_requirement.md` | 55-69, 89-97 | §5.1は`apply_forecast.yml`が新しい構造化factを生成する契約だが、対応するACがproducerを入力から検証しない。AC1は既に`blocking_nodes`が1件入ったfactを与え、AC4も既に空になったfactを与えるため、producerが常に空listを返す、`>=`で比較する、cluster合計を使う、thresholdやnode/countを誤って格納する実装でも、2b consumerのAC1/AC4と静的なAC8は通り得る。`unified_dryrun.node_summaries`を入力して`apply_forecast.yml`を直接includeし、閾値+1のnodeだけが`blocking_nodes`へ`node`/`update_count`付きで入り、閾値一致・以下は入らず、`threshold`がdefaults由来のint、`evaluated=true`になることを確認するACが必要である。あわせて§5.1の型契約をfail-closedで守るなら、AC5は現行4条件に加え、fact自体がdictでない、`blocking_nodes`がlistでない、`node`がstrでない、`update_count`がintでない場合も対象にするか、これらを判定不能条件から明示的に除外する必要がある。 | Major |

### Open Questions

§7の2件を実装者判断とする整理は妥当であり、実装前にCoordinatorが追加決定する必要はない。

### What Looks Good

- `_apply_forecast_node_warnings`を表示専用のまま残し、ゲート入力を構造化factへ分けたため、通知文のparseや判定ロジックの重複を避けられる。
- `evaluated=true`と空の`blocking_nodes`を別軸にしたことで、正常な非超過と未評価を区別できる。
- AC2は本番Slack非送信の境界を守りつつ、channel、呼出し回数、本文、完了titleの不在を観測できる。
- AC3はStep 2c以降の全接続を列挙し、count skip後にmute・evacuate・apply・healthcheck・restore・完了通知へ入らないことを確認できる。
- AC6はforecast factが生成されない`NO_UPDATES` / `BLOCKED`を含む既存分岐の回帰を固定しており、R3を既存clean-skipより先に無条件評価する誤実装を検出できる。

### Confirmed / Not Confirmed

確認済み:

- 更新された§1、R2/R3、§5.1、AC1〜AC9、§7、§11と、初回findingの対応。
- 現行`apply_forecast.yml`の入力が`unified_dryrun.node_summaries[*].update_count`、比較が厳密な`>`、閾値がapply role defaults由来であること。
- 現行Step 2b以降のskip flag参照箇所と、単一node guest-blocked通知の先例。

未確認:

- 実装差分と新factの実際の生成・探索・validation。まだ実装されていない。
- 実ホスト、Semaphore本番ジョブ、本番Slack。Reviewerの権限境界に従い実行していない。

### Verdict

**Request Changes**

初回finding 3件はcloseできる。新規Major 3として、producerの変換規則と残る型契約を直接観測するACを追加してからImplementerへ渡す。blocking findingが1件残るためApproveしない。

---

## Re-review 2 (2026-09-20)

対象: Re-review 1後に更新された`2026-09-20_001_requirement.md`。

### Summary

Major 3は解消した。AC10が`apply_forecast.yml`を直接includeし、元のnode別件数から構造化factを作るproducer責務を境界値込みで観測する。AC1/AC4は生成済みfactに対するconsumer責務、AC5はconsumerが停止すべき契約違反、AC8は閾値の正本と再lookup禁止をそれぞれ担当しており、責任の切れ目に未検証の判定ロジックは残っていない。

要素値の型をconsumerの停止条件から外す判断も、適用可否が`blocking_nodes`の空/非空だけで決まり、producerが型をAC10で保証する構成ならfail-closedを崩さない。consumerは適用判断に必要なcontainer・必須key・評価済み印・閾値型を引き続き検証する。

### Resolved Findings

| finding | 判定 | 根拠 |
|---|---|---|
| Major 3: producer変換規則と残る型契約をACが直接観測しない | 解消 | AC10が閾値+1 / 一致 / -1を同時入力し、node単位、厳密`>`、`blocking_nodes`の内容、defaults由来のint threshold、`evaluated=true`を実task includeで観測する。AC5はfact非dictと`blocking_nodes`非listを追加し、要素値の型はproducer責務とする境界を§5.1で明記した。 |

### Findings

Critical / Major / Minor findingはない。

### Producer / Consumer Boundary

- producer: AC10が`unified_dryrun.node_summaries`から`_apply_forecast_apply_gate`への変換を確認する。3つの境界入力により、cluster合計、`>=`、常時空list、誤ったnode選択を検出できる。
- handoff: §5.1がfact名、host scope、dict schemaを固定する。AC10の直接includeでproducer出力を、AC1/AC4で同schemaを読む2bの挙動を確認する。
- consumer: AC1が非空listのclean-skip、AC4が空listのproceed、AC5が適用判断に必要なschema違反の非ゼロ停止を確認する。
- source of truth: AC8がthreshold値の正本1箇所と、2bによる再lookup禁止を確認する。

### Type Exclusion Assessment

`node`非str / `update_count`非intをconsumer側の判定不能から除外してよい。2bがこの2値を比較や対象選定へ再利用せず、非空であることだけを適用停止に使う限り、値の型が壊れても危険側のproceedには倒れない。値は通知表示にだけ使われ、producerの正しい型はAC10で固定される。将来consumerがこれらの値を判定へ使う変更を行う場合は、その時点でvalidation対象へ昇格させる必要がある。

### Confirmed / Not Confirmed

確認済み:

- AC10と現行`apply_forecast.yml`の入力・比較・閾値取得箇所の対応。
- AC10、AC1/AC4、AC5、AC8がproducer、consumer、schema、正本を分担し、同じ判定を重複実装させないこと。
- §5.1の型除外が現在の2bの適用判断に影響しないこと。
- §7の実装者判断2件がP0/ACを曖昧にしないこと。

未確認:

- 実装差分とfixtureの実行結果。差分レビューとTester工程の対象である。
- 実ホスト、Semaphore本番ジョブ、本番Slack。Reviewerの権限境界に従い実行していない。
- chain全体のE2E。§6が明示的に対象外とし、producer/consumerを実task fixtureと静的接続確認へ分割している。

### Verdict

**Approve**

blocking findingはすべて解消した。Implementerへ渡してよい。
