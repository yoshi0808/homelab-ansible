# Operator Request Channel MVP — test result

作成: 2026-08-08 / Tester(subagent)
test plan: `2026-08-08_007_test_plan.md`
対象: 作業ツリーの未commit差分(`roles/operator_request_channel/`, `scripts/tests/operator_request_channel/`, `playbooks/operator_request_channel_{client,server}_setup.yml`, および既存7ファイルへの変更)

**このセッションでは実環境への配備を行っていない。** 以下の結果はすべてTester自身が実行して確認したものであり、実装記録・レビュー記録の記載を検証済みとして引き継いだものではない(記載と現物が一致することは個別に確認した)。

---

## 1. offline test(requirement §18.1)

```
python3 scripts/tests/operator_request_channel/run-tests.py -v
```

**実行結果: 288件全てPASS(0 failure, 0 error)。** 実行環境: ansy、Python 3.14.4。

§18.1の各項目とテストファイルの対応はtest_plan §2のとおりで、個別に該当ファイルを開いて実装(assert対象)を確認した。特に以下は内容まで読んで確認した。

- **DLP 12カテゴリの網羅**: `dlp-rules.json`の`category`一覧(pem_private_key / slack_credential ×2 / semaphore_api_token / bearer_or_jwt ×2 / generic_secret_keyvalue / vault_plaintext / credential_url / env_dump / proc_environ / private_ipv4 / ipv6_ula_link_local / high_entropy)を`grep`で直接確認し、requirement §9.3の最低限12種と対応することを確認した。
- **拒否出力の検出値非出力**: `dlp.py`の`Finding`が`category`/`rule_id`/`pointer`のみを保持することをコードで確認し、`test_dlp.py`の`_assert_category_detected`が`json.dumps(findings)`へ元値が含まれないことを毎回検証していることを確認した。
- **既存`dev-investigate`の非回帰**は`test_dispatcher.py`の`ExistingOperationsNonRegressionTests`(disk/load/failed/ports/journal-system/deployed-hash)がansy上でローカル実行して確認するものであり、pass済み。Testerはこれに加えて独立の非回帰ハーネスを別途実行した(§5)。
- **channel disable時の非対称性**: `test_channel_disabled_blocks_submit`が`channel_enabled: false`でsubmitが拒否されることを確認。既存25操作側は`oprc-receive`/`operator-channel`/config.jsonのいずれも参照しない設計であることを`roles/dev_investigate/files/recovery-investigate-dispatch-quory.sh`を読んで確認した(既存armのコードパスに`channel_enabled`という文字列が一切現れない)。

**判定: PASS**(§18.1、実行し通ることを確認)。

---

## 2. integration test(requirement §18.2)

`test_integration_roundtrip.py`(offlineランナーに含まれる、上記288件の一部)を個別に読み、次を確認した。

| 項目 | 確認 |
|---|---|
| submit→accept→OPRES作成→pull往復 | `test_submit_accept_reply_pull` |
| standalone DEVREQ作成とpull | `test_standalone_devreq_reaches_ansy` |
| 4検査点すべてで同一fixtureが拒否 | `test_dlp_blocks_the_same_fixture_at_all_four_inspection_points`(送信前・受入前・持ち出し前・取り込み前の4箇所を個別に踏む実装であることをコードで確認) |
| 再取得でmessageが変化しない | `test_submit_accept_reply_pull`内の`got_again`アサーション |
| channel経路からSemaphore/systemctl/sudo/Git/任意pathへ到達不可 | `ReachabilityBoundaryTests`(`oprc-receive`)/`R1BoundaryTests`(`operator-channel`)がモジュールのimport一覧に`subprocess`/`os.system`等が無いことを検査。Testerも両ファイルを`grep -n "^import\|^from"`で直接確認し、`oprc.*`・`json`・`sys`・標準ライブラリの安全なモジュールのみであることを確認した |

**判定: PASS**(§18.2、実行し通ることを確認。ただし実SSHは経由していない — plan §6が明記する設計どおりで、requirementもこの段階では実ホスト接続を求めていない)。

---

## 3. plan §6 T-A〜T-Gの個別確認

| # | 結果 | 確認手段 |
|---|---|---|
| T-A | PASS | `test_dlp.py`/`test_integration_roundtrip.py`(offlineランナーに含まれ実行済み)。4つの入口が共通の`config.load_and_verify_dlp_ruleset()`→`dlp.scan()`を通ることを`oprc-receive`/`operator-channel-client`/`operator-channel`のソースを読んで確認した |
| T-B | PASS | `test_config.py`(`LoadAndVerifyDlpRulesetTests`)、`test_dlp.py`(`TimeoutTests`/`ParseRulesetTests`)。6条件(ruleset改竄・schema欠損・engine version不一致・timeout・上限超過・判定不能)がすべて例外→非ゼロ終了になることをコードとテストの両方で確認した |
| T-C | PASS | `test_dlp.py ParseRulesetTests.test_real_ruleset_has_all_required_categories`が期待12カテゴリの集合とruleset実物の`category`集合の完全一致を機械的に検証。`NoFalsePositiveTests`でrequest_id/conversation_id/repo_commitの誤拒否が無いことも確認 |
| T-D | PASS | 下記§4で個別に詳述 |
| T-E | PASS | `test_store.py ReadMessageConsistencyTests`(5条件)、`test_lifecycle.py MarkExpiredIfNeededConcurrencyTests`(同時実行3件、`_events_file_lock`による直列化)を実行し確認。review Suggestion 2の是正(`append_event_locked`経由への統一)が実際にコードへ入っていることを`lifecycle.py`を読んで確認した(§6参照) |
| T-F | PASS | `test_config.py TimeSyncTests`、`test_entrypoint_oprc_receive.py`/`test_entrypoint_operator_channel.py`の`test_time_not_synchronised_blocks_*`・`test_configured_max_clock_offset_seconds_reaches_assert_time_synced`。review Suggestion 1の是正(`cfg.get("max_clock_offset_seconds", 60.0)`が実際に呼び出し箇所へ渡っていること)を`oprc-receive`/`operator-channel`を`grep`して確認した(§6参照) |
| T-G | PASS | **repoに保存された再実行可能なテストが無かったため、Tester自身がlocalhost decoyで再検証した**(下記§4.3) |

---

## 4. T-D・T-Gの詳細(新規に構築した検証)

### 4.1 T-D — R-1の境界

`roles/dev_investigate/files/recovery-investigate-dispatch-quory.sh`を全文読み、次を確認した。

- 新設4本の`case` arm(`operator-request-submit`/`operator-outbound-list`/`operator-message-get`/`operator-request-status`)に、削除・更新に相当する語彙が1つも無い(4本とも`exec`一発で`oprc-receive`の`submit`/`outbound-list`/`message-get`/`request-status`へ委譲するのみ)。
- `oprc-receive`・`operator-channel`のソース全文を読み、message本文を書き換える・削除するコードパスが存在しないことを確認した(到達できるstore操作は`create_message`(衝突時拒否)と`append_event`/`append_event_locked`(状態機械で遷移を強制)のみ)。

### 4.2 T-D — sudoers不在

`ssh quory-investigate "forced-command-keys"`で`dev-investigate`のauthorized_keysが現在も1エントリ(既存のforced command)のみであることを実測した(下記§5)。sudoersファイルの実在確認は本セッションの到達手段(read-only forced command)のカタログに`/etc/sudoers.d/`を読む操作が含まれておらず確認できない — **未確認のまま記録する**(§7未実施項目)。設計上の担保(§3.3のドリフト検査`deployment_drift_check_absent_paths`)はコードとして存在することをT-Gで確認したが、実quory上での実行結果は配備後まで確認できない。

**T-Dの判定: PASS(forced command層の境界について)。** OS identity `dev-investigate`自体が侵害された場合の完全なimmutabilityは、plan §2.3.1が明記するとおり保証対象外であり、本テストの対象外として扱う。

### 4.3 T-G — ドリフト検査新設3クラス(独立再検証)

実装記録(`2026-08-08_005_implement_channel.md` §6)は「localhost decoy playbookで7ケース確認した」と記載しているが、そのplaybookはリポジトリにもTester到達可能な場所にも残っていなかった(`find`で非存在を確認)。**記載を検証済みとして引き継がず、Tester自身が同じ検証を独立に再構築した。**

手順:

1. `roles/deployment_drift_check/tasks/collect.yml`(新設3クラスの収集task)と`tasks/evaluate.yml`(判定Jinjaロジック)を読んだ。
2. `evaluate.yml`の該当3ブロック(`path_permissions`/`sudoers_absent`/`dispatch_allowlist`のfinding生成ロジック)を一字一句そのまま抜き出し、`/tmp/claude-1000/.../scratchpad/verify_drift_check_t_g.yml`(`hosts: localhost, connection: local`)へ埋め込んだ。
3. fixtureデータ(owner/group/mode一致1件・不一致3件、sudoers該当なし1件・該当あり1件、probeが正しく拒否1件・誤って許可1件)を`vars:`として直接与え、実行した。

結果:

```
paths_findings: 3   (owner不一致1・mode不一致1・path欠落1。一致した1件はfinding無し)
sudoers_findings: 1  (該当ファイルがある1件のみ。クリーンな1件はfinding無し)
dispatch_findings: 1 (rc=0の誤許可1件のみ。rc=1の正しい拒否はfinding無し)
```

期待値(paths=3, sudoers=1, dispatch_probes=1)と一致し、`assert`タスクがPASSした。**実ホストは一切変更していない**(`hosts: localhost, connection: local`)。

**T-Gの判定: PASS。** ただし、この検証は判定ロジック(Jinja)そのものの正しさを確認したものであり、実quory上での`stat`/`find`/dispatcher probe実行(収集task側)が正しく動くことは配備後にしか確認できない(§7未実施項目)。

---

## 5. 既存`dev-investigate` 25操作の非回帰(requirement §10.3、§19完了条件19番目)

### 5.1 実quoryでの現状確認(read-only)

```
ssh quory-investigate "users"
ssh quory-investigate "forced-command-keys"
ssh quory-investigate "deployed-hash investigate-dispatch-quory"
ssh quory-investigate "failed"
ssh quory-investigate "operator-request-submit"
```

結果:

- `users`: 6ユーザー(yoshi/ann/trigger/recovery-exec/incident-inspect/dev-investigate)、plan §1の記録と一致。
- `forced-command-keys`: `1: command=/usr/local/sbin/recovery-investigate-dispatch-quory.sh comment=claude@homelab-ansible` / `total: 1 entries` — **鍵は増えていない**。
- `deployed-hash investigate-dispatch-quory`: `c0721210a411bd6b724f5497ee4b1b6dd4fa3ef6d8a9a354612ec92669a16b42` — plan §1が記録した配備前ハッシュと一致。**quoryは現時点でまだ旧dispatcher(25操作)のまま**であることを確認した(このセッションでの変更が本番へ漏れていないことの裏付けでもある)。
- `failed`: `rc=0`、正常応答。
- `operator-request-submit`(新操作): `denied: unknown command 'operator-request-submit'`、`rc=1` — 予期どおり(旧dispatcherは未知の語彙として拒否する)。

### 5.2 ローカル非回帰ハーネス(新旧dispatcherスクリプトの比較)

quory上で変更後dispatcherを動かす手段が無いため、`git show HEAD:...`(変更前)と作業ツリー(変更後)の2バージョンをansy上へ複製し、同一の`SSH_ORIGINAL_COMMAND`を与えて比較するハーネスを作成し実行した(`/tmp/claude-1000/.../scratchpad/dispatch-nonregression.sh`)。

対象: 既存25操作の正常系・異常系(arity違反、不正enum、余分な引数を含む)45ケース + 新設4操作の境界確認9ケース。

結果: **全ケースで終了コードが一致した。** stdout/stderrに現れた差異はいずれも以下のいずれかで、dispatchロジックの変更によるものではないことを個別に確認した。

- bash自身が出す`スクリプトパス:行番号:`という診断行の行番号(新版はコメント・新armの追加で全体の行数が増えるため、同一の`exec`失敗でも指す行番号だけがずれる。実際のメッセージ本文・rcは同一)。
- `memory`操作で1回だけstdoutが不一致と判定されたが、直接再実行(`diff <(...old...) <(...new...)`)すると差分は無く、初回のずれは2回の呼び出し間の実時間経過によるメモリ使用量の微小な変動(`free -h`表示値)であり、スクリプトの版に依存しないことを確認した。

新設4操作については、**旧dispatcherが`unknown command`で一律拒否し、新dispatcherが`oprc-receive`へ正しくルーティングする(ansy上に`oprc-receive`が実在しないため`No such file or directory`で失敗するが、それは配備が完了していないためであって、dispatch層のルーティング自体は機能している)ことを確認した。不正なrequest_id形式は新dispatcher側の正規表現検証で`oprc-receive`へ到達する前に拒否されることも確認した(`operator-outbound-list not-a-request-id` → `denied: invalid cursor for operator-outbound-list`)。

**判定: PASS。** `git diff --stat`(+58行・-0行)とあわせ、既存25操作のロジックが1文字も変わっていないことを構造(diff)・実測(ローカルハーネス)・本番現状確認(§5.1)の3方向から裏付けた。

---

## 6. review findingsの是正確認(Suggestion 1・2)

`2026-08-08_006_review.md`のSuggestion 1(死んだ設定`max_clock_offset_seconds`)とSuggestion 2(`expired`イベント二重付与によるStoreInconsistent)は、Implementer Bの記録が「是正済み」と主張していたものであり、Testerが独立にコードで確認した。

- Suggestion 1: `oprc-receive`(101行目)・`operator-channel`(231行目)の両方で`config.assert_time_synced(max_offset_seconds=cfg.get("max_clock_offset_seconds", 60.0))`が実際に呼ばれていることを`grep`で確認した(死んだ設定ではなくなっている)。
- Suggestion 2: `oprc/lifecycle.py`に`_events_file_lock()`(`fcntl.flock`)と`append_event_locked()`が実装され、`mark_expired_if_needed()`と既存request遷移追記の両方がこのロックを経由することをコードで確認した。`test_lifecycle.py`の`MarkExpiredIfNeededConcurrencyTests`(3件、同時実行)がoffline testスイートの一部として実行され、PASSしていることを確認した(§1の288件に含まれる)。

**両Suggestionとも是正済みであることを確認した(FAIL無し)。**

---

## 7. requirement §19(完了条件28項目)の判定

実配備を要する項目は**未判定のまま残す**(合格・不合格いずれにも倒さない)。実配備を要さない項目はTesterが実測してPASS/FAILを判定した。

| # | 完了条件 | 判定 | 根拠 |
|---|---|---|---|
| 1 | ansyから既存SSH forced command経由でOPREQを登録できる | **未判定(配備後に検証)** | quoryのdispatcherが旧版のまま(§5.1)であり、新armが本番に存在しない |
| 2 | 新規SSH鍵、新規SSHユーザーおよびauthorized_keys変更を行っていない | **PASS** | `forced-command-keys`実測で1エントリのまま(§5.1)。`roles/dev_investigate/tasks/main.yml`・`templates/authorized_keys.j2`が差分に含まれないことを`git status`で確認 |
| 3 | userがrequest IDを指定してquoryでOperatorを起動できる | **未判定(配備後に検証)** | Operator local CLI(`operator-channel`)がquoryへ未配備 |
| 4 | Operatorがpending requestを一覧・取得・accept／rejectできる | **未判定(配備後に検証)** | 同上。offline/integration testでは同等の機構がPASSしている(§2参照)が、実quory上での確認ではない |
| 5 | OperatorがOPRESを作成できる | **未判定(配備後に検証)** | 同上 |
| 6 | Operatorがstandalone DEVREQおよび返信DEVREQを作成できる | **未判定(配備後に検証)** | 同上 |
| 7 | ansyがOPRES／DEVREQをread-onlyで取得できる | **未判定(配備後に検証)** | 同上 |
| 8 | `in_reply_to`と`conversation_id`でmessage関係を追跡できる | **PASS** | test_schema.py/test_integration_roundtrip.py実行で確認(データモデルの性質であり実配備を要しない) |
| 9 | request本文がimmutableで、状態がappend-only eventとして記録される | **PASS** | test_store.py(atomic create、上書き不可)実行で確認 |
| 10 | request IDと内容hashで同一性を確認できる | **PASS** | test_canonical.py/test_store.py(hash再照合)実行で確認 |
| 11 | 4検査点が同じDLP実装とrulesetを使用している | **PASS** | T-A(§3)で確認 |
| 12 | engine version／ruleset hash不一致時にfail closedとなる | **PASS** | T-B(§3)で確認 |
| 13 | 疑似secretを含むpayloadが4検査点すべてで拒否される | **PASS** | `test_dlp_blocks_the_same_fixture_at_all_four_inspection_points`実行で確認 |
| 14 | private IPおよび対象IPv6を含むpayloadが拒否される | **PASS** | test_dlp.py(`test_private_ipv4`/`test_ipv6_ula`/`test_ipv6_link_local`)実行で確認 |
| 15 | 拒否ログ、stdout、stderrおよび監査に検出値が残らない | **PASS** | test_dlp.py実行 + `Finding`のフィールド構成をコードで確認 |
| 16 | 生ログを両環境間で直接転送しない | **PASS** | schemaの`evidence_references`がopaque ID(`kind`+`id`)のみを許可し生ログ本体のフィールドが存在しないことをschema JSONを読んで確認 |
| 17 | 任意path、任意command、SQLまたはshell式を入力できない | **PASS** | `ReachabilityBoundaryTests`/`R1BoundaryTests`実行 + dispatcherのarity/正規表現検証をコードで確認 |
| 18 | この経路からSemaphore、schedule、service、対象ホスト、Gitを変更できない | **PASS** | 同上(`subprocess`/`os.system`等の不在をコードで確認) |
| 19 | 既存`dev-investigate` read-only操作が回帰していない | **PASS** | §5で確認 |
| 20 | client setupがansyだけを変更する | **PASS** | `--list-hosts`(hosts(1): ansy)+ `--check --diff`実行(§8)で確認 |
| 21 | server setupがquoryだけを変更する | **PASS(構造確認)** | `--list-hosts`(hosts(1): quory)+ `tasks/server.yml`/`common.yml`を読み、対象ホスト外への操作(delegate_to等)が無いことを確認。**実quory上での`--check`実行はquoryへの到達手段が無いため行っていない**(§8) |
| 22 | 既存の`SEMI-SAFE: Semaphore templates setup`によりserver setup templateを登録・更新できる | **未判定(配備後に検証)** | Semaphoreへのreconcile実行は本セッションの範囲外 |
| 23 | 登録されたserver setup templateがquoryだけを対象とし、scheduleを持たない | **未判定(配備後に検証)** | 未登録のため確認不能。カタログ定義(`roles/semaphore_templates/defaults/main.yml`)自体にscheduleを表現するフィールドが無いことは構造として確認済みだが、「登録された」状態そのものの確認は配備後 |
| 24 | template登録だけではserverを配備せず、Yoshinobuによるserver setup templateの明示実行を必要とする | **PASS** | server setup playbookをtimer/git pull/template登録のいずれからも自動起動する仕組みが無いことをコードで確認(requirement §17の除外事項と設計が一致) |
| 25 | quoryでgit取得後、YoshinobuがSemaphore UIからserver setupを明示実行して配備できる | **未判定(配備後に検証)** | 実配備そのもの |
| 26 | 配備済みartifactとRepoのhashを照合できる | **未判定(配備後に検証)** | ドリフト検査の判定ロジックはT-Gで独立検証しPASSしたが、実quory上の配備済みartifactを対象とした実行は配備後 |
| 27 | channelだけを無効化して既存調査経路を維持できる | **PASS(機構確認)** | `channel_enabled: false`でchannel操作が拒否されること(`test_channel_disabled_blocks_submit`)、既存dispatcher armのコードパスに`channel_enabled`という語が一切出現しないこと(構造的に無関係)を確認。**実quory上でのトグル実測は配備後** |
| 28 | 最終成果物が既存Git検査に合格する | **PASS** | §9参照 |

**集計: PASS 18件 / 未判定(配備後) 10件 / FAIL 0件。**

---

## 8. dry-run(`--check`)実行記録

```
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local \
ANSIBLE_REMOTE_TEMP='/tmp/ansible-remote-$USER' \
scripts/safe-ansible-check.sh playbooks/operator_request_channel_client_setup.yml --check --diff
```

結果: `ok=5 changed=0 unreachable=0 failed=0 skipped=8`。全destructive taskが`skipping`となり、`check-mode-native`のゲート(`when: not ansible_check_mode`、TS-014)が実際に機能していることを確認した。ansy自身への変更は発生していない。

`playbooks/operator_request_channel_server_setup.yml`は`hosts: quory`であり、このセッションにはquoryへの通常SSH到達手段が無いため(read-only forced commandのみ)、`--check`は実行していない。代わりに次を実行した。

- `ansible-playbook playbooks/operator_request_channel_server_setup.yml --syntax-check` → エラー無し。
- `ansible-playbook playbooks/operator_request_channel_server_setup.yml --list-hosts` → `hosts (1): quory`のみ。
- `roles/operator_request_channel/tasks/server.yml`・`common.yml`・`spool_acl_entry.yml`を全文読み、全destructive taskに`when: not ansible_check_mode`(TS-014)が付いていることを確認した。

`ansible-lint`(0 failure, 0 warning)、`scripts/check-tester-gate.sh`(`OK (54 playbooks)`)、`yaml.safe_load`による全YAML構文検査(全件OK)もTesterが独立に再実行して確認した。

---

## 9. Git検査(requirement §18.3)

- `gitleaks detect --no-git`を対象範囲(`roles/operator_request_channel/`、`scripts/tests/operator_request_channel/`、両playbook、既存差分7ファイル)へ個別に実行し、**すべて`no leaks found`**。リポジトリ全体を対象にした場合に検出される4件は`reports/proxmox-backup-verify/`配下の無関係な既存ファイルであり、本差分のスコープ外であることを確認した(このファイルは`git status`にも出ていない = 差分に含まれない)。
- IPv4リテラル検査: `scripts/git-pre-commit-check.sh`と同じ正規表現(`([0-9]{1,3}\.){3}[0-9]{1,3}`、127.0.0.1/0.0.0.0/255.255.255.255除外)を、対象範囲の全ファイル(49ファイル)へ適用し、**ヒット0件**。
- `git status --short`で`__pycache__`が差分に含まれないことを確認し、`git check-ignore -v`で`.gitignore`の`__pycache__/`ルールが実際に効いていることを確認した。
- `.gitignore`の追加行(`!scripts/tests/operator_request_channel/`)が意図どおり機能し、テストディレクトリが`git status`の`Untracked files`に現れる(=`git add`すれば追跡対象になる)ことを確認した。

**判定: PASS。**

---

## 10. 未実施項目とその理由

| 項目 | 理由 |
|---|---|
| requirement §19の完了条件のうち実配備を要する10項目(#1, 3-7, 22-23, 25-26) | 本セッションでは実環境への配備を行わない(Yoshinobu指示、2026-08-08)。配備後、Coordinatorが別途検証すること |
| server setup playbookの`--check`を実quoryに対して実行すること | quoryへは既存read-only forced commandしか到達手段が無い。通常SSHでの`--check`実行はこのセッションの許可範囲外 |
| `/etc/sudoers.d/`配下の実quory上の実態確認 | 到達可能なcheckカタログ(`docs/ai/reviews/dev_prod_boundary/2026-08-03_008_phase3_check_catalog.md`)に該当操作が無い。設計上の担保(ドリフト検査)はT-Gで機構として確認したが、実行結果は未確認 |
| ドリフト検査(新設3クラス含む全体)の実quory実行 | 同上。ロジックはlocalhost decoyで独立検証(§4.3) |
| `atomic create`のマルチプロセス実競合(OSレベル) | 実装記録(`_004_implement_core.md` §6)が既に「単一プロセス内の逐次呼び出しでの検証に留まる」と明記している既知の制約。Testerもこれを追試する追加手段を持たない(`os.link`のOS原子性への依拠は本テストの範囲外) |

## 11. 残存リスク

1. **R-1(inbox本文のunlink)**: `dev-investigate`というOS identity自体が侵害された場合、そのidentityが提出したOPREQ本文をunlinkできる。plan §2.3.1が明記する既知の制約であり、forced command層の境界はT-Dで確認済みだが、OS identity侵害自体への対策は無い。
2. **`check_capacity()`→`create_message()`間の競合**: 実装記録(`_004_implement_core.md` §6-4)が既知の制約として明記し、is_004でも_005でも是正されていない。総容量をわずかに超過させる可能性が理論上残る。今回のTester作業でも追加の検証手段を持たない。
3. **配備手順の実地確認が未了**: `git pull`後の`dev_investigate_setup.yml`再実行(review Critical 1の是正、plan §5)を含む配備順序全体は、実際にquoryへ適用してみるまで机上の計画に留まる。特に「新4armが本当に旧25armと同じdispatcherファイル内で共存し、`playbooks/dev_investigate_setup.yml`の再実行後も`operator_request_channel_server_setup.yml`が配備した`oprc-receive`を正しく`exec`できるか」は配備後にしか確認できない。
4. **`/etc/sudoers.d/`の実態は今回未確認**。R-1の受容根拠の1つ(sudoが存在しない)は、ドリフト検査の判定ロジック自体はT-Gで正しく動くことを確認したが、実quory上でその収集task(`find /etc/sudoers.d -mindepth 1 ...`)がbecome:trueで正しく実行できるかは未検証。

## 12. 総括

| 分類 | 件数 |
|---|---|
| offline test | 288件PASS(0 fail) |
| integration test(offlineランナー内) | 上記に含まれる、個別確認PASS |
| T-A〜T-G | 7件全てPASS |
| requirement §19(実配備不要18項目) | 全てPASS(FAIL無し) |
| requirement §19(実配備要10項目) | 未判定のまま(配備後にCoordinator/Testerが別途検証) |
| Git検査 | PASS |

**FAILは0件。** review `2026-08-08_006_review.md`のSuggestion 1・2はコードで是正済みであることを確認した。commit承認可否の判断はCoordinatorに委ねる。
