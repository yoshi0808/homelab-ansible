# OPREQ送信と回答受領の安定化 test_plan / test_result

対象: 001要件、002実装記録、現在の未commit差分(`scripts/oprc-submit.sh`変更、`scripts/oprc-prepare.py`/`scripts/oprc-replies.py`/`scripts/oprc-sync-check.py`新規、`scripts/tests/oprc_submit/`のtest_helpers.py新規・run-tests.sh変更、`docs/ai/context/operations/agent-messaging.md`・`operator-request-channel.md`・`docs/ai/roles/coordinator.md`)。すべて`/tmp`のスタブ・fixtureのみで実施し、検証後に削除した。実ホスト・外部agmsg本体・本番spool・本番通知へは一切接続していない。Reviewerの003追加は競合として扱っていない(本Testerは003を参照していない)。

## 計画

1. 静的検証(既存スタブ・unittestの独立再実行)。
2. 独立fixtureで、既存テストが直接カバーしていない角度を追加検証する: 作成時のserver-assigned field拒否とreply-on-OPREQ拒否、同期判定の追加ケース(未来時刻)、両team確認とpagination・除外検知・不正cursorの実subprocessによるend-to-end確認(既存test_helpers.pyはpython mockベースのため、実スタブスクリプトでの再現を別途行う)。

## 静的検証(独立再実行)

- `bash scripts/tests/oprc_submit/run-tests.sh`: **19/19 PASS**。
- `python3 -m unittest discover -s scripts/tests/oprc_submit -p test_helpers.py -v`: **6/6 PASS**。
- `python3 scripts/check-doc-consistency.py`: 3チェックともOK。
- `git diff --check`: rc0。

## 動的検証(独立fixture、実subprocessスタブ)

### 作成・拒否(`oprc-prepare.py`)

| ケース | 入力 | 結果 |
|---|---|---|
| server-assigned `request_id`を投入 | `{"purpose":"x","request_id":"req-..."}` | PASS: rc2、`--output`ファイル未作成、`OPREQ schema invalid`のみ出力(値は非出力) |
| server-assigned `created_at`を投入 | `{"purpose":"x","created_at":"2020-..."}` | PASS: rc2、出力ファイル未作成 |
| 新規OPREQに`in_reply_to`を投入(`opreq_must_not_reply`) | `{"purpose":"x","in_reply_to":"req-..."}` | PASS: rc2、出力ファイル未作成 |
| 正常系(対照) | `{"purpose":"normal fixture request"}` | PASS: rc0、`type`/`schema_version`が既定補完されたJSONが作成される |

既存`test_helpers.py`の`test_invalid_or_secret_request_never_creates_output`(type不一致・`source`混入・未知key・DLP秘密情報)とあわせて、requirement §6.1の3つのserver-assigned fields(`request_id`/`source`/`created_at`)すべてと、reply関係の禁則をこのTesterの独立fixtureで実測した。

### 同期判定(`oprc-sync-check.py` / `oprc-submit.sh`)

既存`run-tests.sh`の4モード(stale/old/nosync/fail)を独立再実行しPASS(19件中に含む)。追加で、既存fixtureに無い「同期時刻が未来」ケースを`oprc-submit.sh`のend-to-endで確認した。

- 独自stubの`remote.sh`が`last successful sync 2099-01-01T00:00:00.000Z`を返す状態で`oprc-submit.sh`を実行 → **PASS**: `sync_unobservable: 同期時刻が未来です。時計を確認してください。`でrc4、`OPRC_CLIENT`のsubmitへ到達せず、agmsg通知も送られない(登録・通知とも0件)。

### 両team確認・既読OPRES取得・一覧不完全時の非正常扱い(`oprc-replies.py`)

既存`test_helpers.py`はpython `unittest.mock`で`run()`を差し替える構成のため、実subprocess(独自stubスクリプト)でも同じ契約が成立するかを別途確認した。

| ケース | 結果 |
|---|---|
| 片team(`homelab-ops`)のinbox.shが失敗 | PASS: `homelab`は正常表示、`homelab-ops`は`inbox unavailable`と明示され、**それでも処理を止めずpending IDのreply取得へ進む**。最終rc=2(失敗を握り潰さない) |
| pagination(1ページ目空+`next_cursor`、2ページ目に対象item) | PASS: 2ページとも走査し、対象replyを`get`で取得できた |
| 取得したreplyの`state`が`submitted`(既読相当) | PASS: 既読・submittedによるフィルタは無く、そのまま返る(`replies()`はstateを見ていない) |
| 対象IDに該当replyが1件も無い | PASS: rc0、「一覧に対応回答なし（**未回答の断定ではない**）」と表示され、断定しない文言になっている |
| `excluded_count != 0`(一覧が不完全) | PASS: rc2、「OPRES reconciliation incomplete; do not report unanswered」で失敗させ、未回答と報告しない |
| 不正な`next_cursor`(id形式契約に不適合な値) | PASS: rc2、同じくreconciliation incompleteとして失敗(fail-closed) |
| pending IDを渡さない場合 | PASS: rc0、両teamのinbox確認は行われる(reply照合自体は行われない) |

## 未確認事項(範囲外/Not Run)

- `operator-channel-client`実物・実spool・実agmsg(homelab/homelab-ops本番team)への接続はTester権限外/本要件の非ゴールのためNot Run。すべてスタブで代替した。
- `docs/ai/context/operations/agent-messaging.md`・`operator-request-channel.md`・`docs/ai/roles/coordinator.md`の文面差分は読解のみ行い、記述内容と実装の一致は本Testerの検証範囲外(規範文書の整合はReviewer/Coordinatorの領分)として個別の付き合わせは行っていない。
- sandbox内外の実測(要件2の背景事実)そのものの再現はTester環境では不可能であり、コードが「観測不能」を「停止」と区別する分岐を持つことのみを確認した。

## Verdict

**PASS。** 静的検証(19+6件)を独立再実行し、要件の3点(取得不能を未回答扱いしない、観測不能/停止/期限超過の区別、作成時のtype/server-assigned fields検査)を、既存テストがカバーしない角度(server-assigned fieldsの個別分離、未来時刻の同期判定、実subprocessでのboth-team/pagination/excluded_count/不正cursor)を含めて独立fixtureで確認した。対象実装は無変更、scratchは検証後にすべて削除した。
