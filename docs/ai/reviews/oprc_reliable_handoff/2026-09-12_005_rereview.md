# Code Review (再レビュー): OPREQ送信と回答受領の安定化 — 003 findings対応確認

### Summary
003で挙げたCritical 1件・Suggestion 2件はすべて解消を自分で再現・再測定して確認した。差分のスコープ・非ゴールは001から変わっておらず、`run-tests.sh`(19件)・新規unittest(6件、標準経路へ接続済み)・`check-doc-consistency.py`(3件)・`git diff --check`をすべて自分で再実行しPASSした。テスト用のOPREQ登録・実ホスト・外部agmsg本体への接続は行っていない。

### Critical Issues
なし。003の指摘(`docs/ai/roles/coordinator.md`の「正規の権限昇格」表現)は解消を確認した。

- `docs/ai/roles/coordinator.md`(該当節)、`docs/ai/context/operations/agent-messaging.md`(1行追記部)とも、対象コードのメッセージ文言(`scripts/oprc-submit.sh`/`scripts/oprc-sync-check.py`)と同じ「sandbox外実行」へ語彙を統一した。`grep -n 権限昇格`を対象ファイル一式に対して実行し、残存が無いことを確認した。
- coordinator.mdの文言に「**同じOSユーザー・同じ認証情報のまま**」「**これはOSユーザーの切替や新しい接続権限の取得ではなく、EXEC-082の禁止は維持する**」が明記され、EXEC-082(実行identityを昇格しない)との関係が文面上でも明確になった。
- 「`exec_command`の`sandbox_permissions=require_escalated`」という具体的機構は、本案件以前から本リポジトリで実際に使われてきたもの(`docs/ai/memory/incidents/2026-09-10_reviewer-spawn-permission-block.md`、`docs/ai/reviews/proxmox_storage_monthly/2026-09-10_006_implementation.md`・`2026-09-11_012_readiness.md`に先例あり、いずれも「承認拒否されうる」「境界設定は変更していない」という同じ性質で使われている)であることをgrepで確認した。新設の概念ではなく、既存の承認ゲート付き機構を指しており、識別子昇格とは別軸である。
- 002実装記録に「実測ではexec_commandのsandbox_permissions=require_escalatedを使用し、ユーザー切替・sudo・鍵取得は行っていない」との記録があり、実際の行動記録としても矛盾しない。

### Suggestions
どちらも解消済みで再掲しない。

- (旧#1: テスト配線)`scripts/tests/oprc_submit/run-tests.sh`が末尾で`python3 -m unittest discover -s ... -p "test_*.py" -v`を実行するよう変更され、`bash scripts/tests/oprc_submit/run-tests.sh`という標準の1コマンドでshell 19件+unittest 6件の計25件が走ることを自分で再実行し確認した(exit 0)。sync系のshellアサーションも`expected_rc`(`old`→2、それ以外→4)へ厳密化されており、AC2の区別を機械的に検知できるようになった。
- (旧#2: DLP詳細の握り潰し)`scripts/oprc-prepare.py`に専用の`DlpBlocked`例外が追加され、`main()`はこれを他の例外より先に捕捉して`rule_id at pointer`のみを標準エラーへ出す(値そのものは出さない、`test_invalid_or_secret_request_never_creates_output`で"BEGIN PRIVATE KEY"非出力を確認済み)。`operator-channel-client`の`_findings_summary`と同じ安全基準に揃った。

### What Looks Good
- 003時点で確認済みだった機能面(schema/DLP検査の順序、`--output`時のみのtype/schema_version既定補完、`sync_unobservable`/`sync_stopped`/`sync_old`の分類、両team受信・pagination・`excluded_count`検知・submitted状態の非フィルタ)は今回の修正で変更されておらず、再度差分を読んで同一であることを確認した。
- 004(Tester)は本Reviewerの003を認知せず独立に実施されており、双方が同じ静的検証(19+6件)を独立に再現し一致していることを確認した。004はさらに「同期時刻が未来」「片team inbox失敗時も処理続行」等、003・002が触れていない角度を実subprocessで追加検証しており、内容も矛盾しない。
- スコープは001から変更なく、対象外ファイルへの変更は無い(`git status`で確認)。commit/pushは行われていない。

### Verdict
Approve
