# Review: cert_renew git diff

日付: 2026-06-05
対象: 2026-06-05_032_implement.md 対応後の staged diff

## 指摘事項

### Low: 最新のレビュー/実装ログがまだ未追跡

- 該当:
  - `docs/ai/reviews/cert_renew/2026-06-05_031_review.md`
  - `docs/ai/reviews/cert_renew/2026-06-05_032_implement.md`
- 状況:
  - `024_implement` から `030_implement` までは staged されている。
  - 一方、今回の対応根拠である `031_review.md` と `032_implement.md` は `??` のまま。
- 影響:
  - レビュー履歴を Git に残す運用なら、直近の指摘と対応記録が commit から漏れる。
- 対応案:
  - `031_review.md` と `032_implement.md` も Git 管理対象に追加する。

## 確認結果

- `ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote ansible-playbook --syntax-check -i inventories/homelab/hosts.yml playbooks/cert_renew.yml`
  - OK
- `ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote ansible-playbook --syntax-check -i inventories/homelab/hosts.yml playbooks/cert_renew_quory.yml`
  - OK

補足: sandbox 上の `~/.ansible/tmp` が read-only のため、Ansible の一時ディレクトリを `/tmp` に指定して確認した。

## 良くなっている点

- `001_requirement.md` の冒頭に `cert_renew_policy.md` への参照が追加され、古い Semaphore Secrets 前提を正本として誤読するリスクは下がっている。
- `cert_renew_policy.md` の `cert_renew.yml` 失敗検知説明は、実装の `ignore_errors: true` と整合している。
- 本体変更は staged に揃っており、前回の staged / unstaged 分離問題は再発していない。

## 残留リスク

- systemd timer / `OnFailure=` の実装は別タスク扱いのまま。`cert_renew_quory.yml` の失敗検知は policy に明記されたが、実際の timer unit が追加されるまでは運用で担保する必要がある。
