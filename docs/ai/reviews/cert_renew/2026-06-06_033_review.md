# Review: cert_renew_policy.md 失敗検知記述更新

日付: 2026-06-06
対象: `2026-06-06_032_implement.md` 対応後の git diff

## 結論

修正必須の指摘はなし。

`2026-06-06_031_review.md` の Medium 指摘だった、
`cert_renew_quory.yml` のメール通知追加と `cert_renew_policy.md` の失敗検知方針の不一致は解消されている。

## Findings

なし。

## 確認できたこと

- `docs/ai/prompts/cert_renew_policy.md:106-107` で、`cert_renew.yml` と `cert_renew_quory.yml` の双方が完了メールを送る方針として整理されている。
- `docs/ai/prompts/cert_renew_policy.md:107` で、`cert_renew_quory.yml` の cleanup 失敗はメール送信後に fail し、メール送信失敗は `ignore_errors: true` で無視することが明記されている。
- `docs/ai/prompts/cert_renew_policy.md:109-116` で、systemd timer 実行時に `vault_password_file` と `inventories/vars/mail.yml` が必要なことが追記されている。
- `playbooks/cert_renew_quory.yml:74-114` は `mail.yml` を読み込み、`hostvars['quory']` の `cert_needs_renewal` / `cert_renewed` / `cert_deployed` から通知本文を組み立て、メール送信失敗を `ignore_errors: true` で無視する。
- `playbooks/cert_renew_quory.yml:116-119` は、通知タスク後に `cert_cleanup_status` を確認して fail するため、cleanup 失敗時に systemd unit の終了コードでも検知できる。

## 実行確認

```bash
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local TMPDIR=/tmp ansible-playbook --syntax-check -i inventories/homelab/hosts.yml playbooks/cert_renew_quory.yml
```

結果: OK

```bash
git diff --check
```

結果: OK

## 補足

- `Load mail variables` は `ignore_errors` なしなので、`mail.yml` または Vault パスワードが読めない場合は notification play で fail する。これは今回追加された policy の「通知実行環境要件」と一致している。
- 現在の作業ツリーには、今回のレビュー対象外も含めて `2026-06-06_030_implement.md` / `2026-06-06_031_review.md` / `2026-06-06_032_implement.md` が未追跡として残っている。レビュー履歴を Git に残す運用なら、commit 対象に含めるかを別途判断する。
