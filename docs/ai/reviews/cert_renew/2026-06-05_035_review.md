# Review: cert_renew git diff

日付: 2026-06-05
対象: 2026-06-05_034_implement.md 対応後の staged diff

## 指摘事項

### Low: 最新のレビュー/実装ログがまだ未追跡

- 該当:
  - `docs/ai/reviews/cert_renew/2026-06-05_033_review.md`
  - `docs/ai/reviews/cert_renew/2026-06-05_034_implement.md`
- 状況:
  - `031_review.md` と `032_implement.md` は staged され、前回指摘は解消されている。
  - 一方、今回の対応根拠である `033_review.md` と `034_implement.md` は `??` のまま。
- 影響:
  - レビュー履歴を Git に残す運用なら、直近の指摘と対応記録が commit から漏れる。
- 対応案:
  - `033_review.md` と `034_implement.md` も Git 管理対象に追加する。

## 確認結果

- `ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote ansible-playbook --syntax-check -i inventories/homelab/hosts.yml playbooks/cert_renew.yml`
  - OK
- `ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote ansible-playbook --syntax-check -i inventories/homelab/hosts.yml playbooks/cert_renew_quory.yml`
  - OK

補足: sandbox 上の `~/.ansible/tmp` が read-only のため、Ansible の一時ディレクトリを `/tmp` に指定して確認した。

## 良くなっている点

- `031_review.md` / `032_implement.md` は staged され、前回のレビュー履歴漏れは解消されている。
- `001_requirement.md` の冒頭に `cert_renew_policy.md` への導線があり、現在の CA 鍵取得方式を見落としにくくなっている。
- `cert_renew_policy.md` の `cert_renew.yml` 失敗検知説明は、メール送信失敗を `ignore_errors: true` で無視する実装と整合している。

## 残留リスク

- `cert_renew_quory.yml` の systemd timer / `OnFailure=` はまだ別タスク扱い。policy に方針は記載済みだが、timer unit が実装されるまでは quory 側証明書更新の自動実行・失敗検知は運用で担保する必要がある。
