# Review: cert_renew role Home-TLS-CA 対応 再レビュー 4回目

Review date: 2026-06-12

## Summary

`2026-06-12_009_review.md` の指摘2件は適切に解消されている。

- 証明書確認手順はすべて quory 上から実行する構成に統一された
- ルートCA証明書と中間CA証明書の参照先が、quory 上の配置方針と一致している
- 全サービスの提示チェーンは `s_client -showcerts -CAfile` で確認する手順になった
- 初回 Home-TLS-CA 移行時に `cert_renew.yml` を `force_renew: true` で実行する必要性が Policy に明記された
- `cert_renew_quory.yml` は play 内で既に `force_renew: true` であることも実装と一致している

要求仕様、コード差分、Policy、追加実装メモの間に、commit を止める新たな問題は見つからなかった。

## Findings

新たな指摘事項はない。

## Confirmed Behavior

### CA参照とフルチェーン配布

- CA参照先は `/home/yoshi/.cert/ca/home_tls_ca.{crt,key}` に変更されている
- CA秘密鍵の mode 0600 検証と tmpfs への一時展開は維持されている
- 発行したリーフ証明書と中間CA証明書を連結して `.fullchain.crt` を生成している
- Semaphore、Grafana、pveproxy の全配布処理が `.fullchain.crt` を使用している

### 中間CA有効期限監視

- `home_tls_ca.crt` の残存日数を Playbook 実行時に確認している
- 90日未満の場合に warning fact を設定している
- WARNING を含む通知は Slack の alerts チャンネル、warning status になる

### Playbook分離

- `cert_renew.yml` は ansy / pve1 / pve2 / monnie を対象とする
- `cert_renew_quory.yml` は quory のみを対象とする
- 両 Playbook とも quory 以外からの実行を assert で禁止している
- `cert_renew_quory.yml` の `force_renew: true` は維持されている

### 初回移行手順

Policy §10 に、次の手順が明記されている。

- quory は `cert_renew_quory.yml` を通常実行する
- その他の対象は `cert_renew.yml -e force_renew=true` で全証明書を更新する
- Semaphore 実行時は Extra Variables に `force_renew: true` を設定する

これにより、旧ルートCA署名証明書の残存日数が15日を超えていても、初回移行時に更新がスキップされることを防げる。

## Verification

以下を実行した。

```text
git diff --check
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote ansible-playbook --syntax-check -i inventories/homelab/hosts.yml playbooks/cert_renew.yml
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote ansible-playbook --syntax-check -i inventories/homelab/hosts.yml playbooks/cert_renew_quory.yml
```

結果:

```text
playbook: playbooks/cert_renew.yml
playbook: playbooks/cert_renew_quory.yml
```

構文エラーおよび whitespace error はなかった。

実機での証明書発行、チェーン検証、各サービスの提示チェーン確認は未実施である。これは追加実装メモの「運用者確認待ち」と一致している。

## Final Decision

この差分は commit してよい。

本番反映後は、`2026-06-12_010_implement.md` に記載された初回移行と検証手順を quory 上で実行し、全サービスが Home-TLS-CA を含むフルチェーンを提示することを確認する。

Next step files:
- docs/ai/reviews/cert_renew/2026-06-12_001_requirement.md
- docs/ai/reviews/cert_renew/2026-06-12_010_implement.md
- docs/ai/reviews/cert_renew/2026-06-12_011_review.md
