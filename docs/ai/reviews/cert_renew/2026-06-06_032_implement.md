# implement: cert_renew_policy.md セクション6 更新（失敗検知の記述を修正）

## 実装概要

`2026-06-06_031_review.md` の Medium 指摘に対応した。

`cert_renew_quory.yml` にメール通知が追加されたにもかかわらず、
`cert_renew_policy.md` のセクション6（失敗検知）が旧来の「systemd exitコードのみ」という記述のままになっていた。
policy の記述を実装の実態に合わせて更新した。

Low 指摘（`2026-06-06_030_implement.md` が git untracked）はコード変更を伴わないため、
commit 対象への追加は Yoshinobu が判断する。

## 作成・更新したファイル

| ファイル | 変更種別 |
|---|---|
| `docs/ai/prompts/cert_renew_policy.md` | 更新（セクション6） |

## 変更内容の要約

### cert_renew_policy.md セクション6 の変更

**変更前:**

```
cert_renew_quory.yml: systemd unitのexitコード（journalctl / OnFailure=で検知）
cert_renew_quory.yml はSemaphoreの外から実行するためメール通知を持たない。
```

**変更後:**

- テーブルの `cert_renew_quory.yml` 行を更新：完了メールを送ること、メール送信失敗は `ignore_errors: true` で無視すること、cleanup 失敗はメール送信後に fail することを明記
- systemd exitコード検知（OnFailure=）も「加えて検知できる」として残す
- セクション 6.1 を追加：systemd timer 実行環境での前提条件（vault_password_file + mail.yml）を明記

## 実行した確認コマンド

```bash
git diff docs/ai/prompts/cert_renew_policy.md
```

## 実行結果

差分は diff を参照。policy の記述とコードの実態が一致した。

## 未対応事項

- `2026-06-06_030_implement.md` の git 管理（Low 指摘）: コード変更なし。git add の判断は Yoshinobu に委ねる。

## 注意点

- `cert_renew_policy.md` は `docs/ai/prompts/` にある AI 向け運用正本。変更後は次回の Codex レビュー依頼時に参照されるため、内容の正確性が重要。

---

Next step files:
- docs/ai/reviews/cert_renew/2026-06-06_031_review.md
- docs/ai/reviews/cert_renew/2026-06-06_032_implement.md
