# Review: cert_renew_quory force_renew

日付: 2026-06-06
対象: 2026-06-06_027_implement.md 対応後の git diff

## 指摘事項

### Low: 実装メモが未追跡のまま

- 該当:
  - `docs/ai/reviews/cert_renew/2026-06-06_027_implement.md`
- 状況:
  - コード差分は `playbooks/cert_renew_quory.yml` の `force_renew: true` 追加のみ。
  - 一方、今回の根拠文書である `2026-06-06_027_implement.md` は `??` のまま。
- 影響:
  - レビュー/実装履歴を Git に残す運用なら、今回の判断根拠が commit から漏れる。
- 対応案:
  - 履歴を残す方針なら、`2026-06-06_027_implement.md` も Git 管理対象に追加する。

## 確認結果

- `playbooks/cert_renew_quory.yml:42-44` に `force_renew: true` が追加されている。
- `roles/homelab_cert_renew/tasks/issue.yml:22-27` では `force_renew | bool` が renewal 判定の先頭条件になっており、今回の追加は意図通り効く。
- `roles/systemd_timers/defaults/main.yml:38-42` の `cert-renew-quory` timer は第1土曜 00:35 実行なので、`force_renew: true` による「毎回発行」は月次更新の意図と整合している。
- `ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote ansible-playbook --syntax-check -i inventories/homelab/hosts.yml playbooks/cert_renew_quory.yml`
  - OK

## 結論

コード差分に機能的な問題は見つかりませんでした。

`cert_renew_quory.yml` は systemd timer から引数なしで月1回実行されるため、この playbook に限定して `force_renew: true` を設定する変更は妥当です。他の `cert_renew.yml` 実行には影響しません。
