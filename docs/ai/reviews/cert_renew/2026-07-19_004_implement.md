# homelab_cert_renew ROOT CA配布 — レビューSuggestion対応

- 日付: 2026-07-19（JST）
- 実装: implementer2
- 対応レビュー: `docs/ai/reviews/cert_renew/2026-07-19_003_review.md`
- 状態: Suggestion反映・local-only検証完了。実ホスト実行、commit、pushは未実施。

## 変更内容

新規 `Deploy ROOT CA certificate to system trust store` taskから、task-levelの `check_mode: false` を除去した。

唯一の入口である `playbooks/ca_trust_deploy.yml` は、`import_role` 全体に `check_mode: false` を指定している。この入口をrisk-accepted policyの単一管理点として維持し、ROOT copyを既存の中間CA copyと対称な定義に揃えた。

入口の `import_role`、中間CAのslurp/copy、ROOT CAのsource/content/destination/owner/group/mode/notify、handlerは変更していない。現在の入口からの実行挙動と冪等性は変わらない。

## 更新ファイル

- `roles/homelab_cert_renew/tasks/deploy_ca_trust.yml`
- `docs/ai/reviews/cert_renew/2026-07-19_004_implement.md`（本ファイル）

## 検証

- `ansible-playbook --syntax-check playbooks/ca_trust_deploy.yml`: PASS
- `scripts/check-tester-gate.sh`: 全36 playbook PASS
- `git diff --check`: PASS
- ROOT copyにtask-level `check_mode: false` がないことを確認
- `playbooks/ca_trust_deploy.yml` の `import_role` に `check_mode: false` が残ることを確認

実ホストおよびrisk-accepted playbookの実行は行っていない。

## Next step files

- `docs/ai/reviews/cert_renew/2026-07-19_001_requirement.md`
- `docs/ai/reviews/cert_renew/2026-07-19_003_review.md`
- `docs/ai/reviews/cert_renew/2026-07-19_004_implement.md`
