# incident-capture exit semantics — 差分レビュー

日付: 2026-09-21 (JST)
対象: `roles/incident_capture/files/incident-capture-collector.py`(変更)、
`scripts/tests/incident_capture_exit_semantics/`(新規3ファイル)、`.gitignore`(1行×2箇所)

## Summary

R1/R1b/R2/R2b/R3〜R6と§6のAC1〜AC11を対象に、要求・再査読2(Approve)・実装記録・現物diffを突き合わせた。
免除は`slack_status`が文字列`"info"`/`"warning"`に**完全一致**する場合だけに限定され(`isinstance(slack_status, str) and slack_status in TOLERATED_UNCORRELATED_SLACK_STATUS`)、未知値・非文字列(list/dict含む)・キー欠落はいずれも従来どおり`collection_errors`へ積まれ`isinstance`チェックによりunhashable値の`TypeError`も回避されている。免除された記録は新設`correlation_notes`へ移すのみで、同じbundle summaryへ後段追加される`collection_errors`(missing wrapper / no investigate ops)は影響を受けず、`any_bundle_errors`判定は`collection_errors`リストのみを見るため波及しない。`error`/`critical`は従来経路のまま。終了コード定数・`incident-capture.service.j2`・`ADR-003`前提のクエリ制約はいずれも触れていない。IC-016のjournal非信頼データ隠蔽は`_UNTRUSTED_WHAT_REDACTIONS`のprefixベースの仕組みが従来どおり働き、`correlation_notes`はそもそもjournalへ渡らない(`log_collection_errors_to_journal`は`collection_errors`しか読まない)ため崩れていない。`scripts/tests/incident_capture_exit_semantics/run-tests.py -v`をローカルで実行し10件全PASSを確認した(実ホスト・Ansible・Slack不使用)。

要求に無い実装は見当たらない。blocking findingは無い。

## Major Issues

なし。

## Minor Issues

| # | File | Line | Issue |
|---|---|---:|---|
| N1 | `roles/incident_capture/files/incident-capture-collector.py` | 10-18(モジュールdocstring「終了コード」節) | この節は「この役割内で定義」の終了コード契約を列挙する権威節であり、同種の例外(SSH到達不能がexit 2にならないこと)は§で個別に1段落を割いて説明している(64-70行目付近)。一方、今回追加した「`info`/`warning`の相関不一致はexit 2に数えない」という新しい例外はこの節にも近傍にも追記されておらず、`TOLERATED_UNCORRELATED_SLACK_STATUS`定義直前のインラインコメント(195-198行)にしか書かれていない。将来この節だけを読んだ実装者は「notableな`slack_status`は無条件でcollection_errorsへ積まれる」という誤った理解のまま変更しかねない。ドキュメント上のリスクであり実装の正しさには影響しないため非blocking。 |
| N2 | `scripts/tests/incident_capture_exit_semantics/test_exit_semantics.py` | 134-139 | AC11は例として「wrapperバイナリ不在、名前付き操作なし」の2つを挙げるが、`test_bundle_level_error_after_info_note_remains_exit_two`は`collect_host_snapshot`が`no_ops_error`を返す経路(named operation無し側)しか固定しておらず、`append_missing_binary_errors(base_snapshot, ...)`(983行、missing wrapper側)を通るfixtureが無い。両方とも同じ`summary["collection_errors"].append(...)`パターンで`any_bundle_errors`判定も共通のため実装上のリスクは低いが、AC11が明示した2例のうち1例だけがテストで固定されている。 |

## Suggestions

なし(計画査読S3の非文字列fixture要求はAC10相当テストで`42`/`[]`/`{}`により満たされている)。

## 確認した手段

- `git diff roles/incident_capture/files/incident-capture-collector.py`、`git diff .gitignore`、`git status --porcelain=v1`で対象範囲外の変更が無いことを確認した。
- `roles/incident_capture/files/incident-capture-collector.py`のmain()全体(830-1065行)、`log_collection_errors_to_journal`と`_redact_untrusted_what`(719-828行)、`apply_retention`(567行以降)を現物で読んだ。
- `roles/incident_investigate/files/incident-investigate.py`を`grep`し、`BUNDLE_DIR_RE = re.compile(r"^semaphore-(\d+)$")`(467行)のみを走査対象とし`spool-*`を見ないこと(AC9/再査読2 S2の裏取り)を確認した。他に`collection_errors`/`summary.json`/`correlation_notes`を読むPythonファイルが無いことを`grep -rln`で確認した。
- `scripts/tests/incident_capture_exit_semantics/run-tests.py -v`をローカル実行し、10 tests OKを確認した(状態を変えない、`/tmp`内`tempfile.TemporaryDirectory`のみ使用、実ホスト・Semaphore・Slack不使用)。
- `docs/ai/policies/incident_capture_policy.md`のIC-016本文を`grep`で確認した。
- `.gitignore`の追記2行が既存の`scripts/tests/<case>/`許可リストの様式と一致することを確認した。
- `git check-ignore`で`__pycache__/`が新設ディレクトリ配下でも既存ルールにより引き続き無視されることを確認した。

## 未確認事項

- systemd unit経由の実配備・実行は確認していない(scope外、要求§6配備の申し送りどおり)。
- N1/N2は実装の正しさを損なわない非blocking指摘であり、対応の要否はCoordinator判断とする。

## Verdict

**Approve.** blocking findingは無い。N1/N2はnon-blockingで、対応してもしなくてもcommit可否には影響しない。実装対象コード・テストは変更していない。
