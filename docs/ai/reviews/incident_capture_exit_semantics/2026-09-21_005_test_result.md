# incident-capture exit semantics — test result

日付: 2026-09-23 (JST)
対象: `roles/incident_capture/files/incident-capture-collector.py`(変更)、
`scripts/tests/incident_capture_exit_semantics/`(新規: `_path_setup.py`,
`run-tests.py`, `test_exit_semantics.py`)、`.gitignore`(2行)

Tester。実装者・Reviewerとは別体、経緯は継承していない。実ホスト・Semaphore・
本番Slackへは一切触れていない。全検証はオフラインfixture(`tempfile.TemporaryDirectory`
上の spool/bundle/state)と、リポジトリのテスト一式に加え自作の独立fixture
スクリプト(`/tmp`配下)で行った。

## 実行したコマンド

- `python3 scripts/tests/incident_capture_exit_semantics/run-tests.py -v`
  → 11 tests, OK(全PASS)。
- `python3 -m py_compile roles/incident_capture/files/incident-capture-collector.py`
  → 成功。
- `git diff --check` → 差分なし(whitespace issue なし)。
- `git status --porcelain=v1` / `git diff roles/incident_capture/templates/incident-capture.service.j2`
  → 変更対象は `.gitignore` と `incident-capture-collector.py` の2ファイルのみ。
  service template への差分は0(AC8裏取り)。
- 独立の自作fixtureスクリプト(`/tmp/.../scratchpad/adhoc_ac10.py`、repo内の
  `test_unknown_missing_and_non_string_statuses_fail_closed` とは別実装、同じ
  `_path_setup.collector` をmockして `main()` を直接実行)で、AC10の6ケース
  (`"unknown"`, `42`, `[1,2]`, `{"a":1}`, `None`, キー欠落)を通し、全て
  `exit_code == 2`・stderrに`TypeError`文字列が出ないことを確認した。
- ソースコードを現物で読んで、要求条項ごとに実装箇所を突き合わせた(`main()`
  940-1025行、`append_missing_binary_errors` 462-472行、
  `log_collection_errors_to_journal` 793-828行)。

## AC別結果

| AC | 判定 | 根拠 |
|---|---|---|
| AC1 | PASS | `test_info_uncorrelated_is_note_and_exits_ok_without_journal` で `collection_errors=[]`・`exit_code==EXIT_OK(0)`・`stderr==""` を確認。独立fixtureでも `info`/`warning` 単独は追試済み(既存テストと同じ結論)。 |
| AC2 | PASS | 同テストで `summaries[0]["correlation_notes"][0]` に `"no correlated Semaphore job"` と `"90s"`(tolerance値)を含むことを確認。`correlation_notes` は `collection_errors` とは別キーで、コード上 `collection_errors` へは積まれていない(940-971行)。 |
| AC3 | PASS | `test_error_and_critical_remain_collection_errors` で `error`/`critical` それぞれ `collection_errors` 1件・`exit_code==2`・`correlation_notes`キー無しを確認。 |
| AC4 | PASS | 同上(`critical`側のsubTest)。 |
| AC5 | PASS | `test_mixed_info_and_error_only_records_error_as_collection_error` で `info`+`error` 混在時、`exit_code==2`かつ `collection_errors` 合計1件(`error`分のみ)、`correlation_notes` 合計1件(`info`分)を確認。 |
| AC6 | PASS | `test_reject_move_failure_remains_collection_error` で、必須フィールド欠如(`{"broken": True}`)による退避処理中に `shutil.move` を意図的に失敗させ、`exit_code==2`(バンドル自体は作られず `summaries==[]`、run-level `collection_errors` 経由)を確認。R1の分類(相関不一致)とは別の実在コードパス(`reject_spool_file`)を通っており、R4「本物の収集失敗は分類に吸われない」の裏取りとして妥当。 |
| AC7 | PASS | `test_internal_exception_exits_three` で存在しないconfig pathを渡しsubprocessで実行、`returncode==3`・stderrに`"unexpected internal error"`を確認。AC10の6ケース(独立fixtureおよびrepoテスト双方)でも `exit_code` が3に化けないことを別途確認済み。 |
| AC8 | PASS | `test_exit_constants_and_service_unit_are_unchanged` で `(EXIT_OK, EXIT_COLLECTION_ERRORS, EXIT_INTERNAL_ERROR)==(0,2,3)`、`incident-capture.service.j2`に`flock -n -E 75`が残り`SuccessExitStatus`が無いことを確認。加えて`git status`/`git diff`でservice templateファイル自体に差分が無い(=触られてすらいない)ことを直接確認した。 |
| AC9 | PASS | `test_semaphore_bundle_shape_is_unchanged` で `build_semaphore_bundle` の出力キー集合が `{bundle_id, source, semaphore, spool_records, snapshot, collection_errors}` のままで `correlation_notes` を含まないことを確認。`correlation_notes` はコード上 spool単独バンドル生成箇所(969行)にしか出現せず、`semaphore-*` バンドル生成経路には触れていないことをソースで確認済み(grep 1箇所のみ)。 |
| AC10 | PASS | repoテスト `test_unknown_missing_and_non_string_statuses_fail_closed`(`"unknown"`, `42`, `[]`, `{}`, キー欠落)に加え、独立に書いた別実装のfixtureスクリプトで同種6ケース(`None`含む)を追試し、いずれも `exit_code==2`・`TypeError`なしを確認。実装は `isinstance(slack_status, str) and slack_status in ...` の順でガードしており、非文字列は`isinstance`の時点で短絡するためunhashable値が`in`(集合membership)へ渡ることはない(940-971行を現物確認)。 |
| AC11 | PASS(2経路とも) | (a) 名前付き操作なし側: `test_bundle_level_error_after_info_note_remains_exit_two`(`collect_host_snapshot`が`no_ops_error`を返す経路、992-993行)。(b) wrapperバイナリ不在側: `test_missing_wrapper_after_info_note_remains_exit_two`(`base_snapshot`経由で`append_missing_binary_errors`、987行)。両テストとも `correlation_notes` は残ったまま `collection_errors` に後段エラーが追加され `exit_code==2` になることを確認。依頼文が名指ししていた「2つの経路それぞれ」を両方カバーしている。 |

## R1/R1b/R2/R2bの契約としての確認

- R1: 免除は生成箇所(940-971行、旧948行相当)での判定であり、後段の文字列prefix照合に依存していない(prefix照合は journal 側の redaction 専用ロジックで、分類には無関係)。PASS。
- R1b: 免除は`{"info","warning"}`の2値ちょうど。`NON_NOTABLE_SLACK_STATUS`側にも`isinstance`ガードが入っており(946行)、非文字列のnon-notable判定も安全側。PASS。
- R2: `error`/`critical`は従来経路のまま(AC3/AC4で確認)。PASS。
- R2b: AC11の2テストで確認済み。免除は相関不一致1件についてのみ働き、バンドル全体は免除されない。PASS。

## 未確認事項

- systemd unit経由の実配備・実行、および`playbooks/incident_capture_setup.yml`の実行は確認していない。requirement §6「配備の申し送り」どおりscope外であり、実施しなかった。**現時点でrepo変更は本番へ反映されていない**(実装記録・diff_reviewの申し送りと一致)。
- `journal(stderr)`側で`correlation_notes`の内容が将来誤って出力される経路が無いかは、`log_collection_errors_to_journal`が`run_level_errors`と`summary["collection_errors"]`のみを読む(`correlation_notes`キーへは触れない)ことをソースで確認した限りでは無いが、journal出力の全経路(`_journal_write`呼び出し全箇所)の網羅的grepまでは行っていない。リスクは低いと判断するが「確認した」とは言い切らない。
- N1/N2(diff_reviewのMinor指摘)は現物で解消を確認した — N1はモジュールdocstringの終了コード契約節(10-22行目)直後に新しい例外の説明段落が追加されている。N2はAC11の2経路目(wrapperバイナリ不在)を固定する`test_missing_wrapper_after_info_note_remains_exit_two`が追加されテスト数が10→11件になっている。

## テストが固定しているか(通ることと固定していることの区別)

- 上記AC1〜AC11はいずれも、repoテストの各テストがその条件を狭く踏んでいる(mock対象・assert対象を確認した上での判断)。「通るが固定していない」項目は見つからなかった。
- 唯一、`test_semaphore_bundle_shape_is_unchanged`(AC9)は`build_semaphore_bundle`を直接呼ぶ単体テストであり`main()`全体を経由しないが、AC9の主張(バンドル生成内容が変更されていないこと)自体はこの粒度で十分固定できている。

## 残存リスク

- 低: journal出力の網羅的grepを行っていない点(上記「未確認事項」参照)。ただし`correlation_notes`という文字列自体がjournal出力コード中に一切出現しないことは`grep -n "correlation_notes"`で確認済み(1箇所のみ、969行の代入)。
- 低: 本番配備は本案件のscope外であり未実施。repoの変更だけでは症状(週次`Failed to start`)は解消しない。運用上の申し送り事項(requirement §6と実装記録に記載済み)。

## 総合判定

AC1〜AC11すべてPASS。R1/R1b/R2/R2bもPASS。blocking な欠陥は見つからなかった。
