# incident-capture exit semantics — audit

日付: 2026-09-23 (JST)
対象: `docs/ai/reviews/incident_capture_exit_semantics/`
読み始め: `2026-09-21_001_requirement.md`

## 判定

**再構成は、下記2件のblocking指摘を除き可能。条件付き受入。**

blocking指摘の数値と状態の記録を現物に合わせれば、再監査なしでクローズしてよい。

## 再構成した経緯

1. 月曜09:00の成功したSyslog weekly digestが`slack_status="info"`のspool記録を作り、収集器が失敗Semaphore jobと相関できないことを収集エラーにしてexit 2となっていた(`_001` §1、§6 AC1〜AC2)。
2. 初版の終了コード追加案は、計画査読がIC-024との衝突などC1とM1〜M3を指摘して差し戻した(`_002` 初回)。Yoshinobuの2026-09-21の判断でPolicy・終了コード・unitを変えない案へ切り替えた(`_001` §11)。
3. 再査読は未知値・型違いまで免除するfail-openと、後段のbundle-level収集失敗まで免除しうる点をM4・M5として差し戻した(`_002` 再査読)。R1b・R2b・AC10・AC11へ反映後の再査読2でApproveとなった(`_001` §5〜§6、`_002` 再査読2)。
4. Implementerは免除対象を文字列`info` / `warning`の完全一致に限定し、相関不一致を`correlation_notes`へ記録する差分とオフラインテストを作った(`_003`)。
5. 差分レビューはApproveとし、non-blockingのN1・N2を指摘した(`_004`)。その後N1・N2は反映され、TesterがオフラインfixtureでAC1〜AC11をすべてPASSとした(`_005`)。
6. repo上の実装とテストは完了したが、本番へは未反映である。`playbooks/incident_capture_setup.yml`のSemaphore実行と、配備後最初の月曜09:00の効果観測が申し送られている(`_006` §5)。

## 指摘

| ID | 分類 | クローズを妨げるか | 指摘 | 必要な反映 |
|---|---|---|---|---|
| A1 | 記録どうしの矛盾 | **妨げる** | `_006` §2は計画査読を「Request Changes 3回 → Approve」とするが、`_002` に実在するRequest Changes判定は初回と再査読の**2回**で、再査読2はApproveである。また同表の「Critical 1 / Major 5」に対し、`_006` §4は「Major 4件(初版由来)」+「Major 2件(2版目)」と数え、Major 6件になっている。現物はC1とM1〜M5の計6件で、初回のMajorはM1〜M3の3件である。 | `_006` §2を「Request Changes 2回 → Approve」に直し、§4を初回Major 3件+再査読Major 2件の計Major 5件に合わせる。`producerとconsumerの責任の切れ目`をM3と別の指摘として数えない。 |
| A2 | 状態の正本との突合 / 未解決の明示 | **妨げる** | `_006` は監査受入前に状態を「クローズ」とする一方、§5に本番配備と配備後の月曜09:00観測を未実施として残している。しかし`docs/ai/status.md`には、この本番未反映または配備後観測を追跡する行が無い。次セッションはクローズ記録の残存事項を能動的に発見できず、状態の正本だけで現在地を復元できない。 | 本監査のblocking反映中は`_006`の状態をクローズ済みとしない。反映後に閉じる際は、`status.md`へ本番配備待ちと配備後最初の月曜09:00観測を、`_006` §5へのポインタとともに記録する。本案件のrepo開発を閉じて運用申送りを別の状態行に分ける形でもよい。 |
| A3 | 根拠の実在 | 妨げない | `_006` §5はSemaphore templateを`#39`と特定するが、案件フォルダとそこから参照されるrepo成果物に、その数値IDを裏付ける記録が無い。`playbooks/incident_capture_setup.yml`がSEMI-SAFEとして登録されていることは`roles/semaphore_templates/defaults/main.yml`から確認できるため、配備先の同定自体は可能である。 | `#39`にrepo内の根拠があるなら参照を追加する。無ければ、安定した名称とplaybook pathだけを申送りに残す。 |

## 検査結果

- **決定の追跡**: 方針転換はYoshinobu、2026-09-21、IC-024との衝突を根拠として追跡できる。
- **参照先の実在**: Policy IC-024、変更対象、テスト3ファイル、unit template、配備playbookは実在する。A3の数値IDだけがrepo成果物からは追跡できない。
- **反証の反映**: 初版と再査読のblocking指摘C1、M1〜M5は最終requirement、実装記録、テスト結果へ反映されている。差分レビューのN1・N2の反映も`_005`から追跡できる。
- **受入条件**: AC1〜AC11は`_005`に個別の検証結果があり、`_006`からそれを逆引きできる。
- **未解決の明示**: 本番未反映、配備後観測、timer失敗通知、flock競合は`_006` §5に明記されている。ただしA2のとおり状態の正本へ接続されていない。
- **転記先の実在**: 存在しない転記先へ「移した」とする記述は見つからない。
- **差分外の陳腐化**: `incident_capture_policy.md` IC-024は案件記録の最終方針と矛盾していない。陳腐化しているのはA2の`status.md`である。

## Coordinatorの実施事項

1. A1の回数とfinding数を`_006`で訂正する。
2. A2の本番配備・配備後観測を`status.md`へ接続し、監査受入前のクローズ表記を整合させる。
3. A3はクローズの条件ではない。根拠を追加するか、数値IDを外す。
4. 上記を反映後、本案件をクローズする。再監査は不要。
