# proxmox_healthcheck 再レビュー

## Findings

Blocking findings はありません。

前回レビュー `2026-05-08_003_review.md` の指摘は、以下の通り解消されています。

| 前回指摘 | 判定 | 確認内容 |
|---|---|---|
| `zpool status` 取得失敗が OK 扱いになる | 解消 | `roles/proxmox_healthcheck/files/proxmox-healthcheck.sh` で `zpool_ok` を収集し、`roles/proxmox_healthcheck/tasks/main.yml` で `collection_ok == false` を CRITICAL にしている。 |
| root filesystem 使用率取得失敗が OK 扱いになる | 解消 | `root_used_pct < 0` を CRITICAL にし、通常の閾値判定と warning 判定からは除外している。 |
| `regex_replace('(.*)', ...)` の二重置換リスク | 解消 | 対象 3 箇所が `^(.*)$` に変更されている。 |
| root FS 閾値の「超」と `>=` の表現差分 | 解消 | requirement が `80%以上` / `90%以上` に更新され、実装と一致している。 |

## Notes

- `zfs.collection_ok` は `proxmox_health` には含まれるが、保存 report の `zfs` セクションには現在含まれていない。CRITICAL メッセージには残るため運用上のブロッカーではないが、後から report 単体で「ZFS の取得に成功したか」を見たいなら `proxmox_report.zfs.collection_ok` も保存してよい。
- shell 実行時に Python 3.14 環境では `datetime.datetime.utcnow()` の DeprecationWarning が stderr に出る。stdout の JSON parse には影響しないためブロッカーではない。将来の静かさを重視するなら `datetime.datetime.now(datetime.UTC)` への置き換えを検討してよい。

## 確認結果

実行した確認:

```bash
bash -n roles/proxmox_healthcheck/files/proxmox-healthcheck.sh
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote ansible-playbook --syntax-check -i inventories/homelab/hosts.yml playbooks/proxmox_healthcheck.yml
bash roles/proxmox_healthcheck/files/proxmox-healthcheck.sh
```

結果:

- shell 構文チェックは成功。
- playbook syntax-check は成功。
- shell は stdout に valid JSON を返すことを確認した。
- Codex 環境では通常の Ansible 一時ディレクトリ `~/.ansible/tmp` が read-only のため、syntax-check は `ANSIBLE_LOCAL_TEMP` / `ANSIBLE_REMOTE_TEMP` を `/tmp` に向けて実行した。

## 判定

初期運用版として commit 可能な状態です。

実 Proxmox ノード上での初回実行時には、`pvesr status` の出力フォーマット、ZFS pool の parse 結果、replication の stale 判定が想定通り report に出ることを確認してください。

## Next step files

- `docs/ai/reviews/proxmox_healthcheck/2026-05-08_001_requirement.md`
- `docs/ai/reviews/proxmox_healthcheck/2026-05-08_004_implement.md`
- `docs/ai/reviews/proxmox_healthcheck/2026-05-08_005_review.md`
