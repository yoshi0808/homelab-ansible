# Test Result: pkg-list / schedule-list dispatch語彙追加

作成日: 2026-09-14(Tester)、**2026-09-14 再実測(差し戻し後の再実装に対して)**
対象契約: `docs/ai/reviews/dispatch_pkg_and_schedule_vocabulary/2026-09-14_001_requirement.md` AC1〜AC8
対象差分(未commit、`git diff`、再実測時点の内容):
- `roles/dev_investigate/files/recovery-investigate-dispatch-quory.sh`(コメント/見出し数のみ変更、`pkg-list`アームのロジックは前回検証時と同一)
- `roles/recovery_exec/templates/recovery-investigate-dispatch-pve.sh.j2`(**構造変更** — 到達不能だった`pkg-list\ *`分岐と`$cmd`の再readを除去し、既存の固定parser`p1`/`p2`と`pkg_list()`関数に統一)
- `roles/recovery_exec/templates/recovery-investigate-dispatch.sh.j2`(前回検証時と同一、再変更なし)
- `roles/recovery_exec/files/homelab-semaphore-query`(前回検証時と同一、再変更なし — `git diff`で確認)
- `docs/ai/reviews/dev_prod_boundary/2026-08-03_008_phase3_check_catalog.md`(§10.1の総数表記法が変更・V2へ実測フィールドを追記)

## 再実測の範囲と前回からの引き継ぎ

Coordinatorの指摘どおり、**class P(`dispatch-pve.sh.j2`)は実行経路が変わったため前回のPASSを引き継がず、全項目を再実測した。** class Q・class Gの`pkg-list`ロジック本体は`git diff`上バイト単位で前回と同一であることを確認した上で、それでも**全AC・全3スクリプトを同じ厳密さで再実行し、rcを取り直した**(以下の観測値はすべて今回のコマンド実行によるものであり、前回記録の転記ではない)。`homelab-semaphore-query`(AC5関連)は`git diff --stat`で前回検証時と差分が完全に一致することを確認したため、Coordinatorの指示どおりAPIへの再実測は行わず、**前回のAPI実測結果を引き継ぐ**(該当節に明記)。

## 実施方法(前回と同一手段)

- 実ホスト(quory/pve1/pve2/authy/monnie)へは到達していない。すべてansy上のローカル検証。
- class Qはリポジトリ現物のスクリプトをそのまま複製し、`SSH_ORIGINAL_COMMAND`環境変数を与えて直接実行。
- class P(pve1想定)・class G(authy想定)はJinja2 `Environment(FileSystemLoader(...))`で実値(`inventory_hostname`、`target_item`)を与えてレンダリングし、`bash -n`で構文検証してから実行。
- `dpkg-query`の実行有無は、標準出力の有無ではなく**PATH shadowing法**(ダミー`dpkg-query`実行体をPATH先頭に挿入し、呼ばれたときだけログファイルを作る)で直接確認。有効な入力での陽性対照(呼ばれる)と無効な入力での陰性対照(呼ばれない)の両方を確認。

## AC別結果(すべて今回の再実測)

### AC1 — `pkg-list`引数なし、rc=0、1行1パッケージ
**PASS**(3スクリプトとも、今回の実行で再確認)
- quory: rc=0
- pve(再構造化後の`pkg_list()`関数経由): rc=0
- authy: rc=0
- いずれも`3cpio 0.14.0-1ubuntu1 ii` 形式(タブ区切り、名前/バージョン/status)の出力を確認。

### AC2 — 正常pattern、一致あり/一致0件ともrc=0
**PASS**(3スクリプトとも)
- `pkg-list bash` → 3スクリプトともrc=0、`bash 5.3-2ubuntu1 ii` の1行。
- `pkg-list zzz-definitely-not-a-real-pkg-xyz` → 3スクリプトともrc=0、標準出力は空、stderrに`dpkg-query`自身の「一致するパッケージが見つかりません」。**pve側は今回`pkg_list()`という独立関数内の`|| { rc=$?; [[ "$rc" -eq 1 ]] || return "$rc"; }`がこの正規化を担っており**、関数境界を越えてもrc=0への正規化が成立することを確認した(前回は関数化されておらずcase arm直書きだった)。

### AC3 — 不正pattern、非ゼロ終了かつ`dpkg-query`を実行しない
**PASS**(3スクリプトとも)
- `pkg-list a;b`(セミコロン)→ 3スクリプトともrc=1、`denied: invalid pattern for pkg-list`
- `pkg-list <65文字のa>`(65文字)→ 3スクリプトともrc=1、同メッセージ
- `pkg-list a b_invalid_as_one_token`(空白入り)→ pve・authyともrc=1、`denied: invalid parameter count for 'pkg-list'`(quoryも同様、前回と同じ経路 — 空白入りpatternは検証regexに到達する前にarity側で弾かれる構造は変わっていない)。
- **`dpkg-query`非実行のPATH-shadow確認(今回再実施)**: `pkg-list a;b`を3スクリプトすべてに与え、いずれもダミー`dpkg-query`呼び出しログが作られないことを確認。同条件下で`pkg-list`(引数なし・正常)を与えると3スクリプトとも呼び出しログが作られることも確認し、検査方法自体の有効性を今回も裏付けた。

### AC4 — arity超過(`pkg-list a b`)、既存と同じく非ゼロ終了
**PASS**(3スクリプトとも)
- quory / pve / authy いずれもrc=1、`denied: invalid parameter count for 'pkg-list'`。
- pveは再構造化後、トップレベルの固定parser(`read -r check p1 p2 extra`)が`p1=a p2=b`まで受け取り、case arm内の`[[ -z "$p2" ]] || deny_count`で拒否する経路に変わっている(旧実装は`$cmd`をarm内で再readしていた)。**経路は変わったが観測結果(rc=1・同一メッセージ)は前回と同じ。**

### pve.sh.j2の`pkg_list()`関数化に伴う終了コード伝播の確認(Coordinator指摘、今回新規)
**確認済み・問題なし。**
`dpkg-query`をrc=2相当で終了させるダミー実行体をPATH shadowで挿入し(`|| { rc=$?; [[ "$rc" -eq 1 ]] || return "$rc"; }`の`return "$rc"`が実際に効くかを検証)、3スクリプトすべてで`pkg-list bash`を実行した。
- quory: 最終スクリプトrc=2
- pve: 最終スクリプトrc=2(**関数の`return 2`がcase armの終了ステータスとなり、`set -euo pipefail`下でも吸収されずスクリプト全体の終了コードへそのまま伝播することを確認**)
- authy: 最終スクリプトrc=2
関数境界を挟んでも、rc=1(該当なし)だけが握りつぶされ、それ以外の非ゼロ(実行時エラー等)はスクリプト全体の終了コードにそのまま反映されることを実測で確認した。前回はpve側が未関数化だったため、この確認自体が今回初めて可能になった項目である。

### AC5 — `semaphore-query schedule-list <n>`
**前回の実測結果を引き継ぐ(今回は再実測していない)。** 理由: `homelab-semaphore-query`は`git diff --stat`で前回検証時と完全に同一の差分であることを確認した(Coordinatorの「helperに変更がなければ再実測不要」という指示に基づく判断)。quory dispatch側の`semaphore-query`アーム(enum/arity gate)もロジック無変更。前回実測の要旨(参考として転記、値は前回のもの):
- dispatch層: `schedule-list`はenum通過、`n=abc`は`denied: invalid n for semaphore-query`でrc=1。
- helper本体(ansyのSemaphore API、project id=3、鍵未登録の状態で実測): `schedule-list 5`→rc=0・5件、`schedule-list 200`→rc=0・20件・id昇順、`schedule-list 0`/`201`→rc=1・range外エラー、`schedule-list abc`→rc=1・usage。生API応答(`curl`で取得しid昇順に整列したもの)とhelper出力が20件完全一致。

### AC6 — 既存probe 2本の回帰
**PASS(今回再実測)**
- `totally-unknown-operation-xyz` → rc=1、`denied: unknown command 'totally-unknown-operation-xyz'`
- `operator-request-submit extra-unexpected-argument` → rc=1、`denied: invalid parameter count for 'operator-request-submit'`
- quoryスクリプトの該当箇所(コメント以外)は前回と無変更だが、`pkg-list`アームの追加によりarity検査が緩んでいないことを今回の実行で再確認した。

### AC7 — 配備後hash一致・翌日drift検査
**未実施(前回と同じ理由)。** Testerの到達範囲外(quory/pve1/pve2/authy/monnieへ未到達)。

### AC8 — カタログへの反映(引数・検証・実行内容・総数)
**内容確認はPASS、「commit」を主語にした確認は未実施。**
- `docs/ai/reviews/dev_prod_boundary/2026-08-03_008_phase3_check_catalog.md` §10.1が今回改訂され、総数表記が「D系列の今回追加後」(Q: 27、G/P: +7)と「dispatch実数」(Q: D系列27+OPRC4=**31**、G/P: 変わらず+7)を分けて明記する形になっている。quoryスクリプト側コメントの「Total is now 31」という記述(既存のOPRC由来コメント)と数値上整合していることを確認した。V2の行に`schedule-list`の実測フィールド一覧(`active`/`cron_format`/`delete_after_run`/`id`/`name`/`project_id`/`repository_id`/`template_id`/`tpl_name`/`type`)が追記されており、前回私が実測したAPI応答フィールドと一致することを確認した。
- 本changesetは本検証時点でも未commitであるため、AC8の文言どおりの「commit後の確認」は引き続き未実施。

## requirement §7 オープンクエスチョンへの回答(前回実測を引き継ぐ、再実測不要 — helper・API双方無変更)

`GET /project/<id>/schedules`(ansyのSemaphore、project id=3、20件)が返すフィールド:
```
active, cron_format, delete_after_run, id, name, project_id, repository_id, template_id, tpl_name, type
```
- (a) `task_params`は一覧に含まれない(前回実測)。
- (b) 次回実行時刻に相当するフィールドは無い。`cron_format`(cron式)はあるが実行時刻の算出値は返らない(前回実測)。
- helperの`schedule-list`出力と生API応答は完全一致(前回実測)。
- 今回、カタログ§10.1のV2行にこの実測フィールド一覧が転記されたことを確認した(内容の整合性確認のみ、APIへの再アクセスなし)。

## 残存リスク

- **空白入りpatternの拒否経路が「arity超過」に分類される**(AC3)。3スクリプトとも同じ挙動で、動作としては要件を満たす(非ゼロ終了・dpkg-query未実行)が、エラーメッセージが`invalid pattern`ではなく`invalid parameter count`になる。診断メッセージの精度の問題であり、セキュリティ上の抜けではない。前回から変わらず残る軽微な事項。
- **前回指摘した「pve.sh.j2の到達不能な`pkg-list\ *`分岐」は今回の再実装で解消済み** — 構造そのものが固定parser方式に統一されたため、この残存リスクはクローズしてよい。
- AC7・AC8(commit後の再確認)は引き続き未実施。commitと配備を離さない(requirement §8)ため、配備後にTesterまたはCoordinatorが`deployed-hash`突合と翌日のdrift検査結果を確認する必要が残る。

## 実施できなかったもの(理由つき)

- **AC7の配備側全体**: quory/pve1/pve2/authy/monnieへの到達および配備実行は、Testerの到達範囲外(タスク指示・EXEC-052の境界)。
- **AC8の「commit」を主語にした確認**: 本changesetが未commitのため、commit後に改めて確認する必要がある。
- **AC5・§7オープンクエスチョンのAPIへの再アクセス**: `homelab-semaphore-query`に変更が無いことを`git diff`で確認した上で、Coordinatorの指示に基づき前回実測を引き継ぎ、再実測は行わなかった。
