# Audit: dispatch pkg-list / schedule-list 語彙追加

日付: 2026-09-14
対象: `docs/ai/reviews/dispatch_pkg_and_schedule_vocabulary/`（`_001`〜`_005`）と、その記録が参照する成果物。判定時の変更は未commit（`git status --short` / `git diff`で確認）。

## 判定

**再構成は概ね可能。ただし受入条件の完了までは再構成できない。クローズ判定は差戻し。**

起案の2つの問い、対象外、3 dispatch・helper・語彙カタログの変更、差分レビューM1とMinor 3件への対応、Testerの再実測、schedule一覧APIの限界は `_001`〜`_005` と現行差分から辿れる。M1の是正もカタログ§10.1の「D系列27 + OPRC 4 = dispatch実数31」で現物確認できた。一方、AC7とAC8の完了を示す記録は存在せず、`_004`と`_005`自身が未実施と述べる。追加の配備・観測が必要なので、記述訂正だけを条件とする受入にはできない。実装の技術的な正否は判定していない。

## 指摘

| 分類 | 指摘と根拠 | クローズを妨げるか |
|---|---|---|
| 受入条件の充足 | `_001` AC7は配備後のhash一致と翌日のdrift検査、AC8は「本案件のcommit」を条件とする。`_004` AC7/AC8節と `_005`「未実施」は両方とも未完了と明記し、現在の`git status --short`でも対象5ファイルは未commitである。配備と観測の後に、AC7/AC8の結果を案件記録へ残す必要がある。実施しないなら、受入条件の扱いをYoshinobuの判断として明示する必要がある。 | **妨げる**。追加の事実確認が要るため再監査対象。 |
| 差分の外側の陳腐化・状態の正本 | `docs/ai/status.md:14`は「dispatchにschedule照会が無い」とし、`roles/semaphore_templates/defaults/main.yml:808-809`も「dispatchのカタログにschedule照会が無い」とする。しかし現在の語彙カタログ§10には`semaphore-query schedule-list`が載り、class Q dispatchとhelperの差分にも追加されている。**未配備なので本番でまだ使えない**という事実と、repoのカタログ・実装に語彙が無いという主張を分けて更新する必要がある。`docs/ai/reviews/quory_health_monthly/2026-09-14_024_schedule_deployment_observation.md:26`にも同じ旧理由が残る。`status.md`のquory月次ヘルス観測待ち行は継続対象であり、削除対象ではない。 | **妨げる**。状態の正本と参照先が現物に反する。 |
| 記録どうしの矛盾・反証の反映 | `_002`「未確認事項」はAPI応答に次回実行時刻が含まれるか未確認とするが、同じ文書の「差分レビュー・Tester結果の反映」、`_001` §7、`_004` §7、`_005`「分かったこと」は、ansy Semaphoreの一覧応答にそのフィールドが無いと確定させている。旧い未確認事項を実測済みの結果へ更新する必要がある。語彙カタログ§10の「返さない場合」も実測反映後の条件付き表現として残る。 | 妨げない。訂正箇所と確定根拠が明確。 |
| 記録どうしの矛盾 | `_005`「判断したこと」は、空白入りpatternの拒否経路の違いを「利用者から観測できない」とする。`_004` AC3・「残存リスク」は、無効な記号では`invalid pattern`、空白入りでは`invalid parameter count`と、利用者に見える異なるエラーメッセージを記録している。直さない判断自体は記録されているが、その理由の事実記述を合わせる必要がある。 | 妨げない。判断の結論は変わらず、記述訂正で足りる。 |

## 状態と未解決事項

`docs/ai/status.md`に本案件専用の行は無い。クローズ時に削除すべき本案件の行も無い。quory月次ヘルスの観測待ち行は残し、その中のdispatchについての旧い理由だけを更新する。AC7の配備・翌日観測、AC8のcommit後確認、実ホストでの`dpkg-query`未確認は `_004` / `_005`に明記されている。aptログの閲覧を対象外とした理由も `_001` / `_005`で追える。

監査ではrepo成果物と現行差分だけを読んだ。実ホスト・Semaphore APIには接触せず、実装・レビュー・テスト・既存記録は変更していない。

---

## Coordinatorの判断(2026-09-14)

**4件すべて是正した。**

- 陳腐化(blocking 2件目): `docs/ai/status.md`、`roles/semaphore_templates/defaults/main.yml`、`quory_health_monthly/2026-09-14_024_...md` の3箇所を、「repoには語彙があるが未配備」と「APIが次回実行時刻を返さない」を分けて書く形へ直した。**3箇所のうち2箇所は同じ日にCoordinator自身が書いたものである。**
- `_002` の古い未確認事項: Implementerが自分の記録を訂正した。
- `_005` の「利用者から観測できない」: `_004` の観測(`invalid pattern` と `invalid parameter count` は異なるメッセージ)と食い違っていたため、**「区別が付かないから」ではなく「どちらも拒否で終わり利用者の次の行動が変わらないから直さない」**へ書き換えた。

**AC7 / AC8(blocking 1件目)は配備待ちであり、指摘のとおり現時点では未達である。** commit → 配備(`playbooks/dev_investigate_setup.yml` と `playbooks/recovery_exec_setup.yml`、いずれもquoryのSemaphoreでYoshinobuが実行)→ AC7/AC8の観測を案件記録へ追記、の順で閉じる。**この観測をもってCoordinatorがクローズを判断し、Auditorは再起動しない**(無条件受入の取得を目的とした再監査を行わないため)。観測が期待と違ったときは、その時点で新たに判断する。
