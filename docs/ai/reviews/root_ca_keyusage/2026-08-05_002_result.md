# 実施結果(ROOT CA の KeyUsage 追加)

2026-08-05 / Coordinator。requirement は `2026-08-05_001_requirement.md`。**Step B(鍵を扱う工程)は Yoshinobu、それ以外は Coordinator。**

## 実施したこと

| Step | 誰 | 結果 |
|---|---|---|
| B | Yoshinobu | Bitwarden から ROOT 鍵を取り出し、同じ鍵・同じ subject で KeyUsage 付きの自己署名証明書を再発行。`radius_ca_new.crt` として提供。鍵はオフラインへ戻した |
| C-1 | Coordinator | `scripts/verify-root-ca.sh` で検査 → **PASS**。ansy と quory の**両方の実チェーン**に対して確認した |
| C-2/3 | Yoshinobu | quory の `radius_ca.crt` を差し替え、`ca_trust_deploy` を実行 |
| D | Coordinator | 回避を撤去(repo側2箇所) |

## 受入条件の結果

| AC | 判定 | 根拠 |
|---|---|---|
| **AC1** | **PASS** | `scripts/verify-root-ca.sh` が全項目 OK。**SKI が現行 ROOT と完全一致**(`1E:4D:3C:23:B5:43:0C:77:70:65:49:A5:22:0C:E6:8B:6E:61:88:57`)= 同じ鍵。subject は DN の順序まで一致。KeyUsage は critical |
| **AC2** | **PASS** | Python の `ssl.create_default_context()`(システム信頼ストア+既定の厳格な検証)で、`ansy.internal:3000` / `quory.internal:3000` ともホスト名で**検証成功**(TLS 1.3)。**これが 2026-08-04 に失敗していた経路そのものである** |
| AC3 | **未検証** | `cert_renew` 系の次回実行まで得られない。**中間 CA も leaf も変更していない**ため理屈の上では影響が無いが、実測ではない |
| AC4 | **未検証** | 次の 00:40 のドリフト検査で分かる |
| AC5 | **未検証** | §7 R1 のとおり、AI から確かめる手段が無い |

## 配布の確認

ansy の `/usr/local/share/ca-certificates/home-tls-root-ca.crt` が、シリアル `383E46E5AA4EE88069866EE87FA2B946D13E4631`(= `radius_ca_new.crt` と同一)かつ KeyUsage critical になっていることを確認した。

**他ホスト(quory / monnie / authy / pve1 / pve2)の信頼ストアは、AI から直接確認できない。** `ca_trust_deploy` はそれらを対象に含めており、ジョブは正常終了している。**信頼ストアの中身はドリフト検査の対象にも入っていない**(`template` 配備ではないが、`copy` 先が `/usr/local/share/ca-certificates/` であり、カタログに載っていない) — 必要なら別途載せられる。

## 撤去した回避(repo側)

| 場所 | 撤去内容 |
|---|---|
| `roles/deployment_drift_check/defaults/main.yml` | 接続先とTLS検証の上書き2値を削除 |
| `roles/deployment_drift_check/tasks/semaphore_probe.yml` | 呼び出しsiteの `vars:` 上書きを撤去し、`semaphore_templates` の既定(`https://quory.internal:3000/api` / `validate_certs: true`)をそのまま使う形へ |
| `roles/semaphore_templates/defaults/main.yml` | ansy 向け上書き例から、ループバックと `validate_certs=false` を撤去 |

**ループバックへ寄せていた理由も同時に消えている。** 検証が有効なら、DNS を取られても攻撃者は `quory.internal` の正当な証明書を出せない。名前解決が落ちた場合も、quory の `/etc/hosts` に全 inventory ホストが載っている(同 role の `deployment_drift_check_hosts_file` が日次で確認している)。

## 残っているもの

- ~~quory のブートストラップ template の survey 既定値~~ → **2026-08-05に Yoshinobu が `https://quory.internal:3000/api` / `true` へ変更し、Coordinator が `semaphore-query template-list` で反映を確認した。** これで**検証を切っていた経路は repo 側3箇所・Semaphore 側1箇所ともすべて消えた。** なおこのテンプレートはカタログ管理外(orphan)であり、R5により reconcile はこれを上書きしないため、次に押しても値は戻らない
- **AC3 / AC4 / AC5 は時間が答えを出す**。`docs/ai/status.md` の Watch へ移した
- **信頼ストアそのものがドリフト検査の対象外**。全ホストに正しい ROOT が入り続けているかは、現状どこも見ていない

## この案件で分かった、手順側の罠(記録)

`scripts/verify-root-ca.sh` を作る過程で、**私が用意した手順の欠陥を2つ、スクリプト自身が検出した。**

1. **DN の構成要素の順序。** この環境の ROOT は `CN → O → C` の順で作られており、素直に `/C=JP/O=home/CN=...` と書くと DN が別物になり、中間 CA の発行者名と繋がらない(`unable to get local issuer certificate`)
2. **`openssl verify` は `-CAfile` を渡しても既定の信頼ストアを参照する。** `-no-CApath -no-CAstore` を付けないと、「システムに旧 ROOT がある」だけで通ってしまい、検査が何も検査していない状態になる

**どちらも本番で踏めば事故になり、どちらも「検証手段を先に作る」ことで、実物に触れる前に見つかった。**

## 副産物 — ansy にあった中間CA秘密鍵を撤去した(2026-08-05)

配布の確認中に、**ansy に中間CAの秘密鍵(`~/.cert/ca/home_tls_ca.key`)がある**ことに気づいた。**中身は読んでいない。**

**経緯**: 中間CAを ansy で生成し、テストが通ってから quory へ証明書を配った当時の名残(Yoshinobu)。**運用上の役割は無い。**

**消してよいと判断した根拠**(いずれも実測):

- repo内でこの鍵を参照するのは `cert_renew_ca_host` 上のパスだけ。`cert_renew.yml` / `cert_renew_quory.yml` とも同ホストは `localhost`(= quory 上での実行)であり、**ansy を前提にした参照は1つも無い**
- **quory が鍵を持っていることの証拠**: quory の leaf 証明書の `notBefore` が `2026-08-01 00:35:11 JST` — 月次 timer(`*-*-01 00:35:00`)の発火時刻と一致し、`issuer` が `Home-TLS-CA`。**8月1日に中間鍵で発行している**

**なぜ気にするか**: この鍵があれば、内部のどんな名前に対しても全ホストが信頼する証明書を発行できる。**Phase 4 で捨てた SSH 鍵より強い権限**であり、しかも ansy は AI エージェントがシェルを持つホストである。開発側に本番の認証情報を置かないという設計が、ここだけ抜けていた。

**削除は Yoshinobu が実施**(復元不能な操作のため)。Coordinator は削除後に `.key` が存在しないことだけ確認した。同じことが起きないよう、**鍵を参照している場所**(`roles/homelab_cert_renew/defaults/main.yml`)に理由つきで書いた。

**残る注意**: 中間鍵は **quory の1コピー**になった。失っても ROOT から中間を作り直せるため復旧不能ではない(leaf は45日周期で入れ替わる)が、単一コピーであることは意識しておく。
