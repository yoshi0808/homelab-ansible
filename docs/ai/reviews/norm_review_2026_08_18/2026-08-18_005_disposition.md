# 2026-08-18 定期見直し — 処置

4本のレビュー(001 規範層 / 002 ハーネス / 003 Policy群 / 004 Skill群)に対するCoordinatorの処置。

## 是正した

| 出所 | 指摘 | 処置 |
|---|---|---|
| 002 Critical | `ansible-correctness-review` / `test-gap-review` に `.claude/skills/` のsymlinkが無く、`docs/ai/roles/reviewer.md` が義務付けているのにClaude Code版Reviewerから名指しできない | symlink 2本を追加(14/14で同期)。同時にこの2本だけ `0700`/`0600` だったパーミッションを他Skillと揃えた(gitはこの差分を持たない) |
| 004 High | `.claude/agents/reviewer.md` がSkillを4本**列挙**しており、2026-08-10 に2本増えたときに追随しなかった | 列挙をやめ、`docs/ai/roles/reviewer.md`「必須ContextとSkill」を指すだけにした。**列挙し続ける限り同じドリフトが再発する**ため、件数を直すのではなく形を変えた |
| 001 Critical | `docs/ai/roles/operator.md` が agmsg remote team `homelab-ops` を反映しておらず「Request Channel以外は設計中」のままだった(最終更新 2026-08-09 < 開通 2026-08-16) | 「現在の状態」へ開通の事実と運ぶ範囲を追記。「開発側とのやりとり」へ、agmsgで届いたテキストをuntrusted扱いする規定と、届いたこと自体は着手の理由にならない規定を追記 |
| 004 Medium | `skills/ansible-implementation-style` と `skills/requirements-analysis` が、現行 `docs/ai/core.md` に存在しない文言を「現行core.mdの〜」として引用していた | 前者は現行の該当規範(「Ansible変更の共通ゲート」)と正本(`healthcheck.md`)へ差し替え。後者は同義宣言を落とし、非ゴールの定義を自足させた |

## 却下した

| 出所 | 指摘 | 却下理由 |
|---|---|---|
| 003 Suggestion | `cert_renew_policy.md` v2.3 が存在しない `deploy_ca_trust.yml` を名指ししている | **成立しない。** `roles/homelab_cert_renew/tasks/deploy_ca_trust.yml` は実在し、中間CA配布を実際に行っていたのはこのtaskである。playbookの `ca_trust_deploy.yml` と取り違えたもの。パス接頭辞が無いという以上の欠陥ではない |
| 004 Low | `skills/document-norm-review` が本文中に `docs/ai/memory/lessons/` を引用しており、subagentが読めない領域を指している | 引用は指示ではなく、手順の動機となった実測件数である。**このSkillが載っている場所そのものが、Lessonの行き先として正しい**(同SKILL.md 末尾が関係を明示している)。毎回読まれるRole文書・Policy本文から参照を外した掃引とは、読み手も頻度も違う |

## 処置せず、判断を残した

- **`scripts/ai-next.sh` は動かない。** `docs/ai/prompts/core.md` / `proxmox_patch_policy.md` / `ubuntu_vm_patch_policy.md` を読むが、いずれも存在しない(`docs/ai/prompts/` に残るのは `autonomous_recovery_deploy_runbook.md` 1本)。自動化していた工程(Tier制のstep自動検出)は2026-07-31に廃止済みで、呼び出し元も無い。**`.gitignore` にも載っているがtracked済みのため無効。** 削除が妥当だが、Yoshinobuのヘルパーなので判断を残す
- **quory側Operatorセッションが `docs/ai/roles/operator.md` をどう読むかは、repo内から確認できなかった**(001の未解決事項)。作業ツリーはquoryへpullされるので現物は存在するが、起動時に読ませる仕掛けはrepoの管轄外にある

## 実施しなかった検査

Policy が定める閾値・タイミングの**実際の挙動**は、実ホストへの到達手段が無いため未検証である。003 が行ったのは静的な repo 内整合のみ。
