# テスト結果

対象: AC6・AC7・AC8のみ(AC1〜AC5は対象外)。差分は `roles/proxmox_patch_dryrun/tasks/main.yml`(`apply_forecast.yml`呼び出し5行)、新規 `roles/proxmox_patch_dryrun/tasks/apply_forecast.yml`、`docs/ai/policies/proxmox_operations_policy.md`(2026-09-19変更履歴)。実ホスト・本番Slackへは到達していない。`_003`/`_004`の判定は引き継がず、`apply_forecast.yml`・`main.yml`・`playbooks/proxmox_patch_weekly_full.yml`・merge/collectスクリプトを自ら読み、fixtureで独立に検証した。

## AC充足状況

| AC | 判定 | 根拠 |
|---|---|---|
| AC6 | PASS | fixtureで10シナリオ(自動適用される/されない、cluster理由/node理由/両方、single node、境界値、実データ)を実行。自動適用されない入力(cluster MAJOR判定、remove、node閾値超過)では通知本文に理由付きで警告が追加され、自動適用される入力(閾値未満・remove無し・PATCH_READY)では警告が一切追加されないことを確認した。BLOCKED/NO_UPDATES入力ではapply_forecastの全タスクが`_pre_status`ガードでskipされ未定義変数エラーも起きないが、これらは元々専用の通知文("自動パッチ適用は停止します"等)で非自動適用を告知済みであり、AC6の対象はCodex分類(NEEDS_CLASSIFICATION)経路である。 |
| AC7 | PASS | `apply_forecast.yml`のnode別比較は`unified_dryrun.node_summaries[<node>].update_count`(`proxmox-dryrun-merge.py:79`、その node の`apt-get -s dist-upgrade`出力から"Inst "行を数えたもの)を使っており、apply側`_sim_updates \| length`(`proxmox_patch_apply_node/tasks/main.yml:256-259`、同じく"Inst "行のカウント)と同じnode単位・同じ計測方法である。cluster和集合(`total_unique_updates`)は比較に使われていないことをコードとfixtureの両方で確認した。 |
| AC8 | PASS | `apply_forecast.yml`全体を読んだが`_final_status`・`_notify_channel`・`_notify_status`への代入は無く、`_notify_body`のみを追記する。`grep -rn _apply_forecast_`でrepo全体を検索し、他ファイルからの参照が無いことを確認した(exit codeやSemaphore判定はこの変数を経由しない)。fixture全12シナリオで`_final_status`が入力値のまま変わらないことを確認した。 |

## 実測

- fixture `/tmp/pmut_fixture/test_forecast.yml`(A〜J、10 play)をlocalhost/connection:localで`apply_forecast.yml`を直接includeし、role_pathを実repoの`roles/proxmox_patch_dryrun`に向けて実行(`閾値lookup`は実repoの`proxmox_patch_apply_node/defaults/main.yml`を読む、値100を実際に読んだことを出力で確認)。
  - A: pve1=40/pve2=50・remove無・PATCH_READY → 警告なし。
  - B: pve1=40/pve2=105(閾値100超)・PATCH_READY相当 → node理由のみ、「全体は自動適用されません」行は出ない(_004 Major-1が懸念した誤読み文言は、現行コードでは`_apply_forecast_cluster_warnings`と`_apply_forecast_node_warnings`が別ブロックのため発生しないことを確認)。
  - C: `_final_status=MAJOR_UPGRADE_DETECTED`(内容判定)、node件数は全て閾値未満 → cluster理由のみ。
  - D: `_has_removes=true`(cluster和集合)、node件数は全て閾値未満 → cluster理由(remove)のみ。
  - E: 到達node1台(pve2=60)・閾値未満 → 警告なし。
  - F: 到達node1台(pve1=150、閾値100超) → node理由のみ。
  - G: MAJOR判定+remove+pve1=150件超過を同時発生 → cluster理由2件・node理由1件が両ブロックとも欠落・重複なく出力され、クラッシュしない。
  - H: pve1=100(閾値と同値)・pve2=87 → 警告なし(`>`の厳密不等号どおり、境界一致は超過扱いにならない)。
  - I: pve1=101・pve2=87 → node理由(pve1)のみ、境界+1で確実に検出。
  - J: 2026-09-19実測を模したpve1=87/pve2=88・remove無・閾値100 → 警告なし(自己検証項目「実データを閾値100で通すと予告が出ない」を実行確認)。
  - すべて`ok`終了、`failed=0`。
- fixture `/tmp/pmut_fixture/test_blocked.yml`(K・L)で`_pre_status=BLOCKED`および`NO_UPDATES`を再現し、`apply_forecast.yml`の全タスクがskipされること、`_notify_body`が変更されないこと、未定義変数エラーが起きないことを確認した。
- `grep -rn "_apply_forecast" --include="*.yml" .` — `apply_forecast.yml`自身以外に参照なし(AC8根拠)。
- `playbooks/proxmox_patch_weekly_full.yml`のStep 2b(`_wf_proceed`判定、320-400行付近)を読み、Friday dry-runの`_final_status == MAJOR_UPGRADE_DETECTED`または`remove_count>0`のMAINTENANCE_REQUIREDは実際にweekly full全体をSaturday朝にend_playさせることを確認した(cluster理由の文言が「run全体」を指すことの裏付け)。node単位の閾値超過はweekly full自体のこのゲートには現れず、Saturdayのapply-node roleが個別nodeのre-dry-runで初めて判定するため、node理由の文言が「維持された場合に…見込み」と断定を避けているのは実際の判定タイミングと整合する。
- `roles/proxmox_patch_dryrun/files/proxmox-dryrun-collect.sh`のInst行parse正規表現と`proxmox_patch_apply_node/tasks/main.yml`のInst行parse正規表現を突合し、両者とも`apt-get -s dist-upgrade`の"Inst "行を数える点で同じ計測対象であることを確認した(collect.sh側はバージョン情報の括弧まで要求するやや厳密な正規表現だが、実際のapt出力形式では一致する)。

## Not Run / 安全境界

- 実ホスト(pve1/pve2)への到達、Semaphore実ジョブでの発火、本番Slackへの送信は行っていない。fixtureはすべてlocalhost/connection:localのset_fact・include_tasksのみで、`common_slack`のnotify.ymlは一度も呼んでいない。
- `role_path`変数はfixtureで実repoパスを手動指定したため、`apply_forecast.yml`がroleコンテキスト外から呼ばれた場合の`role_path`解決そのもの(Ansibleが自動設定する経路)は検証していない — 実行経路は常にrole内`include_tasks`のため実運用への影響はないと判断したが、明示的な確認はしていない。
- AC1〜AC5(閾値50→100、Policy追記の内容)は本レポートの対象外。

## 未解決事項

- なし(AC6・AC7・AC8の範囲内で見つかった欠陥は無い)。`_004`が指摘したMajor-1(導入文の無条件フレーズ)は現行コードでは再現せず、実装側の差し戻し修正で解消済みであることをfixtureで確認した。

判定: AC6 PASS / AC7 PASS / AC8 PASS。
