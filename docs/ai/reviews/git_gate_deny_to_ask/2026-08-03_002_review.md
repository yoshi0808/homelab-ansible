# review: `git commit` / `git push` を deny から ask へ

作成日: 2026-08-03 / Reviewer(subagent) / 対象: requirement `2026-08-03_001_requirement.md`

適用Skill: `skills/code-review/SKILL.md`(出力フォーマット)、`skills/document-norm-review/SKILL.md`(規範文書の欠陥クラス)、`docs/ai/roles/reviewer.md`。

## 対象パス

- `/home/yoshi/.claude/settings.json`(user scope、git管理外、読むのみ許可されている)
- `/home/yoshi/homelab-ansible/.claude/settings.json`
- `docs/ai/core.md`
- `docs/ai/roles/coordinator.md`
- `docs/ai/policies/incident_capture_policy.md`
- `docs/ai/memory/decisions/approval-authority-for-real-host-operations.md`
- `docs/ai/reviews/git_gate_deny_to_ask/2026-08-03_001_requirement.md`(新規、要件そのもの)

## Code Review: git commit/push gate (deny → ask)

### Summary

3層(user `permissions.deny` / project `permissions.deny` / project `autoMode.hard_deny`)は全て移行済みで、`jq`で読める妥当なJSONとして`permissions.ask`と`autoMode.soft_deny`に揃っている(AC-4相当、確認済み)。規範文書4本(`core.md`・`coordinator.md`・`incident_capture_policy.md`・decisions記録)は、要件のP0〜P2をすべて満たす形に書き換わっており、「AIは行わない」と読める旧来の断定は`docs/`配下(reviews/adr/memoryを除く)に残っていない(AC-3、機械的grep確認済み)。Critical findingsはゼロ。索引ファイル1件に軽微なドリフトがあり、Suggestionとして報告する。AC-1/AC-2はCoordinator対話セッション内の承認プロンプトが対象であり、本レビューの実行環境(subagent)では受け手になれず検証不能。

### Critical Issues

なし。

### Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| 1 | `docs/ai/core-migration-map.md` | 82(C17-01行) | 「**[既存]** commit/push禁止は`docs/ai/core.md`」という記述が、denyからaskへの変更後もそのまま残っている。移動先パスの指定(`docs/ai/core.md`)自体は正しく、そこを開けば現行の正しい内容が読めるため実害は小さいが、この索引の文言単体を読むと「commit/pushは禁止」という古い分類が今も正だと誤解しかねない(`skills/document-norm-review/SKILL.md`の欠陥クラス6・追跡表のドリフトに該当)。次にこの表へ触れる機会に「commit/push askはcore.md」程度へ言い換えるとよい。blockingではない | document-norm-review クラス6(追跡表のドリフト) |

### What Looks Good

- **3層の解除を個別に確認した(AC-4相当)。**
  - user scope `~/.claude/settings.json`: `permissions.deny`は空配列、`permissions.ask`に`Bash(git commit*)` / `Bash(git push*)`の2件が入っている。
  - project scope `.claude/settings.json`: 同上に加え、`autoMode.hard_deny`は`["$defaults"]`のみに縮小され、旧hard_denyのgit commit/push節は`autoMode.soft_deny`側へ移設された文面で存在する(絶対パス呼び出しへの言及を含め、要件P0の文面をそのまま満たす)。
  - `/etc/claude-code/managed-settings.json`(システム管理設定、今回のdiff対象外)も独立して`permissions.ask`に同じ2件を持っており、3層の外側に矛盾する設定は無い。
  - 両settings.jsonとも`jq .`でパース可能な妥当なJSON。
  - `~/.claude/settings.local.json`・`<repo>/.claude/settings.local.json`(いずれもgit管理外)を確認したが、git commit/pushに関するdeny/askの上書きは無く、隠れた古い層は無い。
- **AC-3相当の機械的grepを`docs/`全体(reviews/adr/memoryを除く)に対して実施した。** 該当行は`core.md`(3箇所)・`coordinator.md`(2箇所)・`incident_capture_policy.md`(1箇所)・`roles/auditor.md`(1箇所)・`roles/implementer.md`(1箇所、パターン非一致だが同義)・`role-routing-index.md`(1箇所)のみで、いずれも「Yoshinobuの都度承認」「subagentは行わない」という新しい境界と整合しており、「AIは(無条件に)行わない」と読める記述は残っていない。
- **文書間ドリフトの掃引。** `docs/ai/core.md`「人間の権限と安全境界」「Codexとの関係」「subagentの禁止事項」の3箇所、`docs/ai/roles/coordinator.md`の承認境界表2行、`docs/ai/policies/incident_capture_policy.md` IC-023は、いずれも「決定権はYoshinobuのまま」「実行できるのは対話セッションのみ」「subagentは承認の有無にかかわらず行わない」の3点で一致しており、矛盾は見つからなかった。
- **P1(subagent不可の明示)を確認した。** `docs/ai/core.md`のsubagent節(「禁止・エスカレーション」相当箇所)に「`git commit` / `git push` も行わない。Yoshinobuの都度承認を得て実行できるのは対話セッションだけであり、subagentはその承認を求めることもしない」という明示があり、要件P1を満たす。`.claude/settings.json` `autoMode.soft_deny`の自然文言も同じ切り分けを明示しており、設定と文書が同じ境界を述べている。
- **P2(過去決定記録の非改変)を確認した。** `docs/ai/memory/decisions/approval-authority-for-real-host-operations.md`は本文を書き換えず、冒頭に後継requirementとcore.mdを指す1行を追記しただけ。`git diff`で本文側に差分が無いことを確認した。
- **`.git/hooks/pre-commit` / `pre-push`はdiff対象外であり、今回の変更で無効化・迂回されていない**(スクリプト自体は変更されておらず、`ask`は許可判定の層でありhookの実行タイミングには影響しない)。

### Verdict

Approve(Suggestion 1件のみ、blockingではない)。

## 未検証事項

- **AC-1 / AC-2(確認プロンプトの実地表示)は本レビューでは検証できない。** 承認プロンプトの受け手はCoordinatorの対話セッションであり、subagentとして起動された本レビューはその対話に居合わせられない。requirementの記載どおり、Yoshinobu本人の前での実施と記録が必要。
- **subagentが実際に`git commit`相当を発行しようとした場合、`permissions.ask` / `autoMode.soft_deny`がRole(Coordinator/subagent)を区別して働くのか、それとも呼び出し元を問わず同じ許可判定になるのかは、設定のJSON構造からは確認できなかった。** 要件・規範文書はいずれも「設定ではRoleを区別できないため、この層は文書でしか引けない」と明記しており前提として承知済みだが、この前提そのもの(=区別できないこと)を実地で確認したわけではない。実際にsubagentへcommitを試みさせて挙動を確かめる検証は、本番リポジトリへの誤commitリスクを伴うため本レビューでは実施しなかった。
