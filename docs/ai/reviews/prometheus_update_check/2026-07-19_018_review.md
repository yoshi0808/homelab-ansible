# prometheus_update_check レビュー016対応 再レビュー

## 結論

**Approve** です。

016 の Must-fix 1件とSuggestion 1件は解消しています。検証済みraw `dry_run`がmode判定の唯一のsourceになり、競合する旧namespaced extra-varでは破壊的gateを迂回できません。rollback dry-run通知も主ゲートとnative check modeの双方に正しい表現へ修正され、既存安全機構への回帰はありません。

## Must-fix

なし。

## Suggestions

なし。

## Nits

なし。

## 確認結果

### 1. raw `dry_run` への一本化

- `prometheus_update_check_dry_run` defaultは削除されている。
- role/playbookの実コードに`prometheus_update_check_dry_run`参照は残っていない。
- 次の全箇所が、playbook preflightとrole先頭で検証するraw `dry_run | bool`を直接参照している。
  - rollback dry-runの`rollback_available`再分類
  - rollback target debugの注記
  - upgrade destructive gate
  - manual rollback restore gate
  - operation summaryのmode表示

assertと実際のdestructive boundaryが同じ変数を見るため、016で指摘した変数優先順位による分離は解消しています。

### 2. 競合extra-varによる迂回不能

localhost fixtureに次を同時指定して確認しました。

```text
-e dry_run=true -e prometheus_update_check_dry_run=false
```

未参照のnamespaced値はfalseでも、raw `dry_run`由来のupgrade gateとmanual rollback restore gateは両方falseのままです。旧aliasはrepoの実コードから消えているため、この競合値でinspectionがapplyへ化ける経路はありません。

他のmode入力についても、`rollback`はnamespaced値を入力検証・discovery・destructive gateで一貫して参照し、target/rollback_to等も検証対象と実使用値が一致しています。今回のようにassertとgateが別変数を見る経路は見つかりませんでした。

### 3. rollback dry-run通知

- `roles/prometheus_update_check/tasks/notify.yml:39`

`rollback_available`本文は`not executed in dry-run mode`へ変更されています。これは主経路`-e rollback=true -e dry_run=true`と、二次経路`--check -e rollback=true -e dry_run=false`の双方で正確です。

ベースcommit `0d8f2e4`との差分はこの1行だけ（1 insertion / 1 deletion）です。status、channel、Slack送出条件、`check_mode: false`、common Slackの`skip_notifications`経路、その他本文に変更はありません。更新後SHA-256も対応記録と一致しました。

- `notify.yml`: `0890b4c9705b0acf61efc28d33c77aef9ac901e4056de87c74a74843c4dfc631`

### 4. 既存安全機構の非回帰

次の3ファイルはベースcommit `0d8f2e4`とのdiffがなく、SHA-256も対応記録と一致します。

- `upgrade.yml`: `446521a2b9aa1f1c62eabcdc2d045f37d034938be79383074e21493d4a1e01a9`
- `manual_rollback.yml`: `2c366876eccfba092d0727a8674ebfb0d8567bfca855f17a6dfd344553c9429a`
- `discover_backups.yml`: `c32cf10735e545056fb55d4aa26047e0e05523445bedb7b9e55b1d2de2ae26f1`

したがって、transaction lock、世代の排他的作成とcleanup所有権、restore直前再検証、checksum、新promtool config検証、active/version/health retry、全AND復旧判定、protected retention、lock解放失敗通知、擬似失敗a〜gの処理本体は維持されています。

### 5. これまでの確認事項

次の点にも回帰はありません。

- playbook preflightとrole先頭の`dry_run is defined`による二重fail-closed
- `true`/`false`だけを許可する入力検証
- upgradeの`not rollback && not dry_run && not ansible_check_mode && upgrade_wanted`
- manual rollbackの`rollback && not dry_run && not ansible_check_mode && selected backupあり`
- backupありだけを`rollback_available`へ再分類し、backup不在／current収集失敗の`rollback_failed`を保持
- status名、通知分類、通知後fail条件の維持
- check mode通知例外と`skip_notifications`
- playbook headerの5起動例、金曜Semaphore taskへ`-e dry_run=true`を追加する必須手順
- tester-gate `check-mode-native`分類

## 実施した静的検証

- `ansible-playbook --syntax-check playbooks/prometheus_update_check.yml`: PASS
- dynamic include 4ファイルをimportする一時playbookのsyntax-check: PASS
- conflicting extra-varでupgrade/restore両gateがfalseとなるlocalhost fixture: PASS
- `scripts/check-tester-gate.sh`: PASS（37 playbooks）
- `git diff --check`: PASS
- 安全機構3ファイルのベースdiff／SHA-256照合: PASS
- notify.ymlの1行限定diff／更新後SHA-256照合: PASS
- 旧namespaced aliasの実コード参照scan: 該当なし

実ホスト、GitHub API、Prometheus swap/restart/restore、Slack POST、Semaphore UIは操作していません。commit/pushも行っていません。
