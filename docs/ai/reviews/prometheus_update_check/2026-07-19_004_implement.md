# prometheus_update_check — レビューSuggestion対応

- 日付: 2026-07-19（JST）
- 実装: implementer2
- 対応レビュー: `docs/ai/reviews/prometheus_update_check/2026-07-19_003_review.md`
- 状態: Suggestion反映・local-only検証完了。実ホスト実行、commit、pushは未実施。

## 変更内容

`collection_failed` のSlack本文に、収集moduleが返した短いdiagnosticを追加した。

- installed binary側: `stderr`を優先し、空の場合だけ`msg`へfallbackする。
- GitHub API側: `uri` resultの`msg`だけを使う。
- どちらも前後空白を除き、先頭200文字だけを通知する。
- module messageがない場合は `(no module message)` と表示する。
- diagnostic行は `collection_failed` の場合だけ本文へ出す。

response content全体、JSON、module invocation、request情報は通知へ含めない。将来GitHub APIへ認証headerが追加されても、今回のdiagnostic経路がそれらを出力しない構造にした。

既存のversion parse、上位版比較、status分類、通知channel/status、通知条件、`not ansible_check_mode` guardは変更していない。`update_available` と `up_to_date` ではdiagnosticを表示しない。

## 更新ファイル

- `roles/prometheus_update_check/tasks/main.yml`
- `docs/ai/reviews/prometheus_update_check/2026-07-19_004_implement.md`（本ファイル）

## 検証

- `ansible-playbook --syntax-check playbooks/prometheus_update_check.yml`: PASS
- `scripts/check-tester-gate.sh`: 全37 playbook PASS
- `git diff --check`: PASS
- diagnostic sourceがbinary `stderr`/`msg`とURI `msg`だけであることを確認
- diagnostic sliceが200文字であることを確認
- diagnostic行が `collection_failed` 条件内だけにあることを確認
- 既存の通知条件とcheck-mode guardが不変であることを確認

実ホスト、GitHub API実通信、Slack送信は行っていない。

## Next step files

- `docs/ai/reviews/prometheus_update_check/2026-07-19_001_requirement.md`
- `docs/ai/reviews/prometheus_update_check/2026-07-19_003_review.md`
- `docs/ai/reviews/prometheus_update_check/2026-07-19_004_implement.md`
