# prometheus_update_check test result (2026-07-19)

## Result

**PASS** — quory から `playbooks/prometheus_update_check.yml` 相当を `--check --diff` で実行し、read-only 収集、version 判定、Slack 抑止、5分岐の擬似 fact テスト、Prometheus 本体の無風を確認した。

- monnie の Prometheus への設定変更・更新・restart: なし
- Slack 送信: なし
- mute: read-only チェックのため未設定
- commit / push: なし
- 実 IP: 記録なし

## 1. Execution arrangement

実行端末 ansy の作業ツリーにある対象 playbook/role は新規・未追跡で、quory の repository にはまだ存在しなかった。quory の repository を変更せずに「quory から実行」という gate を満たすため、次だけを quory の `/tmp` へ一時 staging した。

- `playbooks/prometheus_update_check.yml`
- `roles/prometheus_update_check/`
- 擬似 fact 分岐テスト用 playbook/tasks

quory の既存 `inventories/homelab/hosts.yml` を使用し、`ANSIBLE_ROLES_PATH` の先頭だけ一時 role directory に向けた。全検証後に staging directory を削除した。quory の repository と monnie のファイルには変更していない。

outer/inner Ansible とも `ANSIBLE_LOCAL_TEMP` は `/tmp` 配下を使用した。

## 2. Prometheus pre-state

monnie で read-only 記録した。

```text
prometheus.service active: active
failed state: active (not failed)
ActiveEnterTimestamp: 2026-07-12 18:02:03 JST
ExecMainStartTimestamp: 2026-07-12 18:02:03 JST
MainPID: 1228
binary mtime: 2026-06-20 15:03:10.076489364 +0900
binary size: 228333531
binary inode: 505722
```

## 3. Quory dry-run

quory 上で次に相当するコマンドを実行した。

```text
ansible-playbook -i inventories/homelab/hosts.yml \
  playbooks/prometheus_update_check.yml --check --diff
```

### Execution-host gate

localhost play が quory の facts を収集し、hostname assert は通過した。

```text
Ensure this playbook is running on quory: ok
All assertions passed
```

### Read-only collectors in check mode

次の task は `check_mode: false` により check mode 中も実走し、いずれも `ok` / `changed=false` だった。

- `/opt/prometheus/prometheus --version`
- GitHub latest release API への `uri` GET

実測 summary:

```text
host: monnie
current: 3.12.0
latest: 3.13.1
status: update_available
```

`3.13.1 > 3.12.0` のため `update_available` は論理的に正しい。

### Slack suppression

```text
Send Prometheus update-check notification: skipping
```

status は通知対象の `update_available` だったが、`not ansible_check_mode` guard により include は skip された。Slack 誤送信はない。

### Recap

```text
localhost: ok=2 changed=0 unreachable=0 failed=0 skipped=0
monnie:    ok=8 changed=0 unreachable=0 failed=0 skipped=1
```

## 4. Branch logic test

quory の `/tmp` に使い捨て localhost playbook を作り、production と同じ collection 判定、Ansible `version` 比較、status 分類、通知条件、本文要素を擬似 fact で検証した。

| Case | Pseudo facts | Expected / actual status | Notify | Additional assertion |
|---|---|---|---|---|
| a | current `3.4.0`, latest `3.5.0` | `update_available` | yes | `3.4.0 → 3.5.0`、release URL、「自動更新は行いません」を含む |
| b | current `3.5.0`, latest `3.5.0` | `up_to_date` | no | no notification |
| c | current `3.6.0`, latest `3.5.0` | `up_to_date` | no | latest が下位なら no notification |
| d | binary rc `127` | `collection_failed` | yes | installed diagnostic を表示 |
| e | uri status `-1` | `collection_failed` | yes | GitHub API diagnostic を表示 |

最終実行結果:

```text
newer release:                 update_available, notify=True
same release:                  up_to_date, notify=False
installed newer than latest:  up_to_date, notify=False
installed binary failure:     collection_failed, notify=True
GitHub API failure:            collection_failed, notify=True
localhost recap: ok=30 changed=0 failed=0
```

### Harness observation

初回 harness 実行は、assert 内 diagnostic 文字列のコロンを YAML が mapping と解釈したため、最初の assert で fail-closed した。これは production playbook/role の failure ではなく、使い捨て harness の quoting 不備である。対象2条件を明示的な文字列に修正して全5ケースを最初から再実行し、上記のとおり全件 PASS した。初回を含め実ホストへの task はなく、変更は発生していない。

## 5. Prometheus post-state / no-impact gate

dry-run と branch test 後に pre-state と同じ read-only checks を再実行した。

```text
prometheus.service active: active
failed state: active (not failed)
ActiveEnterTimestamp: 2026-07-12 18:02:03 JST
ExecMainStartTimestamp: 2026-07-12 18:02:03 JST
MainPID: 1228
binary mtime: 2026-06-20 15:03:10.076489364 +0900
binary size: 228333531
binary inode: 505722
```

pre/post で service state、start timestamp、PID、binary mtime/size/inode は完全一致した。Prometheus の restart、binary 更新、設定変更はない。

## 6. Final assessment

| Gate | Result |
|---|---|
| quory execution-host assert | PASS |
| command collector runs under check mode | PASS |
| GitHub uri collector runs under check mode | PASS |
| current/latest/status summary | PASS |
| actual version comparison | PASS (`3.12.0 → 3.13.1`, update available) |
| Slack suppressed in check mode | PASS |
| update-available branch | PASS |
| equal-version no-notify branch | PASS |
| lower-latest no-notify branch | PASS |
| binary failure diagnostic branch | PASS |
| GitHub API failure diagnostic branch | PASS |
| Prometheus service remains active | PASS |
| Prometheus binary metadata unchanged | PASS |
| Temporary files removed | PASS |

総合判定: **PASS**。
