# prometheus_update_check dry_run primary-gate test result (2026-07-19)

## Result

**PASS** — reviewer approve 018 後の実 playbook を quory から6経路で検証し、raw `dry_run` が主ゲート、native `--check` が独立した二次ゲートとして機能することを確認した。

- 実 APPLY: なし
- download / backup / swap / restore: なし
- Prometheus restart: なし
- Slack POST: なし（全実行 `skip_notifications=true`）
- monnie service/binary pre-post: 完全一致
- commit / push: なし
- 実 IP: 記録なし

実 `3.13.1` 適用は行っておらず、Yoshinobu が後日 Semaphore から実施する別作業である。

## 1. Execution arrangement

対象 playbook/role は ansy の未追跡作業ツリーにあり、quory repository には未反映だった。quory repository を変更せず、次を `/tmp/prometheus-update-check-dryrun-gate-tester-20260719` へ一時 staging した。

- `playbooks/prometheus_update_check.yml`
- `roles/prometheus_update_check/`
- notification render に必要な `roles/common_slack/`

quory の既存 inventory を使用し、`ANSIBLE_ROLES_PATH` の先頭だけ staging role directory に向けた。outer/inner Ansible の `ANSIBLE_LOCAL_TEMP` はすべて `/tmp` 配下。検証後 staging を削除した。

## 2. Static safety confirmation

実行前に次を確認した。

- playbook の最初の localhost play に `dry_run is defined` assert がある。
- 続く localhost assert が raw value を `true` / `false` のみに制限する。
- monnie play は両 preflight assert と quory hostname assert の後にだけ開始する。
- Phase U gate は全AND:
  - not rollback
  - not raw `dry_run`
  - not `ansible_check_mode`
  - upgrade wanted
- Phase R gate は全AND:
  - rollback
  - not raw `dry_run`
  - not `ansible_check_mode`
  - selected backup path exists
- 旧 `prometheus_update_check_dry_run` は実コードから削除され、参照がない。

この配置を確認してから、`--check` なしの fail-closed ケースを実行した。

## 3. Monnie pre-state

```text
prometheus.service active: active
failed state: active (not failed)
ActiveEnterTimestamp: 2026-07-12 18:02:03 JST
ExecMainStartTimestamp: 2026-07-12 18:02:03 JST
MainPID: 1228
binary mtime: 2026-06-20 15:03:10.076489364 +0900
binary size: 228333531
binary inode: 505722
```

## 4. Case 1 — `dry_run` omitted

実行条件:

```text
-e skip_notifications=true
```

結果:

```text
Require an explicit Prometheus dry-run mode: FAILED
assertion: dry_run is defined
NO_DRY_RUN_RC=2
```

localhost preflight で停止し、monnie play は開始しなかった。installed version collection、GitHub API collection、role initialization、Phase U/R のいずれにも進んでいない。

```text
localhost: ok=1 changed=0 failed=1
monnie: play not started
```

判定: **PASS — 未指定は auto-upgrade せず fail-closed**。

## 5. Case 2 — `dry_run=true`

実行条件:

```text
-e dry_run=true -e skip_notifications=true
```

結果:

- required-value assert: PASS
- boolean value assert: PASS
- quory hostname assert: PASS
- installed version command: 実走、changed=false
- GitHub latest `uri`: 実走、changed=false
- Phase U upgrade include: skipped
- Phase R rollback include: skipped

summary:

```text
dry_run: true
current: 3.12.0
target: 3.13.1
result: 3.12.0
status: update_available
error: (empty)
```

通知は render のみ行われ、`skip_notifications=true` により webhook load / extraction / POST は skipped。

```text
update available (not applied): 3.12.0 → 3.13.1
```

recap:

```text
localhost: ok=4  changed=0 failed=0
monnie:    ok=15 changed=0 failed=0 skipped=12
```

判定: **PASS — inspection only**。

## 6. Case 3 — `dry_run=false --check`

実行条件:

```text
--check --diff -e dry_run=false -e skip_notifications=true
```

raw mode は apply 判定経路に入ったが、native `ansible_check_mode` が独立した二次 destructive gate になった。

- collection: 実走
- `dry_run: false`
- status: `update_available`
- Phase U upgrade include: skipped
- Phase R rollback include: skipped
- notification text: `not applied under --check`
- Slack POST: skipped

```text
current: 3.12.0
target: 3.13.1
result: 3.12.0
status: update_available
```

```text
localhost: ok=4  changed=0 failed=0
monnie:    ok=15 changed=0 failed=0 skipped=12
```

判定: **PASS — apply 引数経路でも native check-mode により無変更**。

## 7. Case 4 — rollback inspection

実行条件:

```text
-e rollback=true -e dry_run=true -e skip_notifications=true
```

read-only discovery は実走した。

- backup root canonicalization/stat: PASS
- actual backup root: directory 不在
- valid backup candidate: 0
- selected backup: none
- Phase U upgrade include: skipped
- Phase R restore include: skipped
- binary restore/restart: 未実行

rollback target 表示:

```text
Prometheus rollback target (not executed in dry-run mode):
3.12.0 -> ? (backup unavailable)
```

summary:

```text
dry_run: true
current: 3.12.0
status: rollback_failed
result: 3.12.0
error: No valid Prometheus backup generation is available
```

backup 不在のため通知は `rollback_failed` branch が render され、`skip_notifications=true` で POST は skipped。最終 fail task が設計どおり発火した。

```text
ROLLBACK_DRY_RUN_RC=2
localhost: ok=4  changed=0 failed=0
monnie:    ok=22 changed=0 failed=1 skipped=14
```

ここでの非0は期待する fail-closed。valid backup がある場合に使う `rollback_available` notification branch の文言も実装上 `rollback target (not executed in dry-run mode)` であることを確認した。実環境には backup がないため、その branch の actual render は発生していない。

判定: **PASS — discovery のみ、restore なし、backup 不在を fail-closed**。

## 8. Case 5 — legacy alias bypass attempt

実行条件:

```text
-e dry_run=true
-e prometheus_update_check_dry_run=false
-e skip_notifications=true
```

競合する旧 namespaced alias を false にしても、実 playbook/role は raw `dry_run=true` だけを mode source として使用した。

```text
dry_run: true
current: 3.12.0
target: 3.13.1
result: 3.12.0
status: update_available
```

- Phase U upgrade include: skipped
- Phase R restore include: skipped
- Slack POST: skipped
- failed: 0
- changed: 0

判定: **PASS — 旧 alias で迂回不能**。

## 9. Case 6 — invalid `dry_run` value

実行条件:

```text
-e dry_run=maybe -e skip_notifications=true
```

結果:

```text
Validate the explicit Prometheus dry-run mode: FAILED
dry_run must be boolean true or false. Got dry_run=maybe.
INVALID_DRY_RUN_RC=2
```

localhost preflight で停止し、monnie play は開始しなかった。

```text
localhost: ok=2 changed=0 failed=1
monnie: play not started
```

判定: **PASS — 不正値を role 実行前に拒否**。

## 10. Monnie post-state / no-impact gate

```text
prometheus.service active: active
failed state: active (not failed)
ActiveEnterTimestamp: 2026-07-12 18:02:03 JST
ExecMainStartTimestamp: 2026-07-12 18:02:03 JST
MainPID: 1228
binary mtime: 2026-06-20 15:03:10.076489364 +0900
binary size: 228333531
binary inode: 505722
```

pre/post で service state、start timestamps、MainPID、binary mtime/size/inode は完全一致した。restart、swap、restore はない。

## 11. Final assessment

| Gate | Result |
|---|---|
| `dry_run` omitted fail-closed before monnie play | PASS |
| invalid `dry_run` rejected before monnie play | PASS |
| `dry_run=true` collection-only mode | PASS |
| actual version classification `3.12.0 → 3.13.1` | PASS (`update_available`) |
| `dry_run=false --check` independent safety gate | PASS |
| rollback discovery runs in explicit dry-run | PASS |
| backup absent => `rollback_failed` | PASS |
| rollback target wording | PASS (`not executed in dry-run mode`) |
| legacy alias bypass blocked | PASS |
| Slack POST suppressed in all paths | PASS |
| monnie service/binary unchanged | PASS |
| temporary staging removed | PASS |

総合判定: **PASS**。
