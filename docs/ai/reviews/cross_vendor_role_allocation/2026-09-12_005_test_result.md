# テスト結果

対象: staged diff一式、要求 `2026-09-12_001_requirement.md`、gitignoredの `new-session.sh`。実ホスト、外部サービス、tmux reset、agmsg登録変更は対象外とした。

## AC充足状況

| AC | 判定 | 根拠 |
|---|---|---|
| AC1 | 設計上PASS / 実案件未実施 | `coordinator.md` とADR-013はClaude Code Implementerが調査・実装・自己検証・差し戻し対応を担い、Coordinatorは実装ファイルを編集しないと定義している。実案件での挙動は未確認。 |
| AC2 | 設計上PASS / 起動未実施 | 差分ReviewerはfreshなCodex native subagent、ImplementerはClaude Codeと、Role表・ADR-013・agent-messaging Contextで別ベンダー／別経路になっている。実spawnは未実施。 |
| AC3 | 設計上PASS / 起動未実施 | 計画ReviewerとImplementerは別Claude identity、差分ReviewerとTesterは別fresh Codex context、AuditorはClaude Code案件クローズ時1回という記述がRole表・Context・ADRで整合。 |
| AC4 | Not Run | `new-session.sh --reset` はtmux session・app-server・常駐Roleを変更するため実行していない。スクリプト上の `CLAUDE_ROLES=(implementer reviewer auditor)` は確認済み。 |
| AC5 | **未充足（blocking）** | 文書整合検査とdiff checkはPASS。ただし現行 `docs/ai/status.md` のNext欄に「Testerをcodex側へ移すか(未決)」が残り、新配分のTester=Codexと矛盾するlive記述。独立規範レビューのblockingなしはこの残存を解消するまで確定できない。 |
| AC6 | Not Run | 実験対象3件のcloseout観測はまだ存在せず、評価値・継続判断は未実施。 |

## 実測

- `python3 scripts/check-doc-consistency.py`: rc=0。check1 `OK (122 compared)`、check2 `OK (8 compared)`、check3 `OK (115 compared)`。index（staged内容）基準で実行されることをスクリプト現物で確認した。
- `bash -n new-session.sh`: rc=0。
- `git diff --cached --check`: rc=0。
- Role/CLI/model/effort表を `coordinator.md`、ADR-013、`.claude/agents/*.md`、`agent-messaging.md`、`new-session.sh`で突合。agmsg常駐Claude Roleは Implementer / 計画Reviewer / Auditor、native Codex Roleは差分Reviewer / Tester。実験表は全Roleで指定model・medium。Claude agent定義4件はcheck2の8比較で一致。
- `new-session.sh`の現物で `CLAUDE_ROLES=(implementer reviewer auditor)`、各Roleへの `--fresh`、共通 `--model sonnet`を確認した。
- 旧配分の履歴として保持されるADR・過去案件記録を除き、現行経路を旧配分として述べる記述を確認したところ、`docs/ai/status.md:61`のTester未決行が残存していた。これは履歴ではなく現在のNext欄であるため、AC5の未解決事項と判定した。
- `git status --short`で変更は指定されたstaged成果物群のみ。`new-session.sh`はgitignored。commit/pushは実施していない。

## Not Run / 安全境界

- 実ホスト・外部サービスへの操作、tmux sessionのreset、agmsg team roster／登録の変更は行っていない。
- `new-session.sh --reset`によるAC4実resetはNot Run。したがって実際の常駐pane数、Claude起動成功、fresh session、app-server切替は未確認。
- 実験3案件の運用観測（AC1の実行実績、AC2/AC3のfresh起動実績、AC6の評価値）は未確認。

## 未解決事項

1. `docs/ai/status.md`の「Testerをcodex側へ移すか(未決)」を現行配分に合わせてCoordinatorが整理する必要がある。
2. AC4は実resetが必要だが、本テスト範囲では実施しない。
3. 3案件のcloseout後にAC6の継続・修正・撤回判断が必要。

判定: 条件付き。静的な経路・表の整合とローカル検査はPASSだが、AC5はlive status残存のためblocking、AC4/AC6はNot Run。
