# 再レビュー結果: unifi_backup_fetch

日付: 2026-06-15

対象:
- `docs/ai/reviews/unifi_backup_fetch/2026-06-15_001_requirement.md`
- `docs/ai/reviews/unifi_backup_fetch/2026-06-15_006_implement.md`
- `playbooks/unifi_backup_fetch.yml`
- `roles/unifi_backup_fetch/defaults/main.yml`
- `roles/unifi_backup_fetch/tasks/main.yml`
- 現在の git 差分（未追跡の新規ファイルを含む）

## 結論

前回レビュー 005 の Medium 1件、Low 1件は修正されている。
追加の commit 阻害事項は確認されなかった。

要求仕様、`core.md`、`ubuntu_vm_patch_policy.md` との不整合もなく、
コード差分はこの内容で commit してよい。

## Findings

**指摘事項なし。**

## 前回指摘の確認

- レスポンスヘッダーを先に評価し、`X-CSRF-Token`、
  `X-Updated-CSRF-Token` の両方が空の場合だけ JWT fallback block を実行する
  構成へ変更されている。
- ヘッダーが有効な場合は JWT の分割、base64 デコード、JSON 変換を実行しないため、
  JWT 構造が変化してもヘッダー由来 CSRF を使用できる。
- ヘッダー値の選択、供給源判定、JWT fallback、最終 CSRF 決定、認証材料 assert
  には `no_log: true` が設定されている。
- `unifi_csrf_source` は秘密値を含まないラベルのみで、成功通知への記録も問題ない。

## 要求仕様との整合

- `hosts: pve1`、`become: true` で実行し、`connection: local` は使用していない。
- CloudKey への接続は `cloudkey.internal` を使用し、IPリテラルは含まれていない。
- TOKEN、CSRF、認証情報、認証ヘッダーを扱うタスクは `no_log` で保護されている。
- バックアップ取得、filename 検証、60秒の鮮度ガード、NFSへの保存、
  8世代ローテーションが実装されている。
- 同名ファイルでも `creates` でスキップせず、取得したファイルを `mv -f` で
  確定するため、保存せず成功扱いになる経路は解消されている。
- API由来 filename は allowlist、basename 一致、パス区切りと親参照の不在を
  root の `mv` より前に検証している。
- block / rescue / always による失敗捕捉、best-effort Slack通知、失敗時の再 fail
  は要求どおり維持されている。
- Semaphore UI で同一 job の同時実行を認めない運用前提のため、固定一時ファイルの
  並行実行競合は指摘対象外とした。
- `ubuntu_vm_patch_policy.md` が定義する Ubuntu patch / reboot 動作への変更はない。

## Verification

- `ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote ansible-playbook -i inventories/homelab/hosts.yml playbooks/unifi_backup_fetch.yml --syntax-check`: PASS
- 同条件の `--list-tasks`: PASS
- 対象の新規 YAML / Markdown に対する `git diff --check --no-index`:
  whitespace error なし
- `inventories/vars/cloudkey.yml` は既存の追跡ファイルであり、今回の変更対象には
  含まれていない。

## 残余リスク

CloudKey API と Synology NFS を使った実機 E2E は、バックアップ生成・保存・削除を
伴う変更系操作のためレビュー中には実施していない。APIレスポンスヘッダー名、
実際の filename 形式、NFS上の rename と権限変更は初回手動実行で確認が必要である。

## 判定

**承認。commit してよい。**

## Next step files

- docs/ai/reviews/unifi_backup_fetch/2026-06-15_001_requirement.md
- docs/ai/reviews/unifi_backup_fetch/2026-06-15_006_implement.md
- docs/ai/reviews/unifi_backup_fetch/2026-06-15_007_review.md
- docs/ai/reviews/unifi_backup_fetch/2026-06-15_008_final.md
