# review: `skills/` 経緯注記除去(round4)

日付: 2026-08-02 (JST)
対象: `git diff skills/`(8ファイル、12箇所)。requirement/implement記録は本レビューの対象外(記録として参照するのみ)。
参照: `2026-08-02_001_requirement.md`、`2026-08-02_002_implement.md`

## Summary

AC1・AC2・AC4は独立検算で成立を確認した。AC3(逐行突合)で、本ラウンドの編集が原因で生じた**同一ファイル内の宙ぶらりん参照**を1件検出した(`skills/ansible-implementation-style/SKILL.md`)。除去対象と判定した10箇所の分類自体(クラスC)は妥当で、誤ってクラスA/Bを削った箇所は無い。無編集とした4本の判断も妥当。

## Critical Issues

なし。

## Major Issues

1. **`skills/ansible-implementation-style/SKILL.md`:91 — 削除した定義文への宙ぶらりん参照が生じた**
   - 改訂前L65は「出典: 旧`docs/ai/prompts/core.md` §18.3(項目1・4・5)および§18.2(項目2・3のinclude例外)から移設(2026-07-26、移行表C18-03/04/06/07/08/10)。」で始まり、この文が「出典」=旧core.md §18.3という定義を与えていた。実装はこの文全体(クラスC相当の移設経緯部分を含む)を削除した。
   - ところが同ファイルL91(「適用条件」節末尾、本ラウンドで変更していない行)に「ただし上記『check_modeの実装上の落とし穴』はReviewerも確認対象とする(**出典の§18.3**が実装時とレビュー時の双方を対象としていた)。」が残っている。L65の定義文が消えたことで、L91の「出典の§18.3」が**同一ファイル内のどこにも先行定義を持たない**宙ぶらりんな参照になった。
   - 実害の切り分け: 「Reviewerも確認対象とする」という規則そのものは文として独立して読め、手順は曖昧にならない(R3は形式的には非該当)。一方で、その規則の根拠として書かれた括弧内の説明は読者が検証不能になっており、`skills/document-norm-review/SKILL.md`欠陥クラス1(宙ぶらりん参照)に該当する。requirement R4「他の文書から…参照が壊れていないか」は他ファイルへの言及のみ検証されており、**同一ファイル内での参照断絶**は実装の確認(削除文言のリテラル検索)では検出できない種類の欠陥だった。
   - 対応案: L91の括弧内「(出典の§18.3が実装時とレビュー時の双方を対象としていた)」を、外部出典を指さない自己完結の表現に書き換える(例:「実装時とレビュー時の双方で踏む問題であるため」)。

## Suggestions

なし。

## What Looks Good

- **AC1(クラスC=0件)**: `grep -n "2026-0[0-9]-[0-9][0-9]"`を12本全件に独立実行し、残存する日付付き記述はすべてクラスA(revision追跡)または明示`根拠:`タグ付きのクラスBであることを確認した。旧`確定)`/`確認済み)`/`自作)`/`に移設`/`廃止に伴い`等の断片パターンでの再検索でも残存ゼロ。
- **AC2(件数不変)**: 実装の報告(A:10→10、B:21→21)を鵜呑みにせず、`git show HEAD:<path>`と現行ファイルの両方に対しファイル別に`grep -c`を独立実行し、ファイル単位・合計単位の両方で一致を確認した。文言変更によって検索網から漏れた形跡は無い(diffの12箇所すべてが日付部分のみの削除で、「取り込み時点のrevision」「revision追跡」「根拠」という語自体には触れていない)。
- **AC3(逐行突合)**: `git diff skills/`の全12ハンクを確認。落ちているのは日付・組織再編理由・旧core.md移設経緯のみで、手順・判定基準・出力の型は1箇所も変わっていない。上記Major #1を除き、削除文と直前直後の文の接続にも問題なし。
- **B/C境界の5箇所**(`ansible-implementation-style` L46・L50、`document-norm-review` L20・L63、`test-strategy` L35-36): いずれも直前・直後の規則を具体的事故・実測で裏づけており、Coordinatorが定義した機能基準(規則を裏づける=B、文書自身の来歴=C)に照らして5箇所ともB判定は妥当。自己言及的な来歴(「自作」「確定」「統合」「移設」)側だけを一貫してCとして除去している。
- **無編集4本**(`code-review`・`risk-assessment`・`requirements-analysis`・`incident-recording`)の判断: 独立に`grep -n`で日付付き記述を再確認し、いずれもクラスA(取り込み時点のrevision)または明示`根拠:`タグ(requirements-analysis L38)のみで、クラスCが存在しないことを確認した。
- **見出しの内容表現**: `duplication-reuse-check`「## homelab-ansibleでの運用手順」、`test-strategy`「## Ansible文脈への翻訳」「## 実行コマンドの組み立て」いずれも、日付注記を外した後も見出しが節内容を過不足なく表している。
- **宙ぶらりん参照(他ファイル)**: 削除した10パターンの文言と、変更した見出しテキストをリポジトリ全体(`docs/ai/`・`skills/`・`roles/`・`playbooks/`・`inventories/`・`CLAUDE.md`・`AGENTS.md`)で検索し、`docs/ai/roles/{reviewer,implementer,tester}.md`からの参照はいずれもSKILL.mdファイル単位の参照であって節見出し・削除文言単位ではないことを確認した。`docs/ai/context/system/*.md`等に類似の「(2026-07-29、Tech Lead廃止に伴い統合)」という文言があるが、これは独立した別記述(round2/3のスコープ)であり本ラウンドの削除箇所への参照ではない。
- **AC4**: `python3 scripts/check-doc-consistency.py`・`bash scripts/git-pre-commit-check.sh`を自分で再実行し、いずれもOK・exit 0を確認(実装の報告値のコピーではなく再実行)。
- **非ゴールの遵守**: `docs/ai/`・`playbooks/`・`docs/ai/reviews/`・`docs/ai/memory/`は本diffに含まれず(`git diff --stat skills/`で確認)、対象は`skills/`のみに収まっている。手順本体(check_modeの3パターン分類、0件ガードの検証手順、B/C判定基準等)はいずれも一言一句変化していない。

## Verdict

Major 1件(`skills/ansible-implementation-style/SKILL.md`:91の宙ぶらりん内部参照)を要修正。他の観点(AC1・AC2・AC4、無編集4本の判断、B/C境界5箇所、他文書からの参照)はいずれも独立検算の上で問題なし。

---

## Coordinatorによる対応(2026-08-02)

**Major 1件を是正した。** クラスCとして落とした「出典: 旧`docs/ai/prompts/core.md` §18.3…から移設」の定義を、同ファイル末尾が `(出典の§18.3が実装時とレビュー時の双方を対象としていた)` として参照しており、定義だけが消えて参照が残っていた。

**当該括弧を削除した。** 直前の「上記『check_modeの実装上の落とし穴』はReviewerも確認対象とする」という**規則本体は無改変**であり、括弧は規則の由来を述べる経緯(クラスC)だった。参照先が消えた以上、残す理由も無い。

**同型の見落としが本シリーズで2回目である**(round3では `proxmox_operations_policy.md` の「§2.2 B案」ラベル削除が `playbooks/proxmox_patch_weekly_full.yml` の参照を切った)。**経緯を落とすときは、その経緯が定義していた語を他所が参照していないかを必ず確認する。** 今回は同一ファイル内、前回はファイル外だった。
