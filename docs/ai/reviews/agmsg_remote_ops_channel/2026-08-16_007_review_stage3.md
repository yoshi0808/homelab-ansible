# Code Review: `agmsg_remote_ops_channel` 段3(ansy側)文書差分 — round 4

## Summary

round 3 の High 1 / Medium 1を反映した staged 文書を再照合した。requirement のゴール・非ゴール・R5・AC6とdecisionの境界4は、**sync engine 1プロセスの常駐と復号・local store書込みまでは許容し、AIセッション・watcher・ジョブはメッセージ到達から起動しない**という同じ境界へ揃った。

人が見ていない間もメッセージが復号され、quory のlocal storeへ保存されるという代償も明示されている。blocking findingはなく、結論は Approve とする。

## Critical Issues

なし。

## Suggestions

なし。

## Previous Findings Resolution

| 前回 finding | round 4 判定 | 根拠 |
|---|---|---|
| High: requirement のゴール4・非ゴールがsync engine常駐許容と矛盾 | **解消** | ゴール4は「人が見ていないときにAIの文脈へ入れる常駐物」の禁止へ限定された。非ゴールもAIセッション・watcher・ジョブの起動禁止と、sync engine 1プロセスだけを例外とする常駐制約へ改訂された。R5 / AC6と同じ判定軸である。 |
| Medium: decision 境界4の「何かが動く」がengine同期まで否定し得る | **解消** | 境界4の禁止対象が「AIセッション・watcher・ジョブが起動する経路」へ限定され、その直後のengineによるlocal store書込み許容と両立した。 |

## Requirement Traceability

| ID | 判定 | staged差分で確認した根拠 |
|---|---|---|
| R3 | **ansy側を満たす** | implement §3 / §6にE2EE、server側cipher、別経路handoffを記録。quory側は未実施。 |
| R4 | **満たす** | runbook §2 / §7、implement §6、statusに`homelab` local-onlyとserver DB不在を記録。 |
| R7 | **文言を満たす** | runbook §8とdecision境界2・3に、送信前の人の同意、受信内容を権限扱いしないこと、着手前確認を明記。 |
| R8 | **満たす** | runbook §8とdecision境界3に、到達報告は可能だが着手前にYoshinobuへ尋ねる規則を明記。 |
| R12 | **ansy側を満たす／quory側は残作業** | runbook §6 / §9、implement §3にteam別identityを記録。quoryの`operator` joinは手順化済み・未実施。 |
| R13 | **ansy側を満たす／quory手順を明示** | runbook §7 / §9、implement §3 / §6、statusで`actas`を避ける。 |
| R16 | **満たす** | status「スナップショットから戻したときに要るもの」の8項目目にage鍵束を追加し、復旧可能性も場合分けした。 |

## What Looks Good

- R5 / AC6とdecision境界4は、常駐の有無ではなく「人が見ていないときにAIの文脈へ入るか」で線を引く。ゴール・非ゴールにも同じ例外範囲が反映され、受入判定が一意になった。
- accepted riskはdecision「引き受けたリスク」、requirement R5 / AC6、implement §7、runbook §8 / §9、statusから読み取れる。engine常駐という名前だけでなく、人不在時にも復号してlocal storeへ保存する実態と採用理由を残している。
- 新decisionは既存`ansy-must-not-trigger-production-changes.md`と同じ「本番の状態が変わるか」を判定軸にする。agmsgは情報共有に留まり、送信側の人選択、受信側の着手前確認、到達起点のAI処理禁止を別の境界として積むため、既存decisionと両立する。
- 撤回した旧境界を`docs/ai/`、`skills/`、`roles/`、`playbooks/`、`inventories/`、`CLAUDE.md`、`AGENTS.md`から機械的に検索した。残る一致はrequirement / implementの明示的な変更履歴だけで、現行規範として旧方針を主張する箇所はない。
- 二層の到達制御、鍵喪失時の条件別帰結、quoryの順序付き立ち上げ手順、R15とsync engine復帰の分離は維持されている。Operator Request Channelの本文・DLP・spool・forced commandを第2経路へ複製していない。
- 参照先のパスと見出しを再確認した。`roles/agmsg_server/templates/compose.yaml.j2`、`roles/agmsg_server/tasks/firewall.yml`、`playbooks/agmsg_server_setup.yml`、両Operations Context、既存decision、案件内の参照成果物はいずれも実在する。
- 文書差分のみでコード / playbookの変更はない。重複実装、Ansibleの変数注入・秘密露出、多層エスケープ、rc判定、check-mode分岐、例外吸収は対象差分に該当しない。

## Unverified / Residual Risk

- AC1-b、AC3、AC5の実受信、quory側のidentity登録・watcher lifecycle、R15、再起動後の各復帰は未確認である。
- watcherの具体的な組込み方法はrequirement Q6のとおりquory側の未決事項であり、runbook・status・implementで残作業として明示されている。
- `_008_test_result_stage3.md`は依頼どおり対象外とし、内容を読まず変更していない。本再レビューでReviewerが変更したrepoファイルは本`_007_review_stage3.md`だけである。

## Verdict

**Approve**

round 3の2 findingは解消した。未検証項目は明示された次工程の残作業であり、段3 ansy側の文書差分を妨げるblocking findingではない。
