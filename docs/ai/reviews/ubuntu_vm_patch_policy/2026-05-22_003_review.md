# レビュー結果: ubuntu_vm_patch_policy.md v1.1 更新

- 日付: 2026-05-22
- レビュー対象: git diff
- 参照:
  - `docs/ai/prompts/core.md`
  - `docs/ai/reviews/ubuntu_vm_patch_policy/2026-05-22_001_requirement.md`
  - `docs/ai/reviews/ubuntu_vm_patch_policy/2026-05-22_002_implement.md`
  - `docs/ai/prompts/ubuntu_vm_patch_policy.md`

## Findings

新たな指摘事項はありません。

## 結論

`docs/ai/prompts/ubuntu_vm_patch_policy.md` の v1.1 更新は、要求仕様の範囲を満たしています。

`quory` を Ubuntu ノードとして追加し、自身が Ansible 実行基盤であるため `ubuntu_nightly.yml` の管理対象にしないこと、`unattended-upgrades` の自動 reboot を 03:00 固定にすることが明記されています。

systemd timer 名と実行時刻も、現在の `roles/systemd_timers/defaults/main.yml` と一致しています。

## 確認したこと

### 要求仕様との対応

- タイトル・版表記は `Ubuntu VM Patch Policy v1.0` から `Ubuntu ノード Patch Policy v1.1` に更新されている。
- 対象範囲は Ubuntu VM から Ubuntu ノード全般（VM・物理ノード）へ拡張されている。
- ノード一覧に `quory` が追加されている。
  - 種別: 物理ノード
  - reboot 方針: 自動 reboot（時刻固定）
  - reboot 時刻: 03:00
  - healthcheck: なし
  - 理由: 自身が Ansible 実行基盤のため `ubuntu_nightly.yml` による管理不可
- `authy` の reboot 時刻は 03:30、healthcheck 時刻は 05:30 に更新されている。
- systemd timer 実行ノードは quory と明記されている。
- 深夜リブートスケジュール全体が追加されている。
- 2026-05-18 時点の設定確認として、authy と quory の unattended-upgrades 設定が記載されている。
- Ansible playbook / role / timer unit の実体変更は行われておらず、要求仕様の「ドキュメント変更のみ」に収まっている。

### 既存 role との整合

`roles/systemd_timers/defaults/main.yml` では以下の定義になっている。

- `name: authy-healthcheck`
  - 生成 timer: `ansible-authy-healthcheck.timer`
  - schedule: `*-*-* 05:30:00`
  - playbook: `playbooks/radius_healthcheck.yml`
- `name: authy-reboot-if-required`
  - 生成 timer: `ansible-authy-reboot-if-required.timer`
  - schedule: `*-*-* 03:30:00`
  - playbook: `playbooks/ubuntu_nightly.yml`

`roles/systemd_timers/tasks/main.yml` では `ansible-{{ timer.name }}.timer` として unit 名を生成しているため、ポリシー本文の timer 名は実装と一致している。

### 注意点

現在の working tree には、別トラックの `scripts/codex-classify.sh` 差分も残っています。

これは `proxmox_patch_dryrun` 側でレビュー済みの変更であり、本レビューのブロッカーではありません。commit 時は、既存方針通り Ubuntu policy 側と Proxmox dry-run 側を別 commit に分けてください。

## テスト・検証

実施:

- `git diff -- docs/ai/prompts/ubuntu_vm_patch_policy.md`
- `git diff --stat`
- `rg` による timer 名・playbook 名の参照確認
- `roles/systemd_timers/defaults/main.yml` の確認
- `roles/systemd_timers/tasks/main.yml` の確認
- `git status --short`

未実施:

- Ansible syntax-check
  - 理由: 今回はドキュメント変更のみで、playbook / role / inventory の変更はないため。

## 最終判断

このまま進めてよいです。

Next step files:
- `docs/ai/reviews/ubuntu_vm_patch_policy/2026-05-22_001_requirement.md`
- `docs/ai/reviews/ubuntu_vm_patch_policy/2026-05-22_002_implement.md`
- `docs/ai/reviews/ubuntu_vm_patch_policy/2026-05-22_003_review.md`
