# findings の処置(案件クローズ)

2026-08-05 / Coordinator。Reviewer 2本(`003_review_structure.md` / `004_review_distillation.md`)の findings を統合して処置した。**却下した指摘は無い。** 変更後の文面は commit の diff が正本であり、ここへ写さない。

| 出所 | 重大度 | 件数 | 処置 |
|---|---|---|---|
| R2(蒸留) | **Critical** | 1 | 是正 |
| R2(蒸留) | Major | 1 | 是正 |
| R1(構造) | Minor | 1 | **今回是正**(Reviewerは「次の機会」を推奨したが前倒しした) |
| R1(構造) | — | 0 | Approve。blocking無し |

## Critical — I1 が技術的に誤っていた

`docs/ai/core.md` decoy節へ追加した I1 は「`risk-accepted` は `--check` を無効化する分類であるため、decoyと `--check` を重ねても本適用が走る」と書いていた。**これは現在の実態と異なる。**

Coordinatorが現物で検算した結果、指摘は正しい。

- `docs/ai/policies/ansible_test_safety_policy.md` **TS-030** は「`risk-accepted` は変更を行う各playの `pre_tasks` に、`ansible_check_mode` が真なら停止するassertを置く」と要求している。
- 現存する `risk-accepted` は **3本**(`cloudkey_cert_deploy` / `proxmox_backup_restore_verify` / `unifi_backup_fetch`)で、**3本とも停止assertを持つ**(`that: not (ansible_check_mode | bool)`)。`proxmox_backup_restore_verify` は変更を行う2つのplay双方に持つ。
- `scripts/check-tester-gate.sh` L41-45 がassertの存在を機械的に要求しており、52 playbookでOK。
- I1 の原資である `2026-07-31_subagent-unintended-deploy-risk-accepted-check.md` の対象playbook(`incident_inspect_setup.yml`)は、既に `check-mode-native` へ再分類済みである。

**I1 は「再発防止は未実施」と書かれた時点の状態を、修正後も変わらない性質として書いてしまっていた。** 同日・同案件(`check_mode_semantics`)で構造修正が入っていたことを、分析が拾えていない。

**是正**: 分類に依存しない、いま真である部分だけへ書き換えた — decoyが偽装するのは接続先だけで、実行ホスト自身への変更(ユーザー作成・`sudoers.d`・ACL付与)は起きること、`--check` の意味は**対象playbookの分類が決めるものでdecoyの側が決めるものではない**こと。TS-030 の具体(lintが見るのはassertの存在だけで、play単位の充足はレビューが見る)は Policy と lint 自身が既に持つため `core.md` へ複製しなかった。

**Coordinator側の誤りも記録する。** 検算の初回、`risk-accepted` を **4本**と数えた(`grafana_provisioning.yml` を含めた)。実際には同ファイル L11 の**散文中で他playbookの流儀に言及していた**だけで、実効マーカーは `check-mode-native` である。行頭マーカー(`^# tester-gate:`)で数えるべきところを素の文字列で数えた — `skills/document-norm-review/SKILL.md`「構造化要素はマーカー形式で数える」が名指ししている誤りそのものである。

## Major — `rollback-cli-argument-convention` が孤児化していた

分析は不採用の理由を「実装規約であり `skills/ansible-implementation-style/SKILL.md` の領分」としたが、**同SKILLに `rollback` の記述は1件も無かった**(Coordinatorが確認、grep 0件)。①でsubagentがKnowledgeを読まなくなるため、この規約へ到達する経路が消える。`playbooks/alloy_setup.yml` と `playbooks/prometheus_update_check.yml` が現に使っている。

**是正**: 同SKILLへ節を新設した(CLI表面の統一、`<role>_rollback` へのmap、意味論、`-e` のコンマ区切り不可)。**不採用の理由づけが誤っていた**のであって、不採用という結論だけが誤っていたのではない — 「既に正本がある」と書くときは、その正本を開いて実在を確かめる。分析 §2 は `code-review` / `ansible-security-review` の2 SKILLについてこの確認を行い誤りを1件見つけていたが、**他の項目へ同じ確認を及ぼしていなかった。**

## Minor(R1)— 前倒しで是正

動的includeのインライン化で、原資の適用4項目目「防御を意図的に置かない箇所には、置かない理由をコメントに残す」が落ちていた。Reviewerは非ブロッキングとして「次にこのSKILLを触る機会に」と推奨したが、**このcommitで既に触っているファイル**であり、後から足すとその部分だけ検証をやり直すことになるため今回入れた。

一般形として書いた。根拠は2件ある — 原資の当該項と、`2026-07-27_incident-capture-spool-root-ownership.md` の「直さない理由をコード側にも明記し、将来の読み手が善意で直そうとしないようにした」。同SKILL L84 に個別ケース(`strftime` の `utc=True` 要否)の同型指示が既にあり、一般形が無い状態だった。

## レビュー範囲外で追加した2件(Yoshinobu指示、2026-08-05)

**「sandbox VM と ansy の Semaphore がテスト環境として使えることは、どこかに記述されているか」**という問いから、規範層に無いことが判明したため本案件へ含めた。

- **`docs/ai/context/system/autonomous-recovery.md`** へ「検証用target(`sandbox`)」節を新設。実体は `playbooks/recovery_probe_sandbox_setup.yml` のヘッダコメントと `docs/ai/status.md` のWatch 1行にしか無く、Context にも Policy にも無かった。**常設しない理由(週次パッチの mute 対象3件に `sandbox` が含まれず、常駐するとパッチ中のノードでVMを起動しにいく)を落とさずに移した。**
- **`docs/ai/roles/tester.md`** へ「使ってよい検証環境」の表を新設。**到達可否の非対称が要点である** — decoyと ansy の Semaphore は直接使えるが、`sandbox` は窓の開閉も failover 段のテンプレート起動も quory 上の操作であり、**AIからは起動できない**。これを書かないとTesterが到達を試みる。

`docs/ai/policies/proxmox_operations_policy.md` L75(ansyに閉じた実行はテスト目的で可)は**動かしていない**。あれは「本番Proxmoxの実行端末はquoryに限る」というPolicyの例外であり、検証環境の一般的な許可ではない(2026-08-04に「C4の但し書きをC5〜C7へ展開しない」と決めた判断と同じ理由)。Tester側には独立に書いた。

## 検証

| 手段 | 結果 |
|---|---|
| `scripts/check-doc-consistency.py` | check1 / check2 / check3 とも **OK**(104 / 8 / 91) |
| `scripts/check-tester-gate.sh` | **OK**(52 playbook) |
| `git grep` による旧節名・旧表現の掃引 | 規範層に残存ゼロ(実装記録 `002` の掃引表を再実行) |

`docs/ai/core.md` は **144行 → 136行**。

## Auditor

**起動していない。** 記録は分析・実装記録・レビュー2本・本ファイル、および commit メッセージで再構成でき、対象が規範文書の文面変更に閉じている。工程を積み増さない判断をした(`docs/ai/roles/coordinator.md`「委任するときの独立性」に基づくCoordinatorの裁量)。**R2 が Critical を1件検出しており、独立レビューの層は機能している。**

## この案件が残す申し送り

**規範文書の改訂がsubagentの挙動を実際に変えるかは、次に起動したときにしか確かめられない。** `docs/ai/core.md`「subagentが共通して守ること」末尾が自ら述べているとおり、塞げるのは書き落としまでである。**Watch行は立てない** — 検証手段を「次のReviewerがR1/R2の観点を使うか」と書いても、使わなかったことと観点が不要だったことを区別できず、`docs/ai/status.md` の規律2(検証手段のない項目は載せない)に反する。
