# review: 一次調査結果のSlack通知と、quory→ansy同期の即時実行

日付: 2026-08-01
Role: Reviewer(独立、Implementer別体)
契約: `docs/ai/reviews/incident_investigation_notify/2026-08-01_001_requirement.md`
実装記録: `docs/ai/reviews/incident_investigation_notify/2026-08-01_002_implement.md`

対象: `roles/incident_investigate/`、`roles/incident_sync/`、`playbooks/incident_investigate_notify.yml`、`playbooks/incident_sync_trigger_setup.yml`、`playbooks/README.md`(いずれも未commit差分)。実ホストへは一切接続していない(quory/ansyともread-only含め接続なし)。

## Code Review: incident_investigate_notify / incident_sync_trigger 一式

### Summary

N1〜N11・AC1〜AC3/AC7/AC8/AC10は差分内の証跡(コード・自己検証の再現・grep・ansible-lint・syntax-check)から確認できた。AC4〜AC6・AC9は実ホスト検証が必要で本レビューの対象外(Tester領分、実装記録も同様に認めている)。Critical Issuesは無い。Major 1件 — `incident_investigate_notify.yml`のtester-gate分類`role-guarded`がPolicyの定義(`common_slack/tasks/notify.yml`のガードで止まる)と一致しておらず、このリポジトリで唯一のもう1本の`role-guarded`(`recovery_probe_notify.yml`)との比較でも明確に逸脱している。common_slackを経由しない設計判断そのもの(N2充足のため)は現物(モジュールソース)で裏付けが取れ妥当と判断するが、その帰結としてこの通知だけがテスト実行時に無条件で実送信される経路になっている。

### Critical Issues

なし。

### Major Issues

| # | File | Line | Issue | Severity |
|---|---|---|---|---|
| M1 | `playbooks/incident_investigate_notify.yml` | 2, 88-92 | `tester-gate: role-guarded`が実態と一致しない。Policy(`docs/ai/policies/ansible_test_safety_policy.md` 28行目)の定義は「副作用がSlack通知のみで、`roles/common_slack/tasks/notify.yml`の抑止guardで止まる」ことを`role-guarded`の要件とするが、本playbookはヘッダ自身が「追加の抑止ガードは持たない」と明記するとおり、`notify.yml`をincludeしていない(独自に`community.general.slack`を直接呼ぶ)。TS-022により`role-guarded`は`--check`なしの通常実行が許される分類であり、Testerが検証のためこのplaybookを実行するたびに、抑制手段(`tester_mode`/`skip_notifications`)が一切無いまま`#alerts`へ実際にSlackメッセージが送信される。このリポジトリで唯一比較対象となる既存の`role-guarded`playbook `playbooks/recovery_probe_notify.yml`は実際に`common_slack/tasks/notify.yml`をincludeしており(2行目のコメントどおり)、本playbookはそれと異なる構造でありながら同じ分類名を名乗っている — `docs/ai/policies/ansible_test_safety_policy.md` TS-026が警告する「marker drift」の実例に一致する。<br>**代替**: 送信task自体は`community.general.slack`のネイティブ`check_mode`実装により`--check`下で実送信しない(`/usr/lib/python3/dist-packages/ansible_collections/community/general/plugins/modules/slack.py:520-521`で確認済み、実装記録の自己検証でも`--check`実行で本文検証している)。したがって`check-mode-native`へ再分類し、Testerには`--check`での検証を義務付ける方が構造と一致する。 | Major |

### Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| S1 | `roles/incident_investigate/tasks/sync_trigger.yml` | 71 | `lookup('pipe', 'ssh-keyscan -H ' + incident_investigate_sync_trigger_host)`が`become: true`(root)配下で変数をquoteフィルタなしに文字列結合している。`skills/ansible-security-review/SKILL.md`が挙げる「lookup()プラグイン経由での信頼境界超え」に該当する形。現状`incident_investigate_sync_trigger_host`はdefaultsの固定値であり実害は無いが、`roles/incident_sync/tasks/finalize.yml:174`は同種の`lookup('pipe', ...)`組み立てに`| quote`を使っており、本差分の実装はより安全な既存パターンを踏襲していない(ただし`roles/recovery_exec/tasks/target_setup.yml:271,284`という既存の踏襲元自体も同じ形でquoteなし — 本差分固有の新規劣化ではなく、既存の穴をそのまま継承したもの)。 | security-hardening |
| S2 | `playbooks/incident_investigate_notify.yml` / `roles/incident_investigate/files/incident-investigate.py` | notify.yml:78-82 / incident-investigate.py:769-792 | Slack本文の「Codex呼び出しエラー:」見出しが、`process_bundle`の「Semaphoreの終了確定待ちで諦めた」経路(`give_up_reason`、Codexを一度も呼んでいない)でもそのまま使われる。読み手が「Codexが落ちた」と誤読しうる。ヘッダをより中立な文言(例: 「調査未完了の理由:」)にするか、原因種別を区別するフィールドを足すことを検討されたい。機能的な破綻ではなくラベリングの精度の問題。 | clarity |
| S3 | `playbooks/incident_investigate_notify.yml` | 56-63 | `roles/common_slack/tasks/notify.yml`のwebhookロード・token抽出ロジック(`replace('https://hooks.slack.com/services/', '')`)をこのplaybookが独立に複製している。§3.1の判断(N2充足のためcommon_slack変更なしでは再利用不可)はモジュールソースで裏付けが取れ妥当と判断するが、複製である以上、将来`common_slack`側でwebhookロード方式が変わった場合にこちらへ追随されない構造的リスクが残る(このファイル自身をレビュー・変更対象に含めない限り検出できない)。対応必須の欠陥ではないが、`common_slack`変更時のレビューチェックリストに「`incident_investigate_notify.yml`も追随したか」を一言残すことを推奨する。 | duplication |

### What Looks Good

- **N1〜N9・N11**: `roles/incident_investigate/files/incident-investigate.py`の`post_artifact_actions`が、両方の`write_artifact()`呼び出し直後(`process_bundle`の give-up 経路・通常完了経路の両方)から呼ばれ、`trigger_ansy_sync()`→`send_investigation_notification()`の順(N7)で、各々独立した`try/except Exception`で包まれている(N4/N6)ことをコードで確認した。フィクスチャ(`/bin/false`をssh/ansible-playbook binへ充てる形)で`post_artifact_actions`を実行し、両方が内部失敗しても例外が外へ伝播しないことを直接再現・確認した。
- **N8**: `roles/incident_sync/files/incident-sync-trigger.sh`が`$SSH_ORIGINAL_COMMAND`を一切参照しないこと、`authorized_keys-incident-sync-trigger.j2`が`command=`で強制していること、`sudoers-incident-sync-trigger.j2`がワイルドカードなしの完全一致1行(`/bin/systemctl start --no-block ansible-incident-sync.service`)であり、スクリプトが呼ぶコマンド文字列と一致することを確認した。
- **N9**: `roles/recovery_exec/tasks/main.yml`・`roles/incident_inspect/tasks/main.yml`のACL付与が両方とも`/home/yoshi`への`x`(traverse)のみであり、`.ssh`ディレクトリ自体は`sync_trigger.yml`で`mode: 0700`(yoshi専有)にしていることを確認した。構造的にrecovery-execは`.ssh`配下へ到達できない設計になっている(実測はTester領分、実装記録も明記済みで異論なし)。
- **N10・AC10**: `playbooks/*.yml`(48件)と`playbooks/README.md`の見出しを機械的に突き合わせ、未記載0件を確認した。新規4行のtester-gate値(README記載)と各playbookヘッダのマーカーも一致している。
- **変数名の一致**: `incident-investigate.py`内の全`cfg[...]`参照キー(21個)と`incident-investigate.json.j2`がレンダリングするJSONキー集合が完全一致することを機械的に確認した(過不足なし)。
- **AC1・AC2**: `playbooks/incident_investigate_notify.yml`の本文組み立てで、ジョブ番号・テンプレート・playbook・所見・確信度・既知条件・レポートパスがすべて含まれ、未定義時は「(unknown)」「(取得できなかった)」へ明示的にフォールバックすることを確認した。`community.general.slack`は`color`省略時(既定`normal`)かつ`text`指定時に`payload = dict(text=...)`となり、`attachments`/`color`を含まない(モジュールソース`build_payload_for_slack`関数、327-328行で確認)ため、N2(色分けなし)は構造的に満たされている。
- **AC8**: (**2026-08-01追記**: この固定文言はレビュー後にCoordinator指摘で削除された。AC8が求めるのは「断定しない書き方」であって注意書きの掲載ではなく、削除後も「レポート: `<path>`」は現物の存在を主張しないため充足は変わらない。以下はレビュー時点の記述である。)通知本文に「この通知が届いた時点でansy側に現物が揃っているとは限りません」の固定文言があり、`incident-sync.service.j2`側の`-E 0`ロジックは本差分で変更されていない(既存設計のまま)ことを確認した。
- **IC-013**: ansy側`incident-sync-trigger`アカウントが持つ唯一の能力は既存unit 1本の起動であり、ファイル書込・他unit起動の経路が無いことをtemplate/sudoers/scriptの全体から確認した。IC-013の「quoryにansyへの書込権を与えない」は保たれている。
- **N11**: `incident_investigate_ansy_report_path_prefix`(`reports/incidents/quory/_investigations`)が、`roles/incident_sync/defaults/main.yml`の`incident_sync_mirror_dir`(`{{ reports_base_dir }}/incidents/quory`)と`roles/incident_sync/tasks/transfer.yml`が転送する`incident_sync_source_dir`(quoryの`reports/incidents/`全体、`_investigations`を除外しない)の組み合わせと整合することを確認した。
- **参照パスの実在確認**: requirement/implement記録が挙げる`docs/ai/adr/009-...`、`docs/ai/reviews/incident_investigate_trigger/2026-07-31_001_requirement.md`、`docs/ai/memory/incidents/2026-07-31_incident-investigate-callback-did-not-enqueue.md`、`docs/ai/reviews/incident_auto_investigation/2026-07-31_004_u1_implement.md`、`roles/incident_inspect/templates/AGENTS.md.j2`はすべて実在を確認した。
- **IPv4リテラル**: 変更・新規ファイル全件を機械grepし、新規のIPv4リテラルは無い(既存のコメント2件のみヒット、変更前から存在)。
- **秘密情報の取り扱い**: `include_vars`・token抽出・Slack送信の各taskに`no_log: true`が付与されている(`common_slack/tasks/notify.yml`と同型)。一時ファイル(`tempfile.mkstemp`)はfinally節で確実に削除され、webhookトークン自体は含まない(ジョブ情報・所見のみ)。gitleaksスキャン(実装記録)・目視ともtoken/秘密鍵の直書きは検出していない。
- **syntax-check / ansible-lint / tester-gate lint**: 本レビューで`--syntax-check`・`scripts/check-tester-gate.sh`(48 playbook OK)を再実行し、いずれもパスすることを確認した。

### 未確認事項(Testerへ)

- AC4・AC5・AC6・AC9は実ホスト検証が必須で、本レビューでは構造の妥当性確認に留まる(実装記録§5・§6と同じ限界)。
- M1の再分類(role-guarded → check-mode-native)を採るかは実装判断であり、Coordinatorの決定を要する。再分類する場合、Testerへの実行指示(現状「role-guardedは`--check`不要」)も合わせて更新が必要。
- S1〜S3はいずれも動作を止める指摘ではなく、次回this areaに触れる際の申し送り事項として記録する。

### Verdict

**Needs Discussion**(M1について)。N1〜N11・AC1〜AC3/AC7/AC8/AC10のコード上の充足は確認できており実装の主要な論理に欠陥は無いが、M1はTesterの実行安全性に関わる分類ミスであり、Testerへ引き渡す前にCoordinatorの判断(再分類 or 現状維持の明示的な承認)を要する。
