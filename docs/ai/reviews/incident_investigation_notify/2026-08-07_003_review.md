# review: 一次調査の通知の成否を成果物へ残す(案件A) / healthcheck src 明示パス化(案件B)

日付: 2026-08-07
Role: Reviewer
対象A: `git diff roles/incident_investigate/files/incident-investigate.py`(契約: `2026-08-07_001_requirement.md` R1〜R7 / AC1〜AC8。先行契約 `2026-08-01_001_requirement.md` N1〜N11 は不変であることを確認対象に含む)
対象B: `git diff roles/monitoring_healthcheck/tasks/check.yml`(契約: `docs/ai/memory/incidents/2026-08-07_ubuntu-nightly-healthcheck-src-unresolvable.md`「確認方法」節)

両案件とも、実装記録・検証記録・Incident記録に書かれた結論を出発点にせず、コード本体を読み、必要な範囲は自分でローカル実行して確かめた。実行内容は下記に明記する。

---

## 案件A: `roles/incident_investigate/files/incident-investigate.py`

### Summary

`post_artifact_actions()` へ通知試行結果の記録を追加した差分。`process_bundle()` / `main()` は無変更、既存の `write_artifact()`(tmp+`os.replace`)・`redact_ipv4()`・`now_jst_str()` を再利用しており、新しい書込経路や新しい機構を作っていない。R4(秘密情報が入らない)の核心的主張(「現行の `incident_investigate_notify.yml` の失敗では `stderr` は空になる」)を自分で decoy 実行し独立に再現・確認した。blocking findingは無い。

### 実施した確認(自分で行ったもの)

- `roles/incident_investigate/files/incident-investigate.py` の該当関数(`write_artifact`・`redact_ipv4`・`record_notification_result`・`post_artifact_actions`・`process_bundle`・`main`)を全文読み、diffのhunk境界どおり `process_bundle`/`main` が無変更であることを確認した。
- **R4の独立検証**: `/tmp/.../scratchpad/reviewer_decoy/` に `ansible_connection: local` のdecoy inventoryと、スラッシュを含まない偽token(`DECOYNOSLASHTOKEN`)を持つ decoy `vars/slack.yml` を作り、本番の `playbooks/incident_investigate_notify.yml` を変更せずそのまま `ansible-playbook` で実行した(cwd=リポジトリ直下)。`community.general.slack` のモジュール実装(`/usr/lib/python3/dist-packages/ansible_collections/community/general/plugins/modules/slack.py:415-421`)を先に読み、この形のtokenは `fetch_url` を呼ぶ前の `module.fail_json()` 分岐(domain未指定)で落ちる、すなわち**ネットワークへ一切出ない**ことを確認してから実行した。結果: rc=2、**stdout(1432バイト)に定型メッセージとcensored表示が出た一方、stderrは長さ0**。実装記録の主張(§3-1)と一致する結果を自分の実行で独立に得た。
- `subprocess.TimeoutExpired` の `str()` がコマンド配列(バイナリパス・playbook相対パス・`-e @<一時ファイルパス>`)のみを含みペイロード本体を含まないことを、ローカルの `python3` ワンショットで確認した(ペイロードはJSON一時ファイルへ書きargvには渡らない設計のため)。
- `roles/recovery_exec/files/incident-bundle-helper` の `show-investigation` 分岐(69-90行)を読み、`cat -- "$path"` のみで JSON parse や `schema_version` 検査を一切行わないことを確認した(R6の判断根拠)。
- `.gitignore:5` の `reports/incidents/` 除外と `git check-ignore -v` / `git ls-files reports/` を自分で実行し、`reports/incidents/` 配下がGit管理外であることを確認した。
- `git diff roles/dev_investigate/files/recovery-investigate-dispatch-quory.sh` 等、`git status` に含まれる本案件対象外の未commit変更を検出した(下記「未解決事項」参照)。

### Critical Issues

なし。

### Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| S1 | `roles/incident_investigate/files/incident-investigate.py` | 748-796(`record_notification_result`) | `write_artifact()` が同一task_idに対し1回の起動内で**2回**呼ばれる(1回目=`assemble_artifact`直後、2回目=本関数から)。`write_artifact()` はJSON/MDをそれぞれ独立に tmp+`os.replace` するため、2回目の呼び出し中に例外(例: ディスク満杯がJSON置換成功・MD書込失敗の間で起きる)が起きると、JSONに`notification`が反映済みでMDが旧内容のまま、という**JSON/MD間の一時的な不整合**を経由しうる。これは`write_artifact()`自体の既存の性質(今回変更していない)であり、AC3が検証した「書込先が全面読み取り専用」というケースはこの隙間を通らないため見えていない。壊れた/読めないJSONを経由するわけではなく(R2の主張どおりJSON単体は常に読める)、blockingではないが、次にこの関数へ触れる人のために記録として残す。 | Correctness(残存リスク・低) |
| S2 | `roles/incident_investigate/files/incident-investigate.py` | 786-796 | `notification.attempted` は常に `True` になる(`record_notification_result`を呼ぶ経路が必ず送信を試みた後のみであり、`attempted=False`を作る分岐が存在しない)。R1の文言上は「試行したか」を独立フィールドとして持たせる設計だが、現状の実装では`sent`/`error`から常に導出できる値と等価で、フィールドとしての情報量はゼロ。将来「送信を意図的にスキップする」経路(例: 抑止フラグ)が追加されたとき、このフィールドが初めて意味を持つ。今回の契約(R1)は満たしているため blocking ではない。 | Design(将来への注記) |

### What Looks Good

- R1〜R3・R7: `post_artifact_actions()`内の2つの独立した`try/except`が、送信の失敗・記録処理自身の失敗をそれぞれ握りつぶし`process_bundle()`/`main()`へ伝播しない構造を、コードを読んで確認した。`process_bundle()`の`return`文(913行)が参照する`artifact["status"]`は`assemble_artifact()`が返した元の辞書のフィールドで、`record_notification_result()`内の`updated = dict(artifact)`(コピー)による変更の影響を受けない — 戻り値・終了コードへの波及経路が構造的に無いことを確認した。
- AC6(give-up経路): `post_artifact_actions()`は`process_bundle()`内の2箇所の呼び出し元(886行・912行)いずれからも同一関数として呼ばれており、片方にだけ記録が付く余地がコード構造上ない。
- R4: 上記の独立検証により、現行実装のまま`stderr`を記録先にしても実際に秘密が漏れる経路が見当たらないという結論を自分の実行結果で裏付けた。`redact_ipv4()`の二次適用は既存機構(IC-040の精神)の再利用であり新設ではない。
- R6: `schema_version`を上げない判断の根拠(継続稼働の読み手が`show-investigation`しかなく、それが生byte転送のみであること)をコードで確認した。
- R2: `write_artifact()`の再利用、`artifact_already_exists()`がファイル存在のみを見る既存ロジックである点を確認し、IC-039の重複排除根拠に影響しないことを確認した。
- N1〜N11(先行契約): `playbooks/incident_investigate_notify.yml`・`build_notify_payload()`・`send_investigation_notification()`本体はdiffに一切含まれておらず、通知本文・チャンネル・プレーンテキスト・`--check`抑止のいずれも変更されていない。

### Verdict

**Approve**(blocking finding無し。S1・S2は残存リスクの記録であり、対応を今回の契約は求めていない)。

---

## 案件B: `roles/monitoring_healthcheck/tasks/check.yml`

### Summary

`copy`タスクの`src`を裸のファイル名から`{{ playbook_dir }}/../roles/monitoring_healthcheck/files/monitoring-healthcheck.sh`への明示パスへ変更。2つの呼び出し元(`playbooks/monitoring_healthcheck.yml`の`roles:`、`playbooks/ubuntu_nightly.yml:453`の`include_tasks`)がいずれも`playbooks/`直下に置かれているため`playbook_dir`が両者で同一値になり、同じ相対パスが両方で正しく解決することを確認した。test_resultの検証(Test A〜D、sha1/md5照合)は主張を実際に支えている。blocking findingは無い。

### 実施した確認(自分で行ったもの)

- `playbooks/ubuntu_nightly.yml`・`playbooks/monitoring_healthcheck.yml`がいずれも`playbooks/`直下にあることを`ls`で確認した(ネストしたサブディレクトリではない — `playbook_dir`が両呼び出し元で同一値になる前提)。
- `roles/monitoring_healthcheck/tasks/main.yml`を読み、role呼び出し経路が`include_tasks: check.yml`(裸・role_path基準)で`check.yml`自体へ到達し、その内部の`playbook_dir`はrole階層に関係なく最上位playbookのディレクトリを指すことを確認した。
- リポジトリ全体で`include_tasks`によるrole task へのパスinclude箇所を`grep`で横断し(`playbooks/*.yml`)、`src:`を持つファイルが`roles/recovery_push/tasks/drill_setup.yml`と本diffの`check.yml`の2本のみであること、前者は既に同じ明示パス形式であることを確認した(Incident記録の横断確認の主張と一致)。
- `roles/monitoring_healthcheck/tasks/check.yml`全文を読み、`Copy`タスク以外に裸の相対パス参照が無いことを確認した。
- test_resultが記載するTest A〜Dの手順・コマンド・checksum突合(sha1sum `4d7eac9a91d1237169e7d2a5fb58b067be6c4cc2` / md5sum `28955f25ec1ee2519c7196612182c7ab`)を読み、Test C/Dの`--check`実行が「`copy`モジュールが`--check`下でもchecksum照合により`src`解決失敗を検出できる」という前提(`ansible.builtin.copy`は`check_mode: support: full`)に立っており、実際に両呼び出し元でその照合結果を得ている記述であることを確認した — skipを成立と誤読していない(`Copy`タスク自体は両テストとも`skipped`ではなく`ok`/`changed`)。

### Critical Issues

なし。

### Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| S3 | `roles/monitoring_healthcheck/tasks/check.yml` | 19 | 修正は「2つの呼び出し元が両方とも`playbooks/`直下にある」という現在の配置に暗黙に依存する(`playbook_dir/../roles/...`という相対段数が両呼び出し元で一致することが前提)。将来どちらかのplaybookが別の深さのディレクトリへ移動すると、この`src`は片方の呼び出し元でだけ再び解決に失敗しうる。ただしこれは同種の`{{ playbook_dir }}/../roles/...`形式を使う他8ファイル(`common_slack/notify.yml`等)・`recovery_push/drill_setup.yml`と共通の設計上の制約であり、本diffが新たに持ち込んだ脆弱性ではない。次にplaybookの配置を変える人向けの記録として残す。 | Design(既存パターン踏襲・低) |

### What Looks Good

- 修正の技術的な仕組み(`playbook_dir`がrole context の有無に関わらず最上位playbookのディレクトリを指す、path includeにはrole検索パスが無い)をコードとAnsibleの一般的挙動から確認し、Incident記録の原因説明と一致することを確かめた。
- 横断確認(パス include される役割 task ファイル中、裸の`src`を持つものは今回の対象のみだった)を自分でも再現し、Incident記録の「同型は他に無い」という主張を裏付けた。
- test_resultの4パターン(role×sandbox本実行、path-include×sandbox本実行、role×monnie `--check`、path-include×monnie `--check`)はいずれも`Copy`タスク自体の解決可否を直接観測しており、`ubuntu_nightly.yml`本体の`reboot`を避けつつ両呼び出し元を同じ形で再現する設計として妥当である。使い捨てplaybookの削除・非commitも`git status`で確認した(現在の作業ツリーに残っていない)。
- test_resultが「monnieの状態は不変」をmtime実測で示し、「sandboxでの検証は承認境界内」であることも明記しており、Reviewerが確認すべき安全境界の主張として十分。

### Verdict

**Approve**(blocking finding無し。S3は将来の設計制約の記録)。

---

## 未解決事項・観測事項(両案件共通)

- **本案件のscope外の未commit変更が作業ツリーに存在する**: `git status`で`roles/dev_investigate/files/recovery-investigate-dispatch-quory.sh`・`docs/ai/status.md`・`docs/ai/reviews/dev_prod_boundary/2026-08-03_008_phase3_check_catalog.md`が変更済み(未commit)として検出された。これらは`docs/ai/reviews/dev_prod_boundary/2026-08-07_00{1,2,3}_*.md`という別案件の記録と対応しており、本レビューの対象2案件とは無関係と判断した。**読んだが変更していない**(diffの内容はD8/journal-unitの行数operand追加であり、本案件の通知記録・healthcheck srcとは独立)。Coordinatorへの報告のみとし、自分で統合・削除等は行っていない。
- 案件Aの配備後の実本番観測(次に一次調査が走ったとき`notification`が実際に記録されるか)は本レビューのscope外(Implementer記録も同様に明記)。
- 案件Bの`ubuntu_nightly.yml`本体を通したend-to-end実行(reboot含む)は次にmonnieが`reboot_required`を出す夜まで検証不可能であることを確認した(test_resultの記載どおり)。

## 確認範囲の明示

- 状態を変えない確認: `git diff` / `git status` / `git log`は使っていない(不要だった)、対象ファイルの`Read`、リポジトリ内`grep`、`ls`。
- 状態を変える確認(decoy・localhost限定、承認不要の3条件を満たす): `/tmp`配下decoy inventory(`ansible_connection: local`)による`playbooks/incident_investigate_notify.yml`のローカル実行1回(実Slackへのネットワーク到達なしをモジュールソース読解で事前確認した上で実行)。
- 実ホスト(monnie等)へは一切触れていない。
- 未確認: 案件Aの本番配備後の挙動、案件Bの`ubuntu_nightly.yml`本体のend-to-end実行。いずれも上記のとおり明示済み。
