# audit: 一次調査結果のSlack通知と、quory→ansy同期の即時実行

日付: 2026-08-01
Role: Auditor
対象: `docs/ai/reviews/incident_investigation_notify/2026-08-01_001_requirement.md`〜`_004_test_result.md`、および現物(未commit差分)。
入力: Coordinatorの説明は受け取っていない。上記案件フォルダと、そこから参照される成果物・Policy・ADR・現物のみを読んだ。

## 再構成できたか

**できた。** N1〜N11・AC1〜AC10の要求、Implementerの実装判断(§3.1〜§3.8)、独立レビューのM1/S1〜S3とその対応、Testerの検証範囲と限界は、001→004を順に読めば辻褄が追える。技術的な主張(cfgキーの一致、tester-gateの実効性、`community.general.slack`の`--check`挙動、N8/N9のACL・sudoers・forced command構造)は現物と照合し、確認できた範囲では記録の主張と一致していた。

ただし、**現物(作業ツリー)とこの記録群だけでは説明がつかない差分が1件**、および**記録どうしの矛盾が1件**見つかった。いずれも軽微ではない。

## 指摘

### 1. `playbooks/README.md` の未commit差分に、この案件の記録が説明しない12行が混在している(**クローズを妨げる**)

`playbooks/README.md`の現在の未commit差分(28行変更)のうち、この案件の記録(requirement N10、implement §1・§2)が主張しているのは「未記載2本(`incident_inspect_setup.yml`/`incident_investigate_setup.yml`)+ 本件新設2本(`incident_investigate_notify.yml`/`incident_sync_trigger_setup.yml`)」の**計4行の追加**だけである。

しかし実際の差分には、これとは別に**12行の既存エントリの`tester-gate`値変更**(`risk-accepted` → `check-mode-native`)が含まれている: `ca_trust_deploy.yml`、`cert_renew.yml`、`recovery_exec_setup.yml`、`recovery_io_setup.yml`、`recovery_probe_setup.yml`、`recovery_push_drill_setup.yml`、`recovery_push_setup.yml`、`incident_capture_setup.yml`、`incident_sync_timer.yml`、`codex_update_check.yml`、`time_sync_ntp_reference.yml`、`systemd_timers.yml`。

これらの12本のplaybook本体には差分が無い(`git diff`で確認、ヘッダは既に`check-mode-native`でHEAD時点から変化なし)。つまりREADME側だけが古い値を持っていた既存のドリフトであり、本件のN10(「未記載2本」に限定)とは別種の欠陥である。

`2026-08-01_004_test_result.md`は「新規発見」節で本件自身が持ち込んだ1行のドリフト(`incident_investigate_notify.yml`行の`role-guarded`表記)だけを報告し、**「既存12件は本差分が触れていない行であり対象外として扱った」と明記している**(24-25行目)。ファイルのタイムスタンプを確認すると、`playbooks/README.md`は`004_test_result.md`確定(08:27)の**後**、08:28に更新されている。Testerが「対象外」と切り分けた時点より後に、この12行を含む形でファイルが変わっている。

この12行の変更が何を根拠に、誰が、いつ行ったかは、この案件フォルダのどの記録にも書かれていない。この変更の意図らしきものは案件フォルダの外(`docs/ai/status.md`、本監査の入力から除外されている)にしか見当たらない。**この案件フォルダだけを読む読者は、`playbooks/README.md`の実際の差分の半分近くを説明できない。**

このままYoshinobuが本件をcommitすると、無関係な12行の是正が本件のコミットに紛れ込む。是正自体の当否は判定しないが(技術的正否はAuditorの範囲外)、**この案件の記録が「自分の差分」として説明する範囲と、実際にcommit対象になる差分の範囲が一致していない**ことは記録の整合性の問題であり、クローズ前に解消が要る。

対応案(いずれか): (a) 本件の差分から12行を分離し、別途正しい所属先で扱う。(b) 分離しないなら、この案件のtest_result.mdまたは新設の追記で「12行は別件の是正であり本件が引き取った」ことと根拠を明記する。

### 2. `R3差し戻し`が実装記録・playbookヘッダに登場するが、対応するレビュー指摘がどこにも存在しない(**クローズを妨げる**)

`2026-08-01_002_implement.md`は2箇所(43行目、69行目)で「**R3差し戻しで定型の注意書き3行を削除**」と述べ、`playbooks/incident_investigate_notify.yml`のヘッダコメント(47-50行目)も同じ削除を「2026-08-01独立レビューR3差し戻し」に帰属させている。この3行とは、AC8(月次実行中はexit 0で静かにスキップ)に関する「この通知が届いた時点で…」という定型の断定回避文言を指す。

しかし`2026-08-01_003_review.md`(この案件で唯一の独立レビュー記録)のMajor/Suggestionsは**M1・S1・S2・S3の4件のみ**であり、implement.md §0の対応表もM1(R1)・S1(R5)・S2(R2)・S3(R4)の4件だけを列挙している。「R3」に対応するレビュー指摘は表にも本文にも無い。**R3は定義されないまま2箇所で引用されている。**

さらに、review.md自身の「What Looks Good」(42行目、AC8の項)は**この今は削除された3行の定型文言が通知本文に含まれていることを、AC8充足の根拠として挙げている**——「通知本文に「この通知が届いた時点でansy側に現物が揃っているとは限りません」の固定文言があり…」。現在のplaybook本体(`playbooks/incident_investigate_notify.yml`の`_iv_body`、92-113行目)にはこの文言は存在しない。**review.mdが読んだ版と、その後implement.mdが「R3」を理由に書き換えた版は別物であり、review.mdの当該記述は現物と食い違ったまま残っている。**

タイムスタンプ上、review.md確定は08:09、playbook編集は08:15であり、review.mdが見た時点ではまだ3行が存在していた可能性と整合する。しかし**その後の削除の決定が誰によるものか(Coordinatorの口頭指示か、Implementerの独自判断か)、この記録のどこにも書かれていない。** 「R3」という番号だけが、実在しない4件目のレビュー指摘であるかのように記録へ残っている。

この記録だけでは、AC8を満たすとされる文言が「無い方が正しい」のか「review.mdが確認した『ある』状態の方が正しい」のか判定できない——review.mdは削除前の版を根拠にAC8をPASS扱いし、その根拠となった文言自体はもう存在しない。

対応案: 「R3」が指す実際の決定(誰が・いつ・どんな理由で3行削除を決めたか)を記録へ追記し、review.mdの該当箇所(AC8「What Looks Good」)を現状に合わせて更新するか、少なくとも「この記述は削除前の版に基づく」旨の注記を加える。

### 3. review.mdの自己矛盾: playbook総数が「51件」と「48 playbook」の2通りで書かれている(**クローズを妨げない、修正推奨**)

`2026-08-01_003_review.md`39行目は「`playbooks/*.yml`(51件)」、同ファイル48行目は「`scripts/check-tester-gate.sh`(48 playbook OK)」——**同一ファイル内で総数が食い違う。** `implement.md`・`test_result.md`はいずれも一貫して「48件/48 playbook」と書いており、実際に`playbooks/*.yml`をカウントしても48件、`scripts/check-tester-gate.sh`の出力も`OK (48 playbooks)`である。「51件」は review.md 単独の誤記と判断できる(他の記録・現物のいずれとも一致しないため)。

内容の正否(N10・AC10の充足)には影響しない——review.mdの結論自体は48件ベースの他の記述と整合しており、この1箇所だけが孤立した数値の誤り。

## 確認して問題が無かった主な点

- N1〜N9・N11の各要件と`incident-investigate.py`(`trigger_ansy_sync`/`build_notify_payload`/`send_investigation_notification`/`post_artifact_actions`/`process_bundle`)の対応は、記録の説明どおりに現物へ実装されていた。`codex_error`/`wait_error`の分離(S2/R2対応)も現物で確認できた。
- `playbooks/incident_investigate_notify.yml`のtester-gate再分類(M1/R1対応、`role-guarded`→`check-mode-native`)は現物のヘッダ・`when: not ansible_check_mode`・`tags: [destructive]`と記録が一致していた。
- `roles/incident_investigate/tasks/sync_trigger.yml`の`| quote`追加(S1/R5対応)は現物で確認できた。
- `cfg[...]`参照キー(21個)と`incident-investigate.json.j2`のレンダリング結果は完全一致(独自に再現して確認)。
- N8/N9の構造(forced command・sudoers完全一致1行・鍵のパスとowner/mode・recovery-execのACL付与範囲)は記録の主張どおりだった(実効性そのものはTester領分としてAC5/AC6/AC9が未検証のまま残っており、これは requirement §9 が最初から実ホスト検証必須と明示した範囲であって記録の欠落ではない)。
- 参照先(ADR-009、`incident_capture_policy.md` §3.5/§4、`ansible_test_safety_policy.md` TS-005/TS-014/TS-021〜TS-026/TS-030/TS-032、2026-07-31 Incident記録)はいずれも実在し、引用内容も一致していた。
- AC4〜AC6・AC8・AC9が実ホスト検証待ちであることは、requirement §9 が最初から明示しており、Implementer/Reviewer/Testerの3記録が一貫して同じ理由(本番影響を伴う配備前提)でTester/配備後へ委ねている。未解決の明示は成立している。

## 状態の正本との突合

`docs/ai/status.md`はこの監査の入力から除外されている(Coordinatorの指示により、本件と無関係な変更が混在するため)。突合は行っていない。

## 判定

**条件付き受入。**

クローズを妨げる指摘は2件——(1) `playbooks/README.md`の差分に本件の記録が説明しない12行が混在している、(2) 実在しない「R3差し戻し」への帰属と、review.mdに残る現物と食い違った記述。いずれも技術的な再実装を要さず、記録の追記・整理と、必要なら差分の切り分けで解消できる。反映すればクローズしてよく、再監査を前提にしない。

(3)のreview.mdの「51件/48件」不一致は修正を推奨するが、単独ではクローズを妨げない。

---

## Coordinatorの判断(2026-08-01)

**指摘3件をすべて反映し、この案件を閉じる。**

- **指摘1(README 12行の帰属不明)** — `2026-08-01_004_test_result.md` へ追記し、誰が・いつ・何を・なぜ・影響と、分離せず同じ差分に含めた判断を明示した。commitを分けたい場合の対象一覧にもなる形にした。
- **指摘2(存在しない「R3」)** — **原因はCoordinatorにある。** 差し戻し時に、独立レビュー由来のfindings(M1 / S1〜S3)と自分自身の指摘を同じR番号で通番したため、Implementerが「独立レビューR3」と記録した。`2026-08-01_002_implement.md`(2箇所)と `playbooks/incident_investigate_notify.yml`(1箇所)の帰属を訂正し、`2026-08-01_003_review.md` のAC8欄には「この固定文言はレビュー後に削除された」旨と、削除後も充足が変わらない理由を追記した。
- **指摘3(playbook総数 51/48 の不一致)** — 48へ訂正した。

**教訓として残す**: 複数の出どころ(独立レビュー / Coordinator自身 / Tester)の指摘を1通の差し戻しへまとめるときは、**各項目に出どころを明記する**。番号を通しで振ると、受け取った側は最も権威のある出どころへ帰属させる。今回は3箇所へ誤った帰属が伝播した。

**未検証のまま残るもの**: AC4 / AC5 / AC6 / AC8 / AC9 は配備後にしか確かめられない。観測手順は `2026-08-01_004_test_result.md` が持ち、期日のないものは `docs/ai/status.md` のWatchが持つ。
