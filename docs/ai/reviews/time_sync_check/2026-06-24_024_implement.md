# 追加実装内容: time_sync_check(本番incident解消の実機確認)

対象レビュー: `docs/ai/reviews/time_sync_check/2026-06-24_023_review.md`

## 概要

023_reviewの承認時、Codexは「quory/Semaphore側でのlocal connection実動作は未確認。
確認完了までは本番incidentの解消を確定扱いにしない」と指摘していた。commit
(`88b7085`)後、本番Semaphore(quory上でネイティブ実行)で実際に
`playbooks/time_sync_check.yml` が再実行され、解消を確認できた。

## 確認結果(本番Semaphore実行ログ、Yoshinobu提供)

- quoryの`chronyc tracking`収集が成功した(以前の`ann@quory.internal: Permission
  denied (publickey)`は再発していない)。
- 全7対象ホスト(pve1 / pve2 / ansy / monnie / authy / sophos-fw / cloudkey)が
  `collected: true, ok: true`。
- `Warnings: []`、`Errors: []`。
- time_sync_check自身のSlack通知タスク2箇所(critical/error通知、warning/error集約
  通知)はともに `skipping`(全ホスト正常のため、通知の必要がなかった)。
- `PLAY RECAP`でquoryを含む全ホストが`failed=0`
  (インシデント発生時は`quory: failed=1`だった)。

ログ末尾の「Sent successfully slack alert」はSemaphore自体のジョブ完了通知であり、
time_sync_check role自身の通知(今回は両方skipping)とは別物である。

`inventories/homelab/host_vars/quory.yml` の動的 `ansible_connection`
(`hostname -s` が `quory` の場合は `local`)が、Semaphoreのネイティブ実行環境で
意図通り機能していることが実機で確認できた。

## 結論

本番incident(quoryの自己SSH失敗による偽critical通知)は解消したと判断する。
023_reviewで要求された確認は完了した。

## Next step files

- (なし。本incidentの対応はここで完了)
