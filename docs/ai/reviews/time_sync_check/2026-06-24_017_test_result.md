# Test Result: time_sync_check T1 再テスト

## 結論

**PASS。**

`2026-06-24_015_test_result.md` で失敗した T1（`--check --diff`）を、
`2026-06-24_016_implement.md` の修正後に再実行した。rc=0 で完了し、全7対象の
収集・判定に成功した。Slack 通知タスクは実行されず、diff も出力されなかった。

前回 PASS 済みの T2（連続2回実行）と、環境に `ansible-lint` がなく SKIP となった
T3 は、依頼どおり再実行していない。

## T1. `--check --diff` 再テスト

実行コマンド:

```bash
ansible-playbook -i inventories/homelab/hosts.yml \
  playbooks/time_sync_check.yml --check --diff
```

結果: **PASS（rc=0）。**

確認内容:

- quory の `chronyc tracking` が check mode 下でも実行され、Phase 1 は正常判定となった。
- quory 異常時の critical Slack 通知と fail はともに `skipping` だった。
- Phase 2 以降が実行され、全7対象が `collected=True`、`ok=True` だった。
  - pve1
  - pve2
  - ansy
  - monnie
  - authy
  - sophos-fw
  - cloudkey
- `Warnings: []`、`Errors: []` だった。
- 集約 Slack 通知と collection error の fail はともに `skipping` だった。
- `--diff` による差分出力はなかった。
- unreachable / failed は全ホストで0だった。

recap:

- pve1 / pve2 / ansy / monnie / authy: `ok=3 changed=0 failed=0`
- sophos-fw: `ok=2 changed=1 failed=0`
- quory: `ok=9 changed=1 failed=0`

`changed=1` は localhost に delegate された Sophos / CloudKey の read-only expect 収集 task が
Ansible 上で changed と報告したものであり、対象状態の変更や diff はなかった。

## 015 の再テスト条件との突合

| 再テスト条件 | 結果 |
|---|---|
| check mode でも収集 command を実行する | PASS |
| false critical による Slack 通知を送らない | PASS |
| `--check --diff` が rc=0 で完了する | PASS |
| 全対象の収集・判定結果を確認できる | PASS |

## Next step files

- `docs/ai/reviews/time_sync_check/2026-06-24_014_test_plan.md`
- `docs/ai/reviews/time_sync_check/2026-06-24_015_test_result.md`
- `docs/ai/reviews/time_sync_check/2026-06-24_016_implement.md`
- `docs/ai/reviews/time_sync_check/2026-06-24_017_test_result.md`
