# Test Result: time_sync_check 本番実行

## 結論

**PASS。**

`time_sync_check.yml` を `--check` なしで実行し、rc=0 で正常終了した。
Sophos / CloudKey の実際の expect セッションはいずれも完了し、結果サマリは正常範囲内だった。
`Warnings: []` / `Errors: []` のため Slack 通知タスクは skip され、fail も発生しなかった。

## 実行コマンド

```bash
ansible-playbook -i inventories/homelab/hosts.yml playbooks/time_sync_check.yml
```

結果: **PASS（rc=0）。**

## 確認結果

### expect セッション

- sophos-fw: PASS
  - `Check current time (Sophos via Advanced Shell, direct date comparison)` が完了。
  - 判定 task も `ok: [sophos-fw]`。
  - detail: `offset=737ms (SSH往復 約1000ms、date直接比較。往復遅延が誤差として混入)`
  - `time_sync_check_sophos_threshold_ms` の正常範囲内。
- cloudkey: PASS
  - `Check NTP sync status (CloudKey via password SSH)` が完了。
  - 判定 task も `ok: [quory -> localhost]`。
  - detail: `Offset: +2.031ms`
  - 通常閾値の正常範囲内。

### 結果サマリ

Reference:

- quory: `System time     : 0.000067369 seconds fast of NTP time`

Results:

| host | collected | ok | detail |
|---|---:|---:|---|
| pve1 | true | true | `System time     : 0.000071019 seconds slow of NTP time` |
| pve2 | true | true | `System time     : 0.000004542 seconds slow of NTP time` |
| monnie | true | true | `System time     : 0.000203579 seconds fast of NTP time` |
| authy | true | true | `System time     : 0.000625774 seconds slow of NTP time` |
| sophos-fw | true | true | `offset=737ms (SSH往復 約1000ms、date直接比較。往復遅延が誤差として混入)` |
| cloudkey | true | true | `Offset: +2.031ms` |

補足:

- 実行元の ansy は動的除外により収集対象から外れ、recap では `ok=0 skipped=6`。
- pve1 / pve2 / monnie / authy は chrony 自己報告で正常。
- sophos-fw は直接比較方式、cloudkey は `timedatectl timesync-status` の数値 Offset 方式で正常。

Warnings / Errors:

```text
Warnings  : []
Errors    : []
```

### Slack 通知と fail

- `Send Slack notification (quory NTP status could not be collected)`: skipped
- `Fail because reference node status could not be collected`: skipped
- `Send Slack notification (quory NTP not synchronized)`: skipped
- `Fail because reference node time is not trustworthy`: skipped
- `Send Slack notification (NTP not synced and/or collection errors)`: skipped
- `Fail if collection errors occurred`: skipped

閾値超え・収集失敗がないため、期待どおり通知も fail も発生しなかった。

## Play recap

```text
ansy       : ok=0 changed=0 unreachable=0 failed=0 skipped=6
authy      : ok=3 changed=0 unreachable=0 failed=0 skipped=5
monnie     : ok=3 changed=0 unreachable=0 failed=0 skipped=5
pve1       : ok=3 changed=0 unreachable=0 failed=0 skipped=5
pve2       : ok=3 changed=0 unreachable=0 failed=0 skipped=5
quory      : ok=9 changed=1 unreachable=0 failed=0 skipped=11
sophos-fw  : ok=4 changed=1 unreachable=0 failed=0 skipped=2
```

`changed=1` は read-only の expect / delegate 収集 task が Ansible 上 changed と表示されたもの。
今回の playbook は対象ホストの設定変更を行っていない。

## Next step files

- `docs/ai/reviews/time_sync_check/2026-06-25_001_requirement.md`
- `docs/ai/reviews/time_sync_check/2026-06-25_002_implement.md`
- `docs/ai/reviews/time_sync_check/2026-06-25_003_review.md`
- `docs/ai/reviews/time_sync_check/2026-06-25_004_test_result.md`
