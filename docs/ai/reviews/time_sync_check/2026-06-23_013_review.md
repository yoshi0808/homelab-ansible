# 再レビュー結果: time_sync_check

日付: 2026-06-23

対象:

- `docs/ai/reviews/time_sync_check/2026-06-23_001_requirement.md`
- `docs/ai/reviews/time_sync_check/2026-06-23_011_review.md`
- `docs/ai/reviews/time_sync_check/2026-06-23_012_implement.md`
- 現在の git 差分（未追跡の新規ファイルを含む）

## 指摘事項

**新たな指摘事項なし。**

## 前回指摘の確認

### CloudKey の `inactive` を `active` と判定する

**解消済み。**

`System clock synchronized:` と `NTP service:` の値を正規表現の capture group で抽出し、
それぞれ `== 'yes'`、`== 'active'` の完全一致で判定している。`inactive` の部分文字列を
`active` と誤認する経路はなくなった。

012 implement には以下4パターンのローカル検証結果が記録されている。

- yes / active: collected=true, synced=true
- yes / inactive: collected=true, synced=false
- no / active: collected=true, synced=false
- 必須行欠落: collected=false

実機 read-only 実行でも CloudKey の detail が
`System clock synchronized: yes / NTP service: active` となり、全7対象が正常終了している。

## 全体確認

- Yoshinobu 承認済みの NTP 自己報告方式への設計転換は、006 implement 以降に経緯と根拠が
  記録されている。
- quory の chrony 判定を Phase 1 で単独実行し、異常時は他ホストへ接続せず critical 通知・
  fail する。
- quory / pve1 / pve2 / ansy / monnie / authy は chronyc の Leap status、System time の
  解析成否、500 ms閾値を共通ロジックで確認する。
- Sophos は ntpclient ログ取得 rc、最終行の成功状態、mtime の `0〜30時間` 鮮度を確認する。
- CloudKey は timedatectl rc、必須2行の解析成否、synchronized=yes、service=active を確認する。
- 接続・収集失敗は error 通知後に fail、同期異常は warning 通知、全対象正常時は通知しない。
- CloudKey パスワードと Sophos SSH 鍵を扱う task は `no_log: true` で保護されている。
- check 系コマンドは収集だけを行い、正常・異常判定、通知、fail 制御は Ansible tasks 側に
  ある。対象ホストへの変更操作はなく、read-only である。
- IP リテラル、秘密鍵本体、平文パスワード、runtime report の混入は確認されなかった。
- radius_healthcheck の判定されない chrony 収集削除と、Sophos 接続変数の group_vars への
  移設にも問題はない。

## Verification

- `ansible-playbook -i inventories/homelab/hosts.yml playbooks/time_sync_check.yml --syntax-check`: PASS
- 同条件の `--list-tasks`: PASS
- `git diff --check`: PASS
- 対象の未追跡 YAML / Markdown: whitespace diagnostic なし
- 012 implement に記録された4パターンの CloudKey ローカル検証結果を確認
- 012 implement に記録された全7対象の実機 read-only 正常実行を確認
- reviewer は今回、実ホストへの再実行を行っていない

## 判定

**承認。意図した変更ファイルは commit してよい。**

元 requirement の quory 基準生時刻比較から、承認済みの NTP 自己報告方式へ変更されているが、
目的に対する技術的妥当性と監査証跡があり、実機正常系・ローカル異常系も確認されている。

未追跡の `.codex/hooks.json` は time_sync_check の成果物ではなく環境固有の絶対パスを含むため、
これまでの方針どおり commit 対象から除外すること。

## Next step files

- docs/ai/reviews/time_sync_check/2026-06-23_001_requirement.md
- docs/ai/reviews/time_sync_check/2026-06-23_012_implement.md
- docs/ai/reviews/time_sync_check/2026-06-23_013_review.md
- docs/ai/reviews/time_sync_check/2026-06-23_014_test_plan.md
