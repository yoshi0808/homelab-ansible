# U3: 独立レビュー(AC1 / AC2 / AC4)

- 作成: 2026-07-28 Reviewer(U3担当、U1・U2とは別体)
- 対象: 作業ツリーの未追跡差分全体(U1: `skills/subagent-briefing/SKILL.md` 新設 + `.claude/skills/subagent-briefing` symlink、U2: `.claude/skills/document-norm-review` + `.claude/skills/incident-recording` symlink)
- 入力: `2026-07-28_001_requirement.md` §5(AC1/AC2/AC4)、`2026-07-28_002_plan.md` §2(契約表S1〜S9)、`progress.md`「ゲート決定(2026-07-28)」節(S10追加・S7訂正・S5訂正)、`2026-07-28_004_u1_implement.md`、`2026-07-28_005_u2_implement.md`
- 併用Skill: `skills/document-norm-review/SKILL.md`(正本パス指定どおり)、`skills/code-review/SKILL.md`(出力フォーマット)
- **計画§0(因果の判断)は読んでいない**(ゲート決定6のとおり、requirement §1の観測と契約表のみを出発点にした)
- 実施した操作: `git status`、`grep`/`sed`による現物照合、`readlink`/`diff`による symlink検証、`git add`→`scripts/git-pre-commit-check.sh`→`git reset`(AC4検証。commit/pushは行っていない)。Ansible実行なし(`--check`含め0回)。identity昇格なし。`skills/`・`.claude/skills/`・他の案件記録への変更は行っていない

## Summary

契約要素S1〜S10はすべて構造として存在し、名称・順序の違いは無視してよい水準で対応が取れている(AC1・S8のAC1対応表も充足)。AC4は`git add`→静的検査で`exit=0`を確認した。一方AC2(規範本文の複製なし)については、**Role別差分表のTech Lead行が`docs/ai/roles/coordinator.md`「Tech Leadへ渡すもの」の列挙(目的・ゴール・概要設計・受入条件・制約・安全境界、および詳細設計/機能分割/インターフェースの委譲)をほぼそのまま複製しており、かつ参照節にその引用元が明示されていない**。これは契約が想定した「参照するだけで規範を複製しない」という設計の想定外の穴であり、Criticalとして扱う。U1がflagした「型が塞がないもの」節のL47引用は、複製ではなく事実引用と判断した(下記参照)。

## Critical Issues

| # | File | Line | Issue | Severity |
|---|---|---|---|---|
| 1 | skills/subagent-briefing/SKILL.md | 39-41(「Role別の差分」表・Tech Lead行) | `docs/ai/roles/coordinator.md`「計画に全力を注ぎ…」節の「Tech Leadへ渡すもの」列挙(L30: 目的・ゴール・概要設計・受入条件・制約・安全境界、および「詳細設計・機能分割・インターフェースの確定はTech Leadの責務」)を、項目の並び・内容とも実質同一の形で複製している。SKILL.mdの「参照」節(L76以降)はcoordinator.mdの別の2節(「依頼文には〜」節、「実ホストへの非冪等操作の承認」節)のみを挙げており、複製元のL30を指していない。coordinator.md側でこの列挙が変わっても(例: 項目追加)SKILL.mdのこの行は追随せず、AC2が禁じる「正本の二重化・ドリフト」の典型形になる | Critical |

## Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| 1 | skills/subagent-briefing/SKILL.md | 58-60(「型が塞がないもの」節) | lessonの「それでも残る設計則」表の段数(2段)を事実として引用しており、AC2違反とは判断しない(下記「U1からの再確認依頼への回答」)。ただしこの引用は「lessonの表が将来変わっても気づかれない」というJ-1類似の陳腐化リスクを構造的に持つ。将来lessonの表構成が変わった場合に追随できるよう、次回のdocument-norm-review系振り返りで「事実引用の陳腐化検知」を欠陥クラス候補として検討する価値がある(finding化はしない・記録のみ) | duplication-risk |
| 2 | skills/subagent-briefing/SKILL.md | 62-70(「AC1対応表」) | 「渡すもの/渡さないもの」の章が②③両方を塞ぐ構成で、S8の表もそれをそのまま反映している。契約(S8)は満たしているが、①②③がすべて同じ2章にひもづく形なので、将来この2章のどちらかだけを削る改訂が起きた場合に3クラスのうち2つが同時に無防備化するリスクがある(finding化はしない・設計メモとして記録) | design-note |

## What Looks Good

- **S1〜S10すべてが構造として存在する**(frontmatter、実行identityと権限境界、渡すもの/渡さないもの、並行作業の境界、自己検証、報告形式、Role別差分表、参照節、型が塞がないもの、AC1対応表、差し戻し時の再起動、symlink)。要素の欠落は無い。名称・順序はImplementerの裁量どおりで契約違反ではない。
- **ゲート訂正3件はすべて反映されている**。
  - S10(差し戻し時の再起動)は無条件の必須節として存在し、条件分岐になっていない(ゲート決定2)。
  - S7は2段(能力の不在/判断を要する制御)+L47の位置づけで書かれており、3段のラダーとしては書かれていない(ゲート決定3。本文中「3段」の語も現物検索で0件)。
  - S5のRole別差分表は対象がTech Lead / Implementer / Reviewer / Tester / Auditorの5役で、「`docs/ai/roles/`の現存ファイルと一致させる」という削除対象の根拠づけは本文に現れない(ゲート決定4)。ただしTech Lead行の**内容そのもの**は上記Critical #1のとおり複製に当たる — 「括弧書きの根拠づけを削る」という訂正の文言自体は守られているが、複製の実体は残っている。
- **参照節の実在確認**: `docs/ai/roles/coordinator.md`「依頼文には『何を満たすか』を書き、『どう作るか』を書かない」節(L36)、「実ホストへの非冪等操作の承認」節(L114)、`docs/ai/core.md`「安全機構がブロックしたとき」節(L28)、`docs/ai/memory/lessons/permission-boundaries-must-be-designed-not-prompted.md` 教訓1(L23)・教訓2(L29)、`docs/ai/roles/auditor.md`「入力を絞る理由」節(L40)を現物のgrepで確認した。参照先はすべて実在し、記述と矛盾しない(Critical #1で指摘した箇所を除く)。
- **他の規範文書欠陥クラスの掃引**: 宙ぶらりん参照(完全パス・略記の双方でSKILL.md中の全参照パスを照合)、撤回済み規範・根拠の残存、判定ラダーの全域性・一意性(本Skillに判定ラダーは無い)、追跡表・索引のドリフト(該当なし)、適用範囲の広狭変化(該当なし)のいずれも検出しなかった。
- **AC4**: `git add`で対象一式(U1・U2の全ファイル+案件記録)をstagingし、`scripts/git-pre-commit-check.sh`を実行。gitleaksは"no leaks found"、`tester-gate-lint`は"OK (40 playbooks)"、`[pre-commit] OK`で`exit=0`を確認した。検証後`git reset`でstagingを解除し、作業ツリーをU1・U2完了直後の状態(全件未追跡)に戻した。commit/pushは行っていない。
- **symlinkの形式**: `.claude/skills/subagent-briefing`・`document-norm-review`・`incident-recording`はいずれも`../../skills/<name>`形式の相対symlinkで、既存10件(`ls -la .claude/skills/`)と矢印表記・階層とも同一。リンク先の`SKILL.md`実在も確認済み。
- **境界遵守**: `skills/`・`.claude/skills/`・他の案件記録に対する変更を行っていない。Ansible実行(`--check`含む)なし、identity昇格なし、`git commit`/`git push`なし。harnessの安全機構によるブロックは発生しなかった。

## U1からの再確認依頼への回答 — L47引用はAC2の「規範本文の複製」に当たるか

**当たらない、と判断する。**

- SKILL.md「型が塞がないもの」節が引用しているのは、lessonの「それでも残る設計則」表が**2段**であるという構造上の事実と、「文章による依頼」がその2段のどちらでもなく境界不在として扱われるというL47の位置づけである。lesson本文が持つ規範(教訓1「結果で書く」、教訓2「identityを明示する」、教訓3「構造的に到達できなくする→記録に残す→ASKは最後の手段」という順序原則)そのものは、SKILL.mdのどこにも再掲されていない。
- 文言も逐語一致ではなくパラフレーズである(lesson L47「境界は最初から構造として存在していなかった」→ SKILL.md「境界が最初から構造として存在しない状態」)。逐語コピーではなく、事実への言及に留まっている。
- この引用は契約(S7、ゲート決定3)が明示的に要求した内容であり、「3段のラダーとして書かない」という訂正を満たすには表の段数(2段)への言及が実質的に不可欠である。契約が要求する内容を満たす記述を、AC2違反として引っかけるのは契約同士を矛盾させる。
- ただし将来lessonの表構成が変わった場合にSKILL.md側が追随しない陳腐化リスクは残る(Suggestions #1)。これは「規範の複製」ではなく「事実引用の陳腐化」という別クラスであり、今回はfinding化していない。

対照的に、Critical #1(Tech Lead行)は表の構造的事実ではなく、**「何を渡すか」という規範そのものの列挙**を再掲しており、性質が異なる。U1の判断(「表の形状に関する事実の引用であって規範の複製ではない」)はL47の箇所に限れば妥当だが、S5のTech Lead行は同じ理由づけでは正当化できない別の箇所である。

## AC判定

| AC | 判定 | 根拠 |
|---|---|---|
| **AC1** | **充足** | S8のAC1対応表が①②③それぞれについて「実行identityと権限境界」章・「渡すもの/渡さないもの」章との対応を1行で説明しており、契約どおりの構造で塞がれている。要素の欠落なし |
| **AC2** | **未充足(Critical #1により)** | 参照先はおおむね実在し矛盾もないが、Role別差分表のTech Lead行が`docs/ai/roles/coordinator.md`「Tech Leadへ渡すもの」の列挙を引用元の明示なく複製しており、「規範本文の複製が無い」という条件を満たさない |
| **AC4** | **充足** | `git add`後の`scripts/git-pre-commit-check.sh`実行で`exit=0`(gitleaks: leaks無し、tester-gate-lint: OK)。IPv4リテラル等の混入なし |

## Verdict

**Request Changes**(Critical #1のみ)。修正方法はfindingとして返すに留め、本レビューでは実施しない。想定される対応は次のいずれか(採否はTech Lead/Coordinatorが判断): (a) SKILL.mdのTech Lead行を「詳細設計・機能分割・インターフェースの確定はTech Leadに委ねる」という結論部分だけに削り、具体的な渡す項目列挙は`docs/ai/roles/coordinator.md`「Tech Leadへ渡すもの」への参照に置き換える、(b) 参照節に該当箇所を追加して複製であることを明示し二重化のリスクを許容する、のいずれか。Coordinatorへ返す(`+R`工程ではないため、返却先は本案件のowner=Coordinator。requirement/計画の工程表上、U3はTech Leadを介さずCoordinatorのゲート下で動いているため)。

## 未解決事項

- Critical #1の修正方式(上記(a)/(b)のどちら、または別案)は未決定。Coordinatorが決めて再割当てすること。
- Suggestions #1・#2はfinding化していない設計メモであり、対応不要(記録のみ)。
- AC3・AC5は本レビューのスコープ外(AC3はCoordinator確認済み・W2で解消記録あり、AC5はAuditor)。

---

## 再照合(2026-07-28、Coordinatorからの差し戻し対応 — Critical #1のみ)

- 差し戻し元: 本ファイルのCritical #1
- 修正記録: `2026-07-28_004_u1_implement.md` §4(追記)
- 変わった契約要素: なし(S1〜S10は初回のまま)
- 実施した操作: `skills/subagent-briefing/SKILL.md`の現物を`Read`、修正前バージョン(初回レビュー時に読んだ内容)との`diff`、`grep`による参照先実在確認、`git add`→`scripts/git-pre-commit-check.sh`→`git reset`(AC4再実行)。Ansible実行・identity昇格・`skills/`/`.claude/skills/`/他案件記録への変更は無い

### 差分の確認(現物、説明でなく)

修正前バージョンをスクラッチファイルへ再構成し`diff`を取った。**差分は1行のみ**、「Role別の差分」表のTech Lead行のみが変わっている。他のS1〜S10・AC1対応表(L62–70)を含む全行は無変更であることを確認した。

```
41c41
< | Tech Lead | 目的・ゴール・概要設計・受入条件・制約・安全境界を渡す。詳細設計・機能分割・インターフェースの確定はTech Leadに委ね、渡さない |
---
> | Tech Lead | `docs/ai/roles/coordinator.md`「Tech Leadへ渡すもの」の一覧をそのまま渡す。詳細設計・機能分割・インターフェースの確定はTech Leadの責務であり渡さない(同箇所) |
```

### 再確認条件1 — Critical #1は解消したか

**解消したと判断する。** 新しい行は`docs/ai/roles/coordinator.md`「Tech Leadへ渡すもの」の列挙(目的・ゴール・概要設計・受入条件・制約・安全境界)を**書き写していない**。「の一覧をそのまま渡す」という参照だけを持ち、coordinator.md側で項目が増減しても、SKILL.md側の記述は書き換え不要のまま正しさを保つ。

残る「詳細設計・機能分割・インターフェースの確定はTech Leadの責務であり渡さない」という一文は、coordinator.mdの結論(「機能分割の切れ目とインターフェースの確定はTech Leadの責務である」)と重なるが、直後に**明示的に「(同箇所)」と出典を付している**。これはAuditor行(`docs/ai/roles/auditor.md`「入力を絞る理由」を明示して要点だけを述べる形)や、S3「渡すもの/渡さないもの」章の「免罪符にならない旨を明示(`docs/ai/roles/coordinator.md`「...」節)」と同じ、この文書全体で一貫した「要点を一文で述べ、出典を隣接して示す」書式であり、Critical #1が問題にした「列挙の複製かつ出典が無い」状態とは異なる。`grep -n "Tech Leadへ渡すもの" docs/ai/roles/coordinator.md`でL30に実在することを確認済み(引用ラベルの一致)。

### 再確認条件2 — 契約S1〜S10・AC1対応表(S8)への影響

**影響なし。** 上記diffのとおり変更は1行に閉じており、S1〜S4・S6・S7・S9・S10・AC1対応表(S8)は逐語一致で無変更。S5(Role別差分表)も表の構造(独立1節・1つの表)・対象5役(Tech Lead/Implementer/Reviewer/Tester/Auditor)に変更はなく、行の**中身**だけが修正された。契約は要素の存在と対応関係だけを定めており、この修正は契約の充足状態を変えない。

### 再確認条件3 — AC4の再実行

`git add`で対象一式(SKILL.md修正後の内容を含む全ファイル)を再stagingし、`scripts/git-pre-commit-check.sh`を再実行した。gitleaks「no leaks found」、`tester-gate-lint`「OK (40 playbooks)」、`[pre-commit] OK`で`exit=0`。検証後`git reset`でstagingを解除し、作業ツリーを未追跡状態に戻した。commit/pushは行っていない。ゲート決定8(差し戻し発生時はAC4を再実行する)を満たした。

### 再確認条件4 — AC1・AC2の最終判定

| AC | 判定 | 根拠 |
|---|---|---|
| **AC1** | **充足**(変更なし) | AC1対応表(S8)は無変更。①②③すべて構造として塞がれている |
| **AC2** | **充足** | Critical #1が解消し、他に規範本文の複製は検出していない(初回レビューのWhat Looks Goodの掃引結果は本再照合でも変わらない)。参照先はすべて実在し記述と矛盾しない |
| **AC4** | **充足**(再実行) | 再staging後の`scripts/git-pre-commit-check.sh`で`exit=0`。IPv4リテラル等の混入なし |

### 最終Verdict

**Approve。** Critical #1は解消済み、他のCriticalは無し。Suggestions #1・#2(事実引用の陳腐化リスク・AC1対応表の2章依存構造)は初回同様finding化しない設計メモのまま据え置く。Coordinatorへ返す。

### 未解決事項(更新)

- 新たな未解決事項は無い。AC3・AC5は引き続き本レビューのスコープ外(AC3はCoordinator確認済み・W2で解消記録あり、AC5はAuditor)。
