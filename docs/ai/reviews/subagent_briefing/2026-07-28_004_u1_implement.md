# subagent依頼文Skillの新設 — U1実装記録

- 作成: 2026-07-28 Implementer(A・U1担当)
- 入力: `2026-07-28_001_requirement.md`(正本)、`2026-07-28_002_plan.md`(契約表 §2、ただし訂正あり)、`2026-07-28_003_plan_review.md`、`progress.md`「ゲート決定(2026-07-28)」節(訂正の正本)
- 対象パス(このU1が変更した範囲): `skills/subagent-briefing/SKILL.md`(新設)、`.claude/skills/subagent-briefing`(新設symlink)
- 本記録以外に変更したファイル: なし。`git status` で `docs/ai/reviews/subagent_briefing/`(このファイル自身を除く)・`.claude/skills/document-norm-review`・`.claude/skills/incident-recording` が未追跡として現れるが、いずれもU2(並行Implementer)またはこのセッションの先行工程の成果物であり、**自分の成果物として扱っていない・削除も整形もしていない**
- 実ホストへの操作: なし。Ansibleは`--check`を含め一度も実行していない
- identity昇格: なし(`sudo`/`--become-user`を使用していない)。実行identityはansy上の作業ツリーのみ
- `git add` / `git commit` / `git push`: 行っていない

---

## 1. 契約S1〜S10の充足状況

`skills/subagent-briefing/SKILL.md`(以下SKILL.md)の行番号は本ファイル作成時点のもの。

| # | 構造要素 | 充足 | SKILL.mdの対応箇所 |
|---|---|---|---|
| S1 | frontmatter(`name`/`description`) | ✅ | L1–4。`---`区切り、`name:`/`description:`の2キーのみ。既存12 Skillの形式と同一(全12件のfrontmatterを事前に確認し例外なし2キーであることを確認済み)。descriptionに「依頼文を書く」「subagentを起動する」「差し戻す」「再起動する」の発火語を含めた |
| S2 | 実行identityと権限境界の章 | ✅ | 「実行identityと権限境界」節。書くもの=identity/書込先/触れてよいホストを結果の状態で書く、書かないもの=個々の手段の列挙。根拠に lesson 教訓1・2 を参照(複製せず) |
| S3 | 渡すもの/渡さないものの章 | ✅ | 「渡すもの / 渡さないもの」節。書くもの=受入条件・制約・観測された現象、書かないもの=実装方法・章立て以下の作り方・因果メカニズムの完成形。「未検証」断りが免罪符にならない旨を明示 |
| S4 | 骨格の残り3項目 | ✅ | 「並行作業の境界」「自己検証」「報告形式」の3節。各節に書くもの/書かないものを対で記載 |
| S5 | Role別差分の表 | ✅(訂正反映済み) | 「Role別の差分」節。対象はTech Lead/Implementer/Reviewer/Tester/Auditorの5役。**ゲート決定4のとおり「`docs/ai/roles/`の現存ファイルと一致させる」という根拠づけは書いていない** — 現存ファイルは6件(auditor/coordinator/implementer/reviewer/techlead/tester)だが、Coordinatorは依頼文の書き手であって宛先ではないため対象外、という理由だけをQ2決定(独立1節・章非依存)とあわせて表の直前文に書いた |
| S6 | 参照節 | ✅ | 「参照」節。最低限の3点(coordinator.md、core.md「安全機構がブロックしたとき」、lesson)に加え、auditor.md「入力を絞る理由」を追加(S5のAuditor行の根拠) |
| S7 | 型が塞がないものの1行 | ✅(訂正反映済み) | 「型が塞がないもの」節。**ゲート決定3のとおり3段のラダーとして書いていない** — lessonの「それでも残る設計則」表は2段(能力の不在/判断を要する制御)であり、「文章による依頼」は段ではなく境界の不在として扱われている(同ファイルL47)と明記。自己検証でL47の現物とcorrespondしていることを確認済み(下記2節) |
| S8 | AC1対応表 | ✅ | SKILL.md本文に配置(「AC1対応表」節)。①→実行identityと権限境界の章、②③→渡すもの/渡さないものの章、という対応を1行ずつで記載 |
| S9 | symlink | ✅ | `.claude/skills/subagent-briefing` → `../../skills/subagent-briefing`(相対)。`readlink`で確認、既存10件と同形式 |
| S10 | 差し戻し時の再起動の1節 | ✅ | 「差し戻し時の再起動」節。初回用の型は再掲せず、差し替え・追加する4項目(差し戻す理由/再確認条件/変わった契約要素だけ/前回成果物との関係)のみを列挙。本文は見出し除き9行 |

**行数**: SKILL.md本体は79行(`wc -l`実測)。契約の上限130行以内、既存Skillの実測レンジ23〜139行の範囲内。

---

## 2. 自己検証で確認したこと

### 2-1. 参照先の実在(grepで現物照合)

| 参照 | 確認方法 | 結果 |
|---|---|---|
| `docs/ai/roles/coordinator.md`「依頼文には『何を満たすか』を書き、『どう作るか』を書かない」節 | `grep -n` | L36に実在、逐語一致 |
| `docs/ai/roles/coordinator.md`「実ホストへの非冪等操作の承認」節 | `grep -n` | L114に実在(見出し) |
| `docs/ai/core.md`「安全機構がブロックしたとき」節 | `grep -n` | L28に実在(見出し) |
| `docs/ai/memory/lessons/permission-boundaries-must-be-designed-not-prompted.md` 教訓1・教訓2 | `grep -n` | L23・L29に実在(見出し) |
| 同ファイルL47の文言(「境界は最初から構造として存在していなかった…文章はsubagentに解釈される」) | `sed -n '47p'` | 逐語一致。SKILL.mdのS7節が引用した内容と一致 |
| 「それでも残る設計則」表が2段であること | 同ファイルL72–77を確認 | 2行(能力の不在/判断を要する制御)のみ。SKILL.mdは3段ラダーとして書いていないことを再確認 |
| `docs/ai/roles/auditor.md`「入力を絞る理由(設計の核心)」節 | `grep -n` | L40に実在。「依頼文に書いてよいのは『どの案件を検査するか』と『どこから読み始めるか』だけ」を確認し、S5のAuditor行と整合させた |

### 2-2. 量的上限・形式

- `wc -l skills/subagent-briefing/SKILL.md` = 79行(≤130行、契約充足)
- frontmatterが`name`/`description`の2キーのみであることを目視確認(既存12件と同形式)
- IPv4リテラルの混入なし(`grep -nE '([0-9]{1,3}\.){3}[0-9]{1,3}'`で該当なし)。VLAN ID・VM ID・実ホスト名の実値も書いていない(inventory group名・既公開ホスト名すら本文に登場しない、抽象的な記述に留めた)
- 時刻表記: SKILL.md本文に時刻表記なし(該当箇所がないため該当なし)

### 2-3. 規範の複製が無いこと(AC2相当の自己照合)

SKILL.mdの各節は「書くもの/書かないもの」という**分類そのもの**を定義しており、lesson・coordinator.md・core.mdの規範文そのもの(判断基準の文言、越権事例の詳細記述)は書き写していない。唯一lessonのL47を引用した箇所(S7)は、**規範ではなく事実記述(表の段数)の引用**であり、規範の複製ではないと判断した。ここが最も複製との境界が近い箇所のため、独立レビュー(U3)での再確認を推奨する。

### 2-4. `.claude/skills/subagent-briefing` の検出(参考観測)

本Implementer subagentの起動時、`.claude/skills/subagent-briefing` はまだ存在しなかったため配布されたSkill一覧には含まれていなかった。**symlink作成後の同一セッション内で、`subagent-briefing` がSkill一覧に現れた**(system-reminderで確認)。ただしrequirement AC3は「確認はCoordinatorが行う」と定めており、この観測はあくまで補助的な参考情報として記録するのみで、AC3充足の正式な確認はCoordinatorに委ねる。

---

## 3. 未解決事項

- **AC3の正式確認**: requirementにより確認主体はCoordinatorと定められているため、このU1では実施していない。上記2-4の観測(同一セッション内で検出された)を参考情報として残す。
- **S7引用の複製性の判定**: 2-3節に記載のとおり、L47の事実引用が「複製」に当たるかはU3(独立レビュー)で再確認してほしい。自分ではAC2に抵触しないと判断したが、この判断自体が実装者本人のものであるため独立の目が必要。
- **`docs/ai/context-classification.md`「Skillの配置とCodex/Claude Codeへの公開方法」節との整合**: 本節はsymlinkの作り方(相対パス、`.claude/skills/<name>`)を定めており、S9の実装はこの節の記述どおりに行った(節番号ではなく節タイトルで参照を特定した。同ファイルに`## 6.`が2つある問題はJ-1として認識済み・本案件では触れていない)。
- 契約の要素以外に自発的な追加は行っていない(章名・順序・語調は契約対象外のためImplementerの裁量で決めたが、水増しは避けた)。

以上。U3(Reviewer)への引き継ぎ事項は上記「未解決事項」の3点。

---

## 4. 差し戻し対応(U3 Critical #1、2026-07-28)

- 差し戻し元: `docs/ai/reviews/subagent_briefing/2026-07-28_006_u3_review.md` Critical #1
- 差し戻す理由: SKILL.md「Role別の差分」表のTech Lead行が、`docs/ai/roles/coordinator.md`「Tech Leadへ渡すもの」の列挙(目的・ゴール・概要設計・受入条件・制約・安全境界/詳細設計・機能分割・インターフェースの確定は委ねる)を実質同一の形で持ち、かつ引用元の明示が無かった。正本が変わってもSKILL.md側が追随しない状態になっていた
- 初回の依頼内容・契約S1〜S10・AC1対応表(S8)に変更なし。今回の修正はTech Lead行1箇所のみ

### 何をどう直したか

SKILL.md L41(旧)を次のように変更した。

- 旧: 「目的・ゴール・概要設計・受入条件・制約・安全境界を渡す。詳細設計・機能分割・インターフェースの確定はTech Leadに委ね、渡さない」(列挙を複製)
- 新: 「`docs/ai/roles/coordinator.md`「Tech Leadへ渡すもの」の一覧をそのまま渡す。詳細設計・機能分割・インターフェースの確定はTech Leadの責務であり渡さない(同箇所)」(列挙を削り、正本パスと節ラベルへの参照に置き換えた)

Auditor行が既に取っていた形(`docs/ai/roles/auditor.md`「入力を絞る理由」への参照のみで内容を複製しない)に揃えた。他の4行(Implementer/Reviewer/Tester/Auditor)は元々参照止まりか、正本に対応する列挙が存在しない独自の要約(Reviewer/Tester/Implementer行はrequirement/plan由来の一般論であり、単一の正本箇所の逐語列挙ではないため複製に該当しないとU3も指摘していない)であるため変更していない。

### AC2にどう答えたか

- 修正後、SKILL.md本文にcoordinator.mdの列挙(目的・ゴール・概要設計…安全境界)を書き写している箇所は無い。Tech Lead行は正本のラベル名「Tech Leadへ渡すもの」を指し示すだけで、列挙内容そのものは持たない
- coordinator.md側の列挙が将来変わっても(項目の追加・削除)、SKILL.md側は参照を通じて追随し、内容の食い違いは発生しない構造にした
- lesson L47の引用(「型が塞がないもの」節)はU3判定により複製に当たらないため変更していない

### 再確認条件への充足状況

| 再確認条件 | 状況 |
|---|---|
| Tech Lead行で、正本が変わったときSKILL.md側だけが古くなる状態に到達しない | 充足。列挙を削除し参照のみにした |
| 契約S5(Role別差分を独立1節の表1つで持つ、対象は5役)を引き続き満たす | 充足。節構成・対象5役に変更なし。表は1つのまま |
| AC1対応表(S8)と他の契約要素に影響を与えない | 充足。L62–70(AC1対応表)は無変更(diff確認済み)。他のS1〜S10要素も無変更 |
| 130行以内を維持する | 充足。`wc -l` = 79行(変更前と同一。文字列の長さは変わったが行数は変わっていない) |

### 自己検証

- `grep -n "Tech Leadへ渡すもの" docs/ai/roles/coordinator.md` でL30に実在することを確認(参照先の実在)
- 修正後ファイル全体を再読し、AC1対応表(L62–70)・S1〜S4・S6・S7・S9・S10相当節が変更前と同一であることを確認
- IPv4リテラル混入なし(`grep -nE '([0-9]{1,3}\.){3}[0-9]{1,3}'`で該当なし)
- `git status`で自分が触れたパス以外(`.claude/skills/document-norm-review`、`.claude/skills/incident-recording`、`docs/ai/reviews/subagent_briefing/`内の他ファイル)に変更が無いことを確認
- `git add`は行っていない(指示どおりAC4再検証は独立レビュー側に委ねる)

### 未解決事項(更新)

- 上記「3. 未解決事項」のうち、AC3の正式確認(Coordinator実施)は引き続き未実施
- S7引用の複製性については、今回のU3レビューでCritical該当なしと判定済みのため解消済みとして扱う
- 新たな未解決事項は無し
