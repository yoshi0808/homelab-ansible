# U2: `.claude/skills/` 未symlink2件の修復 — implement

- 作成: 2026-07-28 Implementer(B)
- 入力: `2026-07-28_001_requirement.md` D2、`2026-07-28_002_plan.md`§3(U2)・§5・§8
- 対象: `.claude/skills/document-norm-review`、`.claude/skills/incident-recording`

## 1. 実施内容

`.claude/skills/` 配下に、既存10件と同じ相対symlink形式で2件を新規作成した。

```
ln -s ../../skills/document-norm-review .claude/skills/document-norm-review
ln -s ../../skills/incident-recording   .claude/skills/incident-recording
```

作成前に対象2ディレクトリ(`skills/document-norm-review/SKILL.md`、`skills/incident-recording/SKILL.md`)が実在することを確認済み。作成後の一次記録:

```
lrwxrwxrwx 1 yoshi yoshi 33 ... .claude/skills/document-norm-review -> ../../skills/document-norm-review
lrwxrwxrwx 1 yoshi yoshi 31 ... .claude/skills/incident-recording   -> ../../skills/incident-recording
```

`skills/<name>` 本体は変更していない(requirement境界どおり)。

## 2. 既存10件との形式一致の確認方法

目視ではなく、既存symlinkを1件 `readlink` して形式を先に確定させてから作成した。

- `readlink .claude/skills/code-review` → `../../skills/code-review`(相対パス、`../../skills/<name>` 形式であることを現物で確認)
- 作成後、`ls -la .claude/skills/` で12件を一括表示し、既存10件と新規2件が**同じ矢印表記・同じ相対パス階層**であることを目視差分ゼロで確認(モード・所有者の桁も揃っている)
- `docs/ai/context-classification.md`「Skillの配置とCodex/Claude Codeへの公開方法」(節タイトルで特定。同ファイルに `## 6.` が2つあるため番号でなく本文中の記述「`.claude/skills/<skill-name>` から `skills/<skill-name>` への相対symlinkで公開する」と照合)と一致することを確認した

## 3. 自己検証の結果

| 検証 | 方法 | 結果 |
|---|---|---|
| symlinkの向きと相対性 | `readlink` | `../../skills/document-norm-review`、`../../skills/incident-recording`。既存10件と同一階層の相対パス |
| 解決可能性(壊れたリンクでないこと) | `test -f .claude/skills/<name>/SKILL.md` | 両方とも到達し、真値を返した(既存の欠陥=リンク先が無い、という状態ではない) |
| symlinkが本体のコピーでなく実体参照であること | `diff <(cat .claude/skills/document-norm-review/SKILL.md) <(cat skills/document-norm-review/SKILL.md)` | 差分ゼロ。symlink経由で正本と同一内容が読める(複製ではない) |
| 変更範囲がrequirement §6の書込先内に収まっているか | `git status --porcelain=v1` | `.claude/skills/document-norm-review`、`.claude/skills/incident-recording`、および並行実行中のU1由来の未追跡 `docs/ai/reviews/subagent_briefing/` のみ。`skills/` 本体・他ファイルへの変更なし |
| 実ホストへ触れていないこと | 実施したコマンドの棚卸し | `ln -s` / `ls` / `readlink` / `test` / `diff` / `git status` のみ。Ansible実行・ad-hoc・sshは一切なし |
| identity昇格の有無 | 実施したコマンドの棚卸し | `sudo`・`--become-user` は使用していない。すべてansy上の作業ツリーに対する非特権コマンド |
| `git add`/`commit`/`push` | 実施したコマンドの棚卸し | 未実施(stagingもしていない) |

**Claude Code自動検出そのもの(AC3)の確認はスコープ外。** requirement AC3は「確認はCoordinatorが行う」と明記しており(harnessの登録タイミングが版依存のため)、本Implementerはsymlink作成とその形式一致・解決可能性の確認までを担当した。

## 4. 未解決事項

- **AC3の実地確認(Claude Codeのプロジェクトskill一覧に `document-norm-review` / `incident-recording` が候補として現れるか)は未実施。** requirement/計画とも「確認はCoordinatorが行う」と定めており、本Implementerの権限・役割の範囲外と判断し実施しなかった。
  - **付随観測(参考情報、正式確認ではない)**: symlink作成後、本Implementer自身のセッションのharnessに提示されたskill一覧に `document-norm-review` と `incident-recording` が現れた(このファイル冒頭のsystem-reminderで両者のdescriptionが提示された)。ただし本Implementerは正式なAC3確認の主体ではなく、`ls -la`によるファイルシステム上の存在確認とharnessのUI表示確認は別物であるため、これをAC3合格の記録として扱わず、Coordinatorが正式に確認する際の傍証としてのみ残す。
- 並行実行中のU1(`skills/subagent-briefing/SKILL.md` 新設、`.claude/skills/subagent-briefing`)の成果物は本報告時点で `git status` に未追跡として現れていない(U1がまだ着手前か、ちょうど作成前のタイミングと見られる)。本Implementerはこれに一切手を加えていない。
- `git add`/`commit`/`push` は計画どおりU3(Reviewer)のAC4検証時にまとめて行われる想定であり、本単位では未実施のまま完了とする。

## 5. 変更ファイル一覧(本Implementerの成果物のみ)

- `.claude/skills/document-norm-review`(新規symlink、`../../skills/document-norm-review` を指す)
- `.claude/skills/incident-recording`(新規symlink、`../../skills/incident-recording` を指す)
- `docs/ai/reviews/subagent_briefing/2026-07-28_005_u2_implement.md`(本ファイル)
