# requirement: 障害の証拠バンドル生成(Step 1)

日付: 2026-07-27(JST)
状態: **requirement。実装未着手。**
Tier: **4**(軸A=本番影響のある実ロジック変更かつ実装前に調査と受入を分ける価値がある / 軸B=Tier 4はReviewerを工程内に含むため`+R`は付けない)
前提: `docs/ai/reviews/incident_auto_capture/2026-07-27_001_design_agreement.md` の決定D1〜D7。本requirementはそれを覆さない。

## 1. 問題定義

Ansibleジョブの失敗はSlackへ通知され、YoshinobuがSlack経由でCodex→`recovery.io`のallowlistコマンドで調査している。**調査の道具としては機能している**が、次の3つが失われている。

1. **捕捉されない失敗がある。** 気づかなかった事象・後回しにした事象が記録に残らない。2026-07-27の月次Knowledge振り返りが「種別:未遂の該当2件が未起票、母数不足でタグ集計が働かない」と報告済みで、Incident→Policy/Skill昇格のループが入口の枯渇で回っていない。
2. **調査結果が蒸発する。** `recovery.io` が返す良質な調査結果はSlackの散文であり、機械可読でもリポジトリ内でもない。同じ障害を再度調べても、前回何を確かめたかが残っていない。
3. **3者に共通の識別子が無い。** Slack通知 / `recovery.io` の調査結果 / `docs/ai/memory/incidents/` が互いに紐づかない。

さらに、既存の2つの観測面はそれぞれ**系統的な死角**を持つ。

| 観測面 | 拾えるもの | 落とすもの |
|---|---|---|
| Semaphoreのジョブ結果 | 中断・crash・rc≠0、生ログ全文、ジョブID | **rc=0の意味的WARNING**(Semaphoreは緑のまま) |
| `common_slack/notify.yml` | `slack_status`(warning/critical/error) | **notifyへ到達しない失敗**(UNREACHABLE中断、`any_errors_fatal`、テンプレート/構文エラー、timeout、kill)。加えて**caller側でincludeごとゲートされる経路** |

後者の実例は、`proxmox_patch_dryrun` が単一ノード対応前に持っていた `hosts: proxmox` + `any_errors_fatal: true` の組み合わせである(pve1 UNREACHABLE時にplayが即中断しnotifyが発火しない)。**ただしこの `any_errors_fatal` はADR-002 decision a-1で撤去済みであり、現行コードで `any_errors_fatal: true` を持つのは `playbooks/proxmox_patch_weekly_full.yml` L118 の1箇所のみである**(2026-07-27に現物確認)。一方 `proxmox_patch_dryrun` は意図的に rc=4→rc=0 へ変更しており、前者の死角も現実に開いている。

**caller側ゲートの実在(2026-07-27に現物確認)**: `notify.yml` のincludeは38箇所/25ファイルにあり、そのうち `radius_healthcheck` / `monitoring_healthcheck` / `proxmox_healthcheck` の3つは **include 自体に `when: not (skip_notifications | default(false) | bool)` と「criticals または warnings が存在すること」を課している**。したがって、①抑止指定時はnotify.ymlがそもそも読み込まれない ②healthcheckがOKのときはnotifyが一度も呼ばれない。**`notify.yml` は「全ジョブ結果の絞り」ではない。**

## 2. ゴール

**quory上で、ジョブ失敗の証拠を決定論的に(LLMなしで)バンドルとして保全する。**

- 失敗時点の事実(要約ログ・生ログ・rc・時刻)と、収集時点の現況スナップショットを1つのIDの下にまとめる。
- 通知が抑止されている局面でも証拠は残す。
- **観測が被観測(本番ジョブ)の挙動を変えない。**

Step 1単独でも価値がある。人がSlackからバンドルIDを`recovery.io`へ渡せば、調査の初動が速くなる。あわせてStep 2(ansy側の`claude -p`による第一報起票)の入力仕様がこれで確定する。

## 3. 非ゴール

このStepでは次を**実装しない**。

- LLMによる叙述、原因推定、対応提案(Step 2)
- ansy側での取り込みと `docs/ai/memory/incidents/` への起票(Step 2)
- 名前付き操作カタログの拡張(D1/D2。実際の障害が必要性を証明してから)
- あらゆる復旧・是正の実行(`recovery_probe`ラダーと人の領分)
- `recovery.io` allowlistの拡張、新しい常時接続チャネル
- Semaphore UI設定・スケジュールの変更

## 4. ユーザーストーリー

- **Yoshinobu(障害対応時)**: Slack通知にバンドルIDが載っているので、それを`recovery.io`へ渡すだけで、失敗時点の生ログと現況が揃った状態から調査を始められる。何が起きたかを組み立て直す手間が要らない。
- **Yoshinobu(事後)**: 週末や翌朝に、その週に起きた失敗の一覧を証拠つきで見返せる。Slackを遡らなくてよい。
- **Coordinator(対話セッション)**: `/clear` 後でも、バンドルを読めば過去の障害の一次情報に到達できる。Slackの散文でなく構造化された事実から判断できる。
- **月次Knowledge振り返り**: 捕捉漏れが減り、`原因分類`タグの母数が確保される。

## 5. 要件

### P0(初回実装に不可欠)

**R1. 通知経路での捕捉(T1)**
`roles/common_slack/tasks/notify.yml` の**冒頭**、`tester_mode` / `skip_notifications` の抑止ゲート**より前**に捕捉を挿入する。通知の抑止は人間の注意管理、証拠の保全は開発ループであり、別の要件である。`tester_mode` 中はスキップせず**フラグとしてバンドルへ記録**し、仕分けは叙述側(Step 2)へ委ねる。

**T1は安価かつローカル完結でなければならない。** メタデータ(`slack_status` / `slack_channel` / `slack_title` / `slack_message` / playbook名 / 時刻)のファイル書き込みのみを行い、**SSHもHTTPも行わない**。38箇所(25ファイル)から呼ばれるため、ここに時間のかかる処理を置くと全通知が遅くなり、ハングの経路にもなる。

**T1は `slack_status` で絞らない。** `ok` を含む全ての通知を記録する。D7が価値を置く「要約と生ログの食い違い」(Semaphore #461: 要約 `Result=OK` / ジョブ rc=4)は、`ok` のレコードが残っていなければ検出できないため、`warning` 以上に絞る設計は本案件が最も欲しい情報を捨てる。副次的に、捕捉側に条件式が不要になりAC4のリスクも減る。

**T1で越えられない限界を明示する(2026-07-27の現物確認による)。** 抑止ゲートより前に置いても、**caller側で `when:` によりincludeごとゲートされている経路は捕捉できない**(§1参照)。該当は healthcheck 3 role で、①`skip_notifications` 指定時、②healthcheckがOKで warnings も criticals も無いとき、notify.ymlは読み込まれない。**evidence収集のためにcaller側の通知ロジックを書き換えることはしない**(観測が被観測を変える。R3に反する)。この死角は受け入れ、Semaphore側の観測面で補う。①はTester検証時に主に使われる指定であり本番障害では稀、②はジョブ結果自体が正常なケースである。

**R2. 収集器(quory上、周期実行)**
T1が残したレコードと、Semaphoreのジョブ結果を突き合わせてバンドルを完成させる。

- Semaphoreのジョブ結果(SQLite)から、失敗ジョブのジョブID・テンプレート名・開始/終了時刻・終了ステータス・**生ログ全文**を取得する。
- 既存 `recovery.io` allowlist を機械的に一巡して**現況スナップショット**を取得する(`status` / `failed` / `journal-*-err` / `disk` / `memory` / `load` / `network` / `ports`)。**コマンドは組み立てず、既存の名前を呼ぶだけ**(D1)。
- `/usr/bin/flock -n` で多重起動を防ぐ(`knowledge-review.service` に前例あり)。

**R3. 観測が被観測の挙動を変えない**
最重要。T1の追加によって、**呼び出し元playbookの終了コード・タスク結果・所要時間の有意な増加のいずれも発生させない**。捕捉自体の失敗はplayを失敗させず、失敗した事実をレコードへ残す。

**R4. 成果物は作業ツリーを汚さない**
バンドルは `reports/incidents/<id>/` 配下へ置く。`.gitignore` は `reports/**/*.json` / `*.log` / `*.md` **のみ**を除外するため、**バンドルの全ファイルをこの3拡張子のいずれかにする**(`.txt` は追跡対象になってしまう)。`.gitignore` を変更しない方針とする。

**R5. 収集の失敗を可視化する**
最悪の壊れ方は**静かに空のバンドルを作り続ける**ことである。Semaphoreのスキーマ不一致、SQLiteロック、SSH到達不能、allowlistに無いコマンド指定などは、握りつぶさずバンドル内へ「何を試みて何が取れなかったか」として記録する。この記録はStep 2で §5「未確認事項」の材料になり、カタログ拡張の根拠になる。

### P1(Step 1のうちに入れたいが、無くても成立する)

**R6. 相関IDをSlack本文へ載せる**
IDはSemaphoreジョブ由来ならジョブ番号を使う(**発明しない**)。Semaphore外は `timer-<unit>-<timestamp>`。これにより通知が終点でなく入口になる。

**R7. バンドルのローテーション**
quory上に無限に溜めない。保持世代または保持日数で削除する。

**R8. Semaphore外ジョブの保険**
Semaphoreを経由しない本番ジョブは実質 `cert-renew-quory` のみ。終了コードからの薄い捕捉経路を用意する。

### P2(将来)

**R9. 秘密情報のredaction**
Step 1のバンドルは**quory内に閉じており、`.gitignore` 済みで公開repoへ出ない**ため緊急度は低い。redactionが本当に要るのは、バンドルを公開repoの `docs/ai/memory/incidents/` へ叙述するStep 2の境界である。そこでは「生ログを本文へ転記させず、バンドルへの参照だけ書かせる」規律を仕様に含める。

### Won't

`docs/ai/status.md` に載せない項目、`recovery.io` の対話的調査の置き換え、Notionの時刻管理表の参照(D5により明示的に不採用)。

## 6. 受入条件

すべて `Given / When / Then` で書き、**成否をどこで観測するか**を `Then` に含める。

**AC1(通知経路の捕捉)**
- Given: 本番quoryで、`common_slack`経由の通知を行うplaybookが `slack_status: error` で通知する
- When: 当該playbookを通常どおり実行する
- Then: **playbookの終了直後**に spool レコードが生成され、`slack_status` / `slack_channel` / `slack_title` / playbook名 / 時刻(タイムゾーン明示)が記録されている。**playbookの終了コードは捕捉導入前と同一**であり、タスクの failed 数も増えていない。**バンドル `reports/incidents/<id>/` の完成は次の収集周期の後に確認する**(IDは収集器が確定するため。R6/ADR-003参照)

**終了コード比較の前提**: 「捕捉導入前と同一」を判定するには**導入前のベースラインが必要**であり、配備後には取得できない。対象playbookの終了コードと所要時間の実測を、実装着手前(W0)に済ませておくこと。

**AC2(notifyを逃す失敗クラスの捕捉) — replay方式(2026-07-27改訂)**
- Given: Semaphoreのデータベースに、**過去に発生済みの**失敗ジョブが存在する(notifyへ到達せず中断したもの。例: 2026-07-26のジョブ #461 = pve1 UNREACHABLE、rc=4)
- When: 収集器を実行し、その過去ジョブを対象として読ませる
- Then: notify由来のレコードが存在しないにもかかわらず、**収集器がSemaphoreのジョブ結果だけからバンドルを生成している**。バンドルにはジョブID、`UNREACHABLE` を含む生ログ、終了ステータスが含まれる

**この改訂の理由**: 当初は「新たに失敗を起こして観測する」形だったが、①現行コードで `any_errors_fatal: true` を持つのは実パッチ適用playbook 1本のみで、検証のために本番パッチ経路を失敗させるのは容認できない、②pve1到達不能状態への依存は夏季平日シャットダウン運用の時間窓に縛られる。**過去の失敗を読ませるreplay方式なら、再現条件を作る必要がなく、時間窓への依存も消える。** これにより §8 のタイムライン制約(AC2をpve1停止運用中に済ませる)は解消する。

**AC3(抑止下でも捕捉される) — `tester_mode` に限定(2026-07-27改訂)**
- Given: `tester_mode: true` を指定する。かつ対象は **`notify.yml` を無条件にincludeするplaybook**(caller側 `when:` を持たないもの)とする
- When: 通知を行うplaybookを実行する
- Then: **Slack通知は送信されない**(既存挙動を変えない)が、バンドルは生成され、`tester_mode` フラグが記録されている

**この改訂の理由**: `skip_notifications` は healthcheck 3 role では **caller側でincludeごとゲート**しており、notify.yml内に何を置いても到達しない(§1・R1参照)。ACとして成立しないため対象から外す。caller側を書き換えて成立させることはしない(R3)。なお `-e tester_mode=true` で assert 失敗するplaybookが存在するため、AC3の対象playbook選定時にそれらを除外すること(Tech Lead調査で5本を特定済み。`docs/ai/reviews/incident_auto_capture/2026-07-27_003_investigation.md` 参照)。

**AC4(観測が本番を壊さない)**
- Given: 捕捉の書き込み先が存在しない/権限がない/ディスクフルなど、捕捉が失敗する条件
- When: 通知を行うplaybookを実行する
- Then: **playbookは捕捉導入前と同じ終了コードで完了する**。捕捉の失敗はplayを失敗させず、Semaphoreのジョブ結果にも失敗として現れない。捕捉が失敗した事実は収集器側で「取得失敗」として記録される

**AC5(収集器の異常が可視)**
- Given: Semaphoreのスキーマが想定と異なる(カラム名変更等)状況を模擬する
- When: 収集器を実行する
- Then: **空のバンドルを黙って作らない**。「Semaphoreからの取得に失敗した」ことが明示的にバンドルへ記録され、収集器の終了コードは非ゼロ、その値と意味が定義されている

**AC6(作業ツリーを汚さない)**
- Given: バンドルが1件以上生成された状態
- When: quoryの当該リポジトリで `git status --short` を実行する
- Then: **出力が空である**(バンドル由来の untracked ファイルが1件も現れない)

**AC7(多重起動の抑止)**
- Given: 収集器が実行中である
- When: 同じ収集器がもう1つ起動される
- Then: 後発は `flock -n` により即座に終了し、終了コードは「多重起動により実行せず」を意味する定義済みの値になる。**本番ジョブへの影響は発生しない**

## 7. オープンクエスチョン

**OQ1. Semaphoreのタイムスタンプの実体(要・実測)**
Yoshinobuの確認では `/etc/semaphore/config.json` は ansy / quory とも `Asia/Tokyo`。一方 `recovery.io` から取得したジョブ #461 の出力は `2026-07-26 20:45:01 UTC` と表示されていた。**両者は矛盾しうる**(多くの実装はDBにUTCで保存し表示時にローカルへ変換する。逆に、ローカル時刻へリテラルで `UTC` を付けている可能性もあり、その場合は表示自体が誤り)。

**設定ファイルの記述で結論を出さない。** 解決方法を先に決めておく: 既知の壁時計時刻に実行されたジョブを1件選び、SQLiteの当該カラムの生値と突き合わせて保存形式を実測する。バンドルは**タイムゾーンを明示したフィールド**で保持し、ローカル時刻に裸の `UTC` / `Z` を付けない。

**OQ2. 収集器の起動間隔。** 短いほど現況スナップショットが失敗時点に近いが、負荷とノイズが増える。

**OQ3. 現況スナップショットの対象ホスト。** 失敗したホストのみか、同じinventory groupの全ホストか。pve1の障害でpve2の状態が要るように、**隣のホストの正常性が判断材料になる**ため単純に「失敗したホストだけ」ではない可能性が高い。

**OQ4. バンドルの保持期間・世代数(R7の具体値)。**

**OQ5. Step 2で顕在化する「作業ツリー汚れ vs 月次振り返り」。** 無人実行はcommitできないため、Step 2でIncidentを自動起票すると作業ツリーが汚れたまま残り、月次Knowledge振り返りの「作業ツリーが汚れているときは中止」と衝突する。**Step 1では発生しない**(バンドルは`.gitignore`済みでrepo管理外、AC6で担保)。したがってこの判断はStep 2のrequirementへ持ち越す。

**OQ6(解決済み、2026-07-27)。対象ジョブ範囲。** 「Semaphoreで失敗した全ジョブ(スケジュール実行・手動実行を問わない)」+「**notify.ymlに到達した全ケース(`ok` を含み、`slack_status` で絞らない)**」。playbookを列挙せず観測面で定義する方針は維持する。当初案の「`warning` 以上」は、D7が最も価値を置く「要約と生ログの食い違い」を検出不能にするため却下した(R1参照)。

**OQ1〜OQ5の解決状況**: OQ2 / OQ3 / OQ4 / OQ5 は `2026-07-27_003_investigation.md` で解決済み。**OQ1は未解決**だが、本requirementが当初示した実測方法が成立しないことが判明した — 既存の名前付き操作 `homelab-semaphore-query` の `recent-failed` は `substr(t.start,1,19)` を返し、**保存形式を決める末尾(`Z` / `+09:00` / 無印)をちょうど切り落とす**。したがってOQ1の観測にはカタログ拡張(タイムゾーンを切らないクエリの追加)が必要であり、これはD1の拡張経路そのものに当たるためYoshinobuの判断事項とする。

## 8. タイムライン考慮

- Step 1はStep 2の**入力仕様を確定させる前提工程**であり、先に完成させる。
- ~~AC2は pve1が到達不能な状態を必要とする~~ **解消(2026-07-27)。** AC2をreplay方式へ改訂したため、pve1停止運用の時間窓への依存は無くなった。既にsemaphore.dbにある過去の失敗ジョブを読ませればよい。
- **終了コードのベースライン取得(AC1)は配備前にしか行えない。** 実装着手前のW0で済ませる。
- 8/26に月次Knowledge振り返りの初回無人実行がある。Step 1は`reports/`配下に閉じるため干渉しないが、Step 2の設計判断(OQ5)はこの日程を意識する。

## 9. 参照

- `docs/ai/reviews/incident_auto_capture/2026-07-27_001_design_agreement.md`(決定D1〜D7と理由)
- `roles/common_slack/tasks/notify.yml`(単一の絞り、抑止ゲート)
- `roles/recovery_exec/templates/recovery-investigate-dispatch.sh.j2`(現行allowlist、引数面ゼロ)
- `skills/incident-recording/SKILL.md`(Incidentの型、2段階)
- `docs/ai/policies/ansible_test_safety_policy.md`(`safe-readonly` 他の分類)
- `docs/ai/context/system/semaphore.md`(success/failでWARNINGの意味が潰れる件)
