# incident_auto_capture_step2 — 進捗と課題

- 正本の読み方: `docs/ai/roles/techlead.md`「進捗・課題の記録」
- **書き手**: 計画時・統合時=Tech Lead / 実行フェーズ=Coordinator(`docs/ai/roles/coordinator.md`)
- requirement(**現在有効**): `docs/ai/reviews/incident_auto_capture_step2/2026-07-28_006_step2_requirement.md`(Step 2 本体 — 転送と評価)
- 先行フェーズのrequirement(**完了**): 同 `..._001_oq5_requirement.md`(OQ5の決着のみを扱った調査・設計判断フェーズ)
- 案件のTier: **Tier 4**
- 規範の正本: `docs/ai/policies/incident_capture_policy.md`。**案件記録より上位**であり、競合したらPolicyを優先する。**条文の範囲を数で書かない**(2026-07-28にIC-033が追加され、範囲表記が古くなった)

**状態: 計画受領ゲートを通過した(2026-07-28 Coordinator)。実行フェーズへ入る。** 次はU0(Tester・read-only)。

**ゲートの判定**(`docs/ai/roles/coordinator.md`「計画受領時のゲート」。1回だけ適用し、走行中は繰り返さない)

| # | 確認 | 判定 |
|---|---|---|
| 1 | 並行して立てられる単位の組が申告されているか | **○** P1={U1,U3} / P2={U2,U4} / P3={U5,U6} |
| 2 | 並行する単位が同じファイルに触れないか。触れ合う面が実装前に確定しているか | **○** I-1〜I-8で確定。初版で未確定だったplay構造・argv・挿入位置が第2版で固定された |
| 3 | 層1(単位の大きさ・未決定の数)が通っているか | **○** 12単位・最大45・未決定1。**Coordinatorが算術を再計算して一致**(440 = 30+45+30+45+40+35+40+40+20+35+40+40、配分160/75/205) |
| 4 | 層2(技術的前提の反証)が実施され、根拠が現物で確かめられているか | **○** 実施済み。**Coordinatorも要点2件を現物で再確認** — `knowledge-review.service.j2:12` が `TimeoutStartSec={{ knowledge_review_timeout + 600 }}`(=2400)であること、`incident-capture-collector.py:788` が `summary["generated_at"]` を書いていること |
| 5 | Yoshinobuのcommit/pushが工程上の待ちとして計画に入っているか | **○** W1に依存する単位(U7・U10・U8・U9・U11)を列挙済み。**初版に欠けていた「既存unitを書き換えたのに配り直す工程が無い」がU10で埋まった** |

## この案件の現在地

| フェーズ | 状態 | 成果物 |
|---|---|---|
| OQ5(出力先の設計判断) | **完了** | `..._002_oq5_investigation.md`、`..._003_adr_review.md`、`docs/ai/adr/005-...md` 初版 |
| quory側の実装実態の棚卸し | **完了** | `..._004_quory_units_survey.md` |
| Policyの制定と査読 | **完了** | `docs/ai/policies/incident_capture_policy.md`、`..._005_policy_review.md` |
| Step 2 requirement の確定 | **完了** | `..._006_step2_requirement.md` |
| 実装計画とADR改訂(初版) | **完了(2026-07-28)** | `..._007_step2_plan.md` 初版、`docs/ai/adr/005-...md` **第2版** |
| 計画査読(層1+層2) | **完了(2026-07-28)** | `..._008_plan_review.md`。**層1は通過、層2で差し戻し4件**。ADRはAC10充足を確認済み |
| **実装計画の改訂(第2版)** | **完了(2026-07-28)** | `..._007_step2_plan.md` **第2版**。F-1〜F-14の処遇は同 §7.1 |
| **Coordinatorの計画受領ゲート** | **通過(2026-07-28)** | 本ファイル冒頭の判定表 |
| 実行 → 統合 → クローズ | **進行中(U0から)** | — |

### 目的(2026-07-28 Yoshinobu、最優先)

> quoryでインシデントをエラー発生ごとに確保する(**完了**)。そして**8/26を皮切りに、ansy側でタイマー起動した `claude -p` がインシデントの評価をして、開発プロセス全般の見直しに寄与したい**。

追加要求: **対話セッションからも repo で障害の中身を確認できること。**

### 確定した設計の骨格(`..._007_step2_plan.md` §2 が正本)

- 転送 = **ansy起点のpull・毎時**。quoryには何も配備しない(既定の設計では)。着地点は **ansyの `reports/incidents/quory/`**(repo作業ツリー内・gitignore済み)。**playbookは2 play構成**で、到達不能時も状態記録とSlack通知が必ず出る
- 評価 = **既存の月次timerの中で2回目の `claude -p` を専用プロファイル(`Edit` エントリゼロ)で起動**。新しいtimerもjobも作らない。**挿入位置は `Save report` の直後・`Collect resulting diff` の直前**
- 出力 = LLMは標準出力のみ。ファイル化はAnsible。成果物は `reports/incidents/_evaluations/`(ミラーの外側 — IC-031で上書きされるのはミラーだけ)
- 検知 = ①同期の非ゼロ終了+systemd `failed`+Slack(毎周期)②ミラー心拍のstale判定(毎周期)③月次評価での滞留報告(件数と経過日数)。**尺度は `summary.json` の `generated_at`** であり、転送機構から独立している
- 時間予算 = `claude -p` が2本になるため **unitの `TimeoutStartSec` を 2400 → 3600 秒へ**(1800 + 900 + 余裕900)。**既存unitの変更を伴うため、配備単位とcommit待ちが工程に要る**

---

## 単位ごとの状態

**実行フェーズの記入者はCoordinator。** 各単位の完了時に実績(`tool_uses`)・逸脱(割合)・未決定の残数を更新する。

| 単位 | Role | 状態 | 見積 `tool_uses` | 実績 | 逸脱 | 未決定の残数 |
|---|---|---|---|---|---|---|
| OQ5 requirement | Coordinator | **完了** | — | — | — | 0 |
| OQ5 決着・ADR起草(初版) | Tech Lead(1体目) | **完了** | 基準対象外 | 37 | — | 0 |
| ADR査読(層2) | Tech Lead(2体目・別体) | **完了** | 基準対象外 | 20 | — | 0 |
| ADR改訂(初版→A-2′) | Tech Lead(1体目・再起動) | **完了** | 基準対象外 | — | — | 0 |
| quory unit棚卸し | Tester | **完了** | 基準対象外 | — | — | 0 |
| Policy査読(初回+再照合) | Reviewer | **完了** | 基準対象外 | — | — | 0 |
| Step 2 実装計画(初版)+ ADR第2版 | Tech Lead(3体目) | **完了** | 基準対象外 | — | — | — |
| **計画査読(層1+層2)** | Tech Lead(4体目・別体) | **完了** | 基準対象外 | — | — | — |
| **実装計画の改訂(第2版)** | Tech Lead(3体目・再起動) | **完了** | 基準対象外 | — | — | — |
| U0 事実確認(read-only) | Tester | **完了(2026-07-28)** | 30 | **13** | **-57%**(下振れ) | **0** |
| U1 `incident_sync` 同期本体 | Implementer | **未着手** | 45 | — | — | 0 |
| U2 `incident_sync` 配備+playbook+`.gitignore` | Implementer | **未着手** | 30 | — | — | 0 |
| U3 評価工程 + unitの時間予算 | Implementer | **未着手** | 45 | — | — | 0 |
| U4 決定論メトリクスと通知 | Implementer | **未着手** | 40 | — | — | 0 |
| U5 転送側の独立レビュー | Reviewer | **未着手** | 35 | — | — | 0 |
| U6 評価側の独立レビュー | Reviewer | **未着手** | 40 | — | — | 0 |
| **W1 commit / push** | **Yoshinobu** | **未着手** | — | — | — | — |
| U7 転送の配備と正常系 | Tester | **未着手** | 40 | — | — | 0 |
| **U10 `knowledge_review` unitの再配備**(新設) | Tester | **未着手** | 20 | — | — | 0 |
| U8 転送の異常系と検知 | Tester | **未着手** | 35 | — | — | 0 |
| U9 評価工程の単独検証 | Tester | **未着手** | 40 | — | — | 0 |
| **U11 月次フル経路の1回通しとAC9回帰**(新設) | Tester | **未着手** | 40 | — | — | 0 |
| **実行単位 合計** | — | — | **440 / subagent 12体** | — | — | **1** |

**並行して立てられる組**: P1={U1, U3} / P2={U2, U4} / P3={U5, U6}。**検証はすべて直列**(U7 → U10 → U8 → U9 → U11)。詳細と根拠は `..._007_step2_plan.md` §4。

**判定基準(層1が数えるだけで使えるもの)**

- 実行単位が80 `tool_uses` を超えていないか → **最大45。超過ゼロ**
- 1単位の未決定が2件以上ないか → **最大1件(U0)。超過ゼロ**
- 工程配分: 製造160(36.4%)/ レビュー75(17.0%)/ 検証205(46.6%)。**検証が重いのは、F-1とF-7が「実行しなければ分からない」検証を2単位分足したため**

---

## 未決定

| ID | 内容 | 決める人 | 状態 |
|---|---|---|---|
| ~~OQ5~~ | 自動起票した第一報の出力先 | Tech Lead提案 → Coordinator承認 | **決着済み。**ただし**問いそのものが変質した** — IC-007で「事象ごとの第一報」が消えたため、決着の対象は「証拠のミラーと評価成果物の置き場所」になった。結論は `docs/ai/adr/005-...md` **第2版**(repo作業ツリー内のgitignore済みパス)。初版の推奨A-2′は**制約12(対話セッションから読める)を満たさないため不採用**へ反転した |
| **OQ-A** | **IC-025の適用範囲。** 書込許可ゼロの評価段は、作業ツリーが汚れていても走らせてよいか | **Yoshinobu**(Policyの領域) | **閉じた(2026-07-28)。現設計のまま=走らせない(安全側)。**IC-033が「中止は喪失でなく遅延」を前提条件付きで是認し、計画はその枠内にある |
| **OQ-B** | `.gitignore` へ `reports/incidents/` のディレクトリ単位除外を足すか | Coordinator(承認範囲) | **承認済み(2026-07-28)。「推奨」から「rsync系機構の前提条件」へ格上げ。**査読F-4が、rsyncの一時ファイル(既定形・`.~tmp~/` 形とも)が現行の除外に一致しないことを示した |
| **OQ-C** | AC1の解釈。**`-- reports/incidents/` にスコープを絞る** | Coordinator | **承認済み(2026-07-28)** |
| **OQ-D** | P0-6の評価段への適用。**評価段は `fail` させない** | Coordinator | **承認済み(2026-07-28)** |
| **OQ-E** | ansy側の保持(P1-3)。v1は**削除しない** | Coordinator(U0の実測後) | **v1の方針は確定。**次期に世代管理を設計するときは**IC-033の前提条件1が制約として掛かり、Coordinatorの承認範囲で完結しない可能性がある**(計画 §10-5へ申し送り済み) |
| ~~U0-1~~ | 転送機構(`ansible.posix.synchronize` か `find`+`fetch` ループか) | U0の観測が決める(判定規則は計画に固定済み) | **決定済み(2026-07-28 U0)。`ansible.posix.synchronize`(rsync)。** 両端にrsync 3.4.1が実在したため判定規則が一意に決まった。**あわせてU1′分岐(quory側へ `ann` のread ACLを配備)は不要と確定** — `ann` は昇格せずに `summary.json` と `.log` を `rc=0` で読めた(`other::r--` で成立、固有ACL不要)。一次記録は `..._009_u0_test_result.md` |
| ~~I-7 play構造~~ | 到達不能時に状態記録と通知が出るか | Tech Lead | **決定済み(第2版)。2 play構成。**初版は未申告の未決定として抱えていた(査読F-2) |
| ~~I-8 argv~~ | 封じ込め3条件のうちCLI側2条件 | Tech Lead | **決定済み(第2版)。**JSONに書けない2条件をargvの表として固定(査読F-5) |
| ~~挿入位置~~ | 評価工程を `main.yml` のどこへ入れるか | Tech Lead | **決定済み(第2版)。`Save report` の直後・`Collect resulting diff` の直前**(査読F-14) |
| ~~時間予算~~ | `claude -p` 2本のtimeout実値と実行順 | Tech Lead | **決定済み(第2版)。900 / 1800、振り返り本体が先。`TimeoutStartSec` は3600**(査読F-1) |

---

## 課題

| # | 内容 | 影響する単位 | 滞留開始 | 状態 |
|---|---|---|---|---|
| J-1 | ADR-005初版の Decision (1) と (2) が両立しない(封じ込め構成ではrepo外へ書けない) | OQ5の結論 | 2026-07-28 査読 | **解消済み。**改訂でA-2′へ差し替え、第2版でも「書込主体はAnsible / LLMは標準出力だけ」として生き残っている |
| J-2 | A-1を退けた唯一の根拠(`.gitignore` の拡張子依存)が、Coordinatorの承認範囲で動かせる1行だった | OQ5の結論 | 2026-07-28 査読 | **解消済み。**第2版は**A-1+H(ディレクトリ単位除外)を採用**した。加えて同期側のスコープ付き `git status` 検査を足し、依存を「1行に賭ける」から「壊れたら即座に失敗する」へ変えた |
| J-3 | 中核論証(i)「構造的に母数に入らない」の導出が誤り | ADRの論拠 | 2026-07-28 査読 | **解消済み。**撤回済み。結論は別の3本の論拠で支えられており、第2版でも無傷 |
| J-4 | 「Step 2は新たな中止要因を作らない」が未設計の転送経路に及ばない | Step 2 requirement | 2026-07-28 査読 | **解消済み。**転送経路が設計され、(4′)①②で二重に担保された。**検証はU7・U9で行う**(設計上の担保であって、まだ実測ではない) |
| J-5 | 未昇格の第一報を検知する主体が誰も居ない | Step 2 requirement | 2026-07-28 査読 | **解消済み(設計として)。**P0-5として要件化され、計画§2.6が①転送停止②滞留の2つを決定論的な指標に落とした。**指標は粗い** — Incidentの型に相関ID欄が無いため、「新しい証拠がN件届いたがIncidentは1件も増えていない」という形にとどまる(§10-1へ申し送り) |
| J-6 | `roles/knowledge_review/templates/job-settings.json.j2` が指すLessonファイルが存在しない(dangling参照) | Step 2の着手前 | 2026-07-28 起草時 | **解消済み(2026-07-28 現物で確認)。**同ファイル6行目は現在 `claude-code-unattended-session-confinement.md` を指しており、実在する。`write-confinement` の文字列はrepo内に**案件記録の2ファイル(本フォルダの `_002` と `_003`)にしか残っていない。** 本課題表が「未修正」としていたのは記録が古かった |
| **J-8** | **計画初版が「実行可能性」の側に欠陥を4件残していた。** ①`claude -p` 2本が月次unitの `TimeoutStartSec`(2400秒)を超え、SIGKILLでSlack通知ごと消える ②`hosts: quory` 前提だと到達不能時に状態記録も通知も出ない ③転送機構の未決定が下流の指標定義へ漏れる(`fetch` はmtimeを保存しない)④rsyncの一時ファイルが `.gitignore` に一致しない。**初版のfile:line引用15件は全件正しかった** — 誤っていたのは引用ではなく、そこから組んだ実行可能性である | 全実装単位 | 2026-07-28 計画査読 | **解消済み(第2版)。**4件とも計画側で決め切り、実装者に委ねていない。処遇は計画 §7.1 |
| **J-9** | **U11の後始末が未追跡ファイルを戻せない。** 計画 §4 U11 手順4 は「振り返り本体が書いた差分を `git checkout --` で戻す」としているが、**`git checkout --` は追跡ファイルしか戻さない。** 月次振り返りは `docs/ai/memory/` 等へ**新規ファイルを作りうる**(allowlistは `Edit(docs/ai/memory/**)` で、`job-settings.json.j2:20-22` が「`Edit()` が全ファイル編集ツールを兼ねる」と記録している)。新規作成分は未追跡のまま残り、作業ツリーが汚れたままになる。**手順1の「開始前に清潔」は「戻せること」を担保していない** | U11 | 2026-07-28 ゲート適用時にCoordinatorが発見 | **未処理(記録のみ)。**U11は最後の単位であり、着手はかなり先。**U11の依頼文で後始末を明示することで解消する**(計画の差し戻しは行わない — 単位1つの手順の精度の問題であり、分解や工程の欠陥ではないため)。忘れると作業ツリーが汚れたまま8/26を迎えうる |
| **J-7** | **ansy側の `reports/incidents/_spool/` に、誰も消費しないspoolレコードが溜まる構造がある。** `notify.yml` は全通知で `capture.yml` をincludeし、`delegate_to: localhost` で**コントローラ側**にレコードを書く。ansyには収集器が居ない。requirement O10「ansy側にも `reports/incidents/` が実在する。由来は未確認」の**由来がこれである** | 本案件のscope外 | 2026-07-28 計画時に発見 | **未処理。**害は微小(`.json` はgitignore済みでgit状態に影響しない)。計画は同期の成功時に通知しない設計にして増加を抑えたが、**構造そのものは残る。Coordinatorへ報告済み** |

---

## 計画外事象

| 内容 | 他工程への波及 | 記録日 |
|---|---|---|
| **ADRの査読(層2相当)が、実装前に実行不能な決定を検出した。** 引用が全件正しくても、そこから組んだ決定が実行可能とは限らなかった | 波及した(OQ5の結論が保留)。査読が実装前に潰した実質的な欠陥として2件目 | 2026-07-28 |
| **Coordinatorの依頼文が `progress.md` を成果物に含めておらず、Tech Leadが役割規範と依頼文の境界の衝突に直面した。** Tech Leadは境界を優先し、衝突自体を報告した | 波及なし。Coordinatorが本ファイルを起こして解消。**Step 2の依頼文では成果物に含められた** | 2026-07-28 |
| **確定したはずの設計判断(ADR-005)が、上位の要求変更(IC-007 / D-2)で反転した。** 初版はA-2′(作業ツリー外)を推奨し査読も通っていたが、第2版はA-1+H(作業ツリー内)を採る。**引用も論証も誤っていなかった** — 前提が変わった | 波及した(ADR全面改訂)。**教訓の候補**: 案件スコープの正本(ADR)は、上位の要求が動くと結論ごと反転する。Policyを先に立てた判断はこの反転コストを下げる方向に働いた | 2026-07-28 |
| **課題J-6が、記録上は未処理のまま実際には解消していた。** 着手時に現物で確認して判明 | 波及なし。**他人の調査記録のfile:lineを鵜呑みにせず現物で再確認する**という作法が機能した実例 | 2026-07-28 |
| **計画査読が、実装前にCritical 1件を含む欠陥4件を検出した(J-8)。** うちF-1は本番の月次ジョブを無通知で殺す形であり、**実装後・実行後でなければ露見しにくい**(`--check` では2本目の `claude -p` が skip されるため回帰検証にも掛からない) | 波及した(計画を差し戻し・第2版へ)。**この案件フォルダで層2の査読が実質的欠陥を実装前に潰したのは3件目**(ADR初版 → 計画初版)。層1(数える)は3基準とも通過しており、**検出したのはすべて層2**である | 2026-07-28 |
| **査読の最中に上位正本が動いた(IC-033の追加)。** 査読者が `git status` の変化に気づき、影響を評価したうえで判定を維持した | 波及した(計画 §2.7・§7 OQ-E・§10-5へ反映)。**IC-033の前提条件2(評価はcatch-up型)は計画が既に満たしていたが、前提条件1(ミラーを削除しない)は将来の保持設計をYoshinobu判断側へ寄せる可能性がある** | 2026-07-28 |
