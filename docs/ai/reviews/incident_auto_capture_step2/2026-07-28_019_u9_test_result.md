# U9 — 評価工程の単独検証 test_result

- 実施: 2026-07-28 17:48〜17:56 JST / Tester(subagent)
- 契約: `docs/ai/reviews/incident_auto_capture_step2/2026-07-28_007_step2_plan.md` §4「U9」、§2.4〜§2.6・§2.8、§8(AC5・AC6・AC8)
- requirement: `2026-07-28_006_step2_requirement.md` §5 P0-4・P0-5、§6 AC5・AC6・AC8
- Policy: IC-017(生ログ転記禁止・参照のみ)、IC-018(許可の不在で書込を禁止)、IC-020・IC-032(昇格は人が行う)
- 前提: `2026-07-28_018_u8_test_result.md`(U8完了。ミラー41件、`ansible-incident-sync.timer` enabled/active、`ansible-knowledge-review.timer`は無変更)
- 承認: Coordinatorが`playbooks/incident_evaluation.yml`(手動専用)の実行を承認済み。**実際に`claude -p`を起動した**(Max枠を消費。API従量課金なし)。

## 0. 実施方法と安全境界の遵守

- **実行前に`git status --porcelain`が空であることを自分で確認した**(17:48:19 JST)。汚れていれば実施せず報告する予定だったが、空だったため着手した。
- 実行したのは`ansible-playbook playbooks/incident_evaluation.yml -v`のみ。`--check`は付けていない(この単位の目的が「`claude -p`を実際に起動して評価成果物を作ること」であり、Coordinatorの承認範囲どおり)。
- 対象は`localhost`/`connection: local`のみ。**quory・Proxmox・Sophos・UniFi・monnie・authyへは一切接続していない**(playbook自体もhosts: localhostのみで構成される)。
- `-e`によるオーバーライドは行っていない(既定値のまま実行)。`tester_mode`は本playbookが参照しない変数であり、本playbookは通知タスクを持たない(`grep`で確認済み)ため、通知誤爆のリスクはそもそも無い。
- `git add`/`git commit`/`git push`は実行していない。
- **harnessの安全機構によるブロックは発生しなかった。**
- identityの昇格(`sudo`等)は一切行っていない。
- `playbooks/knowledge_review.yml`も`ansible-knowledge-review.service`も一度も起動していない(§3で実行前後の状態を確認)。
- 評価成果物・バンドルの中身(生ログ・内部IP)は、本ファイルへ一切転記していない(§1・§2の記述はすべて「どのフィールドが存在したか」「件数」「grep結果の有無」であり、内容の引用はしていない)。

## 1. 実行記録(コマンドと生の観測結果)

### 1.1 実行コマンドと結果

```
$ date '+%Y-%m-%dT%H:%M:%S%:z'   # 実行直前
2026-07-28T17:48:19+09:00
$ git status --porcelain          # 実行直前(空)

$ /usr/bin/time -v ansible-playbook playbooks/incident_evaluation.yml -v
...
PLAY RECAP *********************************************************************
localhost                  : ok=40   changed=6    unreachable=0    failed=0    skipped=1    rescued=0    ignored=0

	Elapsed (wall clock) time (h:mm:ss or m:ss): 5:53.51
	Maximum resident set size (kbytes): 369900
```

**playbook全体のシェル終了コード: 0。** `failed=0`・`unreachable=0`。

### 1.2 `claude -p`本体の実測(U11の時間予算検証に直結)

`Run incident evaluation`タスクの登録結果(`ansible.builtin.command`の`register`)から、Ansibleが実測した値をそのまま転記する(生のstdout本文は転記しない):

| 項目 | 実測値 |
|---|---|
| `rc` | **0** |
| `start` | 2026-07-28 17:48:35.004992 |
| `end` | 2026-07-28 17:54:15.786151 |
| `delta`(実所要時間) | **0:05:40.781159**(340.78秒) |
| `knowledge_review_incident_timeout`(予算) | 900秒 |
| 予算に対する使用率 | 約37.9%(340.78 / 900) |
| `changed` | true(`changed_when: rc==0`どおり) |
| `stderr` | `Permission ask rule (managed policy settings): Write(.claude/settings.json) is not matched by file permission checks` の1行のみ |

**`stderr`の1行について**: これは`/etc/claude-code/managed-settings.json`(`permissions.ask`に`Write(.claude/settings.json)`を含む、システム全体の設定)に対する**CLI起動時の静的な書式警告**であり、`--setting-sources ""`により本評価セッションはこのファイルをそもそも読み込んでいない。**評価セッション内でWriteツールが実際に呼ばれたことを示す証跡ではない**(このstderrはコマンドの構成に依らず`claude`起動のたびに出る警告と判断する。根拠: 評価専用プロファイル`incident-job-settings.json`にはこの文字列を含む記述が無く、評価対象の`reports/incidents/**`にも該当しない、システムレベルの`managed-settings.json`のみに存在する文字列と一致した)。

playbook全体の実測時間(5:53.51 = 353.51秒)のうち、`claude -p`自体が340.78秒を占め、残り約13秒がmetrics算出・prompt/settings描画・成果物保存・index書き出し・`git status`収集にあたる。

### 1.3 rendered済み権限プロファイルの直接確認(AC5・IC-018)

テンプレートではなく、**実行時にAnsibleが実際にレンダリングし`claude -p`が読み込んだファイル自体**を確認した。

```
$ cat /home/yoshi/.local/state/knowledge-review/incident-job-settings.json
```

`permissions.allow`は`Read(docs/**)` / `Read(skills/**)` / `Read(reports/incidents/**)`の3件のみ。**`Edit`エントリは1つも無い。** `permissions.deny`は`Bash` / `WebFetch` / `WebSearch`のみ。

### 1.4 評価セッションが書込を行わなかったことの実行結果ベースの確認(AC5)

権限プロファイルの記述(§1.3)だけでなく、**実行結果**から書込が起きなかったことを確認した。

```
$ find reports/incidents/quory -maxdepth 2 -newermt '2026-07-28 17:48:00'
(出力なし)
$ ls reports/incidents/quory | grep -v '^_' | wc -l
41
```

- `reports/incidents/quory/`配下(ミラー本体。`claude -p`が読取許可を持つ唯一の対象領域)に、**実行開始時刻(17:48:00)より新しいファイルが1件も無い。** `claude -p`は17:48:35〜17:54:15の間動作しており、この間にミラー配下へ何か書かれていれば`-newermt`に検出されるはずだが、検出は0件だった。
- バンドル件数は実行前後で**41件のまま不変**(U8完了時点と同数)。
- `git status --porcelain`は実行前(空)・実行後(空)ともに変化なし(§1.5)。

**これは推論ではなく、ファイルシステムの実測に基づく直接証拠である。** 権限プロファイルに`Edit`が無いという「設定の記述」に加えて、「対象領域に実際に何も書かれなかった」という「実行結果」の両方から、書込が起きなかったことを確認した。

### 1.5 実行後の状態

```
$ ls -la reports/incidents/_evaluations/
-rw-r----- 1 yoshi yoshi 15150 Jul 28 17:54 2026-07-28-incident-evaluation.md
-rw-r----- 1 yoshi yoshi   637 Jul 28 17:54 2026-07-28-index.json

$ git status --porcelain
(出力なし)
```

`_evaluations/`配下の2ファイルは、mode `0640`・所有者`yoshi`で、いずれも**Ansibleの`copy`タスクが書いたもの**(`claude -p`終了後、17:54にタイムスタンプが揃う)。

### 1.6 index.jsonの内容(AC8。値のみ転記。生ログ・IPは含まれない)

```json
{
    "bundles_new_since_last_eval": 41,
    "evaluation_artifact": ".../reports/incidents/_evaluations/2026-07-28-incident-evaluation.md",
    "evaluation_rc": 0,
    "generated_at": "2026-07-28T17:48:25+09:00",
    "incidents_added_since_last_eval": 5,
    "mirror_bundle_total": 41,
    "mirror_heartbeat_age_seconds": 1404,
    "mirror_stale": false,
    "oldest_unreviewed_bundle": {
        "age_days": 0,
        "generated_at": "2026-07-27T19:42:35+09:00",
        "id": "semaphore-61"
    },
    "previous_evaluation_date": null,
    "schema_version": 1,
    "time_source": "summary.json:generated_at"
}
```

`previous_evaluation_date`が`null`(初回)、`bundles_new_since_last_eval`がミラー全件(41)であることは、計画I-3が定める初回の挙動どおり。`time_source`が`summary.json:generated_at`であることも確認した(mtimeベースではない)。

### 1.7 評価成果物(`_evaluations/2026-07-28-incident-evaluation.md`)の内容確認(IC-017)

成果物本文(148行)を読み、次を確認した(内容そのものはここへ転記しない):

- **生ログ(`*.log`)の内容が一言一句として転記されている箇所は無かった。** 個別バンドルへの言及はすべて「バンドルID」+「そのバンドルで何が起きたかの要約(評価者自身の言葉)」の形で、生の`stdout`/`stderr`行やJSONの生データ片は含まれていない。
- 参照は`reports/incidents/quory/<bundle_id>/summary.json`という**パス表現**で書かれており、生ログを開かず参照だけで済ませる指示(prompt「書いてはいけないこと」節)どおりの形になっている。
- **内部IPアドレスの実値は含まれていなかった**(`grep -nE '([0-9]{1,3}\.){3}[0-9]{1,3}'`で成果物ファイルを検査、マッチ0件)。
- ホスト名(pve1、pve2、monnie、quory等の**既公開**の名称)への言及はあるが、これはrequirement/Policyが禁じる「内部IP」ではなく、既存のIncident記録・Policy文書でも使われている既公開のホスト名の範囲内。
- プロンプト注入(バンドル内の指示文混入)についても評価本文自身が走査結果を報告しているが、**これはLLMの自己申告であり本単位で独立に再検証していない**(§5「観測できなかったこと」に明記)。

## 2. 受入条件ごとの合否

| AC | 判定 | 観測方法と根拠 |
|---|---|---|
| **AC5** | **合格** | ①`_evaluations/2026-07-28-incident-evaluation.md`が実在する(§1.5)。②`claude -p`はrc=0で正常終了(§1.2)。③**評価セッションが書込許可を持たないことを、レンダリング済み設定ファイルの直接確認(§1.3、`Edit`エントリ0件)と、実行結果(§1.4、ミラー配下に実行時間内で新規/更新ファイルが1件も無い)の両方から確認した。** 後者は推論ではなく`find -newermt`によるファイルシステムの直接観測である |
| **AC6** | **合格** | 評価完了後の`git status --porcelain`が空(§1.5)。評価成果物(`_evaluations/**`)は`.gitignore`のディレクトリ単位除外(`reports/incidents/`)により追跡対象に現れない。既存の書込allowlist配下(`docs/ai/memory/**`等)への差分も本単位では発生していない(本playbookは`knowledge_review`本体を経由しないため) |
| **AC8** | **合格** | `index.json`に`bundles_new_since_last_eval`(41)・`incidents_added_since_last_eval`(5)・`oldest_unreviewed_bundle.age_days`(0)がすべて存在し、値が計算されている(§1.6)。評価成果物本文にも「新規バンドル数」「既知条件由来と見られる件数」「滞留の有無」が件数・経過日数として明記されていることを確認した(§1.7、内容は転記しない)。**滞留の判定ロジック自体が機能していること**(「記録件数上は5件増えているが、いずれもこのバンドル群からの昇格ではない」という趣旨の指摘を評価本文が自ら行っていた)も確認した |
| **IC-017**(生ログ転記禁止・参照のみ) | **合格** | §1.7のとおり、成果物本文に生ログの逐語転記は見つからなかった。参照はバンドルIDとパス表現に留まっている。**「文章による規律であって境界ではない」という実装の申告どおり、構造的な強制ではなく規律(prompt)頼みだが、今回の1回の実行では実際に守られていた** |

**満たせなかった受入条件は無い。** 3条件+IC-017すべてで合格の観測が取れた。

## 3. 終了時のシステム状態(U11が前提とする状態)

- **`reports/incidents/_evaluations/`に2ファイルが新規作成された**(`2026-07-28-incident-evaluation.md`・`2026-07-28-index.json`、いずれもmode 0640)。**これは本単位が意図的に作った差分であり、この状態のままU11(月次フル経路の1回通し)へ引き継ぐ。** U11が実行すると`previous_evaluation_date`はこの2026-07-28になり、初回ではなく2回目の評価として動く。
- **`git status --porcelain`は空。**(`_evaluations/**`は`.gitignore`済みのため現れない)
- **`ansible-knowledge-review.timer`は enabled/active、次回発火 `2026-08-26 07:15 JST`(不変)。** `ansible-knowledge-review.service`は`inactive`/`dead`のまま、直近実行時刻`2026-07-27 09:23:03 JST`(U9開始前と同一)。**本単位は`playbooks/knowledge_review.yml`も`ansible-knowledge-review.service`も一度も起動していない。**
- **`ansible-incident-sync.timer`は enabled/active、次回発火 `2026-07-28 18:07:00 JST`(直近実行`17:10:49`、U8完了時と同一)。本単位はこのtimer・serviceに一切触れていない。**
- **`reports/incidents/quory/`は41バンドルのまま不変**(U8完了時と同数。§1.4で確認)。
- **`reports/incidents/_sync/last-sync.json`はU8完了時点の内容(`result: ok`・`transferred_files: 5`・`mirror_bundle_count: 41`)のまま不変**(本単位は同期を一切実行していない)。
- **MEMORY.md「次回のKnowledge月次振り返り」行は`2026-08-26`のまま不変。**
- quory・Proxmox・Sophos・UniFi・monnie・authyには一切接続していない(無変更)。
- identityは昇格していない(無変更)。

## 4. 自己検証(依頼文の要求項目)

- **割り当てられた受入条件それぞれについて、何を観測して合格と判断したか**: §2の表に、各条件の観測方法(ファイル実在、レンダリング済み設定の直接確認、`find -newermt`、`git status`、index.jsonの各フィールド値、成果物本文のgrep結果)を記載した。
- **評価セッションが書込許可を持たないことを、権限プロファイルの記述ではなく実行結果から確かめられたか**: **確かめた。** §1.3(レンダリング済み設定に`Edit`エントリ0件)は設定の記述の確認だが、**§1.4(`find -newermt`でミラー配下に実行時間内の新規/更新ファイルが0件、バンドル件数不変)が実行結果からの確認にあたる**。両方を分けて記録した。
- **`claude -p`の実所要時間と終了コードを記録したか**: **記録した。** §1.2に`rc=0`・`start`/`end`/`delta`(340.78秒)・予算(900秒)に対する使用率(約37.9%)を明記した。playbook全体の所要時間(353.51秒)も併記した。
- **満たせなかった条件、観測できなかったことを、成功と混ぜずに分けて書けているか**: 割り当てられた3条件+IC-017はすべて合格であり、失敗と混ぜていない。観測できなかったことは§5に分離して記載する。

## 5. 観測できなかったこと・残存リスク

- **プロンプト注入対策(F13と同型の注入対策)が実際に機能するかは、本単位では独立に再検証していない。** 評価成果物自身が「異常な記述: なし」と自己申告しているが、これはLLMの自己申告であり、意図的に指示文を混入させたバンドルを用意して耐性を試すテストは実施していない(スコープ外。IC-017の観測が本単位の割当であり、注入耐性のテストはP0-4の範囲でもAC5〜AC8の範囲でもない)。
- **`stderr`の1行(`Write(.claude/settings.json)`警告)が評価セッション内の実際のツール呼び出しに由来しないことは、状況証拠(該当文字列がシステムレベルの`managed-settings.json`にのみ存在し、`--setting-sources ""`で読み込まれないはずである)からの判断であり、`claude -p`自体の完全なツール呼び出しトレースを取得して確認したわけではない**(`--output-format text`は最終結果のみを返し、中間のツール呼び出しログを含まない)。**§1.4のファイルシステム実測(ミラー配下に新規/更新ファイル0件)が、この判断を独立に補強する直接証拠になっている**が、完全なトレースでの否定ではないことを明記する。
- **既知条件(pve1の平日運用)の判定基準そのものの妥当性は、本単位の評価対象ではない**(P1-1として要件が初回は決め切らないことを許容している)。評価成果物がこの点について独自の分析(既知条件と見られる件数、既存記述との不整合の指摘)を行っていたが、その分析内容の当否を検証することは本単位のスコープ外である。
- **`incidents_added_since_last_eval`の指標設計(記録件数がバンドル由来かどうかを区別しない)について、評価成果物自身が指標の限界を指摘していた。** これは実装の欠陥ではなく計画§2.6が最初から認めている設計上の粗さ(「粗いが決定論的」)であり、本単位はこの指標が**計算され報告されること**を確認する担当であって、指標の精度そのものの改善は本案件のスコープ外(§10申し送り事項の対象になりうる)。
- **U9はU11(月次フル経路の1回通し)の前提となる状態を作った。** `_evaluations/`に初回評価(2026-07-28分)がすでに存在するため、**U11が実行すると2回目の評価としてcatch-up型の窓計算が働く**(`previous_evaluation_date`が`null`でなく`2026-07-28`になる)。U11の担当者はこれを開始前提として認識すること。

## 6. 変更したファイル

- `reports/incidents/_evaluations/2026-07-28-incident-evaluation.md`(新規。`.gitignore`済み。Ansibleの`copy`タスクが作成)
- `reports/incidents/_evaluations/2026-07-28-index.json`(新規。同上)
- 本ファイル(`docs/ai/reviews/incident_auto_capture_step2/2026-07-28_019_u9_test_result.md`)

**上記以外のリポジトリ追跡対象は変更していない**(`git status --porcelain`は実行前後とも空)。`roles/`・`playbooks/`・`.gitignore`・Policy本文・`docs/ai/core.md`には一切触れていない。`git add`/`git commit`/`git push`は実行していない。
