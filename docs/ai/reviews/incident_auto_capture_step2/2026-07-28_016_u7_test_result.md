# U7 — 転送の配備と正常系 test_result

- 実施: 2026-07-28 17:07〜17:12 JST / Tester(subagent)
- 契約: `docs/ai/reviews/incident_auto_capture_step2/2026-07-28_007_step2_plan.md` §4「U7」、§8(AC1・AC2・P0-3・P0-7)
- 前提: `2026-07-28_010_u1_implement.md`(同期本体)、`2026-07-28_012_u2_implement.md`(配備)、`2026-07-28_014_u5_review.md`(独立レビュー)。commit `33fee67`(W1完了済み、Coordinator確認済み)
- 承認: Coordinatorがこの依頼文で配備(systemd timer/serviceの有効化)を承認済み

## 0. 実施方法と安全境界の遵守

- 実行ホストはansy自身(`hostname`実測: `ansy`、`whoami`: `yoshi`)。ansible接続はinventory設定どおり`ann`鍵経由で`ansy.internal`(dev_nodes)・`quory.internal`(control_nodes)の両方へ。
- quoryへは**読み取りのみ**行った。実行したコマンドは `ssh ann@quory 'ls ...'`、`find ... -name summary.json | xargs stat`、`md5sum`によるmtime突合の3種のみ。quory上でファイル作成・変更・削除、systemd unit操作、git設定変更は一切行っていない。
- ansible実行を通じた転送自体は`ansible.posix.synchronize(mode: pull)`であり、quory側は読み取られる側(source)。`delete`/`--remove-source-files`はコード上も存在しない(U1/U2実装記録・U5レビューで既に確認済み、本ラウンドでも再確認していない)。
- ansy・quory以外のホスト(pve1/pve2/sophos-fw/UniFi/monnie/authy)へは一切接続していない。
- identityは昇格させていない場面と、承認済みで昇格させた場面がある:
  - `playbooks/incident_sync_timer.yml`実行はrole内タスクが`become: true`を使う(計画・実装どおりの設計。risk-accepted、Coordinator承認済み)。
  - それ以外(`systemctl start ansible-incident-sync.service`の起動そのもの、git status、Read/Grep)は`sudo`を使ったのは`systemctl start`のみ(理由は下記)。`ansible-playbook`実行自体はyoshiのまま(`become: false`固定、`playbooks/incident_sync.yml`のplayレベル設定どおり)。
  - `sudo systemctl start ansible-incident-sync.service` / `sudo systemctl show ...`はsystemd unit操作(状態変更を伴う起動)であり、通常ユーザーでは実行できないため`sudo`を使った。これは配備した本番unitを設計どおりの経路(`docs/ai/policies`が前例とする`systemctl start`方式、`knowledge_review_timer.yml`のコメントに明記された手動実行手順と同型)で1回動かしただけであり、Proxmox/Sophos/UniFiへの操作ではない。
- 確認プロンプトへの代理応答は発生していない(`sudo -n true`が事前にpasswordlessであることを確認済みで、対話プロンプトは出ていない)。
- harnessの安全機構によるブロックは発生しなかった。
- `git add`/`git commit`/`git push`は実行していない。
- **成果物以外でリポジトリ内が変更された箇所は無い**(§6で確認)。

### 実行前提の現物確認(依頼文の要求どおり)

指示文に実行前提の明記は無かったが、着手前に以下を自分で確認した(推測で進めなかった)。

| 確認事項 | 実測 |
|---|---|
| quoryの到達性 | `ssh ann@quory 'echo ok; hostname'` → `ok` / `quory` |
| ansyの到達性(dev_nodes接続) | `ssh -i ~/.ssh/id_ann ann@ansy.internal 'whoami; sudo -n true'` → `ann` / `sudo-ok` |
| rsyncの存在 | `command -v rsync` → `/usr/bin/rsync`(ansy側。quory側はU0で既確認、本ラウンドは再確認していない) |
| `ansible-incident-sync.timer`が未配備であること | `systemctl list-timers 'ansible-*'` → `ansible-knowledge-review.timer`のみ表示、incident-sync系は存在せず |
| ansy側`reports/incidents/`の現況 | `ls -la` → `_spool/`のみ存在(F8どおり、ミラー`quory/`は無い) |
| commit状態 | `git log --oneline -1` → `33fee67`(`make review OK`)、`git status`→ clean |
| `# tester-gate:`マーカー | `incident_sync.yml`: `check-mode-native`、`incident_sync_timer.yml`: `risk-accepted`(grep実測。§本文2章参照) |

Coordinatorの依頼文にある実行前提と矛盾する事実は見つからなかった。

## 1. 実行記録(コマンドと生の観測結果)

### 1.1 静的検証

```
$ ansible-playbook playbooks/incident_sync_timer.yml --syntax-check
playbook: playbooks/incident_sync_timer.yml   (rc=0)

$ ansible-playbook playbooks/incident_sync.yml --syntax-check
playbook: playbooks/incident_sync.yml   (rc=0)
```

### 1.2 配備(`playbooks/incident_sync_timer.yml`、risk-accepted・本実行)

```
$ ansible-playbook playbooks/incident_sync_timer.yml
PLAY RECAP: ansy  ok=7  changed=3  unreachable=0  failed=0  skipped=0  rescued=0  ignored=0
(shell rc=0)
```

配備直後の確認:

```
$ systemctl is-enabled ansible-incident-sync.timer   → enabled
$ systemctl is-active  ansible-incident-sync.timer   → active
$ systemctl show ansible-incident-sync.timer -p Persistent   → Persistent=yes
$ systemctl list-timers ansible-incident-sync.timer --all --no-pager
NEXT: Tue 2026-07-28 18:07:00 JST (56min後)  UNIT: ansible-incident-sync.timer  ACTIVATES: ansible-incident-sync.service
```

### 1.3 転送1周目(手動起動、`systemctl start`。実転送)

配備前(サービス起動前)の状態:

```
$ git status --porcelain -- reports/incidents/   → (空、rc=0)
$ ls reports/incidents/                          → _spool のみ
```

起動と同時に、転送の**最中**をscoped `git status`で0.02秒間隔・最大400回(8秒分)ポーリングしながら`systemctl start`を実行:

```
$ sudo systemctl start ansible-incident-sync.service &
$ for i in 1..400: git status --porcelain -- reports/incidents/ ; sleep 0.02; done
→ poll-lines=0(非空を1回も観測しなかった)
$ (service-start-rc=0)
```

サービス完了後:

```
$ systemctl show ansible-incident-sync.service -p ExecMainStatus -p Result
ExecMainStatus=0
Result=success

$ cat reports/incidents/_sync/last-sync.json
{
  "schema_version": 1,
  "result": "ok",
  "source_host": "quory",
  "mechanism": "rsync",
  "transferred_files": 193,
  "mirror_bundle_count": 41,
  "mirror_heartbeat_generated_at": "2026-07-28T17:10:01+09:00",
  "mirror_heartbeat_age_seconds": 70,
  "mirror_stale": false,
  "errors": [],
  "started_at": "2026-07-28T17:11:07+09:00",
  "finished_at": "2026-07-28T17:11:10+09:00"
}

$ git status --porcelain -- reports/incidents/   → (空、rc=0)
$ git status --porcelain                          → (空、rc=0。リポジトリ全体も清潔)
$ ls reports/incidents/quory/ | grep -v '^_' | wc -l   → 41
$ ls reports/incidents/quory/_runs/ | wc -l            → 41
$ find reports/incidents/quory -name "*.tmp" -o -name ".~tmp~*"   → 0件(残存なし)
```

journalctlでplay recapを確認: `localhost ok=22 changed=2 ... failed=0 skipped=6`(Slack通知2件は`skipping`= 成功時は通知しないI-6どおり)、`quory ok=7 changed=2 ... failed=0`。

### 1.4 転送2周目(冪等性、AC2)

1周目実行前にquory側のsource情報を記録:

```
$ ssh ann@quory 'find .../reports/incidents/ -maxdepth 2 -name summary.json | xargs stat -c "%Y %n" | sort | md5sum'
→ 7c822d2a1fb691fc539b8ed57f7c5cd2
```

2周目起動(同様にscoped git statusを0.02秒間隔でポーリングしながら):

```
$ sudo systemctl start ansible-incident-sync.service &
→ poll-lines=0(非空を1回も観測しなかった)
→ service-start-rc=0
```

完了後:

```
$ systemctl show ansible-incident-sync.service -p ExecMainStatus -p Result
ExecMainStatus=0
Result=success

$ cat reports/incidents/_sync/last-sync.json
{ ..., "result": "ok", "transferred_files": 0, "mirror_bundle_count": 41, "errors": [], ... }

$ ssh ann@quory 'find .../reports/incidents/ -maxdepth 2 -name summary.json | xargs stat -c "%Y %n" | sort | md5sum'
→ 7c822d2a1fb691fc539b8ed57f7c5cd2   (1周目と完全一致 = quory側は無変更)

$ git status --porcelain -- reports/incidents/   → (空、rc=0)
```

### 1.5 P0-7(対話セッションからの可読性)

```
Read(reports/incidents/quory/semaphore-114/summary.json) → 正常に読めた(JSON全文表示)
```

内容に内部IPアドレスが含まれていたため、本記録・報告へは転記しない(`docs/ai/core.md`「内部IPアドレスをリポジトリ内へ直接記載しない」)。`bundle_id`・`generated_at`・構造(`semaphore`/`snapshot`/`collection_errors`キー)が期待どおりであることのみ確認した。

### 1.6 `.gitignore`実効性のピンポイント確認

```
$ git check-ignore -v reports/incidents/quory/semaphore-114/summary.json
→ .gitignore:5:reports/incidents/  (一致)
$ git check-ignore -v reports/incidents/_sync/last-sync.json
→ .gitignore:5:reports/incidents/  (一致)
```

## 2. 受入条件ごとの合否

| AC/P0 | 判定 | 観測値による根拠 |
|---|---|---|
| **AC1** | **合格** | 1周目実行後、ミラー(`reports/incidents/quory/`)にquoryの全41バンドルが存在(`ls`実測)。`git status --porcelain -- reports/incidents/`が空(rc=0、§1.3)。**あわせて転送の最中**(`systemctl start`実行中の0.02秒間隔×400回ポーリング)も非空を1回も観測しなかった(§1.3)。playbookの終了コードは`ExecMainStatus=0`・`Result=success`(systemd unit側の実測値) |
| **AC2** | **合格** | 2周目`transferred_files: 0`(rsyncが差分無しと判定)。quory側source `summary.json`群のmtimeのmd5集約値が1周目・2周目で完全一致(`7c822d2a...`)= 元ファイルは無変更。終了コードは`ExecMainStatus=0`・`Result=success`(§1.4) |
| **P0-3** | **合格** | 1周目・2周目とも、転送**中**(ポーリング400回、計8秒分)・転送**後**のいずれも scoped `git status --porcelain -- reports/incidents/` が非空を1回も出さなかった。リポジトリ全体の`git status --porcelain`も転送後は空(§1.3)。rsyncの一時ファイル痕跡(`*.tmp`・`.~tmp~*`)も転送後に0件(§1.3)。`git check-ignore -v`で着地物・状態ファイルとも新設の1行(`.gitignore:5`)に一致することを実測確認(§1.6) |
| **P0-7** | **合格** | `Read`ツールでミラー内の実バンドル(`semaphore-114/summary.json`)を対話セッションから直接読めた(§1.5)。追加実装なしで成立するという計画の想定どおり |

**満たせなかった受入条件は無い。** U7に割り当てられた4条件すべてで合格の観測が取れた。

## 3. 終了時のシステム状態(U8が前提とする状態)

- **`ansible-incident-sync.timer`は有効(enabled)かつ稼働中(active)。** 次回発火は本記録作成時点で**約56分後**(`18:07:00 JST`、`Persistent=yes`)。以降は毎時7分に自動発火する。
- **`ansible-incident-sync.service`は直近2回とも成功(`Result=success`・`ExecMainStatus=0`)で終了し、現在は`inactive`(`dead`、次回発火待ち)。**
- **`reports/incidents/quory/`に本番quoryの実バンドル41件(と`_runs/`41件・`_heartbeat.json`)が実際にミラーされている。** 内容は本番の実データ(2026-06〜07月の障害捕捉。既知条件由来のものを含む、requirement O12どおり)。
- **`reports/incidents/_sync/last-sync.json`が存在し、直近の同期結果(`result: ok`・`transferred_files: 0`・`errors: []`)を反映している。**
- **ansyの作業ツリーは`reports/incidents/`を含め清潔(`git status --porcelain`が空)。** `.gitignore`のディレクトリ単位除外により、ミラー・状態ファイルとも追跡対象に見えない。
- **quoryは無変更。** 読み取りのみが行われ、書込・削除・unit操作・git設定変更は一切発生していない(mtime突合で確認済み)。
- **`roles/knowledge_review`側(評価工程・`ansible-knowledge-review.timer`)には一切触れていない。** 既存の月次timer(次回`2026-08-26 07:15 JST`)は本ラウンドの影響を受けていない。
- U9・U10・U11(評価工程の検証、`knowledge_review`unit再配備、月次フル経路の1回通し)は未着手。U8(異常系)も未着手。
- タイマーは今後も毎時自動的に本物のquoryへ接続し実転送を行う。**次回発火(18:07)以降も無人で継続する** — これはU7が意図的に到達させた状態であり、後続のU8着手前に再度手動起動を挟む必要は無い(自然に周期が回る)。

## 4. 自己検証(依頼文の要求項目)

- **受入条件それぞれについて観測値を書いたか**: §2の表に、各条件の「何を測って合格と判断したか」を実測値(バンドル件数、md5集約値、poll-lines数、rc値)で記載した。「動いた」という記述のみに頼った箇所はない。
- **終了コードを記録したか**: playbook(`ansible-playbook ... --syntax-check`のrc、`systemctl start`のシェル終了コード)、systemd unit(`ExecMainStatus`・`Result`)の両方を実測して§1に記録した。
- **quoryの状態を変えていないこと**: mtime集約値(md5)を1周目前後・2周目前後で突合し、完全一致を確認した(§1.4)。quoryへの接続はすべて`ssh ... 'ls'/'find'/'stat'`の読み取りコマンドのみで、書込系コマンドは実行していない。
- **満たせなかった条件・観測できなかったことを分けて書けているか**: 下記§5「観測できなかったこと」を参照。本ラウンドで割り当てられた4条件はすべて合格であり、失敗と混ぜていない。

## 5. 観測できなかったこと・残存リスク

- **quory側の`git status`は確認していない。** requirement O11のとおり、Tester接続identity(`ann`)と所有者(`yoshi`)の相違により`dubious ownership`でquory側の作業ツリー状態を直接見る手段が無い(承認範囲外の`safe.directory`設定を要するため)。ただし本単位は「quoryが変わっていないこと」をmtime突合という別の手段で確認しており、この制約が受入条件の判定を妨げてはいない。
- **転送機構の「遅い失敗」(SSHがblackholeして無応答になるケース)は本ラウンドでは作っていない。** これはAC4(U8の担当)に属する検証であり、U7の範囲外として意図的に実施していない。
- **3周目以降・長期運用時の挙動(heartbeat staleの実発火、複数時間にわたる連続運転)は未観測。** U7は正常系1〜2周目のみを担当しており、この先は自然な周期進行(次回18:07)またはU8以降の検証に委ねる。
- **`.gitignore`除外が壊れた場合の検出(§2.3の「検出」側、finalize.ymlのscoped git statusによる`fail`化)は実際には発火していない。** 今回2回とも除外は正しく機能したため、この経路(除外が壊れたときに同期自身が失敗する経路)は実発火では確認できていない。静的なコードレビュー(U5・U6)でのみ確認済み。
- **残存リスク(設計として許容済み。再掲ではなく本ラウンドで動作を変えていないことの確認)**: R-11(月次評価実行中はincident_syncが`-E 0`で静かにスキップする)は本ラウンドでは発火条件(月次評価との同時実行)が無かったため未観測。R-4(timerのdisable/mask時の検知死角)も本ラウンドの観測対象外。

## 6. 変更したファイル

**なし。** 本単位はコードへの変更を一切行っていない(実装は既にHEAD `33fee67`にcommit済み)。ansy上のsystemd unit配備(`/etc/systemd/system/ansible-incident-sync.{service,timer}`)と`reports/incidents/quory/`・`reports/incidents/_sync/`への書込は、リポジトリ管理下ではなく(`.gitignore`済み)、`git status --porcelain`が示すとおりリポジトリの追跡状態に影響していない。本ファイル自体が本単位の唯一の新規リポジトリ内成果物である。
