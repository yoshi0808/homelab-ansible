# U11 — 月次フル経路の1回通しとAC9回帰 test_result

- 実施: 2026-07-28 18:09〜18:31 JST / Tester(subagent)
- 契約: `docs/ai/reviews/incident_auto_capture_step2/2026-07-28_007_step2_plan.md` §4「U7 / U10 / U8 / U9 / U11(Tester)」の**U11 5ステップ**、§2.8
- 計画査読: `2026-07-28_008_plan_review.md` **F-7**(この単位が新設された理由。`--check`では2回目の`claude -p`がskipされ実経路を一度も通らないという指摘)
- requirement: `2026-07-28_006_step2_requirement.md` **AC9**
- Policy: `docs/ai/policies/incident_capture_policy.md` **IC-025**(作業ツリーが汚れているとき月次評価を実行しない)・**IC-033**(中止時の再実行はYoshinobuが行う。自動化は再実行しない)
- 前提: `2026-07-28_019_u9_test_result.md`(U9完了。`_evaluations/`に2026-07-28分の評価成果物が既に存在。この単位はしたがって**同日2回目**の評価になる)
- 承認: Coordinatorが本依頼文で、計画§4固定の5ステップに沿って月次playbook全体を1回だけ通すことを承認済み。**`claude -p`が2本走った**(ansyのMax枠を消費。API従量課金なし)。

## 0. 実施方法と安全境界の遵守

- 実行ホストはansy自身(作業ツリー内、`connection: local`)。**quory・Proxmox・Sophos・UniFi・monnie・authyへは一切接続していない。**
- 実行したのは `ansible-playbook playbooks/knowledge_review.yml -e tester_mode=true -e knowledge_review_automemory_dir=<scratch複製>` のみ。**`systemctl start ansible-knowledge-review.service` は一度も実行していない**(§3で実行前後の`ExecMainStartTimestamp`が`2026-07-27 09:23:03 JST`のまま不変であることを確認)。
- `tester_mode=true` によりSlack通知は実際には送られなかった(§1.4で実測)。
- **本番の`~/.claude/projects/-home-yoshi-homelab-ansible/memory`(auto-memory)には一切書き込んでいない。** 実行前に同ディレクトリをスクラッチ(`/tmp/claude-1000/.../scratchpad/u11_work/automemory_copy`)へ`cp -a`で複製し、**複製後に`diff -rq`で完全一致を確認してから**、`-e knowledge_review_automemory_dir=<複製先>`で差し替えて実行した。本番側`MEMORY.md`の期日行は実行前後で`> **⏰ 次回のKnowledge月次振り返り: 2026-08-26**(前回: 2026-07-27)`のまま不変(§1.2で実測比較)。
- `git add` / `git commit` / `git push` は実行していない。
- identityの昇格(`sudo`等)は行っていない。
- 実行後、振り返り本体が作った差分(追跡4件・未追跡分含め計6項目)をすべて列挙したうえで元に戻した(§1.5・§4)。自分が作ったスクラッチ(`u11_work`一式)も削除した。

### harnessの制約と対処(手順から外れていない)

- 最初の2回の試行は、Bashツールの既定タイムアウト(2分)で`ansible-playbook`本体ごと`SIGTERM`により強制終了された。**いずれも「Run monthly knowledge review」タスク(1本目の`claude -p`)が開始する前後で切れており、`claude -p`のオーファンプロセスは残らなかった**(`pgrep`で確認、`report-2026-07-28.md`が生成されていないことでも裏付け)。2回とも作業ツリーは清潔なまま(`git status --porcelain`が空)だったため、手順1のやり直しから再開した。
- 3回目は`run_in_background: true`を付けて再実行し、正常に完走した(exit code 0)。**別の形で結果に到達したのではなく、同じコマンドを同じ手順で、ツール呼び出し方法だけ変えて再試行した。** harnessのブロックは安全機構(permission classifier等)によるものではなく、ツール呼び出し側のタイムアウト設定であり、本文冒頭の「安全機構がブロックしたとき」の対象事象ではないと判断した。

## 1. 実行記録(コマンドと生の観測結果)

### 1.1 手順1 — 開始前確認

```
$ git status --porcelain   # 18:09:19 JST
(出力なし)
```

空であることを確認して着手した。

### 1.2 事前の複製差し替え確認

```
$ cp -a /home/yoshi/.claude/projects/-home-yoshi-homelab-ansible/memory <scratch>/automemory_copy
$ diff -rq /home/yoshi/.claude/projects/-home-yoshi-homelab-ansible/memory <scratch>/automemory_copy
COPY_IDENTICAL(差分なし)
$ grep "次回のKnowledge月次振り返り" <scratch>/automemory_copy/MEMORY.md
> **⏰ 次回のKnowledge月次振り返り: 2026-08-26**(前回: 2026-07-27)
```

### 1.3 手順2 — 本実行

```
$ ansible-playbook playbooks/knowledge_review.yml \
    -e tester_mode=true \
    -e knowledge_review_automemory_dir=<scratch>/automemory_copy \
    -vvv
=== START 2026-07-28T18:16:43+09:00 ===
...
PLAY RECAP: localhost  ok=40 changed=6 unreachable=0 failed=0 skipped=1 rescued=0 ignored=0
Elapsed (wall clock) time (h:mm:ss or m:ss): 10:07.33
RC=0
=== END 2026-07-28T18:26:50+09:00 ===
```

**playbook全体のシェル終了コード: 0。** `failed=0`・`unreachable=0`。

### 1.4 手順3 — 2本の`claude -p`の実所要時間(時間予算検証、AC9の核)

`ansible.builtin.command`の`register`結果(`start`/`end`/`delta`/`rc`)をそのまま転記する。生のstdout本文(振り返り・評価の内容)は転記しない。

| タスク | rc | start | end | delta(実所要時間) | タスク側timeout | 使用率 |
|---|---|---|---|---|---|---|
| **1本目**「Run monthly knowledge review」(振り返り本体) | **0** | 2026-07-28 18:16:46.278994 | 2026-07-28 18:23:42.566061 | **0:06:56.287067 = 416.29秒** | 1800秒(`knowledge_review_timeout`) | 約23.1% |
| **2本目**「Run incident evaluation」(障害の自動評価) | **0** | 2026-07-28 18:23:53.118230 | 2026-07-28 18:26:48.533687 | **0:02:55.415457 = 175.42秒** | 900秒(`knowledge_review_incident_timeout`) | 約19.5% |

- **2本合計: 591.70秒**。unitの`TimeoutStartSec`実効値(U10確認済み `3600秒`)に対して**約16.4%**。
- playbook全体の実測(`10:07.33` = 607.33秒)のうち`claude -p`2本が591.70秒を占め、残り約15.6秒がbundle読取・metrics算出・index書き出し・`git status`収集・通知組み立てにあたる。
- U9が単独実行で計測した評価側`claude -p`の所要時間(340.78秒)より本実行の2本目(175.42秒)は短い。**これは欠陥ではない** — U9実行時は`previous_evaluation_date`がnull(初回、全41バンドルを評価対象)だったのに対し、本実行は同日2回目でcatch-up窓が3バンドルに絞られたため(§1.6)、評価対象データ量が異なる。時間の差は指標の意味の違いから予測できる範囲内。

### 1.5 手順4 — 振り返りが生成したものの一覧(戻す前に記録)

`git status --porcelain`(実行後、戻す前):

```
 M docs/ai/memory/lessons/distinguish-nothing-found-from-not-run.md
 M docs/ai/memory/lessons/verify-the-outside-of-a-claimed-boundary.md
?? docs/ai/memory/lessons/a-checker-is-only-as-independent-as-its-input.md
?? docs/ai/memory/lessons/read-case-records-oldest-first.md
?? docs/ai/memory/temporary/proposal-2026-07-28-record-omitted-plan-gates.md
?? skills/gate-verification/
```

6項目(通知本文の「作業ツリーの変更: 6 ファイル」と一致)。内訳:

| # | 種別 | パス | 内容(要約のみ、本文は転記しない) |
|---|---|---|---|
| 1 | 追跡・変更 | `docs/ai/memory/lessons/distinguish-nothing-found-from-not-run.md` | 「関連」節に新規Lesson2件への相互参照を追加(2行差分) |
| 2 | 追跡・変更 | `docs/ai/memory/lessons/verify-the-outside-of-a-claimed-boundary.md` | 末尾に新規skillへの相互参照を1段落追加(2行差分) |
| 3 | 未追跡・新規 | `docs/ai/memory/lessons/a-checker-is-only-as-independent-as-its-input.md` | 新規Lesson1件 |
| 4 | 未追跡・新規 | `docs/ai/memory/lessons/read-case-records-oldest-first.md` | 新規Lesson1件 |
| 5 | 未追跡・新規 | `docs/ai/memory/temporary/proposal-2026-07-28-record-omitted-plan-gates.md` | 昇格提案の草稿1件(Policy本文は書き換えていない) |
| 6 | 未追跡・新規ディレクトリ | `skills/gate-verification/SKILL.md` | 新規Skill1件(既存4 Lessonをチェックリスト化したもの) |

**すべて依頼文が指定した書込allowlist(`docs/ai/memory/`・`docs/ai/context/`・`skills/`)配下。** allowlist外(`roles/`・`playbooks/`・Policy本文・`docs/ai/core.md`・`.claude/`等)への書込は無かった(`git status --porcelain`にそれらの行が出ていないことで確認)。

依頼文が指摘したとおり、**4番目・5番目・6番目は`git checkout --`では戻せない未追跡ファイル/ディレクトリだった。** `Edit`ツールが全ファイル編集ツールを兼ねる(job-settings.json.j2のコメントどおり)ため、既存ファイルの追記だけでなく新規ファイル作成もallowlist配下では起こりうることが、今回実際に観測された。

### 1.6 評価成果物(2回目の評価としての挙動、AC9とは別軸の確認)

```
$ cat reports/incidents/_evaluations/2026-07-28-index.json
{
    "bundles_new_since_last_eval": 3,
    "generated_at": "2026-07-28T18:16:45+09:00",
    "incidents_added_since_last_eval": 0,
    "mirror_bundle_total": 41,
    "previous_evaluation_date": "2026-07-28",
    "time_source": "summary.json:generated_at"
}
```

- `previous_evaluation_date`が`"2026-07-28"`(U9実行時の日付)になっている。**依頼文が予告したとおり**、U9が同日に評価済みのため「初回」ではなく「同日2回目」の挙動になった。
- `bundles_new_since_last_eval`が3件(U9実行時の41件から縮小)。**期待どおりの縮小であり、欠陥ではない** — 同日中に生成された新規バンドルだけがcatch-up対象になる、実装が選んだ「取りこぼさない側へ倒す」包含判定の結果。
- `mirror_bundle_total`は41で不変(U8・U9完了時点と同数。`ansible-incident-sync.timer`が実行中に1回発火した形跡があるが`transferred_files: 1`のみで新規バンドル追加は無かった)。
- **`_evaluations/2026-07-28-*.md`・`*.json`はU9が作った同名ファイルを上書きした。** これは依頼文が明示した既知の受容事項(「同日の評価成果物は上書きされる」)であり、`.gitignore`済みのため`git status`には現れない。

### 1.7 通知の抑止(tester_mode)の実測確認

```
TASK [[tester_mode] Show notification instead of sending]
ok: [localhost] => {
    "msg": [
        "[tester_mode] 通知スキップ（tester_mode/skip_notifications=true）",
        "channel : info", "status  : info",
        "title   : [knowledge_review] 月次Knowledge振り返り CHANGES",
        ...
    ]
}
TASK [Load Slack variables] → skipping
TASK [Set Slack color and icon] → skipping (false_condition: not (tester_mode ... ))
TASK [Set Slack webhook URL] → skipping
TASK [Extract token from webhook URL] → skipping
TASK [Send Slack notification] → skipping
```

**実際のSlack送信タスク(`Send Slack notification`)は`skipping`だった。** `tester_mode`によりwebhook解決・token抽出・送信のすべてが`skip`され、通知本文の内容だけが`debug`で表示された。実際にSlackへは何も飛んでいない。

### 1.8 実行後の`git status`と復元

```
$ git checkout -- docs/ai/memory/lessons/distinguish-nothing-found-from-not-run.md \
                   docs/ai/memory/lessons/verify-the-outside-of-a-claimed-boundary.md
$ rm -f docs/ai/memory/lessons/a-checker-is-only-as-independent-as-its-input.md \
         docs/ai/memory/lessons/read-case-records-oldest-first.md \
         docs/ai/memory/temporary/proposal-2026-07-28-record-omitted-plan-gates.md
$ rm -rf skills/gate-verification/
$ git status --porcelain
(出力なし)
```

**手順1で開始前の空を確認済みのため、この復元で人の作業が失われることはない。**

## 2. 受入条件・確認事項ごとの合否

| # | 項目 | 判定 | 根拠 |
|---|---|---|---|
| **AC9**(既存の月次動作が変わっていないこと) | 中止条件(IC-025)・auto-memory仕分けの流れ・期日更新の仕組みが、追加された評価工程と無関係に従来どおり動いた | **合格** | §1.3(`rc=0`・`failed=0`)、§1.5(6ファイルの変更内容がすべて「既存Lessonへの相互参照追加+新規Lesson/Skill」という通常の月次振り返りの成果物の形であり、評価工程が振り返り本体の判断へ介入した形跡が無い)、§1.7(通知フォーマットに障害評価セクションが追加された以外、既存の`CHANGES`/`件数`表示は従来どおり) |
| 実経路が一度も通らない(F-7の欠陥)が解消されたか | 2本の`claude -p`が実際に起動しrc=0で完了した | **合格** | §1.4。`--check`では2本目がskipされるため、この単位が無ければF-1級の欠陥が実行前に露見しないという査読の懸念どおり、**今回`--check`を使わず実経路を通したことで確認が成立した** |
| 2本の`claude -p`が時間予算(3600秒)に収まるか | 合計591.70秒(約16.4%) | **合格** | §1.4。個別timeout(1800秒・900秒)に対してもそれぞれ23.1%・19.5%で十分な余裕がある |
| 本番のauto-memoryが無傷であること | `MEMORY.md`の期日行が実行前後で`2026-08-26（前回: 2026-07-27）`のまま不変 | **合格** | §0・§1.2。複製側は`前回: 2026-07-28`に正しく更新されており、差し替えが機能していたことも確認済み |
| `systemctl start`を使っていないこと | 使用していない。`ansible-knowledge-review.service`の`ExecMainStartTimestamp`が実行前後とも`2026-07-27 09:23:03 JST`のまま不変 | **合格** | §3 |
| Slackへ実際に通知が飛んでいないこと | `Send Slack notification`タスクは`skipping`だった | **合格** | §1.7 |
| ansy以外のホストに触れていないこと | quory・Proxmox・Sophos・UniFi・monnie・authyへの接続なし | **合格** | playbook自体が`hosts: localhost`のみ。実行ログにも他ホストへの接続は無い |
| 2つのtimerの状態を変えていないこと | `ansible-knowledge-review.timer`は`active`/`enabled`、次回`2026-08-26 07:15 JST`のまま。`ansible-incident-sync.timer`も`active`/`enabled`、次回発火は時刻経過に伴う自然な前進のみ(状態変更ではない) | **合格** | §3 |
| 評価成果物・バンドルの中身を転記していないこと | 本ファイルはファイル名・件数・フィールド値のみを記載し、生ログ・内容本文・内部IPは含まない | **合格** | 本ファイル全体 |
| 作業ツリーが「開始時+自分の成果物1件」に戻ったこと | `git status --porcelain`が空(本ファイルのみが新規追加対象) | **合格** | §1.8 |

**満たせなかった条件は無い。**

## 3. 終了時のシステム状態

- **`git status --porcelain`は空。** 本ファイル(`2026-07-28_020_u11_test_result.md`)のみが新規追加対象で、まだ`git add`していない。
- **本番`MEMORY.md`の期日行は`2026-08-26`(前回: 2026-07-27)のまま不変。** auto-memory本体には一切書き込んでいない。
- **`reports/incidents/_evaluations/`の2ファイルはU9作成分から本単位により上書きされた**(`previous_evaluation_date: "2026-07-28"`、`bundles_new_since_last_eval: 3`)。**これはU11自身が意図的に作った差分であり、既知の受容事項(同日の評価成果物は上書きされる)どおり。** 次にこのフォルダを見る者は、`_evaluations/2026-07-28-*`がU9・U11のどちらの実行結果を最終的に反映しているか(=U11、時刻18:26)を区別できるよう、本記録を参照すること。
- **`reports/incidents/quory/`は41バンドルのまま不変。**
- **`ansible-knowledge-review.timer`**: enabled/active、次回発火`2026-08-26 07:15:00 JST`(不変)。**`ansible-knowledge-review.service`**: inactive/dead、`ExecMainStartTimestamp`は`2026-07-27 09:23:03 JST`のまま(本単位は起動していない)。
- **`ansible-incident-sync.timer`**: enabled/active、不変。同期は本単位の実行中に自然に1回発火した(`transferred_files: 1`、バンドル総数は変わらず)。本単位はこのtimer/serviceの設定を一切変更していない。
- quory・Proxmox・Sophos・UniFi・monnie・authyには一切接続していない。
- identityは昇格していない。
- 自分が作ったスクラッチ(`/tmp/.../scratchpad/u11_work/`一式、auto-memory複製・run.log含む)は削除済み。リポジトリ内にシンボリックリンクや残置ファイルは無い(`find . -xtype l`で確認、該当0件)。

## 4. 自己検証(依頼文の要求項目)

- **2本の`claude -p`それぞれの実所要時間と終了コードを記録したか**: 記録した(§1.4)。合計591.70秒、`TimeoutStartSec`(3600秒)に対して約16.4%で十分な余裕がある。
- **本番のauto-memoryが無傷であること**: `MEMORY.md`の期日行を実行前後で比較し不変を確認した(§0・§1.2・§3)。スナップショット増加については、本単位が`knowledge_review_automemory_dir`を複製へ差し替えたため、「Snapshot auto-memory before review」タスクが対象にしたのも複製側であり、**本番側の`~/.local/state/knowledge-review/automemory-snapshot-*`は本単位により増えていない**(実行前から存在した`automemory-snapshot-2026-07-27`・`automemory-snapshot-2026-07-28`のいずれも本単位の実行時刻(18:09〜18:31)より前のタイムスタンプであることを確認済み)。
- **振り返り本体・期日更新・report保存・通知が、従来どおり起きたか(AC9)**: すべて起きた。`knowledge_review_run`(1本目)がrc=0で完了し、`Update next review date in MEMORY.md`が複製側の期日行を更新し(§1.2との比較で確認)、`Save report`が`~/.local/state/knowledge-review/report-2026-07-28.md`を書き、`Notify result to Slack`が`tester_mode`により送信のみ抑止された形で実行された(§1.7)。
- **作業ツリーが「開始時の状態 + 自分の成果物1件」に戻っていること**: `git status --porcelain`が空であることで確認した(§1.8・§3)。本ファイルはまだ`git add`前のため、次にこのファイルを保存した直後の`git status`には`?? .../2026-07-28_020_u11_test_result.md`の1行のみが現れる想定。
- **満たせなかった条件、観測できなかったことを、成功と混ぜずに分けて書けているか**: 満たせなかった受入条件・確認事項は無い。観測できなかったことは§5に分離して記載する。

## 5. 観測できなかったこと・残存リスク

- **`~/.local/state/knowledge-review/report-2026-07-28.md`(振り返り本体のstdout全文を含む運用ログ)は、本単位のスコープ外として内容を確認していない。** repo外・commit不要な運用ログであり、依頼文の確認対象(作業ツリーの状態)には含まれないと判断した。ファイルの存在自体は確認済み。
- **同期(`ansible-incident-sync.timer`)が評価実行中に1回発火し、`transferred_files: 1`を記録した。** ミラー総数(41)は変わっていないため実害は無いが、**評価セッションが読んでいる最中にミラーが書き換わる組み合わせ自体は査読F-6が「設計として破綻ではない」と認めた既知のリスクのまま残っている。** 本単位はこの1回の発火が評価の`index.json`の値に影響したかどうかまでは切り分けていない(件数が一致しているため実害は無かったと推測されるが、因果を追跡する手段が今回の観測項目に含まれていない)。
- **`skills/gate-verification/SKILL.md`等、振り返り本体が今回生成した6件の内容の当否(記録として適切か、重複が無いか)は評価していない。** 本単位の担当はAC9(既存動作の無変化)とF-7の代替検証(実経路確認・時間予算)であり、**生成物の質を検証する担当ではないため、生成されたことの記録に留め、内容はそのまま`git checkout --`/削除で戻した。** この振り返り自体の当否(次回2026-08-26の月次振り返りで対象になる項目)は、本単位のスコープ外。
- **`_evaluations/2026-07-28-*`がU9からU11で上書きされたことにより、U9が観測した「初回評価」の成果物実体はもう存在しない。** U9のtest_result(`2026-07-28_019_...md`)はファイル内容を転記していないため実害は無いが、**同日中に同じ`_evaluations/`パスへ2回書く設計が、実行順序を記録側で追わないと再現できない状態を作る**ことは、今回実際に体験した事実として記録する(査読・要件のスコープに含まれるかはCoordinator判断)。
- **これがこの案件フォルダの最後の実行単位である旨は依頼文に明記されているが、本単位はU7・U8・U9・U10の各test_resultへ改めて触れておらず、それらの記録との整合性の最終確認(Auditor相当の役割)は行っていない。**

## 6. 変更したファイル

- 本ファイル(`docs/ai/reviews/incident_auto_capture_step2/2026-07-28_020_u11_test_result.md`)のみ。

**上記以外のリポジトリ追跡対象・未追跡対象は変更していない**(§1.8で復元済み、§3で`git status --porcelain`が空であることを確認)。`roles/`・`playbooks/`・`.gitignore`・Policy本文・`docs/ai/core.md`には一切触れていない。`git add`/`git commit`/`git push`は実行していない。auto-memory本体(`~/.claude/projects/.../memory/`)にも一切書き込んでいない。
