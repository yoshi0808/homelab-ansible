# Audit: cert_renew_unreachable_node

作成: 2026-08-01 / Auditor
入力: 本案件フォルダの4ファイル(`2026-08-01_001_requirement.md`〜`004_test_result.md`)、そこから参照される `playbooks/cert_renew.yml`・`docs/ai/policies/cert_renew_policy.md`・`docs/ai/status.md` の現物diff、`docs/ai/adr/008-proxmox-readonly-check-unreachable-node.md`、`roles/proxmox_reachable_nodes/tasks/main.yml`、`docs/ai/policies/proxmox_operations_policy.md`(SB-095)。Coordinatorからの説明は判断材料にしていない。

## 再構成できたか

できた。requirement→implement→review→test_resultの4段は、要件ID(P0-1〜P0-4)・AC番号(AC1〜AC7)・file:line引用のいずれも相互に一致しており、経緯を自力で追える。実装記録・レビュー・テストが主張するfile:line(probe 4task、summary分岐、`slack_channel`/`slack_status`式)は現物の`playbooks/cert_renew.yml`と行番号レベルで一致することを確認した。Policyの変更履歴表(v2.4)もテーブル末尾に正しく収まっており、レビューSuggestion 1の反映も現物で確認できた。`docs/ai/status.md`への新規Watch行も、着手時点で存在した2ファイル変更(playbook・Policy)に加え本件クローズ時にCoordinatorが追加したものと解釈すれば矛盾はない(Reviewer/Testerがそれぞれ時系列的にstatus.md追記前に書いたと見られる記述と整合する)。

## 指摘

| # | 分類 | 内容 | クローズを妨げるか |
|---|---|---|---|
| 1 | 記録どうしの矛盾 | `docs/ai/adr/008-proxmox-readonly-check-unreachable-node.md`(Status: Accepted, 2026-07-30)は Decision (c) で c-1 を採用し、Consequences に **「変更系playbook(`cert_renew.yml`、`ca_trust_deploy.yml`、`time_sync_ntp_reference.yml`)は対象外であり、pve1停止中に実行すれば失敗する。これは仕様である」** と明記している。本案件は、まさにこの3本のうちの1本である `cert_renew.yml` について、pve1/pve2到達不能時に**失敗させず**警告付きで正常終了させる形へ変更するものであり、ADR-008 c-1の決定と正面から矛盾する。requirement §5 は ADR-008 の Consequences(「probeの消費者が3つ目に増えた時点で共通化を再検討する」)を引用しており、Coordinatorが ADR-008 を読んだこと自体は確認できるが、c-1(`cert_renew.yml`を対象外・厳格運用のままにするという決定そのもの)が本件により覆っている事実には、4ファイルのどこにも触れられていない。ADR-008は現時点でも「Accepted」のままであり、3か月後の読者がADR-008だけを見れば「`cert_renew.yml`がpve1停止で赤くなるのは仕様」という、既に反証済みの前提を正としてしまう。 | **はい**(ただし反映で足りる、再監査不要) |

### 指摘1の反映方法(参考)

同種の欠陥クラス(撤回した根拠が古い記録に残る)は `docs/ai/reviews/norm_drift_mechanical_check/2026-08-01_007_review_policy_rename.md` が既に一度扱っており、その回は「決定当時の記録は保持しつつ、現状との齟齬を1文で注記する」形で解決している。ADR-008 の Consequences か Decision (c) のいずれかに、「`cert_renew.yml`についてはc-1の決定が`cert_renew_unreachable_node`(2026-08-01)により見直された」旨を1〜2行追記すれば足りる。ADR本文の書き換え・再判定は不要。

## 状態の正本との突合

`docs/ai/status.md` の該当行(「`cert_renew` の到達不能node対応が実運用で意図どおり出るか」)は、検証手段(Semaphoreジョブが緑・通知本文の具体的文言)と、限定(「検証はすべてdecoyで行っており実ホスト経由の実行は一度も無い」)の両方を備えており、requirement OQ2・test_result §4の記述と整合する。除去すべき行はない(観測待ちがまだ観測されていないため)。

## 案件をクローズしてよいか

**条件付き受入。**

指摘1(ADR-008への1〜2行の注記追加)を反映すればクローズしてよい。反映後の再監査は前提にしない。指摘1以外に、決定の追跡・根拠の実在・受入条件の充足・未解決事項の明示のいずれにも欠落は見つからなかった。

---

## Coordinatorのクローズ判断(2026-08-01)

指摘1に同意し、`docs/ai/adr/008-proxmox-readonly-check-unreachable-node.md` の Decision (c) と Consequences の該当行へ注記を追加した(ADR本文の決定記述は書き換えず、見直した事実・理由・適用範囲が`cert_renew.yml`に限ること・残る2本は従来どおりであることを明示)。理由は指摘のとおり、ADR-008がAcceptedのまま残るため、そこだけを読んだ人が既に見直された前提を正としてしまうこと。**本案件はこれをもってクローズする。** 再監査は行わない。
