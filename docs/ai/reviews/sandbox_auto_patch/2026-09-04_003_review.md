# review: sandbox が自分でパッチを当て、必要なときに再起動する

日付: 2026-09-04(2026-09-05 再判定・追記)
契約: `docs/ai/reviews/sandbox_auto_patch/2026-09-04_001_requirement.md`(R8/R9/AC9追加後)
対象実装記録: `docs/ai/reviews/sandbox_auto_patch/2026-09-04_002_implement.md`(書き換え後)

## 2026-09-05 再判定

Tester が実機で AC1 FAIL(`docs/ai/reviews/sandbox_auto_patch/2026-09-04_004_test_result.md`)を検出し、requirement へ R8/R9/AC9 が追加され、実装が書き換わった(旧 `52sandbox-auto-patch` → 新 `99sandbox-auto-patch` への改名、旧ファイルの除去task、デプロイ前後の `apt-config dump` 実効値検証blockの新設)。以下は書き換え後のコードに対する再判定。**下の「Code Review」節(初回分)は当時のコードに対する記録として残し、上書きしない。**

### 今回の要点

- **AC9(fail-closed)は自分のfixtureで再現した。** repo外(`/tmp/.../scratchpad`)に `Dir::Etc::parts` を向けた隔離apt configを作り、`connection: local` の fixture playbook で実物の `roles/sandbox_auto_patch` を実行した(`APT_CONFIG`環境変数でrole内の`apt-config dump`コマンドを隔離configへ向けた。destのみoverride、`become`は無効)。
  - 正常系(後続ファイル無し): `Allowed-Origins`に元の3件+updatesが追加、`Automatic-Reboot`実効値`true`/`04:00`/`true`、rc=0。
  - `99sandbox-auto-patch`より後に読まれる`99zz-force-false`(`Automatic-Reboot "false"`のみ)を追加して再実行: **「Verify unattended-upgrades reboot settings are effective」のassertが発火しrc=2で失敗した**(Tester実測の異常系と同じ結果)。
  - `99zz-force-false`を除去して再実行するとrc=0に復帰し、`changed=0`(冪等)だった。
  - よってR9(「効いたことを機械が確かめる」)とAC9は、実装記録の主張どおり実際に成立していることを独立に確認した。
- **R8(命名の使い回し禁止)を確認した。** `roles/sandbox_auto_patch/`・`playbooks/sandbox_auto_patch.yml`のいずれにも`52unattended-upgrades-local`という文字列は無く(`grep`で確認)、cloud-init由来ファイルには一切触れていない。旧`52sandbox-auto-patch`(本role自身が前回作った成果物)を除去するtaskはあるが、これは非ゴール(cloud-init user-dataの不変更)には抵触しない。
- **前回のMajor(実装記録AC4行の記述と現物の不一致)は是正され、かつ現物と一致している。** 現在の実装記録は「guarded blockは`skipping`、`changed=0`、rc=0、SHA-256は前後で同一」と書いており、これは私自身が確認した挙動(block全体が`when: not ansible_check_mode`でcheck mode下は丸ごとskip)およびTesterの実機確認(`2026-09-04_004_test_result.md`のAC4節)と一致する。虚偽・過大主張は残っていない。
- **AC1の核心(origin合成)を再検証した。** 新構造でも`Allowed-Origins`はlist型のscope再オープンで既存3件を保持したままupdatesを追加しており(前回確認済みの`apt.conf(5)`挙動から変わっていない)、「Verify deployment did not remove existing unattended-upgrades origins」のassert(before/after差分が空であること)も自分のfixtureでPASSを確認した。
- **destructive block化はTS-015に沿う。** 「before読み取り→旧drop-in除去→新drop-in配布→after読み取り→検証3件」は相互依存する一連のtaskであり、ファイル上も連続しているため、個別`when`でなく1つのnamed blockでゲートしている。ゲート漏れは無い(block全体に`when: not ansible_check_mode`・`tags: [destructive]`)。
- **通すべきものを誤って落とさないか**: 正常系fixtureでassert 3件(既存origin保持・updates追加・reboot 3設定)すべてPASSし、誤検知(false positive fail)は確認できなかった。値は文字列完全一致で比較しており、defaultsとtemplateのレンダリング結果が一致する限り誤検知は起きない構造である。

### 新規のCritical / Major

なし。

### Suggestions(追加分)

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| 3 | `roles/sandbox_auto_patch/tasks/main.yml` | block全体 | `apt-config dump`をbefore/afterで2回実行し、assert 3件を都度評価する構成のため、taskの見通しが前回より重くなっている。妥当性は確認したが、将来の変更時はblock内taskの依存順序(除去→配布→読み取り→検証)を崩さないことをコメントで一言残すと安全(TS-033の精神)。必須ではない。 | 保守性 |

### 再判定Verdict

**Approve** — 前回のMajorは是正されており記述も現物と一致した。今回追加された R8/R9/AC9 はいずれも自分の独立fixtureで再現・確認できた。非ゴール(cloud-init user-data不変更、他グループ非該当、quory経路無し、Semaphore非登録、50番ファイル不変)への抵触も無い。AC1〜AC5・AC7の実機再確認はTester待ちのまま(Reviewerとしての判断範囲はコード・記録の整合性)。

## Code Review: `roles/sandbox_auto_patch/` + `playbooks/sandbox_auto_patch.yml`

### Summary

非ゴール(§3)への抵触は無く、対象は `sandbox_nodes` のみに閉じている。drop-in が既存 origin を消さずに `-updates` を足すという AC1 の核心主張は、自分でローカル fixture を作って `apt-config dump` を実行し独立に再現できた(後述)。一方で、実装記録が AC4(`--check`)について書いている自己検証は、現在コミットされているコードでは再現しない — `when: not ansible_check_mode` により `--check` では task 自体が `skipping` になり、`changed=1` の予測も diff も出ない。これは repo の他の `check-mode-native` role と同じパターンであり AC4 自体(異常終了しない・実ファイルが変わらない)は満たすが、記録の記述は Tester に誤った期待を持たせる。Major 1件。

### Critical Issues

(なし)

### Major Issues

| # | File | Line | Issue | Severity |
|---|---|---|---|---|
| 1 | `docs/ai/reviews/sandbox_auto_patch/2026-09-04_002_implement.md` | AC4行 | 「時刻を04:00から05:00へ変えた`--check --diff`が`changed=1`を予測し、前後のSHA-256は同一だった」という記述が、コミット済みの`roles/sandbox_auto_patch/tasks/main.yml`(`when: not ansible_check_mode`)では再現しない。自分で同型のfixtureを組み、①実ファイルを一度適用→②変数を変更して`--check --diff`を実行したところ、`skipping: [localhost]`のみでdiff・changed予測は一切出なかった(rc=0で異常終了しないことは事実)。記録の書きぶりは「ネイティブcheck modeでdiffが見える」という誤った期待をTesterに渡す。Tester test_planでAC4を「`--check --diff`でchanged=1の予測が出ることを確認する」と書くと、実機で必ず失敗する項目になる。 | Major |

### Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| 1 | `docs/ai/reviews/sandbox_auto_patch/2026-09-04_002_implement.md` | AC4行 | 「`--check`ではtaskが`skipping`になり実ファイルは変更されず、事前にdiffの予測は出ない(他のcheck-mode-native roleと同型)」という実際の挙動へ記述を訂正するとよい。Tester側のtest_planが正しい期待値で書けるようになる。 | 正確性 |
| 2 | `docs/ai/context/operations/sandbox-vm.md` | §2 | 「repoから再現できる対処は`playbooks/sandbox_auto_patch.yml`である」の直後に、適用後の観測手段(`apt-config dump` / dry-run)への言及があると、次に読む者がAC1・AC2の確認方法を探さずに済む。必須ではない。 | 可読性 |

### What Looks Good

- **非ゴール順守(§3)**: `roles/sandbox_auto_patch/`・`playbooks/sandbox_auto_patch.yml`とも対象は`hosts: sandbox_nodes`のみで、`inventories/homelab/hosts.yml`の`sandbox_nodes`注記(「他のグループへ足さないこと」)を破っていない。quory→sandbox経路、Semaphore登録、`50unattended-upgrades`の改変は一切無い。role配下は`defaults/`・`tasks/`・`templates/`のみで、他の副作用を持つtaskは無い。
- **AC1の核心主張を自分で再現できた**: `apt.conf(5)`の「listを再オープンして匿名要素を追加すると既存要素は消えず末尾に足される」という主張を、`/tmp`配下に独立に作ったfixture(50番=sentinel3件、52番=repoのJinjaテンプレートを実描画したもの)へ`APT_CONFIG=... apt-config dump`を実行して確認した。結果は既存3件を保持したままupdatesが4件目として追加され、実装記録の記述と一致した。
- **`${distro_id}:${distro_codename}-updates`がJinjaに巻き込まれていないこと**: `${...}`はapt.conf側の変数展開でありJinja構文と衝突しないため、`ansible-playbook`で実描画しても文字列のまま出力されることをローカルfixture実行で確認した。
- **AC5(既存ファイル不変)**: `roles/sandbox_auto_patch/tasks/main.yml`は`52sandbox-auto-patch`という新規パスにしか触れず、`50unattended-upgrades`を参照・変更するtaskは無い。
- **AC8(構文)**: `ansible-playbook --syntax-check playbooks/sandbox_auto_patch.yml`をこちらでも実行し、rc=0を確認した。
- **AC6(索引・文書)**: `playbooks/README.md`に1行追加され、`docs/ai/context/operations/sandbox-vm.md` §2の記述が2026-09-04の実測(-updates欠落、reboot-required放置)と新しい対処に更新されている。「何もしなくてよい」という古い結論は残っていない。
- **`when: not ansible_check_mode` + `tags: [destructive]`のみが唯一の変更taskで、`docs/ai/policies/ansible_test_safety_policy.md` TS-014のcheck-mode-native標準形と一致**。他の同種role(`codex_update_check`、`homelab_cert_renew`、`alloy`、`rsyslog_forward_to_monnie`)も同じ`when: not ansible_check_mode`パターンで唯一/一部の破壊的taskをゲートしており、本roleだけが逸脱した書き方をしているわけではない。
- **R4(ログイン中でも再起動)**: `Automatic-Reboot-WithUsers "true"`は`unattended-upgrades`公式README(ローカルにインストール済みのdebパッケージのdocs)の記述と一致する意味で使われている。
- **TS-036準拠**: `tasks/main.yml`はtester-gateの分類・理由をplaybookヘッダへの参照のみで済ませ、複製していない。

### 確認していないこと(実ホスト非接続のため)

- sandbox実機での`apt-config dump`・dry-run実行(AC1・AC2) — Tester待ち。
- sandbox実機での2回目適用によるchanged=0確認(AC3) — Tester待ち。
- sandbox実機での`--check`実行(AC4) — ローカルfixtureでのみ確認。実機でも同じ`skipping`挙動になるはずだが、実測はしていない。
- sandbox実機での`50unattended-upgrades`比較(AC5) — Tester待ち。
- 翌日以降の保留更新解消・reboot-required消滅・uptimeリセット(AC7) — Tester待ち、観測は今後。
- sandboxへのSSH・ansible接続は一切行っていない。

### 実施した確認手段(箇条書き)

- `git status` / `git diff -- playbooks/README.md docs/ai/context/operations/sandbox-vm.md` で差分範囲を確認。
- `roles/sandbox_auto_patch/`配下3ファイル、`playbooks/sandbox_auto_patch.yml`を読了。
- `docs/ai/policies/ansible_test_safety_policy.md`のcheck-mode-native定義(TS-014等)を読み、本実装のtag/whenパターンと照合。
- `inventories/homelab/hosts.yml`の`sandbox_nodes`定義・注記を確認。
- `/tmp`配下(scratchpad)に独立fixtureを作成し、`apt-config dump`でAllowed-Originsの合成順序・件数を実測(ローカルのapt 3.2.0)。
- `unattended-upgrades`公式README(`/usr/share/doc/unattended-upgrades/README.md.gz`、ローカルインストール済み)でAutomatic-Reboot系3変数の意味を確認。
- scratchpad配下に`connection: local`のfixture playbookを作り、実際の`roles/sandbox_auto_patch`をimportして通常実行・`--check --diff`実行の両方を行い、テンプレート描画結果と`--check`時の`skipping`挙動を確認(sandboxへは接続していない)。
- `ansible-playbook --syntax-check playbooks/sandbox_auto_patch.yml`(rc=0)、`ansible-lint playbooks/sandbox_auto_patch.yml`(failure/warning 0)、`scripts/check-tester-gate.sh`(OK)をこちらでも実行。
- 他role(`codex_update_check`、`homelab_cert_renew`、`rsyslog_forward_to_monnie`、`alloy`)の`ansible_check_mode`ゲート方式をgrepで横断し、本実装のパターンが逸脱でないことを確認。

### Verdict

**Request Changes** — Major 1件(実装記録のAC4自己検証記述が現在のコードで再現しない)。コード自体(role/playbook/README/context doc)には非ゴール抵触・destructive分類・AC1核心主張の観点で欠陥は見当たらない。修正はrequirement記録2026-09-04_002_implement.mdのAC4該当行の訂正で足り、role/playbook自体の変更は不要と判断する。
