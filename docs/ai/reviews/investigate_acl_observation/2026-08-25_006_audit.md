# Audit: investigate_acl_observation

作成: 2026-08-25 / Auditor

## 再構成できたか

**できた。** requirement → implement(`868a9b3` / mode復元 `64b760d`)→ review(`0d079f9`、Codex/Approve)→ test_result(配備commit `0d079f9`、quory ジョブ #840)→ finding(`3f6ff41`)の一本道で、各ファイルが次のファイルへ辿れる形になっている。requirementのP0/AC、実装のcase文追記、Reviewerの静的+ローカル実評価、Testerの実機観測、Coordinatorの原因確定まで、記述と現物の間に矛盾は見つからなかった。

現物との突合で確認した主な点:
- `868a9b3` / `64b760d` の diff は implement.md の記述(純追記27行、ファイルモードのみ復元)と一致。
- 現在のスクリプトで top-level arm を数えると **29**(変更前後で同数)。`acl-status` の name→path 表は `yoshi-home` / `semaphore-dir` / `semaphore-db` / `reports-root` に `incident-inspect-root` / `incident-inspect-context` を加えた6項目で、いずれも固定literal、`deny_invalid path` へフォールバックする構造も健在。
- `roles/deployment_drift_check/defaults/main.yml` 77-78行に対象パスの記載あり(requirement参照どおり)。`roles/dev_investigate/tasks/main.yml` は `ansible.builtin.copy`(Jinja無し)のまま。
- AC5: quory側ハッシュ(test_result記載)・`0d079f9` 時点のリポジトリ内容・現在のワークツリーの3者すべて `91b1007c...91d9ea2` で一致。
- `docs/ai/memory/decisions/rejected-proposals.md`・`grant-read-access-rather-than-police-speculation.md`・`measure-whether-the-risk-has-ever-materialized.md`「判断の順序」は、いずれも requirement / finding が引く内容と一致して実在する。
- `docs/ai/status.md` は本案件の結果(観測手段追加→原因確定、修正は未実施)を正しく反映しており、`incident-inspect`所有・ACLエントリ無し・traverseが片方向という finding.md の内容と食い違いはない。「壊れている」節が消えていないのは仕様どおり(修正は本案件の非ゴール)。

## 指摘

**1件、非ブロッキング。**

| # | 分類 | 内容 | クローズを妨げるか |
|---|---|---|---|
| 1 | 決定の追跡 | `2026-08-25_004_test_result.md` はAC3/AC4のフル再実行を省略した理由を「Coordinatorの指示により」としているが、本フォルダ内にCoordinatorがその指示を出した記録は無い。最も近い根拠は `2026-08-25_003_review.md` の Tester Handoff 節(「AC3/AC4はReviewerの静的照合とローカル実評価で充足を確認済み…blockingな未検証ではない」)であり、これはReviewer自身の判断であってCoordinatorの指示そのものではない。誰が・いつ・何を根拠にこの省略を決めたかは、本フォルダの記録だけからは厳密には特定できない | 妨げない — 省略の技術的な妥当性自体はReview文書に明記されており、AC3/AC4はImplementer/Reviewerが独立に2回、配備前に静的照合とローカル実評価で確認済みという実体は追える。欠けているのは「Coordinatorが指示した」という一文の裏付けだけである |

## 状態の正本との突合

`docs/ai/status.md`「壊れている: 一次調査の先読みファイルが書けていない — 原因確定、修正待ち(2026-08-25)」は本案件の観測結果・原因確定を正しく反映している。**この行は消さない** — 本案件は観測手段を足すところまでで、`EACCES` の修正は非ゴールとして明記されており、行の文言自体も「修正待ち」としている。除去すべき行は無い。

## 判定

**受入。** そのままクローズしてよい。指摘1件は記帳上の帰属表記の精度に留まり、技術的な結論・観測事実・受入条件の充足には影響しない。

---

## クローズの判断(Coordinator、2026-08-25)

**クローズする。**

### 指摘への対応 — AC3 / AC4 を再実行させなかった理由

指摘のとおり、その判断は案件記録のどこにも残っていなかった。**依頼文は永続しない**ため、ここへ記録する。

**Implementer と Reviewer が独立に2回、実際にスクリプトを実行して確かめている。**

- Implementer: `SSH_ORIGINAL_COMMAND` を与えて拒否経路を実行(`2026-08-25_002_implement.md`)
- Reviewer: 同じことを独立に実行し、未知名・生pathが既存の文言と `rc=1` で拒否されることを確認(`2026-08-25_003_review.md`)

**AC3 / AC4 は配備前に確定する静的な性質**であり、配備によって変わらない。**3回目の実行は儀式**になる。一方 AC1 / AC2 / AC5 は配備後にしか測れず、そこに Tester の独立性を使うほうが実質がある。

Tester には「配備物側で軽く裏を取るのは構わない」と伝えており、実際 `acl-status etc-passwd` の拒否を配備物に対して1件確認している。**配備物側でゼロ件ではない。**

### 本案件の到達点

**観測手段を足すこと**が目的であり、それは果たした。**副産物として原因も確定した**(`2026-08-25_005_finding.md`)。

**修正は非ゴールである。** `docs/ai/status.md` の「壊れている」行は、原因確定・修正待ちとして残す。
