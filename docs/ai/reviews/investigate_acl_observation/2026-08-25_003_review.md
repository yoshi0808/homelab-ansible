# Code Review: investigate ACL observation

Reviewer: Codex / 2026-08-25  
対象: `868a9b3^..64b760d`（実装 `868a9b3`、file mode復元 `64b760d`）

## Summary

`acl-status`の既存固定enumへ2項目だけを追加する実装は、requirementを満たしている。新しいarm・実行語彙・operand由来のpath構築はなく、未知名と生pathは既存経路で拒否される。配備はJinjaを含まないfileを`copy`するままで、Tier 1 drift checkの適格性も維持している。

## Critical Issues

なし。

## Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---:|---|---|
| 1 | `docs/ai/reviews/investigate_acl_observation/2026-08-25_002_implement.md` | 4 | `対象commit`がrequirement作成commit `f8c4dba`を指している。実装本体は`868a9b3`、最終レビュー対象はmode復元後の`64b760d`なので、将来の追跡で実装commitと誤読されない表記へ直すとよい。 | Record accuracy |

## What Looks Good

- `acl-status`の追加2名は、それぞれ固定literalの`target_path`へ直接対応する。`$p1`は`case`選択にしか使われず、文字列連結、展開、`eval`、shell再解釈は追加されていない。
- 実行可能行の差分は2本の`target_path=...`だけで、呼び出すコマンドは既存の`getfacl -p -- "$target_path"`から増えていない。書込・実行の語彙は追加されていない。
- top-level armは変更前後とも29本で、arm名一覧も完全一致した。対象diffは追加のみであるため、既存25 armsを含む既存armのarity検査と拒否文言は変更されていない。
- ローカル実評価で`acl-status etc-passwd`と`acl-status /etc/shadow`はいずれも`denied: invalid path for acl-status` / rc=1、引数過不足は既存の`denied: invalid parameter count for 'acl-status'` / rc=1となった。
- 追加2名は`deny_invalid`へ入らず、それぞれ固定した`/var/lib/incident-inspect`と配下の`semaphore-context`に対する`getfacl`へ到達した。ansyには対象pathが無いためrc=1の`No such file or directory`となったが、固定表の選択と拒否経路の分離は確認できた。
- dispatch fileにJinja構文はない。`roles/dev_investigate/tasks/main.yml`は引き続き`ansible.builtin.copy`で配備し、`roles/deployment_drift_check/defaults/main.yml`のTier 1カタログも同じrepo fileを参照する。
- 最終file modeは実装前と同じ`100644`。配備時の実行modeは`copy` taskの`mode: "0755"`が明示するため、repo file modeの復元で配備挙動は変わらない。
- 既存の固定enumという資産を拡張しており、別dispatcherや別path解決ロジックを重複実装していない。

## Verification

- `git diff --check 868a9b3^ 64b760d`: PASS。
- `bash -n roles/dev_investigate/files/recovery-investigate-dispatch-quory.sh`: PASS。
- top-level arm count: 変更前29 / 変更後29。arm名一覧のSHA-256も一致。
- invalid name / raw path / arity過不足 / 追加2名を`SSH_ORIGINAL_COMMAND`経由でローカル実評価。
- `scripts/check-tester-gate.sh`: PASS（57 playbooks）。
- `ansible-playbook --syntax-check playbooks/dev_investigate_setup.yml`: PASS。
- `shellcheck`は環境に無いため未実施。差分の実行可能行は固定代入2行のみであり、この未実施は判定を妨げない。
- 実ホストへは接続・実行していない。`docs/ai/memory/`は読んでいない。

## Tester Handoff

- AC1: 配備後、`incident-inspect-root`がowner / group / mode / ACLを返しrc=0になること。
- AC2: `incident-inspect-context`がACLを返すか、親traverse不足を`Permission denied`として直接観測できること。どちらの分岐だったかを記録する。
- AC5: `deployed-hash investigate-dispatch-quory`とrepo fileのSHA-256一致を確認する。
- AC3 / AC4はReviewerの静的照合とローカル実評価で充足を確認済み。実配備物でも同じ拒否境界を確認できればより強いが、blockingな未検証ではない。

## Verdict

**Approve** — blocking defectはない。AC1 / AC2 / AC5は契約どおり、Yoshinobuによる配備後にTesterが実環境で確認する。
