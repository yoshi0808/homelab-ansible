# Code Review: update playbooks dry_run collision（差し戻し再レビュー）

作成: 2026-09-09 / Reviewer（`2026-09-08_005_review.md`と同じ差分Reviewer）

### Summary

005のMajor #1（Semaphore identity）とMajor #2（Prometheus guard順序）、Minor 3件は解消した。Major #3の旧契約掃引とstatus/Policy整合は一部に現在形の矛盾が残っているため、現差分は引き続きRequest Changesとする。

### Critical Issues

なし。

### Major Issues

| # | File | Line | Issue | Severity |
|---|---|---:|---|---|
| 1 | `docs/ai/policies/ubuntu_vm_patch_policy.md`, `roles/semaphore_templates/defaults/main.yml`, `docs/ai/status.md`, `roles/prometheus_update_check/tasks/{main.yml,discover_backups.yml}`, `roles/ubuntu_vm_full_upgrade/tasks/apply.yml` | Policy 294, 320, 369-376; catalog 763-770, 959; status 103-104; Prometheus main 399, 444; discover 112; Ubuntu apply 202 | 005 Major #3が未解消。Policy UV-070/UV-076は現在の定期operationをなお`monthly dry-run`と定義し、Policy変更履歴には本案件の行が無い（旧2026-07-25行を短縮しただけで先頭に余分な空白もある）。catalog第2バッチ説明は「`dry_run`喪失でfull upgrade」「同名`dry_run`が2層にある」と撤回済みの衝突を現在形で説明し、statusの隣接案件は人が`dry_run=false`で適用する、UV-035〜038は`dry_run=true`を要求すると現行Policyと逆の説明をする。さらに案件行は見出しへ`Now:`と書いただけで`## Next`表内に残り、現在地の構造と食い違う。task名/commentにも「explicit dry-run mode」「both dry-run layers」「dry-run's reboot_expected」が現行経路名として残る。P0-10/AC8と計画§1.4を満たすには、liveな記述を`inspect` / `apply|update|rollback` / native checkへ揃え、案件行を実際の`## Now`節へ移し、Policy履歴へ2026-09-09の変更行を追加する必要がある。旧Policyの履歴的説明、reject-only guard、Semaphore native `params.dry_run`、他playbookのdry-run、およびidentity維持に必要なUbuntuの`variant`/`legacy_name`/schedule名は残存してよいが、identity文字列は新しい有効入力ではないことを区別する。 | Major |

### Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---:|---|---|
| 1 | `roles/prometheus_update_check/tasks/notify.yml` | 64 | `rollback_available`は`operation=rollback`かつ`ansible_check_mode`でのみ到達するため、`not executed in inspect/check mode`の`inspect`は成立しない。`not executed in check mode`へ絞るとoperation軸とcheck軸を再び混ぜずに済む。 | Minor |
| 2 | 案件の検証記録 / fixture | - | 005のTest GapはTester工程まで残る。今回、identityのin-place更新、5 template・2 scheduleのcatalog shape、legacy/unknown operationのguard前停止はlocalで補足確認したが、5正常経路とcheck mode副作用/Slackゼロ、実Semaphore readbackは未確認である。修正後のTester検証でAC行列とreadbackを完了する。 | Test Gap |

### What Looks Good

- **005 Major #1解消**: Ubuntu templateは`variant: dry-run`と既存`legacy_name`、scheduleは既存`name`とtemplate参照を保持し、Survey/environmentだけをoperation化した。現行`#dry-run` markerを持つfixtureに対するreconcileは`changed=1`、`new=0`、`orphans=0`となり、ID 7のin-place更新を確認した。schedule名もHEADと一致する。
- **005 Major #2解消**: Prometheus localhost playは`gather_facts: false`となり、legacy guard、operation guard、明示`setup`の順になった。旧`dry_run=true`と未知operationのlocal実行はいずれもfact gathering前にrc=2で停止した。
- **005 Minor #1解消**: 5つのoperation Surveyすべてに`type: enum`が追加され、defaultとvaluesは各playbook enum内である。
- **005 Minor #2解消**: Prometheusは`prometheus_update_check_effective_operation`を1回設定し、role内の分岐とsummaryがこのfactを参照する。
- **005 Minor #3解消**: Prometheus Slack本文にeffective operationが追加された。
- Ubuntu inspect/applyのhost参加、confirmation、Status拒否、apply includeの`not ansible_check_mode`、Prometheus inspect/update/rollbackと変更blockの`not ansible_check_mode`は維持されている。新しいsecurity findingや重複実装は見つからなかった。
- catalogの5 templateと2 scheduleをYAMLとして読み、namespaced key/default、`type: enum`、schedule environment、native `dry_run: false`、Prometheus `debug_level: 4`をassertしてrc=0だった。
- 両playbook syntax、tester-gate lint（60 playbooks）、doc consistency（3 checks）、`git diff --check`、Semaphore task-flow 6 scenariosはすべてrc=0だった。

### 未確認事項

- 実Semaphore 5 template・2 scheduleのreconcile/readback、通常inspectのSlack通知は未実施。
- 実hostでのinspect、apply/update/rollback `--check`と副作用前後差分は未実施。Reviewerの境界上、実ホストへは触れていない。
- 実update、rollback、rebootは本案件の非ゴールであり未実施。

### Verdict

**Request Changes** — Critical 0、Major 1、Minor 1、Test Gap 1。Major #1の掃引・Policy履歴・status配置を修正後、再レビューが必要。
