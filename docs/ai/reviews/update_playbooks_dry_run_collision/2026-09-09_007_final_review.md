# Code Review: update playbooks dry_run collision（最終レビュー）

作成: 2026-09-09 / Reviewer（差分レビュー、最終限定修正後）

### Summary

006で残ったMajor（live旧契約、Policy履歴、status配置）とMinor（rollback通知表現）、追加spot-check 3箇所は解消した。既存Semaphore identityを保持したままSurvey/environmentだけをoperation化し、guard・変更境界・文書はrequirementと整合している。blocking findingは無い。

### Critical Issues

なし。

### Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---:|---|---|
| 1 | `docs/ai/policies/ubuntu_vm_patch_policy.md` | 374-377 | 変更履歴の2026-09-09行が2本目の2026-07-25行と2026-08-23行より前に入っている。規範本文や実行判断には影響しないが、次回この表を触る際に日付順の末尾へ移すと履歴を追いやすい。 | Documentation |
| 2 | 案件のTester成果物 | - | localではidentity、catalog shape、legacy/unknown/missing guardを確認したが、AC1〜AC6の正常5経路、check modeの副作用・Slackゼロ、実Semaphore 5 template・2 scheduleのreadbackは未確認である。計画どおりTesterと配備後観測で閉じる。 | Test Gap |

### What Looks Good

- **Semaphore identity保持**: Ubuntu inspection templateの`variant: dry-run`、既存`legacy_name`、scheduleの既存`name`とtemplate参照はHEADと同じで、Survey/environmentだけがoperationへ変わった。現行`#dry-run` marker fixtureのreconcileは`changed=1`、`new=0`、`orphans=0`となり、既存IDへのin-place updateを確認した。
- **5 template・2 scheduleのexact shape**: 5 Surveyはnamespaced operation、`type: enum`、期待defaultを持つ。Ubuntu/Prometheus scheduleはそれぞれoperation=`inspect`、native `params.dry_run=false`で、Prometheusの`debug_level: 4`、cron、active、identityを維持する。YAMLを直接読みassertした検査はrc=0だった。
- **guard順序**: 両playbookは暗黙fact gatheringを無効にし、legacy guard、operation guard、明示`setup`の順である。Prometheusで旧`dry_run=false`、旧`rollback=false`、未知operation、operation欠落をlocal実行し、すべてfact gathering前にrc=2で停止した。
- **operation到達性とcheck境界**: Ubuntu inspectは全host、applyはnode一致hostだけが参加し、confirmationとStatus拒否を維持する。Prometheusはeffective operation factを全分岐で使い、update/rollbackの変更blockはいずれも対応operationと`not ansible_check_mode`を要求する。inspectから変更blockへ到達する経路は見つからなかった。
- **Slack抑止と表示**: check modeの送信抑止は`common_slack`に委ねたままで、呼出側の`check_mode: false`は無い。Prometheus本文はeffective operationを表示し、`rollback_available`は到達条件どおりnative check modeだけの表現になった。
- **Policy / Context / status**: UV-019、UV-025、UV-035〜039、UV-070、UV-076、UV-086はoperation軸へ揃い、2026-09-09のPolicy履歴も追加された。案件は実際の`## Now`節へ移り、隣接するnon-apt将来案件も現行operation契約を述べる。Operations ContextとREADMEにも旧有効入力は残らない。
- **旧語掃引**: 対象liveファイルの残存`dry_run`は、Semaphore native `params.dry_run`、旧入力reject guard、別playbookのdry-run、旧Policyの歴史説明、または既存object identityを保持するUbuntuの`variant`/`legacy_name`/schedule名に分類できた。現行の有効extra-varやmode gateとして残る箇所は無い。catalogはidentity名とは別に、operation environmentとnative checkの二軸を正しく説明する。
- **文言spot-check**: Prometheus rollback task名/comment、Ubuntu apply/healthcheck comment、Semaphore時刻表はinspect/operation/native checkへ更新されている。
- **Security / reuse**: 新規shell展開、未quote変数、secret出力、危険な`delegate_to`、関連既存roleを迂回する重複実装は見つからなかった。
- **local checks**: 両playbook syntax、tester-gate lint（60 playbooks）、doc consistency（3 checks）、`git diff --check`、Semaphore task-flow 6 scenariosはすべてrc=0だった。

### 未確認事項

- 実Semaphore 5 template・2 scheduleのreconcile/readbackと、通常inspectのSlack通知。
- 実hostでのinspect、apply/update/rollback `--check`および副作用前後差分。Reviewerの境界上、実ホストへは触れていない。
- 実update、rollback、rebootは本案件の非ゴールであり未実施。

### Verdict

**Approve** — Critical / Major / Minorなし。Suggestion 2件はnon-blocking。Tester検証と配備後readback/通知観測は未完了であり、案件全体のクローズ条件として残る。
