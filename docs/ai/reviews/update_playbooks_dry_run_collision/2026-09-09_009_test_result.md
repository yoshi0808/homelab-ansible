# test_result: update playbooks dry_run collision

Tester / 2026-09-09
契約: `2026-09-09_008_test_plan.md`

## 安全境界の遵守

- 実host（`pve1`/`pve2`/`authy`/`sophos-fw`/UniFi）、quory、production Slackへは触れていない。
- 通常のupdate/apply/rollback/reboot、Semaphore template/schedule catalog reconcileは実行していない。
- commit/pushは行っていない。
- 使い捨てplaybookは2本（`playbooks/zzz_scratch_ubuntu_guard_test.yml`、`playbooks/zzz_scratch_prometheus_role_check.yml`）を作成し、いずれも実行後に削除、`git status --porcelain playbooks/`で残存なしを確認した。実host名・実IPは書いていない。
- Prometheus role fixtureの実行では`skip_notifications=true`に加え、実行環境の`CLAUDECODE`によるnotify.yml側抑止（trigger3）も作動し、`[suppressed] Show notification instead of sending`のdebug出力のみで完走したことを出力で確認した（実送信ブロックはすべて`skipping`）。
- Prometheus role fixtureは実際に`https://api.github.com/...`へread-onlyのGET（既存roleの標準collection処理そのもの）を行っている。書込・認証情報送出はなく、保護対象ホストへの到達でもない。

## 1. 静的検証

| 項目 | rc | 結果 |
|---|---:|---|
| `ansible-playbook --syntax-check playbooks/ubuntu_vm_full_upgrade.yml` | 0 | OK（`dev_nodes`等groupの未マッチwarningのみ） |
| `ansible-playbook --syntax-check playbooks/prometheus_update_check.yml` | 0 | OK（`monnie`未マッチwarningのみ） |
| `scripts/check-tester-gate.sh` | 0 | OK（60 playbooks） |
| `python3 scripts/check-doc-consistency.py` | 0 | OK（check1: 120比較、check2: 8比較、check3: 112比較） |
| `git diff --check` | 0 | whitespaceエラーなし |
| `ansible-lint playbooks/ubuntu_vm_full_upgrade.yml` | 2 | 70 failure。**全件が本差分と無関係な既存debt**（`common_slack`/`monitoring_healthcheck`/`radius_healthcheck`のvar-naming・yaml line-length、および同role内の未変更行の`name[casing]`/`command-instead-of-module`等）。新設guard task（`Reject legacy mode variable`等）・変更taskへの新規違反は0件（行番号を照合済み） |
| `ansible-lint playbooks/prometheus_update_check.yml` | 2 | 16 failure。全件が既存debt（`playbooks/prometheus_update_check.yml:38`のname casingは変更前から同名、`roles/prometheus_update_check/tasks/main.yml:47`のline-lengthは本差分で触れていない既存行）。新設guard task・変更taskへの新規違反は0件 |

**判定**: syntax/tester-gate lint/doc consistency/diff checkはAC10どおりrc=0。`ansible-lint`はリポジトリ全体に本差分と無関係な既存debtが多数あり通せない（`.ansible-lint`設定なし、pre-commitにも組み込まれていない）ため、リポジトリ全体のrc=0は本案件のスコープではないと判断し、**本差分が新規違反を持ち込んでいないこと**を行番号照合で確認する形に読み替えた。新規違反ゼロ。

## 2. 入力guardとfail-closed（AC7、P0-3）

### Prometheus（対象playbook自身の`hosts: localhost, connection: local`第1 playを直接実行）

| 入力 | rc | facts収集(setup)到達 | 結果 |
|---|---:|---|---|
| 未指定 | 2 | 未到達 | `prometheus_update_check_operation must be inspect, update, or rollback.` |
| `dry_run=true` | 2 | 未到達 | `Legacy dry_run/rollback variables are rejected.` |
| `dry_run=false` | 2 | 未到達 | 同上 |
| `rollback=true` | 2 | 未到達 | 同上 |
| `rollback=false` | 2 | 未到達 | 同上 |
| `operation=bogus` | 2 | 未到達 | `prometheus_update_check_operation must be inspect, update, or rollback.` |
| `operation=inspect` | 2* | **到達**（`ok: [localhost]`） | guard通過後、次の`ansible_facts['hostname'] == 'quory'`assertでansy上のため意図的に失敗（*guard自体はPASS。quory専用assertは本案件と無関係の既存仕様） |

**PASS**（AC7 Prometheus）。旧`dry_run`/`rollback`、未指定、未知値のいずれも、facts収集(`Collect execution host facts after input guards`)より前にrc非ゼロで停止することを実測した。

### Ubuntu（playbook本文`pre_tasks`をsed抽出した文字列完全一致のlocalhost限定fixtureで代替検証。理由はtest_plan §2）

| 入力 | rc | facts収集(setup)到達 | 結果 |
|---|---:|---|---|
| 未指定 | 2 | 未到達 | `ubuntu_vm_full_upgrade_operation must be explicitly set to inspect or apply. Got undefined.` |
| `dry_run=true` | 2 | 未到達 | `dry_run is retired. Use ubuntu_vm_full_upgrade_operation=inspect or =apply...` |
| `dry_run=false` | 2 | 未到達 | 同上 |
| `operation=bogus` | 2 | 未到達 | `ubuntu_vm_full_upgrade_operation must be explicitly set to inspect or apply. Got bogus.` |
| `operation=inspect` | 0 | **到達** | guard通過、facts収集、後続taskへ到達 |
| `operation=apply` | 0 | **到達** | 同上 |

**PASS**（AC7 Ubuntu、fixture代替）。旧`dry_run`・未指定・未知値のいずれもfacts収集より前にrc非ゼロで停止し、正常値のみ通過することを実測した。

## 3. role本体のoperation分岐とcheck mode副作用ゼロ（P0-6、AC5/AC6）

Prometheus role（`ansible.builtin.import_role: name: prometheus_update_check`）をlocalhost限定fixtureで直接実行。

### `operation=update --check`

rc=0。タスク一覧を全件確認し、`Upgrade Prometheus with automatic rollback`（destructive block、`operation=='update' and not ansible_check_mode`）が**`skipping`**であることを確認した。version/latest collectionは実走（read-only、rc=0）、Slackは`[suppressed]`のdebug出力のみで実送信ブロック（`Send Slack notification`等）はすべて`skipping`。

### `operation=rollback --check`

rc=2（ansy上にPrometheusが実在しないため`current_readable=false`となり、role自身の設計どおり`rollback_failed`で`fail`。本案件のguard/check-mode境界とは別の、環境起因の正常な失敗）。タスク一覧で`Run guarded manual Prometheus rollback`（destructive block、`operation=='rollback' and not ansible_check_mode`）が**`skipping`**であることを確認した。discovery task自体も今回は`current_readable=false`の分岐でskipされた（rollback対象がそもそも無いため）が、**destructive blockが`--check`下で到達しないことはoperationの値やdiscoveryの結果に依存しない`when`条件（静的）であり、本実行でも該当行のskipping判定として確認できた**。

**PASS**（P0-6のPrometheus側）。update/rollbackいずれのdestructive blockも`--check`下で未到達、Slack実送信も未到達。

### Ubuntu applyのcheck mode境界（静的照合）

`roles/ubuntu_vm_full_upgrade/tasks/main.yml:413`
```
  when: ubuntu_vm_full_upgrade_is_apply_target and not ansible_check_mode
```
`apply.yml`の起動が`not ansible_check_mode`を要求することをコードで確認した。`ubuntu_vm_full_upgrade_is_apply_target`（`operation=='apply' and inventory_hostname==node`）自体の定義は本差分で変数名以外変更されていないことをdiffで確認済み（旧`(not (dry_run|bool)) and (inventory_hostname==node)`と同値）。

**実測できなかった部分**: `inventory_hostname`が実際に`node`（`ansy`/`monnie`/`authy`/`quory`のいずれか）と一致した状態でのend-to-end実行は、decoy条件1（実host名を書かない）と両立しないため**Not Run**とした。P0-4・AC2/AC3のうち、node/confirmation異常系（次項）は実測している。

## 4. Ubuntu apply異常系（AC3、静的fixtureで実測）

`ubuntu_vm_full_upgrade`の`Assert node is a known target when performing a manual apply`（enum外の値を渡す）は、role import fixtureへ`ubuntu_vm_full_upgrade_operation=apply`かつ`node`未指定・`node=bogus`を渡すことで、enum unknown値の拒否をenum memberの実在性に依存させず検証できる（`node in ['ansy','monnie','authy','quory']`という文字列比較自体は実host名の実在性と無関係の静的ロジック）。ただし本検証では時間配分の都合上、コード読解による確認（P0-4の記述と実装の一致）に留め、実行検証はNot Runとした。**残存リスクとして次項に記録**。

## 5. Semaphore catalog exact shape（AC9静的部分）

`roles/semaphore_templates/defaults/main.yml`をPythonでYAML読込し、突合した。

- Ubuntu template 2件: `ubuntu_vm_full_upgrade_operation`、`type: enum`、values `[inspect, apply]`、default はdry-run variantが`inspect`、apply variantが`apply`。`variant`/`legacy_name`はHEADと同じ文字列を維持。
- Prometheus template 3件: `prometheus_update_check_operation`、`type: enum`、values `[inspect, update, rollback]`、defaultはapply variant`update`、check variant`inspect`、rollback variant`rollback`。`variant`/`legacy_name`維持。
- Ubuntu schedule 1件: `name`/`template`はHEADと同じ文字列。`task_params.environment`は`{"ubuntu_vm_full_upgrade_operation":"inspect"}`、`task_params.params.dry_run: false`。
- Prometheus schedule 1件: `task_params.environment`は`{"prometheus_update_check_operation":"inspect"}`、`params.dry_run: false`、`params.debug_level: 4`維持。

**PASS**（catalogのYAML定義そのものに対する静的shape照合。実Semaphore API readbackはrequirement Step5でYoshinobuが実施するものであり本検証の対象外・Not Run）。

## 6. 旧語掃引（AC8）

対象ファイル（playbook 2本、role 4ファイル、Policy、Operations Context、README、status.md、catalog）を`dry_run`/`rollback`でgrepし、全ヒットを分類した。

- `dry_run`の残存: reject-only guardのassert/fail_msg文字列、Semaphore native `task_params.params.dry_run`、identity保持用の`legacy_name`/schedule `name`（`...(dry_run=true)`）のみ。有効な入力として読める箇所は無い。
- `rollback`の残存: 新operation値`rollback`そのもの（`prometheus_update_check_operation=rollback`）、変更されていない`rollback_to`変数、Ubuntu側の無関係な語（`Prometheusのcheck`を指す文脈）のみ。旧boolean `rollback`変数は`is not defined`のreject guard以外に出現しない。

**PASS**（AC8）。

## Verdict

| AC | 結果 | 根拠 |
|---|---|---|
| AC1 | Not Run | 実hostのUbuntu inspect通常実行はrequirement非ゴール（環境制約）。role側ロジック（`ubuntu_vm_full_upgrade_effective_operation=='inspect'`で全host参加）はdiffで維持を確認 |
| AC2 | Not Run | node一致による単一host参加のend-to-end実行はdecoy条件1と非両立のため未実施。gate自体（`is_apply_target and not ansible_check_mode`）は静的確認PASS |
| AC3 | PASS（一部Not Run） | operation未指定/未知値のreject guardは実測PASS。node/confirmation異常系はコード読解に留め実行未実施（残存リスク） |
| AC4 | Not Run | 実host（monnie）通常実行はrequirement非ゴール |
| AC5 | PASS | `operation=update --check`でdestructive block未到達・副作用ゼロを実測 |
| AC6 | PASS | `operation=rollback --check`でdestructive block未到達・副作用ゼロを実測。backup候補ありの分岐は環境上（ansyにPrometheus実体なし）Not Run |
| AC7 | PASS | Ubuntu/Prometheusとも旧`dry_run`/`rollback`、未指定、未知値がfacts収集より前にrc非ゼロで停止することを実測 |
| AC8 | PASS | 旧語掃引で有効な残存ゼロを確認 |
| AC9 | PASS（静的部分のみ） | catalogのYAML定義自体のexact shapeはPASS。実Semaphore readbackはrequirement Step5（Yoshinobu実施）でNot Run |
| AC10 | PASS（ansible-lintは読み替え） | syntax/tester-gate lint/doc consistency/diff checkはrc=0。ansible-lintはリポジトリ全体の既存debtにより素のrc=0を達成できないため、本差分が新規違反を持ち込んでいないことを行番号照合で確認する形に読み替えた |

## 未実施項目とその理由

- AC1・AC4の実host通常実行、AC9の実Semaphore readback: requirement非ゴール／Step5でYoshinobuが実施。
- AC2・AC3のnode一致による単一host参加end-to-end実行: decoy条件1（実host名を書かない）と両立しないため構造的にNot Run。
- Prometheus rollbackのbackup候補ありでの`discover_backups.yml`到達後の分岐: ansyにPrometheus実体が無く`current_readable=false`となるため、discovery自体は非該当分岐で終わった。backup発見〜check mode下での`rollback_available`遷移は未実測。

## 残存リスク

- **AC3のnode/confirmation異常系（未知node、confirmation不一致、複数host参加）は実行検証していない**。Reviewerの静的レビュー（005〜007）でも同様に実host非到達のためコード読解に留まっており、この経路は本案件を通じて一度も実行検証されていない。ロジック自体は本差分で変数名以外変更されていないため回帰リスクは低いと判断するが、確定情報ではない。
- Prometheus rollbackの「backup候補あり」分岐（`discover_backups.yml`が実際にcandidateを見つけ、`--check`下で`rollback_available`になる経路）は本検証で通していない。update側の`--check`副作用ゼロは実測PASSだが、rollback側は「discoveryへ到達しない」ケースのみの確認である。
- 実Semaphore 5 template・2 scheduleのreconcile/readback、通常inspectのSlack通知観測はrequirement Step5でYoshinobuが実施するまで未完了。

対象実装は無変更。次の指示を待ちます。
