# Code Review: 規範文書3件(requirements-analysis SKILL / ansible-implementation-style SKILL / status.md)

日付: 2026-08-03(JST) / 実施: Reviewer(独立subagent、`skills/document-norm-review/SKILL.md`適用)
対象: `git diff --cached`(commit未実施)3ファイル。実ホストへは接続していない。

### Summary

3件の規約明文化・移設のうち、`requirements-analysis/SKILL.md`の2節と`status.md`の該当2行削除は現物確認と一致し問題を検出しなかった。一方、`ansible-implementation-style/SKILL.md`のJST節には**参照先の実在性チェックで反例が見つかる誤った一意性主張**と、**掃引に使う正規表現自体が壊れていて自分自身の例示すら拾えない**という2件のCriticalがある。どちらも「検証した感覚を伴うが対象を誤る」型の欠陥であり、コミット前に直すべき。

### Critical Issues

| # | File | Line | Issue | Severity |
|---|------|------|-------|----------|
| 1 | `skills/ansible-implementation-style/SKILL.md` | 76 | 「Jinjaの`strftime`フィルタは既定が`utc=False`…repo内で唯一これに該当するのは`roles/proxmox_snapshot_check`」という一意性の主張が誤り。`playbooks/recovery_monitoring_check.yml:103`と`:137`が同じAnsible組み込み`strftime`フィルタを`utc=True`指定なしで使っており(`hosts: control_nodes`、コントローラのローカルTZに暗黙依存)、同一クラスの2件目である。実際、同ファイルのコメント(L96-99)はこのSKILL節とほぼ同じ理由(「%zは実オフセットを出すため嘘にならない」)を独自に書いており、**同じ設計判断が2箇所に分散している状態**。この一意性主張を根拠に`status.md`は`proxmox_snapshot_check`の行だけを残し、`recovery_monitoring_check.yml`側には対応する既知事項が無い。掃引の選定漏れであり、SKILL.mdの記述と`status.md`の両方が対象を取りこぼしている | Critical |
| 2 | `skills/ansible-implementation-style/SKILL.md` | 70 | クラス2(ローカル時刻にリテラル`Z`を貼る)を掃くための例示パターン`grep -rn "date +.*SZ\""`が、**同じ節が3行上で挙げている詐称の例(`date '+%Y-%m-%dT%H:%M:%SZ'`、単一引用符)にすらマッチしない**。このパターンは「`SZ`の直後にダブルクォート`"`が続く」ことを要求するため、単一引用符で囲んだ形にも、無引用の形(`date +%Y-%m-%dT%H:%M:%SZ`)にも一致しない。実測: `echo "date '+%Y-%m-%dT%H:%M:%SZ'" \| grep 'date +.*SZ"'` はrc=1(不一致)。repo全体に対する実行も0件だったが、これは「クラス2が存在しない」ことの確認ではなく「パターンが壊れているため0件になっている」ケースであり、依頼文が要求した区別のとおり後者である。このままではSKILL.mdを読んだImplementer/Reviewerがこのコマンドをコピペしても検出できない | Critical |

### Suggestions

| # | File | Line | Suggestion | Category |
|---|------|------|------------|----------|
| 1 | `skills/ansible-implementation-style/SKILL.md` | 76 | 上記Critical #1を修正する際、`playbooks/recovery_monitoring_check.yml`を対象に含めるか、少なくとも「他に類似箇所が無いか」の断定を弱める表現に直す | correctness |
| 2 | `skills/ansible-implementation-style/SKILL.md` | 70 | 掃引パターンを`grep -rn "SZ['\"]\?\s*)"`のように引用符の有無・種類を問わない形にするか、「ダブルクォートで閉じている場合の例」と明示してスコープを限定する | correctness |

### What Looks Good

- **`skills/requirements-analysis/SKILL.md`の2節**: 「配備物側にも残っていないこと」節・「索引更新」節とも、引用している案件記録(`tester_mode_full_removal/2026-08-02_012_audit_r10.md`、`incident_auto_capture_step2/progress.md` A-11)・パス(`docs/ai/context/operations/code-delivery-to-production.md`、`playbooks/README.md`)はすべて実在を確認した。`role-map.md`/`playbook-map.md`廃止(2026-07-29)も`git log --diff-filter=D`で実commit(`b8abfc8`, 2026-07-29 09:40 JST)を確認済み。`docs/ai/roles/auditor.md` L61の「Ansible Contextを読まない」という引用も現物と一致。いずれも指示として明確で、その場で従える形になっている。
- **`recovery-loki-helper`の両方向の実例**: `rfc3339_utc()`(L88-89)がLoki問い合わせ側で`astimezone(timezone.utc)`してから`Z`を付け、人間向け出力側(L205-206)は`astimezone(JST)`してから`+09:00`を付けている。SKILL.mdの記述と現物のコードが完全に一致する。
- **AR-085 / AR-100(`autonomous_recovery_policy.md` L185, L268)との関係**: 移設後もこの2箇所は残っており、内容はSKILL.md新節と重複していない。AR-085は「通知はJSTで行う」という振る舞い要件、AR-100は「Loki横断ログ調査の時刻パラメータはJST固定解釈とする」という入力解釈要件で、いずれも「JSTで書け/読め」という**規範**であり、SKILL.mdの新節が扱う「**どう書けば詐称にならないか**」という実装手順とは層が異なる。矛盾も二重の正本化も無い。
- **`docs/ai/status.md`から削除した3行**: いずれも対応する内容が新設2節・1節へ移設されており、規律3(完了したら行を消す)に沿った削除。`proxmox_snapshot_check`の行を残す判断(未修正のまま様子見)自体は妥当だが、Critical #1のとおり同種の2件目が存在するため、その行の記述(「repo内で唯一このクラス」)は本diffの結果として不正確になった。
- **auto-memory移設の宣言**: repo外の変更であり本diffには現れないため検証対象外。ポインタ化されているという主張はここでは確認しようがない旨を明記する(未確認)。

### Verdict

Request Changes(Critical 2件: 一意性主張の誤り、掃引パターンの不備)

---

## Coordinator の処置(2026-08-03)

**Critical 2件とも自分で現物を確認し、指摘が正しいことを確かめたうえで是正した。** どちらも Coordinator が書いた記述の誤りであり、実装側の問題ではない。

| 指摘 | 確認したこと | 是正 |
|---|---|---|
| **Critical #1** — 「Jinjaの`strftime`は repo内で唯一 `proxmox_snapshot_check` が該当」は誤り | `grep -n strftime playbooks/recovery_monitoring_check.yml` → L103 / L137 が同じ組み込み`strftime`を`utc=True`なしで使っている。同ファイル L96-99 には**新設節とほぼ同じ理由づけのコメントが独自に書かれていた** | Skill の記述を「2箇所」へ改め、両方のパスを挙げた。あわせて `docs/ai/status.md` の該当行を1件ぶんの記述から2件へ書き換え、**「唯一」と書いた誤りとその検出経緯を行内に残した**(同じ誤りを再生産しないため) |
| **Critical #2** — 掃引パターン `date +.*SZ"` が単一引用符の形を拾わない | fixture(3通りの引用形)を作って実測。元パターンは二重引用符の形しか一致せず、**同じ節が3行上で挙げている詐称の例そのものに一致しなかった** | パターンを `grep -rnE "date [^|]*%SZ"` へ差し替え、3通りとも拾うことをfixtureで確認。あわせて「**引用符の形を決め打ちしたパターンを書かない**」を明文化し、リテラル`+09:00`側の掃引も対にして載せた |

### 自分の検証手順にも欠陥があった(記録として残す)

是正の過程で掃引を repo へ掛けた際、**直前の fixture テストで `cd` したスクラッチパッドが作業ディレクトリとして残っており、`roles/` `playbooks/` `scripts/` が存在しない場所で grep していた。** `2>/dev/null` が "No such file or directory" を握り潰したため、**結果は「0件」として返り、掃引が通ったように見えた。**

正しいディレクトリで引き直した結果は、クラス2が0件(パターン修正後)、リテラル`+09:00`側が7件でいずれも良性(コメント・ドキュメント・変換後に付けている実装)だった。

**この失敗は、本レビューの Critical #2 と同じクラスである** — 「0件」が「無い」ではなく「探せていない」を意味していた。`docs/ai/memory/lessons/distinguish-nothing-found-from-not-run.md` の実例がもう1件増えた形になる。**掃引の結果を根拠にするときは、パターンだけでなく検索対象が実在したかも確かめること。**

### 再監査は行わない

是正はすべて記述の訂正であり、実装の変更を含まない。Reviewer が What Looks Good として確認済みの範囲(`requirements-analysis` の2節、`recovery-loki-helper` の両方向実例、`autonomous_recovery_policy.md` との非矛盾、status.md の3行削除)には手を入れていない。
