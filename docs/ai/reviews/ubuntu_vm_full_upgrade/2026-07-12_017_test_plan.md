# ubuntu_vm_full_upgrade 通知バージョン差分表示（test_plan）

- 日付: 2026-07-12
- 起案: claude（要件定義・ハブ役）／承認: Yoshinobu
- 対象: requirement 014 / implement 015 / review 016（must-fixなし。
  suggestion「表示上限の正整数assert」は2026-07-12にYoshinobu判断で棄却——
  既定値20の通常運用に影響なく、上書き運用も現状想定しないため。
  棄却理由はfinalにも記録する）
- 実施担当: T-1/T-3/T-4 = tester、T-2 = Yoshinobu（Slack UI目視）

## 前提

- 対象playbookは `# tester-gate: check-mode-native`。testerの実行は
  `scripts/safe-ansible-check.sh` + `--check` 必須（core.md §18.5/§18.6）
- `dry_run=true` の月次判定経路は read-only（apt-get update/check/-s のみ）で、
  共通判定パスは `check_mode: false` のため `--check` でも実データで完走し、
  集約通知も実際に送信される（Phase A実績と同一の実行様式）
- implementerのlocalhost検証（implement 015 §確認3）で、テンプレートの
  桁揃え・(新規)/(remove)・上限切り詰め・候補0件時の非表示・3通知全種の
  レンダリングは実装そのものの逐語抽出で検証済み。本test_planは
  「実ホストの実データ＋実Slack配信」で残りを確認する

## T-1: 月次dry-run経路の実データ実行（tester）

```bash
scripts/safe-ansible-check.sh playbooks/ubuntu_vm_full_upgrade.yml \
  -e dry_run=true --check
```

期待:

- 4ノード（ansy/monnie/authy/quory）で simulate が完走し、レポートJSONが
  保存される
- 集約通知が送信され、候補のあるノードには「候補: install=N remove=N」行の
  直後にコードフェンスのバージョン一覧（`<pkg> <旧> → <新>`）が出る
- 候補0件・healthcheck失敗のノードはブロックなしの従来表示
- 件数行・Status行・product_impact行の形式は従来どおり（リグレッションなし）

## T-2: Slack受信側UIの目視（Yoshinobu）

- コードフェンスが等幅で描画されるか（桁揃えが崩れないか）
- リテラル ``` が見えるだけの場合も1行1パッケージで可読性が保たれるか
- 描画に問題がある場合の `common_slack` 側修正（`mrkdwn_in` 追加。全playbook
  影響）は本テストの合否に含めず、別判断として起案する

## T-3: レポートJSONとの突合（tester）

- 当日レポートの `inst_detail` / `remv_detail` が通知の一覧と一致する
- `inst_detail` の name 集合が既存 `simulation.inst`（名前リスト）と一致する
- 既存フィールドは名称・構造とも従来レポート（Phase A/B期の
  `ansy_20260712T073123.json` 等）と互換

## T-4: 実バージョン値の妥当性確認（tester）

- 任意の1ノードで `apt list --upgradable`（read-only）を確認し、通知/JSONの
  旧→新バージョンが実際の候補と一致する

## スコープ外（本test_planでは実施しない）

- applyサマリ通知の実機確認: apply実行を伴うため、次回apply
  （Phase C等、Yoshinobu実行）の事後検証に含める。テンプレートの
  レンダリング自体はimplementer検証で確認済み
- 上限20行超の実機再現: 実データ依存のため必須にしない（localhost検証済み）

## 合否基準

- T-1 完走（failed=0）+ T-3/T-4 一致 → 合格。T-2 は Yoshinobu の目視確認で
  クローズ（描画問題は別起案でありテスト不合格とはしない）
- 想定外の挙動: 即中断し claude へ返す

## Next step files

- docs/ai/reviews/ubuntu_vm_full_upgrade/2026-07-12_017_test_plan.md（本ファイル）
- docs/ai/reviews/ubuntu_vm_full_upgrade/2026-07-12_018_test_result.md（tester が作成）
