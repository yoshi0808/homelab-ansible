# ubuntu_vm_full_upgrade host_vars反映 — 簡易レビュー結果

対象:

- `docs/ai/reviews/ubuntu_vm_full_upgrade/2026-07-12_012_implement.md`
- `inventories/homelab/host_vars/ansy.yml`
- `inventories/homelab/host_vars/quory.yml`
- `docs/ai/reviews/ubuntu_vm_full_upgrade/2026-07-11_009_test_result.md` A-4

## 結論

**機能上合格。must-fix / suggestionなし、nit 1件。**

両hostの変数名と値はrole実装およびA-4実測結果に一致し、quory.ymlの既存接続設定にも
影響していない。

## 確認結果

### 変数名とrole分岐

追加値:

```yaml
ubuntu_vm_full_upgrade_ansible_managed_by: apt
```

role defaultは同名の
`ubuntu_vm_full_upgrade_ansible_managed_by: unknown`、classify.ymlは値が `apt` の場合に
「apt管理: 判定対象」を表示する。host_varsの優先順位でdefaultを正しく上書きするため、
実装との齟齬はない。

### A-4実測値

test result A-4は次を確定している。

- ansy: `/usr/bin/ansible`、dpkg owner=`ansible-core`、Ansible core 2.20.1 → apt管理
- quory: 同上 → apt管理

両host_varsの値 `apt` はこの確定値およびtest result記載の提案値と一致する。

### quory既存内容

既存の次の式は内容変更されていない。

```yaml
ansible_connection: "{{ 'local' if lookup('pipe', 'hostname -s') == 'quory' else 'ssh' }}"
```

新変数は独立したtop-level YAML keyとして後続に追加され、接続方式の評価や既存コメントへ
影響しない。

## 指摘

### [nit] 両YAMLファイルに末尾改行を追加する

`git diff`ではansy.yml/quory.ymlとも最終行に `No newline at end of file` が示される。
YAML解釈やAnsible動作には影響しないが、通常のテキストファイル規約と今後のdiff可読性の
ため末尾改行を付けるとよい。

## 静的確認

```text
git diff --check -- inventories/homelab/host_vars/ansy.yml inventories/homelab/host_vars/quory.yml
  OK
```

実ホストへの接続・変更は行っていない。

