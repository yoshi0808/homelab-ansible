# ubuntu_vm_full_upgrade quory QDevice unit name fix (review)

- Date: 2026-07-13
- Reviewer: reviewer
- Test result: `docs/ai/reviews/ubuntu_vm_full_upgrade/2026-07-12_027_test_result.md`
- Implementation: `docs/ai/reviews/ubuntu_vm_full_upgrade/2026-07-13_029_implement.md`
- Scope:
  - `roles/ubuntu_vm_full_upgrade/defaults/main.yml`
  - `roles/ubuntu_vm_full_upgrade/tasks/reboot_quory.yml`
  - `scripts/quory_post_upgrade_check/quory-post-upgrade-check.sh`

## Conclusion

No `must-fix`, `suggestion`, or `nit` findings. The replacement of the quory QDevice server unit/package name with `corosync-qnetd` is complete. Check behavior, retry timing, notification structure, and non-quory configuration are unchanged. This diff is ready to commit.

## Review details

### 1. Replacement completeness

Searches across `roles/ubuntu_vm_full_upgrade/` and `scripts/quory_post_upgrade_check/` confirm that every executable or evaluated reference now uses `corosync-qnetd`:

- `wait_for_active` unit argument
- captured status variable (`qnetd_status`)
- CRITICAL condition and message
- Slack body label
- quory apply-summary description
- quory important-package pattern
- quory QDevice product-impact pattern

There are no remaining `qdevice_status` references. The only remaining `corosync-qdevice` strings are intentional comments explaining that it is the pve1/pve2 cluster-node daemon and does not exist on quory; they are not execution targets.

### 2. Logic, retry, and notification invariance

The executable shell change is limited to the unit name and matching variable/message labels:

- `wait_for_active corosync-qnetd 3 5` retains 3 attempts and a 5-second interval.
- Check order remains chrony, qnetd, then Semaphore.
- `wait_for_active`, chrony `waitsync`, critical aggregation, overall/color selection, Slack JSON/curl, and flag cleanup are unchanged.
- `reboot_quory.yml` changes one factual label in the existing message. Channel, status, body structure, reboot scheduling, rescue handling, and gates are unchanged.

### 3. Defaults impact

The important-package change is only under `ubuntu_vm_full_upgrade_important_per_node.quory`. `classify.yml` selects this mapping by `inventory_hostname`, so authy, monnie, and ansy are unaffected.

The product-impact change is only under `ubuntu_vm_full_upgrade_products.quory` in the QDevice product. Product impact is also selected by `inventory_hostname`. Common patterns and every other node's mappings are unchanged.

## Verification

- `bash -n scripts/quory_post_upgrade_check/quory-post-upgrade-check.sh`: OK.
- `git diff --check` for the three scoped files: OK.
- `ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote ansible-playbook playbooks/ubuntu_vm_full_upgrade.yml --syntax-check`: OK.
- `bash scripts/check-tester-gate.sh`: OK (33 playbooks).
- Reference search: no old executable unit name or old status variable remains.
- No deployment to quory, service execution, or Slack delivery was performed during this code review.

## Remaining step

As recorded in the implementation report, manual deployment of the corrected script/service to quory and an on-host post-upgrade check remain for the test/operations phase.

## Next step files

- `docs/ai/reviews/ubuntu_vm_full_upgrade/2026-07-12_027_test_result.md`
- `docs/ai/reviews/ubuntu_vm_full_upgrade/2026-07-13_029_implement.md`
- `docs/ai/reviews/ubuntu_vm_full_upgrade/2026-07-13_030_review.md`
