# ubuntu_vm_full_upgrade 通知バージョン差分表示（test result）

- 日付: 2026-07-12
- 担当: tester
- test_plan: `2026-07-12_017_test_plan.md`
- 判定: **不合格（再実行で T-1/T-3 合格、T-4 に候補集合の不一致）**

## 結論

T-1 は4ノードの healthcheck / simulation / classify / 新規version detail
block構築まで成功したが、その直後の `Ensure report directory exists`
で、`delegate_to: localhost` 用 temporary directory の作成に失敗した。

test_plan の「想定外の挙動: 即中断し claude へ返す」に従い、再実行と
T-3 / T-4 は行っていない。playbook 内の破壊的操作は実行されていない。

## T-1: 月次 dry-run 経路

当初コマンド:

```bash
scripts/safe-ansible-check.sh playbooks/ubuntu_vm_full_upgrade.yml \
  -e dry_run=true --check
```

Ansible 起動時に sandbox 内の read-only `~/.ansible/tmp` で失敗し、
playbook task へは到達しなかった。playbook 動作を変えず、一時パスのみ
`/tmp` に置き換えて再開した。

```bash
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local \
ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote \
scripts/safe-ansible-check.sh playbooks/ubuntu_vm_full_upgrade.yml \
  -e dry_run=true --check
```

成功した範囲:

- 4ノードの facts gathering
- authy / monnie の専用 healthcheck: `OK`
- ansy / quory の generic healthcheck: critical なし
- 4ノードの full-upgrade simulation: `ok`
- simulation JSON parse / Status・important分類: `ok`
- `Build per-package version detail block for notifications`: 4ノードとも `ok`

中断点:

```text
TASK [ubuntu_vm_full_upgrade : Ensure report directory exists]
fatal: [ansy -> localhost]: UNREACHABLE
fatal: [authy -> localhost]: UNREACHABLE
fatal: [monnie -> localhost]: UNREACHABLE
fatal: [quory -> localhost]: UNREACHABLE

Failed to create temporary directory:
/tmp/ansible-remote/ansible-tmp-...
```

事後の read-only 確認で、コントローラ上のパス所有者は次の状態だった。

```text
drwx------ yoshi:yoshi /tmp/ansible-local
drwx------ ann:ann     /tmp/ansible-remote
```

`delegate_to: localhost` は yoshi で実行されるため、ann mode `0700` の
`/tmp/ansible-remote` 以下を作成できない。これはversion detail実装の
assert/レンダリング失敗ではなく、tester実行環境の一時パス衝突である。

play recap:

```text
ansy   : ok=23 changed=1 unreachable=1 failed=0
authy  : ok=27 changed=1 unreachable=1 failed=0
monnie : ok=27 changed=1 unreachable=1 failed=0
quory  : ok=23 changed=1 unreachable=1 failed=0
```

## T-2

対象外（Yoshinobu の Slack UI 目視）。T-1 が通知送信前に中断したため、
今回の受信対象となる新規通知はない。

## T-3 / T-4

**未実施。** T-1 の即時中断条件に従った。当日レポートは保存されておらず、
レポートJSON・通知・`apt list --upgradable` の三者突合はできない。

## 再開条件

Ansible の local/remote temp を別々の書き込み可能なパスに分離して
T-1 から再実行する。例えば remote temp を共有の親directoryではなく
Ansible が実行ごとに固有directoryを作れる設定にする。再実行には
claude / Yoshinobu の指示を待つ。

## 追記: temporary directory 分離後の再テスト

claude の再開指示により、remote temp の `$USER` をターゲット側shellで
展開させ、リモートの `ann` と `delegate_to: localhost` の `yoshi`
でパスを分離して T-1 から再実行した。

```bash
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local \
ANSIBLE_REMOTE_TEMP='/tmp/ansible-remote-$USER' \
scripts/safe-ansible-check.sh playbooks/ubuntu_vm_full_upgrade.yml \
  -e dry_run=true --check
```

### T-1 再実行: 合格

- 4ノードの healthcheck / simulation / classify / version detail block: 成功
- 当日レポート4件を保存:
  - `reports/ubuntu-vm-full-upgrade/ansy_20260712T111841.json`
  - `reports/ubuntu-vm-full-upgrade/authy_20260712T111841.json`
  - `reports/ubuntu-vm-full-upgrade/monnie_20260712T111841.json`
  - `reports/ubuntu-vm-full-upgrade/quory_20260712T111841.json`
- `Send Slack notification`: `ok`
- `Manual apply (skipped under --check; destructive)`: 4ノードすべて skip
- full-upgrade / reboot の実適用なし

play recap:

```text
ansy      : ok=33 changed=2 unreachable=0 failed=0
authy     : ok=29 changed=1 unreachable=0 failed=0
monnie    : ok=29 changed=1 unreachable=0 failed=0
quory     : ok=25 changed=1 unreachable=0 failed=0
localhost : ok=1  changed=0 unreachable=0 failed=0
```

T-1 の test_plan 条件（4ノード完走、report保存、通知送信、
apply 不発火）は満たした。通知の Slack UI 描画は T-2 として
Yoshinobu の目視対象。

### T-3: 合格

実行した機械突合:

```bash
for f in reports/ubuntu-vm-full-upgrade/*_20260712T111841.json; do
  jq -e '([.inst_detail[].name] | sort) == (.inst | sort)
         and ([.remv_detail[].name] | sort) == (.remv | sort)' "$f"
done

jq -r 'keys[]' reports/ubuntu-vm-full-upgrade/ansy_20260712T111841.json

ssh -F /dev/null -i ~/.ssh/id_ann ann@quory.internal \
  'sudo -n jq -r "keys[]" \
   /home/yoshi/homelab-ansible/reports/ubuntu-vm-full-upgrade/ansy_20260712T073123.json'
```

結果:

| node | `inst` | `inst_detail` | `remv` | `remv_detail` | name集合 |
| --- | ---: | ---: | ---: | ---: | --- |
| ansy | 0 | 0 | 0 | 0 | 一致 |
| authy | 30 | 30 | 0 | 0 | 一致 |
| monnie | 23 | 23 | 0 | 0 | 一致 |
| quory | 37 | 37 | 0 | 0 | 一致 |

従来report `ansy_20260712T073123.json` の top-level 10 field:

```text
collected_at, host, inst, inst_important, product_impact,
reboot_expected, remv, remv_classified, status, status_reason
```

は新reportですべて同名のまま残り、`inst_detail` / `remv_detail` の2 field
だけが追加されている。通知blockは、reportに保存したものと同一の
`ubuntu_vm_full_upgrade_sim.inst_detail` / `.remv_detail` から事前レンダリングされ、
件数も一致する。Slack受信側の本文目視は T-2 の境界に残した。

### T-4: 不合格（即時中断）

候補がある `monnie` を任意の1ノードとして、read-only の
`apt list --upgradable` と当日reportを突合した。

```bash
ssh -F /dev/null -i ~/.ssh/id_ann ann@monnie.internal \
  'LC_ALL=C apt list --upgradable 2>/dev/null'
```

- reportの `inst_detail` 23件は、すべて `apt list --upgradable` の
  package name / old version / new version と一致した。
- ただし `apt list --upgradable` は25件を表示し、次の2件が
  simulation / JSON / 通知候補に存在しなかった。

```text
python3-software-properties  0.120 → 0.120.1
software-properties-common  0.120 → 0.120.1
```

つまり「JSONに載った23件のversion値」は正しいが、test_plan T-4 が
求める通知/JSONと実際候補の集合は完全一致しない。想定外の挙動として
この時点で中断し、追加の apt 調査は行わなかった。

### 最終判定

**不合格。** T-1 と T-3 は合格。T-4 は表示された23件の
old/new version自体は全件正しいが、`apt list --upgradable` の25件と
候補集合が一致しないため、test_planの合否基準を満たさない。

未検証事項:

- T-2: Slack UI のコードフェンス/等幅描画（Yoshinobu 担当）
- apply summary 通知の実機確認（test_plan のスコープ外）
- `apt list` だけに現れる2件を `apt-get -s full-upgrade` が候補としない理由
