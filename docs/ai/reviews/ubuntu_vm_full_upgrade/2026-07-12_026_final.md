# Final — ubuntu_vm_full_upgrade 通知改修（第1弾+第2弾）

この内容で確定。

確認者: Yoshinobu
日付: 2026-07-12

## 対象

- 第1弾: 通知へのパッケージ別バージョン差分表示（014_requirement 〜 018_test_result）
- 第2弾: phasing保留表示（件数整合）・#patches化・ノード単位通知・表示上限100
  （019_requirement改訂版 〜 025_test_result）
- 変更ファイル: `roles/ubuntu_vm_full_upgrade/` 配下
  （files/ubuntu-full-upgrade-simulate.sh / defaults/main.yml / tasks/main.yml /
  tasks/classify.yml / tasks/reboot_local.yml / tasks/reboot_quory.yml）
  および `playbooks/ubuntu_vm_full_upgrade.yml`（ヘッダの最終仕様コメント更新）

## 経緯サマリ

- 発端: Phase B（ansy実適用）合格後の Yoshinobu フィードバック
  「何がどう変わるのか分からず、事後に何を検証すべきか判断できない。
  バージョンが判明しないと調査しようがない」
- 第1弾実装で npm outdated 風の `pkg 旧 → 新` 表示を追加。レビュー016は
  must-fix なし
- 第1弾テスト（018）T-4 で monnie の通知候補23件 vs `apt list --upgradable`
  25件の不一致 → 原因は Ubuntu phased updates（保留2件、apt-cache policy で
  `phased 10%` を裏取り）。Yoshinobu 判定「件数不一致は NG」で不合格とし、
  第2弾要求（019）へ拡張
- 第2弾: phasing保留の表示による件数整合、通知チャネルを proxmox 同様の
  #patches へ、集約1通からノード単位通知へ分割、表示上限20→100
  （MAJOR閾値と同値）。レビュー021の must-fix（保留分のバージョン欠落）は、
  019 の制約を「read-only 補助収集（apt-cache policy 等）のみ許可」に改訂して
  解決。suggestion（NO_UPDATES 理由文言）採用、023 の nit（旧コメント残存）
  対応済み
- 第2弾テスト（025）: T-1/T-3/T-4/T-5 全合格。monnie 実機で
  upgrade 23 + phased 2 = apt list 25 の名前・バージョン完全一致。
  T-2（Slack UI 目視）も 2026-07-12 に Yoshinobu が4点
  （#patches 4通 / 保留行のバージョン表示 / 等幅描画 / 他N件非表示）を確認

## 棄却した指摘の記録

- review 016 suggestion「表示上限変数の正整数 assert 追加」: 棄却
  （Yoshinobu 判断 2026-07-12）。既定値の通常運用に影響がなく、変数を
  上書きする運用も現状想定しないため。reviewer へも agmsg で通知済み

## コメント

- 月次dry-run通知は「ノード単位・#patches・全件表示」で確定。
  レポートJSON（`reports/ubuntu-vm-full-upgrade/`、gitignore対象）は
  障害切り分け専用の位置付けとする
- phasing 保留分は Status 判定・important 分類に含めない（表示のみ）。
  full-upgrade と `apt list --upgradable` の集合差は phased で説明される
  （運用知見）
- applyサマリ通知（同じくバージョン一覧・#patches化済み）の実機確認は、
  次回 apply（Phase C 等）の事後検証で行う
- 未消化の派生論点: Slack 通知の等幅描画は今回の実機確認で問題なしと確認
  されたため、common_slack への `mrkdwn_in` 追加は不要と判断
