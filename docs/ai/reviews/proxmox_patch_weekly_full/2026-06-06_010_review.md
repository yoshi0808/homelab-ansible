# Review: proxmox_patch_weekly_full extra vars JSON 例修正

日付: 2026-06-06
対象: `2026-06-06_009_implement.md` 対応後の git diff

## Findings

なし。

High / Medium / Low 相当の問題は見つからない。

## 確認できたこと

- `playbooks/proxmox_patch_weekly_full.yml:83-84` の override 例は、前回レビュー `2026-06-06_008_review.md` の指摘どおり JSON 形式に修正されている。
- 修正後の例:
  ```bash
  -e '{"proxmox_patch_weekly_full_allowed_controllers":["<hostname>"]}'
  ```
- JSON 形式の extra vars は Ansible で list 型として解釈されることを確認した。
- `proxmox_patch_weekly_full_allowed_controllers` は引き続き play vars で `['quory']` として定義されており、要求仕様の「quory（外部 control node）からのみ実行する」に沿っている。
- preflight の3段階チェックは維持されている:
  - クラスタ上の QEMU VM からの実行を拒否
  - `pve1` / `pve2` 自身からの実行を拒否
  - allowlist にない controller からの実行を拒否

## 実行確認

```bash
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local TMPDIR=/tmp ansible-playbook --syntax-check -i inventories/homelab/hosts.yml playbooks/proxmox_patch_weekly_full.yml
```

結果: OK

```bash
git diff --check
```

結果: OK

追加確認:

```bash
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local TMPDIR=/tmp ansible localhost -i localhost, -c local -m debug -a 'msg={{ x | type_debug }}:{{ x }}' -e '{"x":["quory"]}'
```

結果: `list:['quory']`

## 補足

- `2026-06-06_008_review.md` の Low 指摘は解消されている。
- 今回の差分は、運用者向けエラーメッセージの正確性を改善するもので、要求仕様・policy との大きな不整合は見当たらない。
