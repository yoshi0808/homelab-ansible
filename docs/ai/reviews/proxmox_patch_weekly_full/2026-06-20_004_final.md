# Final: proxmox_patch_weekly_full — dry-run スキップ時のジョブ終了コード調整

この内容で確定。

確認者: Yoshinobu
日付: 2026-06-20

## 対象

- `playbooks/proxmox_patch_weekly_full.yml`（Step 2b）

## レビュー結果（003_review）

- must-fix: なし。「commit 可」。
- nit: 先頭 Flow コメントが旧挙動のまま → **反映済み**（コメント更新）。
- suggestion: untracked の `test-output/` → **実際に未 ignore だったため `.gitignore` に追加**（スクショ漏洩防止）。

## トリアージ（§15）

- nit / suggestion とも妥当と判断し反映。棄却した指摘なし。
- 追加分はコメントと `.gitignore` のみ（ロジック不変）のため再レビューは省略（nit ping-pong 回避）。

## コメント

- `MAINTENANCE_REQUIRED` / `MAJOR_UPGRADE_DETECTED`（dry-run が warning 通知済み）は
  clean skip + rc=0。`BLOCKED` / `UNKNOWN` は fail 維持。VM 構成チェックは未変更（Error のまま）。
- 実機 dry-run での挙動確認（tester 工程）は今回スコープ外（syntax-check + レビューで確定）。
