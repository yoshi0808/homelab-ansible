# Review: proxmox_patch_weekly_full NO_UPDATES 重複通知・不要接続対応

日付: 2026-06-06
対象: `2026-06-06_014_implement.md` 対応後の git diff

## Findings

### Low: `NO_UPDATES` 通知元に関するコメント・タスク名が実装とずれている

- 該当:
  - `playbooks/proxmox_patch_weekly_full.yml:117`
  - `playbooks/proxmox_patch_weekly_full.yml:128`
  - `playbooks/proxmox_patch_weekly_full.yml:253`
- 状況:
  - 今回の修正で、weekly full の Step 2b から NO_UPDATES メール送信は削除され、通知は `proxmox_patch_dryrun` role 側に一本化された。
  - しかし Step 2b のコメントは `NO_UPDATES → send notification email and end playbook cleanly` のまま。
  - Step 2b のタスク名も `Notify NO_UPDATES and end playbook cleanly` のまま。
  - Step 11 の skip タスクも `notification already sent in Step 2b` と書いているが、実際の通知元は Step 2a で import される dry-run role。
- 影響:
  - 実行挙動そのものは問題ない。
  - ただし、将来の保守時に「Step 2b が通知している」と誤読され、二重通知の修正が戻ってしまう可能性がある。
- 対応案:
  - コメントを `NO_UPDATES → dry-run role has already notified; set skip flag and end cleanly` のように修正する。
  - タスク名を `Set NO_UPDATES skip flag and end play cleanly` などに変更する。
  - Step 11 の文言も `notification already sent by dry-run role` に変更する。

## 確認できたこと

- `playbooks/proxmox_patch_weekly_full.yml:128-136` から NO_UPDATES メール送信処理は削除されており、前回レビュー `2026-06-06_013_review.md` の Medium 指摘である二重通知は解消されている。
- `playbooks/proxmox_patch_weekly_full.yml:165-177` と `playbooks/proxmox_patch_weekly_full.yml:215-227` で `gather_facts: false` + skip guard + 明示 `setup` に変更されており、NO_UPDATES 経路での不要な fact gathering は避けられる。
- `proxmox_healthcheck` role は `ansible_facts['date_time']` を使うため、PATCH_READY 後の post-healthcheck では明示 `setup` が必要であり、今回の追加は妥当。
- `_weekly_full_no_updates` skip flag により、Steps 3/4/6/7/8/10 の imported playbook と Step 11 summary mail は NO_UPDATES 経路でスキップされる。

## 実行確認

```bash
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local TMPDIR=/tmp ansible-playbook --syntax-check -i inventories/homelab/hosts.yml playbooks/proxmox_patch_weekly_full.yml
```

結果: OK

```bash
git diff --check
```

結果: OK

## 補足

- High / Medium 相当の問題は見つからない。
- 今回の Low 指摘は、挙動ではなく保守時の誤読を避けるための文言整理。
