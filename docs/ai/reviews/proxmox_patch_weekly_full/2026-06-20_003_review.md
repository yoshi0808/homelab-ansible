# Review: proxmox_patch_weekly_full — Step 2b skip handling

## Result

Commit 可。

`MAINTENANCE_REQUIRED` / `MAJOR_UPGRADE_DETECTED` を dry-run warning 通知済みの clean skip として rc=0 終了にする変更は、要求仕様と整合している。
`BLOCKED` / `UNKNOWN` / 想定外 status は引き続き fail になり、`PATCH_READY` の場合だけ後続の evacuate / patch / restore flow に進む。

## Findings

### nit: 先頭の Flow コメントが旧挙動のまま

- 対象: `playbooks/proxmox_patch_weekly_full.yml:13`
- 現状: `dry-run (stop if not PATCH_READY)`
- 実際の変更後挙動: `NO_UPDATES` / `MAINTENANCE_REQUIRED` / `MAJOR_UPGRADE_DETECTED` は stop ではなく clean skip + rc=0。

動作には影響しないが、運用者向けコメントとしては古い。例えば `dry-run (continue only when PATCH_READY; clean-skip for notified non-error statuses)` のように更新すると誤読を避けられる。

### suggestion: untracked の `test-output/` は commit 前に扱いを確認する

- 対象: `test-output/semaphore.png`

今回の playbook diff そのものには含まれていないが、working tree に untracked file として存在する。
`core.md` の「生成物や runtime report を commit 対象に混ぜない」方針に照らし、意図した証跡でなければ commit 対象に含めないことを確認したい。

## 確認したこと

- `docs/ai/reviews/proxmox_patch_weekly_full/2026-06-20_001_requirement.md` と `2026-06-20_002_implement.md` を確認。
- `git diff -- playbooks/proxmox_patch_weekly_full.yml` を確認。
- `_weekly_full_no_updates` から `_weekly_full_skip` への変更は、weekly_full 内の後続 play / task に追従している。
- skip 対象 status では Step 2b で `_weekly_full_skip: true` を set_fact し、後続の import_playbook / post-healthcheck / complete notification が抑止される。
- `BLOCKED` / `UNKNOWN` / 想定外 status は `fail` task に到達する。
- `ANSIBLE_LOCAL_TEMP=/tmp/ansible-local-tmp ansible-playbook --syntax-check -i inventories/homelab/hosts.yml playbooks/proxmox_patch_weekly_full.yml` は成功。

## Notes

初回の syntax check は、sandbox 内で Ansible が `/home/yoshi/.ansible/tmp` を作成できず失敗した。
Ansible の local temp を `/tmp` に向け直して再実行し、構文チェックは通過した。

Next step files:
- docs/ai/reviews/proxmox_patch_weekly_full/2026-06-20_001_requirement.md
- docs/ai/reviews/proxmox_patch_weekly_full/2026-06-20_002_implement.md
- docs/ai/reviews/proxmox_patch_weekly_full/2026-06-20_003_review.md
