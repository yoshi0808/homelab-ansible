# Review: proxmox_restore_vm_placement NO_UPDATES skip guard 追加

日付: 2026-06-06
対象: `2026-06-06_019_implement.md` 対応後の git diff

## Findings

なし。

High / Medium / Low 相当の問題は見つからない。

## 確認できたこと

- `playbooks/proxmox_restore_vm_placement.yml:29-43` の第2 play は、`roles:` 直指定から `tasks:` + `include_role` に変更されている。
- `playbooks/proxmox_restore_vm_placement.yml:34-36` の skip guard により、weekly full の `NO_UPDATES` 経路では restore role 実行前に `meta: end_play` できる。
- `gather_facts: false` に変更されているため、skip guard より前に target node へ fact gathering することも避けられる。
- skip guard 通過後に `ansible.builtin.setup` を明示実行しており、restore role 内で呼ばれる `proxmox_healthcheck` role が必要とする `ansible_facts['date_time']` は確保される。
- standalone 実行では `hostvars['localhost']['_weekly_full_no_updates'] | default(false)` が false 扱いになり、従来どおり restore role が実行される。

## 実行確認

```bash
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local TMPDIR=/tmp ansible-playbook --syntax-check -i inventories/homelab/hosts.yml playbooks/proxmox_patch_weekly_full.yml
```

結果: OK

```bash
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local TMPDIR=/tmp ansible-playbook --syntax-check -i inventories/homelab/hosts.yml playbooks/proxmox_restore_vm_placement.yml -e target_node=pve2
```

結果: OK

```bash
git diff --check
```

結果: OK

## 補足

- 今回の変更は、weekly full の `import_playbook when:` だけでは避けきれない restore role 内 loop/delegate 評価への保険として妥当。
- 実機での `NO_UPDATES` 経路再現までは未実施。レビュー上は構造・構文・既存 role の fact 要件を確認した。
