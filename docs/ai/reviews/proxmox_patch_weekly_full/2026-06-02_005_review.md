# proxmox_patch_weekly_full git diff review

## 結論

重大な追加指摘はありません。

2026-06-02_003_review.md で指摘されていた以下の2点は、今回の差分で解消されています。

- `run_once` + `delegate_to: localhost` の `set_fact` 結果を、localhost ではなく実行元 Proxmox ホストの `hostvars` から参照するよう修正されている
- control node VM が Proxmox クラスタ上に存在する場合の preflight fail が、healthcheck / dry-run / evacuation より前に追加されている

## 確認内容

- `playbooks/proxmox_patch_weekly_full.yml` の Step 2b は、`hostvars[groups['proxmox'][0]]['_final_status']` を参照しており、dry-run role の `run_once` fact 配置と整合しています。
- Step 0 の preflight は、`pvesh get /cluster/resources --output-format json` で QEMU VM を取得し、`proxmox_patch_apply_control_node_name` に一致する VM がクラスタ上にある場合に失敗します。これにより、少なくとも設定上の control node VM がクラスタ内にいる状態で週次フル適用へ進むことは防がれます。
- `inventories/homelab/group_vars/proxmox.yml` に `proxmox_patch_apply_control_node_name: "ansy"` が追加され、preflight から参照可能です。
- playbook の流れは requirement の方針どおり、healthcheck → dry-run 判定 → pve2 evacuate/apply/healthcheck/restore → pve1 evacuate/apply/healthcheck/restore → summary mail になっています。

## 残留リスク

- preflight は「実際に ansible-playbook を起動しているホスト名が quory であること」までは検証していません。現在の要件・ポリシー上の主要な危険は `ansy` から実行して自己退避に巻き込まれることなので、今回の修正範囲としては妥当です。ただし、将来 control node 名が変わる場合は `proxmox_patch_apply_control_node_name` の更新漏れに注意が必要です。
- `playbooks/proxmox_patch_weekly_full.yml` は現時点で untracked です。レビュー対象として内容は確認していますが、コミット時に追加漏れがないよう注意してください。

## 検証

- `ansible-playbook -i inventories/homelab/hosts.yml playbooks/proxmox_patch_weekly_full.yml --syntax-check`
  - 成功
