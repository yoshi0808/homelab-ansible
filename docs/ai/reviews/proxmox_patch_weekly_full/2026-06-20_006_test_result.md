# Test Result: proxmox_patch_weekly_full — Step 2b 終了分岐の検証

## 結論

PASS。

`proxmox_patch_weekly_full.yml` の Step 2b 変更は、dry-run status ごとに期待どおりの
終了分岐になることを確認した。

- `NO_UPDATES` / `MAINTENANCE_REQUIRED` / `MAJOR_UPGRADE_DETECTED`: skip、rc=0
- `BLOCKED` / `UNKNOWN`: fail、rc!=0
- `PATCH_READY`: continue、rc=0

安全制約どおり、実機 pve1 / pve2 には接続していない。`proxmox_patch_weekly_full.yml`
本体の end-to-end 実行も行っていない。実行した Ansible は syntax-check と
`localhost,` inventory の検証用 playbook のみ。

## T1. 構文チェック

実行コマンド:

```bash
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local-tmp \
  ansible-playbook --syntax-check -i inventories/homelab/hosts.yml \
  playbooks/proxmox_patch_weekly_full.yml
```

結果: PASS。

- rc=0
- 出力要約: `playbook: playbooks/proxmox_patch_weekly_full.yml`

## T2. Step 2b 構造の確認

実行コマンド:

```bash
rg -n "_weekly_full_skip_statuses|_weekly_full_skip: true|meta: end_play|when: _status != 'PATCH_READY'|when: not \(hostvars\['localhost'\]\['_weekly_full_skip'\] \| default\(false\)\)" playbooks/proxmox_patch_weekly_full.yml
rg -n "_weekly_full_no_updates" playbooks/proxmox_patch_weekly_full.yml
```

結果: PASS。

確認内容:

- `_weekly_full_skip_statuses` は以下の3値。
  - `NO_UPDATES`
  - `MAINTENANCE_REQUIRED`
  - `MAJOR_UPGRADE_DETECTED`
- skip ブロックは `_weekly_full_skip: true` を `set_fact` し、`meta: end_play` する。
- fail タスクの `when` は `_status != 'PATCH_READY'`。
- 後続の危険操作系 import は `hostvars['localhost']['_weekly_full_skip'] | default(false)` で gate されている。
  - `proxmox_evacuate_node.yml`
  - `proxmox_patch_apply_node.yml`
  - `proxmox_restore_vm_placement.yml`
- 旧フラグ `_weekly_full_no_updates` は残存なし（`rg` rc=1、該当なし）。

補足:

- pve2 / pve1 post-patch healthcheck play と summary mail play にも `_weekly_full_skip` による `meta: end_play` がある。

## T3. 終了分岐ロジックのアサーション

検証用 playbook:

```text
test-output/step2b_status_assert.yml
```

内容:

- `hosts: localhost`
- `gather_facts: false`
- inventory は `localhost,`
- Step 2b と同じ skip status list、skip 判定、fail 判定を使用
- skip 分岐では `assert` 後に `meta: end_play`
- `PATCH_READY` では continue assert を実行

実行コマンド例:

```bash
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local-tmp \
  ansible-playbook -i localhost, test-output/step2b_status_assert.yml \
  -e '{"_final_status":"NO_UPDATES","expected_classification":"skip"}'
```

結果一覧:

| `_final_status` | 期待分類 | rc | 結果 |
|---|---:|---:|---|
| `NO_UPDATES` | skip | 0 | PASS |
| `MAINTENANCE_REQUIRED` | skip | 0 | PASS |
| `MAJOR_UPGRADE_DETECTED` | skip | 0 | PASS |
| `BLOCKED` | fail | 2 | PASS |
| `UNKNOWN` | fail | 2 | PASS |
| `PATCH_READY` | continue | 0 | PASS |

出力要約:

- skip 3件:
  - `Set skip flag for subsequent plays`: ok
  - `Assert skip classification before end_play`: ok
  - `End this play`: 実行
  - recap: `failed=0`
- fail 2件:
  - skip block は skipped
  - `Fail if dry-run status is a genuine error or unexpected`: FAILED
  - fail message は対象 status を含み、`PATCH_READY` または clean skip status が必要であることを表示
- `PATCH_READY`:
  - skip block は skipped
  - fail task は skipped
  - `Assert continue classification`: ok
  - recap: `failed=0`

## 実行時メモ

最初の T3 実行では `ANSIBLE_LOCAL_TEMP` を付け忘れたため、sandbox 内で
`/home/yoshi/.ansible/tmp` が read-only となり rc=5 で失敗した。
これは検証対象の分岐ロジックではなく実行環境の一時ディレクトリ問題である。

以後は T1 と同じく `ANSIBLE_LOCAL_TEMP=/tmp/ansible-local-tmp` を付けて再実行し、
上記のとおり全ケース PASS を確認した。
