# tester_mode 深掘り P1 テスト結果 — 2026-07-02

## 結論

Yoshinobu 承認済みの `2026-07-02_015_test_plan.md` に沿って、全実行に
`-e tester_mode=true` を付けて検証した。

- lint 正常系: OK(29 playbooks)
- 影響 5 playbook の syntax-check: OK
- recovery L2:
  - `authy` の service restart / VM reboot / HA failover は実データ検証後、変更系直前で停止、changed=0
  - ネガティブ 2 件は期待どおり fail、changed=0
  - `recovery_vm_reboot -e target=sophos-fw` は期待と異なり、L2 gate 未到達で fail
- sophos_trim L3:
  - `fstrim --help` で `--dry-run` サポート確認 OK
  - `sophos_trim.yml -e tester_mode=true` は Advanced Shell 経由で `fstrim --dry-run -v` を実行し、RC marker 0、trim_status=SUCCESS
- time_sync_check: OK、changed=0
- regression: `cloudkey_cert_deploy.yml -e tester_mode=true` OK、changed=0

実 trim / 実 restart / 実 reboot / 実 relocate は発生していない。
Slack 実送信も発生していない。

## 実行時の追加条件

実機 SSH / expect 検証では sandbox 制約を避けるため、必要に応じて以下を付与した。

```bash
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local
ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote
ANSIBLE_SSH_CONTROL_PATH_DIR=/tmp/ansible-cp
ANSIBLE_SSH_COMMON_ARGS='-F /dev/null'
```

実機接続を伴う read-only / tester-mode 検証は承認済み escalation で実行した。

## フェーズ 1: 静的検証

### lint

```bash
scripts/check-tester-gate.sh
```

結果:

```text
[tester-gate-lint] OK (29 playbooks)
```

### syntax-check

```bash
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ansible-playbook --syntax-check playbooks/recovery_service_restart.yml -e tester_mode=true -e target=authy
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ansible-playbook --syntax-check playbooks/recovery_vm_reboot.yml -e tester_mode=true -e target=authy
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ansible-playbook --syntax-check playbooks/recovery_ha_failover.yml -e tester_mode=true -e target=authy
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ansible-playbook --syntax-check playbooks/sophos_trim.yml -e tester_mode=true
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ansible-playbook --syntax-check playbooks/time_sync_check.yml -e tester_mode=true
```

結果: 5 本すべて OK。

## フェーズ 2: recovery ラダー L2 検証

共通:

```bash
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local \
ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote \
ANSIBLE_SSH_CONTROL_PATH_DIR=/tmp/ansible-cp \
ANSIBLE_SSH_COMMON_ARGS='-F /dev/null' \
ansible-playbook playbooks/<playbook>.yml -e tester_mode=true <extra vars>
```

| コマンド | 結果 | 確認内容 |
|---|---:|---|
| `recovery_service_restart.yml -e target=authy` | OK | pvesh 実データで vmid=101 / node=pve1 / tags=`hacritical;preferpve1;verify` / running / `freeradius.service` を確認し、restart 前に `end_play`。changed=0 |
| `recovery_vm_reboot.yml -e target=authy` | OK | pvesh 実データで vmid=101 / node=pve1 / tags を確認。plan の手段は `guest agent shutdown`。shutdown 前に `end_play`。changed=0 |
| `recovery_vm_reboot.yml -e target=sophos-fw` | NG | playbook validate は通過したが、pvesh 実データで VM 名 `sophos-fw` が見つからず fail。ACPI shutdown plan まで未到達 |
| `recovery_ha_failover.yml -e target=authy` | OK | tags / HA 登録 / 移行先選定まで実データで確認。plan に `ha-manager crm-command relocate vm:101 pve2` と `pve1 → pve2` を表示し、relocate 前に `end_play`。changed=0 |
| `recovery_ha_failover.yml -e target=monnie` | EXPECTED_FAIL | playbook 側 validate assert で fail。changed=0。Slack 送信なし |
| `recovery_service_restart.yml -e target=sophos-fw` | EXPECTED_FAIL | playbook 側 validate assert で fail。changed=0。role 未到達 |

`recovery_vm_reboot.yml -e target=sophos-fw` の詳細:

```text
対象 VM が見つかりません: target=sophos-fw
```

確認のため read-only で `pvesh get /cluster/resources --output-format json` を取得したところ、
Proxmox 上の VM name は `Sophos`、vmid は `1000`、tags は `hacritical;preferpve1;verify` だった。
現行 role は `name == target` で VM を検索するため、`target=sophos-fw` では一致しない。

このケースでは reboot / stop / start は実行されていない。失敗時の Slack 通知も
common_slack の tester_mode ガードで debug 表示に置換され、実送信されていない。

## フェーズ 3: sophos_trim L3 リハーサル

### 事前確認: fstrim --help

```bash
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local \
ansible localhost -c local -m ansible.builtin.expect -a '<Advanced Shell 経由で fstrim --help; echo __FSTRIM_HELP_RC__:$? を実行>'
```

結果: OK。

確認内容:

```text
-n, --dry-run            does everything, but trim
__FSTRIM_HELP_RC__:0
```

### sophos_trim tester_mode

```bash
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local \
ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote \
ANSIBLE_SSH_CONTROL_PATH_DIR=/tmp/ansible-cp \
ANSIBLE_SSH_COMMON_ARGS='-F /dev/null' \
ansible-playbook playbooks/sophos_trim.yml -e tester_mode=true
```

結果: OK。

確認内容:

- tester_gate は `tester_gate_end: continue` で plan 表示後に継続
- Advanced Shell のメニュー遷移 5 → 3 が成功
- 実行コマンドは `fstrim --dry-run -v /var` と `fstrim --dry-run -v /content`
- RC marker は両方 0
- `trim_status=SUCCESS`
- Slack 通知は tester_mode debug 表示、実送信なし

抜粋:

```text
/var: 0 B (dry run) trimmed
__SOPHOS_TRIM_VAR_RC__:0
/content: 0 B (dry run) trimmed
__SOPHOS_TRIM_CONTENT_RC__:0
[Sophos Trim] SUCCESS - sophos-fw
```

注記: Ansible の expect タスク自体は changed=1 と表示された。
ただし実行された fstrim は `--dry-run` であり、実 trim は発生していない。

## フェーズ 4: time_sync_check

```bash
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local \
ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote \
ANSIBLE_SSH_CONTROL_PATH_DIR=/tmp/ansible-cp \
ANSIBLE_SSH_COMMON_ARGS='-F /dev/null' \
ansible-playbook playbooks/time_sync_check.yml -e tester_mode=true
```

結果: OK。

確認内容:

- quory / pve1 / pve2 / authy / monnie / sophos-fw / cloudkey の確認が完走
- Warnings / Errors は空
- Slack 通知は skip
- 全 recap が changed=0

抜粋:

```text
Warnings  : []
Errors    : []
quory      : changed=0
sophos-fw  : changed=0
```

## フェーズ 5: regression

### lint 正常系

```bash
scripts/check-tester-gate.sh
```

結果:

```text
[tester-gate-lint] OK (29 playbooks)
```

### cloudkey_cert_deploy gate sample

```bash
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local \
ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote \
ANSIBLE_SSH_CONTROL_PATH_DIR=/tmp/ansible-cp \
ANSIBLE_SSH_COMMON_ARGS='-F /dev/null' \
ansible-playbook playbooks/cloudkey_cert_deploy.yml -e tester_mode=true
```

結果: OK。

確認内容:

- ansy 実行許可 assert OK
- plan 表示
- `end_play`
- changed=0

## 未検証事項・要確認

- `recovery_vm_reboot.yml -e target=sophos-fw` の ACPI shutdown plan 分岐は未確認。
  理由: Proxmox VM name が `Sophos` であり、role の `name == target` 検索では
  `target=sophos-fw` が一致しないため。
- 実 restart / reboot / relocate / trim は tester の範囲外のため未実施。

## 総合判定

確認できた範囲では、tester_mode 不変条件に反する実変更・外部副作用は見つからなかった。

ただし `recovery_vm_reboot.yml -e target=sophos-fw` は、テスト計画の期待結果
「ACPI shutdown plan 表示」まで到達していない。これは Sophos VM の Proxmox 表示名と
recovery target 名の不一致による実装/設定上の不整合として扱うべきである。
