# tester_mode syntax-check test result

Date: 2026-07-01
Role: Codex tester

## Summary

- Result: PASS with note
- Scope: requested `tester_mode=true` syntax-check sweep
- Production apply: not executed
- Dry-run / check mode execution: not executed
- `proxmox_patch_weekly_full.yml`: skipped as requested because it uses `import_playbook`

All requested playbooks passed `ansible-playbook --syntax-check` with `-e tester_mode=true`.

`yamllint` checks were not executed because `yamllint` is not installed in this environment:

```text
yamllint: command not found
/usr/bin/python3: No module named yamllint
```

The first Ansible attempt failed because the sandbox cannot write to `/home/yoshi/.ansible/tmp`.
The checks below were rerun successfully with Ansible temp directories under `/tmp`:

```text
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local
ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote
```

## Syntax-check Results

| Priority | Playbook | Extra vars | Result |
| --- | --- | --- | --- |
| A | `playbooks/recovery_vm_reboot.yml` | `-e tester_mode=true -e target=authy` | PASS |
| A | `playbooks/recovery_service_restart.yml` | `-e tester_mode=true -e target=authy` | PASS |
| A | `playbooks/recovery_ha_failover.yml` | `-e tester_mode=true -e target=authy` | PASS |
| A | `playbooks/sophos_trim.yml` | `-e tester_mode=true` | PASS |
| A | `playbooks/ubuntu_nightly.yml` | `-e tester_mode=true` | PASS |
| B | `playbooks/proxmox_evacuate_node.yml` | `-e tester_mode=true -e target_node=pve2` | PASS |
| B | `playbooks/proxmox_restore_vm_placement.yml` | `-e tester_mode=true -e target_node=pve2` | PASS |
| B | `playbooks/proxmox_patch_apply_node.yml` | `-e tester_mode=true -e target_node=pve2` | PASS |
| B | `playbooks/proxmox_backup_restore_verify.yml` | `-e tester_mode=true` | PASS with warning |
| C | `playbooks/ca_trust_deploy.yml` | `-e tester_mode=true` | PASS |
| C | `playbooks/cert_renew.yml` | `-e tester_mode=true` | PASS |
| C | `playbooks/cert_renew_quory.yml` | `-e tester_mode=true` | PASS |
| C | `playbooks/cloudkey_cert_deploy.yml` | `-e tester_mode=true` | PASS |
| C | `playbooks/unifi_backup_fetch.yml` | `-e tester_mode=true` | PASS |
| C | `playbooks/time_sync_ntp_reference.yml` | `-e tester_mode=true` | PASS |
| C | `playbooks/systemd_timers.yml` | `-e tester_mode=true` | PASS |
| C | `playbooks/recovery_exec_setup.yml` | `-e tester_mode=true` | PASS |
| C | `playbooks/recovery_io_setup.yml` | `-e tester_mode=true` | PASS |

Warning observed for `playbooks/proxmox_backup_restore_verify.yml`:

```text
[WARNING]: Could not match supplied host pattern, ignoring: brv_restore_targets
```

The command still exited successfully and Ansible reported the playbook syntax as valid.

## Yamllint Results

Requested files:

- `roles/common_slack/tasks/notify.yml`
- `roles/sophos_trim/tasks/main.yml`

Result: SKIP

Reason: `yamllint` is not installed in the tester environment.
