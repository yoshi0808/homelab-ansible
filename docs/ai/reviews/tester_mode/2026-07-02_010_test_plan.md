# tester_mode 不変条件 テスト計画 — 2026-07-02

対象実装: 2026-07-02_007_implement.md(レビュー: 008 = suggestion 1件対応済み、009 = LGTM)

**承認: Yoshinobu(2026-07-02)。ただし proxmox_patch_weekly_full と proxmox_patch_dryrun は
スキップする(タイマー運用で日常実行されており、次回タイマーで実質確認できるため。
test_result の未検証事項に記録すること)。**

## 目的

1. 全 29 playbook が不変条件「`-e tester_mode=true` 付き実行で対象ホストの実変更・
   外部副作用(Slack 通知含む)ゼロ」を満たすことを検証する
2. 共通ゲート `tester_gate` の動作(plan 表示、end_play / end_host の使い分け)を検証する
3. lint(check-tester-gate.sh)の正常系・異常系を検証する

## 前提・共通ルール

- 実行環境: ansy(開発ノード)。対象ノードの選択が可能な場合は pve2 を優先(core.md §2)
- **全 playbook 実行に `-e tester_mode=true` を付ける**(§18。SAFE でも一律付与)
- sandbox の一時ディレクトリ制約に当たったら `ANSIBLE_LOCAL_TEMP=/tmp/ansible-local` を付与
  (2026-07-02_008_review.md 参照)
- 結果は `docs/ai/reviews/tester_mode/2026-07-02_011_test_result.md` に保存
  (実行コマンド・結果・置き換えた理由・未検証事項を必ず記録)

判定基準の共通原則:

| 分類 | 期待結果 |
|---|---|
| ゲート付き | gate の plan 表示 → 期待どおりの停止方式で停止。危険 role/task へ未到達。changed=0(ローカルレポート出力を除く) |
| safe-readonly | 通常どおり完走。Slack 通知タスクが skip(または debug 表示に置換)。ローカルレポート生成 |
| role-guarded | Slack 送信タスクが skip されること |
| 全ケース共通 | Slack への実送信ゼロ。対象ホストの状態変更ゼロ |

## フェーズ 1: 静的検証

1. `scripts/check-tester-gate.sh` → OK(29 playbooks)
2. 全 29 本 `--syntax-check`(proxmox_evacuate_node / proxmox_restore_vm_placement は
   `-e target_node=pve2` 付き)

## フェーズ 2: ゲート付き playbook のゲート動作検証(全て `-e tester_mode=true` 付き)

| playbook | 追加 -e | 期待停止 | 備考 |
|---|---|---|---|
| ca_trust_deploy | — | end_host(全対象ホスト) | |
| systemd_timers | — | end_host | connection: local |
| recovery_io_setup | — | end_host | hosts: dev_nodes:control_nodes |
| recovery_exec_setup | — | end_host | 同上 |
| time_sync_ntp_reference | — | end_host(5 ホスト) | |
| recovery_service_restart | target=authy | end_play | |
| recovery_vm_reboot | target=authy | end_play | |
| recovery_ha_failover | target=authy | end_play | |
| proxmox_patch_apply_node | target_node=pve2 | play1 end_play + play2 end_play | 2 play とも gate 発火を確認 |
| proxmox_evacuate_node | target_node=pve2 | play1 end_play、play3 end_play | play2(移行先 healthcheck)は gate なしで完走する = safe-readonly。正常 |
| proxmox_restore_vm_placement | target_node=pve2 | play1 end_play + play2 end_play | |
| proxmox_backup_restore_verify | — | play2 end_play | play1 は pvesh get のみ(read-only)で完走が正常 |
| proxmox_patch_weekly_full | — | 入口 play end_play + 各 import 先の gate | 注意: import の dryrun(apt update + シミュレーション + Codex CLI 分類)は実行される。時間がかかる |
| cert_renew | — | play1 end_play、play2-4 end_host、cleanup end_play | 通知 play は common_slack ガードで抑止 |
| cert_renew_quory | — | (ansy では)最初の assert で fail | ネガティブテスト: quory 以外で安全に fail することを確認 |
| cloudkey_cert_deploy | — | end_play | Claude Code が smoke 済み。正式記録として再実行 |
| unifi_backup_fetch | — | end_play | hosts: pve1。gate が先頭のため接続後すぐ停止 |
| ubuntu_nightly | — | reboot_required=true のホストのみ gate(end_host) | reboot_required=false のホストは gate 前に end_host する(gate 表示なしが正常) |
| sophos_trim | — | end_play | gate は role 内先頭。SSH/expect 接続前に停止 |

## フェーズ 3: safe-readonly / role-guarded の不変条件検証(全て `-e tester_mode=true` 付き)

| playbook | 追加 -e | 期待 |
|---|---|---|
| monitoring_healthcheck | — | 完走。通知 skip。ローカルレポート生成 |
| proxmox_healthcheck | — | 同上 |
| proxmox_hw_check | — | 同上 |
| radius_healthcheck | — | 同上 |
| proxmox_snapshot_check | — | 同上 |
| time_sync_check | — | 完走。通知 skip |
| test_ca_env | — | 完走(env 未設定でも安全) |
| proxmox_patch_dryrun | — | 任意。Codex CLI 分類(外部 API)を含むため、時間・コストの判断でスキップ可。スキップ時は未検証事項に記載 |
| recovery_notify_trigger | target=test | Slack 送信タスク skip |
| recovery_escalate | target=test escalate_reason=test | Slack 送信タスク skip |

## フェーズ 4: lint 異常系

1. ゲートもマーカーもないダミー playbook を playbooks/ に一時作成 → lint fail を確認 → 削除
2. 抜け道パターン(`tester_mode` の変数参照のみを含む playbook)→ lint fail を確認 → 削除

## 実施しないこと

- 通常実行(tester_mode なし)での変更系 playbook 実行 = APPLY。Yoshinobu の本番適用判断後の運用手順
- 通常運用(tester_mode=false)の regression 確認は次回の Semaphore タイマー実行で行う
  (test_result の未検証事項に記録する)

## 事後処理

- tester が test_result を保存し claude に返信
- Claude Code が test_result を意図(不変条件・ゲート互換性)と突合し、違和感の有無と
  commit 可否を Yoshinobu に提示する
