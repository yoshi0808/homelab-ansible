# tester_mode 深掘り P1 追加テスト結果 — 2026-07-02

## 結論

016 で未達だった `sophos-fw` の recovery 2 ケースを再検証した。

- Proxmox VM 1000 の表示名が `sophos-fw` になっていることを read-only で確認
- `recovery_vm_reboot.yml -e target=sophos-fw -e tester_mode=true`: OK
- `recovery_ha_failover.yml -e target=sophos-fw -e tester_mode=true`: OK
- どちらも changed=0
- 実 reboot / stop / start / relocate は発生していない
- Slack 実送信なし

## 実行時の追加条件

```bash
ANSIBLE_LOCAL_TEMP=/tmp/ansible-local
ANSIBLE_REMOTE_TEMP=/tmp/ansible-remote
ANSIBLE_SSH_CONTROL_PATH_DIR=/tmp/ansible-cp
ANSIBLE_SSH_COMMON_ARGS='-F /dev/null'
```

実機接続を伴う read-only / tester-mode 検証は承認済み escalation で実行した。

## 事前確認: Proxmox VM 表示名

```bash
ansible pve1 -b -m ansible.builtin.command -a 'pvesh get /cluster/resources --output-format json'
```

確認内容:

```text
vmid=1000
name=sophos-fw
node=pve1
status=running
tags=hacritical;preferpve1;verify
hastate=started
```

## recovery_vm_reboot sophos-fw

```bash
ansible-playbook playbooks/recovery_vm_reboot.yml \
  -e tester_mode=true \
  -e target=sophos-fw
```

結果: OK。

確認内容:

- playbook validate OK
- Phase 1 が VM を発見
  - `vmid=1000`
  - `node=pve1`
  - `tags=hacritical;preferpve1;verify`
- plan の手段は `ACPI shutdown`
- shutdown / stop / start の前に `end_play`
- changed=0

抜粋:

```text
対象  : sophos-fw (vmid=1000, node=pve1)
タグ  : hacritical;preferpve1;verify — 適格性検証済み
手段  : ACPI shutdown
PLAY RECAP: pve1 changed=0 failed=0
```

## recovery_ha_failover sophos-fw

```bash
ansible-playbook playbooks/recovery_ha_failover.yml \
  -e tester_mode=true \
  -e target=sophos-fw
```

結果: OK。

確認内容:

- playbook validate OK
- Phase 1 が VM を発見
  - `vmid=1000`
  - `source_node=pve1`
  - `tags=hacritical;preferpve1;verify`
- HA 登録確認 OK
- 移行先選定 OK: `pve2`
- plan に `ha-manager crm-command relocate vm:1000 pve2` を表示
- relocate の前に `end_play`
- changed=0

抜粋:

```text
操作  : ha-manager crm-command relocate vm:1000 pve2
対象  : sophos-fw (vmid=1000)
タグ  : hacritical;preferpve1;verify — hacritical 検証済み・HA 登録確認済み
移行  : pve1 → pve2
PLAY RECAP: pve1 changed=0 failed=0
```

## 総合判定

VM 表示名 rename 後、016 の未達 2 ケースはいずれも期待どおり L2 gate に到達した。
確認できた範囲で tester_mode 不変条件違反はない。
