# tester_mode 深掘り P1 実装記録 — 2026-07-02

対象要求: 2026-07-02_012_requirement.md(Yoshinobu 承認スコープ: P1 のみ)

## 実装内容

### ① sophos_trim の L3 化(dry-run リハーサル)

| ファイル | 変更内容 |
|---|---|
| `roles/tester_gate/tasks/main.yml` | `tester_gate_end: continue` を追加(plan 表示のみで停止しない。L3 用)。`none` は YAML null になる footgun のため不採用。値のバリデーション assert も追加(typo で黙って素通りするのを防ぐ) |
| `roles/sophos_trim/tasks/main.yml` | tester_mode 時は停止せず、`sophos_trim_fstrim_opts` を `-n -v` に差し替えて実 expect セッションを実行(`fstrim -n` = trim 以外のすべてを実行)。メニュー遷移(5→3)・RC マーカーのパース・trim_status 判定まで本番同等に動く。通常時は従来どおり `-v` |
| `playbooks/sophos_trim.yml` | gated-in-role マーカーの文面を L3 リハーサルの説明に更新 |

- 判定ロジック(`__SOPHOS_TRIM_*_RC__` パース、trim_status、fail 条件)は無変更。
  dry-run が失敗すれば tester_mode でも FAILED → fail するため、テストとして意味を持つ。
- Sophos 側 fstrim が `-n` をサポートしない場合は FAILED になる。tester は事前に
  read-only で `fstrim --help` を確認する(test_plan に含める)。

### ② recovery ラダー 3 本の L2 化(gate を role 内 read-only 検証の後へ)

| ファイル | 変更内容 |
|---|---|
| `roles/recovery_vm_reboot/tasks/main.yml` | Phase 1(pvesh タグ再検証)直後に gate。plan に実データ(vmid / node / 実タグ / shutdown 手段)を表示して `end_play` |
| `roles/recovery_ha_failover/tasks/main.yml` | Phase 1-2(タグ・HA 登録・移行先選定、すべて read-only)直後、Phase 3 (relocate) 直前に gate。plan に relocate コマンド全体と移行元→先を表示 |
| `roles/recovery_service_restart/tasks/main.yml` | Phase 1(タグ・running 確認・サービス一覧解決)直後、Phase 2 (restart) 直前に gate。plan に解決済みサービス一覧を表示 |
| `playbooks/recovery_{vm_reboot,ha_failover,service_restart}.yml` | playbook 側 gate を撤去し、`# tester-gate: gated-in-role` マーカーへ切り替え(lint 対応) |

- 各 role とも gate 直前に `_r*_result: "plan-only"` を set_fact。`meta: end_play` は
  block の always(レポート+通知)を実行せずに終了するが、仮に将来 always が実行される
  経路ができてもレポートが「failed」と誤記録されない防御。
- Phase 1 の pvesh タスクは既存の `changed_when: false` / `check_mode: false` を維持。

### ③ time_sync_check の changed_when 整備(011_test_result の指摘対応)

| ファイル | 変更内容 |
|---|---|
| `roles/time_sync_check/tasks/main.yml` | Sophos expect(date 直接比較)と CloudKey expect(timedatectl 読み取り)の 2 タスクに `changed_when: false` を追加 |

## 検証済み事項

1. lint OK(29 playbooks)— recovery 3 本と sophos_trim は gated-in-role マーカーで通過
2. 影響 5 playbook の `--syntax-check` OK
3. ansy から実機スモーク: `recovery_service_restart.yml -e target=authy -e tester_mode=true`
   - Phase 1 が実データで完走(authy: vmid=101, node=pve1, tags=hacritical;preferpve1;verify,
     running 確認、サービス一覧 freeradius.service 解決)
   - plan に実データが表示され、restart 直前で `end_play`、changed=0
   - `meta: end_play` により always(レポート+通知)は実行されないことを確認

## レビュー観点(お願い)

1. `tester_gate_end: continue` の設計妥当性(L3 で「停止しない gate」を許すことの安全性。
   不変条件の担保が呼び出し側の dry-run 差し替えに移る点)
2. sophos_trim: fstrim オプション差し替えの実装(expect responses 内の Jinja 展開)と、
   `-n` 非サポート時の挙動(FAILED → fail)の妥当性
3. recovery 3 role: gate 位置(read-only / 変更系の境界判定)が正しいか。
   特に ha_failover は Phase 2(移行先選定)まで read-only として gate を Phase 3 直前に置いた
4. `meta: end_play` と block always の関係(スモークで always 未実行を確認済みだが、
   レポート・通知がスキップされる挙動が運用上問題ないか)
5. playbook 側 gate 撤去により、tester_mode 時も SSH 接続+pvesh get が実行されるように
   なった(従来は接続前に停止)。SAFE(read-only)の範囲内という整理でよいか

## 状態

- 未 commit(commit は reviewer に委任)
- レビュー収束後、test_plan(015 予定)を起案して Yoshinobu 承認 → tester 検証
