# tester_mode → ansible_check_mode 移行 テスト計画 — 2026-07-08

対象実装: 2026-07-06〜08 の一連の変更(tester_mode/tester_gate 廃止、`# tester-gate:`
マーカー方式への移行、`docs/ai/prompts/core.md` §16・§18 全面書き換え、`roles/tester_gate`
削除)。詳細は各 playbook ヘッダのマーカーコメントと core.md §18 を参照。

**承認: Yoshinobu(2026-07-08)。3段階(risk-accepted → dry-run-aware → check-mode-native)に分けて実施。**

## 対象外(今回のテスト範囲から除外)

以下4本は Proxmox パッチ適用チェーンで、commit 後に本番(週末土曜朝スケジュール)で確認する。
今回は対象にしない:

- `playbooks/proxmox_evacuate_node.yml`
- `playbooks/proxmox_restore_vm_placement.yml`
- `playbooks/proxmox_patch_apply_node.yml`
- `playbooks/proxmox_patch_weekly_full.yml`

## 前提・共通ルール(tester_mode からの変更点)

- **`-e tester_mode=true` は付けない。** 付けると `[migration] tester_mode is
  deprecated...` の assert で即座に失敗する(意図的な移行ガード)。
- 対象 playbook ヘッダの `# tester-gate: <種別>` マーカーを必ず確認してから実行方法を選ぶ
  (`docs/ai/prompts/core.md` §16「tester の自律境界」参照)。
  - `safe-readonly` / `role-guarded` / `risk-accepted`: 通常実行(`--check` 不要)。
  - `check-mode-native` / `dry-run-aware`: **必ず `--check` を付ける**。
- 結果は `docs/ai/reviews/tester_mode/2026-07-08_019_test_result.md` に追記していく
  (バッチごとに追記可)。

## フェーズ 1: 静的検証

1. `bash scripts/check-tester-gate.sh` → `[tester-gate-lint] OK (31 playbooks)`
2. 対象19 playbook の `ansible-playbook <file> --syntax-check`(-e target_node 等の必須変数が
   あるものは適宜付与)

## バッチ1: risk-accepted(常時本実行、`--check` 不要) — 13本

`safe-readonly` の `proxmox_patch_dryrun.yml` も今回の check_mode バグ修正確認のため同梱する。

| playbook | 実行例 | 確認observation |
|---|---|---|
| `proxmox_patch_dryrun.yml` | `ansible-playbook playbooks/proxmox_patch_dryrun.yml --check` | `_final_status` が UNKNOWN にならず正しく算出される(旧バグ修正確認) |
| `ca_trust_deploy.yml` | 通常実行 | 対象ホスト全台で `copy` の changed が初回のみ、以降は changed=0 |
| `systemd_timers.yml` | 通常実行 | timer/service ユニット配置、`enabled+started` になるが `.service` は即時実行されない |
| `recovery_push_drill_setup.yml` | 通常実行 | authy/monnie に drill unit 配置、`daemon_reload` のみ |
| `recovery_exec_setup.yml` | `-l ansy` で通常実行 | recovery-exec ユーザー・鍵・スクリプト配置、サービス再起動なし |
| `recovery_io_setup.yml` | `-l ansy` で通常実行 | recovery-io 配置、`recovery_io_service_enabled` に応じ enable/start |
| `recovery_probe_setup.yml` | `-l ansy` で通常実行 | recovery-probe 配置、自身のみ再起動対象 |
| `recovery_push_setup.yml` | 通常実行 | authy/monnie へ push key・drop-in 配置(既存サービスは再起動しない) |
| `time_sync_ntp_reference.yml` | 通常実行 | chrony conf.d 配置、初回のみ chrony restart |
| `cloudkey_cert_deploy.yml` | 通常実行 | CloudKey へ実際に新規証明書アップロード・有効化・旧証明書削除。**実本番操作である点に留意** |
| `cert_renew.yml` | 通常実行 | ansy/pve1/pve2/monnie の証明書更新(残日数次第で実際には skip される可能性あり)、各サービス再起動 |
| `unifi_backup_fetch.yml` | 通常実行 | CloudKey で実際に新規バックアップ生成、NFS へ保存・世代ローテーション |
| `proxmox_backup_restore_verify.yml` | 通常実行 | VMID 999 への実 qmrestore → 起動確認 → 破棄。**数分かかる。実ストレージI/Oを伴う** |

## バッチ2: dry-run-aware(`--check` 必須) — 1本

| playbook | 実行例 | 確認observation |
|---|---|---|
| `sophos_trim.yml` | `ansible-playbook playbooks/sophos_trim.yml --check` | 実SSHセッション・メニュー遷移は本実行されるが、`fstrim` は `--dry-run` 引数で発行される(ログに `--dry-run -v` が出ること) |

## バッチ3: check-mode-native(`--check` 必須) — 5本

Proxmox パッチチェーンを除く自律復旧ラダー3本 + ubuntu_nightly + cert_renew_quory。

| playbook | 実行例 | 確認observation |
|---|---|---|
| `recovery_vm_reboot.yml` | `-e target=authy --check` | Phase 1 タグ再検証は実データ、shutdown/start は行われず `_rvr_result: plan-only` のレポート・Slack通知(infoチャンネル)が出る |
| `recovery_service_restart.yml` | `-e target=authy --check` | 同様、`_rsr_result: plan-only` |
| `recovery_ha_failover.yml` | `-e target=authy --check` | 同様、`_rhf_result: plan-only`、relocate は行われない |
| `ubuntu_nightly.yml` | `--check`(radius_servers/monitoring_servers 両方) | reboot-required 判定は本実行、reboot_required=true の場合は `CHECK_MODE_WOULD_REBOOT` レポート、false の場合は通常の `SKIPPED` |
| `cert_renew_quory.yml` | `--check` | prepare_ca/issue は本実行、quory 自身の semaphore デプロイ/再起動は行われず summary に `semaphore deploy/restart skipped` と出る |

## 共通確認事項(全バッチ)

- `-e tester_mode=true` を付けた場合に意図通り assert で失敗すること(1本だけ代表でスモーク確認できればよい)
- Slack 通知が意図した内容・チャンネルで飛ぶこと(誤って `error`/`critical` にならないこと — plan-only 系は `info` になるはず)
- レポートファイル(`reports_base_dir` 配下)が実際に生成されること
