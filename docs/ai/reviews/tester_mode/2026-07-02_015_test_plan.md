# tester_mode 深掘り P1 テスト計画 — 2026-07-02

対象実装: 2026-07-02_013_implement.md(レビュー: 014 = must-fix なし、suggestion は core.md ルール化で対応)

## 目的

1. recovery ラダー 3 本の L2 ゲートが「read-only 検証を実データで実行 → 変更系直前で停止」することを検証する
2. sophos_trim の L3 リハーサル(実 expect + `fstrim -n -v`)が本番同等の経路で完走することを検証する
3. time_sync_check が changed=0 になることを確認する(011 指摘対応)
4. 既存の不変条件・lint が引き続き成立することを確認する

## 前提・共通ルール

- 全 playbook 実行に `-e tester_mode=true` を付ける(§18)
- sandbox 制約時は `ANSIBLE_LOCAL_TEMP=/tmp/ansible-local` 等を付与(011 と同様)
- 結果は `docs/ai/reviews/tester_mode/2026-07-02_016_test_result.md` に保存

## フェーズ 1: 静的検証

1. `scripts/check-tester-gate.sh` → OK(29)
2. 影響 5 playbook の `--syntax-check`(recovery 3 本 / sophos_trim / time_sync_check)

## フェーズ 2: recovery ラダー L2 検証(全て `-e tester_mode=true`)

| コマンド | 期待結果 |
|---|---|
| `recovery_service_restart -e target=authy` | Phase 1 実データ検証(vmid/node/実タグ/running/サービス一覧)→ plan に実データ表示 → restart 前に `end_play`、changed=0 |
| `recovery_vm_reboot -e target=authy` | 同様。plan の「手段」が guest agent shutdown |
| `recovery_vm_reboot -e target=sophos-fw` | 同様。plan の「手段」が ACPI shutdown(分岐確認) |
| `recovery_ha_failover -e target=authy` | タグ + HA 登録 + 移行先選定まで実データで実行 → plan に relocate コマンドと移行元→先を表示 → relocate 前に `end_play`、changed=0 |
| `recovery_ha_failover -e target=monnie`(ネガティブ) | Phase 1 の hacritical assert で fail → rescue → 全体 fail。Slack 実送信なし(通知は tester_mode ガード)。tester_mode でも安全に fail することを確認 |
| `recovery_service_restart -e target=sophos-fw`(ネガティブ) | playbook 側 validate assert で fail(role 未到達) |

## フェーズ 3: sophos_trim L3 リハーサル

1. 事前確認(read-only): Sophos の Advanced Shell で `fstrim --help` を実行し、`-n/--dry-run`
   のサポート有無を確認する(expect 経由、読み取りのみ)
2. `-n` サポートあり → `sophos_trim.yml -e tester_mode=true` を実行:
   - 実 expect セッションでメニュー遷移(5→3)が成功する
   - `fstrim -n -v /var` / `fstrim -n -v /content` が実行され、RC マーカーが 0
   - trim_status=SUCCESS、Slack 通知 skip、実 trim なし(dry-run)
3. `-n` サポートなし → 実行せず、未検証事項として記録(P1① の再設計要否を claude に報告)

## フェーズ 4: time_sync_check

- `time_sync_check.yml -e tester_mode=true` → 完走、**changed=0**(前回 changed=2 だった
  Sophos/CloudKey expect が ok になること)、Slack 通知 skip

## フェーズ 5: regression

1. lint 正常系(29 playbooks)
2. ゲート互換サンプル: `cloudkey_cert_deploy.yml -e tester_mode=true` が従来どおり
   plan 表示 → `end_play`、changed=0

## 注意事項

- recovery 系のネガティブテストは fail が期待値。playbook の rc≠0 を FAIL と誤記録しないこと
- sophos-fw が停止中の場合、sophos_trim / vm_reboot(sophos-fw) は実施不可 → 未検証事項に記録
- 実 trim・実 restart・実 reboot・実 relocate が発生していないことを結果に明記する

## 事後処理

- tester が test_result(016)を保存し claude に返信
- Claude Code が突合して Yoshinobu に提示。commit は reviewer に委任
