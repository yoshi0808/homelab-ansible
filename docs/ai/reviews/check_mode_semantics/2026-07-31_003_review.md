# review: `--check` の意味の一本化(R1〜R5、P0)

日付: 2026-07-31
実施: Reviewer(独立、Implementerとは別セッション)
対象: 未コミット差分 25ファイル(+461/-33) + `docs/ai/reviews/check_mode_semantics/2026-07-31_002_implement.md`
契約: `docs/ai/reviews/check_mode_semantics/2026-07-31_001_requirement.md` P0(R1〜R5)・受入条件§6(AC1〜AC6)。R6/R7はscope外。

## Code Review: check_mode_semantics P0 (R1〜R5)

### Summary

R1(risk-accepted 17本の停止ゲート)・R2(skip_notifications除去)・R3(notify.yml統一)・R4(prometheus_update_check設計撤回)・R5(check_mode:false 7ファイルの個別判定)は、25ファイル全件を読み、うち主要17ファイル+notify.yml+finalize.yml+prometheus role+ubuntu_vm_full_upgrade roleの構造をplay単位で追跡し、localhost限定の使い捨てplaybook(実host名・実IPなし、`connection: local`)でR1/R3のロジックを独立再現した結果、AC1〜AC5の成立に必要な構造・分岐は全て確認できた。blocking findingは無い。OQ1(7ファイルの`check_mode: false`個別判定)はImplementerの判定を独立に再検証し、一致した。ただし、R1の停止メッセージが実際の通知有無と食い違う17本中12本、およびPolicy(TS-005/TS-022)と新挙動の矛盾という、機能には影響しないが規範・案内文の正確性に関わる指摘が残る。

### Critical Issues

なし。

### Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| 1 | `docs/ai/policies/ansible_test_safety_policy.md` | TS-005(24行目)・TS-022(121行目) | 「`risk-accepted`は`--check`の有無で挙動が変わらない」「付けても`risk-accepted`は挙動が変わらない」は、R1導入後は事実と異なる(`--check`はpre_tasksのassertで停止するようになった)。R6でこの2行の改訂が予定されているが、R6着手までの期間はPolicy本文がTS-002(「マーカーと実装が乖離した場合は乖離自体を欠陥として扱う」)に自ら違反した状態になる。Coordinatorへ、R6着手の優先度を上げるべき既知ギャップとして明示的に申し送ることを推奨する | document-norm |
| 2 | `playbooks/ca_trust_deploy.yml`(43行目)・`incident_capture_setup.yml`(57行目)・`incident_investigate_setup.yml`・`recovery_exec_setup.yml`・`recovery_io_setup.yml`・`incident_inspect_setup.yml`(57行目)・`recovery_probe_setup.yml`(37行目)・`recovery_push_setup.yml`・`recovery_push_drill_setup.yml`(37行目) | 各行 | R1停止時のfail_msgに「Slack通知を抑止したい場合は`-e skip_notifications=true`」という案内が入っているが、これら9ファイルはplaybook自身にもimportしているroleにも`common_slack`/`notify.yml`の呼び出しが無い(実測、下記「自己検証」参照)。案内自体は実行しても副作用がなく実害は無いが、「何を抑止する変数なのか」が実態と合っていない | document-norm |
| 3 | `playbooks/codex_update_check.yml`・`playbooks/proxmox_backup_restore_verify.yml`・`playbooks/unifi_backup_fetch.yml` | 各pre_tasksのfail_msg | 上記と逆に、この3ファイルは実際に(役割経由または直接)`common_slack`/`notify.yml`を呼び出すにもかかわらず、fail_msgに`skip_notifications=true`の案内が無い。R1の要件文言(「本適用しつつ通知を抑えたい場合の明示手段を案内する」)を字義通り満たすなら、この3ファイルにこそ案内が要る | document-norm |
| 4 | `playbooks/cert_renew_quory.yml` | 50, 71, 92行目 | `skip_notifications: "{{ ansible_check_mode }}"`が3箇所残っている。この playbook は`check-mode-native`でありR2の対象5ファイルには含まれないため対象外で正しいが、R3導入後は`notify.yml`自身が`ansible_check_mode`を見るため、この var は check-mode 由来の効果としては無意味(死んだ配線)になった。実害はない(意図しない副作用は無い)ため必須修正ではないが、R6以降の整理対象として記録しておくとよい | simplification |

### What Looks Good

- **R3(`roles/common_slack/tasks/notify.yml`)**: debug分岐・送信分岐の両`when:`に`or ansible_check_mode | bool`が対称に追加されており、`tester_mode`/`skip_notifications`の既存抑止経路はそのまま温存されている。localhost限定の使い捨てplaybookで3パターン(`--check`/通常/`-e skip_notifications=true`)を実際にAnsible templar越しに評価し、期待通りの`debug_branch_when`/`send_branch_when`の組を確認した(検証後に削除済み)。`capture.yml`は無変更で、grepでも差分ゼロを確認した(OQ2どおり現状維持)。
- **R1(17本の停止ゲート)**: `grep -l "^# tester-gate: risk-accepted" playbooks/*.yml`で数え直し、ちょうど17件、実装記録の列挙と一致することを確認した。`playbooks/grafana_provisioning.yml`は本文中の言及のみで自身の`# tester-gate:`行は`check-mode-native`であることも確認済み(誤検出は実装記録の主張通り)。17本全てのpre_tasks(または新設pre_tasks)に`ansible.builtin.assert: that: not (ansible_check_mode | bool)`が入っていることを個別に確認した。複数playに分かれる`cert_renew.yml`(5 play中、実際にrisk-acceptedな適用を行う4 play全てにguard)、`codex_update_check.yml`・`proxmox_backup_restore_verify.yml`・`unifi_backup_fetch.yml`(いずれも実変更を行う唯一のplayにguard)は、guard未設置のplay(verify-host、read-onlyのノード選定play)が実害のない到達で、AC1の「role本体のtaskは1つも実行されない」を壊さないことを構造的に確認した。localhost限定の使い捨てplaybookで同一パターンのassertを`pre_tasks`に置き、`--check`時に`rc=2`・`changed=0`・後続task非到達、通常実行時に`rc=0`・後続到達を独立に再現した(検証後に削除済み)。
- **R2(skip_notifications除去)**: `grep -rn 'skip_notifications.*ansible_check_mode' playbooks/ roles/`で全箇所を洗い出し、対象5ファイル(`ca_trust_deploy.yml`・`cert_renew.yml`全5 play・`incident_inspect_setup.yml`・`recovery_exec_setup.yml`・`recovery_io_setup.yml`)は実キーが除去されコメントのみ残存していること、`cert_renew_quory.yml`(check-mode-native、対象外)は無変更のままキーが残っていることを確認した。
- **R4(`prometheus_update_check`設計撤回)**: playbookの`tester-gate`行とrole `main.yml`冒頭コメント・Phase 4コメントの両方から「Slackはcheck-modeの意図的な例外」という言明が撤回され、旧設計記録`docs/ai/reviews/prometheus_update_check/2026-07-19_015_implement.md`への言及が残っていることを確認した。撤回した文言(`intentional check-mode exception`/`成果物であるSlack通知は--checkでも実走する`)で`docs/` `roles/` `playbooks/` `skills/`を横断検索し、生きた主張として残っている箇所は無いことを確認した(実装記録・要件記録内の引用のみ)。
- **R5(OQ1の独立再判定)**: 7ファイル全てで、implementerの分類(除去5件・残置7件)を自分で現物を読んで再判定し、一致した。特に`ubuntu_nightly.yml`と`roles/incident_sync/tasks/finalize.yml`は「近くのcheck_mode:falseがnotify呼び出し自体には付いていない」という実装記録の指摘を`grep`で裏取りし(`finalize.yml`の「Notify Slack」2taskは`check_mode: false`を持たず`when: ... not ansible_check_mode`のみ)、正しい判定であることを確認した。`roles/ubuntu_vm_full_upgrade/tasks/main.yml`の「Send check-mode "would apply" notification」block(ヒューリスティックが当初拾えていなかったもの)が除去対象に正しく含まれていることも確認した。
- **AC2/AC5(通常実行の不変)**: R3の追加条件`or ansible_check_mode | bool`は、通常実行時(`ansible_check_mode=False`)は論理式の結果に影響しないため、`tester_mode`/`skip_notifications`による既存の抑止・非抑止の分岐は変化しない。`prometheus_update_check`のdry_run判定ロジック自体は本diffで変更されておらず、AC5(Semaphore同一引数での本番経路)に影響する変更はない。
- **AC6(静的網羅)**: 上記の通り17件ちょうどであることを独立に確認した。
- **構文**: 変更した21 playbookファイルのうち任意に選んだ12ファイルで`ansible-playbook <file> --syntax-check`を実行し、全て成功(dynamic groupを使う2ファイルの`[WARNING]: Could not match supplied host pattern`は既知の警告であり失敗ではない)。

### Verdict

Approve(indicated Suggestionsは全て非blocking。functionalなAC1〜AC6の成立を妨げるCritical Issueは見つからなかった)。

## 自己検証

- 参照した全ファイルパス・行番号は本レビュー実施時点の作業ツリーで`Read`/`grep`により直接確認済み(引用のみで済ませた箇所はない)。
- 17ファイルの数え直し・除去5ファイルの数え直し・7ファイルの個別判定の再検証は、いずれも実装記録の主張を鵜呑みにせず自分の`grep`/`Read`で裏取りした。
- R1/R3の停止・抑止ロジックは、`/tmp/claude-1000/.../scratchpad/`配下(作業ツリー外)にlocalhost限定・`connection: local`の使い捨てplaybookを作成し、`--check`/通常実行/`-e skip_notifications=true`の3パターンで実行して確認した(実host名・実IP不使用、検証後に削除済み)。対象playbook17本・notify.yml・実host・ansyへは一切適用していない。
- 「問題なし」と判断した項目(R2/R3/R4/OQ2/AC2/AC5/AC6)は上記What Looks Goodに、確認した具体的な方法とともに記載した。

## 未解決事項

- Suggestion #1(Policy TS-005/TS-022のドリフト)はR6のscopeであり、本ラウンドでは対応不要。ただしR6着手までの間、Policyを読む人間・AIが誤った実行時挙動を信じるリスクが残る。Coordinatorでの優先度判断を要する。
- Suggestion #2/#3(fail_msgの案内文と実際の通知有無の不一致)は実害がないため、次回R1関連の変更が入るときにまとめて直す形でよい。
- AC1〜AC5の実playbook・実notify.ymlに対する受入条件検証(実host/ansy上、`--check`込み)はTesterの担当であり、本レビューでは実施していない。

---

## Coordinatorの処理(2026-07-31)

| Suggestion | 扱い |
|---|---|
| #1 Policy TS-005 / TS-022 のドリフト | **同意・即時是正。** R6を後回しにせず本ラウンドのcommitに含めた。TS-005 / TS-022 / TS-024 を改訂し、TS-030(停止ゲート)/ TS-031(check mode下の通知)/ TS-032(昇格の経路)を新設。Policy本文の改訂はYoshinobuの承認事項であり、差分を提示したうえでcommitする |
| #2 通知しない9本に `-e skip_notifications=true` の案内がある | **同意・是正。** Implementerへ差し戻した |
| #3 通知する3本に案内が無い | **同意・是正。** 同上 |
| #4 `cert_renew_quory` の `skip_notifications: "{{ ansible_check_mode }}"` が冗長 | **却下(現状維持)。** 「死んだ配線」ではない — `skip_notifications` の消費者は `notify.yml` だけではなく、`roles/common_slack/tasks/capture.yml` が通知証跡のspool recordへこの値をフラグとして記録している(同ファイル222行目)。除去すると `--check` 実行時に記録される値が変わるため、送信判定としての冗長性だけを理由に消すと、証跡側の意味を独立検証せずに変えることになる。同じ理由で他4ファイル(`sophos_trim` / `proxmox_evacuate_node` / `proxmox_patch_apply_node` / `proxmox_restore_vm_placement`)の同じ結線も残す。**R3導入後は送信判定として冗長である**という事実自体は正しく、整理する場合は capture の記録仕様とあわせて別途扱う |

**Suggestion #1について**: Reviewerの指摘どおり、R6を先送りするとPolicyがTS-002(マーカーと実装の乖離自体を欠陥として扱う)に自ら違反した状態でcommitされる。乖離を残したまま「後で直す」と記録する形は本案件の起点そのものであるため、同一commitに含める判断とした。
