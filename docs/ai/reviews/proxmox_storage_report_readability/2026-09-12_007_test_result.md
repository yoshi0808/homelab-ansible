# ストレージ月次点検レポート可読性改善 test_result

対象006計画。すべて`/tmp`のscratch fixture・独自inventoryで実施し、検証後にすべて削除した。実ホスト・quory・本番Notion/Slackへは一切接続していない。通常実行には常に`skip_notifications=true`を付与した。実装は本Testerによる無変更。

## 静的検証(独立再実行)

- `python3 -m unittest discover -s tests -p test_storage_monthly.py -v`: 18件成功。
- `ansible-playbook playbooks/proxmox_storage_monthly.yml --syntax-check`: rc0(動的group未定義警告のみ、既知)。
- `bash scripts/check-tester-gate.sh`: 61 playbook OK。
- `git diff --check`: rc0。
- `python3 scripts/check-doc-consistency.py`: 3チェックともOK。

## Fixture動的検証(独立構成、実装既存テストとは別データで再現)

| ID | AC | 内容 | 結果 |
|---|---|---|---|
| D1 | AC1 | 正常初回report(独自fixture、直近scrub)を`build_report`→`markdown`/`short_summary`で整形 | PASS: 冒頭「異常は見つかりませんでした」、「初回点検のため前回比較はありません」、ラベル付きZFS/NVMe値(温度49.85°C、予備領域100%等)を確認。`"values"`等の生JSON dumpが本文に無いことを確認 |
| D2 | AC2 | 同一device・同一serialでerror-log 100→105のbaseline比較reportを構築 | PASS: 短報・本文双方に`d2-host`/`D2-SER`、「NVMe error-logの累積数が増加しました」、「要確認」が日本語で出現。本文に`error-log +5`の差分表記あり |
| D3 | AC3 | (a)収集不能(空observation)、(b)history_error=true、(c)DEGRADEDプール+NVMe critical_warning、の3種を独自fixtureで構築 | PASS: いずれも理由が隠されず開示(a: 「収集データが不完全です」、b: 「保存履歴が壊れているため前回比較できません」、c: 「ZFSプール...を確認してください」「NVMeの警告...」)。(c)で本文の`[異常]`行が`## 前回比較`見出しより前(対応・未確認事項の先頭側)に出ることを確認 |
| D4 | AC4 | 実`ansible-playbook`でcollect→保存されたJSONのreport_idを使い`operation=replay`を実行、前後でJSON md5比較 | PASS: replay前後でreport JSONのmd5が完全一致(JSON不変)。replay結果のSlackメッセージは新形式(「異常は見つかりませんでした レポート公開=suppressed」)で再生成される |
| D5 | AC5 | 同fixtureで`--check`実行、`skip_notifications=true`は常時付与 | PASS: rc0、`reports_base_dir`配下にディレクトリ自体が作られない(保存なし)、Slack短報は`[suppressed]`表示のみで実送信なし |
| D6 | 非回帰 | D4で保存された実report JSONのkey集合を確認(top-level: schema_version/report_id/collected_at/month/hosts/issues/health/collection/comparison_source/history_error、host: collection/devices/pools/issues、device: id/serial/model/vdev/namespace/values/temperature_c/comparison/delta、pool: name/state/vdevs/scrub/issues) | PASS: 変更前と同じキー集合(`build_report`自体は本diffで無変更であることをdiffで確認済み)。判定結果(health/collection)もD1〜D3で仕様どおり |
| 追加 | Slack最大3件 | 5件のissues(WARNING×3/CRITICAL×1/UNKNOWN×1)を独自fixtureで`short_summary`に通す | PASS: 表示は判定不能→異常→要確認の順で3件、「ほか2件」を末尾に表示。JSON配列形式は出現しない |

## 手戻り(参考)

D4/D5の初回試行でscrub日時に「Sep 13 2026」を使い、実行日(2026-09-12)からみて未来日付となり`future scrub timestamp`のValueErrorで意図せずFAILした。これは既存コードの正しい安全側挙動(005までに確認済み)であり、本diffの欠陥ではない。日付を過去(Aug 09)に修正して再実施した。

## Verdict

**PASS。** AC1〜AC5すべて独立fixtureで確認し、保存JSON schema・判定ロジック・replay・通知抑止契約に非回帰。対象実装は無変更。scratchは検証後にすべて削除した。
