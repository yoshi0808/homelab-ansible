## Code Review: ストレージ月次点検レポートの可読性改善

### Summary

生JSON dumpの除去、host/device別ラベル付き表示、前回比較・初回基準・比較不能の日本語化、Slack短報の日本語化は要件どおり実装され、JSON schema・収集・判定・保存・Notion契約・通知抑止機構への変更は無い(diffで確認、既存17テストも全通過)。ただし、issuesのSlack向け先頭3件抽出とMarkdown本文の一覧表示がいずれも重大度でソートされておらず、CRITICAL項目がSlack短報から欠落しうる欠陥をfixtureで再現した。AC3(「異常の理由を隠さず、対応事項の先頭側に表示する」)を満たせない実行経路であり、blocking。

### Critical Issues

なし。

### Major Issues

| # | File | Line | Issue | Severity |
|---|---|---|---|---|
| 1 | `roles/proxmox_storage_monthly/module_utils/storage_monthly.py` | 219-235 (`short_summary`)、238-246 (`markdown`の対応・未確認事項欄) | `report["issues"]`はhost/pool/device列挙順のまま並び替えられず、`short_summary`は`issues[:3]`で先頭3件だけをSlackへ出す。host集合の並びによっては軽微なWARNINGが先に列挙され、真にCRITICALな項目が4件目以降に押し出されてSlack短報から完全に脱落する。decoy fixture(4host、a/b/c=WARNING、z=CRITICALなzpool状態)で実際に再現し、`short_summary`が3件のWARNINGのみを表示し「ほか2件」としてCRITICALを覆い隠すことを確認した(実ホスト・実Notion/Slackへは送信していない、純粋関数のみ実行)。AC3「判定不能または異常の理由を隠さず、対応事項の先頭側に表示する」および要件P0「Slackは...最大3件の注意事項」の趣旨(3件に絞っても最重要が漏れないこと)を満たさない。Markdown本文側も同じ未ソートの列順で「対応・未確認事項」を出すため、長い一覧の後半にCRITICALが埋もれる同種のリスクがある | Major |

### Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| 1 | `roles/proxmox_storage_monthly/module_utils/storage_monthly.py` | 246 | 「対応・未確認事項」各行の重大度タグ`[CRITICAL]`/`[WARNING]`/`[UNKNOWN]`は英語のまま(他箇所の「総合判定: 異常（CRITICAL）」のような日本語併記になっていない)。可読性目的からは`HEALTH_LABELS`で揃えると一貫する | i18n/style |
| 2 | `roles/proxmox_storage_monthly/module_utils/storage_monthly.py` | 165-179 (`ISSUE_LABELS`) | `issue_text`は`ISSUE_LABELS`未登録のreasonコードをそのまま英語で返す(現状の全コードはカバー済みで実害なし)。将来`build_report`に新しいissue文字列を追加した際、翻訳漏れが検出されずに英語表示が復活する。テストで「既知コード集合とISSUE_LABELSキー集合の一致」を検査すると回帰を防げる | maintainability |

### What Looks Good

- 生JSON dump除去: `markdown()`から`json.dumps`呼び出しが無くなり、`test_human_readable_normal_report`の`assertNotIn('"values": {', body)`で担保。AC1に対応。
- 非回帰: `build_report`本体・スキーマ・保存/Notion/Slack抑止契約(`report.yml`のsuppress式、`storage_notion`呼び出し)は無変更。既存17件のunittestを独立再実行しすべて成功。
- replay整合: `storage_archive.py`はreplay/check-mode/通常保存の3経路すべてで同じ`markdown()`/`short_summary()`を通し、JSONを書き換えない(AC4)。
- 比較状態の日本語化: baseline/history_corrupt/replacement_or_added/reset_suspected/comparedの5状態と差分(`+n`)がすべて訳出され、`test_human_readable_warning_report_and_summary`が実差分表示を検証している。
- 通知抑止: `report.yml`のsuppress式・`_storage_archive.summary`のフォールバック(`default('点検処理が完了していません')`)は本diffで変更されておらず、AC5に影響しない。

### 確認範囲

- 001requirement全文、002implementation全文。
- 対象diff全体(`storage_archive.py`、`storage_monthly.py`、`report.yml`、`tests/test_storage_monthly.py`)。
- `python3 -m unittest tests.test_storage_monthly -v`の独立再実行(17件成功)。
- decoy fixture(4host、実ホスト非使用)による`build_report`/`short_summary`の直接呼び出しでMajor #1を再現。

### 未解決事項

- Major #1の修正方法(`issues`全体を重大度降順でソートしてから`[:3]`する、またはCRITICAL/WARNING/UNKNOWNを別集計してSlackへ優先度順に詰める等)はCoordinator/Implementerが選ぶ。

### Verdict

Request Changes(Major 1件)。
