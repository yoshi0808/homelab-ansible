# ストレージ月次点検レポート可読性改善 test_plan

正本: 001要件。対象diff: `roles/proxmox_storage_monthly/module_utils/storage_monthly.py`(`HEALTH_LABELS`/`ISSUE_LABELS`/`display_time`/`issue_text`/`sorted_issues`/`comparison_text`/`short_summary`/`markdown`書き換え)、`roles/proxmox_storage_monthly/library/storage_archive.py`(`summary`追加)、`roles/proxmox_storage_monthly/tasks/report.yml`(Slack短報を`summary`ベースへ)、`tests/test_storage_monthly.py`。対象は`# tester-gate: check-mode-native`(playbook自体は002/005レビューの対象と同じ、今回の変更は表示層のみで保存・公開・schedule契約は不変という主張を検証する)。

## 静的検証(独立再実行)

- S1: `python3 -m unittest discover -s tests -p test_storage_monthly.py -v`(現在18件)。
- S2: `ansible-playbook playbooks/proxmox_storage_monthly.yml --syntax-check`。
- S3: `bash scripts/check-tester-gate.sh`。
- S4: `git diff --check`。
- S5: `python3 scripts/check-doc-consistency.py`。

## Fixture動的検証(すべて独立構成、`/tmp`または直接importでの純粋関数呼出。実ホスト・実Notion・実Slack不使用)

- D1 (AC1): 正常初回reportを独自fixtureで構築し`markdown()`/`short_summary()`を実行。「異常は見つかりませんでした」、初回比較の説明文、ZFS/NVMeのラベル付き値、生JSON dump(`"values": {`等)が本文に無いことを確認する。
- D2 (AC2): error-log増加reportを独自fixtureで構築し、対象host/device・増加数・「要確認」の文言がmarkdown/短報双方に出ることを確認する。
- D3 (AC3): 収集不能・履歴破損・ZFS/NVMe異常の3種類を独自fixtureで構築し、理由が隠されず、対応事項リストの先頭側(UNKNOWN/CRITICAL)に来ることを確認する(005が確認した優先順位ロジックの別条件での再現)。
- D4 (AC4): 保存済み旧JSON(002以前の実際のschemaを模した独自fixture)を`operation=replay`でこのplaybookに通し、JSON自体が変わらないこと、新形式のmarkdown/summaryが生成されることを確認する。
- D5 (AC5): native `--check`と`skip_notifications=true`の組合せで実行し、rc=0、実Notion/Slackへ送信されないこと(既存契約からの非回帰)を確認する。
- D6: 既存JSON schema(`schema_version`/`hosts`/`issues`等のキー集合と型)と判定結果(health/collection/comparison)が本変更前後で不変であることを、保存されたreport JSONのキー突合で確認する(非回帰)。

## 対象外・Not Run

- 実Notion表示、実quory配備後のreplay、実pve1/pve2データでの表示確認はTester権限外/未配備のためNot Run。

## 完了条件

各シナリオの実測結果とVerdict(PASS/FAIL/Not Run)を`2026-09-12_007_test_result.md`へ記録する。対象実装は変更しない。
