# レビュー対応

003 Major #1を採用。保存JSONのissues順序は変えず、表示関数内で判定不能、異常、要確認の順へ並べる。Slackは並べ替え後の先頭3件を使い、Markdownの対応事項も同じ順とした。重大度タグは日本語化した。

未ソートの要確認3件より後ろに異常・判定不能を置くfixtureを追加し、Slack短報へ両者が残り、本文では判定不能・異常・要確認の順になることを検査する。

Suggestion #2は現行コードが生成する固定reasonをすべて翻訳済みであり、今回のblockingとはしない。新しいreasonを追加するときの翻訳追加はレビュー対象として残る。
