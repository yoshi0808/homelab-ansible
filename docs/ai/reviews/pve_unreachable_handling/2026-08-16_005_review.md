# review: pve_unreachable_handling(2ラウンド)

査読者: codex 側 `reviewer`(agmsg 経由。別プロセス・別モデル)
記録者: Coordinator(agmsg のメッセージ本文からの転記。**判定と根拠は査読者のもので、Coordinator の要約や再解釈を混ぜていない**)
対象: 作業ツリーの未commit差分。Reviewed HEAD `036ca23`

査読者には repo を変更しない制約を課したため、成果物がこのファイルとして残らなかった。**この記録が無いと、実装 v1 から v2 へ設計が変わった理由を repo から再構成できない**ため、Coordinator が転記した。

## Round 1(2026-08-16 03:24Z) — Verdict: **Request Changes**

対象は5ファイル・180行追加。

### Critical #1

`roles/deployment_drift_check/tasks/evaluate.yml`(finding builder 8本の `when:`)

`deployment_drift_check_reachable` は `collect.yml` 冒頭の ping 結果を固定した値であり、後続の `stat` / `command` / `slurp` の各 result が持つ `unreachable` を見ていない。**ping 成功後に接続が落ちると**、実際には `unreachable: true` かつ content 無しの keys result が `forced_command` / `expected: readable` / `actual: read failed` の finding になり、`report.yml` では当該ホストが `checked`、`unverified` が空になる。

production の evaluate / report 式を使った local-only fixture で `rc=0` / `drift 1` / `checked=decoy_up` / `unverified=(なし)` を再現。これは 2026-08-11 task 676 で観測された誤分類と同じ形である。

実装記録 §7 が「flapping の既知リスク」として受容していた判断は、**R4(到達不能起因を finding にしない)・R5(unverified へ載せる)・AC4 を直接破る**ため受容できない。

### Suggestion #1

`roles/deployment_drift_check/tasks/report.yml`

`selectattr` は `deployment_drift_check_reachable` 未定義を安全に分類できない。collect を通らなかった inventory-only ホストは checked / unverified の双方から無言で消え、先行 play に参加したが fact を publish しなかったホストでは**未定義エラー**も再現した。欠落 fact を明示的に unverified または fail-closed にする fixture を追加すること。

### Suggestion #2 / #3

- 共通 ping を1本足しても、到達不能ホストへの既存 collect task はすべて試行され続ける。probe 後の追加接続回数・所要時間を Tester が観測すること。(→ Tester へ回した)
- AC5 を「自明」、AC6 を「ロジック上同一」で閉じず、3 fixture を成果物へ残すこと。

### Round 1 で成立を確認した範囲

- `recovery_exec` 側は明示 `wait_for` を pve ループ手前で1回実行し、対象/除外名を出し、6タスクすべてを reachable list へ差し替えている。**authy / monnie ループは不変。**
- `wait_for` は構造化引数で shell 注入を増やさず、`check_mode: false` で check-mode auto-skip 誤判定を避けている。timeout 5秒、inventory の pve host/port との不一致なし。
- 初期状態が到達不能な production 経路、AC5 相当、AC6 相当をいずれも decoy で `rc=0` 確認。既存 builder の Jinja 本体は差分上不変。
- `proxmox_reachable_nodes` 本体を無理に再利用せず、明示 probe の考え方だけを踏襲している(requirement 非ゴールどおり)。
- 多層 escape の新設なし。新規 command / shell なし。秘密情報・内部 IP の追加なし。
- 査読者は repo を変更していない(diff SHA-256 `007a70c6a6fb0dd06107a9c90e1c2e41162a7d767ec136c067f647db5b244bad`)。

## Round 2(2026-08-16 03:42Z) — Verdict: **Approve**(findings なし)

対象は5ファイル・245行追加 / 19行削除。

### 前回 findings の解消

- **Critical #1: 解消。** collect の8 register の各 item に `unreachable` が1件でも定義されれば `collect.yml:188-202` でホストの reachability が false へ格下げされ、evaluate の全 finding builder が skip される。production task を include した混在 fixture で、**flap 相当ホストは finding 0 / unverified、到達可能だが keys 読取失敗のホストは `forced_command` finding 1 / checked** となり、AC4・AC5 の区別と他ホストの finding 維持を確認した。
- **Suggestion #1: 解消。** `report.yml:52-77` の明示 loop と `default(false)` により、reachability fact 未定義ホストは例外なく unverified へ分類されることを production report include で確認した。

### 追加確認(Coordinator が挙げた懸念への回答)

`selectattr('unreachable', 'defined')` が真偽値ではなくキーの存在を見ているため、健全なホストが unverified へ落ちないかを確認するよう依頼した。

- `stat` / `command` / `slurp` / `uri` の実モジュールを **loop 有無の両方**で通常実行し、成功結果と通常失敗(uri 接続拒否)に `unreachable` キーが**付かない**ことを確認。
- `--check` でも stat / slurp 実行、command skip、uri(`check_mode: false`)実行の各結果に同キーは無く、新 downgrade 条件で到達可能ホストは unverified へ落ちなかった。**AC2 / AC6 の結果分類に回帰は認めない。**
- `--check` でも新 `set_fact` は実行される。command の skip 結果を含め、到達不能を観測しない限り true を維持する。
- mixed-host fixture は `rc=0` / `drift=1` / `checked=[reachable]` / `unverified=[unreachable]` で assert 通過。
- 両 playbook の `--syntax-check` pass、`git diff --check` pass。

### 観点別

新たな秘密露出、権限拡大、silent exception absorption、終了コード変更、escape 多層化、不要な重複はいずれも認めない。実装記録 §8 の因果説明と fixture 主張は、独立 fixture の結果と整合する。

### 査読者が明示した未確認

実ホストへの接続・実ジョブ実行は Reviewer の権限外のため未実施。**AC2 / AC4〜AC6 の実環境確認は Tester および実運用へ継続すること。**
