# audit: pve_unreachable_handling

作成: 2026-08-16 / Auditor

対象: `docs/ai/reviews/pve_unreachable_handling/`(001〜005)、および作業ツリーの未commit差分5ファイル。

## 1. 再構成できたか

**できた。** 001(requirement)→002(implement、途中で§8として差し戻し対応を追記)→003/004(test_plan/test_result)→005(review、2ラウンドの転記)という経緯を、他の文書やCoordinatorの説明なしに記録だけから辿れた。

再構成の要点:

- ①`recovery_exec_setup.yml`のUNREACHABLE昇格、②`deployment_drift_check`のunverified空洞化という2つの不具合、それぞれの機構がrequirement §1で説明され、implement §2でdecoyにより実測・裏取りされている。
- Q1〜Q4の各オープンクエスチョンが、誰が(Implementer)・どう答えたか(decoy実測/設計判断)がimplement §3に残っている。
- レビューでCritical#1(mid-run flap)とSuggestion#1(`selectattr`未定義クラッシュ)が指摘され、implement §8で修正され、review round2で解消確認、という一往復が両ファイルに整合して残っている。
- Tester(003/004)は実装の主張を鵜呑みにせず現物のコード・実行結果で判定し、否定対照実験でdecoy自体の妥当性も裏取りしている。

file:lineの参照を実物と突き合わせた結果、指し示す内容は一致した(例: review round2の`collect.yml:188-202`・`report.yml:52-77`は実ファイルの当該行と一致)。`git diff --stat`の実測値(5 files, 245 insertions(+), 19 deletions(-))は review round2 が記す最終差分の行数(「245行追加/19行削除」)と一致し、現在の作業ツリーがreviewが承認した対象と同一であることを裏付けている。

## 2. 指摘

| 分類 | 内容 | クローズを妨げるか |
|---|---|---|
| 根拠の実在 | `roles/deployment_drift_check/tasks/collect.yml:187`のコメントが `2026-08-16_002_implement.md §追記2参照` を指すが、implement.md にその見出しは無い。該当箇所は実際には `## 8. 差し戻し対応(2026-08-16、独立レビューRequest Changes)` である。内容自体は見つかるが、見出し名で辿ろうとすると空振りする | **妨げない**(内容は同一ファイル内にあり、実質的な追跡は可能) |
| 未解決の明示(軽微) | R8(`authy`/`monnie`にも同じ構造の欠陥が残る)は requirement 本文にのみ「記録に残す」と書かれており、`docs/ai/status.md` には未記載。本案件クローズ時に該当行(status.md 49行目)が除去されると、この残存欠陥はこの案件フォルダを開かない限り追跡できなくなる | **妨げない**(requirement文書自体に記録は存在する。ただしCoordinatorがクローズ時にstatus.mdへ引き継ぐか判断すべき事項として指摘する) |

いずれもクローズを妨げる指摘ではなかった。決定の追跡・記録どうしの矛盾・反証の反映・受入条件の充足・転記先の実在の各観点で、上記2件以外の欠落は見つからなかった。

### 補足: AC別の再構成結果

- AC1〜AC3: test_result が decoy実行のコマンド・実測出力・判定を個別に記載し、requirementの要求(rc、対象/除外ホスト名、authy/monnie不変)と1対1で対応している。否定対照実験(旧実装が同型で壊れることの裏取り)も再構成できた。
- AC4/AC5: 同一runでの実測として一貫している。到達不能host由来findingが0件、到達可能host側のgenuine読み取り失敗はfinding計上、という対比が記録から読み取れる。
- AC6: test_result 自身が「PASS(部分)」と明記し、完全A/B比較ができない理由(作業ツリーを変更しない制約下でのretreat/restore手段が無い)を述べている。**未解決の明示として機能しており、隠れた欠落ではない。**
- R6/R7: R6(`ignore_unreachable: true`を外さない)はimplement §8.3で「一度も触れていない」と明記され、diffで裏付けられる(`playbooks/deployment_drift_check.yml`はdiffに含まれない)。R7(probeタイムアウトの体感計測)は実pveに到達できないため未実測であることがtest_result・implement双方に明記されている。

## 3. 状態の正本との突合

`docs/ai/status.md` 49行目(「pve1 の平日停止が、到達不能ではなく『異常』として扱われる(2箇所)」)が、本案件が解消した事象そのものを指している。**この行は本案件のクローズ処理としてCoordinatorが除去すべき対象であり、これは指摘ではなく実施事項である。**

他に本案件が影響する`docs/ai/status.md`の行(Next側)は見当たらなかった。

## 4. 判定

**受入。** 記載した2件はいずれも非blockingであり、そのまま案件をクローズしてよい。status.md 49行目の除去は同一セッション内でCoordinatorが行うこと。
