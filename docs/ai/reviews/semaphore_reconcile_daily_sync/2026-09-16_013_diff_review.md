# Diff Review: semaphore_reconcile_daily_sync Phase 2

対象: 作業ツリーの未commit差分(`git status --short`実行時点)。requirement正本
`docs/ai/reviews/semaphore_reconcile_daily_sync/2026-09-15_001_requirement.md`
§5 P0-5〜P0-15、§6 AC9〜AC19、§7.1〜§7.3。実装記録
`docs/ai/reviews/semaphore_reconcile_daily_sync/2026-09-16_012_implementation.md`。

計画査読は本レビューの対象外(別contextが担当済み、`_010_phase2_plan_review.md` Approve)。

## Summary

P0-5/P0-6/P0-8/P0-8a/P0-9/P0-10/P0-11/P0-12/P0-13/P0-14の各実装は、requirementの
決定表・状態表と現物の分岐がおおむね一致しており、AC9・AC9a・AC9b・AC10・AC12・
AC13・AC13a・AC13b・AC14・AC15・AC16・AC17・AC19はコードの分岐として妥当に成立
している(個別の確認は本文参照)。

**Critical 1件**: P0-7の更新合算上限は、書き込み前の計画(`schedules_plan.yml`)
だけで判定され、実際に書き込みへ使われる最終スケジュール差分
(`schedules_diff.yml`、template apply後にfresh GETし直した値)に対しては
一度も再検証されない。テンプレート側の作成上限は`main.yml`と`schedules_diff.yml`
の両方で最終値を再チェックしているのに対し、schedule側の更新件数だけがこの
再チェックを欠いている。AC11の要求(「上限判定がtemplateのapplyより前に行われる
ことを、templateだけ適用済みが発生しないことで示す」)は満たすが、「実際に書く
件数が上限を超えない」という主眼そのものは、plan時点のスナップショットと最終
diffの間に差が生じた場合に保証されない。

**Major 1件**: requirement P0-15最終段落が明記した
「EXEC-052の根拠(鍵が無く到達不能)から『発火しない』は導けないことを
`docs/ai/context/system/semaphore.md`へ明記する」という要求が、差分中の
`docs/ai/context/system/semaphore.md`更新に反映されていない。

Suggestionを2件、Dead codeを1件記載する。

## Critical Issues

| # | File | Line | Issue | Severity |
|---|---|---|---|---|
| 1 | `roles/semaphore_templates/tasks/schedules_diff.yml` | 51-68 | `semaphore_schedules_diff_result`(実際にschedules_apply.ymlが使う最終差分)は、`schedules_plan.yml`とは別の`_semaphore_schedules_detail_raw`GET(main.ymlのtemplate apply後、`schedules_read.yml`/`schedules_timezone_check.yml`/`schedules_preflight.yml`/`schedules_friday.yml`を経た後)から再計算される。この最終`changed`件数を`semaphore_reconcile_max_updates`と突き合わせるassertがここには無い(60-68行のassertは`new`=作成件数のみを再チェックし、`changed`=更新件数を見ない)。 | Critical |
| 2 | `roles/semaphore_templates/tasks/schedules_plan.yml` | 65-90 | 更新上限の判定(`semaphore_reconcile_update_plan_result`)は、この時点で取得した`semaphore_schedules_plan_detail_by_id`(41-53行の独立したGETループ)を使って計算される。これはtemplate applyより**前**に完了する別個のAPI呼び出しであり、finding 1の再取得までの間にscheduleのフィールドがUIで変わった場合(またはこの2つの差分計算ロジック自体が将来ズレた場合)、ここで許可された件数と実際にPUTされる件数が一致する保証が実装のどこにも無い。 | Critical |
| 3 | `roles/semaphore_templates/tasks/main.yml` | 88-105 | Finding 1/2の間に挟まるtemplate apply(`apply.yml`、複数件のPOST/PUTを伴いうる)は、この2つの独立したschedule detail GETの間の時間窓を実質的に広げる。requirement P0-7は「守るのは、望ましい状態を計算する機械が壊れて、write-setが人の手の大きさを超える形へ膨らむ場合」と明記しており、この場合に最終適用件数を検証しない実装は、その主眼を実際には守れていない。 | Critical |

**再現条件の例**: quoryで、`schedules_plan.yml`のGET直後(template apply中)に、UIで
無関係な5件のscheduleの`cron_format`を編集する。plan時点の`changed`は0〜数件
だったため`semaphore_reconcile_update_plan_result.allowed=true`となり書き込みが
進むが、`schedules_diff.yml`の最終`changed`は5件超になりうる。この5件はcapの
再検証を一度も受けずに`schedules_apply.yml`へそのまま渡り、無人実行のまま
`semaphore_reconcile_max_updates`を超えて書き込まれる。

**修正の方向**: `schedules_diff.yml`の51-58行で`semaphore_schedules_diff_result`
を確定した直後に、`semaphore_templates_diff.changed | length`(template側、これは
apply前後で不変)と`semaphore_schedules_diff_result.changed | length`の合算を、
`semaphore_reconcile_update_plan`へ再度通し、`allowed`でなければ**この時点で**
非ゼロ停止する(template側は既にapply済みのため、AC11が求める「template
だけ適用済み」を新たに発生させない範囲での対応が要る — 少なくとも、schedule
apply前に検出してschedule書き込み0件で止め、run reportへ`status=partial`
相当で記録する形になるはずである。既存の作成件数側の再チェック
(main.yml:91-98、schedules_diff.yml:60-68)と同型の対策で足りる)。

## Suggestions

| # | File | Line | Suggestion | Category |
|---|---|---|---|---|
| 1 | `docs/ai/context/system/semaphore.md` | 27-36(追加は19行のみ) | P0-15最終段落が要求する「EXEC-052(鍵が無く到達不能)の根拠から『scheduleが発火しない』は導けない」という注記が無い。2026-09-16のIncident(ansyのactive scheduleがSlackへ実際に通知した事実)を踏まえた、まさにこのSystem Contextへの明記がrequirementの決定事項であり、19行目の日次reconcileの契約説明だけでは代替にならない。 | Requirement対応漏れ |
| 2 | `roles/semaphore_templates/tasks/main.yml` / `roles/semaphore_templates/tasks/schedules_report.yml` | main.yml:251-257,278-314、schedules_report.yml:141 | P0-10は「生の失敗値を出さない」ことを要求しており、既存の`regex_replace`によるtoken-scrub済みメッセージ(`_semaphore_templates_scrubbed_failure`/`semaphore_schedules_run_failed_msg`)は元々この基準を満たす設計だった。本差分はこのscrub済みメッセージ自体を再送出・報告経路から完全に落とし、固定文言("API failure detailはconsole/report/notificationへ出しません"/"failure detail withheld by P0-10")へ置き換えている。AC14には抵触しないが、requirementが要求した以上に診断情報を失っており(scrub済みメッセージがどのAPI呼び出しで何が起きたかを示す唯一の手がかりだった)、この判断はrequirement/決定表のどこにも記載が無い。復元するか、少なくとも実装記録へこの判断とその理由を残すことを勧める。 | 過剰な抑制 |

## Dead Code

| # | File | Line | Note |
|---|---|---|---|
| 1 | `roles/semaphore_templates/tasks/schedules_apply_item.yml` | 40-50 | 「Guard against any accidental false-to-true update value on a non-canonical connection」taskは、`_semaphore_schedules_apply_effective_after.active`が非canonical時に常に`false`(`semaphore_schedules_existing_write_desired`が無条件でそう設定する、`filter_plugins/semaphore_schedules.py:713-721`)であるため、条件式`fresh.active | semaphore_schedules_would_newly_activate(effective_after.active=False)`は`before=*, desired=False`の組み合わせで常にFalseを返し(`semaphore_schedules_would_newly_activate`は`desired_active`がTrueのときしか有効化と判定しない、同ファイル866行以降)、このfail taskは到達しない。実害は無いが、二重の防御に見えて実際には機能していない。 | Minor |

## What Looks Good

- P0-5/AC9/AC9a/AC9b: `semaphore_schedules_diff`(`filter_plugins/semaphore_schedules.py:724-765`)がcanonical時に`desired['active'] = before['active']`で上書きしてから`changed_fields`を計算するため、`active`単独差はdiffに現れない。書き込み直前は`schedules_apply_item.yml:32-38`のfresh GETで`_semaphore_schedules_apply_effective_after`を再構築し、diff計算時点のスナップショットに依存しない。両者を突き合わせて確認した。
- P0-15/AC19: canonical判定の正本が`filter_plugins/semaphore_schedules.py`のモジュール定数`CANONICAL_SEMAPHORE_API_BASE_URL`へ移り、対応するAnsible変数`semaphore_schedules_canonical_api_base_url`は`defaults/main.yml`から削除、`schedules_validate_config.yml`の外部設定allowlist(4項目)からも除外された。`-e`でこの名前を再定義するとreserved-name guard(`schedules_validate_config.yml:95-130`)が拒否することを`canonical_override_guard.yml`+`scenario_h`で確認した。非canonical時の4ケース(新規/既存false/既存true+差分/既存trueのみ)はすべて`semaphore_schedules_existing_write_desired`と`semaphore_schedules_desired`の分岐で無条件にactive=falseへ倒れる実装になっており、requirementの表(§5 P0-15)と一致する。
- P0-7/AC11a: 「計画」と「payload」の分離(§7.2決定#1)は、`schedules_plan.yml`が仮ID(`-(loop.index)`)で件数だけを確定し、実際のtemplate_id解決は既存のR10機構(`schedules_read.yml`のtemplate apply後fresh GET→`schedules_preflight.yml`の名前解決)へ委ねる形で実装されており、新規に導入したpending解決ロジックと既存のR10ロジックを重複させていない。設計として妥当。
- P0-8/P0-8a/AC12/AC13/AC13a/AC13b: `reconcile_finalize.yml`は、run report(94-130行)を必ずmarker(132-168行)より先にpublishし、markerには`marker_published`のような「未来の事象」を持たせていない(§7.3決定#1のとおり、markerの成否は`_semaphore_reconcile_marker_publish_failed`という別factで扱い、run report自身は`status`までしか持たない)。marker publish失敗時にreport側の`status`が`success`のまま据え置かれ、markerは前回の値のまま残る、という状態表の行を実装のwhen条件で辿って確認した。
- P0-6/AC10: `semaphore_schedules_friday_paused`(`filter_plugins/semaphore_schedules.py:658-673`)はタイムゾーン無しの`now`を`ValueError`で拒否し、`active`が非bool型の行を通さない。金曜以外は空リストを返す。
- P0-11/AC15: `semaphore_templates_orphan_baseline_diff`はsorted listでの比較(setにしない、§7.1決定#6)、name/playbookの型・空文字列チェック、baseline自体の型検証を行い、いずれの異常も`errors`へ積んで`main.yml:81-86`のassertで書き込み前に停止する。
- P0-12/P0-13/AC16/AC17: `semaphore_probe.yml`から`semaphore_template`findingを完全に除去し、`semaphore_probe_error`/`checked_counts.semaphore_templates`の生成経路は変更していない。`semaphore_reconcile_stale`は`semaphore_reconcile_marker_freshness`で欠落・非正規ファイル・不正JSON・未来時刻・24時間閾値のすべてをstale扱いにし、判定不能を「正常」へ倒していない。

## Verdict

Request Changes — Critical 1件(P0-7更新合算上限の最終値未検証)の修正が必要。
Major 1件(semaphore.mdのEXEC-052注記漏れ)はrequirement対応の完了条件として
追記が必要。Suggestionsは実装判断として妥当なら実装記録への追記で足りる。
