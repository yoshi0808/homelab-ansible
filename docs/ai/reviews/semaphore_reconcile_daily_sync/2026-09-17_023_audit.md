# 再監査: semaphore_reconcile_daily_sync

日付: 2026-09-17
対象: `docs/ai/reviews/semaphore_reconcile_daily_sync/` の `_001`〜`_022`、`docs/ai/status.md`、および `_019` が参照するcommit。

## 判定

**条件付き受入。**

案件の発端、Phase 1 / Phase 2の計画査読・実装・差分レビュー・受入検証、本番bootstrap、本番probe不具合、初回04:00自然発火、独立監査とその後の是正まで、repoの記録だけで再構成できる。`_020`が指摘した#1132の根拠欠落は`_021`で解消し、工程集計は査読5本・blocking 18件・Phase 2差分レビュー4本へ訂正された。工程の担当・起動方法・contextの分離も`_019` §2に記録された。AC9系、AC10の金曜2ケース、AC13のschedule途中失敗については、未充足・理由・今後の扱いが明示された。

ただし、AC13の「evidence生成失敗」は未実測なのにPASSへ集約され、未充足としても受容されていない。また、closeoutとstatusの是正件数、AC14の確認内容、完了済みNext行の扱いに記録上の不一致が残る。下記のクローズを妨げる指摘を記録へ反映すれば、再監査なしでクローズしてよい。

## 指摘

| 分類 | 指摘と根拠 | クローズを妨げるか |
|---|---|---|
| 受入条件の充足 / 反証の反映 | **AC13の「evidence生成失敗」は実測されていない。** `_022:217-228`は、ansyでは収集器の設定ファイルが無いためcapture taskが構造的にskipされ、`capture.yml`自体を能動的に失敗させる注入は行っていないと明記する。一方、`_022:316`は「evidence生成失敗/Slack送信失敗」をPASSとし、`_019:67`も4ケース実機PASSの1つへ数えている。skipは生成失敗ではなく、P0-8a / AC13が要求する「evidence生成失敗でも本来のrcを変えない」の実測根拠にはならない。にもかかわらず、`_019:76-82`の未充足受容3点にこのケースが無い。 | **妨げる。** 実測するか、未確認のまま受容する判断と理由を`_019`へ追加し、`_022`のPASS集約を「Slack送信失敗はPASS、evidence生成失敗は未確認」へ正す必要がある。記録訂正だけで閉じる場合も再監査は不要。 |
| 記録どうしの矛盾 | **是正件数の要約が詳細と一致しない。** `_019:61`と`status.md:18`は「指摘8点のうち7点を是正」とするが、`_020`の8指摘を単位に数えると、完全に是正されたものはAC14/17、#1132、status、集計、独立性の5指摘であり、AC9・AC10・AC13の3指摘は未充足の受容または観測待ちを含む。さらに`_019:61`は「1点は判断」とする一方、同ファイル`_019:76`は未実測3点を列挙し、`status.md:18`は受容2点+観測待ち1点とする。詳細表から実態は再構成できるが、要約の数え方が一意でない。 | **妨げる。** 「5指摘を完全是正、3指摘は未充足ケースを記録して判断」のように、同じ単位で`_019`と`status.md`を揃える。AC13 evidence生成失敗を受容する場合は、その追加も反映する。 |
| 根拠の実在 / 記録どうしの矛盾 | **AC14の確認内容をcloseoutが過大に記す。** `_019:68`はtokenと生の失敗文が各出力面に無いことを「grep 0件」で確認したとする。しかし`_022:250,261`がgrepしたと明記する対象はtoken値であり、`_022:257`にはscrub済みの接続失敗詳細がconsoleへ出た実測がある。`_022`の結論は、token非露出と、Authorization値・HTTP応答本文を含まないscrub済み診断の維持である。closeoutの「生の失敗文をgrep 0件」は同成果物から再構成できない。 | **妨げる。** `_019`を`_022`の実測範囲どおり、token値はgrep 0件、接続失敗詳細はscrub済み診断として出力、Authorization値やAPI応答本文の露出なし、という記述へ直す。 |
| 状態の正本 / 差分の外側の陳腐化 | **完了済みのNext行が残っている。** `status.md:81`はPhase 1で解消済みと訂正されたが、打消し線付きのままNext表へ残り「次の監査サイクルで削除してよい」としている。`status.md`自身の規律は「完了したら行を消す。履歴を残さない」であり、`_020`が求めた陳腐化解消は、完了履歴をNextへ残す形では完了していない。履歴と根拠は案件成果物とcommit `8dfcbf8`から辿れる。 | **妨げる。** 当該Next行を削除する。 |

## 再構成できた是正と残存事項

- `_021`により、#1132の`user_id: None`、`params: None`、04:00 JST、diff、run report→markerのpublish順、通知なしを辿れる。
- `_022`により、AC10の非金曜ケース、AC13の全成功・変更なし / template途中失敗 / run report保存失敗 / Slack送信失敗、AC14のtoken読取不能・API到達不能、AC17の`semaphore_probe_error`分岐を辿れる。
- AC9 / AC9a / AC9bはfixture代替で閉じる判断、AC13のschedule途中失敗は未実測で受容する判断が`_019`にある。
- AC10の金曜2ケースは2026-09-18の自然実行待ちとして`_019`と`status.md`に残り、黙って落ちていない。案件クローズ後も観測待ちを状態の正本へ残す扱いは再構成できる。
- commit `f0f3558`の「4回・17件」は誤りだが、後から変更できないcommit messageの誤記として`_019`に訂正がある。現物は査読5本・blocking 18件で一致する。
- 1件の意味的誤記、Semaphore / scheduler / quory全停止、全体ansible-lintの既存load-failure、lost-marker判定の限界は、従来どおり残存事項として記録されている。

## クローズ時の実施事項

上記4指摘を`_019` / `_022` / `status.md`へ反映した後は、再監査を起動せずクローズしてよい。金曜のAC10観測待ちを残すため、`status.md`の案件Now行は単純削除せず、クローズ済み案件の後続観測であることが分かる形を維持する。

監査ではrepo成果物とgit現物だけを読み、実ホスト、Semaphore API、Slack、Notionへ接続していない。既存成果物・実装・statusは変更せず、本ファイルだけを作成した。git add / commit / pushは行っていない。
