# Test Result (再検証): semaphore_reconcile_daily_sync AC18修正 + AC11/AC11a/AC19回帰 + Minor 2件

対象: `2026-09-16_015_test_result.md`が報告したCritical欠陥(P0-14 bootstrap行の
`task_params.environment`が自分自身のallowlistで拒否される)への実装者の修正申告を、
実機(ansyのSemaphore、project id=3)で再検証する。安全条件は前回と同じ
(ansyのSemaphoreのタスクとして走らせない、activeなscheduleを残さない、quoryへ触れない、
本番Slackへ出さない)。`_015`は上書きしていない。

実装者の申告(主張であり、以下で確かめた対象): (1) environment preflightがcanonical URL
リテラルと文字列`"true"`の`validate_certs`だけを受理するよう厳格化された、(2) 現行カタログ
全25行(bootstrap含む)のpreflightがerrors=0で通るようになった、(3) run report/marker publish
の`.tmp`一時ファイルの後始末、(4) 鮮度findingの`actual`表示、の4点。

## 0. 検証環境(前回と同一)

- ansyのSemaphore(`https://localhost:3000`、project id=3)のみを使用。quory・本番ホストへは
  接続していない。
- `ansible-playbook`を直接起動する形で実行し、Semaphoreの「タスク」としては一度も起動して
  いない(§4で開始時・終了時のtask一覧の一致を確認)。
- 接続は`-e ansible_connection=local`(inventory既定の`ansible_ssh_private_key_file:
  ~/.ssh/id_ann`は2026-08-19削除済みで到達不能なため)。実行内容(role本体・catalog・API
  接続先)自体は変えていない。
- `semaphore_templates_report_dir`/`semaphore_schedules_report_dir`は`/tmp`配下の書き込み
  可能なpathへ`-e`で上書きした(既定値`/home/ann/...`はyoshi実行では書けないため)。
- 検証用に作成したオブジェクトは全て`ZZ-TEST:`prefixで作り、検証後に自分で削除した(§5)。
- 前回同様、check-mode-nativeへの`--check`必須(TS-023)の原則を、ansy(EXEC-052の指定
  検証環境)への実書き込み検証に限り踏み越えた。判断根拠は`_015`§0と同じで変更していない。

## 1. AC18再検証 — preflight通過から書き込みまで到達するか

### 1.1 コード確認

`filter_plugins/semaphore_schedules.py`の`_ENVIRONMENT_ALLOWED_KEYS`に
`semaphore_templates_api_base_url` / `semaphore_templates_api_validate_certs`が追加され、
`_environment_value_is_allowed()`が両キーの**値**も検査するようになっている(キーの存在だけ
でなく、`semaphore_templates_api_base_url`は`CANONICAL_SEMAPHORE_API_BASE_URL`
(`https://quory.internal:3000/api`)と完全一致する文字列のみ、`semaphore_templates_api_
validate_certs`は文字列`"true"`のみを許可)。

### 1.2 実機確認(現行カタログそのまま、`--check`)

```
ansible-playbook playbooks/semaphore_templates_setup.yml \
  -e semaphore_target=ansy -e ansible_connection=local \
  -e semaphore_templates_report_dir=<書込可能path> \
  -e semaphore_schedules_report_dir=<書込可能path> \
  --check
```

rc=0。**前回は`readset_preflight.yml`で必ず停止していたが、今回はpreflightを通過し、
schedule diff計算・`schedules_apply.yml`の直前(`--check`のためskip)まで到達した。**
schedule diffレポート(`latest.json`)で確認:

```json
"diff": {
  "changed": [],
  "new": [{
    "desired": {
      "active": false,
      "cron_format": "0 4 * * *",
      "name": "SEMI-SAFE: Semaphore reconcile daily",
      "task_params": {
        "environment": "{\"semaphore_templates_api_base_url\":\"https://quory.internal:3000/api\",\"semaphore_templates_api_validate_certs\":\"true\"}"
      },
      "template_id": 62
    },
    "name": "SEMI-SAFE: Semaphore reconcile daily"
  }],
  "unchanged_count": 24
}
```

「新規作成 : 1 件」「無変更 : 24 件」がShow summaryにも表示され、canonical判定
(`url_matches: false`、ansy=非canonical)も正しく効いていることを確認した。

### 1.3 実機確認(実apply、`--check`無し)— bootstrap scheduleを実際に作成

同じコマンドから`--check`を外して実行:

```
message : Semaphore reconcile status=success. templates create/update=0/0;
          schedules create/update=1/0. failed_task=.
```
rc=0。API GETで実物を確認(id=53):

```json
{
  "id": 53, "template_id": 62, "cron_format": "0 4 * * *",
  "name": "SEMI-SAFE: Semaphore reconcile daily", "active": false,
  "task_params": {"environment": "{\"semaphore_templates_api_base_url\":\"https://quory.internal:3000/api\",\"semaphore_templates_api_validate_certs\":\"true\"}"}
}
```

`template_id=62`は`SEMI-SAFE: Semaphore templates setup`(bootstrap template、requirement
P0-14が指す対象)と一致。`active`は非canonical接続先であるため`false`(仕様どおり)。
検証後、このscheduleは削除した(§5、id=53を`DELETE`)。

**結論: AC18(bootstrap)はpreflight通過から書き込み・readbackまで実機で確認できた。
PASS。**(実ホストへの適用自体はYoshinobuの操作であり、本検証はansy上での成立可否の
確認に留まる。)

## 2. 緩めた許可が緩めすぎていないか

現行カタログとは別に、`ZZ-TEST:`のダミーschedule(既存template「SAFE: Time sync check」
参照)を使い、`environment`へ不正な値を注入した3ケースを実機で確認した
(`semaphore_schedules_closed_world=false`で他の24件と切り離して検証)。

| ケース | 注入した値 | 結果 |
|---|---|---|
| 非canonicalなURL | `semaphore_templates_api_base_url: "https://ansy.internal:3000/api"` | **拒否**(`の値 (str) が許可契約に一致しない`)、write 0、rc非ゼロ |
| ネイティブbool | `semaphore_templates_api_validate_certs: false`(JSON bool) | **拒否**(`の値 (bool) が許可契約に一致しない`) |
| 文字列だが値違い | `semaphore_templates_api_validate_certs: "false"` | **拒否**(`の値 (str) が許可契約に一致しない`) |

いずれもreadset_preflightの段階で書き込み前に停止し、対象名・フィールドパスがログへ
出ることを確認した。**正しい値(canonical URL文字列 + `"true"`)の組み合わせのみを別途
試したところ、同じ検証(readset_preflight / schedules_preflight)は環境キーに関するエラーを
一切出さずに通過した**(後段のclosed-world制約による別のfailはあったが、環境値の検証とは
無関係)。**緩めすぎてはいない。PASS。**

## 3. 前回PASS項目の回帰確認

### 3.1 AC11 / AC11a(合算上限の境界)— 実機で再確認、いずれもPASS

- **4件更新(既定`semaphore_reconcile_max_updates=4`)**: 成功(`templates create/update=0/4`)。
  同じ4件を実カタログへ戻す更新も成功。
- **5件更新(template側だけ)**: `template/schedule更新合算 5 件が 上限 4 件を超えました`で
  write 0・rc非ゼロ。APIで対象5テンプレートに変更が無いことを確認。
- **混在(template 2件 + schedule 3件 = 5件)**: 同じく合算capでwrite 0・rc非ゼロ、
  APIで両資源とも無変更を確認(「templateだけ適用済み」は発生しない)。
- **AC11a 作成上限内**(新規template 1件 + それを参照する新規schedule 1件): 両方作成され、
  schedule(id=54)の`template_id`が新規template(id=89)と正しく一致することを確認。
- **AC11a 作成上限超過**(新規template 2件 + 新規schedule 2件 = 4件 > 上限2件):
  readset_preflightで`作成予定=4、上限=2`により書き込み前に停止、既存の1件目
  (先の`AC11a作成上限内`テストで作った分)以外は一切作成されないことを確認。
  検証用オブジェクトは全て削除済み(§5)。

### 3.2 AC19(非canonicalでの`active`の扱い)— 実機で再確認、a〜fすべてPASS

| # | シナリオ | 結果 |
|---|---|---|
| a | 新規作成(カタログ`active:true`) | `active:false`で作成(id=56) |
| b | 既存24件(active:false + 他フィールド差あり)への更新 | 24件とも他フィールドのみ同期、`active`は`false`のまま(前回のテスト状態を踏襲、今回はidempotent確認のみ) |
| c | 既存`active:true` + cron差 | cronが更新され、同時に`active`が`false`へ倒れる |
| d | 既存`active:true`のみ(他は一致) | `active:false`へ倒す書き込みが発生し、changedとして数えられる |
| e | `-e semaphore_schedules_canonical_api_base_url=...` | reserved-name guardが書き込み前に非ゼロで停止(値の中身に関わらず変数名の存在で拒否) |
| f | `semaphore_target=ansy`での実行 | 本レポートの全実行がこの設定で行われており、a〜dの結果自体がfの確認を兼ねる |

いずれも前回(`_015`)の実測と一致し、回帰は無い。

### 3.3 fixtureスイート(AC9/AC9a/AC9b含む)

- `python3 scripts/tests/semaphore_schedules/run-tests.py` → **136 tests, OK**(前回131、
  今回の修正で環境キー検証に関する新規testが5件増えている。自分で実行し確認)。
- `python3 roles/semaphore_templates/tests/task_flow/run_task_flow_tests.py` →
  「all nine scenarios passed」(自分で実行し確認)。
- AC9/AC9a/AC9b自体は前回同様**未確認(fixture代替)** — canonical接続先(quory)へ
  Testerは到達できない(EXEC-052)。該当test(`test_canonical_catalog_deactivation_does_
  not_change_existing_active`等)が上記136件に含まれ、PASSしていることを確認した。

## 4. Minor 2件の修正確認

### 4.1 `.tmp`ファイルの蓄積 — 修正確認、PASS

`reconcile_finalize.yml`に「Remove the published run-report temporary file」
「Remove the published latest-success temporary file」が成功経路へ追加されている
(前回は失敗時の`rescue`にしか無かった)。実機で新規の空ディレクトリに対し連続2回
成功実行し、`reconcile/`配下に`.tmp`ファイルが1つも残らないことを確認した
(残るのは`runs/`配下の2つの本体ファイルと`latest-success.json`のみ)。

### 4.2 鮮度findingの`actual`が`null`になる — 修正確認、PASS

新設フィルタ`semaphore_reconcile_marker_actual()`(`age_seconds`が数値ならそれを
優先して`"<秒> seconds"`を返し、そうでない場合だけ`error`文字列にfall back)を
直接実行し、「単に古いだけ」のケースで秒数が正しく出ることを確認した。さらに
`deployment_drift_check`を実機で実行し(markerのcompleted_atを44時間40分前へ
書き換え)、実際に生成されたfindingで

```json
"actual": "160811 seconds"
```
であることを確認した(前回は`null`)。境界値(20時間40分/24時間ちょうど/44時間40分)の
`stale`判定自体もこの修正で変わっていないことを直接実行で確認した(`actual`表示との
組み合わせで確認)。`category: semaphore_template`が出ないこと、`checked_counts.
semaphore_templates`(58)が出ることも同じ実行で確認し、回帰は無い。

## 5. 自己検証(検証終了時点)

### schedule 24本すべて`active: false`、自分が作ったものが残っていないこと

```
GET /project/3/schedules
count 24
active_true_count 0
```
24件の名称一覧は開始時・`_015`終了時と完全一致(`ZZ-TEST`等の残骸なし)。template側も
`count 58`、`ZZ-TEST`残骸なしを確認した。本検証中に作成した`ZZ-TEST:`schedule(id 53, 55,
56)・template(id 89)はいずれも検証直後にDELETEで削除済み。

### 検証中にSemaphoreタスクが1件も作られていないこと

`GET /project/3/tasks`を検証開始前・終了後の両方で実行し、件数・最大idが一致することを
確認した(開始時: 9件・max id=28。終了時: 9件・max id=28。`_015`終了時と同じ値でもある)。
本レポートの全操作は`ansible-playbook`から直接Semaphore REST APIを呼ぶ形、またはこのAPIへの
直接API呼び出しで行っており、Semaphoreの「タスク」実行は一度も発生していない。

## 6. 結論

| 項目 | 結果 |
|---|---|
| AC18(preflight通過〜書き込み〜readback) | **PASS**(実機、bootstrap schedule作成→削除まで確認) |
| 緩めた許可の妥当性(非canonical URL/型違い/値違いを拒否) | **PASS**(実機3ケース) |
| AC11 / AC11a 回帰 | **PASS**(実機、境界4/5件・混在・作成上限内外すべて再確認) |
| AC19(a〜f) 回帰 | **PASS**(実機、全ケース再確認) |
| AC9/AC9a/AC9b | 前回同様**未確認(fixture代替、PASS)** — quory到達不能のため変更なし |
| Minor: `.tmp`蓄積 | **修正確認(PASS)** |
| Minor: `actual`が`null` | **修正確認(PASS)** |

`_015`が報告したCritical欠陥は解消したと判定する。新規のCritical/Majorは見つからなかった。
