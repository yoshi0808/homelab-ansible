# Diff Review (再レビュー): semaphore_reconcile_daily_sync Phase 2 差し戻し反映

対象: 作業ツリーの現差分(`git status --short`実行時点)。前回レビュー
`docs/ai/reviews/semaphore_reconcile_daily_sync/2026-09-16_013_diff_review.md`
のCritical 1件・Major 1件・Suggestion 2件・Dead code 1件が対象。requirement正本
`docs/ai/reviews/semaphore_reconcile_daily_sync/2026-09-15_001_requirement.md`。

実装者の申告5点(主張)を、現物のコードと実行可能なテストで確認した。

## Summary

5点すべて、申告どおりの修正が現物に入っていることを確認した。Critical・Majorとも
**解消**。round1で指摘したSuggestion 2件・Dead code 1件も、この差し戻しの副産物
として解消されている。round1で「コード上は成立」と判定したAC(AC9/AC9a/AC9b/
AC10/AC12/AC13/AC13a/AC13b/AC14/AC15/AC16/AC17/AC19)に触れるファイルを再確認し、
回帰は無い。新規のCritical/Majorは見つからなかった。

## 指摘5点の確認結果

### 1. Critical: 最終schedule diff直後の合算capチェック — **解消**

新規ファイル`roles/semaphore_templates/tasks/schedules_update_cap_final.yml`が、
`roles/semaphore_templates/tasks/schedules_diff.yml:60`から
`ansible.builtin.import_tasks`で無条件(`when`無し)にimportされている。import位置は
`semaphore_schedules_diff_result`確定(`schedules_diff.yml:53-58`)の直後、かつ
`schedules_canonical_check.yml`/`schedules_apply.yml`(main.yml内、schedule書き込み
本体)より前であることをmain.ymlのtask順(`schedules_read.yml`→
`schedules_timezone_check.yml`→`schedules_preflight.yml`→`schedules_friday.yml`→
`schedules_diff.yml`→`schedules_canonical_check.yml`→`schedules_apply.yml`)で確認
した。`schedules_diff.yml`自体がcheck modeの有無に関わらず常時importされる既存
task(R7)であるため、この再チェックも同様に到達性が保証される。

assert本体(`schedules_update_cap_final.yml:3-5`)は
`(semaphore_templates_diff.changed | length) + (semaphore_schedules_diff_result.changed | length) <= (semaphore_reconcile_max_updates | int)`
で、template側は元から不変(apply前後で再計算しない)、schedule側は
まさにschedules_apply.ymlが使う最終値そのものを見ている。

task-flowシナリオI(`roles/semaphore_templates/tests/task_flow/final_update_cap.yml`
+`run_task_flow_tests.py`の`scenario_i`)は、計画時点4件(cap内)→最終diff5件、という
申告どおりの状況を実際に`ansible-playbook`で実行し、非ゼロ終了と該当メッセージの
出力を確認する構成になっている。`scenario_i()`単体を実行し`[]`(問題無し)を確認、
`run_task_flow_tests.py`全体も「all nine scenarios passed」で通過した(自分で実行し
確認済み、実ホスト・API接続なし)。

### 2. Major: `semaphore.md`のEXEC-052注記 — **解消**

`docs/ai/context/system/semaphore.md`の「ansy の Semaphore」節に1文
(「**EXEC-052の『ansyからホストへ到達できない』という根拠から、『Semaphore
scheduleが発火しない』とは導けない。**schedulerは鍵の有無と独立してscheduleを
起動し、taskは実行開始後にSSH接続で失敗しうる。2026-09-16にはactiveなansy
scheduleが実際に発火し、ホストへ到達しないまま本番Slackへ通知したIncidentが
ある。」)が追加されている。requirement P0-15最終段落が要求した内容(根拠と
帰結の分離、2026-09-16 Incidentへの言及)と一致する。

### 3. token-scrub済み診断文の復元 — **解消、再露出なし**

- `roles/semaphore_templates/tasks/schedules_report.yml:138`: `failed_msg`が
  `semaphore_schedules_run_failed_msg`(scrub済み)を復元(失敗時のみ、それ以外は
  空文字)。
- `roles/semaphore_templates/tasks/main.yml`の2つの再送出fail(schedule側
  `251-257行`付近、template側`310-313行`付近)が、`semaphore_schedules_run_failed_msg`
  /`_semaphore_templates_scrubbed_failure`(いずれもscrub済み変数)を`scrubbed
  failure detail=`として本文へ含める形に変わった。
- scrub機構自体(`regex_replace((semaphore_templates_token | regex_escape), '***REDACTED-TOKEN***')`、
  token非空時のみ適用)は`main.yml`のrescue内(`195-220行`付近)で不変であることを
  確認した。復元された値はすべてこのscrub済み変数を経由しており、
  `ansible_failed_result.msg`を直接読み直す経路は追加されていない。P0-10/AC14の
  「生のtoken・生のAPI失敗文を出さない」は維持されている。

### 4. URL欠損時のcanonical判定をfail-closedへ — **解消**

`filter_plugins/semaphore_schedules.py`の`_is_canonical_api_base_url`が
`if not isinstance(api_base_url, str) or not api_base_url: return False`へ変更され、
`api_base_url`が`None`・空文字・非文字列のときは**非canonical**(より安全側)へ倒れる
ようになった(round1では`None`のときcanonical=Trueへフォールバックしていた)。
`scripts/tests/semaphore_schedules/test_desired_and_diff.py`に
`test_empty_connection_url_does_not_fall_back_to_canonical`が追加され、空文字列
URLで`active`がfalseへ倒れることを確認している。`semaphore_schedules_diff`の全
呼び出し元(`schedules_diff.yml:58`、`schedules_plan.yml:72`)は引き続き実URLを
明示的に渡しており、この経路が実際に到達するのはtest専用ケースのみである。

### 5. PUT直前のfreeze + 非canonical時のassert — **解消、実payloadを検証**

`roles/semaphore_templates/tasks/schedules_apply_item.yml:40-45`で
`_semaphore_schedules_apply_put_payload`を`semaphore_schedules_payload(fresh.json,
effective_after)`として確定し、PUTのbody(`87行`: `body:
"{{ _semaphore_schedules_apply_put_payload }}"`)は**この変数をそのまま**使う(別途
再計算しない)。非canonical時のassert(`47-57行`)は`_semaphore_schedules_apply_put_payload.active
is false`を見ており、まさに送信されるdictそのものを検証している。round1で
指摘した「到達不能なdead code guard」(旧`_semaphore_schedules_apply_would_activate`
方式)はこの差し替えで消えている。

## 前回のSuggestion/Dead codeの状態

- **Suggestion(過剰な抑制)**: 3で解消(上記)。
- **Suggestion(fail-open default)**: 4で解消(上記)。
- **Dead code**(非canonical false→trueガードの到達不能性): 5の書き換えで
  当該taskごと削除され、実際に機能するassertへ置き換わった。解消。

## AC回帰チェック(AC11以外、round1で「コード上は成立」と判定した項目)

- AC9/AC9a/AC9b: `semaphore_schedules_diff`のcanonical分岐(`filter_plugins/
  semaphore_schedules.py`の`desired['active'] = before['active']`)は本ラウンドで
  変更されていない。差分は`_is_canonical_api_base_url`のNone/空文字列時の戻り値
  のみで、実運用の呼び出し(実URL文字列)には影響しない。回帰なし。
- AC10: `semaphore_schedules_friday_paused`とその呼び出し(`schedules_friday.yml`)
  は本ラウンドで無変更。回帰なし。
- AC12/AC13/AC13a/AC13b: `reconcile_finalize.yml`は本ラウンドで無変更(既存
  ファイルとの差分ゼロを確認)。main.ymlの再送出メッセージ文言変更は
  `_semaphore_reconcile_failed`等の判定ロジックに触れていない。回帰なし。
- AC14: 3の変更はP0-10の要求(生値を出さない)を保ったままdiagnostic文言を
  戻しただけであり、scrub機構自体は無変更。回帰なし。
- AC15/AC16/AC17: `filter_plugins/semaphore_templates.py`、
  `roles/deployment_drift_check/tasks/semaphore_probe.yml`・`report.yml`は
  diff --statの増減行数が前回と完全一致しており、無変更を確認した。回帰なし。
- AC19: 非canonical時の6ケース(a〜f)を支える`semaphore_schedules_desired`/
  `semaphore_schedules_existing_write_desired`/reserved-name guardは、4の
  fail-closed化以外は前回と同じロジック。`test_canonical_check.py`・
  `canonical_override_guard.yml`・`scenario_h`は今回も通過を確認した。回帰なし。

## 検証根拠(自分で実行したもの)

- `python3 scripts/tests/semaphore_schedules/run-tests.py` → 131 tests, OK(自分で実行)。
- `python3 roles/semaphore_templates/tests/task_flow/run_task_flow_tests.py` →
  「all nine scenarios passed」(自分で実行)。
- `scenario_i()`単体呼び出し → `[]`(自分で実行)。
- `ansible-playbook --syntax-check playbooks/semaphore_templates_setup.yml` → passed(自分で実行)。
- `ansible-lint --profile min`(対象4ファイル限定)→ production profile相当で
  0 failure/0 warning(自分で実行)。
- 実ホスト・Semaphore API・ansy Semaphoreへは接続していない。対象実装ファイルは
  変更していない。

## Verdict

Approve — 前回のCritical・Majorはいずれも現物で解消を確認した。新規の
Critical/Majorは無い。
