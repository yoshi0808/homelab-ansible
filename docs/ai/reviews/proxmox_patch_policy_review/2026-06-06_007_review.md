# Review

## 判定

軽微な修正が必要です。

`2026-06-06_005_review.md` の指摘事項はすべて反映され、destination node healthcheck、local disk migration、タグなし VM / CT、ノード単位 restore、weekly_full の実行元制限は実装と整合しました。

ただし、文書全体には次の不一致が残っています。

## 指摘事項

### 1. [中] weekly_full の dry-run を pve2 単独実行のように記述している

Section 11.3 と Section 17.1 は、`pve2 patch dry-run`、`pve2 dry-run status PATCH_READY` と記述しています。

- `docs/ai/prompts/proxmox_patch_policy.md:1240`
- `docs/ai/prompts/proxmox_patch_policy.md:1810`

実装は `proxmox_patch_dryrun.yml` を通常どおり import しており、Policy の Section 6.3 にも記載されているとおり、pve1 / pve2 の固定ペアを対象とする dry-run です。

- `playbooks/proxmox_patch_weekly_full.yml:106`
- `playbooks/proxmox_patch_weekly_full.yml:112`
- `docs/ai/prompts/proxmox_patch_policy.md:410`

「pve1 / pve2 の dry-run」または「クラスタ固定ペアの dry-run」と修正してください。

### 2. [中] evacuate の目的が「すべて反対側へ退避」となっている

Section 6.4 の目的は、patch 対象ノード上の VM / CT をすべて反対側ノードへ退避するとしています。

- `docs/ai/prompts/proxmox_patch_policy.md:491`

実装では、non-HA 対象は明示 migration、`hacritical` 対象は HA による退避ですが、それ以外の running VM / CT は反対側へ移動せず、対象ノード上で force stop します。

- `roles/proxmox_evacuate_node/tasks/main.yml:95`
- `roles/proxmox_evacuate_node/tasks/main.yml:210`
- `roles/proxmox_evacuate_node/tasks/main.yml:230`

目的を「patch 対象ノードを reboot 可能な状態にする。対象 VM / CT は分類に応じて migration、HA 退避、または force stop する」のように修正してください。

### 3. [中] report / mail が VM 名と home node を表示するという記述が実装と一致しない

Section 5.2 は、report / mail で VM ID、VM 名、current node、home node、action を表示するとしています。

- `docs/ai/prompts/proxmox_patch_policy.md:252`

実際の evacuation report は migrated / force-stopped の VM ID 配列と HA 数を保存し、restore report も migrated / started の VM ID 配列を保存します。weekly_full の完了メールにも個別 VM の情報はありません。

- `roles/proxmox_evacuate_node/tasks/main.yml:259`
- `roles/proxmox_restore_vm_placement/tasks/main.yml:248`
- `playbooks/proxmox_patch_weekly_full.yml:261`

コードを正本とする今回の要件では、この表示例を削除するか、「将来の推奨表示」と明示的に区別してください。

### 4. [低] `destination_node` を指定できるように見える使用例が残っている

Section 6.4 は、変数例として `target_node` と `destination_node` の両方を示しています。

- `docs/ai/prompts/proxmox_patch_policy.md:497`

実際の playbook は入力として `target_node` のみを検証し、`destination_node` は常に反対側ノードとして内部で決定します。外部から渡した `destination_node` は role 実行時の play vars で上書きされます。

- `playbooks/proxmox_evacuate_node.yml:16`
- `playbooks/proxmox_evacuate_node.yml:43`

使用例から `destination_node` を削除し、自動決定であることだけを記載してください。

### 5. [低] 要求範囲外の差分が working tree に残っている

`scripts/ai-next.sh` の変更は本タスクと無関係です。今回の implement ファイルでも別 commit とする旨が記録されています。

本タスクの commit には、Policy とレビュー工程ファイルだけを含めてください。

## 確認結果

- requirement / 前回 review / 最新 implement を確認
- `proxmox_patch_policy.md` の全差分を確認
- evacuate / restore / weekly_full の Playbook と Role を再照合
- 前回の6件の指摘が解消済みであることを確認
- 文書変更のみのため、Ansible 実行テストは未実施

## 結論

主要な運用フローと安全条件は実装に整合しました。上記の残存する表現差を修正した後、最終確認へ進めます。

Next step files:
- docs/ai/reviews/proxmox_patch_policy_review/2026-06-06_001_requirement.md
- docs/ai/reviews/proxmox_patch_policy_review/2026-06-06_007_review.md
- docs/ai/reviews/proxmox_patch_policy_review/2026-06-06_008_implement.md
