# Host variables for quory

# quory is both a managed host (control_nodes group) and, in production, the
# Ansible/Semaphore execution host itself. When a playbook targets quory and
# is also being run FROM quory (e.g. via Semaphore running natively on quory),
# connecting to "quory.internal" over SSH as ann is unnecessary and may fail
# if ann's authorized_keys / the controller's own SSH key setup doesn't cover
# this self-connection case. Fall back to a local connection in that case;
# otherwise (e.g. run from ansy) use the normal ann-key SSH connection.
# Same controller-identity pattern as playbooks/proxmox_patch_weekly_full.yml's
# preflight check (lookup('pipe', 'hostname -s')).
ansible_connection: "{{ 'local' if lookup('pipe', 'hostname -s') == 'quory' else 'ssh' }}"

# recovery_probe (roles/recovery_probe/defaults/main.yml):
# quory は recovery-probe を常駐させる本番ホストである、という事実をここに置く。
# 以前はこの真実がCLIの `-e recovery_probe_service_enabled=true` にしか無く、
# 渡し忘れると「ファイルだけ配備され daemon は旧コードのまま」という、
# sha256sum でも git diff でも見えない状態になった(2026-07-29 実測。
# docs/ai/reviews/recovery_probe_external_check/2026-07-29_002_test_result_deployed.md)。
# dev(ansy)は role default の false のままで、配置のみ・起動しない。
recovery_probe_service_enabled: true

# worktree_sync (roles/worktree_sync/defaults/main.yml):
# quory はこの仕組みの唯一の配備先であり、timerを実際に動かす本番ホストである。
# recovery_probe_service_enabled と同じ理由でhost_varsへ真実を置く
# (-eの渡し忘れがsha256sumやgit diffでは見えない「配備だけされてtimerは
# 起動していない」状態を作るため)。
worktree_sync_service_enabled: true

# incident_investigate (roles/incident_investigate/defaults/main.yml):
# quory はこのpickerのtimerを実際に動かす唯一の本番ホストである。
# recovery_probe_service_enabled / worktree_sync_service_enabled と同じ理由で
# host_varsへ真実を置く(-eの渡し忘れが「timerがdisabledになっている」という
# ドリフトに対し、deployment_drift_checkのplaybook案内(-lのみ、-eなし)を
# 流しても直らない状態を作るため。
# docs/ai/reviews/deploy_awareness/2026-08-04_001_requirement.md R2)。
incident_investigate_service_enabled: true

# recovery_io (roles/recovery_io/defaults/main.yml):
# quory は recovery-io(Slack I/Oブリッジ)を常駐させる本番ホストである。
# recovery_probe_service_enabled と同じ理由でhost_varsへ真実を置く
# (roles/recovery_io/tasks/main.yml の enable task は
# `enabled: "{{ recovery_io_service_enabled }}"` / `state: "{{ 'started' if
# ... else omit }}"` で、role既定falseのままだと `-l quory` だけでは
# enabled/activeへ収束しない)。
# 期待状態がtrueであることの根拠: 2026-08-04 00:40 の日次ドリフト検査で
# quoryのunit 5件すべてが期待どおりに揃っており(drift_count=1、該当はfile
# カテゴリの1件のみ)、recovery-io.serviceは現に enabled/active だった。
# docs/ai/reviews/deploy_awareness/2026-08-04_001_requirement.md R10。
recovery_io_service_enabled: true

# incident_capture (roles/incident_capture/defaults/main.yml):
# quory はこのcollectorのtimerを実際に動かす唯一の本番ホストである。
# incident_investigate_service_enabled と同じ理由・同じ流儀でhost_varsへ
# 真実を置く。期待状態がtrueであることの根拠は recovery_io_service_enabled と
# 同じ2026-08-04 00:40の日次ドリフト検査(該当unitも期待どおりだった)。
# docs/ai/reviews/deploy_awareness/2026-08-04_001_requirement.md R10。
incident_capture_service_enabled: true

# ubuntu_vm_full_upgrade (roles/ubuntu_vm_full_upgrade/defaults/main.yml):
# how Ansible itself is installed here, so the role's important-component
# report/Slack output can say "apt管理: 判定対象" instead of leaving it
# ambiguous ("該当なし" vs "not yet confirmed"). Confirmed 2026-07-11 by
# tester (Phase A): ansible-core is dpkg-owned, /usr/bin/ansible is owned by
# the ansible-core package. See
# docs/ai/reviews/ubuntu_vm_full_upgrade/2026-07-11_009_test_result.md.
ubuntu_vm_full_upgrade_ansible_managed_by: apt
